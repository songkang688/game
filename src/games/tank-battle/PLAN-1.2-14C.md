# 14-C · `tank-battle`「铁皮坦克大战」1.2 升级计划

窗口 3 / 格号 14-C。基线分支 `origin/game-1.2-window3`,只往这条线推。
只改 `src/games/tank-battle/**`,不碰 `shoot-range`(A 档)、`sky-squad`(B 档)、`bumper-cars`,
也不碰窗口 1 的平台文件与 `src/styles.css`。

## 一、现状审查(规格第三节六问,先给结论)

1. **移动是格子对齐还是自由角度?炮口与移动是否解耦?**
   坐标是连续浮点(`Tank.x/y`),但方向只有上右下左四个轴向(`Dir` 0..3),
   横走时 `moveTank` 会把 y 往车道中线拉(`lane = floor(y)+0.5`),所以是「轴向自由滑动 + 车道吸附」,
   不是整格步进,也没有任意角度。**炮口与移动完全耦合**:`moveTank` 第一行就是 `t.dir = dir`,
   没有单独的炮塔朝向,想只转炮口不挪车做不到。

2. **弹是直线还是可反弹?反弹次数是常量吗?**
   只有直线。`advanceBullet` 每步按 `DX[b.dir] / DY[b.dir]` 推进,撞砖 / 钢 / 堡垒 / 出界一律消失,
   **没有反弹**,自然也没有反弹次数常量;`Bullet` 结构里连速度向量都没有,只有 `dir + speed`。

3. **地图元素有几种?可破坏粒度?**
   五种 tile:`.` 空地、`#` 砖、`S` 钢、`~` 水、`*` 草,外加 `B` 堡垒(`1/2/e` 是出生点标记)。
   **没有冰**。砖的破坏是**整块**:`brickHp = 2`,挨两发整格变空地,没有四分之一格粒度。
   钢是绝对打不动的(没有强化弹这一说)。

4. **基地 / 老巢保护规则?失守判定清晰吗?**
   规则清晰:堡垒在底边正中,开局有一层 `baseShield` 星星护罩替它挡第一发;
   护墙 `scanFort` 记下开局的砖,`fortGaps` 报缺口,玩家按 G / K 用备用砖补回去。
   敌方炮弹打到 `B` 且护罩已碎 → `status="lose"`,`reason="星星堡垒被砸中啦"`。判定单点、可测,没问题。

5. **`coop` 与 `versus` 真能两人同屏打到结算吗?**
   能。`coop` 是选关地图上的「双人合作」开关,`players=2` 时 1 号位出场,两套键位(WASD+F/G、方向键+L/K),
   `sim.test.ts` 已经跑通 188 关双人;`versus` 有独立入口、左右对称地图、先弹飞对方 3 次或时间到判胜,
   `mountVersus` 有结算浮层。**缺的是「可选 AI」**:对战只能两个人玩,一个人来了没有陪练。

6. **AI 会不会卡在墙角?有没有寻路?**
   有寻路,但不是 A\*:`distanceField` 是一张带权 Dijkstra 距离场(砖按 `brickCost` 计价),
   `stepDownField` 顺着往下走。卡墙角靠两个补丁兜:撞不动就 `stuck += dt` 并把重算间隔压到 0.05 秒,
   `stuck > 0.7` 秒才允许拆挡路的砖。实测不会长时间卡死,但**没有强度分档**——
   四种车只有速度 / 装甲 / 冷却和「偷家倾向」的差别,行为模板是同一套,也没有固定 seed 的胜率断言。

## 二、这一版要干的事

| # | 施工项 | 落点 |
| --- | --- | --- |
| 1 | 地形五件套纯函数:砖(四分之一格)、钢(需强化弹)、水(弹过车不过)、草(半透明遮挡)、冰(滑行惯性) | 新增 `terrain12.ts` + 用例 |
| 2 | 砖块四分之一格破坏:每格四个小块独立耐久,打穿一角就能钻出射击缝 | `terrain12.ts` 的 `BrickQuarters` + `logic.ts` 结算 |
| 3 | 冰面滑行惯性:松手后按摩擦系数继续滑,纯函数 `slideOnIce` | `terrain12.ts` + `logic.ts` |
| 4 | 弹道三种:直线弹 / 弹力球(反射 1–2 次,反射公式单测)/ 彩纸穿甲弹(破钢) | 新增 `ballistics12.ts` + 用例 |
| 5 | 弹力球预测虚线:只画第一段反射,短 | `ballistics12.ts` 的 `previewPath` + `index.ts` 渲染 |
| 6 | AI 寻路换成网格 A\*(带墙角惩罚),三档:乱转 / 追人 / 绕后卡位;固定 seed 胜率断言 | 新增 `ai12.ts` + 用例 |
| 7 | 3 张对战专用对称地图(二维字符表,对称性写断言)+ 冰面出现在这里和无尽场 | 新增 `maps12.ts` + 用例 |
| 8 | `versus` 补「可选 AI 陪练」(三档强度可选),一个人也能对战到结算 | `index.ts` + `ai12.ts` |
| 9 | 无尽「守老巢」:波次无限(已有),补冰面地图与 `recordEndlessBest("tank-battle", wave)` 复核 | `index.ts` |
| 10 | 手感:炮口短前摇 + 后坐 4–6px;被击中零件飞散,3 秒后在出生点组装回来 | `logic.ts`(`REBUILD_SECONDS`)+ `index.ts` |
| 11 | CSS 前缀 `tb-` → `tkb-`,局部 style,不改 `src/styles.css` | `index.ts` |
| 12 | 手机 360px:摇杆与开火钮不重叠、热区 ≥ 44px,双人同屏左右各一套;小地图默认折叠 | `index.ts` |
| 13 | 平台接线:`openCampaignLevel(n)` / `initialLevel` / `?level=` 直达,Skip 走 `requestSkip` | `index.ts` |
| 14 | `destroy` 清 rAF / 定时器 / 两套键位 / 监听 / 朗读 / 状态 | `index.ts` + `runtime12.test.ts` |
| 15 | 新增 ≥ 20 个用例;**前 99 关地图一格不改**(冰只进对战与无尽场) | 五个新测试文件 |

## 三、边界

- 分级红线:**铁皮玩具坦克**,发射的是弹力球 / 彩纸弹。没有战争写实、没有碾压、没有爆炸伤人、没有死亡。
  被击中 = 零件散一地,3 秒后修好回场。失败文案只给方法,不评价人。不出现任何同类经典商业作品名。
- 保持 2D 俯视,只允许坦克有一点点斜投影厚度。
- 音效只走 `api.play`;不自建 `AudioContext`,不用 `setInterval`。
- 188 关战役地图由 `levels.ts` 的确定性生成器产出,本次**不动生成器**,
  所以前 99 关(以及后 89 关)的地图快照与既有 `levels.test.ts` 全部保持原样。

## 四、施工对账(收工时逐条回填)

十五条全部落地,一条没删也没挪:

| # | 落点 | 结果 |
| --- | --- | --- |
| 1 | `terrain12.ts` | 五种地形各一个纯函数(`canDrive` / `blocksBullet` / `isSeeThrough` / `friction` / `needsPierce`),`TILE_CHARS` 把字符表和判定绑在一起 |
| 2 | `terrain12.ts` + `logic.ts` | 砖分四小块(`BrickQuarters`),从哪边打就先掉哪半边,打穿一角就能钻出射击缝,四块全掉才整格变空地 |
| 3 | `terrain12.ts` + `logic.ts` | `slideOnIce` 按摩擦系数续滑;`driveTank` 在冰面上松手也停不住 |
| 4 | `ballistics12.ts` | 三种弹丸:直线弹 / 弹力球(`reflect` 反射公式,最多弹 2 次)/ 彩纸穿甲弹(连穿两层拆钢板) |
| 5 | `ballistics12.ts` + `index.ts` | `previewPath` 只算到第一次反射,画成一小段虚线 |
| 6 | `ai12.ts` | 网格 A\*(`findPath`,墙角与死胡同加过路费),三档 `wander` / `chase` / `flank`,固定 seed 战绩排序写成断言 |
| 7 | `maps12.ts` | 三张对战图(镜面冰场 / 双层糖果盒 / 转盘广场)+ 冰原老巢,对称性(镜像 / 旋转 180°)全写断言 |
| 8 | `index.ts` | 对战入口多一排「两个人 / 陪练·乱转 / 陪练·追人 / 陪练·绕后卡位」,一个人也能打到结算 |
| 9 | `index.ts` | 无尽两张场地可挑,成绩走 `save.recordEndlessBest("tank-battle", wave)` |
| 10 | `logic.ts` + `index.ts` | 炮口前摇 `WINDUP` + 后坐 5px;被打中零件飞散,`REBUILD_SECONDS = 3` 后在出生点组装回来(出生点被堵就换一个安全的) |
| 11 | `index.ts` | 全部类名换 `tkb-` 前缀,style 挂在自己这棵树上,`src/styles.css` 一个字没动 |
| 12 | `index.ts` | 摇杆与发射钮分列不重叠,热区单人 46px / 双人 44px,双人左右各一套,小地图默认折叠在角落 |
| 13 | `index.ts` | `openCampaignLevel(n)` / `initialLevel` / `?level=` 都能直达,Skip 走平台的 `requestSkip` 家长门 |
| 14 | `index.ts` | `destroy` 之后 rAF 归零、window 监听归零、两套键位全卸、朗读停掉、再按键一声不响 |
| 15 | 六个新测试文件 | 新增 **120** 个用例;前 99 关地图指纹钉死,并断言老关里不会混进冰面与半块砖 |

### 用例分布

| 文件 | 用例 | 管什么 |
| --- | --- | --- |
| `terrain12.test.ts` | 17 | 五种地形判定、冰面滑行、四分之一格砖 |
| `ballistics12.test.ts` | 16 | 三种弹丸、反射公式(按角度验入射角=反射角)、预测虚线只画一段 |
| `ai12.test.ts` | 15 | A\* 绕开死胡同与墙角、三档 AI 固定 seed 战绩排序 |
| `maps12.test.ts` | 23 | 三张对战图对称性、冰原老巢、前 99 关地图指纹 |
| `logic12.test.ts` | 24 | 世界层结算:破砖、穿甲、反弹、前摇后坐、散架重组、护罩充能、AI 接入 |
| `runtime12.test.ts` | 25 | `tkb-` 前缀、两套摇杆热区、换弹、直达与跳关、`destroy` 归零、分级红线词表 |
| **合计** | **120** | 1.1 原有 87 条一条没删,全库 207 条 |

### 浏览器实测(playwright,375×667 与 360×720 各跑一遍)

不是截一张静图,是把主链路点完:`?level=9` 直达 → 摇杆走位 + 开火打砖(数画布上的砖色像素,
确认真掉了)→ 换弹力球看预测虚线画出来 → 跑满 6 秒过散架与组装 → 小地图开合 →
无尽入口并换到冰原场 → 对战两人同屏、两套键位一起按不打架 → 叫陪练上场打到结算面板。
`coop` 另跑一条:从选关地图进第 1 关,两人同屏各一套摇杆,真键盘一路打到「过关」面板。
每一屏都量了热区最小边(≥ 44px)、按钮两两不重叠、摇杆区不横向溢出。

这一轮浏览器实测揪出来的、单测看不见的问题,都在 `d439c13` 里修掉了:
战场高度原来是 `innerHeight - 300` 拍脑袋估的,而平台那层舞台是 `overflow:hidden`,
估多了摇杆就被裁在屏幕外面点不到;`display:flex` 把 `[hidden]` 顶掉,收起来的模式条
其实还占着地方;换场地时上一局没人收,第二次换完屏幕上冒出两套摇杆。

## 五、合流说明

`origin/game-1.2-window3` 上这一格没有第二路实现:从 1.1 收尾(`ab64a9c`)到现在,
`src/games/tank-battle/**` 的提交全是 14-C 这一条线的,rebase 到 `91c1df3` 零冲突。
1.1 留下的 `logic.test.ts` / `levels.test.ts` / `sim.test.ts` 共 87 条一条没删、没改,
`levels.ts` 与它唯一的依赖 `level99` 也一个字节没动 —— 这是「前 99 关地图不改」最硬的那道保险,
指纹用例只是再钉一层。
