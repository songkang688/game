# 1.3 第 21 步 · C 档 —— `memory-cards`「记忆翻翻乐」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 21 步 · C 档**：给 `memory-cards`「记忆翻翻乐」做只动皮肤不动骨头的美术精修。
本步三人：A `lianliankan`、B `puzzle-tiles`、C `memory-cards`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「C 档 · memory-cards 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/memory-cards/**` 里的渲染函数与 `art.ts` 图标库的**新增图标**、CSS、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 配对判定、翻牌时序（记忆窗口时长）、辅助功能 `renderAssist` 的语义、主题数据结构；`art.ts` 里**已有图标的 shape 数据**（改了等于改题目）；
- 存档 key、`meta.ts` 的 `modes/levels/category`；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，DOM/canvas 程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/memory-cards/index.ts` 与 `art.ts`，渲染入口是 `renderAll` / `renderSlot` / `paintFace` / `paintIcon` / `renderTheme` / `renderAssist` / `renderTop`，图标出自 `art.ts` 的 `drawIcon`（约 251 行起，shape 驱动的矢量绘制）。已核实的问题：

1. `drawIcon` 是**全库少有的自绘矢量图标系统**（好底子，保住并扩充），但图标只有单层填充：无渐变、无高光、无描边统一规范，自查 `Icon.shapes` 的用色确认；
2. 卡背（`paintFace` 的背面分支）预期是纯色或简单纹理，自查确认——卡背是记忆游戏出镜率最高的画面，值得一块精修；
3. 翻牌动画自查：有没有 3D `perspective` 翻转？预期是瞬间换面或简单缩放，把结论记进开工 commit；
4. 配对成功 / 失败的反馈差异自查（预期只有消失 / 翻回）；
5. `renderTheme` 主题切换对卡面的影响范围与 `renderTop` 顶栏样式自查。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **卡背设计（核心）** | 卡背统一设计：深色底 + 星月环形纹样（细线几何 + 中心小徽章「朵朵星星」双星图案）+ 四角圆花角饰 + 1.5px 金色描边；主题色只换底色相，纹样结构不变——任何角度扫一眼都知道「这是记忆卡」。 |
| **3D 翻转** | 翻牌改 CSS 3D：`perspective(600px)` + `rotateY` 180° 双面（背面 `backface-visibility: hidden`），180ms 缓动；翻开瞬间卡面轻微上抬 2px；reduced 时直接淡入换面。**记忆窗口时长与判定时序一律不动。** |
| **图标精修** | 在 `drawIcon` 体系上加统一后处理：每个图标加 1.5px 深色描边层 + 左上高光小斑 + 底部软影；**新增**至少 4 个新图标（丰富主题，shape 数据只增不改）；图标绘制尺寸与卡面留白比统一为 64%。 |
| **配对反馈** | 配对成功：两卡相互轻碰（各向对方位移 3px 回弹）+ 之间冒出小爱心与星屑 → 双卡定格为亮色收纳态（不消失的设计若已有则保住）；配对失败：两卡轻轻摇头 ±3° 再翻回（时序不变），无惩罚色。 |
| **记忆阶段仪式感** | 开局记忆窗口内全场卡面朝上时，边框呼吸微光提示「快记住」；窗口结束集体翻回加波浪时差（每卡 30ms 交错，总时长不超过既有窗口收尾，reduced 同步翻回）。 |
| **布局与 HUD** | `renderTop` 卡片化（回合 / 配对数 / 主题）；`renderAssist` 辅助提示保住语义，视觉统一为柔光样式。 |

- 色板走 `src/art/kit/palette.ts`；描边 / 星屑走 kit 公共件。
- `prefers-reduced-motion`：翻转 3D、波浪时差、爱心星屑全停，保留静态层次。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**（A 档若落了 `icons.ts`，观感规范要对齐但本款继续用自家 `art.ts`）。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`、`outline.ts`、`sparkle.ts`。
- 你需要而 kit 没有的能力：**新增**文件并配套单测，不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看记忆翻牌页面，**只学两件事：画面密度（桌面氛围）与卡背纹样（一眼认出是本作的卡）**。
- 禁止抄任何商标与官方素材：卡背纹样与图标全部原创，不准用扑克厂牌 / 塔罗 / IP 图案。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：**卡片点击热区一个像素都不动**；
- 小卡（低于 48px）省略角饰只留纹样中心徽章；
- 顶栏一行放得下，字号 ≥ 14px；3D 翻转在低端机不掉帧（只用 transform / opacity）。

## 八、独占文件与冲突

只许改 `src/games/memory-cards/**` 与新增 `src/art/kit/` 文件。不要碰 `lianliankan` / `puzzle-tiles`。CSS 前缀沿用本款既有前缀；`destroy` 清干净动画计时。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 卡背生成函数按主题输出不同底色但同一纹样结构（断言）；
- 既有图标 shape 数据未被修改（快照或哈希断言，钉死）；
- 新增图标 ≥ 4 个且与既有图标不重名；
- 翻转动画只用 transform、记忆窗口时长不变（常量断言）；
- 配对成功 / 失败走不同视觉分支（类名断言）；reduced 下 3D / 波浪不启用；
- `destroy` 归零；既有配对逻辑测试断言一个不许改。

## 十、分级红线与回复

配对失败只是摇头无惩罚；失败只鼓励；无商标。完成后回复：你是 1.3 第 21 步 C 档、`memory-cards`；第三节的现状结论；卡背设计与 3D 翻转的实现说明；新增图标数与用例数、`npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
