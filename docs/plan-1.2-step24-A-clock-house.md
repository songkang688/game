# 1.2 第 24 步 · A 档 —— `clock-house`「时钟小屋」六年级难度与攻略不泄题

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 24 步（共 29 步）· A 档**：升级已有游戏 `clock-house`「时钟小屋」。
本步是**学习三连（一）**：A `clock-house`、B `math-farm`、C `pinyin-train`。别人的目录你一个字都不许碰。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「A 档 + 本款升级计划」的 commit**。
- 只在 `game-1.2` 线上干活；收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**

## 二、开工前必须认的基线

- `origin/game-1.1` 收尾后是 **55 款**；本款若落后就先对齐 1.1，续写不推翻。
- 本款在 1.1 的事实：`category: "edu"`、`modes: ["campaign"]`、`levels: 188`；目录 `logic.ts` `levels.ts` `index.ts` `meta.ts` `guide.ts` + 测试。
- 学习类共用资产：答题壳 `src/games/quiz99.ts`、朗读 `src/games/speech.ts`（**只读，不许改**）、188 框架 `src/games/level99.ts`、攻略契约 `src/ui/level188Contract.ts`。
- 1.1 三人组验过学习类的「悄悄提示」与「结算朗读」链路（连错 2 次闪烁提示 + 朗读同句）。**保持这套体验，不许改坏。**
- **攻略红线**：`guide.ts` 只讲方法与检查步骤，**严禁写出题目答案**。

## 三、现状审查（回复里逐条回答）

1. 题型有几类（读钟面 / 拨指针 / 经过时间 / 时刻换算 / 24 小时制 / 时区或作息表）？
2. 出题是生成的还是题库？会不会出现重复题或无解题？
3. 难度是怎么随 188 关上升的？到第 188 关是不是真的到六年级水平（分秒换算、经过时间跨小时、24 小时制）？
4. 拨指针交互：分针与时针联动吗（拨分针时针要跟着走一点，这是常见错误点）？
5. 朗读用的是不是共用 `speech.ts`？关掉语音时体验是否完整？
6. `guide.ts` 有没有泄题？

## 四、1.2 内容与玩法升级

| 项 | 规格 |
| --- | --- |
| **题型补齐到 8 类** | 读钟面（整点 / 半点 / 五分 / 一分）、拨指针、经过时间（含跨小时与跨中午）、时刻加减、12↔24 小时换算、时间单位换算（时分秒）、作息表读取、简单时区（只做「北京时间 + N」这种直观题）。 |
| **联动时针** | 拨分针时，时针按比例移动（写纯函数 + 单测）；这是教学正确性问题，必须做对。 |
| **难度曲线** | 用一张「关号 → 题型权重」表驱动，1–30 入门 … 161–188 六年级综合；写断言：每章题型权重之和为 1，且高段必含经过时间与 24 小时制。 |
| **错题回顾** | 每关结束把错题以「换个数字的同类题」再出一次（不给原答案）；错题类型统计存本地（key 走 `yiduo-yixing.` 前缀）。 |
| **悄悄提示** | 连错 2 次给方法提示（例如「先看时针在哪两个数字之间」），**不给答案**，并朗读同句。 |

## 五、模式矩阵

`campaign` 188 保留（**前 99 关题型参数不改**）；对战 / 无尽**不做**（学习类应保持专注，写明理由）。

## 六、2.5D / 3D 决策

**保持 2D**。钟面必须清晰、刻度准确。

## 七、视觉 · 建模 · 手感

- 钟面刻度、数字、指针长度比例要正确（时针短粗、分针长细、秒针细红）。
- 拨动指针有磁性吸附到最近分钟（可选精确模式）。
- 答对：星星 + `api.play("win")`；答错：温和提示，不打叉不批评。
- `prefers-reduced-motion`：关指针弹跳。

## 八、手机 360px

- 钟面直径 ≥ 200px 或按可用高度自适应；选项按钮 ≥ 44px 且不与钟面重叠。
- 题干字号 ≥ 16px，可换行不溢出。

## 九、平台接线

- 走 `mountLevelGame` / `quiz99` 的 skip 与 `initialLevel` 直开第 N 关要实测。
- `meta.platform` 建议 `"both"`；`blurb` 与事实对齐。

## 十、独占文件与冲突

只许改 `src/games/clock-house/**`。**不许改 `quiz99.ts` / `speech.ts` / `level99.ts`**（公共资产，只读）。不要碰 `math-farm` / `pinyin-train`。CSS 类名 `clk-` 前缀。

## 十一、测试（新增 ≥ 18 个用例）

8 类题型生成与判分、时针联动比例、经过时间跨小时 / 跨中午、12↔24 换算、难度权重表断言、错题回顾生成同类题、提示不含答案（**扫描提示文案不出现正确答案字符串**）、`destroy` 归零。

## 十二、分级红线与回复

学习内容必须正确（时间换算不能错）；失败只鼓励；攻略不泄题；无商标。完成后回复：你是 A 档、`clock-house`；题型清单与难度表；时针联动实现；错题回顾；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
