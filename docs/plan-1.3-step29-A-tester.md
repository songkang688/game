# 1.3 第 29 步 · A 档 —— 视觉验收三人组 **第 3 轮（终验）** · 测试员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉**，不重开玩法验收，也不改任何玩法代码。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 29 步 · A 档 —— 视觉验收三人组第 3 轮（共 3 轮，终验）的测试员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「A 档 · 第 3 轮终验测试员 · 终验计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round3-tester.md`。

## 二、本轮定位：终验，出「放行 / 不放行」结论

前两轮分别做了全库基线扫描与复验深挖，本轮是 1.3 视觉升级的**最后一轮验收**，只回答一个问题：**这套视觉能不能放行**。做三件事：

1. **对账清零**：把第 2 轮 `docs/qa/1.3-round2-tester.md` 的新问题与 `docs/qa/1.3-round2-fixer.md` 的修复、遗留清单逐条对账，阻断级 / 严重级必须为 0 或有 C 档书面降级理由。
2. **全库终扫**：39 款全部过一遍六大专项（见第四节），不抽样，每款每项给「达标 / 不达标」的二值结论。
3. **终验结论**：给出「放行 / 带条件放行（列条件）/ 不放行（列理由）」三选一的正式结论。

先跑 `npm ci` → `npm test` → `npm run build`，记录用例总数与第 2 轮合并态差值（只增不减）。

## 三、两轮遗留对账（本轮前置）

- 第 2 轮报告的每条问题给结论：**已修复（附证据）/ 合理降级（引 C 档理由）/ 仍未修（升级为终验阻断项）**；
- 前两轮沉淀的机器化扫描用例（emoji 直出断言、热区钉死断言、教育数据快照）必须全部还在跑、全绿；被删或被跳过（skip）的用例按阻断处理。

## 四、六大专项全库终扫（39 款 × 6 项，逐款打勾）

| 专项 | 终验标准 |
| --- | --- |
| 火柴人 / 草稿圆扫描 | 任何主角、NPC、敌人不得是无渐变无描边的裸 `arc` 圆或线段火柴人；grep 各款绘制文件里孤立的 `arc(...)`+单色 `fill()` 组合并肉眼确认。 |
| 金币 / 道具体积 | 金币与核心拾取物必须有高光 + 侧缘暗面 + 落影三件套，缩到 16px 仍可辨；平涂黄圆判不达标。 |
| 双人角色可区分 | 双人款（ice-fire-forest / puff-bros / prince-princess / red-blue 三连等）灰度截图下仍能靠剪影分清两名角色，缺一不可。 |
| 跑酷 / 竞速 2.5D 纵深 | sky-squad / tank-battle / bumper-cars / bowling-lane / red-blue-race / brave-path 等必须有地面透视或多层视差；bowling-lane 球道透视是点名项。 |
| 360px 布局 | 360×640 视口下无横向滚动、无 HUD 重叠、无文字截断；逐款过首页入口 / 局内 / 结算三屏。 |
| 商标扫描 | 全库无任何商标、官方角色及高相似度造型；对照 B 档两轮维护的风险清单终查一遍。 |

产出一张 **39 行 × 6 列的终验总表**，不达标格写明位置与现象。

## 五、回归防线（终验加严）

- 热区钉死断言、reduced 功能提示断言、教育数据（笔顺 / 音高 / 题面）快照断言全绿是放行前提；
- 抽 6 款（点名整改重点里挑）在 CPU 4× 节流下复测粒子高峰帧率 ≥ 30fps；
- `npx vite preview` 冒烟：首页 / `sw.js` / `manifest.webmanifest` 均 200，随机进 5 款各玩 30 秒无视觉崩坏。

## 六、报告要求

产出 `docs/qa/1.3-round3-tester.md`：

- 第一节：全量跑数据 + 三轮用例数曲线（只增不减）；
- 第二节：第 2 轮问题终验对账表（三种结论）；
- 第三节：39 款 × 6 专项终验总表；
- 第四节：终验新发现问题（如有，量化描述，交本轮 C 档当轮清零）；
- 第五节：**终验结论**——放行 / 带条件放行 / 不放行，附依据。

## 七、你的权限边界

**测试员只写报告与补测试用例，不改绘制代码。** 你能改的文件限于 `*.test.ts` 与 `docs/qa/1.3-round3-tester.md`。终验新发现的问题交本轮 C 档当轮修完，你复验后更新结论。

## 八、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；`vite preview` 冒烟全绿；
- 阻断级 / 严重级问题终态为 0（或有书面降级理由且不属于六大专项）；
- 终验总表 39 款全覆盖；终验结论明确三选一；
- 三方合并态复验全绿，合并态 SHA 写进报告（由 C 档执行，你要配合）。

## 九、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 29 步 A 档第 3 轮终验测试员；第 2 轮遗留对账统计；六大专项终验达标率（39 款 × 6 项）；终验新问题数与清零情况；**终验结论（三选一）**；报告路径；`npm test` / `npm run build` / `vite preview` 结果；提交 SHA；**实际使用的模型 slug**。
