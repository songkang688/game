# 1.2 第 28 步 · C 档 —— 验收三人组 **第 2 轮** · 监督修复员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 28 步（共 29 步）· C 档 —— 验收三人组第 2 轮（共 3 轮）的监督修复员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。**你是这一轮的收口人。**

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「C 档 · 第 2 轮监督修复员 · 修复计划」的 commit**。
- 收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.2-round2-fixer.md`。
- **开工先读第 1 轮三份报告 + 本轮 A / B 的报告。**

## 二、开工前必须认的基线

- 1.2 的盘子：**76 款**（21 新 + 55 升级）。起点水位 **142 个测试文件 / 3918 个用例**，**只增不减**。
- 存档 key 前缀 `yiduo-yixing.`；公共 key：`save.v1`、`l99.<id>`、`l99skip.<id>`、`collection.v1`、`fav.v1`、`recent.v1`。**只增不改。**

## 三、修复职责

1. 修掉第 2 轮 A 报告的**全部阻断与严重问题**；同时**清掉第 1 轮遗留的一般级问题**（清不完的要写明理由与计划，且必须在第 3 轮清零）。
2. 每修一个问题**先写红测试再修**，再跑全量。
3. **教育正确性问题最优先**：A 本轮会人工核对学习类 6 款的题目答案。拼音声调标注、几何面积周长与展开图、字库错别字、数学解法——**这类错误一律最高优先级**，因为它教错孩子。
4. **无尽模式横评里「声明了但玩不了」的款**，要么修好要么把 `meta.modes` 改成实话，二选一，不许留在那儿骗人。

## 四、监督职责

- **复审 A 是否漏测**，本轮重点看**多游戏共用框架的回归**：`level99.ts` 188 关框架、`parentAuth.ts` 家长门与跳关、`quiz99.ts` / `speech.ts`、`collection.ts` 收藏册、`save.ts` 老存档兼容、首页筛选与计数。共用件一处坏就是几十款坏，A 抽样测不到的地方你要补测。
- **复审 B 的性能改进**：
  - 有没有破坏手感或既有断言（尤其是改了主循环、输入处理、动画时序的地方）；
  - **对 B 的量化数据独立复测一遍，数据对不上就在报告里直接指出**；
  - 有没有偷偷引入外部运行时依赖（查 `package.json` 的 `dependencies` diff）。
- **难度曲线结论的处置**：A 会给出「难度跳崖」与「后期只是数值加大」的量化结论。跳崖属于要修的问题；重复感如果范围太大修不完，要在报告里列出款名与建议，交给第 3 轮或 1.3。
- **三方合并态复验**：把 A / B / C 三人分支 rebase 到一起，跑 `npm test` + `npm run build` + `vite preview` 冒烟，记录**合并态 SHA** 与用例总数。

## 五、报告要求

产出 `docs/qa/1.2-round2-fixer.md`：

- 修了哪些（对应 A 报告编号）、怎么修的、新增了哪些回归测试；
- 第 1 轮遗留问题的处置结论（逐条：已修 / 明确关闭 + 理由）；
- 补测发现的、A 没写到的问题（重点是共用框架回归）；
- 对 B 的 diff 与量化数据的**独立复测结论**（数据对得上 / 对不上，差多少）；
- 三方合并态验证结果、SHA、用例总数；
- **本轮遗留清单**，交给第 3 轮（第 29 步）——第 3 轮是收官轮，遗留必须清零。

## 六、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；preview 冒烟全绿。
- 阻断与严重问题 0 残留；**第 1 轮遗留问题全部有结论**。
- 学习类 6 款的教育内容错误 **0 残留**。
- 声明了 `endless` 的款全部真能玩到结算并落盘成绩。
- 性能有可量化的改进记录且经你独立复测；三方合并态复验全绿并写明 SHA。
- 不改存档 key；无外部运行时依赖；**无任何商业商标或官方角色名**；文案面向约小学六年级、不过于低幼、保持中文粉彩萌系。

## 七、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告、无内购、无抽卡、无联网上报；攻略不泄题。完成后回复：你是 C 档第 2 轮监督修复员；清零了多少阻断 / 严重问题；教育内容错误修了几处；无尽模式处置结果；共用框架补测发现了什么；对 B 数据的独立复测结论；三方合并态 SHA 与用例总数；遗留清单条数；报告路径；提交 SHA；**实际使用的模型 slug**。
