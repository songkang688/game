# 1.2 第 29 步 · C 档 —— 验收三人组 **第 3 轮** · 监督修复员（清零 + 终审）

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 29 步（共 29 步）· C 档 —— 验收三人组第 3 轮的监督修复员。这是 1.2 的最后一个角色：你签完字，1.2 就算收官。**
本步三人：A 测试员、B 学习优化员、C 监督修复员。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「C 档 · 第 3 轮监督修复员 · 清零与终审计划」的 commit**。
- 收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**（是否合回 `main` 由用户自己决定。）
- 报告写到 `docs/qa/1.2-round3-fixer.md`。
- **开工先读第 1、2 轮全部六份报告 + 本轮 A / B 的报告。**

## 二、开工前必须认的基线

- 1.2 的盘子：**新游戏 21 款 + 升级 55 款 = 76 款**（第 15 步 C 档一档带 `bumper-cars` 与 `bowling-lane` 两款，因为这两款是 1.1 做完但没合进 1.2 的遗留）。
- 起点水位 **142 个测试文件 / 3918 个用例**，**只增不减**。
- 存档 key 前缀 `yiduo-yixing.`；公共 key：`save.v1`、`l99.<id>`、`l99skip.<id>`、`collection.v1`、`fav.v1`、`recent.v1`。

## 三、清零

1. 把**三轮累计的全部阻断、严重、一般级问题清零**。建议级问题要么做掉，要么在报告里写明「1.3 再做」的明确理由。
2. 每修一个先写红测试再修；跑全量。
3. 收官轮**不许做大重构**：修复要小而准，改一处就把这一处的回归测试补上。如果某个问题只能靠大改解决，写进「1.3 再做」并说明风险，别在最后一刻动刀。

## 四、终审（这是你本轮最重要的职责，逐条给结论）

| 项 | 怎么审 |
| --- | --- |
| **商标与 IP** | 全库搜一遍商标 / 官方角色名黑名单，确认 **0 命中（含注释与文档）**。1.2 新增了 21 款新游戏，很多是同类玩法的原创化，这一项风险最高——逐款看造型描述与文案。 |
| **依赖与联网** | 全库确认无外部运行时依赖（对比 `package.json` 的 `dependencies`）、无广告、无内购、无抽卡、无联网上报（搜 `fetch` / `XMLHttpRequest` / `navigator.sendBeacon` 的每一处调用并逐个说明用途）。 |
| **存档 key 清单** | 核对全库 `yiduo-yixing.*` 的每一个 key：公共 key 语义不变、只增不改；游戏私有 key 一律带 `yiduo-yixing.` 前缀且不与别款撞名。写成一个测试守住。 |
| **`meta` 与实现一致性** | 全库遍历：`modes` 声明的每种模式都真能玩、`levels` 与关卡表一致、`blurb` 不吹牛、`platform` 与实测一致。写成测试。 |
| **`destroy` 泄漏** | 全库抽查至少 20 款 ×5 次进出，监听器 / 定时器 / `requestAnimationFrame` / `AudioContext` 节点计数不增长。 |
| **分级自审** | 无血、无伤害、无死亡描写；`poop-hero` 这类题材干净不恶心；`kitty-care` 无动物受伤 / 生病恶化；对战类无「击杀」措辞；失败只鼓励。 |
| **教育正确性** | 学习类 6 款的教育内容错误必须 **0 残留**（第 2 轮已查一遍，这里确认没被改回去）。 |
| **B 的文档** | 独立核对 README 的款数与各模式列表是否与代码一致（**不要只信 B 的一致性测试，自己也遍历一遍**）；发布说明与 1.3 基线的数字是否属实。 |
| **合并态** | 三方合并态全量 `npm test` + `npm run build` + `vite preview` 冒烟 + **离线冒烟**，记录合并态 SHA 与最终用例总数。 |

## 五、报告要求

产出 `docs/qa/1.2-round3-fixer.md`：

- 清零明细：三轮累计问题的最终处置（逐条：已修 / 明确关闭 + 理由 / 1.3 再做 + 理由）；
- 终审九项逐条结论与证据；
- 三方合并态验证结果与 SHA；
- **末尾给出最终质量结论 + 1.2 的完整数据**：
  - 游戏总数（应为 76）、其中 1.2 新增几款；
  - 188 关游戏数；
  - 对战 / 无尽 / 双人 / 合作各多少款；
  - 测试文件数与用例总数（对比基线 3918）；
  - 主 chunk 体积与首屏耗时；
  - 存档 key 完整清单；
  - 留给 1.3 的条目清单。

## 六、本轮验收标准（1.2 收官线，三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；preview 冒烟与**离线冒烟**全绿。
- 三轮累计的阻断 / 严重 / 一般问题 **全部清零**。
- 1.2 全部 76 款每一款都至少被实玩走查过一次并有记录。
- 所有 `meta.modes` 声明的模式都真能玩到结算；README 与代码一致且有测试守着。
- 终审九项全部通过。
- 三方合并态复验全绿，SHA 写进报告。
- 不改存档 key；无外部运行时依赖；**无任何商业商标或官方角色名**；文案面向约小学六年级、不过于低幼、保持中文粉彩萌系。

## 七、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告、无内购、无抽卡、无联网上报；攻略不泄题。完成后回复：你是 C 档第 3 轮监督修复员；三轮累计问题的清零情况（修了多少、关闭多少、留给 1.3 多少）；终审九项各自结论；三方合并态 SHA；**1.2 的完整数据（76 款、各模式款数、用例总数、主 chunk 体积）**；最终质量结论；报告路径；提交 SHA；**实际使用的模型 slug**。
