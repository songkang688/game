# 1.2 第 12 步 · B 档 —— `candy-swing`「糖果秋千」绳物理

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 12 步（共 29 步）· B 档**：升级已有游戏 `candy-swing`「糖果秋千」。
本步是物理三连：A `sling-birds`、B `candy-swing`、C `gold-hook`。别人的目录你一个字都不许碰。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「B 档 + 本款升级计划」的 commit**。
- 只在 `game-1.2` 线上干活；收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**

## 二、开工前必须认的基线

- `origin/game-1.1` 收尾后是 **55 款**；本款若落后就先对齐 1.1，续写不推翻。
- 本款在 1.1 的事实：`category: "action"`、**`modes: ["campaign"]`**、`levels: 188`；目录 `physics.ts` `sim.ts` `levels.ts` `index.ts` `meta.ts` `guide.ts` + 测试。已经有 `sim.ts`（关卡可通关性模拟），**继续用它，不要另起一套**。
- 1.1 三人组走查确认过本款「划绳切断 → 糖果落嘴 → 独立结算面板朗读」这条主链路正常，**不要把它改坏**。
- 复用：188 框架、收藏册只读、家长门、`save.recordEndlessBest`。**禁止引入物理引擎依赖。**

## 三、现状审查（回复里逐条回答）

1. `physics.ts` 的绳段是质点弹簧还是刚性约束？迭代几次？糖果重量与摆动阻尼是常量吗？
2. 切绳的判定是「线段相交」还是「点半径」？快速划过时会不会漏切（tunneling）？
3. 有没有气泡、吸盘、传送、风扇这类机关？各自的纯函数在哪？
4. 星星（收集品）摆放是否保证「三星路线存在」？`sim.ts` 覆盖到 188 关的哪些？
5. 触屏划线与页面滚动 / 壳层手势有没有冲突？
6. 三星标准是什么，跟收集星星与用时是什么关系？

## 四、1.2 玩法升级

| 项 | 规格 |
| --- | --- |
| **切绳手感** | 划线用连续采样 + 线段相交（补 tunneling 修正：把上一帧到这一帧的手指位移当作一条线段去判交）。切断瞬间绳头要甩一下，不是直接消失。 |
| **新机关（补 2 个）** | 例如「粘性泡泡（临时挂住糖果）」与「弹簧蘑菇（改变方向）」；每个机关一个纯函数 + 至少 3 个用例。 |
| **连击与轨迹** | 一次划断多根绳给「一刀两断」奖励；糖果轨迹留 300ms 淡出残影，帮孩子理解物理。 |
| **无尽「甜甜塔」** | **补做**：随机生成机关组合，坚持吃到第几颗糖记录成绩，`save.recordEndlessBest("candy-swing", n)`。做不动就写明理由并**不要留半成品**。 |
| **关卡可解性** | 用 `sim.ts` 把 188 关（至少抽样 30 关，含 100 / 145 / 188）验一遍「存在通关解 + 存在三星解」。 |

## 五、模式矩阵

闯关 188 保留，**前 99 关数据不许改**；无尽按上表决定；对战不做（切绳不适合同屏对抗，写明理由）。

## 六、2.5D / 3D 决策

**保持 2D 侧视**。绳的形状与糖果落点必须精确可读。允许背景一层视差。

## 七、视觉 · 建模 · 手感

- 绳子用分段曲线绘制并有粗细变化；切口有小碎屑。
- 糖果落进啾啾嘴里要有「接住 + 咀嚼 + 满足」三段小演出（≤ 700ms，可跳过）。
- `prefers-reduced-motion`：残影与碎屑减少，保留结果与音效。

## 八、手机 360px

- 划线区域覆盖整个舞台，但**不能吃掉暂停 / 攻略按钮的点击**；手指划出屏幕边缘要安全结束，不要卡住输入状态。
- 关卡目标与星星数一行显示，字号 ≥ 14px。

## 九、平台接线

- 走 `mountLevelGame` 就确认 skip 与 `initialLevel` 直开第 N 关；自建地图则提供 `openCampaignLevel(n)`。
- 若历史上有不带 `yiduo-yixing.` 前缀的旧存档 key（1.1 三人组在家长面板见过 `yiduo.candy-swing.campaign.v2` 这类两代前缀），**读一次迁移，不许丢星**，并写迁移用例。
- `meta.platform` 按实测填；`blurb` 与事实对齐。

## 十、独占文件与冲突

只许改 `src/games/candy-swing/**`。不要碰 `sling-birds` / `gold-hook`。CSS 类名 `cds-` 前缀；`destroy` 清干净指针监听与 rAF。

## 十一、测试（新增 ≥ 20 个用例）

绳约束收敛、快速划线不漏切、新机关纯函数、一刀两断计数、188 关抽样可解性（含三星解）、旧 key 迁移、无尽计分（若做）、`destroy` 归零。

## 十二、分级红线与回复

无血无伤；失败只鼓励；无商标（尤其不要出现同类切绳商业作品的角色与命名）。完成后回复：你是 B 档、`candy-swing`；绳物理参数；新机关；可解性验证结果；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
