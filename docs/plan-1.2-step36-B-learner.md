# 1.2 第 36 步 / 共 37 步 · B 档 —— 学习优化员（第 2 轮）

> 验收三人组第 2 轮。仿 1.1 学习优化员：吸收优点并落地。文件所有权与 A/C 互不相交。

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你必须自己动手完成，禁止再用 Task 派生任何云端子代理。全部推 `game-1.2`，不回 `main`，禁止 force。】

仓库 https://github.com/songkang688/game ，**1.2 第 36 步**角色 **B 学习优化员**（第 2 轮）。独占 `docs/qa/1.2-round2-learner.md` 以及你在报告里**登记过的**小 diff。

## 分支纪律（先做）
- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- 开工前先提交一条 git 记录（角色 + 本款工作计划），再改代码。
- 全部工作在 `game-1.2` 线上。**不要改 main、不要合并回 main、不要用 gh 开或改或合 PR。**
- 收尾：`git fetch origin game-1.2` → `git rebase origin/game-1.2`（有冲突就解，绝不 force）→ 重跑 `npm test` 与 `npm run build` → `git push origin HEAD:game-1.2`。被拒就再 fetch+rebase。**禁止 `--force`。**
- 源码对照：先 `git fetch origin game-1.1`，以 `origin/game-1.1` @ `8867138` 的 55 款实现为 **1.1 已做完** 的事实；1.2 第 1 步平台（root / 手游端游筛选 / `playModes` / `view25d`）与 B 档 21 款接入视为已合入 `game-1.2`。不要假设 188 关、2.5D 跑酷、`collection`、`parentAuth`、`guide.ts` 还不存在。
- 模型 slug 只出现在上面四行正文：`claude-opus-5-thinking-high-fast`（无方括号）。回复里写**实际**使用的 slug。

## 现状审查
读本轮 A 尚未完成也可以先找优点；但落地前扫一眼现有代码，避免推翻 1.1 手感与升级步断言。可联网研究开源儿童合集 / Canvas / 手感 / 可测试性。

## 通行规则
改动要小而稳。不许推翻既有玩法手感与断言。不改存档 key 语义。不引入运行时依赖与 three.js。落地必须带测试。

## 职责
性能（对象池、离屏、chunk）、内容去重复、AI 会失误。至少落地 6 条，其中 ≥2 条性能并附前后数据。复核第 1 轮落地无回退。
每条优点写：「来源思路 / 我们的现状 / 落地方式 / 是否本轮落地 / 收益量化」。
与 C 撞车时：**以更彻底且有测试的一方为准**，在报告登记。本轮样本游戏与测试员主样本相同主题，但你不要重做走查报告。

## 独占
`docs/qa/1.2-round2-learner.md`。落地文件必须在该报告列出。避开 A 的 `*-tester.md` 与 C 的 `*-fixer.md`。避开测试员本轮新增的 `*.audit.round2.test.ts`。若改某游戏，选**本轮样本以外**或报告明确「C 不修同一 hunk」的文件；优先改引擎工具（对象池、tween 辅助、输入缓冲工具）的**新文件**，减少碰撞。

## 测试验收
全绿；落地条数达标；量化数据写进报告。

## 冲突
不要清 A 报告里的阻断（那是 C 的活）。不要改 root 密码实现。

完成后回复：优点条数、落地条数、文件列表、量化、**实际模型 slug**。
