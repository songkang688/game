# 1.2 第 2 步 · A 档 —— `orb-arena`「圆圆大作战」

> 短计划：独占新建 `src/games/orb-arena/`。本步另两档是 `snake-royale`、`block-drop`，一个字都别碰。
> 这是俯视 IO 吞噬竞技场（质量、分身、刺球、吐孢子、边界、排行），**不是** `ocean-munch` 侧视吃鱼。

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理；必须自己动手把活干完。只推 `game-1.2`，不要改 main、不要 force。】

仓库 https://github.com/songkang688/game ，产品「一朵一星」，这是 **1.2 版本第 2 步 / 共 7 步新游戏接入** 的 **A 档**：新建 `orb-arena`「圆圆大作战」。假设 1.1 的 55 款（含 `bowling-lane`、`fight-king`、`xiangqi`、`snake-snack`、`ocean-munch`）已全部做完。

## 分支纪律（先做）
- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- 开工前先提交一条 git 记录（角色 A + 本款工作计划）。
- 全部工作在 `game-1.2` 线上。**不要改 main、不要合并回 main、不要用 gh 开/改/合 PR。**
- 收尾：`git fetch origin game-1.2` → `git rebase origin/game-1.2`（有冲突就解，绝不 force）→ 重跑 `npm test` 与 `npm run build` → `git push origin HEAD:game-1.2`。被拒就再 fetch+rebase 重来。**禁止 `--force`。**

## 新游戏统一约定（对接更新后的 1.1）
- 目录 `src/games/orb-arena/`：`meta.ts` 纯数据（`id/title/emoji/category/color/blurb`，以及 `modes`/`levels`；**不许 import 任何玩法代码**）。首页 eager 收集这个文件。
- `index.ts` 顶部 `export { meta } from "./meta"`，导出 `mount(api): { destroy }`。玩法代码全在本目录，进入游戏才懒加载（`import.meta.glob` 自动收集）。
- **不要改 `src/ui/home.ts`。** 不要改 `src/engine/types.ts`（若第 1 步已加 `platform` 字段，按本款填 `both`；若还没有，不要抢加类型，只把端写进 meta 已有字段能容纳的部分）。
- 闯关走 `src/games/level99.ts` 的 `mountLevelGame`，章节和恒等于 **188**，`assertTotal(chapters, 188)`。存档 key 仍是 `yiduo-yixing.l99.<id>`。
- 键位：**朵朵 WASD（`W A S D`）+ `F`（分身）+ `G`（吐孢子）**；双人时星星 **方向键 + `L`/`K`**；`Esc` 暂停。手机：虚拟摇杆 + 两个大按钮（热区 ≥ 44px）。
- `destroy` 必须清干净：所有 `window/document` 监听、`setInterval/setTimeout`、`requestAnimationFrame`、AudioContext 节点。进→玩→退→再进，监听器和定时器归零。
- 音效只用 `api.play("tap"|"win"|"oops"|"coin"|"pop"|"meow"|"jump")`。**不许任何外部资源或运行时依赖。**
- **收藏养成（只读）**：可 `import { collectionEffects } from "../../engine/collection"`，用 `speedMul` / `luckMul` 等做温和手感（移速、彩豆密度），加成已被收藏层封顶 +35%。**禁止改** `src/engine/collection.ts`、`src/ui/collection.ts`、key `yiduo-yixing.collection.v1`。暂停菜单可调 `openCollection("orb-arena")`。
- 文案原创中文、约小学六年级、粉彩萌系。**禁止任何商业商标或官方角色名（含注释）**：不要写「球球大作战」「Agar」等。失败只鼓励不批评。无血无伤：被吃掉是「噗」地变成一圈小彩纸。
- 360px：HUD 字号 ≥ 13px，排行榜可折叠，摇杆不挡视野。验证 360×640、375×667、1280×800。
- 不要改 `docs/plan-1.2-supervisor.md`、`docs/plan-1.2-step1-*`、`docs/plan-1.2-step9-*` 及以后、不要改 1.1 文件、不要删 `docs/game-1.2/new-games/`。

## 你只做这一款（别人的文件一个字都别动）

### meta（必须按此落地）
```
id: "orb-arena"
title: "圆圆大作战"
emoji: "🟣"
category: "action"
color: "#E4D9FF"
blurb: "在圆形竞技场里变大、分身、躲开刺球！排行榜前十才算真正的圆圆高手。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。`ageHint` 可填 8。

### 这不是什么
- **不是** `ocean-munch`「海底大胃王」复刻：那是侧视大鱼吃小鱼闯关，没有分身、刺球、孢子、排行榜、封闭大地图。
- 本款是 **俯视 IO 竞技场**：质量、分身、刺球、吐孢子、地图边界、实时排行榜。

### 完整规则（必须写成纯函数，可单测）

坐标系：世界坐标，地图矩形 `MAP_W × MAP_H`（战役按关缩放，无尽/对战默认 4000×4000）。摄像机跟随玩家最大分身，带平滑。

#### 质量与吞噬
- 每个圆有 `mass`。半径 `r = k * sqrt(mass)`（k 抽成常量）。
- 速度 `v = v0 / (1 + mass / MASS_SPEED_REF)`，质量越大越慢。小圆永远比大圆快。收藏 `speedMul` 只乘在 `v0` 上。
- 质量随时间衰减：`mass' = mass * (1 - DECAY * dt)`，衰减在 `mass > DECAY_START` 后启用，保证必须持续进食。
- 场上刷「彩豆」：随机点，质量 `PELLET_MASS`，吃到 +质量。密度保持在 `PELLET_COUNT`（`luckMul` 可微调密度，不超过 +35%）。
- **吞噬判定**：两圆圆心距 `< |rA - rB| * OVERLAP`（建议 OVERLAP=0.75，抽常量），且较大方质量 ≥ 较小方 × `EAT_RATIO`（建议 1.25）。满足则大吃小，质量 100% 转移，小吃方消失。
- 同一玩家的多个分身：**不能互相吞噬**，除非处于「可合并窗口」（分身出生 `MERGE_DELAY` 秒之后，建议 18–30s，质量越大合并越慢）。合并时质量和并、位置插值。
- 队友（双人合作关）：同样不能误吞最后一个分身。

#### 分身（最多 16）
- 按 `F` / 触屏「分身」：把**当前最大的那个分身**沿瞄准方向弹出一半质量。弹出速度先快后阻尼。已有 16 个则不能再主动分。
- 分身瞬间若质量不足以双方都 ≥ `MIN_SPLIT_MASS`，忽略输入。
- 分身是进攻手段（拍到远处小吃）也是风险（自己变小）。

#### 吐孢子
- 按 `G` / 「吐」：从每个足够大的分身沿瞄准方向吐一颗孢子（质量 `SPIT_MASS`，从本体扣除）。孢子是可被任何人吃的小圆，飞行一小段后停下。
- 对刺球吐孢子：刺球被推动，质量增加；到阈值后**沿吐入方向弹出一颗新刺球**（进攻：把刺球推向对手让对方爆开）。

#### 刺球
- 场上维持 `VIRUS_COUNT` 个「刺球」（海胆状圆，带短刺动画）。质量固定，半径大于中型圆。
- **吃刺球**：若玩家某个分身质量 > 刺球质量，且吞噬几何满足：该分身 **爆开** 成最多 8–16 个小分身（总数仍封顶 16），并获得刺球的质量。已有 16 个分身时：只加质量、不再爆。
- 质量 ≤ 刺球：碰上去无事发生（或被轻微弹开，不要秒死）。
- 刺球不是「毒海藻鱼」，也不是 `ocean-munch` 的毒鱼。

#### 地图边界
- 圆不能出界：圆心夹在 `[r, MAP_W-r] × [r, MAP_H-r]`。
- 无尽后期可加「缩圈」：安全区矩形每 N 秒内收，出圈持续掉质量（卡通：圆圆变淡、掉彩纸），质量掉到 `MIN_MASS` 判休息。这是大逃杀边界，不是碰到边就死。
- 对战默认不缩圈。

#### 排行榜
- HUD 右上：当前场上玩家（含 AI）按 **总质量** 取 Top 10，自己那一行高亮，即使不在前十也在底部显示「第 N 名 · 质量 xxx」。
- 名字用原创：朵朵、星星、糯糯、云云、墩墩、闪闪、绿绿豆、啾啾、以及「圆圆 3 号」这类占位。**禁止网游昵称梗和商标。**

#### 胜负
- **被吃光**：所有分身消失 → 本局结束（闯关失败 / 对战淘汰 / 无尽结算）。文案：「圆圆先去休息啦，再出发一定更稳！」
- **对战计时结束**：质量第一获胜；平局比存活时间。
- **闯关**：见章节目标表。三星 = 目标达成质量余量 + 用时 + 是否爆过刺球（可选）。
- **无尽**：缩圈 + 质量记录 `save.recordEndlessBest("orb-arena", mass)`。

### 系统表
| 系统 | 说明 | 抽成 |
| --- | --- | --- |
| 质量-半径 | `r = k√m` | `massToRadius` |
| 质量-速度 | 反比衰减 | `massToSpeed` |
| 吞噬 | 重叠 + 质量比 | `canEat(a,b)` |
| 分身 | 对半弹出，≤16 | `splitCell` |
| 合并 | 延迟后距离近合并 | `canMerge` / `mergeCells` |
| 吐孢子 | 扣质量、弹射 | `ejectSpore` |
| 刺球吃 | 爆开或加质量 | `eatVirus` |
| 刺球推 | 吐孢子推动 + 分裂刺 | `feedVirus` |
| 衰减 | 大圆掉质量 | `decayMass` |
| 边界 | 夹紧 / 缩圈掉质量 | `clampToMap` / `zoneDamage` |
| 排行 | 总质量排序 | `leaderboard` |
| AI | 四档 | `aiSteer` |

### 模式
| 模式 | 做不做 | 为什么 |
| --- | --- | --- |
| 闯关 188 | 做，走 `mountLevelGame` | 每关一张小地图 + 明确目标，适合六年级练技术 |
| 对战 FFA | 做 | IO 的本命：1 人 + 最多 11 个 AI，或双人同场（合作/混战切换） |
| 无尽 | 做 | 缩圈 + 质量记录 |
| 双人同屏 | 做 | 上下或左右分屏（窄屏上下），两套键位互不抢占 |

### 关卡切分（188 关，10 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 彩豆平原 | 20 | 只有彩豆与 1–2 个很小的 AI，练吞噬与边界 |
| 2 | 刺球花园 | 20 | 引入刺球（先教「别碰」，再教「够大再吃」） |
| 3 | 分身训练营 | 20 | 强制用分身才能吃到远处目标（门后小球） |
| 4 | 孢子工坊 | 18 | 吐孢子喂刺球、推刺球打 AI |
| 5 | 双圆合并岛 | 18 | 必须分身绕墙再合并过关 |
| 6 | 排行赛场 | 20 | 限时，目标「进入前三 / 前一」 |
| 7 | 缩圈荒原 | 18 | 安全区收缩，出圈掉质量 |
| 8 | 团团合作谷 | 18 | 双人 AI 队友（或单人 Tab 切两个圆），不能吞队友最后一圆 |
| 9 | 夜色迷雾 | 18 | 视野缩小（雾），小地图雷达 |
| 10 | 圆圆杯总决赛 | 18 | 全机制 + 高密度 AI + 刺球陷阱 |

合计 20+20+20+18+18+20+18+18+18+18 = 188。每章粉彩主题色 + 一句话介绍。第 100 / 145 / 188 关必须能玩到真实胜负。

### 前端建模与视觉
- **Canvas 2D** 世界（圆、刺球、孢子、网格背景），DOM 做 HUD（质量、排行、技能钮）。
- 圆：径向渐变 + 浅色高光 + 名字标签（跟着最大分身）。
- **禁止消完瞬变**：被吃要 200–350ms 缩小+彩纸粒子；分身弹出有拉伸残影；刺球爆开有刺向四周弹开再变成小圆。
- `prefers-reduced-motion`：关掉拉伸，屏幕跟随阻尼可保留。
- 360px：摇杆左下、分身/吐孢子右下，排行榜默认收成「第 N」条，点开展开。1280×800 桌面完整排行 + 小地图。

### AI 档位
| 档 | 行为 |
| --- | --- |
| 菜鸟 | 随机游走吃彩豆，几乎不分身，见刺球就躲 |
| 普通 | 会追小吃、躲大圆，偶发分身 |
| 高手 | 会算质量比再拍分身；会把刺球推向玩家 |
| 地狱 | 会诱饵（先吐孢子再拍）、会卡合并窗口偷吃、会缩圈卡位 |

固定 seed 下地狱档对菜鸟档 20 局胜率显著更高——写成测试。

### 可参考 GitHub（只看结构，不抄素材，不引入依赖）
- https://github.com/huytd/agar.io-clone （分身列表、视野、排行榜数据流）
- https://github.com/socketio/socket.io （不要用；本游戏离线，用本地 AI 冒充其他人）

### 独占文件清单
只许新建 / 修改：
- `src/games/orb-arena/**`（`meta.ts` `index.ts` `logic.ts` `levels.ts` `ai.ts` `guide.ts` 及对应 `*.test.ts`）
- 可选：`scripts/smoke-step2-a.mjs`

禁止：任何其他 `src/games/<别人>/**`（含本步 `snake-royale` / `block-drop`）、`src/ui/home.ts`、`src/games/ocean-munch/**`、`docs/plan-1.2-supervisor.md`、`docs/plan-1.2-step1-*`、`docs/plan-1.2-step9-*` 及以后。

### 测试（≥ 25 用例，硬性 ≥ 15）
必须覆盖：`canEat` 质量比边界、同玩家未到合并时间不能互吞、分身上限 16、吐孢子扣质量、吃刺球爆开 vs 已 16 不加分裂、边界夹紧、缩圈掉质量、排行排序稳定、188 章和、`assertTotal`、AI 档位胜率、destroy 后 rAF 停止（可 spy）。

### 不要做什么
- 不要联网、不要 socket、不要真实玩家服务器。
- 不要做成大鱼吃小鱼侧视。
- 不要出现血液、疼痛、死亡字样。
- 不要改 `fight-king` / `snake-snack` / `ocean-munch`。
- 不要实现游戏以外的 1.2 平台或升级步。

### 验收 checkbox
- [ ] meta 纯数据，index 懒加载 mount
- [ ] 吞噬 / 分身 / 刺球 / 吐孢子 / 边界 / 排行榜全部可玩
- [ ] 188 关 + 对战 + 无尽 + 双人
- [ ] 360×640 与 375×667 与 1280×800 不溢出
- [ ] 被吃、分身、消刺球都有动画，不是瞬变
- [ ] `npm test` `npm run build` 全绿，只增测试
- [ ] destroy 无泄漏
- [ ] 文案无商标、无低幼词、失败只鼓励
- [ ] 收藏只读，未改 collection 存档

### 测试命令
```
npm test
npm run build
npx vite preview   # 手动：首页进圆圆大作战，玩到真实胜负；再退再进
```
可选冒烟：`node scripts/smoke-step2-a.mjs`（若你写了）：360 与 1280 各进一次。

完成后回复：你是 A、改了哪些文件、用例数、推送 SHA、**实际使用的模型 slug**（派发指定的是 `claude-opus-5-thinking-high-fast`）。
