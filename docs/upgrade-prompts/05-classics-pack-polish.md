有子agent都用指定模型 slug：`[claude-fable-5-thinking-xhigh]`。  我让你派发子任务来做，没让父任务做！！全部重做！请通过 Task 工具派生一个或多个云端子代理执行本任务。  直接做在main里面吧。

# 第 5 步：经典包 4 款体验打磨（弹弹小鸟 / 糖果秋千 / 泡泡瞄准手 / 五子棋）

## 本步目标

经典 4 款结构优秀（弹鸟自写弹弓物理 938 行关卡表、糖果秋千自写绳物理、泡泡瞄准 `simulateShot` 瞄准线与实弹同源、五子棋三档 AI + 99 残局全部机器验证可解）。缺口同样是**零人工抽验**与触控细节。本步做抽验、物理手感与引导打磨，不重写关卡表、不动残局库结构。

## 仓库与分支

- 先 `git fetch origin main`（必须已含第 3 步 meta.ts 拆分），拉分支 `cursor/classics-polish-<你的后缀>`，做完 merge 回 main 并 `git push origin main`（禁止 force）。推不上就 push 分支并写明原因与 SHA。
- 不要合并旧 PR #7–#16，不要用 gh 开 PR。

## 范围与文件所有权（只许碰这些）

- `src/games/sling-birds/`、`src/games/candy-swing/`、`src/games/bubble-aim/`、`src/games/gomoku/` 四个目录
- 不许碰：`src/engine/`、`src/ui/`、`level99.ts`、`quiz99.ts`、其余 27 款目录、配置文件。

## 具体要做

1. **抽验**：弹鸟/糖果秋千/泡泡各抽第 1、17、33、50、66、83、99 关（每主题至少一关）；五子棋每主题抽 3 局残局 + 自由对战三档 AI 各一局。记录：可通关性、三星可达性、6–7 岁难度合理性、窄屏表现。
2. **弹弹小鸟**：
   - 拖弓时的弹道预览虚线是否与实际轨迹一致（含风关卡）；不一致就以物理模拟采样点重画。
   - 检查「剩余小鸟全灭但方块未清」时的失败判定延迟（方块还在滚动时不要立即判负，等静止或 3s 超时）。
   - 触屏拖拽越界（手指拖出画布）时应保持拉弓状态而不是丢失。
3. **糖果秋千**：割绳判定线宽 ≥20px；糖果落出屏判定失败前给 0.5s 缓冲动画；连割两绳的手势不误触发暂停/返回。
4. **泡泡瞄准手**：反弹瞄准线在两次反弹后仍准确；顶部下压新行时的「危险线」预警要提前一行闪烁提示；色弱友好——同色泡泡加纹理/图案区分（可选加分项）。
5. **五子棋**：
   - AI 响应加「对手思考中…」动画且不阻塞 UI（`bestMove` 若同步超 100ms 用 `setTimeout` 分片或降深度）。
   - 残局失败时提示第一步正解方向（已有 `hintMove` 就接上「提示」按钮，每局限用 1 次）。
   - 棋盘在 360px 宽下格子触点 ≥28px，落子有确认机制（点两次或放大镜），避免孩子误触。
6. **destroy 回归**：四款进入→玩→中途退出→再进→通关→退出全流程无报错。

## 如何优化（本步做好的标准）

- 物理参数改动必须先在对应测试文件（`physics.test.ts`、`logic.test.ts`、`ai.test.ts`）里补断言再改实现。
- 弹道预览等视觉改动附前后截图（存放在 PR 描述或回复，不进 git）。
- 单款代码改动 ≤200 行；关卡表 `levels.ts`/`puzzles.ts` 只许做「明显坏关」最小数值修复并注明理由。

## 与其他步骤避免冲突

- 与 04/06 可并行（目录不相交），合 main 串行：先 `git pull origin main` 再 merge。
- 第 1 步已把 candy-swing 首行注释的商标词改掉，如遇冲突以 main 为准。

## 验收标准

- [ ] 4 款抽验记录表在最终回复里（弹鸟/秋千/泡泡各 7 关 + 五子棋 18+3 局）。
- [ ] 弹鸟弹道预览与实弹一致（含风）；五子棋 AI 无卡 UI。
- [ ] `npm test`、`npm run build` 全绿，用例数不减（新增物理断言只多不少）。
- [ ] 360×640 下四款可正常游玩、无 HUD 溢出。
- [ ] 变更已 merge 进 main 并 push（或写明原因与分支 SHA）。

## 测试命令

```bash
npm install && npm test && npm run build
npx vitest run src/games/sling-birds src/games/candy-swing src/games/bubble-aim src/games/gomoku
npm run dev   # 按抽验表走查，360×640 与 1280×800 两档
```
