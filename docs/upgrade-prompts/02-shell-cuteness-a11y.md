有子agent都用指定模型 slug：`[claude-fable-5-thinking-xhigh]`。  我让你派发子任务来做，没让父任务做！！全部重做！请通过 Task 工具派生一个或多个云端子代理执行本任务。  直接做在main里面吧。

# 第 2 步：首页/壳层卡通化二期——对比度、最近玩过、响应式、无障碍

## 本步目标

壳层已有粉彩大圆卡片与 emoji 页签，但审查发现四个适龄性缺口：①正文对比度约 3.4:1 低于 WCAG AA；②「全部」页签一次铺 31 张卡片，6–7 岁孩子找游戏困难；③99 关选关地图 `max-width:480px` 在桌面大屏下留白过多；④缺键盘焦点样式。本步把首页与游戏壳打磨到「孩子一眼找到想玩的、家长挑不出毛病」。

## 仓库与分支

- 先 `git fetch origin main`，从最新 main（应已含第 1 步文档更新）拉分支 `cursor/shell-cuteness-<你的后缀>`，做完 merge 回 main 并 `git push origin main`（禁止 force）。推不上就 push 分支并写明原因与 SHA。
- 不要合并旧 PR #7–#16，不要用 gh 开 PR。

## 范围与文件所有权（只许碰这些）

- `src/ui/home.ts`、`src/ui/app.ts`、`src/ui/gameShell.ts`、`src/ui/dialogs.ts`、`src/ui/parentGate.ts`（仅样式类名层面，逻辑归 07 步）
- `src/styles.css`、`index.html`
- `src/games/level99.ts` **仅 `L99_CSS` 常量里的宽度与配色数值**（框架逻辑与存档归 06 步，绝不动函数体）
- 不许碰：`src/engine/`、各游戏目录、`vite.config.ts`、`package.json`。

## 具体要做

1. **对比度修复**：`--ink-soft: #9c8794` 加深（建议 ≥ `#7d6a76`，与白底对比 ≥4.5:1）；逐一核对卡片副标题、页脚、问候语、l99 地图章节描述文字（`#9a86b8` 系）与浅色背景的对比度，小于 4.5:1 的加深文字或加深底色。粉彩观感靠背景与边框保留，不靠把正文调浅。
2. **首页信息架构**：
   - 新增「⏰ 最近玩过」分区：读 `save.getGameProgress` 已有的 `plays` 数据（如需最近时间戳，可在 `home.ts` 里用一个独立 localStorage key 记录，**不得改 `save.ts` 的存档结构**——那是 07 步的所有权），最多显示 4 张小卡，放在页签上方。
   - 卡片上显示该游戏 99 关进度徽章（读 `yiduo-yixing.l99.<id>` 已有存档，只读不写），例如「🚩 23/99」，比现在只显示三星更有「继续玩」的召唤力。
   - 31 张卡片在「全部」页签下按分类加小节标题（闯关/休闲/对战/学习/动手），滚动时孩子有方位感。
3. **桌面与窄屏响应式**：
   - 桌面 ≥1024px：`l99-wrap` 的 `max-width` 提到 640–720px 并保持网格舒展（只改 `L99_CSS` 字符串里的数值）；首页网格已用 auto-fill 不用动。
   - 窄屏 360px 实测：页签、顶栏、结算弹窗不得横向溢出；游戏顶栏在窄屏下星星 chip 与标题不换行挤压。
4. **无障碍**：
   - 全站加 `:focus-visible` 样式（粉色 3px 外圈），保证键盘/遥控可玩。
   - 核对可点元素热区 ≥44×44px（l99 关卡格子、页签、音效按钮已达标则记录即可）。
   - 装饰 emoji 保持 `aria-hidden`，新加分区标题用语义化标签。
5. **可爱度加分项**（时间允许再做）：结算弹窗三星逐颗弹出音效节奏、首页问候语按时段变化（早上好/下午好）、卡片进入时的错峰浮现动画（尊重 `prefers-reduced-motion`）。

## 如何优化（本步做好的标准）

- 每处颜色改动写进最终回复的对照表（旧值→新值→对比度）。
- 「最近玩过」为空时不显示该分区，不要给新用户看空壳。
- CSS 只加不删，避免影响游戏内部自带样式。

## 与其他步骤避免冲突

- 06 步拥有 `level99.ts` 的逻辑与文案，你只改 `L99_CSS` 字符串内数值；如两步并行，先合 main 者优先，后者 rebase。
- 07 步会改 `parentGate.ts` 的逻辑（防暴力重试），你只调它的样式类。
- 03 步会把 `app.ts` 的游戏挂载改为异步；你若改 `app.ts` 只动 DOM 结构不动路由函数签名。

## 验收标准

- [ ] 所有正文文字与其背景对比度 ≥4.5:1（在回复里附抽查清单）。
- [ ] 首页出现「最近玩过」分区与每卡 99 关进度徽章；「全部」页签有分类小节。
- [ ] 360px 宽无横向滚动；1280px 宽 l99 地图不再挤在 480px 内。
- [ ] Tab 键可以走完首页并用 Enter 进游戏，焦点圈可见。
- [ ] `npm test`、`npm run build` 全绿；随机进入 5 款游戏（每分类至少 1 款）玩一关、退出、再进，无报错无样式错乱。
- [ ] 变更已 merge 进 main 并 push（或写明原因与分支 SHA）。

## 测试命令

```bash
npm install && npm test && npm run build
npm run dev   # 用浏览器分别在 360px / 768px / 1280px 宽度走查首页与任意 5 款游戏
```
