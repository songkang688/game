# 1.1 第 2 步 / 角色 C —— 红蓝对战三款 99 → 188 工作计划

> 独占目录：`src/games/red-blue-race`、`src/games/red-blue-tap`、`src/games/red-blue-tug`。
> A（clock-house / math-farm / shape-kingdom / find-diff）与 B（pinyin-train / word-garden / color-fun / music-stars）
> 的目录一个字都不动；`src/games/level99.ts` 第 1 步已经升到 188，本步不再碰它；
> `duo-arena`、`duo-rush`、`xiangqi` 是纯对战、无关卡表，本步同样不碰。

## 一、硬约束（逐条自查）

1. 前 99 关一个字不动：`CHAPTERS` 前六章的名字 / emoji / 颜色 / 介绍 / size 原样保留，
   `buildLevel` 里 `ci = 0..5` 的分支参数与 seed 表达式原样保留（原来的 `default:` 改写成 `case 5:`，
   语义完全等价），新关只在数组末尾追加。
2. 每款追加 89 关 = 4 个全新主题章节（23 + 22 + 22 + 22），原创中文名 + emoji + 粉彩色 + 一句话介绍。
3. 每款引入 ≥ 2 个前 99 关没有的新对抗机制（实际按 4 个做，见下）。
4. `totalSize(CHAPTERS) === 188` 有断言；1.0 的前 99 关章节切分（六章 17/17/17/16/16/16 与逐章名字）
   硬编码进测试做回归。
5. 第 100–188 关逐关跑「可通关」校验：三款各写一个**纯逻辑无头对局模拟器**，
   用「约小学六年级」的手速 / 反应时参数逐关模拟到真实胜负，断言每一关都赢得下来；
   同时用明显偏低的参数断言会输，证明新关确实更难而不是白送。
6. 存档 key 不动：关卡星级仍是 `yiduo-yixing.l99.<id>`，跳关仍是 `yiduo-yixing.l99skip.<id>`。
7. 失败文案永远只鼓励不批评；文案面向约小学六年级，中文粉彩萌系，禁止任何商业商标或官方角色名。

## 二、三款各自的新章节与新机制

### 红蓝赛跑 red-blue-race（新 4 章）

| 章节 | 新机制 |
| --- | --- |
| ☁️ 云顶接力 | **体力条**：连点会喘，体力见底每步只剩一半，松手才回气 |
| 🎁 拾光集市 | **道具抢夺**：赛道上的礼物箱谁先冲到谁拿，自己加速 + 对手减速 |
| 🎵 节拍风廊 | **连击节拍**：踩稳节奏点才有连击加成，忽快忽慢连击清零 |
| 🌠 决胜星轨 | **读招电脑**：你领先它就提速追、落后时略微收力，四种机关混战 |

### 红蓝点点 red-blue-tap（新 4 章）

| 章节 | 新机制 |
| --- | --- |
| 💫 霓虹连击场 | **连击加成**：连抢 3 个不失手就进连击，之后每个点算双倍分 |
| 🧲 机关道具局 | **道具点**：❄️ 冻住对手一会儿、🧲 自动吸走下一个点 |
| 🔢 序列谜阵 | **顺序抢点**：点点带号码，必须 1→2→3 按序拍，拍错把分让给对手 |
| 🧠 读心决赛 | **读招电脑**：你领先它出手更快、落后时稍稍放慢，全规则混战 |

### 红蓝拔河 red-blue-tug（新 4 章）

| 章节 | 新机制 |
| --- | --- |
| 🏜️ 沙丘角力 | **体力条**：连点会累，体力见底拉力减半，松手 0.6 秒回气 |
| 🧤 补给争夺 | **道具抢夺**：防滑粉（自己变强）和水袋（对手打滑）掉在场上，抢得到才算 |
| 📣 齐心号子 | **号子连击**：跟着「嘿—哟」的节拍踩点拉，连上号子有齐心加成 |
| 🥇 巅峰绳王 | **读招电脑**：眼看你要赢它就发力反扑，全机关混战 |

## 三、无尽模式

三款各加一个「♾️ 无尽模式」入口，挂在关卡地图上方（`mountLevelGame` 挂到子容器里，
`level99.ts` 一行不改）。越往后节奏越快，有速度封顶保证不会瞬间结束。
最高分记进平台钱包 `yiduo-yixing.save.v1` 的游戏进度里：
`GameProgress` 按既有方式**追加**一个可选字段 `endlessBest`（老存档没有就读成 0），
不新开任何存档 key。

## 四、文件与测试

每款：`levels.ts`（追加章节与关卡）、`logic.ts`（新机制纯逻辑 + 无头对局模拟器，新建）、
`index.ts`（新机制的实机表现 + 无尽模式）、`levels.test.ts`（追加断言）、`logic.test.ts`（新建）。
每款新增用例 ≥ 12 个，`npm test` 与 `npm run build` 全绿，用例只增不减（基线 947）。
