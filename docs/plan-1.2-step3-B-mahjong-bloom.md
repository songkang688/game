# 1.2 第 3 步 · B 档 —— `mahjong-bloom`「花开麻将」

> 短计划：独占新建 `src/games/mahjong-bloom/`。本步另两档是 `combo-clash`、`star-estate`。
> 国标向四人麻将（吃碰杠胡、8 番起和、≥40 番种单测），**不是**连连看式单人翻牌。不要改 `landlord-cards`。

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。你就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 3 步 · B 档**：新建 `mahjong-bloom`「花开麻将」。假设 1.1 的 55 款已全部做完。

## 分支纪律
fetch origin/game-1.2 → 工作分支 → 计划 commit → 只推 game-1.2。rebase 后测绿再普通 push。禁止 force。不要改 main。

## 新游戏统一约定（对接更新后的 1.1）
- `meta.ts` 纯数据 + `index.ts` 懒加载 mount。**不要改 `src/ui/home.ts`。** 闯关走 `level99.ts` 188 关。存档 `yiduo-yixing.l99.mahjong-bloom`。
- `destroy` 干净。内置音效。无外部依赖。禁止商标（不要写麻将竞技商标）。
- 键位：点选为主；朵朵 WASD 移动光标 + F 出牌 / G 吃碰杠胡；星星方向键 + L/K。手机：点牌 + 动作按钮（牌面宽 ≥ 28px，热区 ≥ 44px）。
- **收藏只读**：`luckMul` 可微调听牌提示亮度，不要改听牌判定本身。禁止改 collection 源文件。暂停可 `openCollection("mahjong-bloom")`。
- 360px：手牌可横滑。验证 360 / 375 / 1280。不要改 supervisor / step1 / step9+。

## 本款采用的规则（写清，不要含糊）

**主规则：国标麻将竞赛规则的可玩简化落地。**

- 牌张：筒条万 1–9 各 4，风 4 种×4，箭 3 种×4，花 8 张，共 **144**。
- 四人：下家 / 对家 / 上家。玩家永远坐「南」视角（自己在下方）。
- 起手 13，庄 14。逆时针抓打。
- **吃**：只能吃上家打出的牌，组成顺子。
- **碰**：任何一家打出，组成刻。碰后必须打出一张。
- **杠**：明杠（碰后再抓到第 4 张或别人打出第 4 张）、暗杠、加杠。杠后补牌（从牌尾）。
- **胡**：基本型 4 面子 + 将，或七对、十三幺、全不靠、七星不靠、组合龙等国标特殊型。
- **8 番起和**：对战默认开（不足 8 番点胡 = 错和，赔每家 10 分）。闯关第 1–3 章把门槛降到 1/2/4 番教学，第 7 章起恢复 8 番。
- **截和**：一炮只胡一家，顺序下家>对家>上家。
- **不计连庄**：对战一局 16 盘（四圈）或快棋 4 盘（闯关用快棋）。
- 计分：国标 **81 番种**，分值 88/64/48/32/24/16/12/8/6/4/2/1。花牌计分不计番。遵循不重复、不拆移、就高不就低。自摸：(8+番)×3；点炮：点炮者付 8+番，其余付 8。
- 必须实现并单测的番种 **至少 40 个**（含平和、碰碰和、混一色、清一色、七对、十三幺、大三元、大四喜、清龙、三色三同顺、杠上开花、抢杠和、妙手回春、海底捞月、门前清、不求人、全求人、断幺、无字、一般高、喜相逢、老少副、边张、坎张、单钓、自摸、四归一、箭刻、圈风刻、门风刻……）。其余番种可在 `fan.ts` 列全表，遇到就算，没覆盖到的要在注释里标「识别器未做」——但 40 个常用必须绿。

听牌提示：对战可关。闯关默认开「能吃/碰/杠/胡」按钮高亮。

### meta
```
id: "mahjong-bloom"
title: "花开麻将"
emoji: "🀄"
category: "party"
color: "#FFE8F0"
blurb: "吃碰杠胡，把番种凑到八番。和朵朵星星还有两位棋友坐一桌，看谁先开花。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 牌编码 | `Tile` 花色+点数 |
| 洗切 | `shuffleWall(seed)` |
| 合法吃碰杠 | `chiOptions` `ponOk` `kanOptions` |
| 胡牌形 | `isHu(tiles, last, melds)` |
| 向听 | `xiangting`（用于 AI 与提示） |
| 番种 | `scoreFans(hand) → {fans, names, points}` |
| 8 番门槛 | `canHuWithFloor` |
| 截和顺序 | `ronPriority` |
| 错和 | `falseHuPenalty` |

### 模式
| 模式 | 做 | 说明 |
| --- | --- | --- |
| 对战 | 是 | 1 人 + 3 AI，或双人（朵朵星星各坐一家，另两家 AI） |
| 闯关 188 | 是 | 残局/定番：给定牌河与手牌，达成指定番种 |
| 无尽 | 是 | 连庄快棋，累计分，`recordEndlessBest`；错和或点炮过多结束 |
| 教学 | 是 | 嵌在前 3 章 |

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 顺子小溪 | 24 | 只会吃，1 番起和，目标平和 |
| 2 | 碰碰花园 | 24 | 碰、碰碰和 |
| 3 | 杠上开花 | 24 | 明杠暗杠 |
| 4 | 混清一色 | 24 | 混一色 / 清一色 |
| 5 | 七对星空 | 22 | 七对、连七对介绍 |
| 6 | 龙与三色 | 22 | 清龙、花龙、三色三同顺 |
| 7 | 八番考场 | 24 | 打开 8 番门槛 |
| 8 | 国标华章 | 24 | 大三元 / 十三幺等残局（有解） |

24×4 + 22×2 + 24×2 = 96+44+48 = 188。

**每一道残局必须有解**：`solver` 或作者预置胡牌序列，测试对 188 关断言 `solvable === true`。

### 前端建模与动画
- DOM 牌面（CSS 画牌，红中绿发白皮用字+色，不要用版权字体图）。
- 出牌：牌飞到河，200ms；吃碰杠：副子区滑入，不要瞬变。
- 和牌：番种一条条弹出（最多同时 6 条，其余可滚动），禁止只弹「你赢了」。
- 360px：对家手牌用背面小条；自家牌可滚动。

### AI 档位
| 档 | 行为 |
| --- | --- |
| 菜鸟 | 随机打，几乎不吃碰 |
| 普通 | 向听下降优先，会碰不会拆顺 |
| 高手 | 防守：危险牌（现物/字牌）晚打 |
| 地狱 | 会算 8 番路线、会点炮判断、会抢杠 |

固定 seed 100 局：地狱均分 > 高手 > 普通 > 菜鸟。

### 可参考 GitHub（结构 only，禁止运行时依赖）
- https://github.com/Pomax/mahjong （牌墙、副子、计分分文件）
- 不要引入 npm 麻将库。

### 独占
只许 `src/games/mahjong-bloom/**`，可选 `scripts/smoke-step3-b.mjs`。禁止碰 `landlord-cards`、`home.ts`、本步 A/C、collection 源文件、supervisor / step1 / step9+。

### 测试 ≥ 50（硬性 ≥ 15）
吃碰杠合法性、胡牌基本型、七对、十三幺、8 番门槛、错和、截和顺序、至少 40 番种各 1 个正向用例、188 可解、向听单调（打牌后不无故升向听——AI 普通档）。

### 不要做什么
- 不要做成上海麻将消消乐（那是连连看亲戚，本合集已有 `lianliankan`）。
- 不要日麻立直棒里的宝牌表当主规则（可在注释写「本款国标」但不出现商标）。
- 不要真人脸。

### 验收 checkbox
- [ ] 吃碰杠胡 + 番种表 + 8 番可选
- [ ] 188 残局全可解
- [ ] AI 四档
- [ ] 360px 可点牌；和牌番种有列表动画
- [ ] `npm test` `npm run build` 绿；destroy 干净；收藏只读

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 B、实现了哪些番种、188 可解策略、用例数、SHA、实际模型 slug（派发指定 `claude-opus-5-thinking-high-fast`）。
