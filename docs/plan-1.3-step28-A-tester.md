# 1.3 第 28 步 · A 档 —— 视觉验收三人组 **第 2 轮** · 测试员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉**，不重开玩法验收，也不改任何玩法代码。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 28 步 · A 档 —— 视觉验收三人组第 2 轮（共 3 轮）的测试员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「A 档 · 第 2 轮视觉测试员 · 复验计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round2-tester.md`。

## 二、本轮定位：复验 + 深挖

第 1 轮做的是全库扫描建立基线，本轮做两件事：

1. **复验**：把 `docs/qa/1.3-round1-tester.md` 的问题清单与 `docs/qa/1.3-round1-fixer.md` 的修复清单逐条对账——修了的验证「真修好没有、有没有修出新问题」，遗留的确认还在遗留清单里没丢。
2. **深挖**：第 1 轮是广度，本轮抽 20 款做**深度视觉走查**（见第四节）。

先跑 `npm ci` → `npm test` → `npm run build`，记录用例总数与第 1 轮合并态的差值（只增不减）。

## 三、逐条对账（本轮主责）

- 第 1 轮报告的每一条问题给出结论：**已修复（附验证证据）/ 修复不彻底（说清差在哪）/ 修出回归（新问题描述）/ 合理遗留**；
- 六大专项（火柴人草稿圆 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px / 商标）在修复后的整体达标率对比第 1 轮，给两轮对比表；
- 第 1 轮沉淀的机器化扫描用例（emoji 直出断言等）要全部还在跑、全绿。

## 四、深度走查 20 款（抽样规则写进报告）

- 必抽：主管点名的整改重点 `poop-hero` / `ice-fire-forest` / `puff-bros` / `prince-princess` / `fruit-slice` / `balloon-pop` / `sky-squad` / `tank-battle` / `bumper-cars` / `bowling-lane` / `red-blue-race` / `brave-path`（12 款）；
- 再抽 8 款第 1 轮问题最多的款（按报告统计）。

每款深度走查项：

| 项 | 标准 |
| --- | --- |
| 角色特写 | 主角放大到 2× 与缩到 16px 各看一次：渐变层次、描边一致性、剪影可辨。 |
| 动效完整链 | 核心反馈的「预告 → 过程 → 收尾」三段是否完整（如出手前摇光效 → 命中星花 → 飘分）；reduced 下三段的退化是否符合该款 1.3 提示词约定。 |
| 场景层次 | 背景 / 中景 / 主体 / HUD 四层是否分明；装饰层有没有喧宾夺主（饱和度、动效频率）。 |
| 一致性 | 与 `src/art/kit/` 光照约定（左上 45°）、描边规格、粉彩色板的一致性；同款内不同界面（菜单 / 局内 / 结算）风格统一。 |
| 性能 | CPU 4× 节流下粒子高峰帧率 ≥ 30fps，记录数字。 |

## 五、回归防线

- 视觉修复最容易踩的三种回归，逐一排查：**判定热区被挪动**（对照各款「热区一个像素不动」的钉死断言）、**功能提示被动画吃掉**（危险提示 / 信号灯在 reduced 下必须还在）、**教育数据被改**（笔顺 / 音高 / 题目文本的快照断言全绿）；
- `destroy` 泄漏复查：第 1 轮记录过问题的款全部复验。

## 六、报告要求

产出 `docs/qa/1.3-round2-tester.md`：

- 第一节：全量跑数据 + 两轮用例数对比；
- 第二节：第 1 轮问题逐条对账表（四种结论）；
- 第三节：20 款深度走查表（五项 × 20 款）；
- 第四节：本轮新发现问题清单（位置 / 现状 / 期望 / 严重度 / 建议责任文件，量化）；
- 第五节：六大专项两轮达标率对比。

## 七、你的权限边界

**测试员只写报告与补测试用例，不改绘制代码。** 你能改的文件限于 `*.test.ts` 与 `docs/qa/1.3-round2-tester.md`。发现的问题交给本轮 C 档修。

## 八、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；
- 第 1 轮阻断 / 严重级问题的修复确认率 100%（修复不彻底的重新立项给 C 档）；本轮新发现的阻断级当轮清零；
- 三方合并态复验全绿，合并态 SHA 写进报告（由 C 档执行，你要配合）。

## 九、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 28 步 A 档第 2 轮视觉测试员；对账结论统计（已修 / 不彻底 / 回归 / 遗留各几条）；20 款深度走查结论摘要；本轮新问题统计；两轮达标率对比；报告路径；`npm test` / `npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
