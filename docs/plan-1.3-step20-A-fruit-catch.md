# 1.3 第 20 步 · A 档 —— `fruit-catch`「接住小水果」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 20 步 · A 档**：给 `fruit-catch`「接住小水果」做只动皮肤不动骨头的美术精修。
本步三人：A `fruit-catch`、B `fruit-slice`、C `snake-snack`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「A 档 · fruit-catch 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/fruit-catch/**` 里的绘制函数、CSS、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 下落速度 / 生成表 / 接住判定（篮口宽度与判定线）、冰冻 / 磁铁道具的效果逻辑、警告物的扣分规则；
- 存档 key、`meta.ts` 的 `modes/levels/category`；关卡数据；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，全部程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/fruit-catch/index.ts`，绘制入口是 `draw` / `drawItem` / `drawBasket` / `drawSparks`。已核实的问题：

1. **果子是 emoji**：`drawItem` 的主体就是 `c2d.fillText(it.emoji, 0, 0)`（约 166 行）——「接住小水果」的水果没有一笔自绘，字形、大小、基线全看系统字体脸色；
2. **篮子也是 emoji**：`fillText("🧺", x, H - 18 + press)`（约 180 行）——主角道具是一个字符，接住时只有 `press` 一个下压量；
3. 背景装饰同样是 emoji 直出：🌠⭐☁️（391–394 行）；一屏之内自绘元素几乎为零，本款是全库「emoji 拼贴」的重灾区之一；
4. 警告物的红圈脉动（127–136 行）是功能件（保住），但圈里的物体还是 emoji；
5. `drawSparks` 接住火花与冰冻 / 磁铁两个道具底板（138–142 行的圆角矩形）自查，把结论记进开工 commit。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **水果自绘（核心整改）** | 全部 emoji 替换为专属剪影 + 三停径向渐变 + 高光 + 1.5px 描边的自绘水果，且**剪影层面互不相同**：苹果（心形圆 + 凹顶 + 叶）、香蕉（月牙弯 + 两端褐点）、葡萄（六球簇 + 藤须）、橙子（正圆 + 脐点 + 皮孔点阵）、草莓（倒水滴 + 籽点 + 蒂叶）、梨（上窄下宽葫芦形）。落下时每种有 ±8° 慢旋（reduced 停）。 |
| **警告物自绘** | 红圈脉动保留，圈内物换成自绘的「小捣蛋云」（皱眉乌云 + 两滴小雨）——语义是「别接它」，不吓人。 |
| **编织篮自绘** | 🧺 替换为自绘藤篮：横三条纵五条交叉编织纹 + 篮口高光沿 + 双侧提耳；接住瞬间篮身压扁 8% 回弹（沿用 `press` 变量）+ 果子落入篮内可见（篮口裁剪层，最近三个果子小图标堆在篮里）；磁铁道具生效时篮沿泛蓝微光、冰冻时篮口挂两根小冰凌（读道具状态只做映射）。 |
| **接住反馈** | `drawSparks` 升级：接住普通果 = 星屑 4 颗 + 飘分；连接 5 个不落地 = 篮上方小彩虹一闪（300ms）；接到警告物 = 灰色小云雾 + 「哎呀」气泡（不批评）。 |
| **场景层次** | 背景改三层：远层渐变天空 + 自绘太阳 / 月亮（按关卡昼夜）、中层两朵程序云（缓移，reduced 停）、近层果树枝从顶部探入（果子「从树上掉下来」的语义成立）；地面草地条 + 落空的果子在地面弹一下变成半透明再消失（不改扣分逻辑）。 |
| **布局与 HUD** | 分数 / 生命 / 连击卡片化一行；生命用小果子图标不是数字裸奔。 |

- 渐变 / 描边 / 落影统一走 `src/art/kit/`；光源统一左上 45°。
- `prefers-reduced-motion`：旋转、云移、彩虹、回弹全停，保留静态层次。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`（`ballGradient` / `softShadow`）、`outline.ts`（统一描边）、`sparkle.ts`（星屑）。
- 水果剪影做成参数化路径可**新增** `src/art/kit/fruit.ts`（六种剪影 + 渐变皮），供 B 档 `fruit-slice` 对齐观感复用；不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看接水果 / 接物页面，**只学两件事：画面密度（果园氛围层）与水果剪影（六种一眼分清）**。
- 禁止抄任何商标与官方角色；警告物是「小捣蛋云」不是炸弹武器。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：**篮口接住判定宽度一个像素都不动**；
- 水果最小渲染尺寸下剪影可辨（低于 18px 时省略皮孔 / 籽点等细节层）；
- HUD 一行放得下，字号 ≥ 14px；触屏拖篮热区不动。

## 八、独占文件与冲突

只许改 `src/games/fruit-catch/**` 与新增 `src/art/kit/` 文件。不要碰 `fruit-slice` / `snake-snack`。CSS 前缀沿用本款既有前缀；`destroy` 清干净粒子与计时。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 六种水果剪影路径函数两两不相等（至少抽 3 对断言）；
- `drawItem` / `drawBasket` 不再以 emoji `fillText` 直出（字符串断言）；
- 篮身回弹只影响绘制、接住判定线不变（断言）；
- 细节层在小尺寸下自动省略（18px 阈值断言）；
- reduced 下旋转 / 云移 / 彩虹为 0；`destroy` 归零；
- 既有生成表与判定测试断言一个不许改。

## 十、分级红线与回复

无惊吓（警告物是皱眉小云）；失败只鼓励；无商标。完成后回复：你是 1.3 第 20 步 A 档、`fruit-catch`；第三节的现状结论；水果 / 藤篮自绘的实现说明；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
