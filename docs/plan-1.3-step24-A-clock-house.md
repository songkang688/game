# 1.3 第 24 步 · A 档 —— `clock-house`「时钟小屋」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 24 步 · A 档**：给 `clock-house`「时钟小屋」做只动皮肤不动骨头的美术精修。
本步三人：A `clock-house`、B `math-farm`、C `pinyin-train`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「A 档 · clock-house 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/clock-house/**` 里的渲染函数（`dial.ts` 的 `paint` 等）、CSS/SVG 结构、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 指针角度计算（`hourHandAngleAt` / `minuteHandAngleAt` / `handTip`）、读题与判定、`formatClockMinute` / `dialReadout` 的读数语义、`aria-label` 与 `data-t` 的可测接口（96–98 行）、精确模式开关逻辑；
- 存档 key、`meta.ts` 的 `modes/levels/category`；题库；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，SVG/CSS 程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/clock-house/dial.ts` 与 `index.ts`，渲染入口是 `paint`（约 89 行起）与表盘 SVG 构建段。已核实的问题：

1. `paint` 只做一件事：挪两根指针的 `x2/y2` 端点（92–95 行）——指针就是两条 SVG 线段，没有箭头造型、没有轴心铆钉、没有粗细层次，时针分针只靠长短区分；
2. 表盘本体（自查 SVG 构建段）预期是圆 + 刻度线 + 数字的裸排：「小屋」的题眼在名字里，画面上没有屋顶、没有窗、没有摆锤——主题与画面脱节；
3. 精确模式切换按钮是文本按钮（98–99 行 `toggle.textContent`），无状态视觉差异；
4. 读数条 `readout` 预期是裸文本；
5. 答对 / 答错的反馈画法自查，把结论记进开工 commit。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **表盘小屋化（核心）** | 表盘外壳画成布谷鸟小屋：三角屋顶（瓦纹三排 + 烟囱）+ 屋身木纹壁板 + 表盘嵌在屋身正中（圆框加双色木圈）+ 屋底摆锤（左右摆动 2s 周期，reduced 静止）+ 两侧小窗（暖光）。全部 SVG 矢量。 |
| **指针精修** | 时针改胖箭头造型（短粗 + 圆尾配重）、分针细长箭头（带尾羽），两针颜色差异（时针暖橙 / 分针青蓝）+ 轴心铆钉双圆盖住交点；**端点坐标仍由 `handTip` 算出**，只换 `line` 为 `path` 造型层。分针整点跨越时轻微「哒」一格弹性（reduced 关）。 |
| **刻度与数字** | 12 个数字换成小木牌（圆角牌 + 钉点），5 分钟刻度粗、1 分钟刻度细；12/3/6/9 四主位牌加大加色。 |
| **答题反馈** | 答对：屋顶小鸟弹出「咕咕」两下 + 星屑；答错：小鸟探头歪脑袋（不批评）+ 正确指针位置柔光提示（读判定结果只做映射）。 |
| **精确模式视觉** | 开 / 关两态按钮做成拨杆样式（拨上 / 拨下 + 色变）；精确模式下分针刻度增亮提示「要看细」。 |
| **布局与场景** | 背景加温馨房间语义（墙纸淡纹 + 地板线 + 左右盆栽）；读数条卡片化（木牌样式）；题目区与表盘上下排布留白 ≥ 16px。 |

- 色板走 `src/art/kit/palette.ts`；星屑走 `sparkle.ts` 的 SVG/CSS 等价。
- `prefers-reduced-motion`：摆锤、弹格、小鸟弹出全停，保留静态造型与柔光提示。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`、`outline.ts`、`sparkle.ts`。
- 你需要而 kit 没有的能力（比如 `woodSign.ts` 木牌样式）：**新增**文件并配套单测，不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看认时钟教育页面，**只学两件事：画面密度（房间氛围）与指针剪影（时针分针一眼分清）**。
- 禁止抄任何商标与官方素材：不准像米老鼠手表 / 品牌挂钟；小鸟与小屋原创。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：小屋 + 表盘整体缩放后指针仍可精确拖拽（**拖拽热区与角度换算不动**）；
- 数字木牌最小 12px 可读；
- 读数与按钮字号 ≥ 14px；`data-t` / `aria-label` 可测接口原样。

## 八、独占文件与冲突

只许改 `src/games/clock-house/**` 与新增 `src/art/kit/` 文件。不要碰 `math-farm` / `pinyin-train`。CSS 前缀沿用本款既有前缀；`destroy` 清干净摆锤与小鸟计时。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 指针 path 端点与 `handTip` 输出一致（0 点 / 3 点 / 7:30 三例断言，钉死）；
- 时针 / 分针造型路径不同、颜色不同（断言）；
- `data-t` 与 `aria-label` 换肤后原样（回归断言）；
- 答对 / 答错走不同视觉分支（类名断言）；
- 拨杆两态与精确模式开关状态一致；reduced 下摆锤 / 弹格静止；
- `destroy` 归零；既有角度与判定测试断言一个不许改。

## 十、分级红线与回复

答错只是小鸟歪头提示；失败只鼓励；无商标。完成后回复：你是 1.3 第 24 步 A 档、`clock-house`；第三节的现状结论；小屋化表盘与指针精修的实现说明；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
