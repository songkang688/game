# 三人组 r35 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。不回退 N-117…N-164 巡检闸。N-105 无第四版。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-167 | 静态扫 `*-rules-close` / `*-resume`：须 `min-height`≥44 或 `MIN_HIT_PX` / `TOUCH_MIN` 插值。对照象棋 `.xq-rules-close`、双人擂台关闭钮与暂停行 `button`。**不改** duo-rush（N-165/166）。 |
