# 1.3 窗口 4 · 遗留 backlog(1.4 的输入)

> 由第 3 轮终验监督修复员建档，统筹收口时并入 A 档终验条件。范围仅本窗 9 款。
> 每条带「来源轮次 / 位置 / 期望 / 估算改动面」。共 **3 条**,均为一般级,无阻断/严重级遗留。

## BL-W4-01 · DOM 次要文案字号 9–13px(约 18 处)

- **来源轮次**:r1 tester 立项 W4R1-06;r2 tester 复核结论「合理遗留:1.2 存量 DOM 样式、
  无溢出无遮挡、不升级」;r2 fixer 维持书面遗留;r3 终验 A 档未升级 → 按派发口径入 backlog。
- **位置**:本窗 9 款 `src/games/<id>/index.ts` 内嵌 CSS 的次要文案(如 sling-birds
  选关格 `.slb-cell .slb-stars` 9px、各款徽章/角标/副标签小字),合计约 18 处。
- **期望**:若 1.4 划定全库最小字号线(建议 ≥12px),按线统一提字号;
  提完需复测 360px / 320px 两档窄屏零溢出(部分小字是为窄屏格子挤下才压的字号)。
- **估算改动面**:纯 CSS 字号常量,零 JS;但每处提字号都可能牵动窄屏布局,
  需配套跑各款既有窄屏宽度断言 + 人工过一遍 360px 档;9 款 × 约 18 处,改动小、回归面中。

## BL-W4-02 · sling-birds 结算星级仪式 · 全量方案剩余部分

- **来源轮次**:r1 learner TOP9 → r2 learner 顺延 R2-TOP10(唯一书面顺延项)→
  r3 fixer 拆分:绘制层子集已落地(`d7b90ea`,胜利后省下的排队小鸟按 0.25s 间隔
  逐只腾小弧 + 金星屑,endT 驱动、判定零改动),超绘制层的剩余部分入本条。
- **位置**:`src/games/sling-birds/index.ts` 结算流程(`updateFlow` 的 `endT > 0.8` →
  `finishWin` → `api.onWin` 时序)+ 共享外壳 `src/ui/dialogs.ts` `showResultDialog`。
- **期望**(B 档 r2 规格):DOM 星级面板亮出前 0.9s 画布演出——剩余小鸟逐只沿贝塞尔弧
  飞屏幕上缘 + 金色加分飘字;DOM 三星点亮间隔提至 0.3s/颗 + 每颗 3 粒星屑;
  `reduceMotion` 全跳直接亮结果;判定计分零改动。
  注:共享外壳现状已有 0.15s 起步、0.18s/颗的逐颗弹星 + 逐颗音效,DOM 半边已部分覆盖。
- **估算改动面**:两块都出本窗绘制层边界——①结算流程编排:胜利窗口 0.8s → ~1.7s
  或 `api.onWin` 延迟调度,涉及 `finishSent` 冻结口径与 destroy 竞态(演出中退出/重开);
  ②共享外壳 `dialogs.ts`:全库 39+ 款共用的结算面板,改星级节奏是全库回归面,
  需与既有逐颗音效节奏对齐。建议 1.4 作为「结算面板演出统一升级」专项做,不宜单款顺手改。

## BL-W4-03 · duo-vs-star 玩法态顶栏返回/暂停键高度 32px（W4R3-01）

- **来源轮次**:r3 tester 终验新发现（一般）；r2 fixer 只把七颗触控键 `min-width` 修回 40px（W4R2-05），顶栏 `◀ 返回 / ⏸ 暂停` 未动。A 档终验结论「带条件放行」的唯一条件。统筹不改 `src/games`/`src/ui`，书面并入 backlog，不另派第 4 轮。
- **位置**:`src/games/duo-vs-star/index.ts` 内嵌 CSS `.dvs-back`（`padding:7px`、无 `min-height`）。钉住断言：`src/games/__tests__/window4-visual-scan-r3.test.ts`。
- **期望**:触区高度回到 ≥40px（tester 建议一行 `min-height:40px`），360/320 双档零溢出、与七键 40px 底线一致；修后把 r3 扫描断言取反为修复态。
- **估算改动面**:单款 CSS 一行 + 取反 1 例断言；需复测顶栏与七键在 360×640 / 320×568 不重叠、不溢出。属 1.2 存量 DOM 触区族，可与 BL-W4-01 同批做。
