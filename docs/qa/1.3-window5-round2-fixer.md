# 1.3 窗口 5 · 第 2 轮视觉验收 · 监督修复员报告

> 角色:窗口 5 第 2 轮**监督修复员**(方法对齐 1.3 第 28 步 C 档,范围收窄为本窗 10 款)。
> 工作分支 `game-1.3-window5`;输入(只读):`docs/qa/1.3-window5-round2-tester.md`(A 档)、
> `docs/qa/1.3-window5-round2-learner.md`(B 档,可能后到)、`docs/qa/1.3-window5-round1-fixer.md`(第 1 轮遗留清单)、
> `docs/plan-1.3-visual-bible.md` 与各款 1.3 提示词规格。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 红线:只动绘制层(绘制函数 / CSS / 视觉常量 / 各款自有视觉文件);判定、数值、关卡数据、存档 key、玩法断言、parentAuth / parentGate 一律不碰;
> `src/art/kit/` 已有文件只 import 不改一字;tester / learner 报告一字不碰;用例只增不减、ratchet 只紧不松。

## 〇、修复计划(动手前立项)

按「第 1 轮不彻底 / 回归 → 本轮阻断 → 严重 → B 档一致性点名 → 一般 → 建议」排序。
A 档 R2 对账:已修 14 / 不彻底 1(G3 座位标半项 = N1)/ 回归 0 / 合理遗留 6;新发现 阻断 0 / 严重 0 / 一般 2(N1/N2)/ 建议 2(N3/N4)。
B 档报告未到,先按 A 档 + 第 1 轮遗留动手;B 档 push 后 fetch/rebase 补一致性点名项(仍限绘制层)。

| # | 来源 | 款 | 问题 | 修法(全部只动绘制层) |
| --- | --- | --- | --- | --- |
| 1 | N1(= R1 G3 修复不彻底,**最优先**) | snow-fight | 座位标 🌸⭐ 与暖手 🔥 共 2 处画布 `fillText`(3 枚 emoji 字形)仍在 `index.ts:552-556` | `paint13.ts` 新增 `paintSeatMark`(seat0 五瓣小花 / seat1 五角星,kit `star` 只 import)与 `paintWarmFlame`(两停火苗);字形退场后画布 fillText 3→1 只剩风旗功能字;座位判定与 `P_MARK` 的 DOM 功能文字语义不动 |
| 2 | N2(一般) | prince-princess | 门锁 = 🔒 emoji 字形 24×scale px 直出(iff 同类 G2 已修的漏网) | `visual13.ts` 新增 `drawPadlockBadge` 参照 iff `drawPadlock` 思路自绘挂锁开 / 合两态(圆环锁弓 + 圆角体 2 停 + 锁孔),替换 `index.ts:611` 一处画法 |
| 3 | N3(建议) | bumper-cars | G10 足额落地但第 1 关浅粉地板上反射斑观感仍含蓄(中心 alpha 0.16 档) | `visual13.ts drawFloorGlow` 中心 alpha 提一档(0.16→0.24,加中途停)+ 斑心一枚更小的内核亮斑;三块调用点位置 / 半径 / 椭圆几何不动,保持「冰面不画花」 |
| 4 | N4 + G4/L-1(建议 + 第 1 轮遗留) | prince-princess | 画布 `emoji()` 助手 11 调用点:门锁(=N2)、状态小 icon(盾 / 羽毛 / 箱 / 翅 / 风)、指路 🤴👸、小怪克制 ⚔️、BOSS 弱点 ⚔️、敌人退场 ✨、12 种粒子飘字 | `visual13.ts` 新增小徽章画笔(剑 / 盾 / 翅 / 风 / 羽毛 / 木箱 / 迷你王冠与蝴蝶结)+ 事件粒子矢量表(12 种全矢量),`emoji()` 助手整体退休;DOM 出场卡 / HUD chips 的 emoji 属功能文字口径,登记遗留交第 3 轮 |

不修(登记遗留):L-2(G6–G8 全库 1.2 基线,窗外统一轮次)、L-3(G9 HUD 横滑观感,信息可达)、
L-4(puff-bros HUD 🍬 DOM 计数功能文字)、L-5(PLAN 文档口径,主管裁决)、pp DOM 出场卡 / HUD chips emoji(功能文字口径,随 L-4 同批)。
性能:A 档 R2 实测 10 款 4× 节流 52.5–60.2fps 全部 ≥30,本轮先复测被改款,只要仍 ≥30 不砍特效。

每修一处一个 commit(`fix(visual-r2): <款> <问题>`),配套视觉用例只增不减,不放宽 R2 测试员刚收紧的 ratchet
(snf emoji≤55 / fillText≤3;pp emoji≤59 / fillText≤4 / emoji()调用点≤11 / 🔒≤1;修后只降并在新用例里再收紧)。
收尾:fetch → rebase(保留他人文件)→ `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟 →
合并态 SHA / 用例数(对比第 1 轮合并态 740/15365、tester 后 741/15367 只增不减)/ 构建体积入本报告 → 普通 push `origin game-1.3-window5`。
禁 force、不动 `main` / `game-1.2` / `game-1.2-kk` / `game-1.3` 主线、不用 `gh` 开 PR。

(修复清单、遗留处理结果、一致性统一化、性能对比、合并态复验与第 2 轮结论 —— 修复完成后逐节补齐。)
