# 1.3 窗口 5 · 第 2 轮视觉验收 · 监督修复员报告

> 角色:窗口 5 第 2 轮**监督修复员**(方法对齐 1.3 第 28 步 C 档,范围收窄为本窗 10 款)。
> 工作分支 `game-1.3-window5`;输入(只读):`docs/qa/1.3-window5-round2-tester.md`(A 档)、
> `docs/qa/1.3-window5-round2-learner.md`(B 档,中途到齐)、`docs/qa/1.3-window5-round1-fixer.md`(第 1 轮遗留清单)、
> `docs/plan-1.3-visual-bible.md` 与各款 1.3 提示词规格。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 红线:只动绘制层(绘制函数 / CSS / 视觉常量 / 各款自有视觉文件);判定、数值、关卡数据、存档 key、玩法断言、parentAuth / parentGate 一律不碰;
> `src/art/kit/` 已有文件只 import 不改一字;tester / learner 报告一字不碰;用例只增不减、ratchet 只紧不松。

## 〇、修复计划(动手前立项,commit `b7bfbfa`)

按「第 1 轮不彻底 / 回归 → 本轮阻断 → 严重 → B 档一致性点名 → 一般 → 建议」排序。
A 档 R2 对账输入:已修 14 / 不彻底 1(G3 座位标半项 = N1)/ 回归 0 / 合理遗留 6;新发现 阻断 0 / 严重 0 / 一般 2(N1/N2)/ 建议 2(N3/N4)。
B 档报告后到:先按 A 档 + 第 1 轮遗留动手,B 档定稿(`707e448`)push 后 fetch/rebase 把一致性点名补进修复(仍限绘制层)。
每修一处一个 commit(`fix(visual-r2): <款> <问题>`),配套视觉用例只增不减,不放宽 R2 测试员刚收紧的 ratchet。

## 一、修复清单(逐项登记,SHA 为收尾 rebase 后的合并态值)

| # | 来源 / 严重度 | 款 | 位置 | 修法(全部只动绘制层) | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **N1 = R1 G3 修复不彻底**(一般,最优先) | snow-fight | `paint13.ts` 新增两笔;`index.ts` drawFighter 头顶字形段 | 座位标与暖手从画布 fillText emoji 字形换自绘徽记:`paintSeatMark`(seat0 五瓣小花·顶瓣与左上瓣受光提亮 + 奶黄花心高光点;seat1 五角金星·kit `traceStar` 只 import + `ballGradient` 三停)、`paintWarmFlame`(外焰两停渐变火苗 + 奶黄内焰);圆心取原字形占位同处,静态识别件与原 emoji 同性质,reduced 无需分支;座位判定与 `P_MARK` 的 DOM 功能文字语义不动 | `visualFixR2.test.ts` +5 例(两队形状通道 arc 数拉开 / 渐变在场 / 不抛 / **fillText 闸 3→1、emoji 闸 55→53 再拧紧**) | `1bc6306` |
| 2 | **N2**(一般) | prince-princess | `visual13.ts` 新增 `drawPadlockBadge`;`index.ts:611` | 门锁 emoji 字形 24×scale px → 自绘挂锁(圆环锁弓 + 金 2 停圆角锁体 + 锁孔 + 短槽),开门锁弓向右上掀起、合门扣死,开 / 合两态锁弓不同位;思路对齐 iff `drawPadlock`,几何在 pp 自有文件重写不跨款 import | `visualFixR2.test.ts` +3 例(两态不同位 / 极小尺寸不炸 / **锁字形闸 1→0**) | `aee6859` |
| 3 | **N3**(建议) | bumper-cars | `visual13.ts drawFloorGlow` | 反射斑中心 alpha 0.16→0.24 提一档 + 斑心一枚同倾角纯色迷你内核亮斑(0.34r);椭圆几何(r×0.62r·倾角 −0.5)与 render ① 段三处调用点(0.3/0.22/0.18×min)一个数不动,「冰面不画花」保持 | `visualFixR2.test.ts` +3 例(alpha 档位 / 几何与调用点钉死 / 不抛) | `9f789d1` |
| 3b | **N3 回压修订**(性能) | bumper-cars | 同上 | 4× 节流 A/B 实测:初版给大椭圆径向渐变加 0.55 中途停回压 ~7fps(41.7→34.5)——撤停保两停,保留 0.24 提档与纯色内核亮斑(实测成本 ~2fps),复测 39.7fps 回到基线带 | 用例改钉「大椭圆径向渐变只许两停」防线(数量不减) | `7be3fd5` |
| 4 | **N4 + G4/L-1**(建议 + R1 遗留,画布部分) | prince-princess | `visual13.ts` 新增 7 支徽章画笔 + 12 种事件飘图;`index.ts` 10 处调用点 | 画布 `emoji()` 助手**整体退休**:护盾 → `drawShieldBadge`(盾形 2 停 + 白星)、滑翔羽毛 → `drawFeatherBadge`、推箱 → `drawCrateBadge`(push 规范表同款木色 + 十字扎带)、二段跳翅 → `drawWingBadge`(三羽瓣)、移动平台风 → `drawGustBadge`(三道圆头风弧)、小怪克制与 BOSS 弱点剑 → `drawSwordBadge`(两停刃 + 金护手)、掉队指路王子/公主 → `drawRoyalBadge`(白圆牌 + 复用 `crownPath` 金三齿冠 / `bowShape` 粉蝶结,与主角识别件同一套形状语言,不新造小人)、12 种粒子飘字 → `drawEventBadge`(星光/宝石/盾/云/旗/桥/晕环/禁行/爆花/礼花/风/翅,全部两停渐变 + kit 描边);`Particle.text` 字段改 `art`(渲染侧账本,纯视觉);淡出 / globalAlpha / reduced 通道原样 | `visualFixR2.test.ts` +5 例(7 支画笔与 12 种飘图逐个不抛 / 12 种 op 流两两不同 / 指路两角色剪影不同位 / **emoji() 调用点闸 11→0、emoji 码点闸 59→38、fillText 闸 4→3**) | `da5b04a` |
| 5 | **C-1 = B 档一致性点名排名 2**(建议) | prince-princess | `visual13.ts` 渐变 9 处 | 光照方向归位左上 45°:B 档点名的三处纵向线性渐变(`enemyBodyGrad` 小怪体 / 铠甲盾面 / BOSS 大王冠)按规格拉斜(起左上 −0.18w、终右下 +0.18w,停点 +16/−10 幅度不变);本轮新增徽章的金渐变(挂锁/木箱/指路冠/礼花筒/盾徽/剑刃)同规执行 | `visualFixR2.test.ts` +1 例(三处点名位钉死 + 「零纵向线性渐变」通用正则防线) | `49c5c7c` |

**修复统计(按来源)**:A 档 R1 不彻底 1(N1)、A 档新发现 3(N2/N3/N4)、B 档一致性点名落地 3(排名 1 = N1、排名 4 = N3、排名 2 = C-1 + N2 + N4 画布清零)、R1 遗留清掉 1(L-1 画布部分)。
**按严重度**:阻断 0 / 严重 0 / 一般 2(N1/N2)/ 建议 3(N3 + 回压修订、N4+G4、C-1)。共 6 个修复 commit + 1 个计划 commit。
**新增配套用例 17 例(4 个 `visualFixR2.test.ts` 文件,snow-fight 5 / prince-princess 9 / bumper-cars 3)**,既有用例零删改,tester 的 ratchet 文件一字未碰、全部只紧不松。

## 二、第 1 轮遗留清单处理结果(清掉 1 条,剩 4 条 + 新登记 2 条)

| # | 项 | 处理结果 |
| --- | --- | --- |
| L-1 | pp 能力/状态小 icon ~8 处 + 指路 + 粒子飘字 | **画布部分本轮清零**(修复 #4,`emoji()` 助手退休、调用点 11→0);DOM 出场卡(`pcp-cue-icons`,教学口令先图后字)与 HUD chips 的 emoji 属功能文字口径,转登记为遗留 R2-a |
| L-2 | G6–G8 全库 level99 星标 / 按钮触区 / tank 窄屏小字 | 不动(1.2 基线,全库统一轮次,窗口外)——继续挂账 |
| L-3 | G9 sky-squad / tank-battle HUD 芯片行 360px 横滑观感 | 不动(信息可达,B 档修订清单排下一轮量力)——继续挂账 |
| L-4 | puff-bros HUD 糖果 DOM 计数字 1 处 | 不动(功能文字非画布,B 档排下一轮随 HUD 统一走查)——继续挂账 |
| L-5 | PLAN 文档对标点名词口径 | 不动(主管裁决,C 档未私改任何 PLAN 文档)——继续挂账 |

**本轮新登记遗留(交第 3 轮)**:
R2-a:pp DOM 出场卡 / HUD chips emoji(功能文字口径,与 L-4 同类,若第 3 轮统一走查 HUD 可一并处理);
R2-b:B 档一致性点名排名 3 / 5 —— bowling-lane 粉彩夜场(C-2,暗底提暖或霓虹点二选一)与 tank-battle 炮塔高光弧 + 徽章 strokeOutline(C-3),B 档修订清单自排下一轮,本轮不抢跑。

## 三、一致性统一化(B 档第三节点名 5 款的逐款回执)

| B 档排名 | 款 | 处理 |
| --- | --- | --- |
| 1 | snow-fight 字形贴纸 | **已修**(修复 #1):徽记规格按 B 档给的绘制规格执行(五瓣圆花 + 中心圆点 / `traceStar` 五角金星 / 两停火苗,kit 描边),fillText 3→1 与 B 档预期一致 |
| 2 | prince-princess 纵向光照 + emoji 尾巴 | **已修**(修复 #2/#4/#5):`vGrad`(实名 `enemyBodyGrad`)与盾 / 王冠按 B 档规格拉斜 ±0.18w;门锁照 iff 思路自绘;画布 emoji 全清(超额完成 B 档「出场卡四枚优先」的画布部分);指路替身按 B 档商标提醒用自家王冠 / 蝶结几何,未新造带发型裙色组合的小人;DOM 出场卡登记 R2-a |
| 3 | bowling-lane 灰紫夜场 | **登记 R2-b**(B 档自排下一轮,A/B 方案二选一留给第 3 轮裁决) |
| 4 | bumper-cars 反射斑最弱档 | **已修**(修复 #3/#3b):调参不加料,与 B 档「这是调参不是加料」的定性一致 |
| 5 | tank-battle 工艺代差(轻) | **登记 R2-b**(B 档自排下一轮,克制执行两笔的规格已在案) |

另遵守 B 档商标增量风险提醒:snow-fight 座位标重绘**未**给任何雪人形象加纽扣 / 胡萝卜鼻 / 树枝手组合件(徽记只画花 / 星 / 火苗三件);测试禁词表(负向断言)未删改。

## 四、性能回压(前后帧率对比,CPU 4× 节流)

本探针口径:puppeteer-core + 系统 Chrome headless(软件光栅)、720×1280 DPR2、`Emulation.setCPUThrottlingRate 4×`、
进 `#/game/<id>` 连点开始类按钮进玩法 → 连按输入 5s rAF 计帧;与 A 档探针(52.5–60.2fps)路径不完全相同,**对比只在本探针的 A/B 内做**。

1. **bumper-cars N3 的回压发现与修订**(唯一有帧率代价的改动):
   基线(未含 N3,worktree 构建)41.6–42.0fps → N3 初版(渐变加 0.55 中途停 + 内核亮斑)34.2–35.1fps(**回压 ~7fps,肇因是大椭圆径向渐变加停**)→
   修订版(撤中途停、保 0.24 提档与纯色内核亮斑)39.5–39.7fps。交错四轮复跑数字稳定;内核亮斑 ~2fps 成本保留(视觉等级优先,仍高出门槛 30%);
   配套用例加「径向渐变只许两停」防线。
2. **全 10 款复测(修后合并态,本探针口径)**:shoot-range 60.2 / sky-squad 44.4 / tank-battle 60.0 / bomb-buddies 58.6 /
   snow-fight 60.1 / bumper-cars 39.5 / bowling-lane 58.4 / ice-fire-forest 60.2 / puff-bros 60.1 / prince-princess 60.1 ——
   **10/10 ≥ 30fps,无需再回压**;被改 3 款(snf/pp/bc)中 snf 与 pp 顶满 60,pp 的徽章矢量化零帧率代价。

## 五、合并态复验数据与第 2 轮结论

1. 三人 push 齐:`git fetch origin game-1.3-window5` → rebase(两次,B 档计划 / 文案 / 定稿先后到齐),
   三份报告 `docs/qa/1.3-window5-round2-{tester,learner,fixer}.md` 与全部修复在同一合并态;
   `docs/plan-1.3-*.md`、`.cursor/skills/**`、A/B 档报告与测试一字未碰;
2. **合并态复验 SHA:`49c5c7c8f08263a243469f894c9ca69828157d93`**(全部修复入内;本报告定稿 commit 随后压顶,为最终远端 tip);
3. `npm ci` → `npm test`:**745 文件 / 15395 例全绿**——对比第 1 轮合并态(740/15365)+5 文件 / +30 例,
   对比 R2 tester 定稿(741/15367)+4 文件 / +28 例(修复员 4 文件 17 例 + 学习员 1 文件 11 例),只增不减 ✅;
4. `npm run build` 全绿(tsc + vite):dist 5.6M,PWA precache **195 entries / 5240.35 KiB**
   (对比第 1 轮 194 / 5230.80:+1 entry 来自本轮合并态 chunk 变化,体积 +9.6KiB ≈ **+0.18%,远低于 10% 门槛**);
5. `npx vite preview` 冒烟:首页 / `sw.js` / `manifest.webmanifest` 全 200,本窗 10 款产物 chunk 全 200 ✅;
6. 10 款 chunk 体积(KiB,括号内为 R1 fixer 合并态基线):shoot-range 60.1(60.1)、sky-squad 77.9(77.9)、tank-battle 70.5(70.5)、
   bomb-buddies 81.7(81.7)、snow-fight 68.9(67.7,+1.2K 座位标 / 火苗画笔)、bumper-cars 61.1(60.9)、bowling-lane 47.7(47.7)、
   ice-fire-forest 52.2(52.2)、puff-bros 72.9(72.9)、prince-princess 74.5(68.2,**+6.3K ≈ +9.2%**:7 支徽章画笔 + 12 种事件飘图
   约 500 行矢量绘制换掉 emoji 字形,单款涨幅在 10% 以内,总包涨幅 +0.18%)。

**窗口 5 · 第 2 轮视觉验收结论:通过。**
第 1 轮「不彻底 / 回归」问题 100% 处理(唯一不彻底项 N1 已修;回归 0,无需处理);本轮阻断级 0 残留;
严重级残留 0(≤3 达标);一般级 N1/N2 全修;建议级 N3/N4/C-1 落地、C-2/C-3 按 B 档轮次登记;
`npm test` / `npm run build` / preview 冒烟全绿,用例只增不减,ratchet 只紧不松。

## 六、kit 纪律与红线自查

- `src/art/kit/` 已有文件**只 import 未改一字**(本轮新增 import:snow-fight `paint13` 与 prince-princess `visual13` 各引 kit `star.traceStar`);
  未新增 kit 文件——本轮星形复用既有 kit `star`,五瓣花 / 挂锁 / 徽章组各只有单款消费,不到「重复轮子收敛」门槛,不为收敛而收敛;
- 新画笔全部落在各款自有 `paint13.ts` / `visual13.ts` 内并各带 `visualFixR2.test.ts` 单测(谁建谁有单测);禁 three.js 遵守(零依赖新增);
- 玩法红线:判定、数值、关卡数据、存档 key、玩法测试断言零改动——唯一的非绘制函数改动是 pp 渲染侧账本 `Particle.text → art`
  字段改名(粒子只被 render 读写,`git diff` 可核);`parentAuth` / `parentGate` 零触碰;
- 测试红线:tester 的 `visualScan13` ×10、`visualScanR2`、fixer R1 `visualFix13` ×9、learner `copy13` / `copyW5R2` 全部在位在跑,
  阈值一个没放宽;本轮全部收紧动作(snf fillText≤1 / emoji≤53,pp 锁字形 =0 / emoji() 调用点 =0 / emoji≤38 / fillText≤3,bc 两停闸)写在自己新增的用例文件里;
- 分级红线:新增绘制全部 Q 版粉彩(徽章 / 花 / 星 / 火苗 / 挂锁),无血 / 无伤害 / 无死亡描写(pp「爆花」为八角星 + 白心,无火焰烟尘);
  失败反馈沿用原鼓励语义;无广告无内购;无商标与官方角色(B 档增量风险提醒逐条对照执行);
- 分支纪律:只普通 push `origin game-1.3-window5`;未动 `main` / `game-1.2` / `game-1.2-kk` / `game-1.3` 主线;未 force;未用 `gh` 开 / 改 PR。
- 派发指定模型 slug:`claude-fable-5-thinking-xhigh`(实际使用)。
