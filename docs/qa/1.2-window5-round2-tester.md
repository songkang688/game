# 窗口5 · 第2轮测试员

> 本文件由三名测试员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏，不验 76 款全集，不验窗口 1 的平台文件、窗口 2 的点名五项。

## 全量跑

（本节由档A 先落笔，档B / 档C 收到后各自补自己的复跑数字。）

### 档A 第 2 轮的全量跑

| 项 | 数字 |
| --- | --- |
| `npm test` | **212 个测试文件 / 5518 条用例，全绿**（收尾 rebase 到 `37378eb` 之后重跑） |
| `npm run build` | **全绿**，主包 `index-*.js` **86.80 kB**（gzip 30.50 kB），PWA 预缓存 139 项 / 3300.37 KiB |
| 构建告警 | 仍有一条 `[INEFFECTIVE_DYNAMIC_IMPORT]`（`src/ui/parentAuth.ts` 被 `parentGate.ts` 静态引用）——就是第 1 轮的 **W5-A-06**，属 `src/ui/**`，本档禁改，**交给窗口1**，本轮一行没动 |
| 测试基线 | 走查起点 `0d8d15d` 是 204 / 5444；收尾 rebase 到档B 第1轮监督修复员的 `37378eb` 之后变成 212 / 5518，多出来的 8 个文件 / 74 条用例全部来自档B（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`），**档A 本轮没有改任何产品代码**，本档五款的用例数一条没变 |

跑法：本轮所有交互结论都来自 **CDP 真实输入**（`Input.dispatchMouseEvent` / `dispatchTouchEvent` / `dispatchKeyEvent`），
命中判定一律 `document.elementFromPoint(点)` 再比对目标元素，**全程没有用过 `el.click()`**
——这是第 1 轮监督修复员点名的口径（程序化 `click()` 点得着被舞台裁掉的元素，量不出「点不着」）。
纯数据结论（难度曲线、教育正确性、摘要锁边界）另用一次性 `vitest` 探针直接调仓库自己的纯函数算，
探针跑完即删，**没有留在仓库里**。

### 档B 第 2 轮的全量跑

**窗口号 = 5　角色 = 第 2 轮 · 档B 测试员（换样本）　实际模型 slug = `claude-opus-5-thinking-high-fast`　收尾 SHA 基线 = `4904275`**

| 项 | 数字 |
| --- | --- |
| `npm test` | **224 个测试文件 / 5687 条用例，全绿**（收尾 rebase 到 `4904275` 之后重跑） |
| `npm run build` | **全绿**，主包 `index-CO3Ps6tO.js` **86.80 kB**（gzip 30.50 kB），PWA 预缓存 139 项 / 3315.20 KiB |
| 构建告警 | 同档A 那一条 `[INEFFECTIVE_DYNAMIC_IMPORT]`（`src/ui/parentAuth.ts`）——属 `src/ui/**`，本档禁改，**交窗口1**，本轮一行没动 |
| 测试基线 | 走查起点 `bdd649a`（212 / 5518）；收尾 rebase 到 `4904275` 之后是 224 / 5687。多出来的 12 文件 / 169 用例来自档A 第 2 轮学习优化员与档C 的 `kitty-care` 修复，**档B 本轮没有改任何产品代码，也没有新增 `*.test.ts`**，本档五款的用例数一条没变 |
| 档B 问题计数 | **阻断 1 / 严重 1 / 一般 2 / 建议 1** |

档B 本轮**一行产品代码都没动**：五款没有出现白屏 / 起不来这一类允许打补丁的情形，唯一的阻断（W5-B-08）落在布局自救上，属玩法/布局代码，按边界**只登记不动手**。教育正确性复算用的一次性 `vitest` 探针**跑完已删**，不进提交。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 走查计划（动代码 / 动报告前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 2 轮** · 档A 测试员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`0d8d15d`（走查起点）；收尾 rebase 到 `37378eb`（档B 第 1 轮监督修复员报告）
**模型 slug**：`claude-opus-5-thinking-high-fast`
**跑测环境**：Chrome 148 headless + Node 22 自带 WebSocket 直连 CDP，零新增依赖（沿用第 1 轮口径）
**范围**：只验本档五款。不验 76 款全集，不验窗口 2 点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`），不验 root 门 / 手游端游筛选 / 2.5D 基建（窗口 1 的活）。

**读过的输入（第 1 轮三份报告的档A 节）**

| 文件 | 档A 结论 |
| --- | --- |
| `docs/qa/1.2-window5-round1-tester.md` | 阻断 0 / 严重 0 / 一般 5（W5-A-01…05）/ 建议 2（W5-A-06、W5-A-07，交窗口1） |
| `docs/qa/1.2-window5-round1-learner.md` | 十条改进，落地 5 条（W5-L-01…05），留第 2 轮 3 条（W5-L-08 / 09 / 10），交窗口1 两条（W5-L-06 / 07） |
| `docs/qa/1.2-window5-round1-fixer.md` | 补修 4 条（W5-F-01…04）+ 补 `red-blue-race/shell.test.ts`；收尾发现阻断 **W5-F-07**（`.game-stage overflow:hidden` 硬裁）；留第 2 轮 W5-F-05 / W5-F-06 |

**本轮要逐条复测的第 1 轮遗留**

| 编号 | 级别 | 本轮要做什么 |
| --- | --- | --- |
| W5-F-07 | 阻断 | 平台那一半交给窗口1，**本轮复测是否还在**：`color-fun` 调色板、`red-blue-race` 三颗触屏键。必须用真实坐标 / `elementFromPoint`，**不许用 `el.click()`** |
| W5-A-01…05 | 一般 ×5 | 确认没回潮（钟面标签、题干自指、三处 44px 热区、画室 `inert`、画室 Esc） |
| W5-F-01…04 | 严重 1 + 一般 3 | 同上，逐条回归 |
| W5-L-08 | 建议 | `red-blue-race` 战役对手局内动态难度——本轮量化难度曲线，给口径 |
| W5-L-09 | 建议 | `clock-house` 错题回顾链的可回放覆盖——本轮实测这条链 |
| W5-L-10 | 建议 | `landlord-cards` 三档牌力提示「不泄底」的口径——本轮实玩三档定口径 |
| W5-F-05 | 建议 | `word-garden` 13 道题指令语带答案字（受前 99 关摘要锁约束）——本轮复算并定口径 |
| W5-F-06 | 建议 | `clock-house` 摘要锁的边界——本轮复核两条锁合起来严不严 |

**本轮换样本的原则（第 2 轮的核心要求）**

1. **不许只玩第 1 关。** 每款换一批关卡：战役取**中段与末段**（40 / 90 / 140 / 170 附近），外加 100 / 188 若能开。第 1 轮已经把 1 / 100 / 188 走过一遍，本轮的新样本落在 40 / 90 / 140 / 170。
2. 有无尽 / 双人 / 沙盒的**都要再玩到结算**（换打法，不重复第 1 轮的路径）。
3. **难度曲线**：量化每关通关耗时与失败次数，判断有没有跳崖、后期是不是纯数值重复。
4. **竞态**：快速连点、快速进出 10 次、暂停恢复、切后台。查崩溃与 `destroy`。
5. **教育正确性**：`clock-house` / `word-garden` **换一批题**再核；`color-fun` 混色结论再核。
6. **视口**：360px 与 390×844 两档都走。热区判定一律 `document.elementFromPoint(中心点)`，不用程序化 `click()`。

**严重度口径**：阻断 = 白屏 / 点不着 / 教错 / 泄题 / 必现崩溃；其余按严重 / 一般 / 建议记。

**权限边界**：本轮**不改玩法代码**。只有白屏 / 起不来这类阻断才允许在本档五款目录内打最小补丁并补回归测试。不许改 `src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，以及本档以外的任何游戏目录。不写 learner / fixer 报告，不开第 3 轮。别人的报告一个字都不碰。

**收口结果**：走查计划全部执行完，**产品代码一行没动**（五款都没出现白屏 / 起不来，最小补丁的触发条件没成立）。本节余下内容是实测记录。

---

### A1 · 这一轮到底换了哪些关、玩到哪一步

第 1 轮走的是 1 / 100 / 188。本轮把主样本换到 **40 / 90 / 140 / 170**，100 / 188 只作为末段对照保留。
进度用 `localStorage` 的 `yiduo-yixing.l99.<id>` 播种解锁，然后**从关卡地图上用真实坐标点节点**进关，不走内部 API。

| 游戏 | 战役换的关 | 结果 | 关卡外模式 |
| --- | --- | --- | --- |
| `clock-house` | 40 / 90 / 140 / 170 / 100 / 188 | 六关**全对通关 ★★★**，5 / 5 / 9 / 6 / 6 / 10 题，耗时 6.7 / 6.7 / 11.1 / 7.8 / 7.8 / 12.3 秒 | 无（`meta` 只声明 campaign，与实现一致）；关末「📒 错题回顾」轮实测触发 |
| `word-garden` | 40 / 90 / 140 / 170 / 100 | 五关**全对通关 ★★★**，5 / 5 / 6 / 9 / 6 题，耗时 6.7 / 6.7 / 7.8 / 11.1 / 7.8 秒 | 第 188 关是**组字工坊**、第 105 关是**笔顺描红台**，两种非选择题形态单独走（见 A2） |
| `color-fun` | 40 / 90 / 140 / 170 / 188 | 五关**全涂完 ★★★**，四种模式各一关（mix / memory / rule / limited ×2），耗时 6.4 / 4.2 / 3.8 / 8.0 / 13.7 秒 | 🎨 自由涂色画室走完开→涂→存→关全程 |
| `landlord-cards` | 40 / 90 / 140 / 170 / 188 | 40 与 140 **★★★ 赢下**；90 / 170 / 188 照着教练提示打**输**（「就差一点点！」）——这是牌局本身的胜负，不是缺陷，量化见 A3 | ♾️ 无尽连胜 ✅ 走到结算；⚔️ 双人对战 ✅ 走到结算（29 次换人幕） |
| `red-blue-race` | 40 / 90 / 140 / 170 / 188 | 40 / 90 / 140 **★★★ 赢下**（531 / 641 / 480 步，8.2 / 9.5 / 7.4 秒）；170 / 188 用「砸满手速不换气」这套打法**输**在 87 / 88 米（体力条机制，量化见 A3） | ♾️ 跑不完的跑道 ✅ 两种打法各走一趟；🤝 对战场·本地两人 ✅ 走到结算 |

**换打法再走一遍结算**（第 2 轮要求的「不重复第 1 轮路径」）：

- `red-blue-race` 无尽跑道：**「只跑不跳」** 14.2 秒 / 1093 步 → **174 米新纪录**，结算给「送你 1 颗小星星」，两颗按钮 🔁再跑一趟 / 🗺️回关卡 都在。另一趟**「见坑就跳」**跑满 91 秒 / 6950 步 / 511 次起跳仍未收工（设计上撞满 3 次才结束）——见 W5R2-A-09。
- `red-blue-race` 对战场：本地两人，朵朵敲得比星星密，结算「**朵朵 先冲线!**」100 : 92 米，记分牌 🔴 1 : 0 🔵。
- `landlord-cards` 无尽连胜：连开三局，每局都打到结算「这一局被拦下来啦」。第 1 轮走的是「赢一局落 `endlessBest:1`」，本轮走的是**输了之后重开**这条路——发现 W5R2-A-04。
- `landlord-cards` 双人对战：29 次「🙈 轮到 XX 啦 / 我准备好了」换人幕全部正常盖牌，结算「🏆 星星赢啦!」，总比分 朵朵 0 · 星星 1，亮点句「10 手就把牌走干净」。
- `color-fun` 画室：涂 → 存 → 画廊 → 点开旧作 → 接着涂，全程 0 异常。

### A2 · `word-garden` 两种非选择题形态

第 1 轮只提过「描红台在 102/105/108/111/114/117 关，实测第 102 关可用」。本轮把两种形态都真的走了一遍。

**笔顺描红台（第 105 关）**：读出田字格里下一笔的引导折线 `.wgd-next` 的 `points`，插值成一条连续轨迹，用 `touchStart/Move/End` 真描。
「月」四笔一笔不差：`第 1 笔「撇」写好啦！` → `第 2 笔「横折钩」` → `第 3 笔「横」` → `第 4 笔` 后弹「**「月」4 笔一笔不差，花园里开出一朵花！**」。
笔画名与「月」的标准笔顺（撇、横折钩、横、横）**逐笔对上**。描歪了只说「描错顺序也没关系，我们再来一次～」，不判失败——口径没问题。

**组字工坊（第 188 关）**：两步式（先挑偏旁、再挑另一半），反馈分得很清楚：
挑对偏旁挑错另一半时说「**偏旁挑对啦，另一半再看看～**」，两个都对才说「**拼成啦：睛（眼睛）**」。
我用穷举法乱挑，第 4 次错就收工弹「就差一点点！」，容错次数与 `levels.ts` 里 `maxWrong: t > 0.6 ? 3 : 4` 对得上。

### A3 · 难度曲线（量化）

只靠手玩几关看不出「跳崖」和「后期纯数值重复」。这两问改成：**用仓库自己的纯函数把 188 关全跑一遍**，再拿浏览器实测去校准。

**`red-blue-race`**（跑 `logic.ts` 的 `simulateRace`，三档手速各跑 188 关）

| 打法 | 结果 |
| --- | --- |
| 熟练档 `SKILLED_PLAY`（6 次/秒 + 会跳 + 踩鼓点 + 会换气） | **188 / 188 全胜**，一关不输 |
| 封顶手速（7.5 次/秒，`HUMAN_TAP_CAP_HZ`，其余同上） | **188 / 188 全胜** |
| 乱点档 `CASUAL_PLAY`（3 次/秒，不跳不踩点不换气） | 后期章节**全输**——新关不是白送 |

章节余量（熟练档「电脑到线时间 − 我到线时间」的均值）：

```
操场直道 4.02s → 水坑赛道 5.38 → 跨栏赛场 5.22 → 上坡森林 4.18 → 夜跑霓虹 4.47
→ 冠军巡回 3.85 → 云顶接力 2.90 → 拾光集市 4.55 → 节拍风廊 2.99 → 决胜星轨 2.96
```

整体是**单调收紧**的，没有「后期忽然做不到」。逐关看，余量比上一关掉超过 1.2 秒的有 7 处，都落在章节交界上（88 / 145 / 167 / 172 / 176 / 181 / 185），最深的两处是 **第 145 关（节拍风廊首关）5.23s → 0.98s** 和 **第 167 关（决胜星轨首关）5.17s → 2.28s**。145 那一关 0.98 秒的余量是全 188 关最小值——记 W5R2-A-07（建议）。
**末 22 关的配置零重复**（`aiSpeed|tapStep|机关表` 去重后一条不少），不存在「后期纯数值抄一遍」。

浏览器实测的 170 / 188 之所以输，是我这套机器人打法**每秒砸 70 下、从不松手换气**：这两关属云顶接力之后带体力条的章节，`staminaStepFactor` 会把步长按体力打折，而 `cadenceFactor` 又把超过 7.5 次/秒的部分按比例折掉（连点 80 下只推进 4 米，见 A5）。**「砸键 = 最优解」这条路被堵死了，这是 1.2 的设计意图，不是缺陷。**

**`landlord-cards`**（让「一个完全照着 💡 教练提示打的玩家」把 188 关各打一局）

| 章节 | 跟提示的胜率 | 均星 |
| --- | --- | --- |
| 客厅小牌桌 | 24 / 24 | 2.79 |
| 樱花棋牌屋 | 23 / 24 | 2.54 |
| 云顶茶话会 | 18 / 24 | 1.71 |
| 月光甲板 | 16 / 24 | 1.58 |
| 冰晶大厅 | 15 / 23 | 1.26 |
| 星火竞技场 | 14 / 23 | 1.13 |
| 彩虹擂台 | 12 / 23 | 1.00 |
| 王冠塔顶 | 9 / 23 | 0.61 |

合计 **131 / 188**。曲线单调下探，没有断崖。关键是**输了不会卡死**：`index.ts` 每失败一次就 `bump++` 换一副牌，我把 57 个输掉的关各连着换 6 副重打，**每一关都在 5 次以内翻到能赢的牌**，其中 187 关是「连输 ≤2 次」，只有**第 188 关要连输 5 次**才发到能赢的一副——记 W5R2-A-08（建议）。

**`clock-house` / `word-garden`**：题量随章节递增（5 → 6 → 9 → 10 题），全对通关耗时 6.7 → 12.3 秒，线性上升，没有跳崖。
容错走 `quiz99.ts` 的 `defaultMaxWrong(total) = max(3, ⌈total/8⌉)`，故意连错到判负两款都稳定弹「就差一点点！这一关的题目有点调皮，我们休息一下再来一次！」，措辞不评价孩子。

**`color-fun`**：四种模式的耗时 3.8–13.7 秒，随任务数（5 → 6 → 8 个色块）与开锅次数上升。源码零 `ctx.lose`，**设计上不判负**——实测涂错 8 次只把计数条写成「✨ 改过 8 次，没关系」，进度仍是 0/6，↩️撤销可用且不倒扣。这一条与第 1 轮结论一致。

### A4 · 教育正确性（换一批题重核）

#### 🔴 `clock-house` 12 时制 ↔ 24 时制的时段词（**本轮新发现，严重**）

`levels.ts` 里 `PERIODS = ["上午", "下午"]`，`to12Hour()` 走的是「0–11 → 上午 / 12–23 → 下午」这个纯 AM/PM 二分。
把 188 关的 `h12`（60 道）与 `h24`（62 道）全部按小学《24 时计时法》的口径逐题核，**78 道对不上，落在 46 关**：

| 说法 | 道数 | 例子 |
| --- | --- | --- |
| 说成「下午」，课本是**晚上** | 14 + 13 | L126 `22:15` → 「下午 10:15」；L139 题面写「下午 10:15」→ `22:15` |
| 说成「上午」，课本是**凌晨** | 13 + 13 | L128 `02:20` → 「上午 2:20」；L137 题面写「上午 1:15」→ `01:15` |
| 说成「上午」，课本是**早上** | 6 + 10 | L127 `06:00` → 「上午 6:00」；L125 题面写「上午 7:45」 |
| **0 点说成「上午 12 点」** | 2 + 3 | L132 `00:30` → 「**上午 12:30**」；L146 题面写「**上午 12:40**」→ `00:40` |
| **12 点说成「下午 12 点」** | 2 + 2 | L172 `12:10` → 「**下午 12:10**」；L128 题面写「**下午 12:20**」 |

前三类（69 道）还能辩解成「教的是 AM/PM 二分，不是七段时段词」。
但最后两类共 **9 道是硬错**：中文里没有「上午 12 点」（0 点是**凌晨 / 半夜**），也没有「下午 12 点」（12 点是**中午**）；
换成英文口径同样错——12:30 AM 是午夜，不是 morning。
这是一款专门教读钟与时制换算的游戏，**教出来的说法孩子在课堂上会被判错**。记 **W5R2-A-03（严重）**。

`clock-house` 其余题型（`read` / `next` / `arrive` / `depart` / `span` / `spanNoon` / `zone` / `weekday` / `monthdays` / `nthday` / `unitHM` / `unitMS` / `unitMix` / `set` / `setMin` / `routine`）全部**独立重算过一遍答案**，**0 处算错**，选项重复 0、正确项唯一。

#### `word-garden`

188 关 **1181 道**题：`correct` 指错 **0**、三个选项重复 **0**、正确答案在选项里不唯一 **0**。
第 1 轮 W5-A-02 点名的「题干自身写着答案」那三类（成语释义 / 偏旁义项词 / 形近字提示句）本轮 **0 命中**，没有回潮。

#### `color-fun`

22 条混色配方逐条复核：三原色两两相混（红+黄=橙、蓝+黄=绿、红+蓝=紫）**全对**；加白一律变浅、加黑一律变深，RGB 亮度序**全部单调**；
`rule` 模式 188 关的互补色 / 邻近色判定**全对**，每一关的目标色都能从当前调色板里调得出来（可达性 188/188）。
两处口径瑕疵记成建议（W5R2-A-10）：「浅绿 + 绿色 = 中绿」这条的亮度落在两个原料之间、方向上不如别的配方直白；「同色 + 同色 = 更深」（红+红=深红、黄+黄=金黄）是游戏化近似，物理上同色相混不会变深。两条都不影响过关，也不教错主干规则。

#### `clock-house` 拨一拨（`setMin`）

第 149 关第 6 题走到了拨针题：题面「🔧 拨到 5 点 10 分」，起始钟面 4 点 45 分。
用真实手指沿钟面绕了一圈，读数条与 `aria-label` **实时同步**跟到「5 点 48 分」。
这个可拖钟面是**练手件，不参与判分**——判分仍在下面三个钟面选项上（选项的标签是不含时刻的「钟面」）。
`a11yLabels.test.ts` 里写明的「拨一拨那个练手钟面是例外中的例外」在实机上确认属实，**不构成泄题**。

### A5 · 竞态与 `destroy`

五款一律：进关 → 关键控件连点 → 快速进出 10 次 → 读残留。全程收 `error` / `unhandledrejection` / `console.error`。

| 游戏 | 连点 | 快速进出 10 次的残留 | 异常 |
| --- | --- | --- | --- |
| `color-fun` | 色板 30 下 + 色块 30 下，舞台还在，进度与「改过几次」都没错乱 | 舞台 0 / 画室 0 / `game-host` 0；window 上的 `keydown` / `resize` / `scroll` 监听全部**回到 0** | 0 |
| `red-blue-race` | 左右脚 60 下 + 跳 20 下，**只推进 4 米**（`cadenceFactor` 封顶生效，砸键不加速） | wrap 0 / host 0；监听回 0；rAF 残留 14 帧后自停。**卸载之后再敲 10 次键盘，0 异常**（两套键位一次卸干净） | 0 |
| `landlord-cards` | 💡提示 12 下 + ✅出牌 12 下，牌桌还在，手牌数正常 | wrap 0 / host 0 / `ldc-fly-layer` 0；监听回 0；rAF 0 | 0 |
| `clock-house` | 同一个选项连点 25 下，**不重复计错、不提前结算** | host 0 / 选项容器 0；监听回 0；rAF 0 | 0 |
| `word-garden` | 同上 | 同上 | 0 |

**暂停 / 恢复**（`landlord-cards`）：⏸暂停 → 出现「⏸ 先歇一会儿 · 牌都给你留着」遮幕 → 按 `Esc` 能恢复 → 按 ▶️继续玩 也能恢复，牌一张不丢。
**切后台**：`color-fun` / `red-blue-race` 用 hash 反复进出模拟前后台切换，`AudioContext` 恒为 1，没有越切越多。

### A6 · **W5-F-07 复测：还在，而且量得更死了**

第 1 轮的结论是「舞台硬裁，程序化 `click()` 点得着、真手指点不着」。本轮把「用户到底能不能自己滚出来」这一格**用真手势补上了**：

在 `color-fun` @390×844 第 40 关上，**连做 3 次 `touchStart→touchMove×12→touchEnd` 上滑手势 + 3 次滚轮**之后：

```
.game-stage  overflow-y: hidden   clientHeight 730   scrollHeight 1093   scrollTop 0 → 0
页面本身      document.scrollHeight 844 == innerHeight 844      window.scrollY 0
程序化 st.scrollTop = st.scrollHeight  ⇒ 设得进去（363），点不着的从 10 个降到 4 个
```

也就是说：**内容是渲染出来了的，只是被裁在视口外，而用户没有任何手势能把它滚出来**。三档视口逐关量（命中判定全部 `elementFromPoint(中心点)`）：

| 视口 | 游戏 | 40 / 90 / 140 / 170 关点不着的按钮数 | 舞台溢出 |
| --- | --- | --- | --- |
| 390×844 | `color-fun` | **10 / 8 / 8 / 10** | 363 / 194 / 240 / 442 px |
| 390×844 | 其余四款 | 0 / 0 / 0 / 0 | ≤111 px |
| 360×720 | `color-fun` | **10 / 8 / 8 / 10** | 452 / 283 / 327 / 562 px |
| 360×720 | `red-blue-race` | **1 / 1 / 1 / 3** | 197 / 197 / 185 / 281 px |
| 360×720 | `landlord-cards` / `clock-house` / `word-garden` | 0 | ≤63 px |
| 360×640 | `color-fun` | **11 / 8 / 9 / 11** | 488 / 319 / 363 / 598 px |
| 360×640 | `red-blue-race` | **3 / 3 / 3 / 3** | 277 / 277 / 265 / 361 px |
| 360×640 | `landlord-cards` | **1 / 1 / 1 / 1**（⏸ 暂停） | 125 / 125 / 143 / 143 px |
| 360×640 | `clock-house` / `word-garden` | 0 | ≤83 px |

点不着的都是些什么（`color-fun` @390×844 第 40 关的完整名单）：

```
↩️ 撤销 85x44 @y905      ↪️ 重做 85x44 @y905
调锅三原色  红色 / 黄色 / 蓝色  44x53 @y1020
调色板      红色 / 棕色 / 粉色 / 蓝色 / 黄色  48x69 @y1099
```

**调色板整排点不着 = 选不了颜色 = 涂不了色 = 这一关过不去。** 这是阻断，第 1 轮的判定成立且没有改善。

`red-blue-race` @360×720 第 170 关另有一个第 1 轮没记的表现：**舞台一进关就自己滚到 `scrollTop=143`**（焦点带的），
把 `🤝 对战场` / `♾️ 跑不完的跑道` / `🗺️ 选关` 顶到 `y < 0`，这三颗也一起点不着；`🦘 跳` 仍在下边界之外。
也就是说这一档视口上**触屏玩家既开不了别的模式、也跳不了**，只有键盘还能玩。

`clock-house` / `word-garden` 在**三档视口 × 40/90/140/170/188** 上一颗都不缺，与第 1 轮一致。

**根因仍在两个禁改文件上**（`src/styles.css` 的 `.game-stage{overflow:hidden}` 与 `level99.ts` 的 `.l99-stage-wrap`），本轮**一行没动，继续交给窗口1**。本档那一半（内容太高）留给本轮的学习优化员 / 监督修复员。

### A7 · 第 1 轮遗留逐条复测

| 编号 | 第 1 轮结论 | 第 2 轮实测 | 判定 |
| --- | --- | --- | --- |
| **W5-A-01** 钟面标签念答案 | 已修 | 188 关渲染出的 **720 个钟面**，`aria-label` 恒为「钟面，自己读一读」；带阿拉伯数字或冒号的 **0 个** | ✅ 没回潮 |
| **W5-A-02** 题干自指 | 已修 | 成语释义 / 偏旁义项词 / 形近字提示句三类 **0 命中** | ✅ 没回潮 |
| **W5-A-03** 让分开关 30px | 已修 | 五款 × 三档视口 × （地图页 + 关内），`<44px` 的可点热区 **0 个** | ✅ 没回潮 |
| **W5-A-04** 模式入口低于 44px | 已修 | 同上；横向溢出三档视口全部 **0** | ✅ 没回潮 |
| **W5-A-05** 画室浮层底下还能 Tab | 已修 | 画室 `role="dialog"` + `tabindex="-1"`，**2 个 `inert` 容器**，焦点首站落「✖ 关上画室」，选关地图 Tab 不到 | ✅ 已清（有一条残留见 W5R2-A-06） |
| **W5-F-01** 坏存档崩画室 | 已修 | 往 `yiduo-yixing.clf.sandbox.v1` 塞 7 条脏数据（`pic:999` / `pic:-3` / `pic:1.7` / 字符串 / `null` / `pic:"0"` / `fills` 值是数字）→ 归一化后剩 3 张，点开越界那张**不崩**，**0 异常**，还能接着涂 8 个区 | ✅ 没回潮 |
| **W5-F-02 / F-03** 题面写答案 | 已修 | 与 W5-A-02 同一遍扫描，**0 命中** | ✅ 没回潮 |
| **W5-F-04** 涂一笔后 Esc 关不上画室 | 已修 | 用网格采样在色块上找一个真的点得着的点（`grass` 被填成 `#ff6b6b`，确认真涂上了）→ 焦点落到 `DIV.clf-sheet`（`tabindex="-1"` 接住了）→ **`Esc` 关得掉**。刚打开时按 `Esc` 也关得掉 | ✅ 没回潮 |
| **W5-F-07** 舞台硬裁 | 阻断，未修 | **仍在**，且补齐了「真手势也滚不出来」这一格。详见 A6 | ❌ 仍在 → W5R2-A-01 / A-02 |
| **W5-L-08** 战役对手局内动态难度 | 留第 2 轮 | 读招电脑 `aiAdapt` 已在 167–188 关逐关抬（0.277 → 0.349），让分开关封顶 8% 默认关。本轮量化下来：**熟练档全 188 关不输、乱点档后期全输、章节余量单调下探**——「局内动态难度」这条**不是必须**。真要动，先抬的应该是第 145 关那 0.98 秒余量（W5R2-A-07），而不是加一层动态难度 | 📌 给出口径，不建议本窗落地 |
| **W5-L-09** 错题回顾链可回放 | 留第 2 轮 | **这条链实测走通**：第 90 关故意答错「哪个钟面是『1 点 3 刻』？」→ 通关后弹出「📒 错题回顾：同类的题换个数字再来一遍」→ 回顾轮出的是「哪个钟面是『**1 点 1 刻**』？」，**同题型、换了数字**，进度写「第 1 / 1 题」。`runner.ts` 给回顾轮的 `maxWrong = questions.length * 20 + 20`，回顾轮**只复习不判负**，实机确认 | ✅ 行为正确，覆盖率补测的活留给本轮 learner |
| **W5-L-10** 提示「不泄底」口径 | 留第 2 轮 | **口径定了**（见下） | ✅ 按新口径 0 命中 |
| **W5-F-05** 13 道指令语带答案字 | 留第 2 轮 | **复算对上了**（见下） | ✅ 按新口径 13 → 0，不用动摘要锁 |
| **W5-F-06** 摘要锁边界 | 留第 2 轮 | **边界量清楚了**（见下） | ✅ 不是缺陷 |

#### W5-L-10 · `landlord-cards` 提示「不泄底」的口径

三档提示循环实机确认正确：`提示:推荐一手` → `提示:关` → `提示:高亮牌组` → 回到 `提示:推荐一手`。三档各自说什么：

```
关      「挑战档:完全靠自己想,牌桌上什么都不标。」
高亮牌组「现在能出的一共 36 组:单张 11 组 · 顺子 10 组 · 对子 6 组 · 三张 2 组。
         现在圈出来的是第 1 组:单张 4(再点换一组)。」
推荐一手「先走顺子 4-J:打完手数从 9 手降到 8 手,2 和王一张没动,收尾的本钱都还在。
         看了 36 种走法,这一手账面最划算。」
```

**建议把「泄底」定义成：教练的解释里出现了别人手上具体有什么牌、或底牌是什么。**
只引用**自己的手牌结构**（手数、剩几张控牌、有没有动到 2 和王）和**公开信息**（每家还剩几张）不算泄底
——「最强那一档本来就建议出哪一手」是这个功能的用途，不能算泄底，否则这一档就没有存在意义了。

按这条口径扫：188 关的首手解释，加上 131 局完整对局里教练说过的每一句，检查关键词
`底牌` / `对家手里` / `他手里` / `她手里` / `电脑手里`，再加一条正则
`(对家|他|她|电脑|团团|圆圆).{0,6}(手里|有|拿着).{0,10}(王|炸|2|A|K)`——**0 命中**。
对家侧栏显示的也只有「团团·认真 农民 🂠 17 张」这种公开张数。

#### W5-F-05 · `word-garden` 13 道指令语带答案字

复算：188 关里剔掉 9 个组字关，剩 **1115 道**选择题，「指令语里出现了答案字」共命中 **213 道**。其中 **200 道是 `pic2char`**——
「这是「大山」，哪个字是「山」？」这种，题型本身就是「拿字面去认字形」，把目标字写在题干里是**玩法定义**，不是线索。
剔掉这 200 道，剩下 **13 道，和第 1 轮监督修复员的数字逐字对上**：

```
count   10 道  L52 L53 L54 L55 L56 L57 L58 L59 L63 L65   「数一数，用哪个汉字表示？」答案「一」
py2char  3 道  L60 L65 L66                               「读一读拼音，选出对的字～」答案「一」
```

命中的全是**「数一数 / 读一读」这句固定套话里的那个「一」**。孩子必须真的去数图上有几个 🐞、或者真的去读 `yī`，才答得对。

**建议口径：判「题干写着答案」之前，先把固定指令套话剥掉**——`数一数` / `读一读` / `看一看` / `想一想` / `找一找` / `比一比` / `连一连` / `试一试` / `选一选`。
按这条口径这 13 道**全部出局，命中数 13 → 0**。这样就**不需要动前 99 关的题面**，
`levels188.test.ts` 那把 `465a6c3b` 摘要锁一个字节都不用碰——这是本条最重要的结论。

#### W5-F-06 · `clock-house` 摘要锁的边界

两条锁各锁什么，本轮量清楚了：

| 锁 | 位置 | 锁住的是 |
| --- | --- | --- |
| 摘要锁 | `levels.test.ts` 的 `LEGACY_DIGEST` | 算摘要前先用 `asLegacyHtml()` 把 ` role="img" aria-label="…"` 整段按 `data-h`/`data-q` **重写回 1.1 的写法**——所以它对标签文字**本身是瞎的**，它锁的是「**除了这个标签，前 99 关一字节未改**」 |
| 标签锁 | `a11yLabels.test.ts` | 188 关每一个 `aria-label`：① **一个数字都不许有**；② **不许包含本题答案**；③ 两个渲染器的默认标签都是 `FACE_LABEL` 常量 |

合起来能挡住的是「**泄数字**」和「**泄答案**」这两类。挡不住的只剩一格：**把标签换成另一句既不含数字、也不含答案的话**。
但 `clockSVG(4, 0, 82, "钟面")` 这条「调用方可以自己传标签」是**设计内的**（拨一拨的练手钟面就靠它实时改写读数），
所以这一格不该也不能被锁死。**结论：不是缺陷，是这条锁的边界，现在有数了——两条必须一起留，删掉任何一条都会漏。**

### A8 · 本轮问题清单

**阻断 1 / 严重 2 / 一般 2 / 建议 5。**

| 编号 | 级别 | 游戏 | 问题 | 归属 |
| --- | --- | --- | --- | --- |
| **W5R2-A-01** | **阻断** | `color-fun` | 舞台硬裁：390×844 起，第 40 / 90 / 140 / 170 关的**调色板整排 + 调锅三原色 + 撤销/重做**（8–11 颗）用真实坐标点不着；**真手指上滑 3 次 + 滚轮 3 次，`scrollTop` 一格没动**，页面本身也不滚。选不了色 = 涂不了 = **过不了关**。程序化设 `scrollTop` 能救回大半，证明只是被裁不是没渲染 | **平台一半交窗口1**（`.game-stage{overflow:hidden}` / `.l99-stage-wrap`，禁改，本轮一行没动）；**本档一半**（内容太高）留本轮 learner / fixer |
| **W5R2-A-02** | **严重** | `red-blue-race` | 同一根因。360×720 起 `🦘 跳` 点不着；360×640 三颗全挂。另外舞台一进关就自滚到 143px，把 `🤝对战场` / `♾️跑不完的跑道` / `🗺️选关` 顶到 `y<0` 也点不着。**触屏玩家在这两档视口上跳不了、也切不了模式**；键盘（A/D + W）还能玩，所以判严重不判阻断 | 同上 |
| **W5R2-A-03** | **严重** | `clock-house` | 12↔24 时制的时段词不合小学《24 时计时法》口径：**78 道 / 46 关**。其中 **9 道是硬错**——`00:30 →「上午 12:30」`、`12:10 →「下午 12:10」`、题面写 `「上午 12:40」` / `「下午 12:20」`。中文没有「上午 12 点」（0 点是凌晨/半夜）也没有「下午 12 点」（12 点是中午）；换英文口径同样错。**这是一款专门教时制换算的游戏，教出来的说法课堂上会被判错** | 本档 `clock-house/levels.ts` 的 `PERIODS` / `to12Hour()` / `qH12()` / `qH24()`，**都在第 100 关之后**，不碰前 99 关摘要锁 |
| **W5R2-A-04** | 一般 | `landlord-cards` | **无尽连胜输了之后永远发同一副牌**。`showOver()` 的「🔁 从第 1 局再来」把 `streak=0; bump=0` 一起清零，`startRound()` 再 `dealCards(buildEndlessRound(1).seed + 0)`——种子恒定。实测连开三次首局，手牌签名**逐字相同**（`A♥A♣A♦K♠J♠J♦10♣10♦9♥9♦7♣6♠6♣6♦5♥4♥3♣`），三局的耗时、出手数、结算亮点句也完全一致。孩子在第 1 局卡住就会一直卡在同一副牌上。对照：⚔️双人对战的「🔁 再来一局」是 `round++; bump=0`，每局都换牌 | 本档 `landlord-cards/index.ts` `mountEndless()` |
| **W5R2-A-05** | 一般 | `landlord-cards` | 360×640 上 `⏸ 暂停` 点不着（舞台溢出 125–143px，按钮中心落在 y=669）。390×844 / 360×720 干净。与 W5R2-A-01 同根因，但只溢出一行、只影响暂停键，单列 | 平台一半交窗口1；本档一半是矮屏再收一档 |
| **W5R2-A-06** | 建议 | `color-fun` | 画室的焦点圈**没盖住平台壳顶栏**：`muteBehind()` 只 `inert` 掉 `host.children`，壳顶栏（`跳到主要内容` / `🏠返回` / `📖攻略` / `⏸`）在 host 之外，画室开着时仍能 Tab 到这 4 颗。W5-A-05 点名的选关地图已经盖住了，这是剩下的一格 | **交窗口1**（要盖住得动 `src/ui/**` 的壳，本档禁改） |
| **W5R2-A-07** | 建议 | `red-blue-race` | 第 145 关（节拍风廊首关）熟练档余量只剩 **0.98 秒**，是全 188 关最小值，比上一关（5.23s）掉了 4.25 秒——章节交界处最陡的一级台阶。不是做不到（熟练档仍然赢），但第一次遇上鼓点机制就把余量压到 1 秒以内，容易劝退 | 本档 `red-blue-race/levels.ts` |
| **W5R2-A-08** | 建议 | `landlord-cards` | 第 188 关要**连输 5 次**才发到一副「照着教练提示打能赢」的牌（其余 187 关都在 2 次以内）。塔顶紧一点合理，但 6 次才过第一次，对孩子偏长 | 本档 `landlord-cards/levels.ts` 的 `seed` / `boost` |
| **W5R2-A-09** | 建议 | `red-blue-race` | 无尽跑道对「每个坑都跳过去」的玩家**没有收工条件**：机器人跑满 91 秒 / 6950 步 / 511 次起跳，密度到「每百米 6.3 个机关」仍未结束。规则是「撞 3 次收工」，不撞就不结束。真孩子撞得到，所以不是缺陷；但建议加一条时间或距离上的收口，免得一趟停不下来 | 本档 `red-blue-race/index.ts` `mountEndless()` |
| **W5R2-A-10** | 建议 | `color-fun` | 两处混色口径瑕疵：①「浅绿 + 绿色 = 中绿」的亮度落在两个原料之间，方向不如别的配方直白（别的都是「加白变浅、加黑变深」）；②「同色 + 同色 = 更深」（红+红=深红、黄+黄=金黄）是游戏化近似，物理上同色相混不变深。两条都不影响过关，也不教错主干规则（三原色相混、加白加黑全对） | 本档 `color-fun/mix.ts` |

### A9 · 五款一览

| 游戏 | 白屏 | 赢 / 输两条路 | 换的关 | 模式换打法走结算 | 竞态 / `destroy` | 教育正确性 | 阻断 / 严重 / 一般 / 建议 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `landlord-cards` | ✅ | ✅ 40/140 ★★★；90/170/188 输 | 40 / 90 / 140 / 170 / 188 | ♾️无尽（输后重开×3）✅ · ⚔️双人（29 次换人幕）✅ | ✅ 连点 24 下无碍，进出 10 次全归零 | 提示三档 0 泄底 | 0 / 0 / 2 / 1 |
| `red-blue-race` | ✅ | ✅ 40/90/140 ★★★；170/188 输（体力机制） | 40 / 90 / 140 / 170 / 188 | ♾️无尽两种打法 ✅（174 米新纪录）· 🤝对战场 ✅（100:92） | ✅ 连点 80 下只走 4 米，卸载后敲键 0 异常 | 熟练档 188/188 全胜、乱点档后期全输 | 0 / 1 / 0 / 2 |
| `clock-house` | ✅ | ✅ 六关全对 ★★★；故意错到判负 ✅ | 40 / 90 / 140 / 170 / 100 / 188 | 无关卡外模式；📒错题回顾链 ✅ | ✅ 同选项连点 25 下不误判 | ⚠️ 12↔24 时段词 78 道 / 46 关 | 0 / 1 / 0 / 0 |
| `word-garden` | ✅ | ✅ 五关全对 ★★★；故意错到判负 ✅ | 40 / 90 / 140 / 170 / 100 | 组字工坊(188) ✅ · 描红台(105)「月」4 笔全对 ✅ | ✅ 同上 | 1181 道 0 错 0 重复 0 不唯一 | 0 / 0 / 0 / 0 |
| `color-fun` | ✅ | ✅ 五关全涂完 ★★★；**设计上不判负**（涂错 8 次只记「改过 8 次，没关系」） | 40 / 90 / 140 / 170 / 188 | 🎨画室 开→涂→存→点旧作→关 ✅；坏存档 0 异常 | ✅ 连点 60 下无碍，进出 10 次全归零 | 22 条配方 + 188 关规则模式全对 | **1** / 0 / 0 / 2 |

全程 **0 pageerror / 0 console.error**，`AudioContext` 恒为 1。

## 档B · 五款

（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`）

### B0 · 走查计划（动代码 / 动报告前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 2 轮** · 档B 测试员（换样本）。不是学习优化员，不是监督修复员，**不开第 3 轮**。
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`37378eb`（对齐 `origin/game-1.2-window5`，档B 第 1 轮监督修复员的收口 commit）
**范围**：只验本档五款。**不验** 76 款全集，**不验**窗口 2 点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`），**不验**窗口 1 的 root 门 / 手游端游筛选 / 2.5D 基建。不碰档A / 档C 的游戏目录。

**读过的输入（第 1 轮三份报告的档B 节）**

| 文件 | 档B 结论（我要拿来复测的） |
| --- | --- |
| `docs/qa/1.2-window5-round1-tester.md` 档B | 阻断 2（W5-B-01 舞台硬裁横跨 4 款、W5-B-02 `red-blue-tap` 320×640 对战第 4 键）/ 严重 2（W5-B-03 点阵第 7 列与七声沙盒两端、W5-B-04 长宽术语教反 12 道）/ 一般 2（W5-B-05「点两个点」、W5-B-06 提示看听不一致）/ 建议 1（W5-B-07 双声部切 5px、节奏条瞄哪儿）。水位 198 文件 / 5399 用例 |
| `docs/qa/1.2-window5-round1-learner.md` 档B | 列 11 条、落地 7 条（W5-L-11…17）；`.game-stage{overflow:hidden}`（W5-L-18）与 `quiz99` 朗读接缝（W5-L-19）**交窗口1**；W5-L-20「点两个点」、W5-L-21 节奏条判定线**留第 2 轮**。收尾 202 文件 / 5424 用例 |
| `docs/qa/1.2-window5-round1-fixer.md` 档B | 收口 SHA **`37378eb`**，**212 文件 / 5518 用例**。独立复审判学习优化员 7 条里 2 条**空转**（W5-L-15 / 16 的 `max-height:100%` 钳不住）、1 条**算得对但滚不动**（W5-L-14）。补修 6 条：W5-F-08（`fitIntoStage`）/ 09（`.shk-quizhost`）/ 10（七声沙盒真横划）/ **11（`red-blue-tap` 战役竞技场被裁，两份报告都没写过，fixer 新发现）**/ 12（三批 `<44px` 热区）/ 13（提前修掉 W5-B-05）。订正学习优化员的 132px 为**实测 117px**。明说：本档目录里能修的阻断/严重清零，**卡在禁改文件上的不写成已解决** |

**本轮的判据（照抄 fixer 的尺子，不放松）**

- 「点不着」只认一条：**`document.elementFromPoint(元素中心点)` 拿回来的是不是它自己 / 它的后代 / 它的祖先**。拿不回来就叫点不着。
- **一律不用程序化 `el.click()` 判热区可达性**——被 `overflow:hidden` 裁掉的元素 `el.click()` 照样绿，那是假绿，第 1 轮就是这么漏的。
- 「滚一下能不能够着」也不认 `el.scrollIntoView()`（它连 `overflow:hidden` 都推得动，用户推不动）。只认 `overflow-y/x` 为 `auto` / `scroll` **且真有可滚距离**的祖先，手动写 `scrollTop` / `scrollLeft`。
- 横向滑动、点两个点这类手势，用 `Input.dispatchTouchEvent` 发**真实手指事件**。
- 真机手法：`npm run build` 产物用 `vite preview` 起，**Chrome headless + CDP**（Node 22 自带 `WebSocket` 直接说协议，**不引任何依赖**，一次性脚本跑完即删、不进提交）。

**本轮必须逐条复测的第 1 轮遗留 / 修复**

| 编号 | 第 1 轮的状态 | 本轮要做什么 |
| --- | --- | --- |
| W5-B-01 | 阻断·本档侧清零 / 平台侧未解决 | 四档视口复测可点元素是否仍全部够得着；量舞台硬裁还剩多少 px。平台 `.game-stage{overflow:hidden}` **交窗口1，不改** |
| W5-B-02 | 阻断·已宣称清零 | `red-blue-tap` 320×640 对战第 4 键，**必须真实坐标** |
| W5-B-03 | 严重·已宣称清零 | `shape-kingdom` 点阵第 7 列；`music-stars` 八键沙盒两端 |
| W5-B-04 | 严重·已宣称清零 | 长方形长宽术语 12 道，**换一批题**再核 |
| W5-B-05 / W5-F-13 | 一般·已宣称清零 | 「点两个点」作图关真机能不能拉出矩形 |
| W5-B-06 | 一般·交窗口1 | `quiz99` 提示看听不一致，确认仍在、**不改 `quiz99.ts`** |
| W5-B-07.2 / W5-L-21 | 建议·留第 2 轮 | `music-stars` 节奏条判定线：本轮量化「只改画法不改判定」该不该落地，**给口径，不改玩法代码** |
| W5-F-08 / 09 | 阻断·已宣称清零 | 矮屏自滚真滚 / 真钳位（量 `scrollHeight` vs `clientHeight`，以及 `fitIntoStage` 写出来的像素 `max-height`）；答题屏选项点不着 |
| W5-F-10 | 严重·已宣称清零 | 七声沙盒**真手指横划**：`touch-action:pan-x`，两端键够不够得着 |
| W5-F-11 | 阻断·fixer 新发现 | `red-blue-tap` **战役**竞技场被裁——第 1 轮测试员没量过战役这一半，**本轮必须量** |
| W5-F-12 | 一般·已宣称清零 | 本档 `<44px` 热区三批 |
| W5-L-11…17 | 学习优化员落地 7 条 | **独立复算**量化数字，判真修还是假绿 |
| W5-L-20 | 已由 fixer 提前修 | 「点两个点」回归 |
| 平台 overflow / quiz99 朗读 | 交窗口1 | 登记是否仍在，**不改禁改文件** |

**本轮换样本的原则（第 2 轮的核心要求）**

1. **不许只玩第 1 关。** 第 1 轮走的是 1 / 100 / 188，本轮新样本落在**中段与末段**：每款取 **40 / 90 / 140 / 170** 附近，外加 100 / 188 若能开。
2. 有无尽 / 双人 / 沙盒 / 进阶的**都要再玩到结算**，**换打法**，不重复第 1 轮的路径。
3. **难度曲线**：量化每关通关耗时与失败次数，判断有没有跳崖、后期是不是纯数值重复。
4. **竞态**：快速连点、快速进出 10 次、暂停恢复、切后台。查崩溃与 `destroy`。
5. **教育正确性**：`math-farm` / `shape-kingdom` **换一批题**人工核答案。教育出错一律按「严重」起步。攻略只讲方法不给答案。
6. **视口**：至少 360px 与 390×844，另加 360×640 / 320×640（第 1 轮出过阻断的两档）。热区判定一律 `elementFromPoint(中心点)`。
7. 每一款：从首页进入 → 真实胜负（赢一次输一次）→ 退出再进。

**严重度口径**：阻断 = 白屏 / 点不着 / 教错 / 泄题 / 必现崩溃；其余按严重 / 一般 / 建议记。**教育出错一律按「严重」起步。**

**权限边界**：测试员**只写报告与补 `*.test.ts`，不改玩法代码**。只有白屏 / 起不来这类阻断才允许在本档五款目录内打最小补丁并补回归。不许改 `src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，以及本档以外任何游戏目录。不引入依赖，不动存档 key。CSS 前缀沿用 `fs-` / `rbt-` / `mtf-` / `shk-` / `mst-`。

**报告边界**：只写本文件的 `## 档B · 五款` 这一节。**档A 节一个字节不动**，档C 节保持原样。别人的报告文件（round1 三份、round2 learner/fixer、round3）一个字都不改。不写 learner / fixer 报告，**不开第 3 轮，不派下一名角色**。

### B1 · 这一轮到底换了哪些关、玩到哪一步

**跑测环境**：`npm run build` 产物 → `vite preview`（127.0.0.1:4173）→ Chrome headless（`--headless=new`）→ Node 22 自带 `WebSocket` 直接说 CDP。**零新增依赖**，一次性脚本落在 `/tmp/w5r2b/`，**跑完即删、不进提交**。热区一律 `document.elementFromPoint(中心点)`，滚动一律手写 `scrollTop` / `scrollLeft`，手势一律 `Input.dispatchTouchEvent`。

**视口四档**：390×844 / 360×720 / 360×640 / 320×640。

| 款 | 换的样本关 | 首页进入 | 真赢 | 真输 | 退出再进 |
| --- | --- | --- | --- | --- | --- |
| `fishing-star` | 40 / 90 / 140 / 170 / 100 / 188 | ✅ 搜索卡片真坐标点入 | ❌ **一关都赢不了**（见 B4·W5-B-08 阻断） | ✅ 6/6 关都走到「就差一点点！」结算 | ✅ 10/10 |
| `red-blue-tap` | 40 / 90 / 140 / 170 / 100 / 188 | ✅ | ✅ **6/6 关三星** | ✅ 摆烂不点 → 「就差一点点！」 | ✅ 10/10 |
| `math-farm` | 40 / 90 / 140 / 170 / 100 / 188 | ✅ | ✅ **6/6 关三星** | ⚠️ 本款**没有败局**，见下 | ✅ 10/10 |
| `shape-kingdom` | 40 / 90 / 140 / 170 / 100 / 188 + 102 / 174（作图关） | ✅ | ✅ **选择题 5/5 关三星** | ⚠️ 同上 | ✅ 10/10 |
| `music-stars` | 40 / 90 / 140 / 170 / 100 / 188 + 160 | ✅ | ✅ **4 关三星**（40/90/140/100） | ✅ 170 / 188 视奏关真输 | ✅ 10/10 |

**关于「真输」的一条口径更正**：`math-farm` / `shape-kingdom` 走 `quiz99`，**根本没有败局**——答错不掉血、不结算，只是把错题记下来，主套题走完之后再开一轮**「错题重练」**（进度条显示 `第 1 / 1 题`），答错两次就把正解高亮成 `qz-hint` 直接喂给孩子。所以这两款的「输」只体现为**星数变少**，不存在失败浮层。这是设计，不是缺陷，但第 1 轮报告里「赢一次输一次」这一条对这两款**不适用**，本轮据实登记。

**一条容易踩的坑（写给后面几轮的自动化）**：错题重练那一轮**会重新洗牌选项**。第一次跑的时候我按「第几个选项」记答案，重练轮里下标全错位，机器人对着一颗 `disabled` 的按钮点了 180 下——看着像软锁，其实是**我自己的脚本错了，游戏行为完全正确**（它一路把提示升级到高亮正解）。后来改成**按答案文本 / SVG 指纹**认选项才对上。SVG 选项要把 `width` / `height` 一起算进指纹，不然「哪个最小？」这种三个选项同形同色只差尺寸的题会指纹撞车。

### B2 · 难度曲线（量化）

机器人耗时，390×844，同一台机器同一轮。机器人手速恒定，所以**横向比较关与关之间的相对难度是有效的**，绝对秒数不代表儿童耗时。

| 款 | L40 | L90 | L140 | L170 | L100 | L188 | 判断 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `math-farm` | 5.7s ★★★ | 5.7s ★★★ | 9.5s ★★★ | 6.6s ★★★ | 6.6s ★★★ | 10.4s ★★★ | 题量 5→6→9→10 递增，题型从两位加减长到余数/通分/比例/百分数，**没有跳崖** |
| `shape-kingdom` | 5.4s ★★★ | 5.4s ★★★ | 作图关 | 6.3s ★★★ | 6.3s ★★★ | 10.0s ★★★ | 选择题 5→6→10 题，末段进坐标/路径/立体，**梯度平滑** |
| `red-blue-tap` | 13.3s ★★★ | 14.5s ★★★ | **33.5s** ★★★ | 13.0s ★★★ | 10.4s ★★★ | 18.4s ★★★ | L140 明显是个尖峰（2.3 倍于邻关），但仍三星通过；不是数值重复堆料 |
| `music-stars` | 20.6s ★★★ | 27.2s ★★★ | 19.4s ★★★ | **输** | 36.7s ★★★ | **输** | 40/90 跟弹、100 节奏、140 音程都过；170/188 **视奏（score）**两关机器人打不过 |
| `fishing-star` | — | — | — | — | — | — | **全部走不到胜局**，见 B4 阻断 |

- `red-blue-tap` L140 的 33.5s 尖峰值得留意，但它是**关卡目标变长**（点的数量多）造成的，不是判定变苛刻；三星照拿，判**不构成难度断崖**。
- `music-stars` 170 / 188 是简谱视奏关，`replays: 0`（一次重听都不给）、`seqLen` 6–8、`maxMiss` 4。机器人两关都在「就差一点点！」结算。**这两关我打不过，但不能因此判它难度超标**——我没有把握说明是关卡问题还是机器人视奏策略太弱，据实登记为「未攻克」，不计入问题清单。

### B3 · 教育正确性（换一批题，独立复算）

方法：临时 `*.test.ts` 探针**直接调 `buildQuestions(level)` 取全 188 关的题**，用**自己另写的求值器 / 几何解算器**重算一遍答案，与 `q.correct` 对撞。**绝不调用被测代码算答案**。探针跑完已删除，不进提交。

| 款 | 全量题数 | 独立复算覆盖 | 对撞不上的 |
| --- | --- | --- | --- |
| `math-farm` | **1224** 道（188 关） | **1122** 道（91.7%），20 个题型全覆盖 | **0** |
| `shape-kingdom` | **1224** 道（188 关） | **678** 道（55.4%，纯 SVG 选图题无法用数值复算） | **0** |

`math-farm` 复算到的题型与道数：加 191 / 减 148 / 数一数 68 / 填空 39 / 比大小 40 / 连加连减 42 / 竖式 65 / 乘 45 / 除 48 / 带余除法 62 / 混合运算 101 / 找规律 34 / 约分 28 / 通分 34 / 小数加减 26 / 小数乘除 28 / 百分数 26 / 比 27 / 解方程 33 / 两步应用题 37。**一道错的都没有。**

`shape-kingdom` 数值复算到的：形状 130 / 找形状 23 / 颜色 75 / 找颜色 36 / 比大小 95 / 数边 56 / 数个数 78 / 面积 10 / 对称轴 3 / 立体面数 44 / 立体棱顶点 23 / 坐标路径 54。**一道错的都没有。**

**W5-B-04（长宽术语教反 12 道）复测结论：已修，换一批题也没复发。** 全 188 关里扫出 **51 个长方形**，其中 **12 个是「高比宽大」的竖长方形**——正是第 1 轮教反的那一类。逐个核对「长 / 宽」用词与标注边的对应关系，**0 处用反**。

### B4 · 🔴 `fishing-star` 上一条鱼之后就再也抛不了竿（**本轮新发现，阻断**）

**编号 W5-B-08。这是本轮唯一的阻断，也是这一档最要紧的一条。**

**现象**：任意一关，钓到**第一条鱼**之后，`「🎣 按住抛竿」`那颗大按钮**直接掉出屏幕，`elementFromPoint` 拿回 `null`**，再也点不着。而每一关的目标都是 **4–6 条鱼**，所以**一关都通不了**，只能干等到时间耗尽吃「就差一点点！」。

**根因**（量出来的，不是猜的）：上鱼之后游戏会插进一行 `.fss-show`「水桶」（展示这一条鱼 + 「💧 放生」按钮）。这一行**占 48–73px 的常规文档流高度**，把它下面的 `.fss-act` 整个往下顶。而 `.game-stage` 是**定高 + `overflow:hidden`**（平台文件，W5-B-01，交窗口1），顶出去的部分**没有任何滚动余地**。

**四档视口 × L40 / L90 / L140，12 组全中，无一例外**：

| 视口 | 上鱼前 `.fss-act` 中心 y | 上鱼后 y | 出屏 | 点得着 | 手写 scrollTop 后够得着 | 桶行高 | 舞台 cut |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 390×844 | 776 ✅ | **861** | ✅ 出屏 | ❌ | ❌ | 48px | 0 → **77** |
| 360×720 | 658 ✅ | **739 / 764** | ✅ | ❌ | ❌ | 48 / 73px | 0 → **79 / 104** |
| 360×640 | 608 ✅ | **687 / 712** | ✅ | ❌ | ❌ | 48 / 73px | 18 → **97 / 122** |
| 320×640 | 608 ✅ | **712** | ✅ | ❌ | ❌ | 73px | 18 → **122** |

每一组的目标都是 `🎯 1 / 4 条`～`🎯 1 / 6 条`——**卡在 1**。

**为什么第 1 轮没抓到**：第 1 轮量的是**进关那一瞬间**的静态热区，那时候 `.fss-show` 还 `hidden`、桶行高 0、舞台 cut 也是 0，扫出来一片绿。这个缺陷**只有真的钓上一条鱼、让布局长高之后才会出现**——静态扫描永远扫不到，必须真玩。这也正是「换样本 + 真实胜负」这条要求的价值所在。

**和 W5-B-01 的关系**：同一个根（平台舞台 `overflow:hidden`），但**后果完全不是一个量级**。W5-B-01 是「开局有元素在裁切线以下」，W5-B-08 是「**玩到一半布局自己长高，把唯一的操作按钮顶出去，且不可恢复**」。**按阻断记，不并进 W5-B-01。**

**边界**：本款目录（`src/games/fishing-star/`）里够得着的自救手段是有的——`shape-kingdom` 的 `fitIntoStage()` 就是先例（量一次 `.game-stage` 下沿，把像素值写成自己的 `max-height`，再给自己挂 `overflow-y:auto`）。但**这是玩法/布局代码，测试员默认不改**，本轮**只登记不动手**，留给修复员。

### B5 · 🟠 走「🎯 跳到当前关」进关，关内顶部会被永久裁掉（**本轮新发现，严重**）

**编号 W5-B-09。**

**现象**：地图上当前关节点滚出可视区时，唯一的原生逃生路是 `「🎯 跳到当前关」`。点它之后地图确实跳到位了，但它**给 `.game-stage` 留下了一个非 0 的 `scrollTop`**；紧接着进关，**这个 `scrollTop` 不会被重置**，而舞台是 `overflow:hidden`，于是关内 UI 顶部被硬裁掉一截，**且没有任何办法滚回去**。

**因果链是干净的**：132 组（4 视口 × 5 款 × 6–8 关）扫描里，`path=直接点节点` 的行 **bad 一律为 0**；`path=🎯跳到当前关→点节点` 的行 **bad 一律 > 0**。一一对应，没有例外。

| 视口 | 款 / 关 | 被裁掉点不着的 | 遮挡者 |
| --- | --- | --- | --- |
| 360×720 | `red-blue-tap` L188 | `⚔️ 双人对战 · 谁更准`、`♾️ 点到手软 · 点我开抢！` | `DIV.game-title` / `BUTTON.icon-btn--pause` |
| 360×640 | `red-blue-tap` L140 / L188 | 同上两颗 | `SPAN.game-title-text` / `HEADER.game-topbar` |
| 320×640 | `red-blue-tap` L140 / L188 | 同上两颗 | `DIV.game-title` / `HEADER.game-topbar` |
| 360×640 | `math-farm` L188 | `🗺️ 选关` | `HEADER.game-topbar` |
| 320×640 | `math-farm` L140 / L188 | `🗺️ 选关` | `HEADER.game-topbar` |

**后果分级**：`red-blue-tap` 那两颗是**双人对战与无尽模式的唯一入口**，被裁掉就等于这一路进不去；`math-farm` 的 `🗺️ 选关` 被裁掉则是退不回地图。但两种情况都还有**平台顶栏的返回键**可以脱身，不至于卡死，所以**记严重、不记阻断**。

**注意这不是「地图节点点不着」**——节点本身够得着（`🎯` 只是把它滚进视区）。**问题出在 `🎯` 的副作用被带进了关内**。

### B6 · 第 1 轮遗留逐条复测

| 编号 | 第 1 轮状态 | 本轮实测 | 判定 |
| --- | --- | --- | --- |
| **W5-B-01** | 平台侧未解决 | 舞台 `.game-stage` **仍是 `overflow-y:hidden`**，四档视口量到 cut = **0 / 14 / 18 / 66 / 77 / 122px** 不等。本档目录内够得着的元素都已自救 | **仍在**，交窗口1，本轮不改 |
| **W5-B-02** | 阻断·宣称清零 | 320×640 进 `⚔️ 双人对战`：**8 颗按键全部 `elementFromPoint` 命中**，无一 <44px，四档视口皆然 | ✅ **真修** |
| **W5-B-03** | 严重·宣称清零 | ① 点阵：**5 行 × 7 列 = 35 个点，第 7 列在内全部命中，全部 ≥44px**，四档皆然。② 七声沙盒：8 键 `哆来咪发索拉西高哆`，键身 44×52 / 44×70 **全部 ≥44px**；两端在各自的滚动位置都命中（`scrollLeft=0` 首键 ✅ / `scrollLeft=max` 末键 ✅） | ✅ **真修** |
| **W5-B-04** | 严重·宣称清零 | 换一批题：**51 个长方形 / 12 个竖长方形，术语 0 处用反** | ✅ **真修** |
| **W5-B-05 / W5-F-13** | 一般·宣称清零 | 真手指点两个点：`(1行1列)` → `(5行6列)`，读数回 **「现在是 5 × 4：周长 18 厘米，面积 20 平方厘米」**，与题目「面积 20」吻合，交卷后进到下一小题 | ✅ **真修** |
| **W5-B-06** | 一般·交窗口1 | `quiz99` 提示看听不一致——**仍在**。按边界**没碰 `quiz99.ts`** | 仍在，交窗口1 |
| **W5-B-07.2 / W5-L-21** | 建议·留第 2 轮 | 量化完毕，**结论：该落地，且只改画法**。见 B7 | 见 B7 |
| **W5-F-08 / 09** | 阻断·宣称清零 | `.shk-draw` **真的是 `overflow-y:auto` 且真有可滚距离**：360×640 上量到 room = **105 / 174 / 212px**。手写 `scrollTop` 之后 `✅ 我摆好了` **`elementFromPoint` 命中**。不是空转 | ✅ **真修** |
| **W5-F-10** | 严重·宣称清零 | 真手指横划（`touchStart` + 14 段 `touchMove` + `touchEnd`，**没写一行 `scrollLeft`**）：`scrollLeft` 0 → **54 / 76 / 76 / 108**，等于各档满行程；`touch-action:pan-x` 在位 | ✅ **真修** |
| **W5-F-11** | 阻断·fixer 新发现 | **战役**竞技场四档实测：`.rbt-arena` 高 280px，下沿**全部落在舞台下沿以内**（余量 −163 / −58 / −48 / −48px），点全部命中、全部 72×72 | ✅ **真修**，本轮量不出问题 |
| **W5-F-12** | 一般·宣称清零 | 关内 132 组扫描 **<44px 热区 0 个**。但**地图页还剩一批**，见 B7 | ⚠️ **关内已清，地图页仍在** |
| **W5-L-11…17** | 落地 7 条 | 由 B3 全量复算 + B6 各条独立量化覆盖，**未见空转** | ✅ |
| **W5-L-20** | fixer 提前修 | 同 W5-B-05 | ✅ |

### B7 · 本轮问题清单

#### 🔴 阻断 1 条

**W5-B-08 · `fishing-star` 上一条鱼之后抛竿键掉出屏幕，一关都通不了。** 详见 B4。四档视口 × 3 关共 12 组必现，`elementFromPoint` 返回 `null`，手写 `scrollTop` 也救不回来（舞台 `overflow:hidden`）。目标 4–6 条鱼，实际永远卡在 1 条。

#### 🟠 严重 1 条

**W5-B-09 · 走「🎯 跳到当前关」进关，关内顶部被永久裁掉。** 详见 B5。`red-blue-tap` 的双人/无尽入口两颗、`math-farm` 的 `🗺️ 选关` 各自在 360×640 / 320×640 / 360×720 上点不着。还有平台顶栏可以脱身，故不记阻断。

#### 🟡 一般 2 条

**W5-B-10 · `shape-kingdom` 七巧板那一小题，≤640px 高的屏上没有任何一个滚动位置能同时够着所有控件。**

| 视口 | 不滚时点不着 | 滚到底时点不着 |
| --- | --- | --- |
| 390×844 | 1（`✅ 我摆好了`） | **0** |
| 360×720 | 3（`💡 提示`、`🧹 重来`、`✅ 我摆好了`） | **0** |
| 360×640 | 5（多出 `选第 3 块`、`🔄 转一下`） | **4**（轮廓第 1 行 1–4 列被 `guide-btn` / `l99-tool-skip` / `l99-stagebar` 压住） |
| 320×640 | 5（同上） | **4**（同上） |

关键在于：**每一个控件都能在「某一个」滚动位置够着**（滚到顶摆第 1 行，滚到底交卷），所以**能玩通，不是阻断**；但一道题里要来回滚两趟，手感很差。`.shk-draw` 自己的 `overflow-y:auto` 是好的（room 212 / 182px 真在），这一条是**内容在矮屏上就是装不下**。

**W5-B-11 · `fishing-star` 地图页三颗入口按钮只有 34px 高**（`<44px` 触控底线）。`🌙 钓到天黑` 102.4×**34**、`📖 鱼类图鉴 · 0/25` 142×**34**、`🎒 我的装备 · ⭐ 0` 141.3×**34**，**四档视口一模一样**。三颗都点得着（`elementFromPoint` 命中），只是热区偏薄。这是 W5-F-12 在**地图页**的残留——关内那 132 组已经一个都不剩了。

#### 🔵 建议 1 条

**W5-B-07.2 / W5-L-21 · 节奏条判定线：该落地，且只改画法，判定一行都别动。** 本轮给出量化口径：

- 判定本身**是对的**：`judgeTap()` 用的是 `AudioContext` 时钟（不是 `Date.now()`），还减掉了开局测出的输出延迟，档位 `perfect ≤60ms / good ≤120ms / ok ≤200ms`。
- **错的只是画法**。`.mst-bar-line` 判定线 `left = width/2`、宽 4px，**中心在 `width/2 + 2`**；`.mst-bar-tick` 拍块 `left = (at−first) × pxPerSec`、宽 16px（短音）/ 30px（长音），**中心在左沿 +8 / +15**。轨道每秒走 `pxPerSec = 150`。
- 于是在「该敲的那一刻」，拍块的**左沿**正好压线，但它的**中心**还在判定线右边 **6px（短音）/ 13px（长音）**——换算成时间就是**晚 40ms / 晚 87ms**。
- 而游戏对孩子的原话是**「看着黄线走到方块的那一刻再敲」**。孩子照着「方块中心对上黄线」敲：短音 40ms 仍在 perfect（<60），**长音 87ms 直接从 perfect 掉到 good**（>60）。**不会判 miss，但稳定吃掉一档评分。**
- 建议：把拍块改成**以拍点为中心**（`left = (at−first) × pxPerSec − width/2`），或把判定线中心对齐到 `width/2`。**`timing.ts` 的判定逻辑一个字都不要改**——它是对的，改了反而会把已经练熟的孩子打乱。

### B8 · 竞态与 `destroy`

五款各跑一遍，390×844：

| 款 | 进出 10 次 | 残留（`.l99-stage` / `.qz-wrap` / `audio` / `.game-stage` 直接子节点） | 连点 25 下 | 暂停恢复 | JS 错误 / console 错误 |
| --- | --- | --- | --- | --- | --- |
| `fishing-star` | **10/10** | 0 / 0 / 0 / **1** | 单个舞台，标题正确 | ✅ 弹层开→关，舞台存活 | **0 / 0** |
| `red-blue-tap` | **10/10** | 0 / 0 / 0 / **1** | 单个舞台，标题正确 | ✅ | **0 / 0** |
| `math-farm` | **10/10** | 0 / 0 / 0 / **1** | 单个舞台，标题正确 | ✅ | **0 / 0** |
| `shape-kingdom` | **10/10** | 0 / 0 / 0 / **1** | 单个舞台，标题正确 | ✅ | **0 / 0** |
| `music-stars` | **10/10** | 0 / 0 / 0 / **1** | 单个舞台，标题正确 | ✅ | **0 / 0** |

- **连点 25 下**（16ms 一下，砸当前关节点）：五款都**只挂出一个 `.l99-stage`**，`.game-stage` 直接子节点恒为 1，没有重复挂载、没有双份监听。
- **进出 10 次**之后 `.l99-stage` / `.qz-wrap` / `audio` **全部归 0**，只剩 2–3 个 `<style>`（各款自己的 CSS，常驻，正常）。`destroy` 是干净的。
- 四档视口全部跑完，**`page.onError` 与 console error 累计 0 条**。

### B9 · 五款一览

| 款 | 换样本实玩 | 真赢 / 真输 | 教育正确性 | 热区（四档） | `destroy` | 本轮判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `fishing-star` | 40/90/140/170/100/188 | ❌ / ✅ | — | 关内 0 点不着；**地图页 3 颗 34px** | ✅ 干净 | 🔴 **阻断 W5-B-08**、🟡 W5-B-11 |
| `red-blue-tap` | 40/90/140/170/100/188 + 对战 + 无尽 | ✅ 6/6 三星 / ✅ | — | 直接进关 0；**走 🎯 进关 2 颗点不着** | ✅ 干净 | 🟠 W5-B-09 |
| `math-farm` | 40/90/140/170/100/188 | ✅ 6/6 三星 / 本款无败局 | **1122/1224 独立复算，0 错** | 直接进关 0；**走 🎯 进关 `🗺️选关` 点不着** | ✅ 干净 | 🟠 W5-B-09 |
| `shape-kingdom` | 40/90/140/170/100/188 + 102/174 作图 | ✅ 5/5 三星 / 本款无败局 | **678/1224 数值复算，0 错**；长宽术语 51 个矩形 0 处用反 | 选择题 0；**七巧板轮 ≤640px 需来回滚** | ✅ 干净 | 🟡 W5-B-10 |
| `music-stars` | 40/90/140/170/100/188 + 160 + 七声沙盒 | ✅ 4 关三星 / ✅ 170·188 真输 | 音程标注抽查正确（`mi→do` 标「大三度」，与五声音阶一致） | 0 点不着；沙盒两端各自滚动位置可达 | ✅ 干净 | 🔵 W5-B-07.2 |

**档B 第 2 轮计数：阻断 1 / 严重 1 / 一般 2 / 建议 1。**

**没做到的、以及为什么**：
- `fishing-star` **一次都没赢过**——不是我不会打，是 W5-B-08 让它在四档视口上**根本赢不了**（上一条鱼就抛不了竿了）。这一条本身就是那个阻断的证据。
- `music-stars` 170 / 188 **视奏关没打过**。机器人视奏策略弱，**我不能断定是关卡问题**，故只登记未攻克，**不计入问题清单**。
- `shape-kingdom` **七巧板小题机器人没解出来**（要搜索拼块摆位）。这一轮只量了它的**热区可达性**（B7·W5-B-10），**没有验证它的判定正确性**——留给后面的角色。

## 档C · 五款

（`poop-hero` / `red-blue-tug` / `pinyin-train` / `find-diff` / `kitty-care`）

### C0 · 走查计划（动代码 / 动报告前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 2 轮** · 档C 测试员（换样本）。不是学习优化员，不是监督修复员，**不开第 3 轮**。
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支；不推 `main` / 不推 `game-1.2` / 不 `--force`）
**基线**：`d3ca3a0`（对齐 `origin/game-1.2-window5`，档C 第 1 轮监督修复员的收口 commit）
**范围**：只验本档五款。**不验** 76 款全集，**不验**窗口 2 点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`），**不验**窗口 1 的 root 门 / 手游端游筛选 / 2.5D 基建。不碰档A / 档B 的游戏目录。

| id | 中文名 | 目录 |
| --- | --- | --- |
| `poop-hero` | 便便超人 | `src/games/poop-hero/**` |
| `red-blue-tug` | 红蓝拔河 | `src/games/red-blue-tug/**` |
| `pinyin-train` | 拼音小火车 | `src/games/pinyin-train/**` |
| `find-diff` | 找不同 | `src/games/find-diff/**` |
| `kitty-care` | 萌猫小屋 | `src/games/kitty-care/**` |

**读过的输入（第 1 轮三份报告的档C 节）**

| 文件 | 档C 结论（我要拿来复测的） |
| --- | --- |
| `docs/qa/1.2-window5-round1-tester.md` 档C | 阻断 0 / 严重 1（W5C-K01 马拉松秒表逐轮叠加）/ 一般 3（W5C-K02 闪 `Infinity`、W5C-K03 看病提示被盖、W5C-K04 洗澡兜底封顶 53%）/ 建议 6（W5C-K05、W5C-P01、W5C-T01、W5C-T02、W5C-F01、W5C-Y01）。水位 194 文件 / 5379 用例 |
| `docs/qa/1.2-window5-round1-learner.md` 档C | 列 16 条、落地 12 条（W5-L-22…33）；不落地 4 条：W5-L-34（`find-diff` 二级提示，有意设计）、W5-L-35（`quiz99.ts` 闪答案，交窗口1）、W5-L-36（躲纸箱进度清零，**留第 2 轮**）、W5-L-37（`Loop` 句柄推广到 `src/engine/**`，交窗口1）。收尾 209 文件 / 5472 用例 |
| `docs/qa/1.2-window5-round1-fixer.md` 档C | 收口 SHA **`d3ca3a0`**，**224 文件 / 5689 用例**。独立复算判 12 条落地**全真修**（2 条「数字对得上但标准定低了」）。新发现并修完 5 条：**W5-F-14 / 15 / 16（阻断，`kitty-care`）**、**W5-F-17（一般，热区只有一边达标）**、**W5-F-18（阻断，`find-diff` 按屏高摊）**；**W5-F-19（阻断，平台 `.game-stage{overflow:hidden}`）交窗口1，不改 `src/styles.css`** |

**本轮必须逐条复测的第 1 轮遗留 / 修复**

| 编号 | 第 1 轮的状态 | 本轮要做什么 |
| --- | --- | --- |
| W5-F-14 | 阻断·fixer 宣称已修 | `kitty-care` `.ktc-field` 高度、层级、`elementFromPoint`；逗猫 / 打扮真手指做得完吗 |
| W5-F-15 | 阻断·fixer 宣称已修 | 饭碗看得见吗、点得着吗（四档视口） |
| W5-F-16 | 阻断·fixer 宣称已修 | 小屋比舞台高：`fitIntoStage()` 真的先收猫后滚吗；矮屏能不能通关 |
| W5-F-17 | 一般·fixer 宣称已修 | 五处热区**两条边都要 ≥44**：`.ph-btn` / `.rbg-toggle` / `.ph-mode` / `.rbg-open` / `.fdf-zoomrow input` |
| W5-F-18 | 阻断·fixer 宣称已修 | `find-diff` 360×720 提示键与滑杆；`panelCellForRoom` 是不是真按舞台可视高摊 |
| W5-F-19 | 阻断·交窗口1 | **只登记还在不在，不改 `src/styles.css`**。量 `poop-hero` / `red-blue-tug` 在 360×640 / 320×640 上还差多少 |
| W5-L-22 / 23 | 严重（W5C-K01）·已落地 | 马拉松秒表：**换样本再验是不是回潮**。连做 ≥8 轮逐秒采样，`interval` 活口计数 |
| W5-L-24 | 一般（W5C-K02）·已落地 | 徽章 `Infinity` / `NaN` / 负数一次都不许出现 |
| W5-L-25 | 已落地 | 哄睡拍子灯不许累积 |
| W5-L-26 | 一般（W5C-K03）·已落地 | 看病做岔那句针对性的话，屏幕上留不留得住 |
| W5-L-27 | 已落地（fixer 只做了静态复核） | **本轮补真机**：心情打到 0，纸箱画面会不会被护理台盖回去 |
| W5-L-28 | 一般（W5C-K04）·已落地 | 洗澡无指针兜底能不能到 90% |
| W5-L-29 | 建议（W5C-K05）·已落地 | 通关掉收藏后模式条计数当场刷新 |
| W5-L-30 / P01 | 建议·已落地 | 见 W5-F-17 |
| W5-L-31 / T02 | 建议·已落地 | 见 W5-F-17 |
| W5-L-32 / T01 | 建议·已落地 | `K` 与 `L` 都接管得到、老 `J` 仍算数、屏幕上再没有「星星按住 J」 |
| W5-L-33 | 已落地（fixer 说只有源码结构守门） | **本轮补真机行为**：`find-diff` 马拉松换轮不许闪失败屏 |
| W5-L-36 | 留第 2 轮 | `kitty-care` 看病途中躲纸箱，出来之后护理进度清不清零——本轮**实测定性**，不动手 |
| W5C-Y01 | 建议·交窗口1 | `pinyin-train` 连错两次闪答案：**登记口径，不改 `quiz99.ts`** |
| W5C-F01 | 建议·有意设计 | `find-diff` 二级提示点名答案格：**登记口径，不改** |

**本轮换样本的原则（第 2 轮的核心要求）**

1. **不许只玩第 1 关。** 第 1 轮走的是 1 / 100 / 188，本轮主样本换到**中段与末段**：每款取 **40 / 90 / 140 / 170** 附近，外加 100 / 188 作末段对照。
2. 有无尽 / 双人 / 沙盒 / 相册的**都要再玩到结算**，**换打法**，不重复第 1 轮的路径。
3. **难度曲线**：量化每关通关耗时与失败次数，判断有没有跳崖、后期是不是纯数值重复。
4. **竞态**：快速连点、快速进出 10 次、暂停恢复、`destroy` 残留。
5. **教育正确性**：`pinyin-train` **换一批题核**（换题核，不重复第 1 轮那 788 道的抽法）；`find-diff` 复核「提示不直接给答案」的口径。**教错一律按严重起步。**
6. **视口四档**：360px 宽、390×844、360×640、320×640。
7. 每一款：**首页进入 → 真实胜负 → 退出再进**。
8. **动物福利**（`kitty-care` 专项）：零 `ctx.lose` / 零 `onLose` / 零无伤害死亡词，独立复扫，不认自述。

**尺子（照抄第 1 轮监督修复员的口径，不放松）**

- 「点不着」只认一条：**`document.elementFromPoint(元素中心点)` 拿回来的是不是它自己 / 它的后代 / 它的祖先**。拿不回来就叫点不着。
- **一律不用 `el.click()`** 判热区可达性——被 `overflow:hidden` 裁掉的元素 `el.click()` 照样绿，那是假绿。
- 「滚一下够不够得着」也不认 `el.scrollIntoView()`，只认 `overflow-y/x` 为 `auto` / `scroll` 且真有可滚距离的祖先，手动写 `scrollTop` / `scrollLeft`；手势用 `Input.dispatchTouchEvent` 发真实手指事件。
- 真机手法：`npm run build` 产物用 `vite preview` 起，**Chrome headless + Node 22 自带 `WebSocket` 直连 CDP**，**零新增依赖**，一次性脚本落在 `/tmp/` 下、**跑完即删、不进提交**。
- 关卡 100 / 188 默认锁着，用 `localStorage` 播种 `yiduo-yixing.l99.<id>` 打开后**从地图上用真实坐标点节点**进关，不走内部 API、不改平台代码。

**严重度口径**：阻断 = 白屏 / 点不着 / 教错 / 泄题 / 必现崩溃 / 动物福利踩线；其余按严重 / 一般 / 建议记。**教育出错一律按「严重」起步。**

**权限边界**：测试员**默认不改玩法代码**，只写报告。只有白屏 / 起不来这类阻断才允许在**本档五款目录内**打最小补丁并补回归。不许改 `src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，以及本档以外的任何游戏目录。不引入依赖，不动存档 key。CSS 前缀沿用 `ph-` / `rbg-` / `qz-` / `fdf-` / `ktc-`。

**报告边界**：只写本文件的 `## 档C · 五款` 这一节。**档A 节与档B 节一个字节不动。** 别人的报告文件（round1 三份、round2 learner、round3）一个字都不改。不写 learner / fixer 报告，**不开第 3 轮，不派下一名角色**。

**收尾**：`git fetch origin game-1.2-window5` → rebase（档A / 档B 节一个字节不覆盖）→ `npm test && npm run build` 全绿 → `git push origin HEAD:game-1.2-window5`，被拒就再 fetch + rebase，**禁止 `--force`**。

### C1 · 这一轮到底换了哪些关、玩到哪一步

真机口径：`npm run build` 产物 → `vite preview`（`127.0.0.1:4173`）→ Chrome headless（`--headless=new`，一款一个独立 `user-data-dir`）→ Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖**。所有一次性脚本落在 `/tmp/w5r2c/` 下，**不进提交**。点击一律 `Input.dispatchTouchEvent` / `Input.dispatchMouseEvent` / `Input.dispatchKeyEvent`，**全程没有一次 `el.click()`**；「点得着」只认 `document.elementFromPoint(中心点)`。

| 款 | 主样本 | 末段对照 | 关卡外模式 | 额外补的关 |
| --- | --- | --- | --- | --- |
| `poop-hero` | 40 / 90 / 140 / 170 | 100 / 188 | ♾️ 打扫不完的城市、👫 同屏双人（12 键） | 140 三种打法对照（蹲着走 / 走走蹲 / 走走跳） |
| `red-blue-tug` | 40 / 90 / 140 / 170 | 100 / 188 | 🤝 对战四档、♾️ 拉不完的绳（200s / 11 个对手）、👫 同屏双人（K / L / J 三键） | — |
| `pinyin-train` | 40 / 90 / 140 / 170 | 100 / 188 | 本款无关卡外模式 | 90 / 140 专门打到**真输**；1004 道题全量复算 |
| `find-diff` | 40 / 90 / 140 / 170 | 100 / 188 | ♾️ 找不同马拉松（三档视口各清 12 轮） | 提示两级口径专项 |
| `kitty-care` | 40 / 90 / 140 / 170 | 100 / 188 | ♾️ 照顾马拉松（1143 采样点 / 63 件）、📷 小屋相册 | 150 / 160 暖心诊所看病、130 心情小屋、167 / 175 / 182 时装台 |

四档视口全跑：**360×720（360px 宽）、390×844、360×640、320×640**。每一款都走 **首页 → 选关地图上用真实坐标点节点 → 关内真实胜负 → 退出再进**；100 / 188 用 `localStorage` 播种 `yiduo-yixing.l99.<id>` 解锁后**仍然从地图上点进去**，不走内部 API。

**真输拿到了**（不是超时，是游戏自己判的）：

| 款 | 真输画面 |
| --- | --- |
| `poop-hero` | 「就差一点点！」×3 种败因——`摔了一跤,晕乎乎的`（L40）、`尘土风追上来啦`（L170/100/188）、`时间到啦`（L40 360×640） |
| `red-blue-tug` | 「就差一点点！」（L170 @360×720） |
| `pinyin-train` | 「就差一点点！这一关的题目有点调皮，我们休息一下再来一次！」★0（L90 / L140），「🔁 再试本关」按得着、重开回到第 1 题 |
| `find-diff` | 关卡「就差一点点！时间到～」；马拉松「⏰ 这一轮没找完」 |
| `kitty-care` | **设计上不判负**——四档视口 12 次通关 + 看病 6 次 + 心情关干等 75 秒，`ctx.lose` / `onLose` / 结算失败屏**一次都没有**（见 C6 动物福利） |

### C2 · 🔴 `kitty-care` 时装小舞台：选完三件之后「✨ 就这套上台」点不着，这一关通不了（**本轮新发现，阻断**）

时装小舞台（167–188 关）的 `👗 搭配` 任务：挑完三件衣服会翻出评分面板，面板底下那颗 `✨ 就这套上台` 是**交卷的唯一出口**。四档视口全部点不着。

| 视口 | 按钮位置 | 屏高 | `elementFromPoint` | 可滚祖先 | `.ktc-wrap` | `.game-stage` |
| --- | --- | --- | --- | --- | --- | --- |
| 390×844 | 123×44 @ y=827…**871** | 844 | ❌ offscreen | **无** | 608 高 · `overflow:visible` · `max-height:none` | `hidden` 730/848（裁 118px） |
| 360×720 | 123×44 @ y=712…**756** | 720 | ❌ offscreen | **无** | 638 高 · `visible` · `none` | `hidden` 610/878（裁 268px） |
| 360×640 | 123×44 @ y=642…**686** | 640 | ❌ offscreen | **无** | 608 高 · `visible` · `none` | `hidden` 530/848（裁 318px） |
| 320×640 | 123×44 @ y=629…**673** | 640 | ❌ offscreen | **无** | 608 高 · `visible` · `none` | `hidden` 530/848（裁 318px） |

按钮本身 **123×44 是合规的**，问题纯粹是它被推到了屏幕外，而且**没有任何可滚祖先能把它带回来**——真手指从 y=0.78H 往上甩 3 次，`scrollTop` 一格没动。机器人在 L170 四档视口 **6 次全部卡死在这一步**，`won=false`，进关 0.8 秒就报「点不着」。

**根因（读码定位到行）**：`fitIntoStage()` 的钳制只挂在 `renderTask()` 上——

```429:433:src/games/kitty-care/arena.ts
  private renderTask(): void {
    this.paintTask();
    // 每件事摆出来的东西不一样高，钳一次再交给手指
    this.fit?.relayout();
  }
```

而搭配任务的评分面板是在**点击回调里**长出来的，走的是 `drawScore()` 这条路，它把 `.ktc-score`（规则行 + 每件一行 + 合计行，共 5 行）连同装着交卷钮的 `.ktc-tools` 一起塞进 `playEl`，**从头到尾没有再 `relayout()` 一次**：

```852:864:src/games/kitty-care/arena.ts
    const drawScore = (): void => {
      const score = scoreOutfit(picks, round.theme);
      this.playEl.textContent = "";
      this.bubble(`👗 ${round.theme} · ${score.label}`);
      const panel = el("div", "ktc-score");
      panel.appendChild(el("div", undefined, `评分规则：加分标签 +1，减分标签 −1，百搭 +1`));
      for (const line of score.lines) {
        const row = el("div", undefined, `${line.emoji} ${line.name}：${line.reason}（${line.delta >= 0 ? "+" : ""}${line.delta}）`);
        panel.appendChild(row);
      }
      panel.appendChild(el("div", undefined, `合计 ${score.total} / ${score.max} 分 · ${score.label}`));
      this.playEl.appendChild(panel);
      const tools = el("div", "ktc-tools");
```

**反证**：同一个搭配任务，第 182 关四档视口 `ok=true` 全绿。差别就在钳没钳上——390×844 的 L182 拿到了 `.ktc-fit`（`max-height:504px` + `overflow:auto`，可滚祖先 `504/568`），L170 一个都没拿到。所以这不是「内容天生太高」，是 **`drawScore()` 少调一次 `relayout()`**，再叠上 W5-F-19 那层 `overflow:hidden` 才封死的。

**测试员不改玩法代码**，按权限边界只登记不动手。这一条同时说明 **W5-F-16 只修了一半**：`renderTask()` 那条路真修好了，点击回调里重排版的那条路没覆盖。

### C3 · 🔴 触屏操作面掉出屏幕：三款四条（W5-F-17 / F-18 / F-19 的复测结果）

矮屏上「操作面被裁掉」在本档是**系统性**的。先给总量——四档视口 × 五款 × （地图 + 6 关）共 144 组，`elementFromPoint` 判不可达的元素计数：

| 视口 | 390×844 | 360×720 | 360×640 | 320×640 |
| --- | --- | --- | --- | --- |
| 点不着的元素总数 | **0** | 57 | 128 | **146** |

390×844 一个都不缺，往下每矮一档就塌一层。逐条拆开：

**① `poop-hero` 方向键（阻断）**——六颗 56×56 的 `.ph-btn`：

| 视口 | 掉出屏幕的键 | 位置 |
| --- | --- | --- |
| 360×720 | `◀` `⬇` `▶` `🧹` 4 颗 | y=739（屏高 720） |
| 360×640 | `⬆` `💨` `◀` `⬇` `▶` `🧹` **6 颗全掉** | y=646 / 708（屏高 640） |
| 320×640 | **6 颗全掉** | y=673 / 735（屏高 640） |

纯触屏打法在 360×640 / 320×640 上 `secs=0`——**一步都走不动**。键盘还能玩，但这是手机游戏。

**② `poop-hero` 双人合作手柄（阻断 + 热区不达标）**——12 颗手柄：

| 视口 | 边长 | ≥44 的 | 点得着的 |
| --- | --- | --- | --- |
| 390×844 | **41×41** | 0 / 12 | 12 / 12 |
| 360×720 | **37×37** | 0 / 12 | **0 / 12** |
| 360×640 | **37×37** | 0 / 12 | **0 / 12** |
| 320×640 | **34×34** | 0 / 12 | **0 / 12** |

**十二颗手柄没有一颗到 44**，连最宽的 390×844 也只有 41。W5-F-17 点名抬的是 `.ph-btn` / `.rbg-toggle` / `.ph-mode` / `.rbg-open` / `.fdf-zoomrow input` 五处，**双人模式那套手柄不在名单里**，所以没被抬到。

**③ `red-blue-tug` 拉绳钮（阻断）**——六关四档全中：

| 视口 | 按钮 | 位置 | 屏高 |
| --- | --- | --- | --- |
| 360×640 | `🪢 用力拉` ×2，132×76 | y=683…713 | 640 |
| 320×640 | `🪢 用力拉` ×2，116×76 | y=701…731 | 640 |

只剩键盘（`F` / 空格）能玩，触屏一下都拉不动。

**④ `find-diff` 提示键 + 放大滑杆（阻断，W5-F-18 只修了一半）**：

| 视口 | `🔎 圈出大致区域` 202×44 | 放大滑杆 88×44 |
| --- | --- | --- |
| 390×844 | ✅ 6/6 关 | ✅ 6/6 关 |
| 360×720 | ✅ 6/6 关（**这一半真修好了**） | ❌ 40 / 90 / 140 关 offscreen |
| 360×640 | ❌ **6/6 关全掉**（y=647…707） | ❌ 6/6 关全掉 |
| 320×640 | ❌ **6/6 关全掉** | ❌ 6/6 关全掉 |

提示是这一款唯一的救济机制。提示探针在 360×640 / 320×640 上**第 0 次按压就 `ok=false`**，整条提示链走不下去。

**⑤ W5-F-19 平台 `overflow:hidden` —— 还在，按要求只登记不改 `src/styles.css`**。实测 `.game-stage` 被裁掉的高度（`scrollHeight − clientHeight`）：

| 款 | 360×720 | 360×640 | 320×640 |
| --- | --- | --- | --- |
| `poop-hero` | 134 | 145–214 | 176–210 |
| `red-blue-tug` | — | 165–195 | 183–213 |
| `pinyin-train` | — | 79–111 | 79–**203** |
| `find-diff` | 41–101 | 121–181 | 121–181 |
| `kitty-care` | 84–183 | 164–170 | 170–**333** |

上面 ①—④ 都是「游戏内容比舞台高」× 「舞台 `overflow:hidden` 且无可滚祖先」两个因素叠出来的。**平台那一半交窗口 1，本档这一半（内容排布）留给本轮 learner / fixer。**

### C4 · 🔴 `kitty-care` 小屋相册：24 件收藏，20–22 个兑换钮点不着，也滑不出来（**本轮新发现，阻断**）

`📷 小屋相册` 里 24 件收藏，每件底下一颗 `⭐ N 换回来`。星星兑换是相册**唯一的主动玩法**。

| 视口 | 兑换钮总数 | 点不着 | 真手指上滑 3 次之后 | `.game-stage` | 可滚祖先 |
| --- | --- | --- | --- | --- | --- |
| 390×844 | 24 | **20** | 仍然 20 | `hidden` 730 / **2913** | **无** |
| 360×640 | 24 | **22** | 仍然 22 | `hidden` 530 / **2913** | **无** |

内容 2913px 高，舞台只给 530–730px，**裁掉 2183–2383px**，没有任何可滚祖先，真手指甩三次 `scrollTop` 一格没动。够得着的只有最上面 2–4 件（`⭐ 8` / `⭐ 10`），后面 20 件（`⭐ 12` 一路到 `⭐ 30`）**永远换不回来**。

这一条跟 C2 是同一族根因，但相册是独立入口、独立面板，单列一条。

### C5 · 🟠 关内还能点「♾️ 无尽」入口，关卡层只是 `hidden` 着继续跑（**本轮新发现，严重**）

播种解锁后进第 90 关，**关卡进行中**顶上那颗 `♾️` 入口仍然可见、`elementFromPoint` 仍然点得着。点下去之后关卡层**只是被 `hidden` 藏起来，没有被销毁**：

| 款 | 入口关内点得着 | 进关后 → 点♾️后 的活句柄 | 隐藏层里的活节点 | 隐藏的关卡秒表 |
| --- | --- | --- | --- | --- |
| `poop-hero` | ✅ | `raf 1 → 2`、`listeners 9 → 13` | 13 | 无秒表 |
| `red-blue-tug` | ✅ | `raf 1 → 2`、`listeners 8 → 11` | 8 | 无秒表 |
| `find-diff` | ✅ | `interval 1 → 2` | 28 | **还在走：52s → 47s** |
| `kitty-care` | ✅ | `interval 0 → 1`、`listeners 6 → 7` | 8 | 无秒表 |
| `pinyin-train` | — | 本款关内没有♾️入口 | — | — |

`find-diff` 后果最实：在马拉松里连查 52 秒，被藏起来的第 90 关秒表一路走到 0，然后**关卡的「就差一点点！时间到～」结算屏直接盖在正在进行的马拉松上**——同屏两个 `.fdf-hud`（`⏰ 52s` 和 `⏰ 46s`）各走各的，两条提示语并排。`poop-hero` / `red-blue-tug` 则是**两条 `requestAnimationFrame` 循环同时跑**。

**根因**：`endlessBtn` 的处理只藏不销毁，模式条也只在**进入模式之后**才隐藏，关卡进行中它一直露着：

```931:938:src/games/find-diff/index.ts
  endlessBtn.addEventListener("click", () => {
    if (mode) return;
    api.play("tap");
    levelHost.hidden = true;
    bar.hidden = true;
    modeHost.hidden = false;
    mode = mountEndless(modeHost, api, closeMode);
  });
```

**范围提示**：`levelHost.hidden = true` 这个写法全仓 **20 款同款**（`alien-seek` / `fishing-star` / `tank-battle` / `bomb-buddies` / `monster-crisis` / `bowling-lane` / `puff-bros` …），所以这是**平台级的通用写法问题**，建议交窗口 1 统一收口；本条的**证据只在本档五款上取**，不越界断言别档。

### C6 · 动物福利红线：独立复扫（`kitty-care` 专项，不认自述）

**静态**——`src/games/kitty-care/**` 全目录：

| 项 | 结果 |
| --- | --- |
| `ctx.lose` | **0 处**（唯一命中在 `careFeedback.test.ts` 的断言里，是守门测试本身） |
| `onLose(` | **0 处**（`marathonClock.test.ts:78` 那处是测试桩 `onLose: () => {}`） |
| 伤害 / 死亡词 | **0 处**（`打针`/`喂药`/`伤口`/`手术`/`流血`/`去世`/`安乐死`/`遗弃`/`照顾失败` 全无；命中的都是 `upgrade12.test.ts` 的黑名单常量与「写死的规则表」这类正常措辞） |

**真机**——四档视口逐关跑完抓 `document.body.innerText`：

| 场景 | 次数 | 伤害词 | 失败结算屏 |
| --- | --- | --- | --- |
| 通关 L40 / L140 / L188 × 四档 | 12 | **0** | **0**（`loseOverlay=false`） |
| 暖心诊所看病 L150 / L160 × 三档 | 6 | **0** | **0**（L150 收尾有「带去看兽医」） |
| 心情小屋 L130 干等 + 乱点 75 秒 × 三档 | 3 | **0** | **0**（一次都没弹） |

看病流程的口径也复核了：每种小毛病的最后一步固定是「带去看兽医」，配套那句「拿不准就交给大人和兽医，孩子不自己给动物治病」在，**没有让孩子给动物施治的步骤**。

**商标扫**：五款源码 **0 命中**（`宝可梦`/`马里奥`/`迪士尼`/`奥特曼`/`喜羊羊`/`Hello Kitty`/`海绵宝宝` 等），命中的全是各款 `upgrade12.test.ts` 里的黑名单常量。

**没做到的、如实登记**：L130 干等 + 乱点 75 秒，**心情条没能掉到 0**，所以「心情归零 → 躲纸箱 → 出来之后护理进度清不清零」（W5-L-27 / W5-L-36）这一条**本轮既没证实也没证伪**。红线本身没踩（没弹失败屏、没有伤害词），但清零行为留给后面的角色。

### C7 · 教育正确性：`pinyin-train` 换一批题核（1004 道全量复算）

第 1 轮抽的是 788 道，本轮**换题核**——把 188 关的题目全量导出，按人教版口径逐题机检 + 抽样人工复核。

**机检 0 错**：

| 检查项 | 覆盖 | 结果 |
| --- | --- | --- |
| `correct` 下标真指向 `answer` | 1004 道 | 0 错 |
| 选项内部重样 | 1004 道 | 0 处 |
| 答案不在选项里 | 1004 道 | 0 处 |
| `vowel` 题答案在 **24 个韵母**内、干扰项不许也是韵母 | 132 道 | 0 错 |
| `initial` 题答案在 **23 个声母**内、干扰项不许也是声母 | 84 道 | 0 错 |
| 整体认读音节答案在 **16 个**内 | 40 道 | 0 错 |
| 同一个多音词跨关读音一致 | 72 个词 | **0 冲突** |

题型分布：`match` 178 / `vowel` 132 / `duoyin` 118 / `tone` 122 / `initial` 84 / `context` 59 / `neutral` 51 / `syllable` 45 / `erhua` 41 / `whole` 40 / `tonemark` 38 / `sentence` 39 / `confuse` 35 / `odd` 22。

**人工复核挑出两类瑕疵**（都不是「教错」，判一般 / 建议）：

| 关 | 题 | 问题 |
| --- | --- | --- |
| L137 | 「**好学**」里的「好」读什么？ 选项 `hào / hǎo / háo`，答 `hào` | 「好学」两读**都标准**——`hào xué`（爱学）与 `hǎo xué`（容易学）。词本身不定读音，而 `hǎo` 就摆在旁边当干扰项。**这题有两个正确答案** |
| L167 | 「**同行**」里的「行」读什么？ 选项 `hǎng / háng / xíng`，答 `háng` | 同上——`tóng háng`（同业）与 `tóng xíng`（结伴走）都标准，`xíng` 也在选项里 |
| L136 | 「**困觉**」里的「觉」读什么？ 答 `jiào`（读音本身对） | 「困觉」是**方言词**（现汉标〈方〉），普通话该说「睡觉」。同一批题里已经有「睡觉」「午觉」两道，「困觉」既非标准又重复 |

对照组抽查全对：`重要 zhòng` / `重来 chóng` / `轻重 zhòng` / `重量 zhòng` / `校长 zhǎng` / `卷发 juǎn` / `数一数 shǔ` / `有空 kòng` / `空隙 kòng` / `真假 jiǎ` / `好奇 hào` / `好人 hǎo` / `只身 zhī` / `种子 zhǒng` / `归还 huán` / `自觉 jué` / `爱好 hào` / `美好 hǎo`；儿化 `面儿 miànr` / `鸟儿 niǎor`、轻声 `我们 men` / `暖和 huo` / `耳朵 duo` / `收拾 shi` 全对。

### C8 · 教育正确性：`find-diff` 提示「不直接给答案」的口径复核

两级提示，实测行为：

| 按第几下 | 按钮文案 | 屏幕上发生什么 | 泄不泄底 |
| --- | --- | --- | --- |
| 第 1 下 | `🔎 圈出大致区域（N 次）` | `.fdf-hintarea` 点亮 **6 格**（一片区域） | ✅ 不泄底，口径没问题 |
| 第 2 下 | `🎯 再点一次精确指出` | `.fdf-hintspot` 点亮 **恰好 1 格** | ❌ **就是答案格本身** |

机检确认：第 2 下点亮的那一格与该关 5 个答案里的第 2 个**完全重合**（`namedIsAnswer=true`）。一级提示「圈区域」的口径是干净的，**二级提示是直给**。

这一条第 1 轮 learner 已判为「有意设计」（W5-L-34）、tester 记为 W5C-F01 建议。按本轮任务书要求 **只登记口径、不改**，仍记建议（见 C10 · W5R2-C-13）。

同理 **W5C-Y01**（`quiz99` 闪答案）：实测 L90 / L140 打到真输的全过程，**第二次点错就出**「悄悄提示：一闪一闪的那个就是答案！」，且闪的那一颗就是正确选项（`hintIsAnswer=true`）。`quiz99.ts` 是平台共享模块，**按要求不改**，只登记（见 C10 · W5R2-C-14）。

### C9 · 难度曲线（量化）与竞态 / `destroy`

**难度曲线**（同一套机器人策略，四档视口取中位）：

| 款 | L40 | L90 | L140 | L170 | L100 | L188 | 判断 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `kitty-care` | 17.7s ★3 | 7.3s ★3 | 22.2s ★3 | **通不了**（C2） | 12.0s ★3 | 18.6s ★3 | 平缓，四档视口耗时差 <0.6s |
| `find-diff` | 2.5s ★3 | 4.0s ★3 | 4.9s ★3 | 4.9s ★3（2 轮） | 2.4s ★3 | 16.2s ★3（4 轮） | 平缓，靠「轮数」加码而非靠数值堆 |
| `red-blue-tug` | 17.0s ★2 | 15.8s ★2 | 14.0s ★3 | 12.0s ★3 | 12.2s ★3 | 15.6s ★3 | **倒挂**（见下） |
| `pinyin-train` | 5 题 | 5 题 | 7 题 | 9 题 | 6 题 | 9 题 | 题量单调上升，合理 |
| `poop-hero` | 关卡类型换得多（清扫 / 限时 / 护送 / 尘土风），机器人策略差异比关卡差异大，**不做曲线判定**（见下） | | | | | | |

`red-blue-tug` **倒挂**：越往后越快、星越高（L40 17.0s ★2 → L188 15.6s ★3）。对战四档倒是正的，但最后一级很陡——🌱小苗 10.2s → 🌿稳稳 12.4s → ⚡快手 16.2s → 👑**绳王 58.2s**，绳王比快手翻 **3.6 倍**，是全表最陡一级。

`poop-hero` L140 单独查过：机器人「蹲着走」策略 99.2s 只到 75% 清洁度判负，换「走走蹲」15.0s ★通关、「走走跳」12.0s ★通关。**同一关三种打法差 8 倍**——说明 L140 超时是**机器人策略问题，不是关卡缺陷**，不计入问题清单。同理 L170 / L100 / L188 的「尘土风追上来啦」也是策略问题，不判。

**无尽 / 双人换打法走到结算**：

| 款 · 模式 | 换的打法 | 走到哪 |
| --- | --- | --- |
| `red-blue-tug` ♾️ | 节奏型蓄力 + 左右换手 | 200 秒连赢到**第 11 个对手**；连胜牌确认会涨（第 1 局·连胜 0 → 第 2 局·连胜 1 → 第 3 局·连胜 2）。**无尽只在输的时候收工**，赢不完 |
| `red-blue-tug` 👫 | `K` / `L` / `J` 三键分别驱动蓝方 | 三键 `blueMoved` 均 **22.1**，都算数；屏幕标签是「星星按住 **K**」，**再没有「按住 J」** → **W5-L-32 / T01 不回潮** |
| `red-blue-tug` 🤝 | 四档对手逐档打 | 四档全赢，`🏆 朵朵 1:0 星星` |
| `find-diff` ♾️ | 逐格比对 + 连清不停手 | 三档视口各清 **12 轮**，换轮全部正常（见下） |
| `poop-hero` ♾️ | 一路向右 + 边走边扫 | 走到「这趟扫干净了 N 个街区」结算幕，两颗按钮都在 |
| `poop-hero` 👫 | 双角色分工（清扫 + 搬运分类） | 走到 `⭐⭐ 城市干干净净` 结算幕，但 12 颗手柄全部不达标（C3·②） |
| `kitty-care` ♾️ | 连做不停手 | **1143 个采样点 / 63 件事** |

**竞态与 `destroy`**：五款各做「同一颗键连点 24 下」+「快速进出」：

| 款 | 地图节点点得着 | 连点后新报错 | 进关后活句柄 |
| --- | --- | --- | --- |
| `poop-hero` | ✅ | 0 | `raf 0→1`、`listeners 5→9` |
| `red-blue-tug` | ✅ | 0 | `raf 0→1`、`listeners 5→8` |
| `pinyin-train` | ✅ | 0 | 全 0（纯 DOM，无循环） |
| `find-diff` | ✅ | 0 | `interval 0→2` |
| `kitty-care` | ✅ | 0 | `interval 0→1`、`listeners 5→7` |

全程 **0 `pageerror` / 0 `console.error`**（四档视口 × 五款的 `notes` 全空）。句柄增量都在合理范围，**没有量到泄漏**——唯一的例外是 C5 那条「切模式不销毁」，那是行为问题不是泄漏。

### C10 · 第 1 轮遗留逐条复测（W5-F-14…19、W5-L-22…36、W5C-Y01 / F01）

| 编号 | 第 1 轮状态 | 本轮复测结论 | 证据 |
| --- | --- | --- | --- |
| **W5-F-14** | 阻断·宣称已修 | ✅ **真修，不回潮** | `.ktc-field` 四档视口 286×148 / 261×148 / 261×148 / 232×148，`hit=true`；逗猫 / 打扮真手指做得完，L40 / L100 四档 8 次全 ★3 |
| **W5-F-15** | 阻断·宣称已修 | ✅ **真修，不回潮** | 饭碗 `relative/z auto` `hit=true` 四档全绿；L90 / L188 喂饭四档 8 次全过 |
| **W5-F-16** | 阻断·宣称已修 | ⚠️ **只修了一半** | `renderTask()` 那条路真钳上了（L140 @390×844 拿到 `max-height:504px` + `overflow:auto`）；**点击回调里重排版那条路没覆盖** → 新阻断 **W5R2-C-01**（C2） |
| **W5-F-17** | 一般·宣称已修 | ⚠️ **只修了点名的五处** | 点名的 `.ph-btn` / `.rbg-toggle` / `.ph-mode` / `.rbg-open` / `.fdf-zoomrow input` 过了；**没覆盖** `poop-hero` 双人 12 颗手柄（41/37/34px）与 `find-diff` 图格（26×26） → **W5R2-C-02 / C-09** |
| **W5-F-18** | 阻断·宣称已修 | ⚠️ **只修了 360×720 的提示键** | 360×720 提示键 ✅ 6/6；滑杆 360×720 仍掉 3 关；**≤640 两个都全掉** → **W5R2-C-04**（C3·④） |
| **W5-F-19** | 阻断·交窗口1 | 🔴 **还在**，按要求**只登记，`src/styles.css` 一行没动** | 裁掉 41–333px，四档点不着计数 0 / 57 / 128 / 146（C3·⑤） |
| **W5-L-22 / 23（K01）** | 严重·已落地 | ✅ **不回潮** | 马拉松 **1143 采样点 / 63 件事**：一次掉超 2 秒 **0 次**；55 次「秒表涨回」全部对得上完成奖励（63 件 ≈ 55 次），不是叠加；`interval` 峰值 2 / `raf` 峰值 1 / `listeners` 末值 6 |
| **W5-L-24（K02）** | 一般·已落地 | ✅ **不回潮** | 四档视口 `Infinity` / `NaN` / 负数 **0 次** |
| **W5-L-25** | 已落地 | ✅ | L140 哄睡四档全过，拍子灯无累积 |
| **W5-L-26（K03）** | 一般·已落地 | ✅ | 暖心诊所 L150 / L160 三档 6 次全过，护理键 0 次点不着，提示留得住 |
| **W5-L-27** | 已落地（此前只静态复核） | ⚪ **本轮未能定性** | L130 三档干等 + 乱点 75 秒，**心情没掉到 0**，纸箱画面没出现。红线没踩（0 结算屏 / 0 伤害词），但清零行为没验到 |
| **W5-L-28（K04）** | 一般·已落地 | ✅ | L90 洗澡四档全过，没卡在封顶 |
| **W5-L-29（K05）** | 建议·已落地 | ✅ | 每关通关都带「小屋相册收到一件新收藏：X（N/24）」，N 逐关 1→2→3→4→5 当场递增 |
| **W5-L-30 / P01** | 建议·已落地 | 见 W5-F-17 | — |
| **W5-L-31 / T02** | 建议·已落地 | 见 W5-F-17 | — |
| **W5-L-32 / T01** | 建议·已落地 | ✅ **不回潮** | `K` / `L` / `J` 三键 `blueMoved` 均 22.1；标签「星星按住 **K**」，**屏幕上再没有「按住 J」** |
| **W5-L-33** | 已落地（此前只源码守门） | ✅ **真修，不回潮**（本轮补真机行为） | 专项复验：**12 个清完的轮次 × 3 档视口**，每次清完后按 **30ms 一采、连采 900ms**，「⏰ 这一轮没找完」**0 次**。代码里 `onCleared` 先 `stopTimer()` 再 `setTimeout(...,450)` 的写法还在（`find-diff/index.ts:835-847`）。**首轮粗筛报过一次「换轮闪失败屏」，专项复验证明是机器人自己那一轮超时的真失败，不是换轮误闪——已撤回** |
| **W5-L-36** | 留第 2 轮 | ⚪ **本轮未能定性** | 同 W5-L-27 |
| **W5C-Y01** | 建议·交窗口1 | 📌 **口径登记，`quiz99.ts` 一行没动** | 连错第 2 次即闪正确答案（`hintIsAnswer=true`），见 C8 |
| **W5C-F01** | 建议·有意设计 | 📌 **口径登记，不改** | 二级提示点名答案格（`namedIsAnswer=true`），见 C8 |

### C11 · 本轮问题清单

**阻断 5 / 严重 1 / 一般 4 / 建议 4。**

| 编号 | 级别 | 游戏 | 问题 | 归属 |
| --- | --- | --- | --- | --- |
| **W5R2-C-01** | **阻断** | `kitty-care` | 时装小舞台（167–188）搭配任务：挑完三件后评分面板一撑，交卷钮 `✨ 就这套上台`（123×44，尺寸合规）被推到屏幕外，**四档视口全部 `elementFromPoint` 取不到**，`.ktc-wrap` 是 `overflow:visible / max-height:none`、**无可滚祖先**，真手指上滑 3 次 `scrollTop` 一格没动。机器人 L170 四档 6 次全卡死，**这一关通不了**。根因：`drawScore()` 在点击回调里重排版却**没有 `this.fit?.relayout()`**，钳制只挂在 `renderTask()` 上；同任务的 L182 钳上了就四档全绿 | 本档 `kitty-care/arena.ts` `drawScore()`（**W5-F-16 漏掉的另一条路**）；平台一半 W5-F-19 |
| **W5R2-C-02** | **阻断** | `poop-hero` | ①单人方向键：360×640 / 320×640 **六颗 56×56 全掉出屏幕**（y=646…739 vs 屏高 640），360×720 掉 4 颗，**纯触屏 `secs=0`，一步走不动**。②双人合作 12 颗手柄边长只有 **41 / 37 / 34px，没有一颗到 44**，且 360×720 起 **12 颗全部点不着** | 平台一半 W5-F-19；本档一半（布局 + **W5-F-17 没覆盖双人手柄**） |
| **W5R2-C-03** | **阻断** | `kitty-care` | 小屋相册 24 件收藏，**20（390×844）/ 22（360×640）颗 `⭐N 换回来` 点不着**。内容 2913px 高、舞台只给 530–730px、**无可滚祖先**，真手指甩 3 次纹丝不动。星星兑换是相册唯一主动玩法，只有最上面 2–4 件够得着 | 同上 |
| **W5R2-C-04** | **阻断** | `find-diff` | 提示键 `🔎 圈出大致区域` 202×44 与放大滑杆 88×44 在 **360×640 / 320×640 六关全部掉出屏幕**（y=647…761 vs 屏高 640），提示探针第 0 次按压就 `ok=false`；360×720 滑杆仍掉 3 关。提示是这款唯一的救济机制。**W5-F-18 只修好了 360×720 的提示键** | 同上 |
| **W5R2-C-05** | **阻断** | `red-blue-tug` | 拉绳钮 132×76 / 116×76 在 **360×640 / 320×640 六关四档全部掉出屏幕**（y=683…731 vs 屏高 640）。只剩键盘 `F` / 空格能玩，**触屏一下都拉不动** | 同上 |
| **W5R2-C-06** | **严重** | 四款 | 关卡进行中 `♾️` 入口仍可见可点，点下去关卡层**只 `hidden` 不销毁**：`find-diff` `interval 1→2`，被藏起来的关卡秒表继续走（52s→47s），52 秒后**关卡的「时间到」结算屏盖在正在进行的马拉松上**，同屏两个 `.fdf-hud` 各走各的；`poop-hero` / `red-blue-tug` **两条 `raf` 同时跑**。写法全仓 20 款同款 | 建议**交窗口1** 统一收口；证据只取本档五款 |
| **W5R2-C-07** | 一般 | 五款 | 选关地图末尾节点点不着且滑不出来：320×640 上 `poop-hero` / `red-blue-tug` / `kitty-care` 各 **14 颗**、`find-diff` 10 颗、`pinyin-train` 5 颗；360×640 各 12 / 7 颗；360×720 各 7 / 2 颗。地图页同样 `overflow:hidden` 且无可滚祖先。判一般不判阻断：`🎯 跳到当前关` 还能绕过去 | 平台 W5-F-19 |
| **W5R2-C-08** | 一般 | `pinyin-train`、`kitty-care` | 关内顶部功能键被壳顶栏盖住或掉出屏幕。`pinyin-train`：360×640 / 320×640 **六关全部** `🗺️ 选关` `covered←HEADER.game-topbar`，320×640 L140 更是 y=−26 直接出屏，`📖 攻略` 同样。`kitty-care`：360×720 起 `♾️马拉松` / `📷相册` / `🗺️选关` / `📖攻略` 被盖或出屏，320×640 L140 **四颗全掉**（y 低到 **−219**） | 平台一半（壳顶栏层级）；本档一半（内容高度） |
| **W5R2-C-09** | 一般 | `find-diff` | 图格热区 **26×26**，两条边都远低于 44 下限，六关四档每关 12–20 格全中。这是这款**唯一的点击目标**，格子还紧挨着排，点歪一格就判错点。W5-F-17 抬的五处里没有 `.fdf-cell` | 本档 `find-diff/index.ts`（`makeGrid` 的 `px`） |
| **W5R2-C-10** | 一般 | `pinyin-train` | 两道多音词题**词本身定不了读音，等于有两个正确答案**：L137「好学」答 `hào`，但 `hǎo xué`（容易学）同样标准、`hǎo` 就在选项里；L167「同行」答 `háng`，但 `tóng xíng`（结伴走）同样标准、`xíng` 也在选项里。判一般不判严重：**不是教错，是题目缺上下文** | 本档 `pinyin-train/levels.ts` |
| **W5R2-C-11** | 建议 | `red-blue-tug` | 难度曲线**倒挂**：L40 17.0s ★2 → L90 15.8s ★2 → L140 14.0s ★3 → L170 12.0s ★3 → L188 15.6s ★3，越往后越快越高星。另：对战最后一级很陡，⚡快手 16.2s → 👑绳王 **58.2s**，翻 3.6 倍 | 本档 `red-blue-tug/levels.ts` |
| **W5R2-C-12** | 建议 | `pinyin-train` | L136「**困觉**」是方言词（现汉标〈方〉），普通话该说「睡觉」。读音 `jiào` 本身没错，但同一批题里已经有「睡觉」「午觉」两道，「困觉」既非标准普通话词汇又重复 | 本档 `pinyin-train/levels.ts` |
| **W5R2-C-13** | 建议 | `find-diff` | **口径登记（不改）**：二级提示 `🎯 再点一次精确指出` 让 `.fdf-hintspot` **恰好落在唯一答案格上**（`namedIsAnswer=true`），是直给答案。一级提示 `.fdf-hintarea` 圈 6 格「大致区域」的口径干净。第 1 轮 learner 已判有意设计（W5-L-34），本轮按要求只登记 | 本档，**本轮不动** |
| **W5R2-C-14** | 建议 | `pinyin-train` | **口径登记（不改 `quiz99.ts`）**：同一题连错第 2 次即出「悄悄提示：一闪一闪的那个就是答案！」，闪的正是正确选项（`hintIsAnswer=true`）。L90 / L140 真输全程复现 | **交窗口1**（平台共享模块） |

### C12 · 五款一览

| 款 | 白屏 | 换的关 | 真赢 / 真输 | 模式换打法走结算 | 热区（四档） | 教育正确性 | 竞态 / `destroy` | 阻断 / 严重 / 一般 / 建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `poop-hero` | ✅ | 40 / 90 / 140 / 170 / 100 / 188 | ✅ L90 ★3、L40 ★2；✅ 三种真输（摔跤 / 尘土风 / 时间到） | ♾️ 走到街区结算幕 ✅ · 👫 双人走到 ⭐⭐ 结算幕 ✅ | 🔴 单人 6 键 ≤640 全掉；🔴 双人 12 柄 41/37/34px 且 ≤720 全掉 | — | ✅ 连点 24 下 0 报错 | **2** / 1 / 1 / 0 |
| `red-blue-tug` | ✅ | 40 / 90 / 140 / 170 / 100 / 188 | ✅ 六关全过（★2–★3）；✅ L170@360×720 真输 | 🤝 对战四档全赢 ✅ · ♾️ 连赢到第 11 个对手 ✅ · 👫 K/L/J 三键全算数 ✅ | 🔴 拉绳钮 ≤640 六关全掉 | — | ✅ 同上 | **1** / 1 / 1 / 1 |
| `pinyin-train` | ✅ | 40 / 90 / 140 / 170 / 100 / 188 | ✅ 六关四档全过；✅ L90 / L140 打到**真输** ★0，重开回第 1 题 | 本款无关卡外模式 | 🟡 ≤640 `🗺️选关` / `📖攻略` 被顶栏盖住；L170「miānr」选项 ≤720 出屏 | **1004 道全量复算**：结构 0 错、声韵母 0 错、整体认读 0 错、72 个多音词跨关 0 冲突；**2 道缺上下文 + 1 个方言词** | ✅ 无循环句柄 | 0 / 0 / **2** / 2 |
| `find-diff` | ✅ | 40 / 90 / 140 / 170 / 100 / 188 | ✅ 六关四档全 ★3、0 误点；✅ 关卡「时间到」+ 马拉松「没找完」两种真输 | ♾️ 三档各清 **12 轮**，**换轮 0 次闪失败屏**（W5-L-33 不回潮）✅ | 🔴 提示键 + 滑杆 ≤640 全掉；🟡 图格 26×26 | 一级提示圈区域 ✅ 干净；**二级提示直给答案**（登记口径） | ✅ 同上 | **1** / 1 / 1 / 1 |
| `kitty-care` | ✅ | 40 / 90 / 140 / 170 / 100 / 188 + 130 / 150 / 160 / 167 / 175 / 182 | ✅ 五关四档全 ★3；**设计上不判负**（零 lose，见 C6） | ♾️ 马拉松 1143 采样 / 63 件 ✅ · 📷 相册 🔴 20–22 颗兑换钮点不着 | 🔴 交卷钮 L170 四档全掉；🔴 相册；🟡 顶部四键 ≤720 被盖/出屏 | 看病收尾「带去看兽医」口径正确 | ✅ 同上 | **2** / 1 / 1 / 0 |

**档C 第 2 轮计数：阻断 5 / 严重 1 / 一般 4 / 建议 4。**

其中 **W5-F-19 判定为「还在」**，按任务书要求**只登记不改 `src/styles.css`**——上面 5 条阻断里有 4 条（C-02 / C-03 / C-04 / C-05）都是「游戏内容比舞台高」× 「舞台 `overflow:hidden` 且无可滚祖先」叠出来的，**平台那一半交窗口 1**，本档这一半（内容排布）留本轮 learner / fixer。

**没做到的、以及为什么**：

- **W5-L-27 / W5-L-36 没能定性**——L130 三档视口干等 + 乱点 75 秒，心情条**没掉到 0**，纸箱画面没触发，所以「躲纸箱出来后护理进度清不清零」这一条本轮既没证实也没证伪。动物福利红线本身**没踩**（0 结算屏 / 0 伤害词），行为留给后面的角色。
- **`poop-hero` 难度曲线没给判定**——L140 同一关三种打法差 8 倍（99.2s 判负 / 15.0s 过 / 12.0s 过），说明超时是机器人策略问题不是关卡缺陷。**我不能断定关卡有问题，故不计入问题清单**，只记未攻克。
- **`kitty-care` L170 没通关**——不是我不会打，是 W5R2-C-01 让它在四档视口上**根本通不了**，这一条本身就是那个阻断的证据。
- **`red-blue-tug` ♾️ 没走到「输」的结算**——无尽只在输的时候收工，机器人 200 秒连赢 11 个对手没输过。规则如此，**不算缺陷**，只登记。

**本轮改动的文件**：只有本文件 `docs/qa/1.2-window5-round2-tester.md` 的 `## 档C · 五款` 一节。**没有改任何玩法代码**（五款目录 0 改动），没有改平台文件，没有改 `src/styles.css` / `quiz99.ts`，**档A 节与档B 节一个字节未动**。所有真机脚本落在 `/tmp/w5r2c/` 下，跑完即删、不进提交。
