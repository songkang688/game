# 三人组 r44 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。playbook：PR #117。不回退安全区 / `--vv-h` / CTA / 平板 760 / 既有闸。不改 B 的 `.ba-lv`（N-192）、`.cs-lv`（N-193）。不并 N-144 `.fk-ch`。N-105 无第四版。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-194 | 静态扫可点 `*-lv` 关卡格 ≥44。红点 `.ba-lv` / `.cs-lv` 走 bubble-aim / candy-swing 白名单。不扫 `.mcr-card-lv` 文案。不替代 N-144/192/193。 |
