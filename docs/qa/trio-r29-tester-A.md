# 三人组 r29 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。不回退 N-117…N-146 A 面。
> **不抢** B：N-121/122/124/125/126/129/133/134/135 保龄钓鱼/139。N-105 无第四版 combo-clash/mahjong 字号。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-149 | 静态巡检 `*-open` 与按钮型 `*-mode` ≥44 或 kit `touchUpliftCss`。抽验/首修 `sky-squad` `.sks-mode`。顺手补 A 名下暗棋/点迷宫/军棋胶囊、记忆翻牌/贪吃蛇点心/拼图入口 `*-open`。N-121 三款与保龄/碰碰/钓鱼/对战王/朵星对战/勇者大厅/打地鼠入口白名单。 |

`.frc-open` / `.blp-open` 仍归 N-121，本拍不改。`.fk-mode` / `.dvs-mode` / `.bvp-mode` 留给 B。

Chrome 三视口数字见 `trio-r30-tester-A.md`（与 N-152 同页抽验）。
