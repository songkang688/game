# 三人组 r37 测试员 A

> 同分支。不抢 N-171 `.gdh-tally`、N-172 `.ak-card`。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-173 | 静态扫可点 `*-go` ≥44 或 CHIP/TOUCH 插值。对照 `.pk-go`（已 44）、`.pyt-go`（`CHIP_MIN_PX`）。豁免 `.mj-btn.mj-go` / `.jq-btn.jq-go` 与 `*-btn-go` 修饰类。`.dvs-go` 走 B 文件白名单。不扫 `.dvs-lessonbtn`。 |
