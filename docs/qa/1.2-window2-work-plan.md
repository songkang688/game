# 窗口 2 · 工作计划

基线：`origin/game-1.2`  
工作分支：`game-1.2-window2`  
范围：第 6–10 步（9 款新游戏 + 6 款升级）+ 本窗三轮验收（9 份报告）

## 实现顺序

1. 第 6 步新游戏：`dot-maze` → `fruit-stack` → `pool-stars`
2. 第 7 步新游戏：`junqi-camp` → `chess-garden` → `dark-chess`
3. 第 8 步新游戏：`hue-hand` → `hop-pads` → `tap-tiles`
4. 第 9 步升级（点名）：`gomoku` → `match-stars` → `rainbow-run`
5. 第 10 步升级（点名）：`ocean-munch` → `xiangqi` → `fight-king`
6. 本窗口三轮验收：tester / learner / fixer × 3

## 红线

- 只改本窗口独占目录；`src/styles.css` 仅末尾追加。
- 不改窗口 1 平台文件（home / rootGate / playModes / view25d）。
- `xiangqi` 只升级已有目录；`rainbow-run` 接住 1.1 的 `view3d.ts` / `controls.ts` / `endless.ts`。
- 每款 `npm test && npm run build` 全绿后再下一款。
- 商标黑名单、无死亡描写、离线、无 three.js。
