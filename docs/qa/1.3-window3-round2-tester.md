# 1.3 窗口 3 · 第 2 轮视觉验收 · 测试员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 2 轮 · A 测试员。
> 分支：`game-1.3-window3`（第 1 轮合并态 `ef72248` 之上）。只验视觉，不重开玩法验收，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step28-A-tester.md`（思路）、`docs/qa/1.3-window3-round1-{tester,fixer,learner}.md`、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、复验计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数与第 1 轮合并态（`ef72248`：732 文件 / 15342 例）差值，只增不减。
2. 逐条对账（本轮主责）：round1-tester 的 P-01~P-07 × round1-fixer 的 13 笔修复清单 + 6 条遗留清单——每条给四种结论之一：已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留；learner 报告只读对照。
3. 机器化扫描用例复核：第 1 轮沉淀的 9 款 `visual-scan.test.ts`（27 例）、C 档 `visual-fix-r1.test.ts`（46 例）、B 档 `learner-copy.test.ts`（18 例）全部还在跑、全绿。
4. 深度走查本窗**全部 9 款**（第 1 轮是广度、本轮深度）：角色特写（2× / 16px）、动效完整链（预告→过程→收尾 + reduced 退化）、场景层次（四层）、一致性（光照/描边/色板/跨界面）、性能（CPU 4× 节流 ≥30fps）。
5. 回归防线：判定热区钉死断言、reduced 下功能提示仍在、教育数据快照、destroy 泄漏（第 1 轮全 9 款过，本轮抽查进→看→退）。
6. 六大专项两轮达标率对比表。
7. 新增第 2 轮机器化用例（仅 9 款 `*.test.ts`），沉淀给第 3 轮。
8. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window3`。

（正文各节随复验推进补齐。）
