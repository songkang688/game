# 1.3 窗口 3 · 第 2 轮视觉验收 · 测试员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 2 轮 · A 测试员。
> 分支：`game-1.3-window3`（第 1 轮合并态 `ef72248` 之上）。只验视觉，不重开玩法验收，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step28-A-tester.md`（思路）、`docs/qa/1.3-window3-round1-{tester,fixer,learner}.md`、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、复验计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数与第 1 轮合并态（`ef72248`：732 文件 / 15342 例）差值，只增不减。
2. 逐条对账（本轮主责）：round1-tester 的 P-01~P-07 × round1-fixer 的 13 笔修复清单 + 6 条遗留清单——每条给四种结论之一：已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留；learner 报告只读对照。
3. 机器化扫描用例复核：第 1 轮沉淀的 9 款 `visual-scan.test.ts`（27 例）、C 档 `visual-fix-r1.test.ts`（46 例）、B 档 `learner-copy.test.ts`（18 例）全部还在跑、全绿。
4. 深度走查本窗**全部 9 款**（第 1 轮是广度、本轮深度）：角色特写（2× / 16px）、动效完整链（预告→过程→收尾 + reduced 退化）、场景层次（四层）、一致性（光照/描边/色板/跨界面）、性能（CPU 4× 节流 ≥30fps）。
5. 回归防线：判定热区钉死断言、reduced 下功能提示仍在、教育数据快照、destroy 泄漏（第 1 轮全 9 款过，本轮抽查进→看→退）。
6. 六大专项两轮达标率对比表。
7. 新增第 2 轮机器化用例（仅 9 款 `*.test.ts`），沉淀给第 3 轮。
8. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window3`。

---

## 一、全量跑数据与两轮用例数对比

> 实测环境：Node 22.14.0；Chromium 141（Playwright 1.62.1 无头），移动仿真 360×640 / 320×568、DPR 2、触屏、`prefers-reduced-motion` 双态、CDP CPU 4× 节流；`npx vite preview`（4173，构建产物实测）+ `npx vite`（5173，art 模块直调渲染）。

| 项 | 结果 |
| --- | --- |
| `npm ci` | ✅ |
| `npm test`（本轮开工基线，= `ef72248` 合并态） | ✅ **732 文件 / 15342 例全绿**（其后两笔 docs 提交不含用例，符合预期） |
| `npm test`（本轮收尾，含新增 27 例） | ✅ **741 文件 / 15369 例全绿**（见第七节收尾复跑） |
| `npm run build`（tsc --noEmit + vite build） | ✅ 全绿；PWA precache **188 项 / 5226.06 KiB**，与第 1 轮合并态一致 |
| `vite preview` 冒烟 | ✅ 首页 200；9 款进入页 18 组（reduced 双态）0 控制台错误 |

两轮用例数对比（只增不减）：

| 时点 | 文件 | 用例 | Δ用例 |
| --- | --- | --- | --- |
| 第 1 轮开工基线 `b7cd80d` | 705* | 14969 | — |
| 第 1 轮合并态 `ef72248`（tester 27 + learner 18 + fixer 46） | 732 | 15342 | +373 |
| **第 2 轮本报告态（+9 款 `visual-deep-r2.test.ts` 27 例）** | **741** | **15369** | **+27** |

\* 705 为 round1 报告口径的开工文件数；`ef72248` 的 732 含三人全部沉淀。

第 1 轮沉淀的机器化用例复核：9 款 `visual-scan.test.ts`（9 文件 × 3 = 27 例）、`visual-fix-r1.test.ts`（9 文件共 46 例：4+5+4+3+4+8+10+4+4）、`learner-copy.test.ts`（9 文件 × 2 = 18 例）——**27 个文件全部在库、全部在跑、全绿**（收尾全量跑覆盖）。

---

## 二、第 1 轮问题逐条对账（本轮主责）

### 2.1 A 档问题清单 P-01~P-07（四种结论）

| 编号 | 第 1 轮问题 | 本轮结论 | 验证证据 |
| --- | --- | --- | --- |
| P-01 | ocean-munch 360 视口主页标题「海底大胃王」右侧 ≈34px 被图鉴徽章盖住 | **已修复** | 代码：`art.ts` 新增 `titleFitPx`（3 例边界单测），`index.ts` `fitTitle("🐟 海底大胃王", 28, 24, 8, w-128)`——右界 `w-128` < 徽章左缘 `w-120`；对战/海域页同法（L2722/L3605-3606，窄屏堆叠分支保留）。实测：360 截图标题与「📖 图鉴 0/25」完全分离，0 重叠。`visual-fix-r1.test.ts` 10 例含此项，全绿。commit `02e317c` |
| P-02 | rainbow-run 360 视口战役主页标题末字与 🎁 收藏册按钮重叠 ≈18px | **已修复** | 代码：`fitTitle("🌈 彩虹跑跑 · 十二大世界", 26, 24, 10, hasCollection() ? w-54 : w-10)`——右界 `w-54` < 按钮左缘 `w-46`，且按收藏册有无自适应；章节页同法避「◀ 世界」钮（L2246）。实测：360 截图标题与 🎁 无重叠。commit `9d1c128` |
| P-03 | fight-king `.fk-sub` 13px / `.fk-mode-d` 12.5px 低于 14px 门槛 | **已修复** | 代码：两处均 `font-size:14px`，顺带 `.fk-swap` `.fk-info` `.fk-train-hint` `.fk-live` `.fk-padbtn`（含 520px 断点二次声明）`.fk-combo` 全部 ≥14px（本轮逐项 rg 复核）。实测：360 菜单截图副标题/模式卡描述清晰可读、卡片无换行撑破。`visual-fix-r1.test.ts` 4 例钉住（含媒体查询）。commit `41c5992`。注：战斗 HUD 微标签 `.fk-ch-n` `.fk-name` `.fk-clock-r` `.fk-pad-name` `.fk-fd` 共 6 处仍 12–12.5px——A 档未点名、fixer 遗留清单 #4 已登记（360 防溢出的 1.2 既定取舍），见 2.3 |
| P-04 | xiangqi「棋灵象」座位头像 🐘 emoji 兜底 | **已修复（两笔迭代到 B 档规格）** | 代码：`art.ts` `robotAvatarSVG`（复用 `PIECE_FACE` 圆面 + `PIECE_WALL` 月牙侧壁 + `BLACK_INK` 描边勾圆头/大耳/卷鼻、耳内淡红），`index.ts` `avatarHTML()` robot 分支走它。实测：对局截图座位条「棋灵象·黑」为画制小象头 + 将字棋子图标，无 emoji。本轮新增用例钉住矢量/描边/无位图。commits `a59d7a0` → `9223245`。注：`ai.ts` 难度名「🐘 小象过河/棋灵象」与教学文案 🐘 为 DOM 文本装饰前缀（规格允许类，不属 P-04 范围，登记见第四节 N-01 同族观察） |
| P-05 | 共享外壳 `level99.ts` `.l99-star` 10px 装饰星字符 | **合理遗留** | 现状未变（L472/L513），全 188 关共用外壳、非本窗职权；第 1 轮已移交统筹，本轮确认仍在遗留清单未丢 |
| P-06 | hue-hand 对手头像 🐰🐼🦊 DOM emoji 与卡面代差 | **已修复** | 代码：`art.ts` `botFaceSVG`（兔长耳/熊猫圆耳黑眼斑/狐尖耳白面颊，三张互异），`index.ts` `BOT_FACES` 全走画制，非测试源码 0 命中三只 emoji。实测：局内截图「团团·新手」头像为画制小兔。三阶达标（底色 + 1.3–1.6px 描边暗部 + 左上 `FACE_GLOSS` 高光）；与 B 档规格的差异仅「渐变底」实现为平涂底+gloss 高光，宪法「底色+暗部+高光三阶」等效满足，判定不构成不彻底。commit `b152f9a` |
| P-07 | rainbow-run 9 行 / ocean-munch 14 行画布 emoji 小图标 | **已修复（承诺范围）+ 合理遗留（登记明细）** | 已修（最高可见度）：rr 局内 HUD 💗🤍🍬⭐♪ → `drawHeartPip`/`drawNoteChip`/`drawCoinFrame`/`drawStarPickup`（实测跑动截图：三颗画制心+币/星图标在顶栏）；om BOSS 血心/玩家心心/护盾读秒/地图 👑⭐ → `drawHeartPip`/`drawShieldBadge`/`drawCrown`/`drawMiniStar`。commits `094537c` `e0821cc` `7894161`。遗留（B 档规格明示分批，fixer 遗留 #2/#3 登记）：本轮精确清点非测试源码画布 fillText emoji 行——**rr 8 行**（L2208/2301/2311/2314/2345/2490/2525/2663，战役地图锁/旗/表、结算行、无尽入口、岔路标）、**om 9 行 + 2 处数据驱动**（L2468/2471/3277/3650/3738/3751/3771/3792/3909，深度计/对手名牌/增益标/地图锁/对战节点/图鉴/提示行）。均为菜单/地图/HUD 装饰层（规格允许），移交第 3 轮酌情 |

**A 档 7 条统计：已修复 5（P-01/02/03/04/06）· 已修复+登记遗留 1（P-07）· 合理遗留 1（P-05）· 修复不彻底 0 · 修出回归 0。**
第 1 轮阻断/严重级为 0 发现，修复确认率口径下无欠账；一般级 4 条（P-01~P-04）确认全部修复。

### 2.2 C 档 13 笔修复逐笔复验（真修好没有、有没有修出新问题）

| # | 修复（commit） | 驻留验证 | 回归检查 |
| --- | --- | --- | --- |
| 1 | fight-king 字号 ≥14（`41c5992`） | rg 复核：点名两处 + 顺带 6 处全部 ≥14px | 360/320 实测 0 溢出；模式卡不换行撑破 ✅ |
| 2/3 | xiangqi 小象头像两笔（`a59d7a0`/`9223245`） | `robotAvatarSVG` 在、复用棋子材质常量 | 对局截图正常；`view.test.ts` 312 例全绿 ✅ |
| 4 | hue-hand 三头像（`b152f9a`） | `BOT_FACES` 走 `botFaceSVG`，0 emoji | 局内截图头像/牌局渲染正常，`aria-label` 读法用例全绿 ✅ |
| 5 | ocean-munch 标题避让（`02e317c`） | `titleFitPx` + 3 处 `fitTitle` 在 | 360/320 主页/海域/对战页截图无遮挡 ✅ |
| 6 | rainbow-run 标题避让（`9d1c128`） | `fitTitle` 右界自适应在 | 360 截图无重叠 ✅ |
| 7 | rr 局内 HUD 画制图标（`094537c`） | `drawHeartPip(ctx, 8, i < hearts)` / `drawNoteChip` 在 index 接入 | 跑动实测心/币/星图标显示正常，无错位 ✅ |
| 8/9 | om 心/盾/皇冠/星级画制（`e0821cc`/`7894161`） | index 接入点 L3163/L3744-3755/L4143 全在 | 深潜实测 HUD 正常 ✅ |
| 10 | tap-tiles 漂浮符号饰层（`d85dfc0`） | `drawDriftSymbols` 定义 L684、draw() 调用 L729 | alpha 0.07、判定线上方封顶、reduced 冻结用例全绿；4× 节流 60fps ✅ |
| 11 | match-stars 三主题剪影饰（`51fc06f`） | `view.ts` L130-140 三主题 background 多层在 | 静态断言（无 animation/transition）全绿；局内截图棋盘不被喧宾 ✅ |
| 12 | hop-pads 云 3→5 团（`88598e5`） | index L744-753 两段循环（3 团 + 2 团高云 alpha 0.5/0.6、高度 0.26/0.58 错开） | 实测截图云量自然不成排；`project()` 回归用例全绿 ✅ |
| 13 | gomoku 盘外暗角（`ef72248`） | `view.ts` L314-316 径向渐变（盘心圆心、外圈 `rgba(74,50,32,0.06)`），画在 drawGrid 一次填充 | 对局截图暗角极淡聚焦盘面；4× 节流 60fps（不进动画循环）✅ |

**13 笔全部驻留、配套 46 例机器用例全绿、未发现任何一笔修出回归。**

### 2.3 fixer 遗留清单 6 条对账（确认还在、没丢）

| # | 遗留项 | 本轮现状 |
| --- | --- | --- |
| 1 | level99 `.l99-star` 10px | 仍在（L513），移交统筹状态不变 ✅ |
| 2 | rr 战役地图/结算 emoji（约 6 行） | 仍在，本轮精确化为 **8 行**（含无尽入口 ♾️/任务 🎯/岔路 ◀▶，见 2.1 P-07） |
| 3 | om 地图/图鉴 emoji（约 8 行） | 仍在，本轮精确化为 **9 行 + 2 处数据驱动**（含对手名牌 `rivalProfile.emoji`） |
| 4 | fk 战斗 HUD 微标签 12–12.5px | 仍在（6 处：`.fk-ch-n` `.fk-name`×2 `.fk-clock-r` `.fk-pad-name` `.fk-fd`），1.2 既定取舍待携 360 实测评估 |
| 5 | fk 菜单模式卡 🥊🤖🏯🔥🎓 | 仍在（L1669-1673 + L725 🎓），DOM 装饰允许类 |
| 6 | hop ⏸ 已暂停（画布）/ tap ⏸（DOM） | 仍在（hop index L866；tap L400/605/608），功能提示层允许类 |

**6 条全部在册未丢，无一条被静默删除或改坏。**

---

## 三、9 款深度走查（本窗全覆盖）

> 抽样规则：本轮不抽样，**9/9 全覆盖**（窗口仅 9 款，广度即深度基数）。每款五项：①角色特写（2×≈128px 与 16px 各渲染一次，录制型 ctx 统计渐变/描边层 + 实机截图）②动效完整链（预告→过程→收尾 + reduced 退化）③场景层次（背景/中景/主体/HUD 四层）④一致性（光照左上 45°/描边 1.5–2px/粉彩板/跨界面）⑤性能（CPU 4× 节流实战 3s rAF 计帧）。
> 特写量化方法：vite dev server 直调各款 `art.ts` 绘制函数，2× 档记渐变/描边/填充层数，16px 档降采样记非透明覆盖率（cov%，空=0 / 糊满=100 都算失败）。

| 款 | ①角色特写（2× / 16px） | ②动效完整链（预告→过程→收尾） | ③场景层次 | ④一致性 | ⑤4× 帧率 |
| --- | --- | --- | --- | --- | --- |
| hue-hand | 头像/牌背 SVG 矢量（viewBox，缩放无损）；botFace 三阶（底+1.3–1.6px 描边+左上 gloss），16px cov 47.7%，剪影靠耳形可辨；牌背 2 渐变 + 四色花瓣 | 出牌：可出提示（金框/文案「摸到能出的啦」）→ 0.3s 弧线飞行替身 → 落定微旋转 ±5°；摸牌/罚牌连发同链；reduced 一个替身不造直接落位（L555，用例钉死） | 桌布渐变底 / 牌堆+弃堆中景 / 手牌卡面主体 / 连胜条+色条 HUD 四层分明；对手席气泡不抢卡面 | 卡底左上 `lighten` 提亮、白内框、四色符号从卡面到色条一致；菜单/局内/结算同套粉彩 | **60**（无尽连胜局内） |
| hop-pads | 主角脸五姿态 16px（r=6.5）不崩不空（本轮新用例），2× 多层填充（眼白/瞳孔高光/腮红）≥4；台面图案+侧壁双通道 16px 仍可辨 | 蓄力（压扁+底部蓄力条+过头收力提示）→ 起跳拉伸抛物线 → 落地尘土 / 完美金环+星屑+「踩中圆心!N 连」上屏（实测截图证）；reduced 关粒子走静态 | 天空渐变+远山 / 5 团云+浮岛（视差 0.2/0.5）/ 台子+主角（深度雾 FOG_MAX 0.35 拉纵深）/ 画布顶 HUD 计分——四层分明，云量补足后上半屏不空也不喧宾 | 眼高光左上 `(-0.24er,-0.3er)`；台顶纹+侧壁色一致制式；深度雾单调性本轮新用例钉死 | **60**（无尽跳蓄力起跳循环） |
| tap-tiles | 音符 sprite 纵向渐变+顶高光条+底厚边（144px 预渲染，16px 降采样仍成形 cov 89%）；四列列首符号 16px 两两互异（本轮新用例） | 预告：3-2-1 节拍点倒数（`countdownStep` 单调归零衔接首音，本轮新用例）→ 过程：命中列色扩散环+金六芒/白四芒分档爆点 → 收尾：连击数字弹跳上屏（`comboScale` 峰值 1.28 有界）；reduced：光束静止/连击恒 1（本轮新用例）；miss 温柔变暗不闪红 | 底渐变+舞台光束 / 漂浮符号饰层（alpha 0.07 不抢）/ 四轨音符主体 / 画布上连击+DOM 分数 HUD | 列色+符号双通道贯穿菜单列首与局内；粉彩紫夜主题一致 | **60**（无尽加速主动点轨） |
| gomoku | 黑白子 16px（r=6.5）不崩不空、序列互异（本轮新用例）；2× 径向渐变玉石画法 + 投影 + 高光，16px cov 黑 60.2% / 白 70.3% 灰度天然可分 | 落子：提示区金色脉动（预告）→ `dropScale` easeOutBack 回弹（过程）→ 落定波纹+红漆印最后一手（收尾）；胜利扫光 620ms → 五连子逐颗跳 → 结算卡三段完整；reduced 关波纹/跳子直落 | 桌面色圈+盘外暗角 / 精装木框+铜角 / 木纹盘+棋子 / 座位条计时 HUD——暗角 alpha 0.06 聚焦盘面不喧宾（实测截图证） | 高光偏移 `(-0.38r,-0.42r)` 左上（本轮新用例钉死）；菜单/对局/结算同套木色+粉彩 | **60**（自由对战连续落子） |
| match-stars | 六色棋子 SVG viewBox 矢量、形状路径两两互异（本轮新用例，sharp/heart/flower/round/chubby/burst）；顶亮 +34% 渐变+1.8px 描边+左上 -24° gloss；38px 实格截图六形可辨 | 消除：选中双色光标框（预告）→ boom 缩放淡出+星屑+5 连冲击波（过程）→ fall 残影下落 + land 压扁 8% 回弹+微尘（收尾），anim 时序回归用例全绿；reduced 粒子 0/静止/压扁照走不形变 | 主题渐变+剪影饰层（晨光云影/森林树影/星夜星点，静态）/ 深蓝星空衬板 / 棋子主体 / 订单+步数 HUD——饰层 alpha ≤0.2 不抢棋盘（实测截图证） | 六形+色双通道；gloss 全家统一左上（`glossSVG` 单一光源注释即规格）；三主题跨界面一致 | **60**（无尽订单连续消除） |
| rainbow-run | 跑者 2×：11 处描边+多层填充（发带/披风/双臂/姿态脸），16px cov 44.5%；星币 8 帧 16px（r=5.5）逐帧不崩不空（本轮新用例）；LOD 金点恒用金板（本轮新用例） | 金币：旋转 8 帧+1.2s 扫光（预告性吸引）→ 磁吸残影（过程）→ +5 飘字+星屑（收尾）；里程碑横幅「第 N 世界」0.8s；reduced 关扫光（L1892 `!reducedMotion` 分支）；追风棉花云横幅警告常显不靠闪烁 | 天空渐变 / 12 世界视差剪影+彩虹路肩 / 透视路面+障碍立面+跑者（近大远小+接地影统一）/ 画布顶 HUD（画制心/币/星）——伪 3D 纵深实测截图收敛正确 | 币高光 `(-0.35rx,-0.4r)` 左上（本轮新用例钉死）、1.5px 深金描边；战役页/局内/结算同套糖果粉彩 | **60**（换道练习 8 次换道 + 无尽入口复测 60） |
| ocean-munch | 鱼身 2×：1 渐变+5 描边（背腹两档+鳍鳞），16px cov 金冠 30.5% / 银星带 33.2%，三头饰序列互异（本轮新用例）；实测玩家粉鱼金冠 16px 头顶一笔仍可辨 | 吞吃：嘴张大一帧（`mouthOpen01` 预告）→ 被吞方旋入（`swallowStretch` 过程）→ 气泡+飘字（收尾）；BOSS：暗角+剪影游出（进场）→ 红眼加速（过程）→ 翻肚缓沉+星币迸出（战败收尾）；reduced 粒子砍半/光柱静止 | 深度渐变+远景剪影鱼（视差 0.3）/ 斜光柱 ±3° / 主层鱼群+玩家 / 前景水草（视差 1.3，低画质砍装饰不砍主层，本轮新用例钉死 `layerToggles`）+ 顶栏 HUD | 气泡左上高光弧；毒藻绿光环独立于体色且 **reduced 恒 0.62 可见**（本轮新用例）；主页/海域/局内一致 | **48.3–54**（深海马拉松下潜高峰，与 round1 47fps 同水位，≥30 达标） |
| xiangqi | 帅/将 sprite 2× cov 57.3%、16px（r=6.5）cov 63.3% 刻字盘面仍显厚度；座位将/帅小图标矢量红黑互异（本轮新用例）；小象头像矢量+描边无位图（本轮新用例） | 走子：lift 浮起+可走点（预告）→ 160ms 滑动（过程）→ 1.08 回弹+落点波纹+罗盘印记（收尾，实测截图印记可见）；吃子花瓣+大子金环；将军红光呼吸 2 次→静态描边+「将军!」徽章（reduced 静态不消失，本轮新用例钉死源码口径） | 桌面 / 双层木框+如意云角 / 盘面（楚河汉界水波+位点星）+棋子 / 座位条+吃子槽 HUD 四层分明 | 渐变高光 `(-0.35r,-0.4r)` 左上（本轮新用例钉死）；红 `#C23B2E` 黑 `#2F3A2F` 字形+墨色双通道；菜单/对局/结算一致 | **60**（自由对战 vs 棋灵象走子） |
| fight-king | 八位头饰 headR=5（16px 档）不崩不空、序列两两互异（本轮新用例）；镜像同角色实测：P1 粉花头标+粉脚环 vs P2 蓝星头标+蓝脚环双通道可辨（星星 vs 星星截图证） | 出招：`swingTip` 三段收放前摇（预告）→ 命中火花三档（轻 4 线/重 8 线+星爆/破防 6 盾片）+引擎 hitstop（过程）→ 扬尘+连击数字+倒地弹（收尾）；reduced：残影/流光/白闪关、视差静态一帧不消失（`drawSideMarker` reduced 仍绘制，本轮新用例） | 天空+太阳+花瓣 / 远山粉丘 / 近景圆树+主体擂台（地面三阶渐变带近亮远暗）/ 画制头像+元气条+回合星 HUD——樱花道场实测截图四层分明 | lineWidth 1.5/2 描边、粉彩行头八套；选人/局内/结算头像同源画制 | **60**（双人对战键盘连招高峰） |

**五项 × 9 款 = 45 项全部达标；无一款在 2×/16px 两档出现「糊成色块 / 画空 / 单笔平涂」。**

---

## 四、本轮新发现问题清单（全部量化）

| 编号 | 位置（文件+函数） | 现状（量测） | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| N-01 | `src/games/gomoku/ai.ts` L1354-1359 `DIFFICULTY_NAME`、`src/games/xiangqi/ai.ts` L51-52 | 棋灵系对手在**模式选择按钮/难度标签文本**里带 emoji 前缀（gomoku 🐣🐱🦊🐉🐘🌌 六档、xiangqi 🐘 两档）；xiangqi 座位头像已画制（P-04），但两款棋灵家族在菜单层仍是 DOM 文本 emoji，与画制小象头像同屏有轻代差（实测选择页截图） | 第 3 轮酌情：棋灵家族画制小头像（可复用 xiangqi `robotAvatarSVG` 的材质套路），或统一保留为文本前缀并登记 | 建议 | gomoku/xiangqi 各自 index.ts + art.ts（C 档酌情） |
| N-02 | `src/games/rainbow-run/index.ts` L2345/L2490/L2525/L2663 | fixer 遗留 #2 登记「约 6 行」，本轮精确清点为 **8 行**（多出：无尽入口 ♾️、任务行 🎯、结算行 🍬⭐、岔路标 ◀▶）——数量登记与实况有 +2 偏差，需在遗留台账上修正防止第 3 轮漏账 | 遗留台账按 8 行口径接手（本报告 2.1/2.3 已修正） | 建议（台账勘误，非代码问题） | 第 3 轮 tester/fixer 对账口径 |
| N-03 | `src/games/ocean-munch/index.ts` L2471 | 对战 HUD 对手名牌 `⏱ ${left} 秒 · 对手 ${rivalProfile.emoji} ${name}` 为画布 fillText 直出对手 emoji（数据驱动），fixer 遗留 #3 概述为「地图/图鉴层」未单列此行；属装饰名牌允许类，但它在**局内 HUD**（可见度高于地图层） | 第 3 轮优先级内画制对手小头像（与玩家金冠/银星带同套），或至少单列登记 | 建议 | ocean-munch/index.ts（C 档酌情） |
| N-04 | `src/games/ocean-munch/index.ts` 深海马拉松 | CPU 4× 节流下潜高峰 **48.3fps**（本窗最低，其余 8 款全 60）；≥30 门槛达标且与 round1 的 47fps 同水位，无回归，但若第 3 轮再往水体加饰层，此款余量最小 | 第 3 轮加饰层前先复测此款帧率，勿在 `high` 档再加常驻粒子 | 建议（预警登记，非缺陷） | ocean-munch（B/C 档参考） |

**统计：阻断 0 · 严重 0 · 一般 0 · 建议 4（N-01~N-04，其中 2 条为台账勘误/预警类）。本轮无「当轮需清零」的阻断项。**

红线抽查（顺带复核）：tap-tiles 失败页「有个音符溜走啦…再来一次一定能更远!」「点到空白格啦…」只鼓励 ✅；fight-king 条子写「元气」、命中星形火花无血 ✅；ocean-munch 战败翻肚缓沉 ✅；hue-hand 正式源码零「UNO」独立词（本轮新用例加严钉死）✅；18 组进入（含 reduced）0 报错、无广告内购入口 ✅。

---

## 五、六大专项两轮达标率对比

| 专项 | 第 1 轮（9 款） | 第 2 轮（9 款） | 变化 |
| --- | --- | --- | --- |
| ① 火柴人 / emoji 直出（核心资产） | 9/9（装饰层 rr 9 行 / om 14 行登记） | **9/9**（修复后装饰层降至 rr 8 行 / om 11 行±，局内 HUD 高频 emoji 清零；核心资产 0 直出复扫确认） | 持平达标，装饰余量收窄 |
| ② 金币 / 收集物体积 | 9/9 | **9/9**（本轮加严：LOD 金点色板恒定、2× 渐变+描边层数、16px 覆盖率三重新哨兵） | 持平达标，哨兵加深 |
| ③ 双人 / 对阵可区分 | 9/9（灰度量化 28+ 对） | **9/9**（本轮补镜像同角色实测：fk 星星 vs 星星靠头标+脚环双通道可辨；om 金冠/银星带 16px 序列互异钉死） | 持平达标，补盲区 |
| ④ 2.5D 纵深（点名 4 款） | 4/4（其余 5 款精 2D 合规） | **4/4**（hop 深度雾单调契约、rr 透视路面实测、om 分档砍装饰不砍主层、fk 地面三阶带全部复验） | 持平达标 |
| ⑤ 360px 布局 | 9/9 硬溢出 0（画布内遮挡 P-01/02 遗留） | **9/9**（360/320 双档 18 组硬溢出 0px + 非滚动容器越界 0px；P-01/02 画布遮挡修复后实测 0 重叠） | **提升：画布内遮挡清零** |
| ⑥ 商标 | 9/9（0 命中） | **9/9**（visual-scan 23 词黑名单持续全绿 + 本轮 hue-hand 加严「UNO」独立词 0 命中；造型目检无新增风险） | 持平达标，黑名单加严 |

**54/54 → 54/54 全绿；专项⑤实质提升（一般级画布遮挡两处清零），专项①装饰层余量收窄，专项②③⑥哨兵加深。**

---

## 六、回归防线复查

1. **判定热区**：hop-pads `project()`、rainbow-run `view3d.ts` 全导出、tap-tiles `judgeY`/approach、gomoku `px()`/`cs()`、xiangqi `px()/py()`、match-stars `cellPitch`、fight-king `frames.ts` 判定框——钉死断言全部在跑全绿（回归用例分布于各款 index/art/view/view3d.test），本轮 13 笔修复无一触碰判定文件。
2. **功能提示不被动画吃掉**：ocean-munch 毒藻光环 reduced 恒 0.62（本轮函数级新用例）；xiangqi 将军 reduced 静态描边+文字徽章；fight-king 视差 reduced 静态一帧不消失、边侧标记 reduced 仍绘制（本轮新用例）；rainbow-run 追风云警告为常显横幅。全部通过。
3. **教育/内容数据**：tap-tiles `chart.test.ts` 谱面、gomoku `puzzles.test.ts` 题库、hue-hand `deck.test.ts` 牌组、xiangqi `endgames.test.ts` 残局、match-stars `levels188.test.ts`——快照/契约断言全绿，无一改动。
4. **destroy 泄漏**：9 款 × reduced 双态 = 18 组「进 → 看 → 退」实测：退出后 rAF **0 次/秒**、页面 0 报错（18/18）。第 1 轮无问题款，本轮抽查口径全过。

---

## 七、收尾与交接第 3 轮

- 本轮提交：`263b888`（9 款 `visual-deep-r2.test.ts` 27 例）+ 本报告（含前置计划 `d1d82a1`）。
- 收尾全量：`npm test` **741 文件 / 15369 例全绿**（+9 文件 / +27 例，只增不减）；`npm run build` 全绿（tsc + vite，PWA 188 项 / 5226.06 KiB）。
- 交接第 3 轮的活口：N-01~N-03 建议项、遗留台账 6 条（rr 按 8 行 / om 按 9+2 行新口径）、om 帧率余量预警（N-04）、P-05 统筹项跟催。
- 三方合并态复验与合并态 SHA 由本轮 C 档执行后补记于 fixer 报告，本报告数据可复现：`npm ci && npm test && npm run build`；浏览器实测方法见第一节注。
