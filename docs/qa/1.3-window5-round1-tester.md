# 1.3 窗口 5 · 第 1 轮视觉验收 · 测试员报告

> 状态：**扫描计划已立项，执行中**。本文件由窗口 5 第 1 轮视觉测试员独占书写，learner / fixer 报告另立文件，互不触碰。
> 基线：`origin/game-1.3`；工作分支：`game-1.3-window5`。
> 方法对齐 1.3 第 27 步 A 档测试员，范围收窄为本窗 10 款：
> `shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。

## 〇、扫描计划（动手前立项）

1. **全量跑与视觉盘点**：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积；清点本窗 10 款 `src/games/<id>/` 与 `src/art/kit/` 本窗新增文件归属（`git log --follow` 抽查）；对照第 14–16 步升级清单核对 PLAN / visual13 / 测试提交。
2. **六大专项**（只覆盖本窗 10 款 + 它们新增的 kit 文件）：
   - ① 火柴人 / 草稿圆 / emoji 直出扫描（grep `fillText(` + 逐款目检）；
   - ② 金币 / 星星 / 雪球 / 保龄球等收集物体积（≥2 停渐变 + 高光 + 描边或落影之一）；
   - ③ 双人款 A/B 16px 灰度可区分（`ice-fire-forest` / `puff-bros` / `prince-princess` / `bomb-buddies` / `snow-fight`）；
   - ④ 2.5D 纵深（`sky-squad` 云层视差+投影、`tank-battle` 顶侧双面、`bumper-cars` 围栏立面、`bowling-lane` 球道透视收敛+木纹）；
   - ⑤ 360×640 与 320px 极限布局实测（溢出 / 遮挡 / 字号 <14px / 触区 <40px，逐款记像素数）；
   - ⑥ 商标扫描（黑名单 grep + 逐款目检，任何命中记「阻断」）。
3. **横向一致性**：kit 光照约定（左上 45°、高光偏移、描边 1.5–2px）遵守情况；`prefers-reduced-motion` 装饰停 / 功能留；进→看→退→再进 ×5 的 destroy 泄漏；粒子重的 6 款 CPU 4× 节流帧率。
4. **产出**：本报告六大专项各一节 + 问题清单（位置 / 现状 / 期望 / 严重度 / 建议责任文件，全部量化）+ 10×6 结果矩阵；机器化扫描用例沉淀到本窗 10 款 `*.test.ts`。
5. **权限边界**：只写本报告与本窗 10 款 `*.test.ts` 新增用例；不改 `index.ts` / `visual13.ts` 绘制实现、不改 kit 已有文件、不碰 `parentAuth` / `parentGate`、禁 three.js。

（以下各节在扫描完成后填写。）
