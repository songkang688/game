# 窗口 2 · 1.2 三轮验收 第 3 轮 · 学习优化员 · 汇总

> 各包只写自己那一节，别的节留给对应的包自己填。

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 完整明细：[`docs/qa/1.2-window2-round3-learner-packB.md`](./1.2-window2-round3-learner-packB.md)
- 模型：`claude-opus-5-thinking-high-fast`
- 基线：`origin/game-1.2-window2` @ `8816924`（258 文件 / 6608 例全绿）
- 结论：**列了 10 条，本轮落地 5 条，记录 5 条**（1 条留修复员、2 条并进平衡专项、2 条记给主管）。
- 输入是本轮测试员包 B 的 **阻断 0 · 严重 0 · 一般 3**。三条里 **R3B-1 落地**、
  **R3B-2（朵朵后段）一个字节不碰**（`fight-king` 整个目录 diff 为空）、**R3B-3 只记录**（理由见下）。
- 新增 **0 个测试文件 / +27 条用例**（全部加进既有测试文件），既有断言**一条没删、没放宽**。

### 改进清单

| # | 款 | 改进 | 本轮落地 | 收益（实测） |
|---|---|---|---|---|
| P3B-1 | hue-hand | 牌都用完、谁也接不上时收成平局；对战 / 双人 / 无尽三条出口都认平局（R3B-1） | ✅ | 那个残局以前连摸 200 次真摸到 0 张、`moves` 一路涨到 200；现在摸一次就收口。**188 关关卡表逐关比对，指纹完全一致** |
| P3B-2 | hop-pads | 方向键 / `WASD` 按到就闪一句「这一款只用一个键」，**不给它们接语义**（R2B-5 / P2B-9） | ✅ | 八个键逐个按，`phase` 全程 `ready`、座数 0；同样的力度跳出来的结果一字不差 |
| P3B-3 | dark-chess | 记牌面板从「红还盖着 12」列到兵种：「红还盖着 16 · 帅1 仕2 相2 俥2 傌2 炮2 兵5」 | ✅ | 暗棋真正要记的那半截回来了；翻光的兵种自动不占位置，仍旧两行 |
| P3B-4 | hue-hand | 「就一张」抢按窗口给可见倒数（钮上 3/2/1 + 催促配色） | ✅ | 窗口时长 1800ms **一毫秒没动**（用例守住 `-1ms` 不罚、`+1ms` 就罚） |
| P3B-5 | dark-chess | 手数上限也给倒数（残局关只有 60 手）；两条线同时逼近时只摆更紧的那一枚 | ✅ | 不再「怎么突然就结束了」；规则与两条收场路径的结算文案一个字没改 |
| P3B-6 | hue-hand | 相邻档位分层弱（R3B-3） | ❌ | 关卡表是 `simulateGame()` 真打出来的，动 `rate()` 要整张重算 188 关 + 重跑胜率门槛 —— 正是本轮不许做的两件事。**根因证据已量清交出**（见下） |
| P3B-7 | fight-king | 朵朵后段整片 0/8（R3B-2） | ❌ | 留给修复员。本轮 `fight-king` 一个字节没改，不在他量胜率表的过程中挪地面 |
| P3B-8 | fight-king | 无尽天花板（R2B-9）、`foeIdOf` 跨章撞车（R2B-11） | ❌ | 与 R3B-2 在同一张角色数值 / 增益 / 对手轮转表上，建议**合成一个平衡专项**一次收掉三条 |
| P3B-9 | fight-king | 训练场进不了 `meta.modes`（R2B-8） | ❌ | `src/engine/types.ts` 的平台枚举，按纪律不碰，**记给主管**（连着第 3 轮记同一条） |
| P3B-10 | tap-tiles | 无尽 `ENDLESS_MAX_MISS = 0` 且点空即收工，对小小孩偏严 | ❌ | 动它等于改无尽记录口径，且要改 `judge.test.ts` 的既有断言，**要主管定** |

### R3B-3 的根因（本轮新量，交给平衡专项）

四档两两互打各 120 局、**正反座位各跑一遍**（合计 240 局）：

| 对局 | 强的那一档拿下 |
|---|---|
| 新手 vs 普通 | 普通 123 / 240（51%） |
| 新手 vs 高手 / 大师 | 高手、大师各 **240 / 240**（100%） |
| 普通 vs 高手 | 高手 128 / 240（53%） |
| 普通 vs 大师 | 大师 132 / 240（55%） |
| 高手 vs 大师 | **大师 118 / 240（49%）—— 大师其实不比高手强** |

两条结论：① 真正的分水岭是 `aiCatchesOneCard`（rank ≥ 2 才会点破忘喊），
新手自己不喊「就一张」，一到 1 张就被罚抽，于是对高手 / 大师是 0 : 120 的碾压，
而 `rate()` 那套打分在相邻两档之间几乎不产生胜负差；
② 大师那层 `rank >= 3` 的加料是**负收益**，最可疑的是「加牌牌先攒着卡链」的 `-5`
与续链时 `pick = stacks[0]`（拿手上第一张能续的，而不是打分最高的那张）。

### 改了哪些文件

`hue-hand/{rules,sim,index}.ts` · `hop-pads/index.ts` · `dark-chess/{view,index}.ts`，
外加这三款的既有测试文件（`hue-hand/{rules,index,ai}.test.ts` · `hop-pads/index.test.ts` ·
`dark-chess/{smoke,hud}.test.ts`）。**一共 12 个文件，全在本包 5 款目录内。**
`tap-tiles` 与 `fight-king` 两个目录 **diff 为空**；`src/engine/**`、窗口 1 平台文件、
包 A / 包 C 的 10 款、`src/styles.css` **一个字节都没碰**。

### 不回滚复核

- 第 2 轮清零的 **R2B-1**（`dark-chess` 双人光标，`view.ts` 的 `onKey` / `cursors` / `clickCell` diff 为空）、
  **R2B-2**（`hop-pads` 同一把 16 尺，`ai.ts` diff 为空）；
  第 1 轮清零的 **B-2**（`tap-tiles` 打平判平局，整个目录 diff 为空）、
  **B-6**（`hop-pads` 按指号分台，输入段 diff 为空）—— **一条都没回潮**。
- 第 1 轮 **L-1 ~ L-8**、第 2 轮 **P2B-1 ~ P2B-6** 逐条复核仍生效，**P2B-6 的 12px 字号没回滚**。
  P2B-5 的和棋倒数没被 P3B-5 挤掉：新的手数倒数**只在和棋倒数不摆的时候**才出现。

### 红线

- 商标黑名单 **真命中 0**（5 款去掉自带守卫用例后 90 个文件、35 条商标词逐条扫；
  报出的 19 处全是基线自带的子串误报或反义句：`runOne` 含 `uno`、
  「掉下去**不是**死亡」、`PLAN.md` 的「无血、无伤、无死亡描写」、`hero.test.ts` 的黑名单字面量、
  以及镜头注释里的「一跳一跳」含「跳一跳」）。
- 角色只有朵朵 / 星星，本轮没有新增角色名；无血、无死亡，失败只鼓励。
- 没引外部依赖（`package.json` / `package-lock.json` 未改），**没有 three.js / CDN**。
- `npm test` **258 文件 / 6635 例全绿**（基线 258 / 6608，本包净增 **+27 例**）；
  `npm run build` 全绿（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）。

---

## Pack A

> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-learner-packA.md`
> 基线：`d10f3c5`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 列出 | **14 条** |
| 落地 | **10 条**（7 个 commit，每条自带测试） |
| 只列不落 | **4 条**（2 条是明令不动的记录例外，2 条超出「小而稳」范围，留给 1.3） |
| 新增用例 | **33 条**（删除 0 条） |

### 取材角度

第 3 轮测试员包 A 记的是 **阻断 0 / 严重 0 / 一般 1**（`R3-PA-1`，明令不修），
第 1 / 2 轮的 17 条一般项已全部有终态——**本轮没有「照着缺陷单修」的活**。
所以改从「测试员的七条铁则量不到、但孩子真会碰到」的地方取材，定三条口径：

1. **五款之间口径要一致**：同一套动作（暂停、取消、归位）在五款里得是同一个脾气；
2. **触屏与键盘两条通路都要走得通**：铁则量的是热区够不够 44px，量不到的是「手机上这条路到底有没有入口」；
3. **读屏能不能听懂**：`aria-label` 写了什么、光标在哪、按钮为什么点不动。

挑出来的 10 条全是「加一道闸 / 补一个钮 / 补一个属性」，
**不碰关卡表、不碰难度标定、不碰物理判定**。

### 落地的 10 条

| # | 款 | 一句话 | 归类 | commit |
|---|---|---|---|---|
| `L3A-1` | dot-maze | 暂停期间除 `Esc` 外一律不接：键盘 / 虚拟方向键 / 滑动三条通路都不再把转向攒到恢复那一刻 | 口径一致 | `2530c00` |
| `L3A-2` | dot-maze | 方向键盘左上角的占位格改成 `⏸` 暂停钮（≥44px）——在这之前手机上没有暂停入口 | 触屏通路 | `b856192` |
| `L3A-3` | dot-maze | 剩下那个占位 `<button>` 补 `aria-hidden` + `tabindex="-1"`，读屏不再念空按钮 | 读屏 | `b856192` |
| `L3A-4` | pool-stars | 球桌加 `⏸ 暂停` 钮与 `.ps-veil` 遮罩（样式早写好没人用）；**顺手抓到真 bug：遮罩盖着时电脑那一杆照样会打出去**，现在一起停住 | 触屏通路 | `1a0eb5f` |
| `L3A-5` | fruit-stack | 暂停闸补到指针通路：拖盆瞄准 / `◀▶` / 放下 / 归位五处，此前全靠遮罩挡在手指前面 | 口径一致 | `0977b57` |
| `L3A-6` | fruit-stack | 人机对战那一行提示补上朵朵的归位键 `G`（双人那一行一直写着） | 文案 | `0977b57` |
| `L3A-7` | junqi-camp | 暂停时「确认 / 取消」置灰并报 `aria-disabled`（`.jq-off` 样式早写好没人用）；只改外观不改行为 | 口径一致 | `c9d04c5` |
| `L3A-8` | junqi-camp | `➖` `➕` `🏠` 三个钮补读屏说明，不再只念得出符号 | 读屏 | `c9d04c5` |
| `L3A-9` | chess-garden | 棋盘 64 格补读屏说明：光标（`aria-current`）/ 已选中 / 可走点 / 可吃 / 被将军一并读出来——这四样界面上全是用颜色画的 | 读屏 | `f3f9817` |
| `L3A-10` | dot-maze | 三种玩法的说明与攻略都写明暂停怎么按 | 文案 | `b856192`、`a618064` |

### 只列不落的 4 条

| # | 款 | 为什么 |
|---|---|---|
| `L3A-11` | pool-stars | 自由球摆放补键盘通路要新引一套摆球状态机、还要跟瞄准 / 蓄力抢键位，改面超出「小而稳」，留给 1.3 |
| `L3A-12` | fruit-stack | 即 `R3-PA-1`，**明令不修**（要动物理收敛判定，波及 188 关门槛） |
| `L3A-13` | chess-garden | 残局连胜加档 / 扩题面池属难度与内容重标定，要重跑 AI 胜率门槛 |
| `L3A-14` | chess-garden | 即 `R2-PA-3`，**记录在案的例外，不拆**；那条过期提醒断言继续留着 |

### 边界自查

- 只动本包 5 款；别人的 10 款、`src/engine/**`、窗口 1 平台文件 **0 改动**。
- **`src/styles.css` 本轮 0 改动**——本包用到的 `.ps-veil` `.jq-off` `.dmz-pause` 都在各自游戏文件的样式串里。
- 测试员 / 修复员报告原文 0 改动；`R3-PA-1` 与 `R2-PA-3` 都没碰。
- 第 1 / 2 轮已落地的手感与修复一条没回滚。
- 用例只增不减：`.test.ts` 的 diff 里删除的 `it(` 与 `expect(` 都是 0 行。
- 关卡表 / 难度标定 / AI 胜率门槛一个数没动；`package.json` 未动，不引 three.js。
- 无血无死亡描写、角色只用朵朵 / 星星、商标 0 命中，新增文案全过各款自带的红线筛子。

### 水位

| 项 | 结果 |
|---|---|
| 本包五款自己的测试 | **36 文件 / 817 例全绿**（第 3 轮测试员收尾是 784 例，+33 全是本轮新增） |
| `npm test` | ✅ 全绿 |
| `npm run build` | ✅ 通过 |
| `tsc --noEmit` | ✅ 无错误 |
