# 窗口 2 · 1.2 三轮验收 第 3 轮 · 学习优化员 · 包 A（本窗口收官）

> 角色：QA 第 3 轮学习优化员 · 包 A
> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 基线：`origin/game-1.2-window2`（`d10f3c5`）
> 模型：`claude-opus-5-thinking-high-fast`

## 〇、调研与落地计划（动代码前先立此存照）

### 0.1 边界（红线）

- **只改上面这 5 个游戏目录**。别人的 10 款、`src/engine/**`、窗口 1 的平台文件
  （`src/ui/**`、`src/styles.css` 里不属于本包的选择器）一个字不动；
  `src/styles.css` 只许追加或修改**本包自己的选择器**。
- **不改测试员 / 修复员的报告原文**（`docs/qa/1.2-window2-round1-*.md`、`round2-*.md`、`round3-tester*.md`）。
- **不修 `R3-PA-1`**（fruit-stack 极限环）：测试员写明修法要动 `allSettled` / `settleWorld` 的物理收敛判定，
  会波及全部 188 关的过关判定与星级门槛，留给 1.3 / 修复员评估。本轮只列不落。
- **不拆 `R2-PA-3` 的棋盘格例外**（`.cg-sq` 41.4px）：那 2.6px 的差额只能靠动窗口 1 的 `.screen`
  内边距补上，属跨包。例外的过期提醒断言继续留着，本轮不碰。
- 第 1 / 2 轮已落地的手感与修复**一条不回滚**：
  第 1 轮 `L-A-1`…`L-A-6`、第 2 轮 `L2A-1`…`L2A-8` / `L2A-11a`、
  修复员的 `PA-DM-2` `PA-FS-3` `PA-CG-4` `R2-PA-3` `R2-PA-4`。
- 不重写 188 关关卡表、不动难度标定、不重跑 AI 胜率门槛。
- 用例只增不减：既有断言只翻面、不删除、不放宽。
- 无血无死亡描写；角色只用朵朵 / 星星；商标黑名单 0 命中；不引 three.js、不引任何外部运行时依赖。
- 报告两处：本文件（独占）与 `docs/qa/1.2-window2-round3-learner.md` 的 `## Pack A` 一节（**只追加**）。
- 只推 `origin/game-1.2-window2`，被拒就 fetch + rebase 再普通 push，禁止 `--force`，
  不用 `gh` 开 / 改 / 合 PR。

### 0.2 本轮的取材口径

第 3 轮测试员包 A 记的是 **阻断 0 / 严重 0 / 一般 1**（`R3-PA-1`，明令不修），
第 1 / 2 轮登记的 17 条一般项已全部有终态。也就是说：**本轮没有「照着缺陷单修」的活可干**，
学习优化员该做的是从五款自己走一遍，找**测试员的七条铁则量不到、但孩子真会碰到**的粗糙处。

本轮的取材角度定三条：

1. **五款之间的口径要一致**。同一套动作（暂停、取消、归位）在五款里得是同一个脾气。
   哪一款少一道闸、少一个入口，就是那一款的粗糙处。
2. **触屏与键盘两条通路都要走得通**。测试员量的是「热区够不够 44px」，
   量不到的是「这条路在手机上到底有没有入口」。
3. **读屏能不能听懂**。`aria-label` 写了什么、光标在哪、按钮为什么点不动，
   这些不进热区表，但直接决定看不清屏幕的孩子能不能玩。

按这三条把五款过了一遍，挑出来的都是「加一道闸 / 补一个钮 / 补一个属性」的改法，
**小而稳，不碰关卡表、不碰难度、不碰物理判定**。

### 0.3 执行顺序

1. 本 commit：只落调研与计划，不动任何代码。
2. 逐条落地，每条一个独立 commit，自带测试。
3. 回填本报告与 `docs/qa/1.2-window2-round3-learner.md` 的 `## Pack A`。
4. fetch + rebase `origin/game-1.2-window2`（tester B / C 可能同时在推）。
5. `npm test` + `npm run build` 全绿后普通 push。

### 0.4 本轮列出的改进（**列 14 条**，先立此存照，落地情况见第一节）

| # | 款 | 一句话 | 归类 | 打算落地 |
|---|---|---|---|---|
| L3A-1 | dot-maze | 暂停期间除 `Esc` 外一律不接：方向键 / 虚拟方向键 / 滑动 / `G` / `K` 现在都还会被缓冲下来，恢复的那一瞬间集体生效 | 口径一致 | ✅ |
| L3A-2 | dot-maze | 触屏没有暂停入口：方向键盘左上角那个占位格改成 `⏸ 暂停` 钮（≥44px） | 触屏通路 | ✅ |
| L3A-3 | dot-maze | 方向键盘里两个占位 `<button>` 只做了 `visibility:hidden`，读屏仍会念到两个空按钮 | 读屏 | ✅ |
| L3A-4 | pool-stars | 触屏没有暂停入口；而且暂停时只换了一行提示文字，样式表里那套 `.ps-veil` 遮罩写好了却没人用 | 触屏通路 | ✅ |
| L3A-5 | fruit-stack | 暂停闸只挡了键盘：盆上拖动与「◀ ▶ 放下」三个钮在暂停期间程序上照样接，全靠遮罩挡在前面 | 口径一致 | ✅ |
| L3A-6 | fruit-stack | 人机对战那一行提示漏了朵朵的归位键 `G`（双人那一行写了） | 文案 | ✅ |
| L3A-7 | junqi-camp | 暂停时「确认 / 取消」点了没反应，可按钮长得跟能点一样；样式表里 `.jq-off` 写好了却没人用 | 口径一致 | ✅ |
| L3A-8 | junqi-camp | 工具条上 `➖` `➕` `🏠` 三个钮没有 `aria-label`，读屏只念得出符号 | 读屏 | ✅ |
| L3A-9 | chess-garden | 64 格的 `aria-label` 只报「格名 + 棋子」，读屏听不出光标在哪、哪一颗被选中了 | 读屏 | ✅ |
| L3A-10 | dot-maze | 无尽 / 抢豆 / 追逃三种玩法的一行说明里没写暂停键，只有菜单底下那一行写了 | 文案 | ⭕ 随 L3A-2 一起 |
| L3A-11 | pool-stars | 自由球摆放只有指针一条通路，键盘玩家只能吃「✅ 母球放好了」的默认落点 | 触屏通路 | ❌ 只列 |
| L3A-12 | fruit-stack | `allSettled` / `settleWorld` 加收敛兜底（`R3-PA-1`） | 物理判定 | ❌ 明令不动 |
| L3A-13 | chess-garden | 残局连胜过了第 10 局继续加档、题面池从 41 做到上百个 | 难度 / 内容重标定 | ❌ 只列 |
| L3A-14 | chess-garden | `.cg-sq` 在 360px 上 41.4px（`R2-PA-3`） | 跨包 | ❌ 记录在案的例外，不拆 |

打算落地 9 条（`L3A-1`…`L3A-10`，其中 `L3A-10` 并进 `L3A-2` 的 commit），
只列不落 4 条，理由见第三节。
