# r42 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-186**、**N-187**、**N-188**。下一空号 **N-189**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r41 学习 PR 未合主干时，本轮仍从 N-186 起编；若主干或先合 PR 已占用 N-186+ / 已修 `.mmc-open` / `.mmc-toggle`，本号销或顺延，并在笔记勘误。

---

## 红线（抄自 r22–r41 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r42-learn-notes.md` §0。尤其 **不要**再开 `.dvs-pick`（N-94）、`.sks-mode`（N-149）、`.sn-open`（N-180）、`.bl-btn`（N-145）、`.bvp-btn`（N-150）、`*-veil-btn`（N-152）、`.pcp-act`（N-183）、`.hh-catch`（N-184）、`.shr-toggle`（N-134）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#114** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 sticky。

---

## 本轮工单

### N-186 · B · `.mmc-open`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/memory-cards/index.ts` ≈L113；挂载 ≈L757/L869/L932/L935 |
| 现状 | 大厅/结算 `<button>`，`padding:8px 16px`，无 min-height |
| 不做 | 不改 `.pz-open`（已 44）/ `.sn-open`（N-180）/ 翻牌规则 |
| 验收 | 入口钮高 ≥44；点了仍进无尽或对战 |

### N-187 · B · `.mmc-toggle`

| 项 | 内容 |
| --- | --- |
| 文件 | 同上 ≈L115；挂载 ≈L724/L822/L826/L939 |
| 现状 | 回选关 / 主题 / 辅助，`padding:8px 14px`，无 min-height |
| 不做 | 不改 `.shr-toggle`（N-134）/ `.snk-toggle`（已 44） |
| 验收 | 三颗钮 ≥44；点了仍返回/换主题/切辅助 |

### N-188 · A · `*-toggle` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-toggle` 可点块必须 `min-height`≥44 或 TOUCH / `TOGGLE_MIN_H` 插值 |
| 现状红 | `.mmc-toggle` |
| 豁免 | `.shr-toggle` → N-134 |
| 不做 | 不替代 N-134/187 |

---

## 建议顺序

1. A 先合 **N-188**（或与 B 并行）。
2. B 修 **N-186**、**N-187**（同一 `memory-cards/index.ts`，可一 PR）。
3. 回归：主干 N-94/101、N-108 拼图眼与 hint/back、N-87 冲刺菜单、N-180 `.sn-open`、N-183/184、N-177/178、N-174/175、N-99/97/100。bowling `.bl-btn` 仍走 N-145。若 N-149 后合且已含 `.mmc-open`，对账勿双修。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-186/187 的点击矩形，不能只看 CSS 行。`.rbt-vs-btn` / `.rte-btn` 估高 ≈43.2，记在笔记 §1.3 / 下一空号 **N-189**，本轮不派。
