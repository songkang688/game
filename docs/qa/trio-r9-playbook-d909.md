# 三人组第 9 轮（d909 组）· 测试修复员 A/B 任务清单（playbook，供第 10 轮执行）

> 依据：`trio-r9-learn-notes-d909.md`（入场基线 `game-1.3 = 6a9f42d0`，交卷实况 `bf4d84ac`，每条都带实测数字与 915×412 截图）。
> **文件名带 `-d909`**：本仓多组并行，`trio-r9-playbook.md` 已被另一组占用，不覆盖别人的报告。
> **本清单只列「在 `bf4d84ac` 上仍复现」的 🔧**。r8 playbook 的 14 项已结案 11 项（S-2/S-3/S-4 含 quiz99 扩容 / L-1 前半 / C-1 / N-33 / N-1 / N-34 / N-35 / N-36 / N-37 / N-38 / 收藏册两条热区），**不要重做**，对账明细见 learn-notes 第二节。
> 新伤编号从 **N-59** 起（N-1…N-58 已被各组占满，实扫全部 `docs/qa/*.md` 取到的最大号是 N-58）。
> 分工：**A = 壳层 + 闯关学习**，**B = 休闲对战动手**。
> 配方：A/B/C 见 r4 learn-notes 第六节，D/E/F 见 r5 第四节，G/H 见 r6 第四节，I/J 见 r8 第四节；**本轮新增 L（旋转不复算）、M（验收要挑常态）、N（多组共用 worktree 的取证纪律）见 r9-d909 learn-notes 第四节**。

## 通用纪律（与 r4–r8 一字不差的红线）

- **不改存档 key**：`yiduo-yixing.l99.<id>` / `yiduo-yixing.l99skip.<id>` / 平台钱包 key、`meta.id` 一个字符不动。
- **不动题库/判定/关卡生成**：`levels.ts` 数据、seed 链路、win/lose 判定零触碰。
- **测试只增不减**：进场先 `npm test` 记水位；每条修复配 1 份小测试；交卷水位 ≥ 进场。
- **kit 冻结**：`src/art/kit/` 已有文件只 import 不改（`stickers.ts` 扩容例外）。
- **宽屏零回归**：钳高/换排改动在 412×915 与 1280×800 复测「修前修后一致」。
- 收尾：fetch → rebase → `npm test` / `npm run build` 全绿 → 普通 push，**禁 force**。

### 本轮追加的三条纪律（都是 r9 踩出来的，别再踩一次）

1. **先钉版本再取证**（模式 N）。本仓 4 组三人组共用一个 `/workspace`，你量到一半别人会 commit + `npm run build` 把 `dist/` 换掉。做法：
   ```
   git worktree add --detach /tmp/wt-<tag> <commit>
   ln -s /workspace/node_modules /tmp/wt-<tag>/node_modules
   cd /tmp/wt-<tag> && npm run build && npx vite preview --port <各自端口>
   ```
   每个数字标清「在哪个 commit 上量的」。**别动共用 worktree 的 HEAD 和索引**——里面有别人的未提交改动。
2. **动手前先复现**。「playbook 说没修」≠「我拉的 game-1.3 没修」≠「某个组的分支上没修」。复现不出来就销账别修；复现得出来先 `git log -S "<关键字符串>"` 查有没有人正在改同一处，撞车取先合版。
3. **`npm test` 的已知假红**（4 核机默认并发）：`snake-snack/qaC1`、`bomb-buddies/ai`、`snake-royale/ai`(×2)、`sudoku-petal/solver`、`window1-smoke-seeds` 全是 `Test timed out in 5000ms`；`gomoku/tiers` 是 `expected 7 to be >= 9`。**同样 6 个文件 `--maxWorkers=1` 单跑 147 用例全绿**。别为这批红改代码。

## 测试步骤备忘

- 起服：`npm run build && npx vite preview --port <port>`；无头验证 puppeteer-core + `/usr/local/bin/google-chrome`（puppeteer-core 装 `/tmp`，**不进仓库 package.json**）。
- 视口五档：360×640 / 390×844 / 412×915 / **915×412（重灾主档）** / 1024×768；宽屏对照 1280×800。
- **管理员直达**：家长面板 → 管理员权限 → 密码 `kangkang`。**深关优先走 localStorage 进度路**（`yiduo-yixing.l99.<id>` = `Array(N-1).fill(1)`），免 root 抬头污染；进关钮**认 `.l99-continue` 类名，别认文案**（有进度时写的是「继续 第N关 ▶」）。
- 量裁切口径同 r4（`.game-stage` clientHeight）；「crop=0 但控件够不着」先扫自滚容器（r5）；模式菜单款逐模式量（r6）；显隐开关打开再量（r7）；弹窗量内滚 + 末钮是否出框（r8 配方 I）；**本轮追加三条**：
  1. **旋转口径（配方 L）**：竖屏进关 → **不重载**直接 `setViewport` 成横屏 → 与「同视口原生直进」逐项对照，判据 **「旋后数 == 原生直进数」**。反向（横→竖）也要跑一遍，本轮 find-diff 的左右越界只在反向出现。
  2. **常态口径（配方 M）**：涉及首页/收藏/成绩单/深章的验收一律跑两遍——`全新档案` 与 `用过一阵`（脚本造：进一款 → 点 `.l99-continue` → 回首页）。**造「全新档案」必须换一个 browser 实例**，同 browser 的多个 page 共享同源 localStorage。
  3. **够不着 = 出视口 ∪ 被容器剪**：控件 `top < 视口高` 但被某个可滚祖先的可视框切掉一样点不着。向上找 `overflow-y:auto|scroll` 且真能滚的祖先，比 `el.top >= 祖先.bottom`。
- **配方 J 剪枝**：抽深关关型时先与同款第 1 关做差，**差 <30px 归款级老账、不单独立项**（本轮据此判掉了 alien-seek 推理关与 shape-kingdom 作图关两个假清单）。

---

## 壳层（给 A）

### N-59 首页首屏验收漏掉「最近玩过」行 🔧（本轮最重，走配方 M）

- **现象**：同一份 CSS，只差有没有玩过一款游戏。判据仍是 S-1 的「首卡 top ≤ 视口高 − 100」。

  | 视口 | 全新档案 | 玩过一款后 | 判据 | 结论 |
  | --- | --- | --- | --- | --- |
  | 915×412 | 304 ✅ | **407** | ≤312 | ✗ 差 **95px** |
  | 360×640 | 493 ✅ | **603** | ≤540 | ✗ 差 **63px** |
  | 1024×768 | 557 ✅ | **691** | ≤668 | ✗ 差 **23px** |
  | 390×844 | 496 ✅ | 612 ✅ | ≤744 | 过 |
  | 412×915 | 498 ✅ | 615 ✅ | ≤815 | 过 |
  | 1280×800 | 557 ✅ | 691 ✅ | ≤700 | 过 |

  「最近玩过」整块吃 **+103…+134px**。全新档案是罕见状态，**玩过一款之后才是常态**——等于 S-1 只对孩子拥有 App 的第一分钟有效。
- **机理**：A 第 2 轮加的两段媒体查询（`@media (max-width:480px)` / `@media (max-height:500px)`，`styles.css` +222 行）收的是 header / hero / 筛选条 / 搜索框，`.recent-section` 那一块只在 `max-height:500px` 档收了一点，`max-width:480px` 档没收；1024×768 两个档都不命中。
- **改哪**：`src/styles.css`（必要时 `src/ui/home.ts` 只调顺序，不改数据）。三个方向按「hero 是论点」取舍：矮横屏/矮竖屏把最近玩过收成**一行横滑条**（卡片缩到 ~64px 高）；或把「最近玩过」与 hero 气泡二选一显示（有最近玩过时 hero 收起——**这个更对**，因为 frontend-design 说首屏主角应当是「继续玩」而不是欢迎语）；1024×768 档补一条媒体查询。**不要动筛选逻辑、`recordRecent`、卡片数据。**
- **验收**：六档 × **两种档案状态**（全新 / 玩过一款）全部满足 `首卡 top ≤ 视口高 − 100`；三个筛选行热区仍 ≥44px；1280×800 零回归；补一条「有最近玩过时首卡仍在首屏」的断言（脚本造存档，只写公开 key）。
- **顺带**：把 S-1 原有的断言也升级成两遍跑，否则下一轮还会漏。

### N-61 旋转后 find-diff 不复排 🔧（走配方 L）

- **现象**（915×412 ↔ 390×844，不重载直接转）：
  - **竖 → 横**：停在上下两图（面板 y=187 / y=419），文案还是「找出**下**图不一样的」，`.fdf-viewport` 自滚 **311**（比 r7 修之前的 203 还差），**下图 9 个格子全在折叠线下**（top 463 / 511）。同档**原生直进**：并排（x=220 / x=405，同一 y=206）、自滚 **0**、线下 **0**。
  - **横 → 竖**：反过来停在并排，390 宽屏上**左图 x=−42（左边切 42px）、右图右缘 432（右边切 42px）**——两张要比对的图各缺一角。
- **机理**：`src/games/find-diff/index.ts:817` 有 `win?.addEventListener("resize", fitViewport)`，但 `fitViewport` 只钳 viewport 高度；决定行/列的 `panelsSideBySide`（`runtime.ts`，A 第 2 轮新增）在建面板时算一次就定死，文案「上图/下图 ↔ 左图/右图」也跟着定死。
- **改哪**：`src/games/find-diff/index.ts` + `runtime.ts`。把 `panelsSideBySide` 的判断与随之而来的行/列 class、`panelCellPxRow` 选择、显示文案一起搬进 `fitViewport` 的回调（或抽一个 `relayout()` 同时被首建与 resize 调用）。**`regrowCellPx` 只放大不缩小的约定别破坏**；判定、题库、`data-*` 零触碰。
- **验收**：**四条路径**都要跑——原生 915×412 / 原生 390×844 / 竖→横 / 横→竖；判据「旋后数 == 原生直进数」（自滚 0、线下 0、面板 x ≥ 0 且右缘 ≤ 视口宽）；文案与布局一致（并排时说「右图」、上下时说「下图」）；1024×768 / 1280×800 零回归；补一条 relayout 纯函数测试 + 一条「转屏后 side-by-side 判断跟着变」的断言。

### N-62 首页搜索框键盘焦点不可见 🔧（一行级，先做拉绿）

- **现象**：915×412 首页 Tab×24 逐个量 `getComputedStyle().outline`——**只有搜索输入框 `outline: NONE`**，其余 23 个可聚焦件全有 3px 焦点环。全库扫过，这是唯一一处没补偿的逃逸。
- **机理**：`src/ui/home.ts:103` `.home-search-input{...;outline:0;...}` 与全局 `src/styles.css:73` `:focus-visible{outline:3px solid var(--focus-ring)}` 同特异度、靠后生效；包裹层 `.home-search` 也没有 `:focus-within` 兜底。对照组：`.gate-input` 同样 `outline:none`，但配了 `:focus{border-color:var(--pink)}`，所以不算洞。
- **改哪**：`src/ui/home.ts`。二选一：撤掉 `.home-search-input` 的 `outline:0` 让全局规则生效；或保留并补 `.home-search:focus-within{outline:3px solid var(--focus-ring);outline-offset:3px}`（**推荐后者**，焦点环框住整个搜索胶囊比框住裸 input 好看，也和 `.tab` / `.icon-btn` 的观感一致）。搜索逻辑、拼音首字母匹配、`✖` 清除钮零触碰。
- **验收**：Tab 到搜索框有可见焦点环、鼠标点击不出环（`:focus-visible` 语义）；补一条**守门测试**（照 `src/games/modebarHidden.guard.test.ts` 的写法）：凡在组件 CSS 里用 `outline:0|none` 覆盖公共焦点态的，同一块 CSS 必须就近有 `:focus-visible` 或 `:focus-within` 或 `:focus` 的补偿规则——未修前当场红，正好当验收。

### L-1（余下）color-fun / music-stars 横屏答题控件折叠线下 🔧

- A 第 2 轮按**内部目录**把这两款划成「动手款、范围外」而跳过。**范围划法要复议**：两款挂在首页「📚 学习」页签下，孩子是从学习入口进去的，按孩子看到的分类它们在 A 的盘子里。
- 现象与改法照 r4/r7 playbook 原文（915×412 下 7 控 / 4 控折叠线下，走配方 B）；`quiz99.ts` 的矮屏紧凑档已经落地，先看能不能直接复用同一档。

### L-2 clock-house 前 99 关题面钟两代同堂 🔧 / L-3 find-diff 盘面贴纸补第 4–10 章 🔧

- 本轮源码复证均零改动（`clock-house/levels.ts` 里 `arrowHandD` / `hubSVG` 零命中；`boardArt.ts` 头注原样）。r4 原文继续有效：L-2 换 `arrowHandD`/`hubSVG`/11px 刻度，`data-h`/`data-q` 与角度公式零触碰；L-3 净活 = `stickers.ts` 补图 + `boardArt.ts` 映射加行，门控逻辑不改。

---

## 闯关学习（给 A）

### 模式 L 全库自查（A 负责壳层与闯关侧）🔧

- 把「**建完 DOM 后读 `getBoundingClientRect`/`clientHeight` 再写回尺寸或改结构**」的地方列出来，逐个补旋转触发点。已知起步名单：`level99.ts` 的 `fitPanesToStage`（N-60，B 侧两款调用）、`find-diff` 的 `panelsSideBySide`（N-61）、`shape-kingdom/draw.ts` 的 `fitIntoStage`（本轮实测旋转后无新伤，但同属这一类，一并挂监听更省心）。
- 判据统一成「**旋后数 == 原生直进数**」；纯函数本身不用改，**只补触发点**（`ResizeObserver` 观察 `.game-stage`，或 `window.resize` 防抖 150ms 重跑）。测试只需加「换个尺寸再调一次、结果跟着变」一条。

---

## 休闲对战动手（给 B）

> 先与最新 `game-1.3` 对账：多组并行，已合入的按已合入版为准，**撞车取先合版**。

### N-25 fight-king 格斗塔在主干上收口 🔧（先做，是对账不是重修）

- **现状分歧**：d909 组测试员 B 的 `13a4e38e` 已把格斗塔关净——915×412 关内 crop **6**、线下 **0**；而当前主干 `bf4d84ac` 实测 **crop 371、轻击/重击/必杀/防御四个必点钮仍在 top 663/713 线下**。合进主干的是另一组的另一版修法，没关净。
- **动作**：**先对账，别写第三套修法**。比对两版 diff，取先合版为底，把没关净的那部分补齐（或把 d909 版的双栏搬过来）。改完在 915×412 复测 crop 与线下数，两个数字都要写进报告。
- **验收**：915×412 格斗塔第 1 关四个必点钮不滚可点；fight-king 五模式（双人对战 / 人机 / 格斗塔 / 无尽连胜 / 训练场）× 915×412 矩阵回归；390×844 与 1280×800 零回归。

### N-60 旋转后 `fitPanesToStage` 不复算 🔧（走配方 L）

- **现象**（竖屏进关再转横，对照同视口原生直进）：

  | 款 | 旋后 crop | 旋后画布 | 旋后出屏 | 原生横 crop | 原生画布 | 差 |
  | --- | --- | --- | --- | --- | --- | --- |
  | orb-arena | **757** | 648×**873** | **646** | 127 | 648×243 | **+630** |
  | snake-royale | **706** | 648×**821** | **594** | 128 | 648×243 | **+578** |

- **机理**：`fitPanesToStage(wrap, canvases, paneW, paneH)` 在 `orb-arena/index.ts:359` 与 `snake-royale/index.ts:426` 各调**一次**（进关时），按**竖屏**壳卡缺口把画布逻辑高补满。转横屏后画布显示宽 311→648（2.08×），等比锁死的高度跟着 419→873——**竖屏那次的缺口修正量被整个放大了一遍**。A 第 2 轮把竖屏白底从 250px 收到 6px 是真的好，代价是旋转路径的账放大约 3 倍。
- **改哪**：`src/games/level99.ts`（挂触发点）+ 两款调用点。给 `.game-stage` 挂 `ResizeObserver`，或 `window.resize` 防抖 150ms 重跑 `fitPanesToStage`；**`fitPanesToStage` / `fitPaneH` 两个纯函数一个字不改**，钳位区间 `[min(原高,240), 960]` 保持。游戏相机 zoom 只依赖画布宽、判定与地图尺寸零触碰（A 第 2 轮已核过，别破坏）。
- **验收**：两款 × 四条路径（原生竖 / 原生横 / 竖→横 / 横→竖），判据「旋后数 == 原生直进数」；竖屏白底仍 ≤6px（别把 A 的成果改回去）；1024×768 / 1280×800 零回归；补一条「同一组入参换个壳卡高度，返回值跟着变」的纯函数断言 + 一条「resize 后重调」的调用点断言。

### C-6 alien-seek 关内控件折叠线下 🔧（本轮实测最重的未动项，走配方 G）

- **现象**（`bf4d84ac`，第 1 关与第 122 关推理关表现一致）：
  - 915×412：crop **598**，`＋ − ⤢ 🔭望远镜 ▲ ◀ ✓ ▶` 共 **10 个控件线下**，画布 186..621 出屏 209。
  - **1024×768 平板也中**（r4 只记了矮横屏）：crop **282**，`▲ ◀ ✓ ▶ ▼ ⏸` 六个出视口，另有 `＋ − ⤢ 🔭` 四个「在视口里、被壳卡剪掉」。
  - 390×844 crop 79 / 412×915 crop 20，均线下 0，属轻伤别动。
- **机理**：找物画布 + 缩放工具行 + 方向键盘 + 望远镜/确认全部卷进同一条纵向流，无横屏矮屏钳制——学习/找物款版的模式 G。
- **改哪**：`src/games/alien-seek/index.ts`（`as-`/`als-` 前缀一带）。矮横屏双栏：画布左、缩放行 + D-pad + ✓ + 🔭 收右侧一列；或工具行 sticky 置顶、D-pad sticky 置底，画布按可视余量钳。**1024×768 单独补一档**（这一档是宽屏但矮，双栏也吃得下）。找物判定、望远镜次数、推理关线索排除逻辑零触碰。
- **验收**：915×412 与 1024×768 下 10 / 6 个控件不滚可点、画布主体 ≥60% 可见；390×844 / 412×915 / 1280×800 零回归；**两个关型各量一遍**（找物第 1 关 + 推理第 122 关，用 localStorage 进度路直达）；补关内壳断言。

### 测试稳定性（非 UI 账，但每轮都在浪费时间）🔧

- 6 个吃 wall-clock 时间预算的 AI 对局测试在 4 核机默认并发下必假红：`snake-snack/qaC1`、`bomb-buddies/ai`、`snake-royale/ai`(×2)、`sudoku-petal/solver`、`window1-smoke-seeds` 是 5000ms 超时，`gomoku/tiers` 是 `expected 7 to be >= 9`（地狱档被抢核就打不赢）。
- **改哪**：把「按毫秒给 AI 思考预算」改成「按步数/节点数预算」，或给这几个 describe 标注串行执行。**AI 强度参数、seed、判定零触碰**——只改预算口径与并发标注。
- **验收**：默认并发下连跑 3 次全绿；`--maxWorkers=1` 仍全绿；用例数只增不减。

### r4–r8 未动项重申（原编号有效，本清单不重抄）

- **r7/r6**：N-26 duo-vs-star 闯关七键 / N-27 dot-maze 四模式键排 / N-30 adventure-king 无尽古堡 13 控件 / N-31 fight-king 训练场（与 N-25 同批看，d909 组已连带处理，**先对账**）/ N-32 brave-path 无尽地牢三钮。
- **r5**：N-2/N-3/N-4 回合必点组、N-5…N-9 / N-20 盘面完整可见组、N-10 棋类三款横屏、N-11…N-15 + N-19 stagePlayRoom 家当组、N-16 走廊引擎三态、N-17/N-18、N-21/N-22。
- **r4**：C-2 brick-break / C-3 snake-snack / C-4 snake-royale / C-5 mole-pop / C-7 match-stars / C-8 名单 / C-9。
- 全部照各自 playbook 原文执行；**护栏（干净名单）见 r7/r8 与本轮 learn-notes 第六节，勿扩大化**。

---

## 完成定义（两人共用）

1. 全部 🔧 关账或书面降级（降级写数学/物理理由，照 BL-W6-03 格式）。
2. `npm test` / `npm run build` 全绿，用例水位只增不减；每条修复有配套测试或取反断言。**已知的 6 个并发假红先用 `--maxWorkers=1` 复验再判**。
3. 本轮新伤（N-59 首页常态 / N-60 旋转不复算 / N-61 旋转不复排 / N-62 焦点环）逐条对账 N 编号，**修前修后两侧数字都要留**；N-60/N-61 按「四条路径」验收（原生竖 / 原生横 / 竖→横 / 横→竖，判据「旋后数 == 原生直进数」），N-59 按「两种档案状态 × 六档视口」验收。
4. 报告按既有命名续档（多组并行，**文件名带自己的组后缀，别覆盖别人的报告**；新伤编号前先 `rg -o "N-[0-9]+" docs/qa` 取最大号再续编）。与并行批次撞车的按「先合版」纪律记录明细。
