# 1.3 窗口 4 · 第 3 轮终验 · 测试员报告

> 范围（硬限制）：只验本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。终验总表是 **9 行 × 6 专项**，不是全库 39 款。
> 规格模板：`docs/plan-1.3-step29-A-tester.md`（全局 39 款口径按派发收窄为本窗 9 款；报告路径按派发写本文件）。
> 权限：只改 `*.test.ts` 与本报告；不改绘制代码、parentAuth、parentGate；不用 three.js。
> 本轮定位：**终验**——对账清零 + 本窗终扫 + 给出本窗「放行 / 带条件放行 / 不放行」结论（不是全库 1.3 放行）。

## 〇、终验计划（动手前提交）

1. **基线复核**：`npm ci` → `npm test` → `npm run build`。对照统筹复跑水位（SHA `a0c3072` 附近，710 文件 / 15280 例全绿，build PWA 188 项 / 5246.26 KiB）。用例只增不减；前两轮扫描用例（`window4-visual-scan.test.ts` 31 例、`window4-visual-scan-r2.test.ts` 9 例）必须还在跑、全绿，被删或 skip 按阻断。
2. **对账清零**：第 2 轮 tester 新问题（W4R2-01～07）与 r1 遗留（W4R1-01～07）× r2 fixer 16 笔修复逐条读码 + 跑断言验证。阻断/严重必须为 0 或有 C 档书面降级。核对 fixer 宣称：16 笔修复、W4R1-02 严重已清、W4R2 一般/建议全清、B 档 TOP1-9 落地、TOP10（sling-birds 结算仪式）书面顺延、W4R1-06（DOM 小字号）维持书面遗留。
3. **本窗终扫**：9 款 × 六大专项（火柴人草稿圆 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px / 商标），每款每项给「达标 / 不达标」二值结论。双人可区分只看本窗双人款（duo-rush / duo-arena / duo-vs-star / monster-crisis）；2.5D 点名本窗 duo-rush。
4. **回归防线**：热区钉死断言、reduced 功能提示断言、destroy 泄漏口径、`vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 三项 200），本窗随机进几款各玩一会儿无视觉崩坏。
5. **终验结论**：放行 / 带条件放行 / 不放行（三选一，附依据）。
6. **收尾**：fetch → rebase（保留别人文件）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.3-window4`。禁止 force、不改 main、不推 game-1.2 / game-1.2-kk、不用 gh 开 PR；别人的报告一字不改。

（以下各节由终验执行后填写。）
