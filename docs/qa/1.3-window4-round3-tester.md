# 1.3 窗口 4 · 第 3 轮终验 · 测试员报告

> 范围（硬限制）：只验本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。终验总表是 **9 行 × 6 专项**，不是全库 39 款。
> 规格模板：`docs/plan-1.3-step29-A-tester.md`（全局 39 款口径按派发收窄为本窗 9 款；报告路径按派发写本文件）。
> 权限：只改 `*.test.ts` 与本报告；不改绘制代码、parentAuth、parentGate；不用 three.js。
> 本轮定位：**终验**——对账清零 + 本窗终扫 + 给出本窗「放行 / 带条件放行 / 不放行」结论（不是全库 1.3 放行）。

## 〇、终验计划（动手前提交）

1. **基线复核**：`npm ci` → `npm test` → `npm run build`。对照统筹复跑水位（SHA `a0c3072` 附近，710 文件 / 15280 例全绿，build PWA 188 项 / 5246.26 KiB）。用例只增不减；前两轮扫描用例（`window4-visual-scan.test.ts` 31 例、`window4-visual-scan-r2.test.ts` 9 例）必须还在跑、全绿，被删或 skip 按阻断。
2. **对账清零**：第 2 轮 tester 新问题（W4R2-01～07）与 r1 遗留（W4R1-01～07）× r2 fixer 16 笔修复逐条读码 + 跑断言验证。阻断/严重必须为 0 或有 C 档书面降级。核对 fixer 宣称：16 笔修复、W4R1-02 严重已清、W4R2 一般/建议全清、B 档 TOP1-9 落地、TOP10（sling-birds 结算仪式）书面顺延、W4R1-06（DOM 小字号）维持书面遗留。
3. **本窗终扫**：9 款 × 六大专项（火柴人草稿圆 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px / 商标），每款每项给「达标 / 不达标」二值结论。双人可区分只看本窗双人款（duo-rush / duo-arena / duo-vs-star / monster-crisis）；2.5D 点名本窗 duo-rush。
4. **回归防线**：热区钉死断言、reduced 功能提示断言、destroy 泄漏口径、`vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 三项 200），本窗随机进几款各玩一会儿无视觉崩坏。
5. **终验结论**：放行 / 带条件放行 / 不放行（三选一，附依据）。
6. **收尾**：fetch → rebase（保留别人文件）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.3-window4`。禁止 force、不改 main、不推 game-1.2 / game-1.2-kk、不用 gh 开 PR；别人的报告一字不改。

## 一、全量跑数据与三轮用例数曲线

| 项 | 结果 |
| --- | --- |
| `npm ci` | 绿 |
| `npm test`（开工基线复核，SHA `22f2fc5`） | **710 文件 / 15280 例全绿**（185.6s），与统筹复跑水位（`a0c3072`）、r2 fixer 合并态完全一致 |
| `npm test`（合并态收尾，含本轮 +2 例） | **711 文件 / 15282 例全绿**（见第七节） |
| `npm run build` | 绿（tsc --noEmit + vite；PWA precache **188 项 / 5246.26 KiB**，与 r2 fixer 水位一致，零漂移） |
| `npx vite preview :4173` 冒烟 | `/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**；本轮全部动态实测跑在 preview 构建产物上 |

三轮用例数曲线（只增不减 ✅）：r1 基线 707/15198 → r1 合并态 709/15247 → r2 合并态 710/15280 → **r3 合并态 711/15282**（本轮 +1 文件 / +2 例：W4R3-01 钉住 + 触控键回归守护）。

前两轮机器化扫描用例全部在岗、全绿、零 skip：`window4-visual-scan.test.ts` 31 例 + `window4-visual-scan-r2.test.ts` 9 例 + `window4-learner-copy.test.ts` 12 例 = **52 例**（定向复跑确认）；本窗 9 款自有契约测试 **74 文件 / 2137 例**全绿。

## 二、第 2 轮问题终验对账表

方法：16 笔修复 commit 逐一读码 + 配套断言复跑 + **16px 剪影量化复测**（真实绘制函数经 vite dev 导入无头 Chrome，自然尺寸渲染 → 两两联合 bbox 降采样 16px → 剪影 XOR 占比 / 重叠区平均灰度差；阈值 32/100 双档核对，数字对阈值不敏感）+ 360/320 双档实机复测。

### r1 遗留 7 条 × r2 fixer 处理

| 编号 | r2 宣称 | 终验结论 | 独立取证 |
| --- | --- | --- | --- |
| W4R1-01 garden-guard hud12 段串 emoji | 已修（#1 `f288c83`） | **已修复** | `hud12.ts` 冻结契约零改动（git log 终态仍停在 1.2 提交 `b16712d`）；`drawHudRichText` 按 token 槽位接 `drawPetalIcon`/`drawHeartIcon`（index.ts 530/571/572，五处调用点齐）；360px 无尽模式实机截图：HUD「花瓣 16 / 💗×5」已是手绘图标；art.test +4 绿 |
| W4R1-02 monster-crisis 双英雄 16px（**严重**） | 已修（#2 `3a4872d`） | **已修复（严重级清零）** | 读码：P1 扁贝雷+花徽 / P2 尖顶画家帽+帽尖大四芒星（art.ts 240–273），围裙下摆 P1 圆摆 / P2 锯齿摆（202）；量化：剪影 XOR **0% → 6.7%**、重叠灰度差 8.5 → **12.7**；16px 灰度目检图：P2 帽形高出一截进剪影，双人不再只靠颜色。`HERO_R` 与识别色未动 |
| W4R1-03 duo-rush 双主角 16px | 已修（#5 `d22912c`） | **已修复** | 星呆毛 0.32→0.5、花苞轨道加大（art.ts 476–504 注释+代码核对）；量化 XOR 5.9%/灰度 5.3（本 harness 口径），呆毛通道 16px 站进像素网格；四重兜底通道全在（实渲 ~49px、默认照片头像+SEAT_RING——360px 玩法截图复核、双半屏主题色） |
| W4R1-04 duo-vs-star duoduo/xingxing 16px | 已修（#6 `9dbd0f8`） | **已修复** | 花饰抬出轮廓+星呆毛 0.34→0.5（art.ts 308–328）；量化 XOR 7.3% → **17.2%**、灰度 17.5，脱离边界区 |
| W4R1-05 gold-hook 金块无渐变 | 已修（#4 `51715cd`） | **已修复** | drawOre 体表 2 停线性渐变（art.ts 535，lit→fill 左上受光）；r1 钉住断言已按修后取反收紧、绿；扫光/高光/描边/落影四线索保留 |
| W4R1-06 次要文案字号 9–13px（约 18 处） | 维持书面遗留 | **合理降级（维持）** | 1.2 存量 DOM 样式、360/320 双档实测无溢出无遮挡（本轮复测 9 款 ×2 档溢出全 0）；不属六大专项阻断口径，两轮 tester 均未升级——降级理由仍成立 |
| W4R1-07 duo-vs-star 金星无独立高光 | 已修（#6） | **已修复** | drawGoldStar 第三层白高光（art.ts 112–116），与 candy-swing / duo-arena / garden-guard 同族对齐 |

### r2 新问题 7 条 × r2 fixer 处理

| 编号 | r2 宣称 | 终验结论 | 独立取证 |
| --- | --- | --- | --- |
| W4R2-01 duo-arena 事件飘字 8 处字面量 emoji | 已修（#12 `9d9b9e9`） | **已修复** | pushFloat 行内 emoji 扫描 8 → **0**（r2 断言取反后绿）；r1 三件套回归守护绿 |
| W4R2-02 duo-rush 画布 HUD emoji 串 | 已修（#3 `5a150d4`） | **已修复** | `hudText` 不复存在，`hudTokens`+`drawHudToken` 接手绘资产（index.ts 1142/1168），`.emoji` 变量链断开（断言绿）；360px 玩法截图：HUD 小脸/金币/爱心全为手绘 token |
| W4R2-03 duo-vs-star xingxing/jiujiu 16px | 已修（#13 `8fa4f85`） | **已修复** | jiujiu 弧呆毛 0.22→0.34+加粗（art.ts 388–392）；量化 XOR **0% → 4.7%**（16px 目检：实心星块 vs 细弧圈可辨）；同为暖黄系灰度差 7.1 属配色本性，团队赛线型环第四通道兜底在岗 |
| W4R2-04 garden-guard 原型 BOSS 三对 XOR 0% | 已修（#11 `b8ca36c`） | **已修复** | `bossTrimOf` 四配饰查表（art.ts 520，plate/feather/cloud/ring 分支全）；量化六对两两：**最低 10.9%**（bossSwift/bossSplit），原三条 0% 对全部拉开，bossFly 对 56.5–60.8% |
| W4R2-05 duo-vs-star 380px 档触控键 38px + 返回键 32px 高 | 已修（#14 `0c2c6da`） | **大部修复，残余 1 条 → 立 W4R3-01** | 触控键实机复测：360px 与 320px 玩法态七键全部 **40×40**、横向溢出双档全 0（fixer 宽度账实测成立）；**但 `.dvs-back` 返回/暂停键实测 105×32（高 32px<40），r2 原文点名的「返回键加 padding-block」未做**（CSS 196 行 padding:7px 仍是 1.2 存量） |
| W4R2-06 sprout-defense walker/flyer 16px | 已修（#15 `a60ffc4`） | **已修复** | 翅膀锚点 1.15r 出轮廓+翅长 0.8r（art.ts 890–896）；量化 XOR **0% → 12.7%**，calm 静态帧张角 0.8 断言绿 |
| W4R2-07 gold-hook 金/石 16px 明度 | 已修（#16 `1f1b17b`） | **已修复** | 石头系压暗（pebble/boulder，art.ts 465–466）；量化重叠灰度差 nugget/pebble 7.0 → **51.1**、goldBig/boulder **62.4**；玩法截图金石一眼可分；明度比断言绿 |

### fixer 其余宣称核对

- **16 笔修复全部在分支、全部落地**：`f288c83`/`3a4872d`/`5a150d4`/`51715cd`/`d22912c`/`9dbd0f8`/`279ea40`/`96e69d8`/`e3fbb16`/`0146500`/`b8ca36c`/`9d9b9e9`/`8fa4f85`/`0c2c6da`/`a60ffc4`/`1f1b17b` 逐笔读码核实，`d38deb5`/`37df120` 测试配套在。B 档 TOP1–9 全落地（gold-hook 天空 `drawSkyDecor`、monster-crisis `drawSky` 主题物+云漂移、duo-vs-star `drawMidgroundBand` 十场地——玩法截图均实机可见；sprout-defense 星弹金渐变+花粉弹独立分支 index.ts 1986/1994）。
- **TOP10（sling-birds 结算星级仪式）书面顺延**：核实确未实现；两轮 tester 均未立项、不属六大专项、低优先/中高成本——**顺延理由成立，不影响放行**。
- **修出回归 0**：r1 31 例 + r2 9 例 + 9 款契约 2137 例全绿；判定热区（RunnerPose/BirdPose/HERO_R/钩角数学）、hud12 宽度测量、squeeze 契约全部零改动（`1f1b17b` 之后分支上只有 4 笔 docs 提交，无任何代码漂移）。

**对账统计：r1+r2 共 14 条 —— 已修复 12 / 合理降级 1（W4R1-06 维持书面遗留）/ 修复不彻底 1（W4R2-05 触控键达标、返回键残余 → W4R3-01）。阻断 0、严重 0 ✅。**

## 三、9 款 × 六大专项终验总表

方法：静态断言 54 例全绿打底 + 本轮独立动态复测（无头 Chrome 驱动 preview 构建产物：360×640 与 320×568 双档 DPR2 触屏、入口+玩法双态溢出度量、玩法截图目检、CPU 4× 节流 3s rAF 计帧）。

| 款 | ①火柴人/emoji | ②收集物体积 | ③双人可分(16px) | ④2.5D 纵深 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| duo-rush | 达标 | 达标 | **达标**（修后 5.9%+四重兜底；截图复核头像模式默认开） | 达标（点名款：车道收敛/雾/三层视差截图复核） | 达标（双档溢出 0） | 达标 |
| duo-arena | 达标（飘字清零后 canvas 0 emoji） | 达标（drawKitCoin 3 停标准件） | 达标（42%/84.8，r1 口径最优） | 达标 | 达标 | 达标 |
| duo-vs-star | 达标 | 达标（金星三层高光补齐） | **达标**（最难对拉开至 4.7%>0+线型环兜底） | 达标（新中景剪影带实机可见） | **不达标（仅触区子项）**：返回/暂停键 32px 高（W4R3-01）；模板三项（滚动/重叠/截断）与触控键 40×40 双档均达标 | 达标 |
| sling-birds | 达标（横幅角标守护绿） | 达标 | —（单人；五鸟体型通道在岗） | 达标 | 达标 | 达标 |
| candy-swing | 达标 | 达标 | —（单人） | 达标 | 达标 | 达标 |
| gold-hook | 达标 | **达标**（金块渐变补齐，54 例断言收紧后绿） | 达标（双矿工 30.6%；金石明度差 51+） | 达标（矿下三层视差+新天空带） | 达标 | 达标 |
| garden-guard | **达标**（hud12 绘制侧手绘化，实机截图复核） | 达标 | —（单人；BOSS 家族六对全部 ≥10.9%） | 达标 | 达标 | 达标 |
| sprout-defense | 达标 | 达标 | —（单人；walker/flyer 12.7%） | 达标 | 达标 | 达标 |
| monster-crisis | 达标 | 达标 | **达标**（严重级修复后 6.7%/12.7 双通道，16px 灰度目检可辨） | 达标（新天空主题物+云漂移） | 达标 | 达标 |

**适用格 50（③ 有 4 款单人不适用），达标 49 / 不达标 1（duo-vs-star ⑤ 触区子项）= 98%。阻断级 0、严重级 0。**
商标专项：51 词黑名单断言 9 款全绿 + 本轮玩法态截图目检（双画家/五鸟/12 脸谱/双矿工）零相似——达标 9/9。

## 四、终验新发现问题

#### W4R3-01　duo-vs-star：玩法态顶栏返回/暂停键 32px 高（W4R2-05 残余）　**【一般】**
- **位置**：`src/games/duo-vs-star/index.ts:196` `.dvs-back{padding:7px 13px;font-size:13.5px;…}`（无 min-height）。
- **现状**：360×640 DPR2 玩法态实测「◀ 返回」105×32、「⏸ 暂停」同款同高——低于 40px 触区底线 8px。r2 fixer #14 修复触控键时未覆盖 r2 原文点名的返回键（其修复表只写了 `.dvs-pad`）。属次要导航键（非玩法操作键）、1.2 存量样式，故记一般不升级。
- **期望**：`.dvs-back` 补 `min-height:40px`（或 padding-block 提到 ≥11px），一行 CSS；或由统筹书面并入 W4R1-06 存量 DOM 遗留清单。
- **量化**：已钉 `window4-visual-scan-r3.test.ts`（现状 padding:7px + 无 min-height；修复后取反），另附 `.dvs-pad` 40px 底线回归守护 1 例。
- **建议责任文件**：`duo-vs-star/index.ts`（CSS 段），交本轮 C 档。

**终验新问题统计：阻断 0 / 严重 0 / 一般 1（W4R3-01）/ 建议 0。**

## 五、回归防线（终验加严）

- **钉住断言**：前两轮 52 例扫描 + 本轮 2 例全绿零 skip（被删/被跳按阻断的前提成立）；热区钉死（判定字段不动契约）、reduced 功能提示（duo-vs-star 平台裂纹无 motion 门控、sprout-defense 预警角标不受 calm 门控等）断言全部在全量里照跑全绿。
- **CPU 4× 节流帧率（真实玩法态，超采 7 款 > 规格要求 6 款）**：duo-rush 60 / duo-arena 60 / duo-vs-star 60 / gold-hook 60（无尽矿井局内）/ garden-guard **57**（无尽战场布阵+波次预告）/ sprout-defense 60 / monster-crisis 60 —— 全部 ≥30fps ✅。r2 fixer 新增的天空带/中景带/BOSS 配饰装饰未拖帧。
- **`vite preview` 冒烟**：三项全 200；随机 6 款进玩法态各观察 ≥30s 截图目检（duo-rush 竞速 / duo-vs-star 车轮战 / gold-hook 无尽矿井 / garden-guard 无尽战场 / sprout-defense 1-1 / monster-crisis 无尽守家）无视觉崩坏，且 r2 修复的手绘 HUD、天空主题物、中景带、金石明度均实机可见。
- **destroy 泄漏**：r2 已实测 9/9 全 0；`1f1b17b` 后无任何代码改动（仅 docs 提交），结论沿用。

## 六、终验结论：**带条件放行**

**结论：带条件放行。** 条件仅一条：

1. **W4R3-01（duo-vs-star `.dvs-back` 返回/暂停键 32px 高）由本轮 C 档清零**（一行 CSS：`min-height:40px`，配套取反 `window4-visual-scan-r3.test.ts` 钉住断言），或由统筹书面并入 W4R1-06 存量 DOM 遗留清单。

**依据**：

- 阻断级 / 严重级终态 **0**（严重级 W4R1-02 修复经读码+量化+16px 灰度目检三重坐实）；
- r1+r2 共 14 条问题：12 修复、1 合理降级（W4R1-06 书面理由仍成立）、1 残余（即放行条件）；
- 9×6 终验总表适用格达标率 98%（49/50），唯一不达标格即放行条件对应项；
- 三项全绿：`npm test` 711/15282（三轮只增不减）、`npm run build`（188 项 / 5246.26 KiB 与 r2 fixer 水位零漂移）、`vite preview` 冒烟三项 200；
- TOP10（sling-birds 结算仪式）书面顺延成立，不属六大专项，不构成放行障碍；
- 本结论为**本窗 9 款**的放行结论，非全库 1.3 放行。

## 七、收尾复跑（合并态）

- 收尾 `git fetch` → rebase `origin/game-1.3-window4`（保留他人文件：r3 learner 计划 `f7cf3e2` 及此前全部报告一字未碰）；
- `npm test` 合并态：**711 文件 / 15282 例全绿**（15280 → +1 文件/+2 例，只增不减）；
- `npm run build`：绿（precache 188 项 / 5246.26 KiB）；
- 本轮触碰的文件仅两个：`src/games/__tests__/window4-visual-scan-r3.test.ts`（新增）与本报告。未触碰任何绘制代码、判定数值、`docs/plan-1.3-*.md`、`.cursor/skills/**`、他人报告。
- 实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
