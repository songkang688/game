# 窗口 8 · 1.3 视觉升级 · 第 2 轮监督修复员报告

- 角色：窗口 8 三人验收组 · C 监督修复员（第 2 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 新增或本窗独占文件——
  `stickers.ts` / `runnerDuoTrim.ts` 是本窗 C 档自建可继续扩）；
  判定、数值、关卡数据、存档 key、玩法测试断言、`parentAuth.ts` / `parentGate.ts` 一律不碰；
  其余 15 件冻结 kit 只 import 不改；别人的报告只读不改；教育三连不改题目对错。

## 修复计划（动手前提交）

输入三份材料：本轮 A 档 `1.3-window8-round2-tester.md`（对账 已修 6 / 不彻底 0 / 回归 0 / 合理遗留 4；
新发现 严重 1 / 一般 1 / 建议 1 + 跟催 2）、第 1 轮遗留清单 `1.3-window8-round1-fixer.md`、
第 1 轮 B 档 TOP 规格。开工时本轮 B 档 `1.3-window8-round2-learner.md` 尚未产出——
先按 A 档新问题 + 第 1 轮遗留开修，B 档出了再合并跟进。

按铁律排序（不彻底/回归 0 条 → 阻断 0 条 → 严重 → 一般 → 建议），逐条一个 commit：

| 序 | 问题 | 修法 | 责任文件 |
| --- | --- | --- | --- |
| 1 | W8R2-01 严重：red-blue-tap 闯关「点点」核心操作对象裸 emoji（10 章 SKINS mine/trap + 道具 ❄️🧲） | 判定读 `d.kind` 与 62px 热区零改动：`stickers.ts`（本窗 C 档自建件）扩 14 张信号贴纸（🔵🔴🌑🌩️💙👑💣💠🟥🔷🟪🟦🟫🧲，⭐🌟⚡❄️❤️ 已有）；新增 `red-blue-tap/dotArt.ts` 渲染层——点面 = sr-only 原 emoji + aria-hidden 贴纸，查不到贴纸保持原样直出 | `src/art/kit/stickers.ts`；新增 `red-blue-tap/dotArt.ts`；`red-blue-tap/index.ts` makeDot 一处接线 |
| 2 | W8R1-04 严重遗留：find-diff 盘面 emoji 格（题库 SHA-256 钉死） | 题库一字不动、`visual25.test.ts` SHA 用例原样全绿：新增 `find-diff/boardArt.ts` 渲染层映射——**按章门控**（该章 9 emoji 池 + 双胞胎替换对全部有贴纸才整章换装，绝不出「半贴纸半 emoji」的混排盘面）；本轮补齐前三章图集（水果 6 张 + 萌宠 2 张 + 海底 6 张），第 4–10 章 50 张与设计稿一起挂第 3 轮 | 新增 `find-diff/boardArt.ts`；`find-diff/index.ts` paintCell 一处接线；`src/art/kit/stickers.ts` 扩容 |
| 3 | W8R2-02 一般：clock-house 小鸟反馈层遮壳层连对徽章（重叠 1144px²） | 只动 `.clk-fx` 定位 CSS：right 8px → 108px，挪进顶栏两枚徽章（进度 / 连对）中间的空档；小鸟与星屑动效一行不动 | `clock-house/house.ts` HOUSE_CSS 一行 |
| 4 | W8R1-07 一般遗留：前 99 关旧题面钟（细线指针 + 9px 刻度） | `clockSVG`（levels.ts）产物被 `LEGACY_DIGEST` SHA-256 逐字节钉死不能改；走 math-farm countArt / word-garden picArt 同款**消费端 DOM 装饰**：新增 `clock-house/faceLift.ts`，渲染后把 `svg[data-h][data-q]` 的细线指针原位换 `arrowHandD` 胖/细箭头（`CLK_TOKENS.hourOrange/minuteTeal`）、轴心换 `hubSVG()`、刻度 9 → 11px；data-h/data-q/aria-label/角度一字不动，题库摘要用例原样全绿 | 新增 `clock-house/faceLift.ts`；`clock-house/runner.ts` 挂载一处 |
| 5 | W8R2-03 建议：clock-house 答对星屑是裸 ✨ emoji（12 款唯一 emoji FX 粒子） | `s.textContent = "✨"` 换四角矢量星 SVG（starGold 主色 + 白星心）；`sparkleSpecs(rand, CHEER_SPARKS)` 的粒子数 / 轨迹 / 时序一毫秒不动 | `clock-house/house.ts` cheer 一处 |
| 6 | 跟催-1 建议（B 档 TOP9）：red-blue-tap 闯关场地氛围底偏素 | `.rbt-arena` 加 5 个淡色圆点静态氛围底（r 2.5–4px，starGold .10 / blue .08 交替，错落定位，无动效）；点点热区与对比度不受影响 | `red-blue-tap/skin.ts` CAMPAIGN_VISUAL_CSS |
| 7 | 跟催-2 建议（B 档 TOP10）：pinyin-train 车票类别边条 5px 偏细 | ticketBg 边条 17–22px → 17–25px（8px 宽），一处模板字符串；chip 热区与字号下限零改动 | `pinyin-train/scene.ts` |

挂账预案（详见收尾遗留清单）：

- W8R1-04 余量：find-diff 第 4–10 章贴纸 50 张（甜品/夜空/玩具/侦探/灯塔/水面/挑战场），设计稿本轮交，第 3 轮实施；
- W8R1-08（find-diff 26px 盘面格触区）：玩法热区超视觉权限，维持挂账请裁决；
- W8R1-09（壳层 12px 装饰星 / 顶栏）：壳层统一件非本窗 12 款，维持移交全局。

每处修复配套用例只增不减；收尾做合并态复验（fetch → rebase → `npm test` → `npm run build` →
`vite preview` 冒烟）并把合并态 SHA、用例总数、构建体积写进本报告。

（修复清单、遗留处理结果、合并态复验数据随修复推进补齐——见下文。）
