# 窗口 8 · 1.3 视觉升级 · 第 2 轮监督修复员报告

- 角色：窗口 8 三人验收组 · C 监督修复员（第 2 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 新增或本窗独占文件——
  `stickers.ts` / `runnerDuoTrim.ts` 是本窗 C 档自建可继续扩）；
  判定、数值、关卡数据、存档 key、玩法测试断言、`parentAuth.ts` / `parentGate.ts` 一律不碰；
  其余 15 件冻结 kit 只 import 不改；别人的报告只读不改；教育三连不改题目对错。
- 实际使用模型 slug：`claude-fable-5-thinking-xhigh`。

## 修复计划（动手前提交，SHA `9c0411d`）

输入三份材料：本轮 A 档 `1.3-window8-round2-tester.md`（对账 已修 6 / 不彻底 0 / 回归 0 / 合理遗留 4；
新发现 严重 1 / 一般 1 / 建议 1 + 跟催 2）、第 1 轮遗留清单 `1.3-window8-round1-fixer.md`、
第 1 轮 B 档 TOP 规格。开工时本轮 B 档 `1.3-window8-round2-learner.md` 尚未产出——
先按 A 档新问题 + 第 1 轮遗留开修，B 档出了再合并跟进（跟进结果见第三节）。

按铁律排序（不彻底/回归 0 条 → 阻断 0 条 → 严重 → 一般 → 建议），逐条一个 commit：
计划表原文见 `9c0411d`，执行结果见下文第一节（7 条全部落地，无一降级）。

---

## 一、修复清单（来源 / 位置 / 修法 / 配套用例 / commit SHA）

| # | 来源 · severity | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | W8R2-01（A 档新发现 · 严重） | `red-blue-tap/index.ts:527` makeDot；SKINS 10 章 mine/trap + 道具 ❄️🧲 共 **19 种符号** | 判定读 `d.kind`、62/56px 热区零改动：`stickers.ts`（本窗自建件）扩 **14 张信号章**（🔵🔴 圆点 / 🟥🟦🟪🟫 色块 / 🔷💠 宝钻 / 💙 蓝心 / 🌑 睡月 / 🌩️ 雷云 / 👑 皇冠 / 💣 软弹 / 🧲 磁铁，⭐🌟⚡❄️❤️ 与既有图集共用）；新增 `red-blue-tap/dotArt.ts` 渲染层换装——sr-only 原 emoji（读屏一字不差）+ aria-hidden 贴纸 SVG，查不到贴纸兜底直出永不空屏 | `dotArt.test.ts` 7 例：覆盖率从源码现抓（谁加 emoji 不配贴纸直接红）/ 换装结构 / 兜底 / makeDot 接线防拆 / CSS 纪律（0 宽高 0 动画） | `2aa5dbe` |
| 2 | W8R1-04（第 1 轮严重遗留 · 挂账推进） | `find-diff/index.ts` paintCell；题库 `levels.ts` 被 `visual25.test.ts` SHA-256 钉死 | 题库一字不动、SHA 用例原样全绿：`stickers.ts` 扩**找不同图鉴 14 张**（🍌🍇🍓🍍🥝🍒 / 🐼🦊 / 🐙🦀🐬🐳🦑🐚）；新增 `find-diff/boardArt.ts` 渲染层查表 + **整关门控**——该关三块盘面每种图案都有贴纸才整关换装，缺一张整关回退 1.2 原样输出（逐字节一致），绝不出「半贴纸半 emoji」混排；前 3 章（水果/萌宠/海底）本轮亮灯，第 4–10 章配齐一章亮一章；sr-only 保读屏，26px 热区与 diffIdx 判定零改动 | `boardArt.test.ts` 8 例：前 3 章池 + 双胞胎对图集配齐 / 同池贴纸两两不同 / 整关门控开闸关闸 / 关闸输出与 1.2 逐字节一致 / 兜底 / paintCell 接线 | `7a545ae` |
| 3 | W8R2-02（A 档新发现 · 一般） | `clock-house/house.ts` HOUSE_CSS `.clk-fx{top:2px;right:8px}` | 只动定位一行：right 8px → **120px**，小鸟反馈层挪进顶栏进度徽章与连对徽章之间的空档（连对徽章≈108px 贴右缘永远盖不住，320px 窄屏与进度徽章也不打架，重叠 1144px² → 0）；小鸟 / 星屑动效与 pointer-events:none 一行不动 | `fxLayout.test.ts` 3 例：让位几何 / 纯装饰（pointer-events:none + 尺寸） / 动效零改动 | `6f3b420` |
| 4 | W8R1-07（第 1 轮一般遗留） | `clock-house` 前 99 关旧题面钟（细线指针 + 9px 刻度），`clockSVG` 产物被 `LEGACY_DIGEST` SHA-256 逐字节钉死 | 走 math-farm countArt / word-garden picArt 同款**消费端 DOM 装饰**：新增 `clock-house/faceLift.ts`，渲染后把 `svg[data-h][data-q]` 内部就地换装——细线时/分针原位换 `arrowHandD` 胖/细箭头（针尖 = 老 line 端点，角度公式零改动，色 = `CLK_TOKENS.hourOrange/minuteTeal`）、轴心换 `hubSVG()` 铆钉、刻度 9 → 11px；只重写 svg 内部，data-h / data-q / aria-label 与题面容器 childList 无感（不惊动 runner 的题面观察器）；runner 一处挂载正题回顾共用，destroy 断开 | `faceLift.test.ts` 8 例：换装工序 / 幂等（重复装不翻车） / **前 99 关全量扫** / 题库摘要零改动 / runner 接线与 destroy | `53f593a` |
| 5 | W8R2-03（A 档新发现 · 建议） | `clock-house/house.ts:285` cheer `s.textContent = "✨"` | 12 款唯一的 emoji FX 粒子清零：换 `sparkStarSVG` 矢量四芒星（starGold 主色 + 深描边 + 白星心，尺寸沿用 `sparkleSpecs` 字号档）；粒子数 6 / 轨迹 / `SPARK_MS` 时序一毫秒不动，reduced 下 `.clk-spark display:none` 既有防线不变 | `sparkStar.test.ts` 2 例：星本体工序（无 emoji、有描边星心）/ cheer 接线与时序零改动 | `615cfc4` |
| 6 | 跟催-1 = B 档 TOP9（第 1 轮移交 · 建议） | `red-blue-tap/skin.ts` CAMPAIGN_VISUAL_CSS | `.rbt-wrap .rbt-arena` 加 5 个淡色圆点 radial-gradient 错落铺底（金 3 蓝 2 交替、r 2.6–4px、全走 `withAlpha(PASTEL.*)` token、透明度 ≤ .12），纯静态零动效；只写 `background-image` 长手不碰底色 / 几何 / 热区 | `arenaAtmosphere.test.ts` 3 例：数量与色源 / 透明度上限 / 零动效零几何纪律 | `057fafd` |
| 7 | 跟催-2 = B 档 TOP10（第 1 轮移交 · 建议） | `pinyin-train/scene.ts` ticketBg | 类别色边条 17–22px → **17–25px（8px 宽）**，一处模板字符串；25px 仍在 padding-left 28/30px 文字起点之内，打孔圆点（11px±3.5）碰不到；`CHIP_MIN_PX=48` 热区与字号下限零改动 | `ticketBand.test.ts` 3 例：宽度与老写法退役 / TICKET_CSS 6 处 + QUIZ_SKIN_CSS 5 处同步 / 不吃字不吃孔、热区纪律 | `44378fa` |

**修复统计：7 条全落地——按来源：A 档本轮新发现 3（严重 1 / 一般 1 / 建议 1）、第 1 轮遗留 2（W8R1-04 推进 + W8R1-07 清账）、B 档第 1 轮移交 2（TOP9 / TOP10）。配套新增 7 个测试文件 / 34 条用例，既有用例只增不减、零改动。**

---

## 二、第 1 轮遗留清单处理结果（6 条：清 3 · 推进 1 · 维持 2）

| 遗留条目 | 本轮处置 |
| --- | --- |
| W8R1-04 find-diff 盘面 emoji（严重 · 专项） | **推进**：映射管线（boardArt 整关门控）落地 + 前 3 章 14 张图集亮灯 + 第 4–10 章 50 张设计稿成文（见第四节，B 档修订清单第 2 条的本轮验收线达成）；余量挂第 3 轮实施 |
| W8R1-07 clock-house 前 99 关旧题面钟（一般） | **清账**：faceLift 消费端装饰落地（`53f593a`），两代钟面同工序，题库摘要零改动 |
| W8R1-08 find-diff 26px 盘面格触区（一般） | **维持挂账请裁决**：玩法热区改动超视觉三人组权限，钉死用例在位 |
| W8R1-09 壳层 12px 装饰星 / 顶栏（建议） | **维持移交全局**：`level99.ts` 壳层统一件，非本窗 12 款 |
| B 档 TOP9 red-blue-tap 闯关氛围底（建议 · 移交） | **清账**（`057fafd`） |
| B 档 TOP10 pinyin-train 车票边条（建议 · 移交） | **清账**（`44378fa`） |

---

## 三、B 档一致性评审对账（第 2 轮 learner 报告第三 / 四节，最违和 5 款）

B 档报告开工时未出，先按 A 档开修；`d99b73b` 落地后逐条对账，结论：**B 档给本轮 C 档的 6 条（修订清单 1–6）全部落地，且修法与其规格一致或经说明取备选项**。

| B 档点名 | 本轮改了什么 | 与 B 档规格的出入说明 |
| --- | --- | --- |
| 3.1 red-blue-tap（最违和 · 工艺代际断层） | 点点贴纸化（`2aa5dbe`）+ 氛围底（`057fafd`）双双落地，闯关与对战场工艺代际拉平 | B 档建议「同 commit 顺手带 TOP9」——实际分两个 commit（先清严重项再补建议项，遵守「每修一处一个 commit」铁律），同轮完成效果等价。造型全过 B 档 5.2 盯防：⭐🌟 沿既有 goldStar 十点星**无脸无四肢**；👑 三尖圆球顶通用简笔王冠，无徽章化装饰无盾形底；💣 圆滚滚软弹、**火花改星点、无引信明火**、无爆炸伤害暗示；⚡ 三折几何标准形非尾巴状 |
| 3.2 find-diff（盘面停在 1.1 工艺） | 映射管线 + 前 3 章亮灯 + 设计稿成文（第四节） | 与 B 档 TOP5 裁决路径一致；一处保守偏离：B 档提「SHA 用例改钉映射表」，本轮**题库 SHA 用例原样不动**（继续钉题目数据），映射表完整性由新增 `boardArt.test.ts` 单独钉——双保险，第 3 轮实施时也不必动老用例 |
| 3.3 clock-house（两代同堂 + 仪式层三瑕疵扎堆） | 三处全清：小鸟让位（`6f3b420`）、✨ 换矢量星（`615cfc4`）、旧钟面 faceLift（`53f593a`，B 档「必须挂专项不许静默丢账」的那条本轮直接修掉） | 星屑用四芒星而非 sparkle kit 整件替换：cheer 的粒子壳（定位 / 动画类 / 时序）是 clock-house 自有实现，只换粒子本体最小改动；工序（金主色 + 深描边 + 白星心）与 kit 星花一致 |
| 3.4 pinyin-train（类别编码强度掉队） | 边条 5px → 8px（`44378fa`） | B 档「二选一」里推荐圆点副记号方案（补形状通道）；本轮取**加宽方案**——A 档跟催与第 1 轮规格的首选项即加宽，且 360px 类别辨识弱是本案主诉；「打孔圆点下 r=2px 同类色圆」作为形状通道增强**备档第 3 轮**，两案不冲突可叠加 |
| 3.5 shape-kingdom（问答关氛围落差） | 未动 | B 档明确给第 3 轮（修订清单第 7 条），本轮不越单，登记在第六节遗留清单 |

B 档修订清单第 8 条（music-stars 锚点星）同为第 3 轮备档，未动。撤销项（TOP8 附带触屏隐藏）无需处置。

---

## 四、W8R1-04 专项设计稿：find-diff 盘面图集 + 版本化（B 档验收线 · 第 3 轮实施依据）

### 4.1 已落地的管线（本轮 `7a545ae`）

- **映射表在渲染层**：`boardArt.ts` 只做 emoji → `stickers.ts` 贴纸查表，题库 `levels.ts` / `scene12.ts` 零改动，`visual25.test.ts` 的题目数据 SHA-256 用例原样全绿。
- **整关门控**：`sceneStickersReady(scene)` 扫该关左 / 中 / 右三块盘面的全部格子（含双胞胎替换后的图案），全部查得到贴纸才开闸；关闸时 `paintCell` 走 1.2 原模板串（逐字节一致）。一章的池子配齐即整章亮灯，永不混排。
- **可访问性**：开闸后每份图案 = sr-only 原 emoji + aria-hidden 贴纸 SVG；count 差异的两份图案两份 sr 文本。
- **判定纪律**：26px 热区、diffIdx 判定、`scaleX(flip)` / tint / count 变换全部沿用原 transform 管道，贴纸只是换掉了字形节点。

### 4.2 第 4–10 章图集缺口（共 **50 张**，逐章清点）

| 章（池序） | 池子 9 图案 | 已有贴纸 | 缺口 | 备注（双胞胎对 ★ / 翻转差异用 ▲ 需非对称剪影） |
| --- | --- | --- | --- | --- |
| 4 甜品小屋（3） | 🍰🧁🍩🍪🍫🍭🍬🎂🍮 | 🍪🍬 | **7**：🍰🧁🍩🍫🍭🎂🍮 | ★🍰↔🎂、🍭↔🍬、🍩↔🍪：三对双胞胎必须「像但可辨」——奶油层数 / 糖纸朝向 / 糖霜色差做区分位 |
| 5 夜空营地（4） | 🌙⭐🌟✨☁️🪐🌈🌠🔭 | 🌙⭐🌟✨☁️ | **4**：🪐🌈🌠🔭 | ★⭐↔🌟 已有；★✨↔🌠：🌠 画「拖尾流星」与 ✨ 的三星簇拉开 |
| 6 玩具城堡（5） | 🧸🚗🚂✈️🚁⚽🏀🎈🥁 | 🚗⚽ | **7**：🧸🚂✈️🚁🏀🎈🥁 | ★⚽↔🏀（纹路五边形 vs 弧线）、★🚗↔🚂（烟囱）、★✈️↔🚁（旋翼）；🧸 过去色剪影自查（圆耳圆身通用简笔，避开任何知名熊形象） |
| 7 三图侦探社（6） | 🔍🧩🗝️📜🕯️🎩🧭📒📕 | — | **9** | ★📒↔📕（色 + 书签位）；▲🔍🗝️🧩📜📒📕 全部走非对称剪影（柄偏一侧 / 齿偏一侧 / 缺口偏一侧） |
| 8 旋转灯塔（7） | 🌀🚦🛟⚓🪁🎐🌪️🛶🧿 | — | **9** | ★🌀↔🌪️（螺旋 vs 漏斗）；▲🌀🌪️🪁🎐🛶 非对称；🧿 画通用蓝白同心圆珠（装饰珠，不做宗教符号细节） |
| 9 镜像水面（8） | 🪞💧🫧🐟🌊🪷🦢🍃🪸 | 💧🐟🦢🍃 | **5**：🪞🫧🌊🪷🪸 | ★💧↔🫧（单滴 vs 泡群）、🪷↔🪸（花 vs 枝）；▲🌊 浪头偏向一侧；已有 🐟🦢🍃 本轮复核均非对称 ✅ |
| 10 连环挑战场（9） | ⏱️🏅🎯🎲🚀💎🔔🏁⌛ | — | **9** | ★⏱️↔⌛（表冠 vs 沙腰）；▲🚀🏁🏅⏱️ 非对称（尾焰 / 旗面 / 绶带 / 表冠偏一侧） |

复用盘点：第 4–10 章 63 个图案位里 13 个已有贴纸可直接复用（🍪🍬🌙⭐🌟✨☁️🚗⚽💧🐟🦢🍃），净新增 **50 张**。

### 4.3 工序与红线（写给第 3 轮实施者）

- 工序沿 stickers 图集统一约定：双色分面 + 2px 深描边（`o()` 家族色）+ 左上高光 + 软投影 `gs()`，48×48 viewBox。
- **同池两两可辨**：每章 9 张放进 `boardArt.test.ts` 既有的「两两不同」用例（本轮已给前 3 章打样），双胞胎对额外钉「像但有稳定区分位」。
- **FLIPPABLE 契约**：上表 ▲ 标记的 16 种图案会被 `flip` 差异用到（`scene12.ts FLIPPABLE`），贴纸剪影必须左右非对称，否则「换了朝向」肉眼不可辨——实施时逐张过 `scaleX(-1)` 目检并加用例钉非对称（本轮已给 🐬🦐 等打样）。
- 商标 / 分级：全部过「去色剪影自查」；🧸 熊形、🚀 火箭注意避开知名造型；💎🔔🎲 等对称物不进 FLIPPABLE 无非对称要求；无武器化 / 伤害暗示图案（本池不涉及）。
- 版本化路径：贴纸补齐一章、`sceneStickersReady` 自动对该章开闸，无需改任何门控代码；第 3 轮收尾跑 188 关全量回归（含 rush 多轮 / mirror / 三图）确认无混排、无漏网。
- 预估：50 张 × 工序化拼装，`stickers.ts` 单文件扩容即可（本窗自建件），不新增冻结 kit 依赖。

---

## 五、性能回压

A 档第 2 轮深度走查 12 款帧率**全部 60fps**（其表格逐款登记），无 < 30fps 对象，本轮无回压任务。
本轮新增绘制全部零成本档：贴纸 / 氛围底 / 边条 / 矢量星均为静态 SVG 或静态 CSS（无新计时器、无新动画通道）；
faceLift 的 MutationObserver 只在题面变更时对至多几个 svg 做一次字符串替换，幂等标记 `data-clk-lift` 防重复做工。

---

## 六、自查与红线

- **kit 纪律**：冻结 kit（palette / jellyBtn / sparkle / clockface / train …）只 import 零改动；扩容只发生在本窗自建件 `stickers.ts`；新增文件全部为本窗独占（dotArt / boardArt / faceLift + 各 test）。`git log --stat` 可验。
- **玩法零行**：7 条修复无一触碰判定 / 数值 / 关卡数据 / 存档 key / 玩法断言；两处 SHA 钉死题库（find-diff / clock-house）逐字节未动，对应守门用例原样全绿。
- **商标 / 分级**：新增 28 张贴纸逐张过 B 档 5.2 盯防口径与去色剪影自查（⭐ 无脸、👑 无盾无徽、💣 无明火无伤害暗示、⚡ 三折几何、🌑 睡脸通用简笔）；无血 / 无伤害 / 无死亡描写、失败只鼓励、无广告内购、无商标与官方角色。
- **报告纪律**：A / B 档报告只读未改；本报告独立成文。

---

## 七、合并态复验（第五节流程）

- `git fetch origin game-1.3-window8` → 远端头 `1943d9e`（tester 过检 + learner 报告齐）→ rebase 本档 7 个修复 commit 落其上，三份报告与全部修复确认在位。
- `npm test`：**745 个测试文件 / 15559 条用例全绿**（第 1 轮合并态 736/15507 → 本轮 B 档水位 738/15525 → 本档 745/15559，**用例只增不减**，本档净增 7 文件 / 34 例）。
- `npm run build` 全绿（tsc --noEmit + vite build）：PWA precache **192 项 / 5407.10 KiB**（第 1 轮合并态 5385.52 KiB，**+0.40%**，远低于 10% 说明线；增量 = 28 张贴纸 SVG 字符串 + 三个渲染层新模块）。
- `npx vite preview` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**。
- 合并态复验基于 SHA **`44378fa`**（最后一个修复 commit；本报告 commit 落其上后普通 push `origin game-1.3-window8`，未 force）。

## 八、第 2 轮视觉验收结论

- **本轮验收标准对照**：`npm test` 全绿且用例只增不减 ✅；`npm run build` 全绿 ✅；preview 冒烟全绿 ✅；第 1 轮「不彻底 / 回归」问题 0 条（A 档对账即 0）✅；本轮阻断级残留 **0** ✅；严重级残留 **1 条且已登记**（≤3 达标）✅。
- **结论：第 2 轮视觉验收通过。** A 档新发现 3 条全清，第 1 轮遗留清 3 推进 1，B 档本轮 6 条点名全落地。

### 移交第 3 轮的遗留清单

| 条目 | severity | 状态 |
| --- | --- | --- |
| W8R1-04 余量：find-diff 第 4–10 章贴纸 50 张 | 严重（推进中） | 管线已通、设计稿在第四节，第 3 轮按稿实施 + 188 关全量回归 |
| W8R1-08 find-diff 26px 盘面格触区 | 一般（超权限） | 玩法热区，维持挂账请第 3 轮终验裁决 |
| W8R1-09 壳层 12px 装饰星 / 顶栏 | 建议（非本窗） | 维持移交全局 |
| shape-kingdom 问答关城堡氛围帽 | 建议 | B 档修订清单第 7 条，第 3 轮实施（`castleSvg(0)` 静态复用） |
| music-stars 星轨远端锚点星 | 建议 | B 档修订清单第 8 条，第 3 轮备档 |
| pinyin-train 打孔圆点下同类色圆点副记号 | 建议（增强） | B 档推荐的形状通道方案，与本轮加宽可叠加，第 3 轮备档 |
