# 1.3 窗口 7 · 第 1 轮视觉验收 · 学习优化员报告（B 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 1 轮学习优化员（B 档）。只写本报告 + 本窗九款**纯文案**（guide / blurb / 提示语）+ 文案快照测试；绘制代码一行不碰；A 档 / C 档报告一字不改。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 〇、学习计划（动手前提交）

1. **向外看**：调研 4399 及同类成熟平台上本窗九个题材（接水果 / 切水果 / 贪吃蛇·毛毛虫 / 连连看 / 拼图 / 翻翻乐 / 斗地主 / 钓鱼 / 跑酷·清扫）头部作品的**画面密度**（一屏背景层数 / 装饰物件数 / 动效通道数）与**角色剪影**（16px 缩略后靠哪一笔认人）。红线：只记结论文字，不下载、不截图入库、不描摹任何具体造型。
2. **向内看**：通读本窗九款 `visual*.ts` / `index.ts` / kit 引用，量化各款当前的层数 / 装饰数 / 动效通道数，与题材基准做差。
3. **对照评估**：九款逐款出四字段表（密度差距 / 剪影达标 / 观感短板 TOP1 / 建议动作），建议全部写到「绘制规格级」。
4. **给 C 档的清单**：按「严重度 × 修复成本」排序，前 10 条展开绘制规格；对照 A 档报告（阻断 0 / 严重 6 / 一般 5 / 建议 2）的 6 条严重项。
5. **商标风险提醒**：题材 → 易踩线对象 → 规避要点，专节提醒 C 档。
6. **文案优化**（唯一动代码的范围）：九款 `guide.ts` / `meta.ts` blurb / 纯提示语，把 1.3 新视觉亮点讲进引导；每处登记入本报告第五节，并补快照测试。
7. 收尾：fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window7`。不 force、不改 `main`、不动 `game-1.2`、不用 `gh` 开 PR。

（以下各节随工作推进填写。）
