# 窗口 8 · 1.3 视觉升级 · 第 2 轮视觉测试员报告

- 角色：窗口 8 三人验收组 · A 测试员（第 2 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只改 `*.test.ts` 与本报告；不改绘制实现代码；kit 已有文件只 import 不改；他人报告一字不碰。

## 复验计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数并与第 1 轮合并态 **736 文件 / 15507 例** 对比（只增不减）。
2. 逐条对账（本轮主责）：`1.3-window8-round1-tester.md` 的 W8R1-01 ~ W8R1-10 十条，对照 `1.3-window8-round1-fixer.md` 修复清单逐条给结论——**已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留**。修了的验证「真修好没有、有没有修出新问题」，遗留的确认还在遗留清单里没丢。
3. 深度走查：**本窗 12 款全抽**（角色特写 2×/16px、动效完整链三段 + reduced 退化、场景层次四层、kit 一致性、CPU 4× 节流性能 ≥30fps）。必抽点名 `red-blue-race`（2.5D 不许退化）。
4. 回归防线：判定热区钉死断言全绿、功能提示在 reduced 下保留、教育数据（笔顺 / 音高 / 题目文本）快照全绿、destroy 泄漏复查。
5. 六大专项（火柴人 / 金币体积 / 双人可区分 / 2.5D / 360px / 商标）修复后达标率 vs 第 1 轮，给 12 款 × 六专项两轮对比矩阵。
6. 第 1 轮沉淀的机器化扫描用例 `src/games/__tests__/window8-round1-visual-scan.test.ts` 必须还在跑、全绿；本轮按需新增复验用例（仅 `*.test.ts`）。
7. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window8`（禁止 force）。

（以下各节随复验推进填写。）
