# 窗口 8 · 1.3 视觉升级 · 第 2 轮视觉测试员报告

- 角色：窗口 8 三人验收组 · A 测试员（第 2 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只改 `*.test.ts` 与本报告；不改绘制实现代码；kit 已有文件只 import 不改；他人报告一字不碰。
- 测试环境：headless Chrome（360×640 设备模拟 + CDP CPU 4× 节流，puppeteer-core `--no-save` 临时安装未入 manifest）、vitest、sharp 栅格化；走查驱动脚本放 /tmp 未入库。

## 复验计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数并与第 1 轮合并态 **736 文件 / 15507 例** 对比（只增不减）。
2. 逐条对账（本轮主责）：`1.3-window8-round1-tester.md` 的 W8R1-01 ~ W8R1-10 十条，对照 `1.3-window8-round1-fixer.md` 修复清单逐条给结论——**已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留**。修了的验证「真修好没有、有没有修出新问题」，遗留的确认还在遗留清单里没丢。
3. 深度走查：**本窗 12 款全抽**（角色特写 2×/16px、动效完整链三段 + reduced 退化、场景层次四层、kit 一致性、CPU 4× 节流性能 ≥30fps）。必抽点名 `red-blue-race`（2.5D 不许退化）。
4. 回归防线：判定热区钉死断言全绿、功能提示在 reduced 下保留、教育数据（笔顺 / 音高 / 题目文本）快照全绿、destroy 泄漏复查。
5. 六大专项（火柴人 / 金币体积 / 双人可区分 / 2.5D / 360px / 商标）修复后达标率 vs 第 1 轮，给 12 款 × 六专项两轮对比矩阵。
6. 第 1 轮沉淀的机器化扫描用例 `src/games/__tests__/window8-round1-visual-scan.test.ts` 必须还在跑、全绿；本轮按需新增复验用例（仅 `*.test.ts`）。
7. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window8`（禁止 force）。

---

## 一、全量跑数据与两轮用例数对比

| 项 | 第 1 轮合并态（fixer 登记） | 本轮开工态 | 本轮收尾态（含测试员新增 8 例） |
| --- | --- | --- | --- |
| `npm test` | 736 文件 / 15507 例 | **736 文件 / 15507 例全过**（175.2s），与第 1 轮合并态逐位一致 | 737 文件 / 15515 例全过（见第八节收尾复跑） |
| `npm run build` | precache 192 项 / 5385.52 KiB | 绿（tsc --noEmit + vite build），precache **192 项 / 5385.52 KiB**，逐位一致 | 绿（收尾复跑） |
| `vite preview` 冒烟 | 200 | `/` 返回 200（注：本环境 preview 绑 IPv6，需走 `localhost`） | — |

**只增不减达标**：736/15507 → 737/15515（+1 文件 / +8 例，全部为本档新增复验用例）。第 1 轮机器化扫描 `window8-round1-visual-scan.test.ts`（8 例）**还在跑、全绿**。

---

## 二、第 1 轮问题逐条对账表（本轮主责）

对账输入：`1.3-window8-round1-tester.md` 问题清单（阻断 0 / 严重 5 / 一般 3 / 建议 2）× `1.3-window8-round1-fixer.md` 修复清单（修 6 挂 4）。每条给四选一结论；验证手段 = 修复 commit diff 检视 + 全量用例 + 本轮浏览器实测（360×640）+ sharp 独立复现量化。

| 编号 | 第 1 轮 severity | 修复 commit | 本轮结论 | 验证证据（量化） |
| --- | --- | --- | --- | --- |
| W8R1-01 math-farm 数一数计数物裸 emoji | 严重 | `1aa240e` + `a3277f5` | **已修复** | ① 浏览器实测第 1 关：`.mtf-illus-count` 贴纸卡显示、`[data-unit="one"]` 5 枚 SVG（360px 下 26px 缩档生效）、`.qz-prompt` 收进 1×1 sr-only、题卡**可见 emoji 0**（含祖先链的 sr 判定）；② `countArt.test.ts` 9 例绿（1–34 关全量扫、n=答案）；③ `git log 7913def..HEAD -- levels.ts gen.ts` 空 = 题目数据零改动；④ 🐮 贴纸 2× 目检：圆脸+双耳+腮红，双色分面+深描边+左上高光，工序合格 |
| W8R1-02 word-garden 配图裸 emoji | 严重 | `1aa240e` + `c090511` | **已修复** | ① 浏览器实测第 1 关第 1 题（⛰️ 看图认字）：`.wgd-pic-card` 镜像卡显示（1 枚 SVG 贴纸）、题卡挂 `wgd-pic-sr`、可见 emoji 0；② `picArt.test.ts` 10 例绿（**188 关题面+选项全量扫无漏网**）；③ 字库 `logic.ts` 零 diff；④ ⛰️ 贴纸 2× 目检：双峰雪顶原创简笔，合格 |
| W8R1-03 kitty-care 喂饭/逗猫道具裸 emoji | 严重 | `1aa240e` + `4467ad4` | **已修复** | ① 浏览器实测喂饭场景：3 个食物按钮 `.ktc-propicon svg` 3/3、饭碗 SVG、想吃气泡 = sr-only 原 emoji + 贴纸，`.ktc-tray/.ktc-bowl` 区域可见 emoji **0**；② `propArt.test.ts` 4 例绿（喂饭/逗猫端到端照常走通）；③ `tasks.ts` 零 diff（判定读 `food.name` 不受扰）；④ 🍗/🥣 贴纸 2× 目检合格 |
| W8R1-04 find-diff 盘面 emoji 格 | 严重 | 挂账（fixer 遗留清单第 1 条） | **合理遗留** | ① 浏览器实测第 1 关：盘面 18 格仍为 emoji（样本 🍑🍌🍑🍒），与挂账口径一致、没被偷修；② 题目数据 SHA-256 钉死用例（`visual25.test.ts`）仍绿 = 没人为「修」它去动题库；③ 专项路径在案：本轮 C 档出「图集 + 数据版本化」设计稿、第 3 轮实施（B 档 TOP5 裁决）。**跟催本轮 C 档交设计稿** |
| W8R1-05 双跑者 16px 灰度可分辨 0% | 严重 | `1475a8b` | **已修复** | ① sharp **独立复现**（与第 1 轮同口径 16×16 灰度 \|ΔL\|≥24）：run0 19.5% / run1 19.5% / jump 17.6% / slip 22.7%，四帧全部 ≥15% 验收线（修前 0.0%）；② 本轮加测 32px 特写档：14.7–17.7%（修前 0.2%），新用例钉 ≥12%；③ 2× 目检：红双丸子头+白亮带+号码 1 vs 蓝反戴帽舌+深藏青带+号码 2，剪影/明度/附件三通道齐；④ 尾部注入不改原串（`duoTrim.test.ts` 9 例绿），`runnerSvg.ts` 冻结未动；⑤ 浏览器玩局 `[data-duo-trim]` 红蓝各 4 帧在位 |
| W8R1-06 word-garden 花瓣单色平涂 | 一般 | `b70dad7` | **已修复** | `flowerShade.ts` 2 停径向渐变（offset 0.3 瓣根 −16 深 → 1.0 瓣尖 +14 亮，userSpaceOnUse 花心系）；`flower.ts` 冻结零改动（git log 空）；`flowerShade.test.ts` 5 例绿（三色×五帧全组合、字节级不动、两个调用点全接线） |
| W8R1-07 clock-house 前 99 关旧钟面 | 一般 | 挂账（移交第 2 轮，B 档 TOP7 规格在案） | **合理遗留** | 浏览器实测第 1 关：旧题面钟仍在（`.qz-prompt svg text` 12 个刻度数字 font-size=9 SVG 单位；实际显示约 180px 直径下有效字高 ≈16px，可读性尚可但与新木牌钟面观感割裂）；promptHTML 属题目数据的约束未变。**跟催本轮 C 档按 TOP7 规格处理或再挂账** |
| W8R1-08 find-diff 盘面格触区 26px | 一般 | 挂账（超视觉权限） | **合理遗留** | 浏览器实测 `.fdf-cell` 26×26px 原样（热区钉死用例 `cellHotZone.test.ts` 仍绿）；玩法热区裁决权不在视觉三人组，维持挂账 |
| W8R1-09 壳层 12px 装饰星 / 顶栏 | 建议 | 挂账（移交全局） | **合理遗留** | 壳层 `level99.ts` 非本窗 12 款范围；`l99-star` 现状未变，移交全局待办不丢 |
| W8R1-10 red-blue-tug 键帽提示 12px | 建议 | `e6e36f1` | **已修复** | ① 浏览器实测 computed font-size：两处 `.rbg-sub` 均 **14px**；② diff 检视：只动两行 font-size，按钮盒子/热区/「按住 F / 空格」文本零改动；③ `subLabel.test.ts` 2 例绿 |

**对账统计：已修复 6 / 修复不彻底 0 / 修出回归 0 / 合理遗留 4。** 第 1 轮阻断/严重级修复确认率 **100%**（严重 5 条 = 修 4 验 4 过 + 挂账 1 条按裁决路径在案）。

另核对 fixer 遗留清单的移交项：B 档 TOP9（red-blue-tap 闯关氛围底）、TOP10（pinyin-train 车票边条 5px）浏览器截图确认**仍未做**（移交本轮 C 档的建议级，未丢账，见第四节跟催）。

---

## 三、深度走查（本窗 12 款全抽 × 五项）

方法：主角/核心道具 sharp 栅格化 2×（128px）与 16px 各一次目检 + 量化；动效链与一致性做代码走读（`@keyframes` 数 / reduced 处理数落表）；场景层次按 360×640 真实玩局截图判四层；性能 = CPU 4× 节流下玩局连续点按 3s 采样 rAF。

| 款 | 角色特写（2× / 16px） | 动效完整链（预告→过程→收尾 / reduced） | 场景层次（背景/中景/主体/HUD） | 一致性（kit 光照/描边/色板） | 性能（CPU4×） |
| --- | --- | --- | --- | --- | --- |
| red-blue-race | ✅ 2×：渐变背心+1.4–1.6px 描边+落地阴影+duoTrim 配件左上高光；16px 红浅蓝深剪影可分（17.6–22.7%） | ✅ 起跑灯红红绿三段预告→奔跑两帧+视差过程→冲线拱门+彩纸收尾；17 keyframes / 27 reduced 处理，reduced 下起跑灯 4 元素仍在（实测） | ✅ 天空/看台彩旗→透视赛道→双跑者→车道徽章 HUD 四层分明 | ✅ perspective 720px + rotateX18° 在位（用例+浏览器双证）；duoTrim 继承 runnerSvg 描边语境 | **60fps** |
| red-blue-tap | ❌ 对战场 jellyBtn/信号双通道合格，但**闯关「点点」核心操作对象是裸 emoji**（新问题 W8R2-01，第 1 轮漏检更正） | ✅ 信号预告（rbtIn 缩放浮现）→按压回弹→星屑+计分翻页；13/23；reduced 下信号形状通道保留 | ✅（闯关场地氛围底偏素 = B 档 TOP9 建议项，未消化） | ✅ jellyBtn 顶部高光、sparkle 走 kit | **60fps** |
| red-blue-tug | ✅ 2×：红头带结 vs 蓝鸭舌帽剪影清晰、135° 渐变躯干；16px 27.7–30.5%（第 1 轮量化，结构未变） | ✅ 节拍环收缩预告→队伍后仰三档→丝带过线+彩纸抛帽；7/23 | ✅ 天空远山→草地旗串→双队+丝带→体力条 HUD | ✅ tugTeam 对角渐变、头 2px 描边 | **60fps** |
| clock-house | ✅ 小鸟圆身+翅+栖木原创（2× 代码级目检）；小屋/摆锤矢量 | ✅ 摆锤/暖窗常驻微动→答对小鸟弹出咕咕+星屑 6 粒→连对点亮；6/4；reduced 下 bird-body animation:none 有用例钉死。**但星屑粒子是裸 ✨ emoji（W8R2-03）** | ⚠️ 四层分明，但小鸟反馈层遮住壳层连对徽章计数（新问题 W8R2-02，实测重叠 1144px²） | ✅ 指针胖橙/细青双通道；旧题面钟割裂感 = W8R1-07 挂账 | **60fps** |
| math-farm | ✅ 贴纸 2×：双色分面+2px 描边+左上高光+软投影；16px 描边仍可辨 | ✅ 浇水预告→作物三阶段生长→连对蜜蜂+彩纸；12/13 | ✅ 天空太阳→谷仓风车菜畦→题卡贴纸行→木牌选项 | ✅ stickers 沿 crops 工序（契约用例钉死） | **60fps** |
| pinyin-train | ✅ 火车头烟囱+圆灯剪影、`data-face="none"` 钉死 | ✅ 发车白烟预告→挂厢过程→进站彩纸；13/14 | ✅ 天空远山隧道→站台→火车→车票选项 | ✅ 车票三色边条（5px 偏细 = B 档 TOP10 建议项，未消化） | **60fps** |
| word-garden | ✅ 贴纸 2×（⛰️ 双峰雪顶）；花瓣渐变修复后光感与库约定一致 | ✅ 呼吸起笔点+箭头预演→描红笔迹→开花奖励；10/15 | ✅ 宣纸纹理→米字格→镜像贴纸卡/字卡→选项 | ✅ picArt 全部 wgd- 前缀、不碰 qz-（用例钉死） | **60fps** |
| shape-kingdom | ✅ gem 三停 135° 渐变+22% 切面高光（契约用例）；教学形状本体纯净（合理） | ✅ 描金预告→逐段点亮→升旗彩纸；8/8 | ✅ 天空远山→城堡→题卡→选项（问答关氛围帽 = B 档备档项） | ✅ gem 1.5px 最深描边+2px 底部暗边 | **60fps** |
| find-diff | ❌ 盘面主体仍 emoji 格（W8R1-04 挂账中，专项路径在案） | ✅ 命中收圈→金圈常亮→徽章点亮+缎带；13/15 | ✅ 侦探书桌→双画框→盘面→徽章 HUD | ✅ 侦探信物矢量（stage13 渐变 8 处） | **60fps** |
| color-fun | ✅ paintBlob 7 处渐变（径向提亮+高光点+挤压影），27 色遍历用例 | ✅ 呼吸提示轮到色→涟漪铺色→闪光扫过+飞入画廊；9/5 | ✅ 画室→木画框→线稿→图例/调色盘 | ✅ 图例符号 ●■▲★◆♥ 为几何字符非 emoji（合规） | **60fps** |
| music-stars | ✅ glowStar 十点星深描边+白星心，16px 外轮廓可辨（目检） | ✅ 听一遍预告→键按压+命中波环→连击流星+星铃；8/20；reduced 下 3 星 16 键静态在位（实测） | ✅ 夜空→星轨谱线→音符星→果冻键 | ✅ 音高↔彩虹七色+八度提亮双编码 | **60fps** |
| kitty-care | ✅ kitty 2×：耳三角×2+圆头+粗尾+有嘴橙猫（原创合规）；道具贴纸合格 | ✅ 想吃气泡预告→拖拽喂食→飘心+尾摆收尾；15/11 | ✅ 房间墙窗→猫爬架→主角猫→任务 HUD | ✅ propIcon 沿 toolIconSvg 工序（空标签按钮+图标 span） | **60fps** |

**性能结论**：12 款 CPU 4× 节流玩局采样全部 **60fps**（≥30 验收线大幅通过；第 1 轮抽测 57–60fps 水位保持）。
**必抽点名 red-blue-race 2.5D**：无退化（`perspective: 720px` + `rotateX(18deg)` 用例绿 + 浏览器 `.rbr-persp` 实测在位 + 截图近宽远窄）。

---

## 四、本轮新发现问题清单（交本轮 C 档）

| 编号 | severity | 位置（文件+行/函数） | 现状（量化） | 期望 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| W8R2-01 | **严重** | `src/games/red-blue-tap/index.ts:31-46` `SKINS`/`POWER_SKIN` → `:527` 点点按钮 textContent | **闯关模式核心操作对象「点点」是裸 emoji 直出**：10 章 mine/trap 皮肤（🔵🔴⭐🌑⚡🌩️💙❤️👑💣💠🟥🔷🟪🟦🟫🌟）+ 道具点 ❄️🧲，`.rbt-dot` 34px 字号白圆直排（浏览器实测 sigText="🔵"）。第 1 轮专项①只验了对战场 jellyBtn，属**漏检更正**而非回归 | 渲染层贴纸化（沿 stickers 工序扩图集或本款独占件），emoji 收 sr-only 保读屏；`.rbt-dot` 62px 热区与判定（读 skin 字符）零改动 | `red-blue-tap/index.ts` 渲染段 + `src/art/kit/stickers.ts` 扩容（C 档独占件，可自扩） |
| W8R2-02 | 一般 | `src/games/clock-house/house.ts:341` `.clk-fx{top:2px;right:8px;z-index:6}` | 小鸟反馈层（44×40）与壳层连对徽章同位于题区右上，实测重叠 **1144px²**，「连对 n」计数被小鸟盖住不可读（360px 截图双证）；pointer-events:none 热区无碍，纯视觉遮挡 | fx 层下移/左移让出徽章行（只动 `.clk-fx` 定位 CSS，小鸟与星屑动效不动） | `clock-house/house.ts` CSS 段 |
| W8R2-03 | 建议 | `src/games/clock-house/house.ts:285` `s.textContent = "✨"` | 答对星屑粒子为裸 ✨ emoji（12 款中唯一的 emoji FX 粒子）；库内 kit `sparkle.ts` 工序已存在且 red-blue-tap 在用 | 换 kit sparkleBurst 或矢量星形，粒子数/时序不变 | `clock-house/house.ts` |
| 跟催-1 | 建议 | B 档 TOP9：`red-blue-tap/skin.ts` CAMPAIGN_VISUAL_CSS | 闯关场地氛围底仍素（截图证实），第 1 轮移交本轮的建议项未消化 | 按 B 档规格补 5 个淡色圆点静态氛围底（opacity ≤ .12） | `red-blue-tap/skin.ts` |
| 跟催-2 | 建议 | B 档 TOP10：`pinyin-train/scene.ts` ticketBg | 车票类别边条仍 5px（截图证实），第 1 轮移交本轮的建议项未消化 | 边条加宽至 8px 或补同色圆点副记号 | `pinyin-train/scene.ts` |

**统计：本轮新发现 阻断 0 · 严重 1 · 一般 1 · 建议 1（+ 跟催 2 条移交项）。** 另登记不判罚：入口页右缘越界元素 8–9 个全部属壳层 `.l99-tabs` 章节选项卡的横向滚动容器（交互设计，文档级溢出为 0）。

按第 2 轮验收标准「本轮新发现的阻断级当轮清零」：本轮阻断 0，达标；W8R2-01 为严重级，**当轮交 C 档修复**。

---

## 五、六大专项两轮达标率对比（12 款 × 六专项）

✅=过 ❌=有记录问题；③④ 对无双人/非点名款按「不适用=✅」计。R1 = 第 1 轮矩阵原样，R2 = 本轮复验后。

| 款 | ①火柴人/裸emoji R1→R2 | ②收集物 R1→R2 | ③双人 R1→R2 | ④2.5D R1→R2 | ⑤360px R1→R2 | ⑥商标 R1→R2 |
| --- | --- | --- | --- | --- | --- | --- |
| red-blue-race | ✅→✅ | ✅→✅ | ❌→**✅**（16px 17.6–22.7%） | ✅→✅ | ✅→✅ | ✅→✅ |
| red-blue-tap | ✅→**❌**（W8R2-01 漏检更正） | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| red-blue-tug | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| clock-house | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| math-farm | ❌→**✅** | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| pinyin-train | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| word-garden | ❌→**✅** | ❌→**✅**（花瓣 2 停渐变） | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| shape-kingdom | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| find-diff | ❌→❌（挂账专项） | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| color-fun | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| music-stars | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |
| kitty-care | ❌→**✅** | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ | ✅→✅ |

**两轮达标率**：R1 66/72 = **91.7%** → R2 70/72 = **97.2%**（+5.5pp）。剩余 2 格：find-diff①（挂账专项按裁决路径推进）、red-blue-tap①（本轮新立 W8R2-01——为第 1 轮漏检的更正，不是修复回归）。分专项：① 8/12→10/12；② 11/12→12/12；③④⑤⑥ 维持 12/12（③ 由「race 记❌」转全过）。

**商标复扫**：第 1 轮扫描用例持续全绿；本轮新增贴纸/装饰目检——奶牛/山/鸡腿/饭碗/星贴纸、duoTrim 丸子头/反戴帽舌、橙猫（有嘴、无蝴蝶结+白身组合）均无知名角色相似造型；红方帽饰无徽记、蓝方无尖刺后掠（避开 B 档风险清单全部红线）。

---

## 六、回归防线复查

- **判定热区**：find-diff `.fdf-cell` 26×26（钉死用例绿 + 浏览器实测 26×26 原样）；red-blue-tap `.rbt-dot` 62px CSS 原样；red-blue-tug 按钮盒子/热区 diff 级零改动；kitty-care 喂饭按钮换里子但监听在按钮自身（端到端用例：点想吃的→点碗照常完成）。**0 热区被挪动。**
- **功能提示 reduced 保留**（emulateMediaFeatures 实测）：red-blue-race 起跑灯 4 元素在位 + 透视在位；red-blue-tap 信号点在位（形状双通道结构在 versus 场，用例绿）；music-stars 3 星 16 键静态在位；clock-house bird-body animation:none 有用例。**0 功能提示被动画吃掉。**
- **教育数据**：`git log 7913def..HEAD`（第 1 轮报告后至今）对 `math-farm/levels.ts` `math-farm/gen.ts` `word-garden/logic.ts` `kitty-care/tasks.ts` `find-diff/levels.ts` `clock-house/levels.ts` **零提交**；find-diff SHA-256 快照、笔顺/音高/题面快照用例全绿。**0 教育数据被改。**
- **destroy 泄漏复查**（第 1 轮实测过的 4 款）：music-stars / kitty-care / red-blue-race / color-fun 进→玩→退 ×3 轮，退出后活 interval **0**、rAF **0/s**，三轮零增长。
- **kit 纪律**：15 件冻结 kit（palette/jellyBtn/sparkle/tugTeam/runnerSvg/crops/woodSign/train/flower/brush/frame/gem/paintBlob/kittySvg/glowStar）自第 1 轮后 git log **零改动**；C 档新增独占件 2 个（stickers.ts、runnerDuoTrim.ts）只有 C 档自己的提交。

---

## 七、测试员本轮新增用例（仅 `*.test.ts`）

`src/games/__tests__/window8-round2-visual-scan.test.ts`，**8 例**：

1. W8R1-05 复验加码 ×4：红蓝跑者 **32px 特写档**灰度可分辨像素 ≥12%（四姿态帧；修前 0.2%，实测 14.7–17.7%）——把深度走查的特写口径沉淀成机器化防线；
2. 修复接线防退化 ×3：`math-farm/farmLayer.ts` 走 countPlan→renderCountIllustration + mtf-count-sr；`word-garden/runner.ts` 挂 attachPicArt；`kitty-care/arena.ts` 走 propIcon + kit stickers + ktc-propsr——行为用例归各款，这里钉源码接缝防「悄悄拆线」；
3. reduced-motion 防线 ×1：12 款实现代码必须保有 `prefers-reduced-motion` 处理（当前 2–4 文件/款）。

既有用例零改动；第 1 轮 8 例扫描原样全绿。

---

## 八、收尾复跑与验收结论

- 收尾 rebase：见提交记录（本档只动本报告与新增 test 文件，他人文件零触碰）。
- `npm test`：**737 文件 / 15515 例全过**（对第 1 轮合并态 736/15507 只增不减：+1 文件 / +8 例，全部为本档新增）。
- `npm run build`：绿；precache 192 项 / 5385.52 KiB。
- **第 1 轮阻断/严重修复确认率 100%**（严重 5 = 验证通过 4 + 合理遗留 1 按裁决路径推进）；修复不彻底 0、修出回归 0。
- 本轮新发现：阻断 0（当轮清零达标）· 严重 1（W8R2-01 交本轮 C 档）· 一般 1 · 建议 1 + 跟催 2。
- 三方合并态复验与合并态 SHA 由本轮 C 档执行，本档配合（本报告基于本分支 HEAD）。

**分级红线复核**：本轮走查截图与新增贴纸均无血/伤害/死亡描写；失败反馈只鼓励口径未动；无广告无内购；商标 0 命中。
