# 1.3 窗口 4 · 第 2 轮监督修复员报告

分支：`game-1.3-window4`。规格模板：`docs/plan-1.3-step28-C-fixer.md`（范围收窄为窗口 4 的 9 款）。
范围仅本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
修复只落 `src/games/<id>/**` 绘制层与配套 `*.test.ts`；玩法数值 / 判定 / 关卡数据 / 存档 key 零改动。

## 修复计划（开工 commit）

开工时（基线 `fbe1a71`，709 文件 / 15247 例）本轮 A 档 `docs/qa/1.3-window4-round2-tester.md` 与 B 档
`docs/qa/1.3-window4-round2-learner.md` 均未产出。按派发口径以第 1 轮三份报告驱动自查修复，
收尾 rebase 时复核两份报告状态并跟进其高优先项（若届时已产出）。

计划顺序（第 1 轮遗留最优先 → 严重 → B 档一致性 → 一般 → 建议）：

1. **W4R1-01（第 1 轮遗留 · 最优先）** garden-guard `hud12.ts` 🌸/💗🤍 画布 emoji：
   尝试在不破坏窄屏宽度测量契约的前提下做绘制层替换（hudSegments 保持契约字符串、
   drawHud 绘制时以手绘花瓣/心形图标替换 emoji 字符渲染）；做不到则书面降级交第 3 轮。
2. **W4R1-02（严重）** monster-crisis 双英雄 16px 灰度不可分：P2 贝雷帽换形 + 双人围裙下摆差异化，判定字段不动。
3. **B 档 TOP1** duo-rush 画布 HUD emoji 手绘化（drawHud 图标段+文字段分画）。
4. **W4R1-05（一般）** gold-hook 金块体表补 2 停渐变，取反 `window4-visual-scan.test.ts` 钉住断言。
5. **W4R1-03 / W4R1-04（一般）** duo-rush / duo-vs-star 双主角 16px 剪影通道加强（呆毛比例放大）。
6. **W4R1-07（建议）** duo-vs-star drawGoldStar 补独立高光。
7. **B 档 TOP8 / TOP5 / TOP6 / TOP4 / TOP10** sprout-defense 星弹金渐变、gold-hook 地表天空主题物、
   monster-crisis 天空主题物+云漂移、duo-vs-star 中景剪影带、garden-guard BOSS 剪影配饰。
8. 降级候选（视工作量书面登记）：B 档 TOP9（sling-birds 结算仪式，成本中高）、TOP7（DOM 层 emoji，容忍约定）、
   W4R1-06（1.2 存量小字号，非本轮必修）。

每修一处一个 commit：`fix(visual-r2): <款> <问题>`；配套测试只增不减。
收尾合并态复验：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟，
合并态 SHA / 用例总数（对比 round1 合并态 709/15247 只增不减）/ 构建体积写进本报告。

（修复清单与结论随修复进度补全。）
