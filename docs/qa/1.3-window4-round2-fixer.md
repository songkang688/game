# 1.3 窗口 4 · 第 2 轮监督修复员报告

分支：`game-1.3-window4`。规格模板：`docs/plan-1.3-step28-C-fixer.md`（范围收窄为窗口 4 的 9 款）。
范围仅本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
修复只落 `src/games/<id>/**` 绘制层与配套 `*.test.ts`；玩法数值 / 判定 / 关卡数据 / 存档 key 零改动；未触碰 parentAuth / parentGate；无 three.js。

## 修复计划（开工 commit `0172943`）

开工时（基线 `fbe1a71`，709 文件 / 15247 例）本轮 A/B 报告均未产出，按派发口径以第 1 轮三份报告驱动自查修复；
中途 A 档 `d8cd710`（W4R2-01～07 立项）与 B 档 `c1adbb0`（R2-TOP1～10）先后落地，随即逐条跟进对齐，
两份报告点名的全部「一般」与「建议」项在本报告提交前全部清零（唯一书面顺延项见下文 R2-TOP10）。

## 一、修复清单（16 笔修复 + 2 笔测试配套，每处一个 commit）

| # | 来源 | 款 / 位置 | 修法（只动绘制层） | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | W4R1-01 遗留（r1 fixer 降级项）/ learner R2-TOP8 | garden-guard `index.ts` drawHud / 原因气泡 / 说明条 | `hud12.ts` 冻结契约与宽度测量一字不动；drawHud 侧新增 `drawHudRichText` 按 token 渲染：🌸/💗/🤍 码点槽位（1.15 字宽，与 `estimateTextWidth` 系数一致）改画手绘 `drawPetalIcon` / 新增 `drawHeartIcon`（实/空两态），其余文字照旧 fillText，maxW 横向缩放模拟旧挤压 | art.test +4（心形两态互异、HUD 区段源码零 emoji fillText、token 函数接线） | `f288c83` |
| 2 | W4R1-02 **严重**（r2 tester 复验坐实 XOR 0%/8.5） | monster-crisis `art.ts` drawHero | 剪影双通道：P2 贝雷帽换尖顶画家帽+帽尖大星揪揪（帽形进剪影），围裙下摆 P1 圆摆 / P2 锯齿摆（重叠区灰度纹理）；`HERO_R` 与识别色不动 | art.test +1（双人帽形/下摆几何序列互异） | `3a4872d` |
| 3 | W4R2-02 / learner R2-TOP1（两轮第一优先） | duo-rush `index.ts` HUD 段 | `hudText` 整串 emoji 拆成 `hudTokens` + `drawHudToken`：小脸 `drawMiniFace`、幽灵 `drawGhostWisp`、心 `drawHeart` 实/空两态（art.ts 补 filled 参数）、金币 `drawCoinFrame` 正面帧、道具槽白圆底板+`drawPowerIcon`；支路与让分改纯文字；1.2 squeeze 挤压逻辑保留（先算 token 总宽再横向缩放）；`r.emoji`/`POWERUPS[*].emoji` 变量链断开 | art.test +2（drawHeart 两态、HUD 源码段零 emoji 且 token 接线） | `5a150d4` |
| 4 | W4R1-05 / learner R2-TOP7 | gold-hook `art.ts` drawOre | 体表补 2 停线性渐变（lit→fill 左上受光），与 `drawKitCoin` 全产品金币标准对齐；扫光/高光/描边/落影全保留 | `window4-visual-scan.test.ts` 钉住断言按修后状态取反收紧 | `51715cd` |
| 5 | W4R1-03 | duo-rush `art.ts` drawRunnerSprite | 双主角呆毛放大一档：P2 星呆毛 headR×0.32→0.5，P1 花苞轨道 0.16→0.24、花瓣 0.11→0.16、茎加粗——16px 像素网格下剪影主通道不再消失 | art.test +1（钉放大档比例） | `d22912c` |
| 6 | W4R1-04 + W4R1-07 | duo-vs-star `art.ts` drawCharTrait / drawGoldStar | duoduo 花瓣轨道 0.26→0.34 抬出轮廓、xingxing 星呆毛 0.34→0.5；drawGoldStar 补独立白高光第三层（对齐库内金星三层规格） | art.test +2（钉头饰比例、金星白高光层） | `9dbd0f8` |
| 7 | learner R2-TOP5（原 TOP8） | sprout-defense `index.ts` 子弹段 | 星弹平涂 `#ffd868` 换径向金渐变+深金描边（与 `drawClearStar` 三色同族）；花粉弹独立三瓣圆云分支，不再借星形 | art.test +1（源码段钉渐变+描边+独立分支）；区段定位修正另记 `37df120` | `279ea40` |
| 8 | learner R2-TOP3（原 TOP5） | gold-hook `art.ts`+`index.ts` 地表天空 | 新增 `drawSkyDecor`：暖章太阳/冷章月牙/蓝紫章三星按调色板查表 + 两朵慢云（6px/s，calm 定格） | art.test +2（章节主题物互异、云动/定格） | `96e69d8` |
| 9 | learner R2-TOP4（原 TOP6） | monster-crisis `art.ts` drawSky | 天空带补主题物：暖调太阳径向光晕 / 冷调月牙+四粒定格星按场景色调查表；云 4/7px/s 相位漂移，reduced 定格 | art.test +2（云漂移/定格、暖冷主题互异） | `e3fbb16` |
| 10 | learner R2-TOP6（原 TOP4） | duo-vs-star `art.ts`+`index.ts` 背景 | 新增 `drawMidgroundBand` 按 stageId 查表十张中景剪影带（云丘/远岛/厂房/棒棒糖/糖山/风车/冰脊/远塔/夜丘/城堡），sky[1] 加深 12% 单色静态，插在天空与装饰层之间 | art.test +1（十场地剪影带非空且互异） | `0146500` |
| 11 | learner R2-TOP9（原 TOP10）/ W4R2-04 | garden-guard `art.ts` drawMonsterSprite | 皇冠保留作 BOSS 通用标识；新增 `bossTrimOf` 按最近原型查表：铁壳系肩甲铆钉护板 / 疾风系三根后掠速度羽 / 浮云系身下云座 / 双生系双色虚线分裂环（每件 ≤3 path，配色本体色加深 30%）；17 位 BOSS 全覆盖，净版只剩大软软与自带回血光环的泥泥大王 | art.test +3（查表映射、五家剥色几何互异+皇冠保留、配饰几何细节） | `b8ca36c` |
| 12 | W4R2-01 / learner R2-TOP2（本轮新增） | duo-arena `index.ts` 事件飘字 | 8 处 `pushFloat` 字面量 emoji 前缀（🫧×3 💫 🎉 ❄️ ✨ 🌀）全部改纯文字——状态本身已有画面表达（盾圈/冰花/星光/转圈动画），按 learner 建议不另配图 | r2 扫描断言 8→0 取反 | `9d9b9e9` |
| 13 | W4R2-03（本轮新增） | duo-vs-star `art.ts` drawCharTrait jiujiu | 弧呆毛 0.22→0.34、线宽 0.1→0.14（16px 下 1.8px→2.7px），与放大后的 xingxing 星呆毛拉开——全花名册最难分一对（XOR 0%/7.2）双向修复 | r2 扫描断言取反 + art.test 补钉 2 条 | `8fa4f85` |
| 14 | W4R2-05（本轮新增，360px 玩法态补课） | duo-vs-star `index.ts` CSS `@media(max-width:380px)` | 触控键 `min-width` 38→40px 回到触区底线，改收 gap（6→4）保零溢出：360px 下 7 键 7×40+6×4=304px（+两侧内边距 316）<360，320px 下 304≤308 也放得下，再窄有 flex-wrap 兜底 | r2 扫描断言取反（钉 min-width:40 + gap:4） | `0c2c6da` |
| 15 | W4R2-06（建议） | sprout-defense `art.ts` drawBugBody 翅膀 | 飞虫翅膀锚点 0.9→1.15r 上移出身体轮廓、翅长 0.55→0.8r、calm 静态张角 0.5→0.8——16px 剪影多出翅膀通道，不再只靠悬浮影子分离兜底 | art.test +1（钉 calm 帧翅尖高过体顶） | `a60ffc4` |
| 16 | W4R2-07（建议） | gold-hook `art.ts` ORE_SKIN | 石头系（pebble/boulder）fill/lit/edge 整体压暗约 30/255，金块每档 lit ≥ 石 fill 明度 ×1.25（tester 给的公式），色弱 16px 金/石不再靠猜；石仍无彩度灰褐、金仍暖黄 | art.test +1（钉 4 金 ×2 石全组合明度比） | `1f1b17b` |

测试配套另有 `d38beb5`：A 档 `window4-visual-scan-r2.test.ts` 的 W4R2-01～05 现状钉住断言按其注释指示全部取反为修复态
（duo-rush 改钉 `hudTokens`/`drawHudToken` 无 emoji 无 `.emoji` 链；garden-guard 改钉 `bossTrimOf` 四配饰分支；
其余三条随对应修复 commit 同步取反）。A 档第 1 轮 31 例扫描与其余守护断言一字未删。

## 二、第 1 轮遗留处理结果（对照 r2 tester 对账表）

| 编号 | r2 tester 结论 | 本轮处理 |
| --- | --- | --- |
| W4R1-01 hud12 emoji | 合理遗留，learner 给出渐进换绘方案 | **已修**（#1）：冻结契约完整保留（`hud12.ts` 零改动、窄屏宽度测量测试原样全绿），绘制侧按 token 换手绘图标，不再降级交第 3 轮 |
| W4R1-02 双英雄 16px（严重） | 遗留待修，本轮 C 档必修项 | **已修**（#2）：帽形+下摆两条新剪影通道 |
| W4R1-03 duo-rush 16px | 合理遗留 | **已修**（#5）：呆毛放大一档 |
| W4R1-04 duo-vs-star 16px | 合理遗留 + 换样本挖出更差一对（W4R2-03） | **已修**（#6 + #13）：duoduo/xingxing/jiujiu 三位头饰全部放大 |
| W4R1-05 gold-hook 金块无渐变 | 合理遗留 | **已修**（#4） |
| W4R1-06 次要文案字号 9–13px（约 18 处） | 合理遗留：1.2 存量 DOM 样式、无溢出无遮挡、不升级 | **维持书面遗留**：均为 1.2 存量 DOM 字号取舍，tester 明示不升级，非画布资产问题；如第 3 轮升级再统一处理 |
| W4R1-07 金星无独立高光 | 合理遗留（建议级） | **已修**（#6） |

其余遗留口径照旧：DOM 文案层 emoji（candy-swing 徽章、duo-rush 帮助/按键/结算等）仍属容忍约定，tester 未升级为阻断，本轮不动玩法 DOM。

## 三、本轮新发现（W4R2-01～07）处理结果

一般 5 条（W4R2-01～05）**全部修复**（#12/#3/#13/#11/#14），建议 2 条（W4R2-06/07）**也已修复**（#15/#16）——
成本均为极低/低，顺手清零，A 档 r2 报告的问题清单本轮全部关闭。

**唯一书面顺延**：learner R2-TOP10（sling-birds 结算星级仪式，原 TOP9，严重度低 / 成本中高）。
理由：需要给结算 DOM 面板加逐只飞入+逐颗点亮的时序演出，改动面在结算流程编排而非纯画笔，
与其低优先级不成比例；两轮 tester 均未立项（其五项走查中 sling-birds 动效链√、结算面板功能完好）。
交第 3 轮列专项，learner 的 0.3s 间隔 CSS transition 方案与 reduceMotion 直亮口径可直接沿用。

## 四、一致性改动摘要

- **金币/金星/金块口径归一**：gold-hook 金块 2 停渐变（#4）、sprout-defense 星弹金渐变+深金描边（#7）、
  duo-vs-star 金星补白高光（#6）——全窗「金物=渐变+深金描边+独立高光」现与 duo-arena `drawKitCoin`、
  candy-swing 金星、garden-guard `drawGoldStar` 同族。
- **画布 emoji 清零收口**：duo-rush HUD（#3）、duo-arena 事件飘字（#12）、garden-guard 战内 HUD（#1）
  三处两轮扫描盲区（变量链/字面量前缀/冻结契约段）全部清掉；本窗 9 款画布 fillText 无 emoji
  仅剩 hud12 契约字符串本体（不上画布，只用于宽度测量）。
- **16px 剪影通道补齐**：monster-crisis 双英雄（#2）、duo-rush 双主角（#5）、duo-vs-star 三位（#6/#13）、
  garden-guard 17 位 BOSS（#11）、sprout-defense 飞虫（#15）、gold-hook 金石明度（#16）——
  tester 量化点名的 XOR 0% 对全部拿到新形状或新明度通道。
- **背景密度短板三款补层**：gold-hook 地表天空（#8）、monster-crisis 天空（#9）、duo-vs-star 中景带（#10），
  与 learner 的密度账对齐：9 款背景层差额全部归零；新增动效全部带 reduced/calm 定格分支。
- **触区**：duo-vs-star 380px 档回到 ≥40px 底线且零溢出（#14），tester 的 360/320 双档宽度账写进注释与断言。

性能：本轮新增均为每帧 ≤10 条 path 的静态/慢速装饰，无新建离屏画布、无每帧渐变对象分配热点
（云/主题物渐变按帧构造与库内既有画法同口径）；tester 本轮 9 款 4×CPU 节流全部 60fps 的基线未受影响（收尾未重测帧率，无数字更新）。

## 五、合并态复验

- 收尾 `git fetch` → `rebase origin/game-1.3-window4` 两次（`e12f109` 与 `d8cd710`），均干净无冲突；
  三份 round2 报告（A `d8cd710` 正式版 / B `c1adbb0`+`40b1d77` / C 本报告）与 round1 三份、
  `docs/plan-1.3-*.md`、skills、learner 的 guide 文案改动全部保留，他人报告一字未改。
- `npm ci`：绿。
- `npm test`（合并态，含本轮全部修复与三方新增用例）：**710 文件 / 15280 例全绿**
  （round1 合并态 709/15247 → +1 文件/+33 例：r2 learner +3、r2 tester +9、本轮修复配套 +21，只增不减 ✅）。
- `npm run build`：绿（tsc --noEmit + vite；**PWA precache 188 entries / 5246.26 KiB**，
  较 tester 水位 5237.48 KiB +8.78 KiB，为本轮新增绘制代码的正常增量）。
- `npx vite preview :4173` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200** ✅。
- 合并态代码终态 SHA：**`1f1b17b`**（本报告 commit 紧随其后）。

## 六、第 2 轮结论

- 第 1 轮遗留 7 条：6 修复 + 1 维持书面遗留（W4R1-06，tester 明示不升级）；**严重级 W4R1-02 清零**。
- 本轮新发现 7 条（一般 5 + 建议 2）：**全部修复**；A 档 r2 钉住断言全部按其注释取反为修复态。
- B 档 R2-TOP1～TOP9 全部落地，TOP10（sling-birds 结算仪式）书面顺延第 3 轮（低优先/中高成本，理由见第三节）。
- 修出回归 0：第 1 轮 31 例扫描、r1 修复回归守护、判定热区/冻结契约断言全程全绿；
  hud12 窄屏宽度测量、duo-rush squeeze、`HERO_R`、gold-hook 钩角数学等判定口径零改动。
- 红线自查：未 force、未碰 main / game-1.2 / game-1.2-kk、未开 PR、未改他人报告、无 three.js、
  无跨窗改动（`src/games/` 下仅本窗 9 款 + 本窗测试目录有 diff）。
