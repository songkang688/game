# 1.3 窗口 3 · 第 3 轮视觉验收（终验）· 测试员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 3 轮（终验）· A 测试员。
> 分支：`game-1.3-window3`（第 2 轮合并态 `4f68090` 之上）。只验视觉，不重开玩法验收，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step29-A-tester.md`（思路）、`docs/qa/1.3-window3-round2-{tester,fixer,learner}.md`、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、终验计划（动手前提交）

本轮是窗口 3 视觉升级的最后一轮验收，只回答一个问题：**这套视觉能不能放行。**

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数与第 2 轮合并态（`4f68090`：747 文件 / 15438 例）差值，只增不减。
2. **对账清零**（前置）：round2-tester 的 N-01~N-04 × round2-fixer 的 9 笔修复 + 登记遗留 3 条逐条对账——每条给三种结论之一：已修复（附证据）/ 合理降级（引 C 档理由）/ 仍未修（升级为终验阻断项）。round2-learner 报告只读对照。
3. 机器化扫描用例复核：前两轮沉淀的 `visual-scan`（27 例）/ `visual-fix-r1`（46 例）/ `visual-deep-r2`（27 例）/ `visual-fix-r2`（60 例）/ `learner-copy`（27 例）全部还在跑、全绿；被删或被 skip 的按阻断处理。
4. **本窗终扫**：9 款 × 六大专项（火柴人 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px 布局 / 商标），不抽样，逐格给「达标 / 不达标」二值结论，产出 9 行 × 6 列终验总表。
5. 回归防线（终验加严）：热区钉死断言、reduced 功能提示断言、教育数据快照断言全绿；9 款 CPU 4× 节流粒子高峰 ≥30fps（重点 ocean-munch 余量）；`vite preview` 冒烟（`/` `/sw.js` `/manifest.webmanifest` 均 200 + 随机 5 款各 30 秒）。
6. 新增第 3 轮机器化终验用例（仅 9 款 `*.test.ts`），沉淀终验口径。
7. **终验结论**：放行 / 带条件放行（列条件）/ 不放行（列理由）三选一。
8. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window3`。

红线重申：测试员只写报告与补测试用例，不改绘制代码；能改的文件限于本窗 9 款 `*.test.ts` 与本报告；不碰别人的报告文件；不改 `level99.ts` 共享外壳；禁止 force push；不动 main / game-1.2 / 1.2-kk / game-1.3 主线。

（以下各节待终验完成后回填。）
