# 1.3 窗口 2 · 第 3 轮（终验） · 监督修复员报告

> 角色：窗口 2 第 3 轮终验修复员（C 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货态：`7fbbdbe`（包工头收 r3 tester 派单；A 档终验报告 `f0d2ef9`：放行、54/54、新问题 0、阻断/严重残留 0、合理降级 2）。
> 范围：只修本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 红线：只动绘制层（绘制函数 / CSS 末尾追加 / 视觉常量）。判定、数值、关卡、存档 key、`parentAuth`、玩法断言一律不碰。用例只增不减、不删既有 `it()`、不 skip。终验轮不引入新风格、不引入透视。

## 清零计划（动手前登记，`0506dae`）

输入：A 档 `docs/qa/1.3-window2-round3-tester.md`（`f0d2ef9`）已读——终验结论放行、9×6 总表 54/54、终验新问题 0、r2 对账 14 行（已修复/已验证 12 · 合理降级 2 · 仍未修 0）；r2 fixer 遗留清单（`docs/qa/1.3-window2-round2-fixer.md` 第二节）已读——移交本轮 1 条（dark-chess textContent 测试桩）。B 档 r3 报告与 `docs/qa/1.3-window2-style-baseline.md` 开工时尚未落地（`docs/qa/` 下无对应文件），先按 A 档 + 自查修，B 档落地后 rebase 跟进一致性建议，不空等。

顺序：A 档终验总表不达标格（本轮 0 格——读码复核若发现回归仍修）→ 第 2 轮遗留仍影响玩家项 → B 档点名（待落地）→ 一般 → 建议。每修一处一个 commit（`fix(visual-r3): <款> <问题>`）。

| 序 | 级别·来源 | 款 | 项 | 预定处置 |
| --- | --- | --- | --- | --- |
| 1 | 遗留（r1 遗留 1 → r2 fixer 移交 → A 档 r3 对账 #12 合理降级） | dark-chess | `setCellContent` 的 `textContent` 赋值总被 `innerHTML` 覆盖（测试桩契约） | **评估后本轮清零**：读码确认 `view.ts` `refresh()` 已给全部 32 格按三态设真读屏 `aria-label`（「第 X 行第 Y 列 空格 / 还盖着 / 红|蓝+兵种」，view.ts:418/421/425–428），桩层零渲染零读屏价值；既有消费断言仅 2 处（`art.test.ts:73`、`smoke.test.ts:122`，均为视觉文本层断言，非玩法断言）。修法：删 `setCellContent` 内 3 处死写 + 同步注释，两处断言换 aria-label 口径（it() 数不变），新增 round3-fix 契约钉住修后状态（桩层不复写 + aria-label 三态口径），满足 A 档「换 aria-label 且用例只增不减」的清零前置。若全量跑发现波及玩法断言立即回退、维持遗留写 backlog |
| 2 | 自查 | 9 款 | A 档 54/54 复核 + kit/styles.css/商标红线 | `src/art/` 不存在、`src/styles.css` 相对 `origin/game-1.3` 仅末尾追加、商标黑名单 0 命中（r3 终验契约在跑佐证）；发现回归才立案 |
| 3 | 登记 | — | `docs/qa/1.3-window2-backlog.md`（本窗专用） | 成文：未修项 + 降级项去向（来源轮次 / 位置 / 期望 / 改动面） |

---

## 一、清零清单（本轮 1 处修复，全部落地）

| # | 来源 | 款·位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | r1 遗留 1 → r2 fixer 第二节移交 → A 档 r3 对账 #12（合理降级、登记为收官后清理项） | dark-chess `view.ts` `setCellContent`（:346–367）+ 文件头注释 | **textContent 死桩清零，文字口径统一走 aria-label**：删 `setCellContent` 三态里的 3 处 `b.textContent` 死写（真实 DOM 中总被紧随的 `b.innerHTML` 整体抹掉，零渲染零读屏价值）；读屏与测试契约由 `refresh()` 每轮逐格重设的 `aria-label` 独家承担（该口径 1.3 起就在：空格「第 X 行第 Y 列 空格」/ 盖着「…还盖着」/ 翻开「…红|蓝+兵种」，本轮零新增语义）。绘制资产、SVG 面板层、钥匙缓存、动画时序一字未动；判定/数值/存档/parentAuth 零触碰 | ① 既有断言换口径 2 处、**it() 数不变**：`art.test.ts:73`（textContent="🌸" → aria-label 含「还盖着」+ 桩层恒空）、`smoke.test.ts:122`（同口径）；② 新增 `round3-fix.test.ts` **1 文件 / 4 例**钉修后状态：aria-label 三态口径逐字钉死、三态桩层恒空、翻子后 aria-label 换兵种名 + SVG 换脸 + 桩层仍空、源码契约（view.ts 无「🌸」桩字符、格子层无 `b.textContent` 写入）；③ dark-chess 全目录 12 文件 / 133 例定向跑全绿 | `166f926` |

**A 档终验总表 54/54 复核**：未发现回归，0 格需修。**B 档 r3**：截至收官仅落计划（`c1874d6`）与文案快照用例（`c16847a`，+4 例，不涉视觉点名）；终版报告与 style-baseline 未到，无点名项可跟进——收官后若 B 点名仍违和，按 backlog 流程交下一清理轮。

**自查（红线逐条）**：`src/art/` 不存在；`src/styles.css` 相对 `origin/game-1.3` **0 删改行**（纯末尾追加）；9 款非测试源码商标黑名单（4399/马里奥/皮卡丘/托马斯/Kitty/Battle City/宝可梦等）**0 命中**；透视关键字（perspective/rotate3d/matrix3d/translateZ/three.js）**0 命中**（r3 终验契约 27 例同口径在跑）；本轮改动不含任何 font-size、不新增动效、不引入新风格。

## 二、降级记录

| # | 项 | 终态处置 |
| --- | --- | --- |
| 1 | A 档 r3 对账 #12：dark-chess textContent 测试桩（r1 遗留 1，原「合理降级·维持遗留」） | **本轮清零**（`166f926`，见第一节），降级记录关闭，不再入 backlog |
| 2 | A 档 r3 对账 #10：merge-2048「方块静饰改圆点」（B 档 r2 #7） | **维持有据不采纳**（r2 fixer 核实 `.mg-wrap::before/::after` 为 clip-path 十顶点五角星，前提不成立；A 档 r3 复核维持）。非缺陷、闭环，登记进 backlog「不入册」节防重复立案 |

**终态书面降级：1 条（建议级、有据不采纳、零玩家影响）。阻断 / 严重终态 = 0。**

## 三、backlog（`docs/qa/1.3-window2-backlog.md`，`e0defe8` 建档）

**待清理 3 条**（全部建议级 / 登记跟踪，无一般级以上）：① dark-chess 记牌面板 span 双写模式（textContent+aria-label+innerHTML 同款家族，读码自查发现，A 档本轮双断言验证过该面板故终验轮不扩面动）；② merge-2048 badge 圆丸可选案（r2 learner #7 可选、r2 fixer #14 登记，回归面大于收益）；③ `l99-star` 12px 装饰字符豁免跟踪（共用件不在本窗 9 款目录，跨窗统筹）。另「不入册」裁定 4 条防复查重复立案（mg 星形角饰、cg-coord/cg-piece-tag 核准口径、cg 360 格宽 1px 取整备注、已清零的 textContent 桩）。

## 四、终态合并数据（规格第六节流程）

- 流程：fetch → rebase（`166f926` 后收 B 档 `c1874d6`/`c16847a` 两 commit，rebase 干净零冲突；他人文件——A/B 报告、`docs/plan-1.3-*.md`、`.cursor/skills/**`、`docs/qa/1.3-window2-plan.md`——一字未动）→ 全量门禁 → 普通 push（未用 force，一次 push 被拒即按 fetch+rebase 重推）。
- **终态验证 SHA：`e0defe8`**（本报告收尾 commit 落于其上）。
- `npm ci`：绿（node 22.14.0 / npm 10.9.7）。
- `npm test`：✅ **748 文件 / 15320 例全绿**（179.2s）。对账：r3 tester 收尾 746/15312 → +1 文件/+4 例（本档 round3-fix）→ +1 文件/+4 例（B 档 copy-r3）= **748/15320，只增不减成立**；三轮全部机器化用例（round1/round2/round3 契约 49 文件）零删零 skip 在跑。
- `npm run build`：✅ 全绿（`tsc --noEmit` 单独复跑亦干净 + vite build）；PWA precache **188 entries / 5215.34 KiB**（对 r2 门禁 5215.41 KiB 微降 0.07 KiB，为删桩死码所致，entries 数不变）。
- `npx vite preview` 冒烟：✅ `/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**；本窗 9 款深链（`#/game/<id>`）**360×640 与 320×568 两档各进一局第 1 关盘面：18/18 通过——0 pageerror、0 横向溢出、盘面标志节点全部在位**（mg-board/mn-cell/sp-cell/jq-cell/cg-sq/dc-cell/canvas×3）；截图逐张目检**无视觉崩坏**（dark-chess 320 八列全在木框内零互叠、chess-garden 320 h 列完整可见、fruit-stack 粉白壳卡+警戒线正常、junqi 帐篷地形与旗数正常、dot-maze 呆毛主角与豆点正常、pool-stars 台面与出杆线正常、merge-2048 色阶+星角标正常、sudoku-petal 木框花田正常、mine-garden 苗床正常）。探针脚本与截图只落 `/tmp`，未入库；puppeteer-core 为 `npm i --no-save` 临时驱动，未入库。

## 五、窗口 2 · 1.3 视觉收官声明（仅本窗 9 款）

> **本窗 9 款（merge-2048 / mine-garden / sudoku-petal / dot-maze / fruit-stack / pool-stars / junqi-camp / chess-garden / dark-chess）1.3 视觉升级三轮收官：终态可放行。**
>
> - 终态验证 SHA `e0defe8`（收官报告 commit 在其上）；`npm test` 748 文件 / 15320 例全绿、`npm run build` 全绿（PWA 188 entries / 5215.34 KiB）、preview 冒烟 18/18 绿；
> - 三轮问题账全平：r1 立案 8 条、r2 立案 7 条全部修复或有据闭环；r1/r2 全部遗留清零（含本轮清掉的最后 1 条 textContent 桩）；**阻断 / 严重终态 0**；书面降级 1 条（建议级有据不采纳）；A 档 9×6 终验总表 54/54；
> - backlog 3 条（全部建议级 / 跨窗登记跟踪），成文于 `docs/qa/1.3-window2-backlog.md`；
> - 本声明**只覆盖本窗 9 款**，不替全库 1.3 放行、不覆盖他窗款；分支只推 `game-1.3-window2`，**不合并进 `game-1.3` / `main`**。B 档终版报告（采纳率终账 / style-baseline / 商标终审 / 首页终评）截至收官未落地，其后续若点名仍违和，按 backlog 流程接力，不影响本声明范围内结论。

## 六、验收对照（规格第八节）

- `npm test` 全绿只增不减 ✅（748/15320，较 746/15312 净增 8 例、零删零 skip）；`npm run build` 全绿 ✅；preview 冒烟绿 ✅；
- 本窗 9×6 专项维持达标（A 档 54/54，本轮读码复核零回归）✅；阻断/严重终态 0 ✅；书面降级 1 条 ✅；
- backlog 成文 ✅、收官声明成文 ✅；
- 分级红线：无血/无伤害/无死亡描写、失败只鼓励、无广告内购、无商标与官方角色——本轮改动为删死码与断言口径切换，零文案零画面语义变化，红线无涉 ✅。
