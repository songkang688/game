# 1.3 窗口 1 · 本窗三轮验收 · 第 3 轮（收官）· 监督修复员报告

> 角色：窗口 1 第 3 轮监督修复员（C 档，收官轮）。落地 A 档（r3 tester）与 B 档（r3 learner）
> 清单中的本窗可动手项（本轮两档均明示 **0 条**），执行自查底线与合并态复验，出具收官验收结论。
> 只动绘制层（若有修）；判定、数值、关卡数据、存档 key、玩法测试断言一律不碰。
> 输入：`docs/qa/1.3-window1-round3-tester.md`（阻断 0 / 严重 0 / 一般 R3-1 = R2-1 复登记 / 建议 0 新增；
> 72 格全 ✅；728 文件 / 15591 例；代码基线 `1b77c15`）、
> `docs/qa/1.3-window1-round3-learner.md`（「收官：本窗绘制无待办」；12/12 达标；5 项跨窗转交）。
> 依据：`docs/plan-1.3-visual-bible.md`（视觉宪法）、`docs/qa/1.3-window1-plan.md`（十二格登记）、
> round1 / round2 六份报告（只读，遗留基线）。
> 收官纪律：**禁止为交差而发明绘制改动**；唯一允许动手 = 自查发现本窗新阻断（商标 / 360 新溢出）
> 或新严重（裸 emoji 主角等），若有当轮清零、一处一 commit（`fix(visual-r3): …`）。

## 0. 修复计划（开工承诺）

修复顺序铁律：阻断（本轮输入 0 条，自查复核）→ 严重（0 条，自查复核）→ 一般 → 建议。

1. **本轮输入可动手项 = 0 条**：r3 tester 交 C 档严重级清单为空、无新增可动手项；r3 learner 明写
   「收官：本窗绘制无待办」，且明令「不要为『显得有活干』往达标格加装饰（2B/4B/4C 密度余量为 0，
   多加即过载）」。本轮不发明改动——若自查底线亦无新阻断 / 严重，修复清单即为「本轮 0 条落地」。
2. **禁修项恪守**：R3-1（= R2-1 = L-1）`level99.ts` 星级——共享壳 owner / 统筹轮，本窗禁改此文件；
   L-3 kit `decor.ts`——1A owner 独占，不动 kit；L-4 `bomb-buddies`——他窗游戏零触碰；
   L-5 文案层 emoji——观察项不抬级不大改；R2-3——零代码已结。豁免 G-2 / G-3 / G-4 不碰热区。
3. **自查底线（第三节，收缩到本窗，必须做）**：① 商标 grep（九款 + kit + motion + runner 非测试源码，
   30 词黑名单）；② 本窗 `fillText` emoji 直出扫描；③ 360/320 新溢出核查；④ reduced-motion 抽查；
   ⑤ kit 纪律 `git log --diff-filter=M`。若发现本窗新阻断 / 严重，当轮清零，一处一 commit
   （`fix(visual-r3): <款> <问题>`），配套用例只增不减。
4. **水位纪律**：进场水位 **728 文件 / 15591 例**（r3 learner 交卷 = r3 tester 交卷持平，
   代码基线 `1b77c15`）；零绘制改动则水位持平即「只增不减」成立。
5. **收尾合并态复验（第五节，必须执行）**：`git fetch origin game-1.3-window1` → rebase（保留他人文件）→
   确认 r3 tester / learner 报告在位 → `npx vitest run` → `npx tsc --noEmit` → `npm run build` →
   preview 冒烟；合并态 SHA、用例总数、构建体积写进本报告，给「第 3 轮（收官）视觉验收结论」
   （阻断清零、严重残留、遗留清单、本窗 12 格是否收官通过）。普通 push `origin game-1.3-window1`，
   禁 force、不动 `main` / `game-1.2` / `game-1.2-kk`、不用 `gh` 开 PR；别人的报告一字不碰。

（修复清单、遗留清单、自查结论、合并态复验数据见后续章节，随执行逐条回填。）

---

## 一、修复清单：本轮 0 条落地

修复统计：**阻断 0 条（无输入，自查亦未发现）；严重 0 条（无输入，自查亦未发现）；
一般 0 条落地（唯一一条 R3-1 为跨窗禁改文件，见遗留清单）；建议 0 条（本轮无新增输入）。**

- r3 tester 交 C 档严重级清单为**空**，问题清单仅 R3-1（= R2-1 遗留复登记，共享壳 `level99.ts`，
  任务书明令本窗不抢）；r3 learner 明写「**收官：本窗绘制无待办**」（12 格 12 达标，0 格待改）。
- 本轮自查底线五项（第三节）**亦未发现任何本窗新阻断 / 新严重**——按收官纪律不发明绘制改动，
  不往密度余量为 0 的达标格（2B/4B/4C 等）加装饰。
- 本轮代码 diff：**`src/` 零触碰**（唯一改动 = 本报告 docs 文件）。零绘制改动下测试水位
  728 文件 / 15591 例持平，「只增不减」成立（任务书明示口径）。

**修复红线自查**：绘制 / 判定 / 数值 / 关卡数据 / 存档 key / 玩法测试断言全部 0 diff；
`parentAuth.ts` / `parentGate.ts` 零触碰；无 three.js；未动 `src/art/kit/`、`src/games/level99.ts`、
他窗游戏（含 `bomb-buddies`）；他人报告一字未碰；未做全局第 27–29 步。

---

## 二、遗留清单与去向（5 项全部有主，交统筹 / 对应 owner）

| 项 | 内容（本轮复核现状） | 理由与去向 |
| --- | --- | --- |
| R3-1（= R2-1 = L-1 = r1 G-1，一般级） | 共享壳 `src/games/level99.ts:512` `.l99-node-stars{font-size:11px}`、`:561` @media 降 10px，`span.l99-star` 计算样式 10–12px，九款关卡页同源受累（r3 tester 18 组实测唯一 <14px 命中） | 共享关卡壳属壳层 owner / 统筹轮，跨窗文件本窗三轮任务书均明令禁改。修法 r1 learner P7 已留档（★ 字符改 12×12 内联 SVG 星形，双态 fill `#ffb937`/`#e3ddef`，连带 `.l99-beststars` / `.l99-ov-stars` 一次改净）。**转交壳层 owner / 统筹轮**；严重度维持「一般」（1.2 逐字遗留非 1.3 回归） |
| L-3 | `src/art/kit/` 无 `decor.ts` 中景三件套（软云 / 花草簇 / 星簇，Canvas + SVG 双出口）供给缺口；本轮 `ls` 复核仍为 6 源码 + 6 测试 | kit 为 1A owner 独占（本轮任务书明令 C 档禁改 kit）；本窗九款需求已按「各游戏内自绘」满足（r1 P2/P3/P6/P9），无受害面。**维持登记 kit owner 待办**，后续窗口出现同类中景需求时先补供给再开工 |
| L-4 | `bomb-buddies/index.ts:102` `P_EMOJI = ["🌸","⭐"]` 裸 emoji 座位标（`:423/:562` 引用） | 他窗游戏，本轮任务书明令禁改。**维持转交对应窗口 fixer**；r2 fixer 已留全库 `P_EMOJI` 登记清单（bomb-buddies / pool-stars / bumper-cars / bowling-lane / monster-crisis / fruit-stack，均他窗）可直接取用 |
| L-5 | 文案层 emoji（combo-clash 选人卡 `<em>${c.emoji}</em>` `index.ts:997`、star-estate 地契列表行首 `${sel.emoji}` `index.ts:730` 等） | 维持「文案层可留」口径，观察项不抬级、不大改（任务书明令）；若全局轮（27–29）要求文案层画形，工作量估算已在 r1 fixer L-5 留档。**维持观察，本窗不动** |
| R2-3 | r1 fixer 报告笔误 `.mj-table`，选择器真名 **`.mj-board`**（`mahjong-bloom/index.ts:102`） | 零代码项，r2/r3 与本报告全程用真名，**处置完毕**，后续轮次文档引用照此 |

豁免项 G-2（`mj-tile` 30×42）/ G-3（`se-tile` 23×23）/ G-4（`fc-token` 19×19）维持豁免，
本轮未碰任何热区（任务书明令）。

---

## 三、自查修复结论（底线检查，收缩到本窗，全部实测）

0. **零漂移前提**：`git diff --stat 1b77c15..HEAD -- src/` 为**空**——r3 tester 代码基线 `1b77c15`
   之后仓库仅有 docs-only 提交（`ebd9985` / `8cefc59` / `dcc8e3b` / `a17cc5a` / `aa76726` + 本轮计划
   `35b4421`），本窗 12 格源码与 A/B 两档验证过的状态逐字节相同。
1. **商标扫描**：对本窗九款 + `src/art/kit` + `src/art/runner` + `src/ui/motion.ts` 的**非测试源码**
   全量 `rg -i` 黑名单 30 词（4399 / 任天堂 / 迪士尼 / 马里奥 / 皮卡丘 / 宝可梦 / Hello Kitty / 托马斯 /
   Bomberman / Tetris / 俄罗斯方块 / 球球大作战 / 贪吃蛇大作战 / slither / agar / Monopoly / 大富翁 /
   三国杀 / 雀魂 / 野狐 / 蛋仔 / 地铁跑酷 / 超级玛丽 / Nintendo / Disney / Mario / Pokemon / Pikachu /
   Subway Surf / Kirby）：**0 命中**（含注释）。**阻断 0**。
2. **裸 emoji 主角（`fillText` 直出）**：本窗 12 格非测试源码逐行扫描——`fillText(` 共 15 行
   （orb 3 / snake 4 / block 1 / combo 4 / weiqi 1 / kit 2，其余 0），**携带 emoji 的 0 行**；
   `\p{Extended_Pictographic}` 码位分格计数 41/38/47/45/57/166/121/41/49（九款序同 r3 tester 表）、
   kit/runner/motion 全 0，与 r3 tester 口径**逐字持平**——收官后无任何人新增装饰性 emoji。**严重 0**。
3. **360/320 溢出**：合并态产物 `vite preview` + Chrome headless 148（CDP，
   `Emulation.setDeviceMetricsOverride`）实开九款 × 360×640 / 320×640 共 **18 组**，每组
   「关卡页初测 → 点『开始冒险』进对局 → 复测」：文档级 `scrollWidth − innerWidth` **36 次量测全 0**；
   主场景渲染正常（canvas 3/1/3/5/…、svg 15/115/19/22，与 r3 tester 逐字一致）；
   **18 组 console error / pageerror 全 0**。**新增溢出 0，阻断 0**。
4. **reduced-motion 抽查**：CDP `prefers-reduced-motion: reduce` 实开 flight-chess / star-estate /
   combo-clash / mahjong-bloom 进对局：**运行中无限循环动画（`document.getAnimations()`
   iterations=∞ 且 running）4 款全 0 个**；对局界面可见功能按钮 19 / 53 / 13 / 24 个，功能未被削减；
   页面错误 0。✅
5. **kit 纪律**：`git log --diff-filter=M origin/game-1.3..HEAD -- src/art/kit/ src/ui/motion.ts
   src/art/runner/`——**0 条**；全部历史仍只有三条 owner 创建提交（`9c345d4` / `dbb8bd4` / `6dea9e6`）。
   三轮验收全程无人修改过基建文件；本轮 C 档对 kit 零触碰（连 import 都未新增）。✅

**自查结论：本窗新阻断 0、新严重 0——「修复清单 0 条落地」成立，无需动手项。**

---

## 四、kit 纪律检查结论

**通过**。`src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` 自基线 `origin/game-1.3` 以来
无任何修改型提交（`--diff-filter=M` 空），仅三条 1.3 第 1 步 owner 创建 commit；
`src/art/kit/` 文件清单复核 = palette / chars / props / fx / testing / index 共 6 源码 + 6 测试，
无 `decor.ts`（= L-3 现状）。本轮未新增、未修改任何 kit 文件，未复制粘贴 kit 代码。

---

## 五、合并态复验（由 C 档执行，全部本轮实跑）

1. **git**：`git fetch origin game-1.3-window1` → rebase（origin 无新提交，rebase no-op）→ 确认
   三轮八份报告（round1 / round2 各三份 + round3 tester / learner）、`docs/plan-1.3-visual-bible.md`、
   `docs/qa/1.3-window1-plan.md`、十二格产物全部在位，他人文件零改动。推送方式：普通 push，
   无 force，未动 `main` / `game-1.2` / `game-1.2-kk`，未用 `gh` 开 PR。
2. **全量验证（合并态，代码 = `1b77c15` 同源）**：
   - `npx vitest run`：**728 文件 / 15591 例全绿**——与 r3 tester / learner 交卷水位**逐字持平**
     （本轮零绘制改动，持平即「只增不减」成立 ✅）；
   - `npx tsc --noEmit`：**0 错误** ✅；
   - `npm run build`（tsc + vite build + PWA）：**全绿** ✅，主包 `dist/assets/index-CL9tGIe2.js`
     **110.70 kB（gzip 38.67 kB）**，PWA precache **188 项 / 5291.55 KiB**，`sw.js` /
     `manifest.webmanifest` 正常生成——三项与 r2 fixer / r3 tester 登记逐字持平；
   - `npx vite preview` 冒烟：`GET /` 200；18 组实开数据见第三节第 3 条（溢出全 0 / 错误全 0 /
     主场景计数与 r3 tester 逐字一致）；另运行时取证 R2-2：`getComputedStyle(.hc-table).backgroundImage`
     第一层实测 `repeating-linear-gradient(45deg, rgba(170, 120, 80, 0.04) 0px, … 6px, rgba(0,0,0,0) 6px,
     … 12px)`，织纹在位且为多重背景第一层、alpha 恰 4%、纸色渐变未动——**R2-2 收官无回退**。
3. **合并态代码 SHA：`1b77c15`**（r3 tester 用例 commit，= 当前源码态最后一个代码 commit；
   其后至本报告全部为 docs-only 提交，全绿结论对推送 tip 同样成立，最终 tip 见收尾登记）。

---

## 六、窗口 1 · 第 3 轮（收官）视觉验收结论

- **阻断级：0 残留**（A 档输入 0；C 档自查商标 0 命中、360/320 新溢出 0，当轮保持清零 ✅）；
- **严重级：0 残留**（A 档输入 0；C 档自查裸 emoji 主角 0，亦未发现 ✅）；
- **一般级**：1 条 = R3-1（共享壳星级，跨窗禁改文件）——按纪律不抢，遗留清单转交壳层 owner / 统筹轮；
- **建议级**：0 条新增；历史建议 R2-2 已落地无回退、R2-3 零代码处置完毕；
- **遗留清单**：5 项（R3-1 / L-3 / L-4 / L-5 / R2-3）全部有主有去向（第二节）；
- **测试水位**：728 文件 / 15591 例全绿（合并态复跑），零绘制改动持平，只增不减成立；
- **本窗 12 格收官判定**：r3 tester 六专项 72 格全 ✅ + r3 learner 12 格 12 达标 + 本轮 C 档自查底线
  五项全过 + 合并态三验全绿——**本窗 12 格视为收官通过**。

**结论：窗口 1 第 3 轮（收官）视觉验收通过（阻断 0 / 严重 0 / 一般 1 条按纪律移交 /
本窗绘制待办清零）。窗口 1 的 1.3 视觉升级三轮验收至此收官。**

**收尾复验登记**：本报告 commit 之上再次执行 `git fetch origin game-1.3-window1` → rebase →
`npx vitest run` / `npx tsc --noEmit` / `npm run build` 三验全绿后普通 push `origin game-1.3-window1`
（无 force、未动 `main` / `game-1.2` / `game-1.2-kk`、未用 `gh` 开 PR）。本登记为 docs-only commit，
三验结论对推送 tip 同样成立。

本轮实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
