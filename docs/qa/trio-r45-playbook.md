# r45 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-195**、**N-196**、**N-197**。下一空号 **N-198**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r44 学习 PR 未合主干时，本轮仍从 N-195 起编；若主干或先合 PR 已占用 N-195+ / 已修 `.shr-back` / `.l99-continue`，本号销或顺延，并在笔记勘误。

---

## 红线（抄自 r22–r44 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r45-learn-notes.md` §0。尤其 **不要**再开 `.shr-toggle`（N-134 的 36）、`.pk-go`（N-173）、`.ba-lv`（N-192）、`.oa-back`（N-135）、`.l99-tool`（N-138）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#117** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 sticky。

---

## 本轮工单

### N-195 · B · `.shr-back`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/shoot-range/index.ts` ≈L190；挂载 ≈L1377 |
| 现状 | 选关返回，`padding:8px 13px`，估高 ≈32.8 |
| 不做 | 不改 `.shr-toggle`（保持 36）/ `.shr-mode`（已 44）/ 靶纸 |
| 验收 | 返回钮 ≥44；点了仍回选关 |

### N-196 · B · `.l99-continue`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/level99.ts` ≈L561；挂载 ≈L885 |
| 现状 | 地图「继续」，`padding:8px 16px`，估高 ≈34 |
| 不做 | 不改 `.l99-back`/tool/tab/ov-btn（N-138）/ 跳关输入（已 44）/ 关卡表 |
| 验收 | 继续钮 ≥44；点了仍续关 |

### N-197 · A · `*-continue` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-continue` 可点块必须 `min-height`≥44 |
| 现状红 | 仅 `.l99-continue` |
| 不做 | 不替代 N-138/196 |

---

## 建议顺序

1. A 先合 **N-197**（或与 B 并行）。
2. B 修 **N-195**（shoot-range）、**N-196**（level99），分 PR。
3. 回归：N-134 toggle 仍 36、N-138 其余壳层、N-173 `.pk-go`、N-192 地图格、N-189 vs 结算。bowling `.bl-btn` 仍走 N-145。若 N-134/138 后合，对账勿双修同一选择器。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-195/196 的点击矩形，不能只看 CSS 行。
