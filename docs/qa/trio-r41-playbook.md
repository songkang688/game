# r41 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-183**、**N-184**、**N-185**。下一空号 **N-186**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r40 学习 PR 未合主干时，本轮仍从 N-183 起编；若主干或先合 PR 已占用 N-183+ / 已修 `.pcp-act` / `.hh-catch`，本号销或顺延，并在笔记勘误。

---

## 红线（抄自 r22–r40 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r41-learn-notes.md` §0。尤其 **不要**再开 `.dvs-pick`（N-94）、`.sks-mode`（N-149）、`.sn-open`（N-180）、`.bl-btn`（N-145）、`.bvp-btn`（N-150）、`*-veil-btn`（N-152）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#113** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 sticky。

---

## 本轮工单

### N-183 · B · `.pcp-act`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/prince-princess/index.ts` ≈L256；挂载 ≈L2212 |
| 现状 | 结算 overlay `<button>`，`padding:9px 18px`，无 min-height |
| 不做 | 不改 `.pcp-btn`（已 44）/ `.pcp-veil-btn`（N-152）/ `.pzt-eye`（主干已 44）/ `--k`（N-154） |
| 验收 | 结算钮高 ≥44；点了仍进再来或回选 |

### N-184 · B · `.hh-catch`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/hue-hand/index.ts` ≈L183 |
| 现状 | 接牌 CTA，`padding:4px 9px`，无 min-height |
| 不做 | 不改 `.dvs-pick`（N-94）/ `.hh-deck` 牌堆 / `.hh-back*` |
| 验收 | 接牌钮 ≥44；点了仍接牌 |

### N-185 · A · `*-catch` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-catch` 可点块必须 `min-height`≥44 或 TOUCH 插值 |
| 现状红 | 仅 `.hh-catch` |
| 不做 | 不扫 `.hh-catch` 的牌面兄弟；不替代 N-148/184 |

---

## 建议顺序

1. A 先合 **N-185**（或与 B 并行）。
2. B 修 **N-183**（prince-princess）、**N-184**（hue-hand），分 PR。
3. 回归：主干 N-94/101、N-108 拼图眼、N-87 冲刺菜单、N-180 `.sn-open`、N-177/178、N-174/175、N-99/97/100。bowling `.bl-btn` 仍走 N-145。若 N-148/151 后合，对账勿双修同一选择器。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-183/184 的点击矩形，不能只看 CSS 行。
