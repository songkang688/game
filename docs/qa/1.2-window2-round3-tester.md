# 窗口 2 · 1.2 三轮验收 第 3 轮 · 测试员汇总

> 本文件按包追加。各包只写自己那一节，不改别人的。

## Pack A

> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-tester-packA.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **1**（本轮新增 `R3-PA-1`；第 1 / 2 轮的 17 条一般项已全部有终态） |
| 遗留最终结论 | **21 条**：**19 条已修 / 2 条仍在但可发布 / 0 条交给主管** |

**就本包五款，能否随本窗口一起发布：能，但有 2 条记录在案的例外。**

### 本轮抽样

中间关改抽 **第 76 关**（第 1 轮是 100、第 2 轮是 84，三轮不重复），统一走 **第 1 / 76 / 188 关**。

### 七条铁则

| 铁则 | 结果 |
|---|---|
| 1 首页 / 深链 `#/game/<id>` | ✅ 5 个 id 全落得下去，`modes` 四种齐全、`levels` 均 188 |
| 2 真赢一次 + 真输一次 | ✅ 五款各取到一赢一输，都点到了结算浮层 |
| 3 战役第 1 / 76 / 188 关 | ✅ 全部从选关地图点进去、玩到结算 |
| 4 对战 / 无尽 / 双人各玩到结算 | ✅ 五款 × 三模式全部到结算浮层 |
| 5 360px 热区 ≥ 44px | ✅ 五款按钮全达标；唯一不足是 chess-garden 棋盘格 41.4px（`R2-PA-3` 记录在案的例外） |
| 6 `destroy` 进→玩→退出→再进 | ✅ 每款 20 轮，`window` 监听 / `root` 子节点 / `document.head` 注入样式全归零 |
| 7 商标黑名单 24 词 | ✅ 53 个产品文件 **0 命中** |

### 重点复验

| 项 | 结果 |
|---|---|
| 第 1 轮三条严重项无回潮 | ✅ fruit-stack 五类按钮全 44；pool-stars 暂停 20 轮键盘 / 按钮 / 台面拖动一概不接、恢复后照旧；junqi-camp 双人两套键各管各的光标 |
| 第 2 轮学习优化员 9 项（`L2A-1`…`L2A-8` `L2A-11a`） | ✅ 逐项重新指认，一项没回滚、一项没回潮 |
| 第 2 轮修复员 5 项 | ✅ 同上 |
| `R2-PA-3` 棋盘格例外是否仍成立 | ✅ **仍成立**：`8×44 = 352 > 331.2` 前提没变，负外边距还在（一格 41.4px），键盘通路完整，其余热区全 ≥ 44，过期提醒断言还活着 |
| `R2-PA-4` 题面池 / 最高档 chip | ✅ 池子 41 个；第 10 局起 chip 挂「已到最高档」、第 42 局起挂「题面第 2 轮」；界面挂的就是 `endlessChip` 拼出来的那一串 |

### 两条记录在案的例外

| 编号 | 款 | 级别 | 一句话 | 为什么可以发 |
|---|---|---|---|---|
| `R2-PA-3` | chess-garden | 一般 | 棋盘格 `.cg-sq` 在 360px 上 41.4px，差 2.6px | 差额只能靠动窗口 1 的 `.screen` 内边距补（跨包）；硬压会裁列或重叠；键盘通路（`WASD`/方向键 + `F`/`L` + `G`/`K`）完整、实测能不点格子走完一步杀；其余六类按钮全 44；例外带自动过期提醒 |
| `R3-PA-1` | fruit-stack | 一般（本轮新增） | 刻意挑最差落点时果盆会进入极限环，三个收场条件都不成立、这一局收不了场（+60000 帧仍未停稳） | 正常玩法撞不上（本轮五种入口全量走查一次没遇到）；不崩不错乱，暂停 / 回选关 / `destroy` 通路都完好；修法要动物理收敛判定，会波及 188 关的过关与星级判定，收官轮不宜临门一脚改 |

### 水位

| 项 | 结果 |
|---|---|
| 本包五款自己的测试 | **36 文件 / 784 例全绿**（与第 2 轮收尾一致） |
| 临时走查脚本 | 5 文件 / **77 例全绿**，取证后已删除，`src/games/**` 里不留 `*.test.ts` |
| `npm test` / `npm run build` | ✅ 全绿 |
| 产品代码改动 | **0**（没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`） |

### 附：同岗第 2 名 · 独立复走（样本换成 1 / 132 / 188）

> 上面是同岗第 1 名的结论，**原文一个字没动**。这一小节是同岗第 2 名按另一套样本独立重走一遍的结果，只追加。
> 详版见 `docs/qa/1.2-window2-round3-tester-packA.md` 的「附：同岗第 2 名 · 独立复走」一节。
> 基线 `d4decfc19cd5de98712b6a1417bdb22f377ccec6`；临时脚本 5 文件 **105 例**全绿，取证后已删，`src/` 0 字节改动。

| | 第 1 名 | **第 2 名** |
|---|---|---|
| 阻断 | 0 | **0** |
| 严重 | 0 | **2** |
| 一般 | 1 | **3** |

**回潮判定两次完全一致**（第 1 轮三条严重项、第 2 轮学习优化 9 项与修复 5 项、`R2-PA-3` 例外、`R2-PA-4` 题面池与 chip
——全部没有回潮），两份报告可互为佐证。

**唯一分歧是同一个缺陷的定级**：第 1 名的 `R3-PA-1` 与第 2 名的 `R3-PA-FS-1` 是同一个根因
（`allSettled` 回不了真，两条判负通路都挂在它上面）。第 1 名定为「一般 · 仍在但可发布」，
前提是「刻意挑最差落点才逼得出来，正常玩法撞不上」；**第 2 名的取证不支持这个前提**：

- **随机落点**（不挑最差）下第 **188 关 20 盆全中**、第 132 关 9/20、第 1 关 2/20，第 188 关平均投到第 22 / 38 颗就卡住；
- 界面上第 132 关落点一步不挪投完 43 颗，**空跑 100 秒不出结算浮层**，盆里 16 颗有 10 颗速度仍有 58～130px/s；
- **人机对战里电脑自己会把自己冻住**（`R3-PA-FS-2`，第 1 名未发现）：人一次都不碰键，电脑投到第 29 颗后
  空转 283 秒再没出手（`stepAi` 也卡在 `allSettled` 前面），这一局怎么等都不结束；此时人这边照常投得动。

按「严重＝一整条铁则不成立」的口径，铁则 2/3 与铁则 4 在这两种情形下不成立，故第 2 名**建议重定为严重**；
它确实不崩、不脏数据、退得出去，所以**不到阻断**。两名在「本轮不动手改物理」上没有分歧
（测试员本就不写 `src/`），第 2 名建议发布前先落一条低成本兜底
（果子用完后连续 N 帧无人达标就按当前分数结算 / 给电脑的等待加上限）。

第 2 名另记 3 条一般项：

| 编号 | 款 | 一句话 |
|---|---|---|
| `R3-PA-FS-3` | fruit-stack | `checkEnd` 双盆分支按 0 号 → 1 号顺序问，同帧都达标永远判朵朵赢（实测两人同步投，投放数 81:81 却是 3:0）；`roundOver` 里那句「这一局打平」是够不着的死文案 |
| `R3-PA-PS-1` | pool-stars | `loseLine` 在 reason 后面硬接一句，而最常见的那条 reason 自己就是这一句 —— 结算浮层上「这一杆差一点点，换个角度再来。」连着出现两遍 |
| `R3-PA-DM-1` | dot-maze | 无尽第一次玩就是历史最好，收场话却还在说「再来一次就能刷新它」，也没有一句「新纪录」（同窗 fruit-stack 会说「这是你的新纪录!」） |

第 2 名换的样本：战役 **1 / 132 / 188**；chess-garden 残局连胜抽第 **11/14/19/23/30/37/41/42/83** 局
（题面池 41 一轮不重样、**与 188 关撞车 0 道**、抽检 8 道后段题面白方都真有强制杀）；
pool-stars 无尽连过 4 局后打空收场；dot-maze 与 fruit-stack 各抽第 132 关深链。

---

## Pack B

> 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-tester-packB.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0**（第 2 轮三条 + 第 1 轮两条严重，本轮独立复验**全部未回潮**） |
| 一般 | **3**（本轮新记 `R3B-1` `R3B-2` `R3B-3`；第 1 / 2 轮的 8 条一般项已全部有终态） |
| 遗留最终结论 | **13 条**：**7 条已闭合 / 5 条仍在但可发布 / 1 条交给主管（R2B-8 平台枚举）** |

**就本包五款，能否随本窗口一起发布：能。**

### 本轮抽样

中间关改抽 **第 76 关**（第 1 轮 100、第 2 轮 84/88/92/96，三轮不重复），五款统一走 **第 1 / 76 / 188 关**。
`fight-king` 的战役就是格斗塔，关号口径与另外四款一致，76 与第 2 轮的 92 不同段。

### 七条铁则

| 铁则 | 结果 |
|---|---|
| 1 首页 / 深链 `#/game/<id>` | ✅ `collectGames()` 收到 64 款，本包 5 个 id 全在，`load()`→`mount()` 全通，`modes` 四种齐全、`levels` 均 188 |
| 2 真赢一次 + 真输一次 | ✅ 五款共取到 15 赢 6 输，全部点到结算浮层；失败文案只鼓励不批评，无血无死亡 |
| 3 战役第 1 / 76 / 188 关 | ✅ 十五关全部从选关地图点进去玩到结算，**星数另按关表算一遍、两边一致**；`tap-tiles` 另把 188 关整表跑通（谱面自检 0 错、参考机器人 188 关全可通） |
| 4 对战 / 无尽 / 双人各玩到结算 | ✅ 5 款 × 3 模式 = 15 局全部到结算；`hop-pads` / `tap-tiles` 三模式另挂镜像，**逐跳 / 逐拍一致** |
| 5 360px 热区 ≥ 44px | ✅ 真按钮全部 ≥44、字号全部 ≥12px。剩下 5 处 `min-height<44` 逐处看过都是文字行 / 装饰行（`.dc-note` `.hh-backs` `.hh-say` `.hp-say` `.tt-say`），不是热区 |
| 6 `destroy` 进→玩→退出→再进 | ✅ 每款 20 轮，`window` 监听 / `root` 子节点 / rAF 队列 / timer / AudioContext 全归零 |
| 7 商标黑名单 24 词 | ✅ 54 个产品文件 **真命中 0**（唯一报警是 `fight-king/index.ts:1214` 注释里「一跳一跳」含子串，误报、基线自带、非可见文案） |

### 重点复验（不看它们自带的用例，另起一套镜像重量）

| 项 | 结果 |
|---|---|
| `R2B-1` dark-chess 双人各管各的光标 | ✅ **未回潮**。朵朵光标 1 / 星星光标 30→22 互不干涉；星星回合朵朵把 `w a s d f g` 按一整套，手数仍是 1；单人局方向键与 `L` 的别名没丢 |
| `R2B-2` hop-pads 幽灵与玩家同一把「16」尺 | ✅ **未回潮**。八局里没有一档幽灵比满分玩家多站一座、也没有一档反超；四档逐局严格递增；高手 vs 大师 20 局**同分 0 局** |
| `R2B-3` fight-king 默认出战 | ⚠️ **症状未回潮，根因只是绕开**。默认已是 ⭐ 星星（常量 / 塔内高亮 / HUD 三处对齐），星星后段八关 1~6/8、**一处 0/8 都没有**；但**朵朵本人一个数没动，抽的八关里 6 关仍整片 0/8** → 记为 `R3B-2`（一般，平衡专项） |
| `B-2` tap-tiles 同谱对战平局 | ✅ **未回潮**。满分玩家对大师 4200:4200 → 「🤝 打成平手!」，两边胜场都不涨、单记「平 1」、星星照给；赢仍是严格大于、输仍是严格小于 |
| `B-6` hop-pads 双人分台 | ✅ **未回潮**。指针 / 触摸 / 键盘三条通路各验一遍，一个人抬手只弹一台，陌生指号碰不着别人 |

### 三条本轮一般项

| 编号 | 款 | 一句话 | 为什么可以发 |
|---|---|---|---|
| `R3B-1` | hue-hand | 抽牌堆与弃牌堆同时见底时 `refill()` 不洗牌、`drawFromDeck()` 只轮空，局面不再前进也没有判和收口（实测连摸 200 次真摸到 0 张） | 要凑出这个局面得让 108 张里的 107 张全在手上（两人各约 53 张），正常对局到不了；战役另有手数上限兜底 |
| `R3B-2` | fight-king | 朵朵在格斗塔后段仍是墙（抽的八关 6 关 0/8），R2B-3 只把默认角色挪开 | 三层兜底都在：默认出战已换星星、连输 3 场提示换人、换人立刻能打（实测第 160 关换糯糯一把赢）。建议与 `R2B-9` `R2B-11` 一起排平衡专项——三条都在同一张角色数值 / 增益曲线 / 对手轮转表上 |
| `R3B-3` | hue-hand | 四档两两对打 120 局，「普通」没打过「新手」（62:58），相邻两档分层不明显 | 跨档很清楚（新手 0:120 大师）；这款自带 `ai.test.ts` 本来也只断言最强压最弱，口径一致 |

### 遗留终态（第 1 / 2 轮 8 条一般项 + 5 条严重项）

| 编号 | 终态 |
|---|---|
| `R2B-4` tap-tiles 四档同分 | **已闭合**——八局四档逐局严格递增、无一组同分 |
| `R2B-5` hop-pads WASD / 方向键无语义 | **仍在，判为口径题**——单键蓄力玩法本来就只有 `f` / 空格，屏幕提示也只写蓄力键；建议主管定口径 |
| `R2B-6` 档名偏冲 | **已闭合**——本包四款屏幕上都是 `新手 / 普通 / 高手 / 大师` |
| `R2B-7` tap-tiles 键位口径 | **已闭合**——42 个音符全用别名键打完 0 漏，屏幕提示与实现逐字吻合 |
| `R2B-8` fight-king 训练场进不了 `meta.modes` | **仍在，交给主管**——`src/engine/types.ts` 的 `GAME_MODES` 里没有 `training`，平台侧枚举，本包按纪律不碰 |
| `R2B-9` fight-king 无尽天花板 | **仍在，判为一般**——增益第 21 场封顶（这本身是对的），默认出战逐场 8 局：第 1 场 8/8、第 13 场 1/8、第 26 场 0/8 |
| `R2B-10` hue-hand 重开换牌 | **已闭合（设计口径）**——生成时验过的那副牌三关全过，重开 12 副过 5~6 副、**卡死 0 副** |
| `R2B-11` fight-king 对手跨章撞车 | **仍在，判为一般**——八章首关：星星 / 绿绿豆 / 云云 / 朵朵 / 闪闪 / 闪闪 / 闪闪 / 闪闪，后四章连着撞同一位 |
| `B-2` `B-6` `R2B-1` `R2B-2` `R2B-3` | 见上「重点复验」，**均未回潮** |

### 水位

| 项 | 结果 |
|---|---|
| `npx vitest run`（全量） | **258 文件 / 6608 例全绿** |
| 本包五款自己的测试 | **41 文件 / 754 例全绿** |
| 临时走查脚本 | 6 文件 / **71 例全绿**，取证后已删除，`src/games/**` 里不留任何测试 |
| `npm run build` | ✅ 全绿，PWA 157 条预缓存照常生成 |
| 产品代码改动 | **0**（没碰 `src/games/**` `src/engine/**` `src/ui/**`，也没改第 1 / 2 轮任何报告原文） |

---

## Pack C（点名五项 · 本窗口收官）

> 范围：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-tester-packC.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **0**（本轮未新记；观察项 1 条 `R3C-G4`，战役不可达） |
| 遗留最终结论 | **10 条**：**8 条已闭合 / 1 条仍在但可发布（`R2C-G3`）/ 1 条为观察项 / 0 条交给主管** |

**就本包五款，能否随本窗口一起发布：能。点名五项硬要求逐条达标。**

### 本轮抽样（三轮不重复）

| 款 | 第 1 轮 | 第 2 轮 | **本轮** |
|---|---|---|---|
| 战役（五款统一） | 1 / 100 / 188 | 1 / 88 / 188 | **1 / 76 / 188** |
| `gomoku` 解局 10 关 | 1/12/34/…/188 | 7/22/45/…/188 | **11/28/51/73/94/108/126/151/173/188** |
| `gomoku` 相邻档 seed | 1000 | 4200/8600/15400 | **2718 / 5772 / 13331；着法尺 60607** |
| `match-stars` 时间线 | 引擎小盘 | 1 / 88 / 188 关 | **42 / 76 / 133 关** |
| `xiangqi` 残局 10 课 | 1/21/…/188 | 7/33/…/181 | **3/17/39/62/70/76/96/118/140/163/185** |
| `ocean-munch` 无尽 | 767 / 147 | 591 | **442 米** |
| `xiangqi` 强弱考卷 | 一步杀 60 道 | 隔两道另取 60 道 | **两步杀 40 道（换尺子）** |

### 点名五项逐条交账

| # | 款 | 本轮证据 | 判定 |
|---|---|---|---|
| 1 | `gomoku` | 10 关 `solutionLine()` **实摆到底** + `findWinLine()` 验黑五连；第三组 seed 五对相邻档全部单调（第 1 轮那两条阻断没有回潮）；30 局面第二把尺六档分开；前 99 题对 1.1 漂移 **0**；188 关无解 **0** | ✅ 达标 |
| 2 | `match-stars` | 第 42 / 76 / 133 关飘空帧 **25 / 14 / 66**（≠0 即非瞬变）；`fall` 段视觉偏差 1.00–3.00 格连续收敛；`planRefill` 的 `fromRow` 全为负；降速走同一状态机，无瞬变旁路 | ✅ 达标，**瞬变阻断不成立** |
| 3 | `rainbow-run` | `view3d.ts` / `controls.ts` 对 1.1 **0 行 diff**；`endless.ts` 是严格超集（1.1 的 42 个导出一个不丢，1.2 加到 56）；three.js / CDN / 联网 **0 命中**；`makeCamera` 500 帧只在尺寸真变时 +1 | ✅ 达标 |
| 4 | `ocean-munch` | 无尽泡到系统判失败（442m）→ `recordEndlessBest("ocean-munch", 442)` 精确 1 次 → `destroy` 后读回 442；纪录只增不减、`NaN` 不污染、不串别款 | ✅ 达标 |
| 5 | `xiangqi` | 全库 65 个目录里象棋只有一个；六档层数 / 预算 / 延时三条严格单调（0→5 层、20→400ms、320→760ms）；188 课有解、虚标 **0**（将死 136 / 困毙 52）；抽 11 课实走终局与自报逐课相符；长将四条边界都对 | ✅ 达标 |

### 七条铁则（五款横向）

| 铁则 | 结果 |
|---|---|
| 1 首页 / 深链 `#/game/<id>` | ✅ 五款 `meta` 全是纯数据（无 `import`、无函数值），`modes` / `levels` 与实现对得上，`?level=` 直开 |
| 2 真赢一次 + 真输一次 | ✅ 五款各取到一赢一输；失败话术扫 `死 / 血 / 残忍 / 杀` **0 命中**，只鼓励 |
| 3 战役第 1 / 76 / 188 关 | ✅ 十五关全部玩到结算；赢的面板有「下一关」、输的只有「再试本关」，两套按钮一次没串 |
| 4 每种模式玩到结算 | ✅ gomoku 4 种 / match-stars 4 种 / rainbow-run 2 种 / ocean-munch 3 种 / xiangqi 4 种 |
| 5 360px 热区 ≥ 44px | ✅ CSS 三款全部 **44.0px**（含 `max-width:380px` 那一档）；纯画布两款的偏小按钮面全部过 `touchArea()` 外扩并真点到 |
| 6 `destroy` 进→玩→退→再进 20 轮 | ✅ 监听 / 根子节点 / head 样式 / 定时器全归零，累计 `cancelAnimationFrame` 每轮正好 +1 |
| 7 商标黑名单 + 红线词 | ✅ 五款 **0 命中**；离线自查（three.js / CDN 字体 / 外链音源 / `fetch` / `sendBeacon`）也是 **0** |

### 遗留项终态

| 编号 | 终态 |
|---|---|
| `R2C-G1` `R2C-G2` gomoku 热区 | **已清**——`.gmk-btns` / `.gmk-seg` / `.gmk-mode` / `.gmk-over-btn` 全部 44.0px，`MIN_HIT_PX` 是 `view.ts` 导出的常量 |
| `R2C-R1` rainbow-run 热区 4 处 | **已清**——四处全过 `touchArea`，`btnEndless` 自身已 44 |
| `R2C-R2` rainbow-run 红线词 2 处 | **已清**——「掉血」已改卸护甲口径、《广告墙走廊》已改霓虹招牌，全款重扫 0 命中 |
| `R2C-O1` ocean-munch 热区 6 处 | **已清**——六处全过 `touchArea`，`btnDex` 外扩后边界真点到 |
| `R2C-X1` xiangqi 热区 3 处 | **已清**——`.xq-btns` / `.xq-mode` / `.xq-over-btn` / `.xq-rules-close` 全部 44.0px |
| `R2C-X2` xiangqi easy/normal 同分 | **已闭合，是量具问题**——旧尺（一步杀）easy 以上全部 40/40 天花板；换两步杀考卷后六档 7→8→10→26→34→40 严格单调。**不改搜索，不上交** |
| `R2C-G3` gomoku smart/normal 和棋率 | **仍在，一般，不阻断发布，转 1.3**——三组 seed 共 72 局里 smart 只输 3 局赢 39 局，无倒挂；和棋率 25%–47% 是 15 路满盘的样本噪声。要压下来必须动 `ai.ts` 评分权重，本轮红线不许碰 |
| `R17` rainbow-run 第 1 关摆烂通关 | **关闭为设计如此**——5 个 seed 过 4 摔 1，且**一次都没白拿 3 星**（最高 2 星）。断言已固化进回归网 |
| `G15` gomoku 基本功电池饱和 | **关闭为量具问题**——与 `R2C-X2` 同类；相邻档实战五对全单调，30 局面第二把尺已把六档分开 |
| `X20` xiangqi 上两对档和棋多 | **关闭**——固定 seed 重量：master vs hard 11:0（1 局未分）、hell vs master 12:0（0 局未分），弱档一局没赢 |

### 本轮新记

| 编号 | 款 | 级别 | 一句话 |
|---|---|---|---|
| `R3C-G4` | gomoku | **观察项（不记缺陷）** | 强行打开禁手规则时 188 关里有 6 关（46–51「双冲」系列）解集变空——它们的杀法本身就是双冲四。解局战役把 `forbidden:false` 写死，玩家打不开这个开关，**战役不可达** |

### 水位

| 项 | 结果 |
|---|---|
| 包 C 走查脚本 | **3 文件 / 58 例全绿**（`c3-rainbow-run` 26 + `c4-ocean-munch` 13 + `c5-xiangqi` 19），**留库当回归网**（放 `src/qa-window2/`，不在 `src/games/**` 下） |
| 产品代码改动 | **0**（走查期间没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`） |
| 第 1 / 2 轮报告原文 | **0 字节改动** |
