# 窗口 2 · 1.2 三轮验收 第 3 轮 · 测试员汇总

> 本文件按包追加。各包只写自己那一节，不改别人的。

## Pack A

> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-tester-packA.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **1**（本轮新增 `R3-PA-1`；第 1 / 2 轮的 17 条一般项已全部有终态） |
| 遗留最终结论 | **21 条**：**19 条已修 / 2 条仍在但可发布 / 0 条交给主管** |

**就本包五款，能否随本窗口一起发布：能，但有 2 条记录在案的例外。**

### 本轮抽样

中间关改抽 **第 76 关**（第 1 轮是 100、第 2 轮是 84，三轮不重复），统一走 **第 1 / 76 / 188 关**。

### 七条铁则

| 铁则 | 结果 |
|---|---|
| 1 首页 / 深链 `#/game/<id>` | ✅ 5 个 id 全落得下去，`modes` 四种齐全、`levels` 均 188 |
| 2 真赢一次 + 真输一次 | ✅ 五款各取到一赢一输，都点到了结算浮层 |
| 3 战役第 1 / 76 / 188 关 | ✅ 全部从选关地图点进去、玩到结算 |
| 4 对战 / 无尽 / 双人各玩到结算 | ✅ 五款 × 三模式全部到结算浮层 |
| 5 360px 热区 ≥ 44px | ✅ 五款按钮全达标；唯一不足是 chess-garden 棋盘格 41.4px（`R2-PA-3` 记录在案的例外） |
| 6 `destroy` 进→玩→退出→再进 | ✅ 每款 20 轮，`window` 监听 / `root` 子节点 / `document.head` 注入样式全归零 |
| 7 商标黑名单 24 词 | ✅ 53 个产品文件 **0 命中** |

### 重点复验

| 项 | 结果 |
|---|---|
| 第 1 轮三条严重项无回潮 | ✅ fruit-stack 五类按钮全 44；pool-stars 暂停 20 轮键盘 / 按钮 / 台面拖动一概不接、恢复后照旧；junqi-camp 双人两套键各管各的光标 |
| 第 2 轮学习优化员 9 项（`L2A-1`…`L2A-8` `L2A-11a`） | ✅ 逐项重新指认，一项没回滚、一项没回潮 |
| 第 2 轮修复员 5 项 | ✅ 同上 |
| `R2-PA-3` 棋盘格例外是否仍成立 | ✅ **仍成立**：`8×44 = 352 > 331.2` 前提没变，负外边距还在（一格 41.4px），键盘通路完整，其余热区全 ≥ 44，过期提醒断言还活着 |
| `R2-PA-4` 题面池 / 最高档 chip | ✅ 池子 41 个；第 10 局起 chip 挂「已到最高档」、第 42 局起挂「题面第 2 轮」；界面挂的就是 `endlessChip` 拼出来的那一串 |

### 两条记录在案的例外

| 编号 | 款 | 级别 | 一句话 | 为什么可以发 |
|---|---|---|---|---|
| `R2-PA-3` | chess-garden | 一般 | 棋盘格 `.cg-sq` 在 360px 上 41.4px，差 2.6px | 差额只能靠动窗口 1 的 `.screen` 内边距补（跨包）；硬压会裁列或重叠；键盘通路（`WASD`/方向键 + `F`/`L` + `G`/`K`）完整、实测能不点格子走完一步杀；其余六类按钮全 44；例外带自动过期提醒 |
| `R3-PA-1` | fruit-stack | 一般（本轮新增） | 刻意挑最差落点时果盆会进入极限环，三个收场条件都不成立、这一局收不了场（+60000 帧仍未停稳） | 正常玩法撞不上（本轮五种入口全量走查一次没遇到）；不崩不错乱，暂停 / 回选关 / `destroy` 通路都完好；修法要动物理收敛判定，会波及 188 关的过关与星级判定，收官轮不宜临门一脚改 |

### 水位

| 项 | 结果 |
|---|---|
| 本包五款自己的测试 | **36 文件 / 784 例全绿**（与第 2 轮收尾一致） |
| 临时走查脚本 | 5 文件 / **77 例全绿**，取证后已删除，`src/games/**` 里不留 `*.test.ts` |
| `npm test` / `npm run build` | ✅ 全绿 |
| 产品代码改动 | **0**（没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`） |

---

## Pack B

> 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king` 共 5 款
> 详版：`docs/qa/1.2-window2-round3-tester-packB.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0**（第 2 轮三条 + 第 1 轮两条严重，本轮独立复验**全部未回潮**） |
| 一般 | **3**（本轮新记 `R3B-1` `R3B-2` `R3B-3`；第 1 / 2 轮的 8 条一般项已全部有终态） |
| 遗留最终结论 | **13 条**：**7 条已闭合 / 5 条仍在但可发布 / 1 条交给主管（R2B-8 平台枚举）** |

**就本包五款，能否随本窗口一起发布：能。**

### 本轮抽样

中间关改抽 **第 76 关**（第 1 轮 100、第 2 轮 84/88/92/96，三轮不重复），五款统一走 **第 1 / 76 / 188 关**。
`fight-king` 的战役就是格斗塔，关号口径与另外四款一致，76 与第 2 轮的 92 不同段。

### 七条铁则

| 铁则 | 结果 |
|---|---|
| 1 首页 / 深链 `#/game/<id>` | ✅ `collectGames()` 收到 64 款，本包 5 个 id 全在，`load()`→`mount()` 全通，`modes` 四种齐全、`levels` 均 188 |
| 2 真赢一次 + 真输一次 | ✅ 五款共取到 15 赢 6 输，全部点到结算浮层；失败文案只鼓励不批评，无血无死亡 |
| 3 战役第 1 / 76 / 188 关 | ✅ 十五关全部从选关地图点进去玩到结算，**星数另按关表算一遍、两边一致**；`tap-tiles` 另把 188 关整表跑通（谱面自检 0 错、参考机器人 188 关全可通） |
| 4 对战 / 无尽 / 双人各玩到结算 | ✅ 5 款 × 3 模式 = 15 局全部到结算；`hop-pads` / `tap-tiles` 三模式另挂镜像，**逐跳 / 逐拍一致** |
| 5 360px 热区 ≥ 44px | ✅ 真按钮全部 ≥44、字号全部 ≥12px。剩下 5 处 `min-height<44` 逐处看过都是文字行 / 装饰行（`.dc-note` `.hh-backs` `.hh-say` `.hp-say` `.tt-say`），不是热区 |
| 6 `destroy` 进→玩→退出→再进 | ✅ 每款 20 轮，`window` 监听 / `root` 子节点 / rAF 队列 / timer / AudioContext 全归零 |
| 7 商标黑名单 24 词 | ✅ 54 个产品文件 **真命中 0**（唯一报警是 `fight-king/index.ts:1214` 注释里「一跳一跳」含子串，误报、基线自带、非可见文案） |

### 重点复验（不看它们自带的用例，另起一套镜像重量）

| 项 | 结果 |
|---|---|
| `R2B-1` dark-chess 双人各管各的光标 | ✅ **未回潮**。朵朵光标 1 / 星星光标 30→22 互不干涉；星星回合朵朵把 `w a s d f g` 按一整套，手数仍是 1；单人局方向键与 `L` 的别名没丢 |
| `R2B-2` hop-pads 幽灵与玩家同一把「16」尺 | ✅ **未回潮**。八局里没有一档幽灵比满分玩家多站一座、也没有一档反超；四档逐局严格递增；高手 vs 大师 20 局**同分 0 局** |
| `R2B-3` fight-king 默认出战 | ⚠️ **症状未回潮，根因只是绕开**。默认已是 ⭐ 星星（常量 / 塔内高亮 / HUD 三处对齐），星星后段八关 1~6/8、**一处 0/8 都没有**；但**朵朵本人一个数没动，抽的八关里 6 关仍整片 0/8** → 记为 `R3B-2`（一般，平衡专项） |
| `B-2` tap-tiles 同谱对战平局 | ✅ **未回潮**。满分玩家对大师 4200:4200 → 「🤝 打成平手!」，两边胜场都不涨、单记「平 1」、星星照给；赢仍是严格大于、输仍是严格小于 |
| `B-6` hop-pads 双人分台 | ✅ **未回潮**。指针 / 触摸 / 键盘三条通路各验一遍，一个人抬手只弹一台，陌生指号碰不着别人 |

### 三条本轮一般项

| 编号 | 款 | 一句话 | 为什么可以发 |
|---|---|---|---|
| `R3B-1` | hue-hand | 抽牌堆与弃牌堆同时见底时 `refill()` 不洗牌、`drawFromDeck()` 只轮空，局面不再前进也没有判和收口（实测连摸 200 次真摸到 0 张） | 要凑出这个局面得让 108 张里的 107 张全在手上（两人各约 53 张），正常对局到不了；战役另有手数上限兜底 |
| `R3B-2` | fight-king | 朵朵在格斗塔后段仍是墙（抽的八关 6 关 0/8），R2B-3 只把默认角色挪开 | 三层兜底都在：默认出战已换星星、连输 3 场提示换人、换人立刻能打（实测第 160 关换糯糯一把赢）。建议与 `R2B-9` `R2B-11` 一起排平衡专项——三条都在同一张角色数值 / 增益曲线 / 对手轮转表上 |
| `R3B-3` | hue-hand | 四档两两对打 120 局，「普通」没打过「新手」（62:58），相邻两档分层不明显 | 跨档很清楚（新手 0:120 大师）；这款自带 `ai.test.ts` 本来也只断言最强压最弱，口径一致 |

### 遗留终态（第 1 / 2 轮 8 条一般项 + 5 条严重项）

| 编号 | 终态 |
|---|---|
| `R2B-4` tap-tiles 四档同分 | **已闭合**——八局四档逐局严格递增、无一组同分 |
| `R2B-5` hop-pads WASD / 方向键无语义 | **仍在，判为口径题**——单键蓄力玩法本来就只有 `f` / 空格，屏幕提示也只写蓄力键；建议主管定口径 |
| `R2B-6` 档名偏冲 | **已闭合**——本包四款屏幕上都是 `新手 / 普通 / 高手 / 大师` |
| `R2B-7` tap-tiles 键位口径 | **已闭合**——42 个音符全用别名键打完 0 漏，屏幕提示与实现逐字吻合 |
| `R2B-8` fight-king 训练场进不了 `meta.modes` | **仍在，交给主管**——`src/engine/types.ts` 的 `GAME_MODES` 里没有 `training`，平台侧枚举，本包按纪律不碰 |
| `R2B-9` fight-king 无尽天花板 | **仍在，判为一般**——增益第 21 场封顶（这本身是对的），默认出战逐场 8 局：第 1 场 8/8、第 13 场 1/8、第 26 场 0/8 |
| `R2B-10` hue-hand 重开换牌 | **已闭合（设计口径）**——生成时验过的那副牌三关全过，重开 12 副过 5~6 副、**卡死 0 副** |
| `R2B-11` fight-king 对手跨章撞车 | **仍在，判为一般**——八章首关：星星 / 绿绿豆 / 云云 / 朵朵 / 闪闪 / 闪闪 / 闪闪 / 闪闪，后四章连着撞同一位 |
| `B-2` `B-6` `R2B-1` `R2B-2` `R2B-3` | 见上「重点复验」，**均未回潮** |

### 水位

| 项 | 结果 |
|---|---|
| `npx vitest run`（全量） | **258 文件 / 6608 例全绿** |
| 本包五款自己的测试 | **41 文件 / 754 例全绿** |
| 临时走查脚本 | 6 文件 / **71 例全绿**，取证后已删除，`src/games/**` 里不留任何测试 |
| `npm run build` | ✅ 全绿，PWA 157 条预缓存照常生成 |
| 产品代码改动 | **0**（没碰 `src/games/**` `src/engine/**` `src/ui/**`，也没改第 1 / 2 轮任何报告原文） |
