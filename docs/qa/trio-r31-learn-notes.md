# 三人组第 31 轮 · 学习笔记（学习员 C / 1cd5，仅增量）

> 基线：`origin/game-1.3` @ `17356717`（含 `b7677155` N-99/97/100/109 源码 + r19 A 交卷）。
> 前文：PR #101 r30（N-150…N-152）… #90 r22；**PR #94** A、**PR #96** B。
> 本轮 `src/**` 零改动。三视口 390×844 / 915×412 / 1024×768。
> **编号从 N-153 起**。撞车取主干先合版。

## 一、抽验方式

- 读 r22–r30。对账主干新 SHA，不改已销号的义。
- 主干 grep：`--k:\d+px` <44；方向键 `width/height:var(--k)` 无 `min-height:44`。
- 未开无头浏览器。

## 二、号段（先合版）

N-100…N-109 主干；**N-99/97/100/109 已由 `b7677155` 销账**，只回归。N-108 仍是拼图无尽画廊。N-110…116 跳过。N-117…152 在途 PR 已占。本轮 **N-153 起**。

并号：N-94/101 dvs；N-104/141 地主；N-142/144 fk；N-133/134；N-135 四款返回；N-145 bowling/bumper；N-147 snake/pz **返回**；N-148 hh-catch；N-149 open/mode；N-150 brave-path；N-151 pzt 工具 / `.pcp-act`；N-152 veil-btn。仓鼠 N-47。kit N-132。fruit-catch 开/返回 N-121。poop 摇杆已走 `padMetrics` ≥44，`MIN_HOT_DUO=34` 只是注释遗物，**不开号**。

## 三、对账

- 相对 r30：主干前进 `206d0522` → `17356717`。N-99 数独盘可滚、N-97 农场钉底、N-100 进场锚定、N-109 root 门收档 → **只回归**，勿第二套。
- N-105 / N-12 族 / C-8（禁改 `SKY_H`）/ N-90 只回归。
- sky-squad `hotspots.r11.test.ts` 只钉双人 `--k:44`、禁止 36/34，**仍允许单人 42/38**。
- `.pcp-key` 已写 `min-height:44`，但格子 `--k` 可到 34，两套尺码打架（重叠/裁切），见 N-154。

## 四、新抽验（N-153 起）

### N-153 🔧 B · sky-squad 方向键 `--k` 单人 <44

`.sks-key{width:var(--k);height:var(--k)}`，**没有** `min-height:44`。

| 规则 | 行号约 | `--k` |
| --- | --- | --- |
| 默认 `.sks-pads` | ~144 | **42px** |
| `@media (max-width:420px)` | ~174 | **38px** |
| `[data-players="2"]` | ~145 / ~175 | 44（已绿） |

390 单人方向/开火键边长 38。修法：`--k` 下限 44，或给 `.sks-key` 钉 `min-width/min-height:44`（格子跟着让）。勿改飞行物理。

### N-154 🔧 B · prince-princess 双人 D-pad 格子 34–42

`.pcp-pad` 用 `grid-auto-rows:var(--k)`。双人档：

- 默认 `--k:42`
- `max-width:420` → **37**
- `max-height:620` → **34**
- N-79 的 `max-height:500` → **36**

`.pcp-key` 虽钉 44，格子更矮会叠键或被 overflow 吃点击。学 poop：双人改列数/让画布，**不要**把 `min-height` 撤掉。N-79 只压画布，不管键边。N-151 是 `.pcp-act`，勿混。

### N-155 🔧 A · `--k:` 数字地板静态巡检

N-136/143/146/149/152 都不扫 CSS 变量。本号：`src/games/**` 里 `--k:\d+px` 必须 ≥44，或写成 `${TOUCH_MIN}` / `MIN_HOT`。豁免：非按键用途的同名变量（若有，写白名单）。N-153/154 修完应变绿。puff-bros 窄屏已用 `${TOUCH_MIN}`，当对照。

## 五、r22–r30 只派不换号

A：N-155；N-152/149/146/143/138；PR #94。N-99/97/100 主干已合则回归。  
B：N-153/154；N-150/151/147/148/144/145；N-135 三款；N-125/126/129；N-108。

## 六、纪律

只交本文 + `trio-r31-playbook.md`。下一空号 **N-156**。
