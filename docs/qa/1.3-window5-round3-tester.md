# 1.3 视觉升级 · 窗口 5 · 第 3 轮(终验)测试员报告(A 档)

> 角色:窗口 5 第 3 轮(共 3 轮,终验)**视觉测试员**。本轮只回答一个问题:**本窗 10 款这套视觉能不能放行**。
> 工作分支 `game-1.3-window5`;基线 = 远端 tip `5e46214`(round2 fixer 定稿),合并态复验 SHA `49c5c7c` 在链上;
> 自 `49c5c7c` 以来 `src/` **零改动**(`git diff --name-only 49c5c7c..HEAD -- src/` = 0 个文件,仅 fixer 报告定稿与本报告计划 commit),
> 即本轮验的代码与 R2 三方合并态复验通过的代码逐字节一致。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 权限边界:只写本报告 + 本窗相关 `*.test.ts`(本轮新增 1 个守卫文件);绘制 / 判定 / 数值 / parentAuth / parentGate / 他人报告零触碰。
> 终验新问题写入第四节交本轮 C 档;结论以当前合并态为准,不空等 C 档修完。

**方法与工具**:vitest 全量跑;`npm run build` 产物 + `npx vite preview`(4173)与 `vite dev`(5199,画笔模块动态 import)双服务;
puppeteer-core 25.9 驱动系统 Chrome headless(720×1280 DPR2 / 360×640,CDP `Emulation.setCPUThrottlingRate` 4× 节流,
`prefers-reduced-motion: reduce` 仿真);帧率采用**分片协议**(每片 2s rAF 计帧、片前后校验无结算 / 暂停弹窗,防止轰炸输入把短关打通关后把帧率量在结算屏上,取 3 有效片最低值);
节流机制先做 1×/4×/8×/16× A/B 自证(60.3 / 52.3 / 27.2 / 14.3fps,单调递减,确认节流真实生效)。
画笔特写走 dev 服务动态 import(pp `drawEnemy` 五母形 / `drawBossFigure` 两变体、snf `paintSnowFoe` 2× 与 16px、bb `drawCritter` 四型 / `drawBossKing`)。
截图与放大裁片(sharp)一律存 `/tmp`,不入仓库(1.3 宪法:不提交位图)。

---

## 一、全量跑数据与三轮用例数曲线

### 1.1 R3 起点复验(基线 `5e46214`,代码 = `49c5c7c` 合并态)

| 项目 | R2 fixer 宣称(合并态 `49c5c7c`) | R3 实测(起点) | 结论 |
| --- | --- | --- | --- |
| `npm test` | 745 文件 / 15395 例全绿 | **745 文件 / 15395 例全绿**(180.5s) | 宣称属实 ✅ |
| `npm run build` | tsc + vite 全绿 | **全绿** | ✅ |
| PWA precache | 195 entries / 5240.35 KiB | **195 entries / 5240.35 KiB** | 逐字一致 ✅ |
| `dist/` 总体积 | 5.6M | **5.6M** | ✅ |
| 10 款 chunk | sr 60.1K…pp 74.5K | **逐款一致**(sr 64K/ss 80K/tb 72K/bb 84K/snf 72K/bc 64K/bl 48K/iff 56K/pb 76K/pp 76K,du -k 取整口径) | 无出入 ✅ |

### 1.2 三轮用例数曲线(只增不减 ✅)

| 节点 | 测试文件 | 用例数 | 增量 |
| --- | --- | --- | --- |
| R1 合并态(fixer `1638cde`) | 740 | 15365 | — |
| R2 tester 定稿 | 741 | 15367 | +1 / +2 |
| R2 learner | 742 | 15378 | +1 / +11 |
| R2 合并态(`49c5c7c`) | 745 | 15395 | +3 / +17(fixer) |
| **R3 tester 定稿(本轮)** | **746** | **15399** | **+1 / +4**(守卫用例,见 1.3) |

曲线三轮单调递增,无任何回退。

### 1.3 机器化防线在位复核(放行前提)

- 前两轮沉淀的机器化扫描用例**全部在位、全部在跑、全绿**:`visualScan13.test.ts` ×10、
  `visualFix13.test.ts` ×9(tank-battle R1 无修复项本就没有)、`visualFixR2.test.ts` ×3(snf 5 例 / pp 9 例 / bc 3 例)、
  `visualScanR2.test.ts` ×1、`copy13.test.ts` ×1、`copyW5R2.test.ts` ×1;
- 全库测试**零 skip / todo / fails**(`rg "(it|describe|test)\.(skip|todo|fails)" src/games` = 0 命中);
- ratchet 阈值逐个抽读:无一处放宽;fixer R2 收紧值(snf fillText≤1 / emoji≤53,pp 🔒=0 / `emoji()`=0 / emoji≤38 / fillText≤3,
  bc 反射斑两停上限 + `FLOOR_GLOW_CORE_ALPHA` 档位,pp 零纵向线性渐变)全部在跑全绿;
- **本轮测试员新增 `src/games/visualGuardW5R3.test.ts`(+1 文件 / +4 例,commit `0643617`)**:
  ① 上列 26 个机器化测试文件在位钉死(谁删谁红);② 本窗测试文件零 skip 语法钉死;
  ③ 10 款绘制文件 emoji 码点终态水位整表锁死(sr52 / ss43 / tb49 / bb64 / snf53 / bc43 / bl47 / iff28 / pb72 / pp38,只降不升);
  ④ 10 款画布 fillText 终态水位整表锁死(sr5 / bb3 / pp3 / iff2 / snf1 / 其余 0,只降不升)。
  与既有各款 ratchet 互为双保险,只紧不松。

**收尾定稿复跑:`npm test` 746 文件 / 15399 例全绿;`npm run build` 全绿(PWA 195 entries / 5240.35 KiB)。**

---

## 二、第 2 轮问题终验对账表(逐条三选一:已修复 / 合理降级 / 仍未修升级阻断)

R2 tester 新问题 4 条(N1–N4)+ R2 fixer 落地的 B 档点名 C-1、登记项 C-2/C-3(R2-b)+ 遗留清单 R2-a、L-2–L-5。
验证手段 = 代码复扫(独立 grep/正则,不抄 fixer 结论)+ 玩法截图 + 画笔特写渲染 + 机器化断言在跑。

| 编号 | 款 / 位置 | R2 问题 | 终验结论 | 证据(R3 独立复核) |
| --- | --- | --- | --- | --- |
| N1(=R1 G3 半项) | snow-fight 座位标 🌸⭐ / 暖手 🔥 三处画布 fillText | 字形贴纸钉在视线焦点 | **已修复**(`1bc6306`) | `paintSeatMark` / `paintWarmFlame` 在 `index.ts:555/557` 实际调用;绘制文件 fillText 实测 **1**(只剩风旗功能字)、emoji 码点 **53**;720×1280 玩法截图角色头顶已是自绘五瓣花徽记(奶黄花心清晰可辨);`visualFixR2` 5 例全绿(两队形状通道 arc 数拉开:花 ≥6 圆 / 星 0 圆) |
| N2 | prince-princess `index.ts` 门锁 🔒 24px 字形 | iff 同类已矢量化,本款漏网 | **已修复**(`aee6859`) | `drawPadlockBadge`(visual13.ts:840,圆环锁弓 + 金 2 停锁体 + 锁孔)在 `index.ts:619` 调用;🔒 字形实测 **0**;开 / 合两态断言全绿 |
| N3 | bumper-cars 反射斑观感含蓄 | 第 1 关粉底上近平涂 | **已修复(含回压修订)**(`9f789d1`+`7be3fd5`) | `FLOOR_GLOW_CORE_ALPHA = 0.24`(0.16→0.24 提档)+ `FLOOR_GLOW_SHEEN_ALPHA = 0.12` 纯色内核亮斑(0.34r 同倾角);大椭圆径向渐变**两停**(回压修订撤 0.55 中途停,防线用例钉死);三处调用点 0.3/0.22/0.18×min 一个数未动;玩法截图反射斑与灯串光晕肉眼可读、「冰面不画花」保持;本探针 4× 节流 60.0fps(fixer 探针口径 39.7,均在基线带) |
| N4 + G4/L-1 画布部分 | prince-princess `emoji()` 助手 11 处(出场卡 / 状态 icon / 指路 / 飘字) | ≤24px 画布 emoji 尾巴 | **已修复(画布全清)**(`da5b04a`) | `emoji(` 调用点实测 **0**(助手退休);emoji 码点 59→**38**、fillText **3**(功能文字);7 支徽章画笔 + 12 种事件飘图断言全绿;指路徽章复用 `crownPath` / `bowShape`(符合 B 档商标提醒:不新造小人) |
| C-1(B 档点名排名 2) | prince-princess visual13 纵向线性渐变 3 处 | 款内两套光照语言 | **已修复**(`49c5c7c`) | R3 独立正则复扫 visual13.ts `createLinearGradient` 起终点 x 相等者 **0 处**;±0.18w 拉斜参数在场 17 处;「零纵向渐变」防线用例全绿 |
| C-2(B 档排名 3,R2-b) | bowling-lane 灰紫夜场提暖 / 霓虹点 | 色板明度与粉彩家族距离 | **合理登记(交本轮 C 档,非放行前置)** | B 档自排「下一轮」、fixer 按轮次登记未抢跑,符合流程;终验口径复核:该项属「同语言内深浅差」(B 档原话),暗场馆主题成立、亮度预算让给球道与灯箱的克制成立,不属六大专项任何一格的不达标;A/B 二选一规格在案随时可落 |
| C-3(B 档排名 5,R2-b) | tank-battle 炮塔高光弧 + 徽章 strokeOutline | 工艺代差(轻) | **合理登记(交本轮 C 档,非放行前置)** | 同上按轮次登记;终验截图复核:坦克三件套 + 砖珠高光落影 + 金框加固墙成立,「平半档」是深浅差不是断层;克制执行两笔的规格在案 |
| R2-a | pp DOM 出场卡 / HUD chips emoji | 功能文字口径 | **合理降级(挂账在案)** | DOM 层非画布,教学口令「先图后字」语义在用;与 L-4 同类,第 3 轮无 HUD 统一走查任务,继续挂账勿丢 |
| L-2(G6–G8) | 全库 level99 星标 / 按钮触区 / tank 窄屏小字 | 1.2 基线 | **合理降级(窗外,挂账在案)** | 360 复测无回归;全库统一轮次 |
| L-3(G9) | sky-squad / tank-battle HUD 芯片行 360px 横滑观感 | 纯观感 | **合理降级(挂账在案)** | R3 360×640 复测:仍为可横滑容器,横向溢出 0px、信息可达 |
| L-4 | puff-bros HUD 🍬 DOM 计数字 1 处 | 功能文字 | **合理降级(挂账在案)** | 实测仍 1 处,钉 ≤2 的用例在跑 |
| L-5(Z2/G11) | PLAN 文档对标点名词 | 主管口径 | **合理降级(挂账在案)** | R3 复扫:「炸弹人」仅存 `PLAN-1.2/1.3-15A.md` 与测试禁词表(负向断言),玩家可见内容 0 命中 |

**对账统计:R2 新问题 + B 档点名 5 条(N1/N2/N3/N4/C-1)全部已修复,修复确认率 5/5 = 100%;
合理登记 / 降级 7 条(C-2/C-3/R2-a/L-2/L-3/L-4/L-5)全部在案未丢;仍未修升级阻断 0 条。阻断级 / 严重级终态 = 0。**

另核出 R2 fixer 报告一处笔误(不影响任何结论,见第四节 R3-b)。

---

## 三、10 款 × 6 专项终验总表

口径:每格二值「达标 / 不达标」,不适用的格明标「不适用」(专项③仅点名 5 双人款、专项④点名 4 款 + 其余按适用性,均不为凑格改绘制)。
证据 = 720×1280 DPR2 玩法截图 + sharp 放大裁片(角色 / 道具 / 地面)+ dev 服务画笔特写(2× 与 16px)+ 灰度转换 + 独立 grep + 在跑断言;
R2 已量化且代码零变化的读数(双人三通道等)直接引用并标注。

| 款 | ① 火柴人 / 草稿圆 | ② 金币 / 道具体积 | ③ 双人可区分(灰度剪影) | ④ 2.5D 纵深 | ⑤ 360px 布局 | ⑥ 商标 |
| --- | --- | --- | --- | --- | --- | --- |
| shoot-range | **达标**(多环渐变靶 + 木框支架;奖品架玩偶 = 刻意压灰剪影布景;无角色裸圆) | **达标**(金星 `ballGradient` 三停 + 描边,断言在跑) | 不适用 | 不适用(固定靶棚侧视,静态透视装置在位) | **达标**(溢出 0 / 三屏完整) | **达标**(玩偶保持压灰剪影,黑名单 0 命中) |
| sky-squad | **达标**(主机三停渐变 + 双翼缺口剪影,截图实证) | **达标**(七型拾取徽章:专属色圈 + 矢量符号 + 2px 描边,`visualFix13` 断言 + R2 特写) | 不适用 | **达标**(双层云视差 0.15/0.3 截图实证,点名项) | **达标**(溢出 0;芯片行横滑 = L-3 挂账) | **达标**(八章 Boss 徽章自家星 / 云 / 糖图形) |
| tank-battle | **达标**(坦克三件套:炮塔 + 车体 + 履带 + 花徽,截图放大实证) | **达标**(基地金星 + 光圈、砖珠高光 + 落影,截图实证) | 不适用 | **达标**(block25d 顶侧双面 + 砖块体积光影,截图实证,点名项) | **达标**(溢出 0;窄屏小字 = L-2 挂账) | **达标**(星花齿轮徽记原创,禁鹰徽军徽守住) |
| bomb-buddies | **达标**(双人 chibi + 四小怪母形 + 泡泡王,特写渲染实证) | **达标**(硬块铆钉双面 + 软砖扎带 + 层数圆牌,截图实证) | **达标**(R2 量化 8.6% / 16.1% / 19.1 三通道可辨 + 截图 朵朵花发卡;代码零变化) | 不适用(顶视棋盘;块体 2.5D 语言在位) | **达标**(溢出 0) | **达标**(「泡泡布阵」在位;PLAN 旧名 = L-5 主管口径) |
| snow-fight | **达标**(雪怪双球三停 + 歪帽 2× / 16px 特写实证;主角渐变 + 落影截图实证) | **达标**(座位花 / 星徽记 + 暖手两停火苗 + 溅雪反馈,N1 修后截图实证) | **达标**(圆绒帽 vs 尖睡帽 + seat1 帽体 −18 灰度差 ≥15/255 断言在跑;R2 帽形通道 100%) | **达标**(远山 + 双层松树视差,截图实证) | **达标**(溢出 0) | **达标**(雪怪 = 帽 + 竖椭圆眼两件,未凑雪宝组合) |
| bumper-cars | **达标**(三层车体 + chibi 司机 + 小旗,截图放大实证) | **达标**(反射斑 0.24 + 内核亮斑、灯串光晕 + 立柱落影,截图实证) | 不适用 | **达标**(围栏立面 + 灯串立柱 + 地面反射斑纵深,截图实证,点名项) | **达标**(溢出 0) | **达标**(车贴花 / 星自家件) |
| bowling-lane | **达标**(细颈宽肩瓶 + 红颈环、三停渐变球 + 指孔,截图实证) | **达标**(球体高光 + 暗面 + 落影三件套,跟球镜头截图实证) | 不适用 | **达标**(球道会聚透视 + 近大远小瓶排 + 邻道暗剪影,截图实证,**点名项**) | **达标**(溢出 0) | **达标**(纯几何布景;C-2 若做霓虹点只图形不字母的提醒在案) |
| ice-fire-forest | **达标**(凛凛雪晶发饰 / 焰焰右偏尖 + 渐变描边五官,截图放大实证) | **达标**(宝石高光点 + 上下分面 + 描边 + 光晕,截图放大实证) | **达标**(灰度裁片:水滴 + 晶饰 vs 火苗剪影完全可分,R3 实证;R2 量化 7.1% / 46.6 全场最佳) | 不适用(格子顶视;地形顶侧双面在位) | **达标**(溢出 0) | **达标**(门徽记复用主角几何,未照原版红蓝菱形) |
| puff-bros | **达标**(呆毛熊渐变 + 围耳 + 咕噜怪高光描边,截图放大实证) | **达标**(糖果四型:纸角圆糖 + 白螺旋棒棒糖上台面,截图实证;`candyGloss` 断言在跑) | **达标**(R2 量化 10.1% / 18.9% / 16.5 三通道 + R3 截图呆毛件清晰;代码零变化) | **达标**(双层云视差 0.15/0.3 + 平台顶条亮边,截图实证) | **达标**(溢出 0) | **达标**(双熊远离吐泡小龙 / 蓝白工装;丸串为通用和果子) |
| prince-princess | **达标**(五母形小怪 + BOSS 七变体参数化骨架,R3 特写渲染 2× / 17px 双档实证) | **达标**(宝石三件套 + 呼吸光圈,截图放大实证) | **达标**(灰度裁片:大冠裤装披风剑 vs 小冠蝶结钟裙星杖,剪影完全可分,R3 实证;R2 量化 30.4% / 50% / 13.9) | **达标**(城堡双层视差 + 灌木近景,截图实证) | **达标**(溢出 0;出场卡完整不裁切) | **达标**(指路徽章 = 自家王冠 / 蝶结几何;禁公主具体组合守住) |

**统计:60 格中 达标 53 / 不适用 7 / 不达标 0。六大专项终验达标率 10/10 满格(不适用格均为口径不适用,非缺陷)。**

### 回归防线终验(加严项逐条)

1. **热区钉死 / reduced 功能提示 / 教育数据快照断言**:全库 15395(+4)例全绿即全部在跑;
   本窗 10 款无教育数据文件;`src/` 自 `49c5c7c` 零 diff = 判定 token 零变化(R2 已做过全量 diff 扫描,链上未再有任何提交)。✅
2. **CPU 4× 节流粒子高峰帧率(全 10 款,不抽样)**:分片协议取 3 有效片最低值 ——
   shoot-range 60.3 / sky-squad **56.0**(全场最低,仍富余 87%)/ tank-battle 60.2 / bomb-buddies 60.3 / snow-fight 60.2 /
   bumper-cars 60.0 / bowling-lane 57.8 / ice-fire-forest 59.8 / puff-bros 60.3 / prince-princess 60.3。**10/10 ≥ 30fps。**✅
   (节流有效性 A/B 自证:同场景 1×/4×/8×/16× = 60.3/52.3/27.2/14.3fps 单调递减。)
3. **`vite preview` 冒烟**:首页 / `sw.js` / `manifest.webmanifest` + 本窗 10 款产物 chunk = **13/13 全 200**;
   10 款逐款进玩法轰炸输入抽玩(普通 + reduced 双通道各 ≥15s)无视觉崩坏,结算屏(胜 / 败两态)渲染正常、败局文案只鼓励(「就差一点点!」实拍)。✅
4. **destroy / rAF / interval 泄漏(全 10 款)**:每款「进 → 玩法 → 退」×5 循环后,活跃 `setInterval` **全部 0**、
   600ms 窗口 rAF 采样 **全部 0**。✅(探针首页基线自证 = 0,排除本底干扰。)
5. **普通与 reduced 双通道 pageerror**:10 款 × 2 通道 = 20 次进玩法会话,pageerror **全部 0**。✅
6. **360×640 三屏**:10 款首页 / 入口(关卡图)/ 局内横向溢出全部 **0px**;结算屏走共享 `showResultDialog`
   在 360×640 实渲染胜 / 败两态(弹窗宽 331px 完整入屏、溢出 0、无文字截断)+ sky-squad / tank-battle 局内 HUD 目检无重叠。✅

---

## 四、终验新发现问题

| 编号 | 位置 | 现象(量化) | 严重度 / 去向 |
| --- | --- | --- | --- |
| R3-a | `src/ui/parentAuth.ts` + `src/ui/dialogs.ts`(共享层,1.2 基线) | 家长验证弹窗开着时用浏览器返回(hash 置空)导航,弹窗节点不随路由销毁:复现 5/5 —— 弹窗继续覆盖首页(`dialogVisible: true`)、500ms 轮询 interval 持续(其自清守卫只在节点脱离文档时触发,而节点一直挂在 body 上);点弹窗自身取消按钮可正常关闭并自清。正常应用内流程(先关弹窗再返回)不受影响 | **一般(非视觉,窗外共享层)**。非本窗 10 款代码、非六大专项、非 1.3 视觉升级引入(1.2 基线行为);三档均被禁改 `parentAuth`,故**登记挂账交主管 / 全库轮次**,不构成本窗视觉放行障碍。修复建议(仅供全库轮次参考):路由切换时对活跃 dialog 统一 `close()`,或 parentAuth 轮询里增加 `location.hash` 变化即 `finish(false)` |
| R3-b | `docs/qa/1.3-window5-round2-fixer.md` 修复统计行 | 笔误:「新增配套用例 17 例(**4 个** `visualFixR2.test.ts` 文件)」实为 **3 个**文件 17 例(snf 5 / pp 9 / bc 3);用例总数数学与合并态完全吻合(741+1+3=745、15367+11+17=15395),纯笔误 | **笔误级**。不改他人报告(纪律),在此登记备查,不影响任何结论 |

**统计:阻断 0 / 严重 0 / 一般 1(R3-a,窗外共享层挂账)/ 笔误 1(R3-b)。本窗 10 款绘制层终验新问题 0。**

---

## 五、终验结论

# **放行**(范围仅本窗 10 款)

依据:

1. **对账清零**:R2 全部新问题(N1–N4)与 B 档一致性点名(C-1)修复确认率 100%,每条有代码 + 截图 / 特写 + 机器化断言三重证据;
   阻断级 / 严重级终态 **0**;C-2 / C-3 为 B 档建议级观感项(「同语言内深浅差」),按 B 档自排轮次登记交本轮 C 档,
   不属六大专项任何一格的不达标,不构成放行前置;其余挂账项(R2-a / L-2–L-5)全部在案未丢且均在窗外或功能文字口径。
2. **六大专项终扫满格**:10 款 × 6 项 60 格,不达标 **0**(达标 53 / 口径不适用 7);
   R1「主角精、配角草」断层已在两轮内补平,本轮终扫确认小怪 / BOSS / 拾取物 / 糖果 / 雪怪与主角同一工艺水位,
   剩余 emoji 全部为 DOM 功能文字口径且有整表水位闸(本轮再加一道守卫)。
3. **回归防线全绿**:`npm test` 746 文件 / 15399 例全绿(三轮只增不减)、零 skip、ratchet 只紧不松;
   `npm run build` 全绿;preview 冒烟 13/13;fps@4× 10/10 ≥30(最低 56.0);destroy 泄漏 10/10 清零;
   pageerror 双通道 20/20 全 0;360×640 三屏(含结算弹窗实渲染)溢出全 0。
4. **分级红线**:终验抽玩与截图未见血 / 伤害 / 死亡描写;败局文案只鼓励(实拍证据);无广告无内购;
   商标黑名单玩家可见内容 0 命中,B 档增量风险提醒(雪宝组合件 / 公主组合 / 奖品架压灰等)逐条对照守住。

唯一登记项 R3-a 为窗外共享层交互尾巴(1.2 基线、非视觉、三档禁改区),已量化挂账交主管,不影响本窗视觉放行。

---

## 六、纪律自查与收尾

- 权限边界:本轮改动仅 `src/games/visualGuardW5R3.test.ts`(+1 文件 / +4 例)与本报告;零绘制代码、零判定数值、
  零 parentAuth / parentGate、他人报告一字未碰(R3-b 笔误仅在本报告登记);
- 分支纪律:工作基于 `origin/game-1.3-window5` tip `5e46214`;动手前计划 commit `bd8e8ed`;
  只普通 push `origin game-1.3-window5`;未动 `main` / `game-1.2` / `game-1.2-kk` / `game-1.3` 主线;未 force;未用 `gh` 开 / 改 PR;
- 收尾:fetch → rebase(保留他人文件,`docs/plan-1.3-*.md` / `.cursor/skills/**` 零触碰)→
  `npm test` **746 文件 / 15399 例全绿** → `npm run build` **全绿**(PWA 195 entries / 5240.35 KiB)→ push;
- 三方合并态复验由本轮 C 档执行,本报告与用例已及时 push 配合;
- 派发指定模型 slug:`claude-fable-5-thinking-xhigh`(实际使用)。
