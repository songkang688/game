# r43 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-189**、**N-190**、**N-191**。下一空号 **N-192**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r42 学习 PR 未合主干时，本轮仍从 N-189 起编；若主干或先合 PR 已占用 N-189+ / 已修 `.rbt-vs-btn` / `.rte-btn`，本号销或顺延，并在笔记勘误。

---

## 红线（抄自 r22–r42 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r43-learn-notes.md` §0。尤其 **不要**再开 `.mmc-open`（N-186）、`.mmc-toggle`（N-187）、`.sks-mode`（N-149）、`.sn-open`（N-180）、`.bl-btn`（N-145）、`.bvp-act`（N-150）、`*-veil-btn`（N-152）、`.pcp-act`（N-183）、`.hh-catch`（N-184）、`.shr-toggle`（N-134）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#115** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 sticky。

---

## 本轮工单

### N-189 · B · `.rbt-vs-btn`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/red-blue-tap/arena.ts` ≈L213；挂载 ≈L637/L646/L935/L944 |
| 现状 | 对战 overlay「再来 / 回关卡」，`padding:12px 24px`，估高 ≈43.2 |
| 不做 | 不改 `.rbt-vs-back`（已 TOUCH）/ 点判定 / 双人隔离带 |
| 验收 | 结算钮高 ≥44；点了仍再来或回关卡 |

### N-190 · B · `.rte-btn`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/red-blue-tap/index.ts` ≈L160 |
| 现状 | 与 vs-btn 同套 CSS；本文件 JS **未挂 class** |
| 不做 | 不改 `.rte-open`/`.rte-back`（已 44）/ 闯关点数 |
| 验收 | 规则写上 `min-height`≥44；即使本轮无按钮，静态绿 |

### N-191 · A · overlay 复制体巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-vs-btn{` 与 `.rte-btn{` 必须 `min-height`≥44 |
| 现状红 | `.rbt-vs-btn`、`.rte-btn` |
| 绿对照 | `.rbe-over-btn` / `.rbv-over-btn`（48） |
| 不做 | 不替代 N-139/152/175/189/190 |

---

## 建议顺序

1. A 先合 **N-191**（或与 B 并行）。
2. B 修 **N-189**、**N-190**（同一游戏，可一 PR）。
3. 回归：主干 N-94/101、N-108、N-87、N-186/187、N-180、N-183/184、N-149 `.sks-mode`。bowling `.bl-btn` 仍走 N-145。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-189 的点击矩形；N-190 若仍无挂载，以 CSS 断言为准。
