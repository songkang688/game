# 三人组 r47 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。`.l99-*` 只给 A。不改 B 游戏文件。不回退安全区 / `--vv-h` / CTA 回卷 / 平板 760 / N-117 徽章 / 五钮 44。N-105 无第四版。

## 390×844 完整闯关（时钟小屋）

路径：首页 → 选关地图（划到底、末章页签）→ 进关 → 暂停 / 攻略 / 返回地图 / 回首页。

| 面 | 滚动 | 热区 |
| --- | --- | --- |
| 首页 | `scrollH` 12893 / 844，`pan-y` 可滚；末卡滚到底 `bottom` 752 &lt; 844 | 卡 / 顶栏图标 / 页签高 ≥44。分类「对战」起 x=366，390 上溢出 |
| 地图 | `.l99-view` 本章 `room=0` 贴合；末章 `room=12`，末格不裁 | 继续 / 工具 / 页签 / ⏸ / 攻略 / 🏠 均 ≥44 |
| 关内 | — | `.l99-back` 44 |
| 暂停 | 弹窗 `transform:none`（等 500ms） | 修前分组把 `.btn` 压成 **44**；修后 **58** |
| 攻略 | `.guide-body` 687 / 381 可滚 | ✕ / 知道啦 44 |

## 本拍缺口与修复（N-201）

1. **首页分类页签划不动**：`.tabs{overflow-x:auto}` 是设计内横滑，但 `.home-screen{touch-action:pan-y}` 沿祖先取交集，把横滑掐掉。390 上看不见「学习 / 动手」。改为 `pan-x pan-y`。不给 `.l99-tabs` 加 `overflow-x:auto`。
2. **暂停列太扁**：触控分组 `.btn, .icon-btn, … { min-height:44px }` 与 `.btn{min-height:58px}` 同优先级、后写获胜，量出来 44。分组后重申 `.btn{min-height:58px}`。不改 `dialogs.ts` 冷静期。

闸：`src/ui/shell.r47.test.ts`。

## 大厅 `*-back`（学习票曾写 N-203）

学习票把「大厅 `*-back`」标成 **N-203**。本分支 **N-203 是结算 overlay 竖滚、N-204 是 915 overlay 收边**，不改号语义去抢票。大厅巡检用 **N-205**（见 `trio-r51-tester-A.md`）。排除 `.l99-*`；钓鱼 `.fs-back`、光球 `.oa-back` 归 B。N-105 无第四版。
