# 1.3 窗口 2 · 第 2 轮视觉验收 · 测试员报告

> 角色：窗口 2 第 2 轮视觉测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货合并态：`0955aa5`（r1 fixer 门禁 721 文件 / 15247 例）。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库、不碰其他窗口目录。
> 权限：只写本报告 + 本窗 9 款目录下新增测试用例（只增不减）。不改绘制代码、不改玩法数值、不改 `src/styles.css` / `src/art/**` / `src/ui/**`。
> 实测环境：node 22.14.0 / npm 10.9.7 / headless Chrome（`/usr/local/bin/google-chrome`，puppeteer-core 25.9.0 `--no-save` 临时驱动，未入库）。截图与探针脚本全部只落 `/tmp`，不入库。

## 复验计划（动手前登记，`cc9bbc3`）

1. **全量跑**：`npm ci` → `npm test` → `npm run build`，记录用例总数与 r1 合并态门禁（721 文件 / 15247 例、PWA 188 entries / 5212.27 KiB）的差值，只增不减。
2. **逐条对账**（本轮主责）：r1 tester 问题清单 9 条（阻断 2 / 严重 1 / 一般 4 / 建议 2）× r1 fixer 修复清单 15 commit × r1 learner TOP10——每条给四种结论之一：已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留。
3. **特别复验**：r1 阻断（mine-garden `[hidden]`+320 溢出、dark-chess 8 列裁切）；r1 严重（junqi-camp 双通道+灰度）；r1 fixer 遗留 2 条给结论。
4. **深挖**：本窗全部 9 款深度走查（角色特写 2×/16px、动效三段链 + reduced 退化、场景四层、一致性、CPU 4× 节流性能）。
5. **回归防线**：热区挪动 / reduced 提示丢失 / 教育数据被改 三类逐一排查；destroy ×5；r1 机器化用例全在跑全绿。
6. **产出**：本报告五节 + 第 2 轮视觉契约用例（只增不减）。
7. 收尾：fetch → rebase → `npm test` / `npm run build` 全绿 → 普通 push，合并态 SHA 写进报告。

---

## 一、全量跑数据与两轮用例数对比

| 项 | 结果 |
| --- | --- |
| `npm ci` | ✅ 通过（4.8s） |
| `npm test`（收货基线 `0955aa5`） | ✅ **721 文件 / 15247 例全绿**（179.5s）——与 r1 fixer 门禁逐字一致，只增不减基线成立 |
| `npm test`（本轮收尾，含新增用例） | ✅ **730 文件 / 15261 例全绿**（176.5s，**+9 文件 / +14 例**，见新增用例清单） |
| `npm run build` | ✅ 全绿；PWA precache **188 entries / 5212.27 KiB**（与 r1 门禁逐字一致） |
| `npx vite preview` 冒烟 | ✅ HTTP 200；9 款深链全部可进第 1 关盘面，360/320 两档 **0 条 pageerror** |

**两轮用例数对比**：r1 收尾合并态 721 / 15247 → r2 收货 721 / 15247（中间零丢失）→ r2 收尾 **730 / 15261**。r1 沉淀的机器化用例**全部还在跑、全绿**：9 个 `round1-visual-scan.test.ts`（38 例）、8 个 `round1-fix.test.ts`（27 例）、5 个 `round1-packa.test.ts`、`window2-r1-learner-copy.test.ts`（4 例）——共 23 文件逐一清点在案。

本轮新增用例（**9 文件 / 14 例**，全部锁「已验证达标性质」，发现的问题只立案交 C 档、不写红灯用例）：

| 文件 | 例数 | 锁什么 |
| --- | --- | --- |
| `mine-garden/round2-visual-verify.test.ts` | 1 | drawMini 收敛公式上下限（容器 2000px→≤300、容器 60px→96 兜底） |
| `junqi-camp/round2-visual-verify.test.ts` | 1 | **12 兵种 × 双方**翻开面全带 `jq-mark` 且去色互异（r1 只钉了司令一种） |
| `dark-chess/round2-visual-verify.test.ts` | 2 | 同兵种铺 32 格渐变 id 两两互异且引用配对；≤400px 收缩契约（gap/padding/border/点数行） |
| `chess-garden/round2-visual-verify.test.ts` | 1 | cg- 提级追加段排在 420px 回降规则**之后**（层叠顺序，窄屏 computed 才真 ≥ 提级值） |
| `dot-maze/round2-visual-verify.test.ts` | 2 | r=8（16px 直径）三尖裙边不退化、呆毛 1.5px 下限、三态序列互异 |
| `fruit-stack/round2-visual-verify.test.ts` | 2 | 盆沿内衬与座位条字色同源、3px×上 60% 高、落在 drawRim 纯视觉层 |
| `pool-stars/round2-visual-verify.test.ts` | 2 | 320/360/768 球径 ≥ MIN_BALL_PX；≥380px 视口 fontPx≥14 |
| `sudoku-petal/round2-visual-verify.test.ts` | 1 | 精 2D：index/art 源码零 perspective/rotate3d/matrix3d/translateZ/three |
| `merge-2048/round2-visual-verify.test.ts` | 2 | 精 2D 零透视关键字；mgshake/mgflash 反馈通道 + reduced 分级入口都在 |

---

## 二、第 1 轮问题逐条对账表（本轮主责）

对账口径：r1 tester 立案 9 条 + r1 fixer 追加修复 4 条（B 档 TOP10 尾部）+ r1 fixer 遗留 2 条，共 12 行。四种结论：**已修复 / 修复不彻底 / 修出回归 / 合理遗留**。

| # | 来源·级别 | 款·问题 | 结论 | 验证证据 |
| --- | --- | --- | --- | --- |
| 1 | A 5-1 · 阻断 | mine-garden 小地图 `[hidden]` 失效 + 320 越界 | ✅ **已修复** | 实测第 1 关（窄盘）`hidden=true` 且 **computed display:none**（360/320 双档）、盘下空底板消失（截图）；**宽盘可见态**（中级林 16×16 @320）canvas 属性宽 240 / 右缘 281 < 320 不越界；`round1-fix` 2 例在跑；本轮补上下限边界 2 断言 |
| 2 | A 5-2 · 阻断 | dark-chess 8 列 360/320 裁切 | ⚠ **修出回归**（原阻断清零，新副作用暴露） | 原问题已解：360/320 实测 32 格全部在视口内（offscreen 0）、首行 8 格中心命中 8/8。**但** `.dc-cell{min-height:44px;aspect-ratio:1/1}` 在收缩轨道（列宽 31.0px@360 / 27.1px@320）上被 Chrome 传导成 **44×44 固定格**：相邻格横向**重叠 9.98px@360 / 13.88px@320**（cell0.right 89.4 > cell1.left 79.4），有效触区宽只剩 ≈34/30px（< 40px 红线）；末列右缘 327.6@360 / 295.9@320，**出棋盘框 6.0/9.9px**（特写截图：末列牌面盖过棋盘圆角框）。r1 修复前格子不互叠（只是被裁）；互叠是本次修复引入 → 新立案 r2-1 交 C 档 |
| 3 | A 3-1 · 严重 | junqi-camp 双方 16px 灰度不可分、形状通道缺位 | ✅ **已修复** | 形状通道：`sideMarkSVG` 朵朵圆瓣花 / 星星尖角星，实测明棋第 1 关盘上 7 枚翻开面 **7 个 `jq-mark`** 全在（DOM 取证）；本轮把 12 兵种 × 双方全量钉死（去色后互异）。旗形互异（crest/hq/hoist）契约在跑。颜色通道：面板渐变实测灰度差顶停 **Δ≈21.0**、底停 **Δ≈32.7**（≥20 达 fixer 口径；未到 B 档建议的 ≥40，因形状+颜色双通道已成立判达标，灰度余量登记备注供 C 档酌情再拉） |
| 4 | A 2-1 · 一般 | chess-garden 棋子主体平涂 0 渐变 | ✅ **已修复** | `duoTone` 三层（本色描边 → 暗纱 rgba(56,34,16,.14) → matrix(.92) 顶部内缩亮层）6 兵种全上；4× 特写：蘑菇兵帽顶亮、底右留暗圈，光源左上不变；零 defs/id |
| 5 | A 2-2 · 一般 | dark-chess `<defs>` 固定 id 32 格撞车 | ✅ **已修复** | `backSVG` 三停层叠实心零 defs/url(#/id（契约在跑）；`pieceFaceSVG` id 拼格号——本轮加固：同兵种铺 32 格 **id 集合大小 = 32** 且 url(#) 引用与自身 defs 严格配对 |
| 6 | A 5-3 · 一般 | chess-garden 坐标 9px / 角标 11px | ✅ **已修复** | 实测 computed：`.cg-coord` **10.5px**×16、`.cg-piece-tag` **12px**（360/320 双档）；styles.css 追加段压得住 420px 回降块（本轮层叠顺序契约钉死） |
| 7 | A 5-4 · 一般 | 六款 HUD chips/tip/note/pick <14px | ⚠ **修复不彻底** | 点名类六款 6 commit 已覆盖且契约在跑（dmz/fs/ps/jq/dc-chip 等实测 ≥14px）。**尾巴 ①**：pool-stars `.ps-tip` 实测 360/320 仍 **13px**——CSS 提到 14px 后被 `view.ts:90` `tableLayout` 的 `fontPx: w<380 ? 13 : 14` **内联样式盖掉**（`view.ts:525`），修复只动了 CSS 没动运行时来源 → r2-3。**尾巴 ②**：A 档点名的「各款 pick 按钮」漏了 dark-chess `.dc-pick` 13.5px 与 chess-garden `.cg-pick` 13.5px（fs/ps/jq 的 pick 已提级）→ r2-4 |
| 8 | A 1-1 · 建议（fixer 遗留 1） | dark-chess `textContent` 死代码 | ☑ **合理遗留（结论已给）** | 复核结论：真实 DOM 里 `textContent` 赋值总是被紧随的 `innerHTML` 整体覆盖（实测翻开面 textContent 为 SVG 内文本），「SVG 失效时的降级」在真浏览器**不成立**——该文本层实为**测试桩契约**（测试 DOM 桩把 textContent/innerHTML 存两个字段，`art.test.ts:73` 等断言依赖它）。删除需改既有断言，违反只增不减；对玩家零影响（不渲染、不吃性能）。维持遗留，建议第 3 轮把桩断言口径换成 aria-label 后一并清理 |
| 9 | A 6-1 · 建议 | dot-maze 幽灵剪影与街机同构 | ✅ **已修复** | 三尖裙边（tooth=r/1.5）+ 头顶呆毛（darken 20% 描边）落地，fright 同步、eyes 不带；本轮加固 16px 直径不退化 + 三态互异；四色淡彩契约未动 |
| 10 | B #9 · 建议 | fruit-stack 双盆本体无差异 | ✅ **已修复** | 盆沿内侧 3px 座位色内衬（#a8306a/#28568f）实测可见（360 截图）；本轮钉死「与座位条字色同源 + 上 60% 高 + 纯视觉层」 |
| 11 | B #10 · 建议 | merge-2048 盘外留白偏素 | ✅ **已修复** | 两颗淡星点入背景栈、radial 总数恰 6、列于不透明渐变之前（r1 契约 3 例在跑）；360 截图星点可辨 |
| 12 | fixer 遗留 2 | sudoku-petal 行/列/宫完成缺轻反馈 | ☑ **合理遗留（结论已给）** | 复核：仍未实现（预期内）。补充勘察：完成瞬间的判定其实可以在 view 渲染层由前后 state diff 推出（不必动玩法状态机），B 档给过 CSS transition 方案 + reduced 跳终态口径——建议本轮 C 档按「只动渲染层」重估；若仍判越权则继续遗留 |

**对账统计：已修复 8 · 修复不彻底 1（A 5-4 尾巴）· 修出回归 1（A 5-2 格间重叠）· 合理遗留 2（结论均已给）。**
r1 阻断/严重修复确认率：**mine-garden 阻断 100% 修复；junqi 严重 100% 修复；dark-chess 阻断的「不可见不可点」100% 清零**，但重叠副作用按验收标准「修复不彻底的重新立项给 C 档」处理（r2-1）。

---

## 三、9 款深度走查表（五项 × 9 款，本窗全覆盖）

抽样规则：9 款全走，不抽样、不跨窗。每款「深链 → 模式 → 第 1 关盘面」，特写用 dpr3–4 截图（等效 2× 放大）+ 16px 用小半径机器化断言/矩阵缩看；动效链读码核对「预告 → 过程 → 收尾」三段与 reduced 分支；性能为 CPU 4× 节流下 headless 实测（rAF 计帧 2s，60Hz 上限）。

| 款 | 角色特写（2× / 16px） | 动效完整链（预告→过程→收尾 / reduced 退化） | 场景层次（背景/盘面/主体/HUD） | 一致性（左上光照/描边/粉彩/三态统一） | 性能（CPU4× 静置/高峰 fps） |
| --- | --- | --- | --- | --- | --- |
| merge-2048 | ✅ 2×：块面三停渐变+左上提亮、星角标描边+高光点清晰；16px：色阶+角标双通道仍可辨（角标 5 档互异契约在跑） | ✅ 新块 mgpop（预告）→ 合并 mgburst/mgflash+撞墙 mgshake（过程）→ 飘分/里程碑（收尾）；reduced：粒子分级入口+transition 缩时，反馈通道不消失（本轮契约钉死） | ✅ 星点渐变底 / 木框盘 / 宝石块 / HUD chips 四层分明；星点静饰克制（6 颗上限契约） | ✅ 135° 左上提亮；同款菜单/局内/结算统一 | 60.2 / 60.2（连发合并中） |
| mine-garden | ✅ 2×：盛开花外瓣+内高光瓣 5+5、花芯高光点；旗三角旗面+褶皱；16px：旗面实心三角是第一剪影、种子点数量×形状双通道 | ✅ 光标预落波纹（预告）→ 翻格 mnplant/mncrumb（过程）→ 绽放 mnpop/胜利（收尾）；reduced 12 处接线正常 | ✅ 草地底 / 浮雕格 / 花与旗 / HUD+小地图 四层分明；**小地图收起时零占位**（本轮实测） | ✅ 顶亮底暗；三档花互异契约在跑 | 60.4 / 60.1（连点翻格） |
| sudoku-petal | ✅ 2×：花瓣三层灰度递增、枯叶卷边形状通道；16px：宫色+花瓣层次仍可辨 | ✅ 选格高亮（预告）→ 错误抖动/数字键翻面（过程）→ 通关礼花（收尾）；reduced 14 处；**行列完成轻反馈缺位仍是链上唯一豁口（遗留 #12）** | ✅ 田埂木框 / 九宫 / 数字花瓣 / HUD 分明 | ✅ 翻面纯压扁零透视（本轮契约钉死）；注释明示免 defs/id | 60.3 / 60.1（连点+按键） |
| dot-maze | ✅ 2×：玩家渐变身体+呆毛+单大眼、幽灵三尖裙边+呆毛+顶部高光；16px（r=8 机器断言）：三齿不退化、呆毛 1.5px 下限、三态互异 | ✅ 能量豆脉动/幽灵警戒（预告）→ 移动吃豆/变身闪白帧（过程）→ 飘字/委屈脸重来（收尾）；reduced 7 处 | ✅ 深底星点 / 迷宫墙 / 豆与角色 / HUD 分明；豆阵即装饰不抢戏 | ✅ 渐变心左上 (-0.24r,-0.3r)；瞳孔随向 | 60.5 / 60.1（方向键连发） |
| fruit-stack | ✅ 2×：果卡三停径向+梗叶破圆+双态脸、盆沿座位色内衬清晰；16px：圆+顶部一点绿仍是果 | ✅ 下一颗预览+警戒线标（预告）→ 落体/合并爆汁+扩散环（过程）→ 金星/飘字/震屏（收尾）；reduced 23 处全窗最多 | ✅ 天空渐变+云 / 陶瓷盆 / 果堆 / HUD 四层分明；密度全窗最高但不喧宾 | ✅ 高光 (-0.35r,-0.4r)；11 hi 色互异契约在跑 | 60.2 / 60.2（连续投果） |
| pool-stars | ✅ 2×：球三停+spot 高光、花印/星印压印清晰；16px：退化形圆点 vs 菱形点契约在跑；球径 ≥14px 三档钉死（本轮） | ✅ 瞄准线+力度条脉动（预告）→ 出杆滚动（过程）→ 进袋星光（收尾）；reduced：13 处、犯规抖动关停 | ✅ 木框 / 库垫+台呢径向 / 球 / HUD 四层分明，全窗层次最完整 | ✅ 高光 (-0.34r,-0.38r)；**`.ps-tip` 360/320 实测 13px（r2-3 立案）** | 60.4 / 60.2（拖杆出杆） |
| junqi-camp | ✅ 2×：军衔条金星描边、**左下角形状角标**（花 vs 星）不遮汉字不挡落点绿点；16px：角标+灰度差 21–33 双通道可分（本轮 12 兵种全量钉死） | ✅ 选中/落点绿点（预告）→ 走子滑动 jq-glide/对撞烟云（过程）→ 升旗 hoist（收尾）；reduced 6 处 | ✅ 板底 / 铁轨枕木网+地形件 / 棋面 / HUD 四层分明；HUD 徽旗双方旗形互异实测可辨 | ✅ 面板顶亮；牌背双方逐字节同一张（暗棋红线契约在跑） | 60.1 / 60.1（连点走子） |
| chess-garden | ✅ 2×：蘑菇兵 duoTone 顶亮+帽点+高光椭圆、底座投影；16px：六种头部剪影（冠/花/垛口/马头/尖树/圆帽）+白黑 Δ≈174 | ✅ 绿芽/红三角双形状提示（预告）→ 吃子花瓣 capture-fx（过程）→ 开花/结算插画（收尾）；reduced：演出层 display:none、提示标记（sprout/capmark 非动画）保留 | ⚠ 360 四层分明；**320 下 h 列被 `.cg-board{overflow:hidden}` 裁到只剩 12px、中心点不可点（r2-2 阻断，r1 漏检的存量）** | ✅ 高光全左上象限；坐标/角标提级后 computed 复核达标 | 60.2 / 60.1（连点选子） |
| dark-chess | ✅ 2×：牌背暗字印章+双线框、翻开面象牙渐变+阵营环+战力点；16px：环色+点数主通道成立 | ✅ 提示环/炮虚线弧（预告）→ 翻子 dcFlipIn/走子滑动（过程）→ 吃子花瓣/胜利花瓣雨（收尾）；reduced 4 处、花瓣雨整层不放（装饰层，功能提示 dc-note 实测保留） | ⚠ 板底/格阵/牌面/HUD 分明；**但格间横向重叠 10–14px、末列出框 6–10px（r2-1 严重）** | ✅ 渐变心 cx.38 cy.3 左上；14 汉字互异契约在跑 | 60.2 / 60.1（连点翻子） |

**横向复核**：光照左上 45° 9/9 同向；`prefers-reduced-motion` 仿真下 9 款进盘 **0 JS 错误**、功能提示（msg/tip/note/chip 类）**9/9 可见**；粒子/翻面高峰全部 60fps（红线 ≥30）。

---

## 四、本轮新发现问题清单（全部量化，只立案，交 C 档修）

| # | 位置 | 现状（量化） | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| r2-1 | `src/games/dark-chess/view.ts:38`（`.dc-cell{min-height:44px;aspect-ratio:1/1}`） | 轨道收缩后格子仍 44×44（aspect-ratio 把 min-height 传导进宽度）：相邻格横向重叠 **9.98px@360 / 13.88px@320**，牌面互相盖右缘、末列出棋盘框 **6.0px@360 / 9.9px@320**（特写截图坐实）；有效触区宽 = 列距 **≈34px@360 / 30px@320**（< 40px 红线，且低于 r1 fixer 自评的 ≥35px） | 格尺寸跟随轨道（如 `min-height:0` + 高度由 aspect-ratio 出），44px 触控红线改由**扩展点击区**保住（`::after{position:absolute;inset:-7px}` 之类零视觉方案），或 ≤400px 断点把 gap 2px/padding 2px 再还格宽并登记实测值 | **严重**（修复引入） | `src/games/dark-chess/view.ts` |
| r2-2 | `src/styles.css:1807`（`.cg-sq{min-width:40px}`）+ `:2044`（420px 断点 `.cg-board{margin-inline:-10px}`）+ `.cg-board{overflow:hidden}` | 320px 视口：`min-width:40px` 使 `repeat(8,1fr)` 轨道最小 40px，8 列内容 320px > 板箱 292px，被 `.cg-board` 自己的 overflow:hidden 裁在 306px——**h 列仅 12px 可见（40px 格裁掉 70%）、格中心 elementFromPoint 落空不可点**（实测 `centerHitsSameCell:false`；截图：h7 黑兵被拦腰截半）。360px 恰好放得下不受影响。存量（`.cg-sq` min-width 与负外边距均为 1.2 遗产），**r1 320 扫描漏检**，本轮深挖暴露 | `.cg-sq{min-width:0}` 让 1fr 真收缩（320 下格宽 ≈36.5px），触控靠整格 aspect-ratio 面积保住并登记实测值；或 ≤340px 断点收 `.cg-mode` 内边距还宽度 | **阻断（存量·r1 漏检）** | `src/styles.css`（cg- 段，测试员无权动，交 C 档） |
| r2-3 | `src/games/pool-stars/view.ts:90`（`tableLayout` 的 `fontPx: w < 380 ? 13 : 14`）+ `:525`（`tipBox.style.fontSize` 内联） | `.ps-tip` 的 CSS 已提 14px，但每次 resize 被内联样式改回 **13px**（360/320 实测 computed 13px）——A 5-4 修复被运行时来源架空 | `fontPx` 下限提到 14（`w < 380 ? 14 : 14` 即删分支），或窄屏不写内联字号交给 CSS | 一般 | `src/games/pool-stars/view.ts` |
| r2-4 | `src/games/dark-chess/index.ts:35,43,45`（`.dc-sub`13px / `.dc-tip`12px / `.dc-pick`13.5px）+ `src/games/chess-garden/index.ts:61,66`（`.cg-pick`13.5px / `.cg-over-s`13.5px） | 两款模式菜单/结算的正文与 pick 按钮仍 <14px（A 5-4 点名过「各款 pick 按钮」，fs/ps/jq 的已提级、这两款漏网）；均为局外屏，360 实测无裁切但字号低于红线 | 五类统一提 ≥14px（同 5-4 口径） | 一般 | `src/games/dark-chess/index.ts`、`src/games/chess-garden/index.ts` |
| r2-5 | `src/games/dark-chess/view.ts:100`（`.dc-count span` 12px） | 记牌面板文本层 12px；实际画面以 SVG 图标层为主、文本层兼读屏正文（aria-label 同文） | 提 14px 或登记豁免（图标层为主的辅助文本）；C 档权衡 | 建议 | `src/games/dark-chess/view.ts` |
| r2-6 | `src/games/junqi-camp/view.ts:120`（`.jq-empty .jq-face` 13px） | 空位点位字 13px（装饰性点位提示，非正文） | 提 14px 或登记豁免 | 建议 | `src/games/junqi-camp/view.ts` |
| r2-7 | `src/styles.css:1953`（`.cg-log-row` 13px，420px 断点内同值） | 记谱行 13px（1.2 存量，A 5-3/5-4 均未点名，本轮补登记；`cg-log-sum` 同 13px） | 提 14px（与 cg-tip 同口径追加覆盖） | 建议 | `src/styles.css`（cg- 段，交 C 档） |

**统计：阻断 1（r2-2，存量漏检）· 严重 1（r2-1，修复引入）· 一般 2（r2-3、r2-4）· 建议 3（r2-5/6/7）。**
按验收标准「本轮新发现的阻断级当轮清零」：r2-2 已立案待本轮 C 档修（测试员无权动 `styles.css`）。

**回归防线三类排查结论**：
- **判定热区被挪动**：15 个修复 commit 逐一 `git show --stat` 审计，只碰 index/view/art/styles.css(cg 追加)/domStub/测试，判定与数值文件（rules/board/ai/levels/parentAuth）零触碰；唯二热区变化即 r2-1（dark-chess 格互叠使右缘 10–14px 被邻格接管）与 r2-2（chess-garden 320 h 列，存量）——均已立案。fruit-stack 盆沿内衬实测纯绘制（碰撞盒 `lv.box` 不变）。
- **功能提示被动画吃掉**：reduced 仿真下 9 款实测功能提示全部可见（junqi 落点绿点、chess-garden sprout/capmark 是静态标记不受 animation:none 影响、dark-chess dc-note 在）；被整层关掉的只有纯装饰（dc-rain 花瓣雨、cg-capture-fx/bloom 演出）。
- **教育/玩法数据被改**：`git log origin/game-1.3..HEAD` 全部 71 commit 无一触碰教育数值/关卡/存档 key；仅有的 2 处既有断言口径更新（dark-chess smoke 列模板、junqi 证据翻转）r1 fixer 已登记。
- **destroy 泄漏 ×5**：9 款「进盘→退出」×5 实测，DOM 节点数逐款恒定（777–779）、window 监听器计数恒定（净 +1 为壳层常驻），**0 泄漏**。

---

## 五、六大专项两轮达标率对比

矩阵口径与 r1 相同：❌ = 该格存在阻断或严重级问题；✅ = 通过（可带一般/建议级登记）。

| 款 \ 专项 | ①火柴人 | ②金币体积 | ③双人区分 | ④精2D | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| merge-2048 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mine-garden | ✅ | ✅ | ✅ | ✅ | ✅（r1 ❌→修复） | ✅ |
| sudoku-petal | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| dot-maze | ✅ | ✅ | ✅ | ✅ | ✅ | ✅（6-1 已修） |
| fruit-stack | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| pool-stars | ✅ | ✅ | ✅ | ✅ | ✅（r2-3 一般） | ✅ |
| junqi-camp | ✅ | ✅ | ✅（r1 ❌→修复） | ✅ | ✅ | ✅ |
| chess-garden | ✅ | ✅（2-1 已修） | ✅ | ✅ | **❌**（r2-2 阻断·存量漏检） | ✅ |
| dark-chess | ✅（1-1 遗留·建议） | ✅（2-2 已修） | ✅ | ✅ | **❌**（r2-1 严重·修复引入） | ✅ |

| 专项 | r1 达标 | r2 达标 | 变化 |
| --- | --- | --- | --- |
| ① 火柴人/草稿圆 | 9/9 | 9/9 | 持平（遗留 1-1 为建议级死代码） |
| ② 金币体积 | 9/9（一般 2 条登记） | 9/9（一般 2 条已修） | 质量提升：chess-garden 补层叠明暗、dark-chess id 撞车清零 |
| ③ 双人可区分 | 8/9 | **9/9** | junqi 严重项修复（形状+颜色双通道，12 兵种全量钉死） |
| ④ 精 2D | 9/9 | 9/9 | 持平；本轮新增 2 款机器化零透视契约 |
| ⑤ 360px | 7/9 | 7/9 | **结构变化**：mine-garden ❌→✅、dark-chess 阻断→严重残留（r2-1）、chess-garden 漏检暴露为 ❌（r2-2） |
| ⑥ 商标 | 9/9 | 9/9 | 修复新增造型（三尖裙边/角标/提亮层）复扫零命中；黑名单 grep 9 款非测试代码 0 命中 |
| **合计** | **51/54（94.4%）** | **52/54（96.3%）** | 净 +1 格；r1 的 3 个 ❌ 修掉 2 个半（dark-chess 降级残留），深挖新暴露 1 个存量 ❌ |

---

## 附：分级红线复核与收尾登记

- 红线：9 款盘面/文案/新修复造型复查——无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；主角只有朵朵/星星；商标黑名单（4399/马里奥/皮卡丘/托马斯/Kitty/Battle City 等）非测试代码 **0 命中**。
- kit 纪律：`src/art/` 目录仍不存在，无人拷入窗口 1 文件；本轮测试员未改绘制代码、未改 `src/styles.css`。
- 收尾：fetch → rebase（保留他人全部文件）→ `npm test` **730 / 15261 全绿** → `npm run build` 全绿（188 entries / 5212.27 KiB）→ 普通 push。
- 合并态 SHA：收货 `0955aa5`；本轮提交链 `cc9bbc3`（复验计划）→ `2965855`（r2 契约用例 9 文件 14 例，收尾全量跑于此态）→ 本报告收尾 commit 落于其上。
