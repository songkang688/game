# 1.3 窗口 2 · 第 2 轮视觉验收 · 测试员报告

> 角色：窗口 2 第 2 轮视觉测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库、不碰其他窗口目录。
> 权限：只写本报告 + 本窗 9 款目录下新增测试用例（只增不减）。不改绘制代码、不改玩法数值、不改 `src/styles.css`、不改 `src/art/**`、不改 `src/ui/**`。

## 复验计划（动手前登记）

1. **全量跑**：`npm ci` → `npm test` → `npm run build`，记录用例总数与 r1 合并态门禁（721 文件 / 15247 例、PWA 188 entries / 5212.27 KiB）的差值，只增不减。
2. **逐条对账**（本轮主责）：r1 tester 问题清单 9 条（阻断 2 / 严重 1 / 一般 4 / 建议 2）× r1 fixer 修复清单 15 commit × r1 learner TOP10——每条给四种结论之一：已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留。
3. **特别复验**：
   - r1 阻断：mine-garden `[hidden]` 失效 + 320px 溢出（`0fa590d`）；dark-chess 8 列 360/320 裁切（`f0930a0`）；
   - r1 严重：junqi-camp 双方形状第二通道 + 灰度差（`9811355`/`147f48f`）；
   - r1 fixer 遗留 2 条给结论：dark-chess `textContent` 死代码；sudoku-petal 行列完成轻反馈。
4. **深挖**：本窗全部 9 款深度走查（角色特写 2×/16px、动效三段链 + reduced 退化、场景四层、一致性、CPU 4× 节流性能），逐款五项打表。
5. **回归防线**：判定热区被挪动 / 功能提示被动画吃掉（reduced 下必须还在）/ 教育玩法数据被改，三类逐一排查；destroy 进退 ×5；r1 机器化用例（round1-visual-scan / round1-fix / learner copy）全部还在跑、全绿。
6. **产出**：本报告五节（全量跑数据、对账表、9 款深度走查表、新问题清单、六大专项两轮达标率对比）+ 新增第 2 轮视觉契约用例。
7. 收尾：fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push。合并态 SHA 写进报告。

（以下各节复验完成后填写。）
