# r49 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-207**（B `.oa-back`）、**N-208**（B 钓鱼 `.fs-back`）、**N-209**（A `*-back` 巡检，不含 `.l99-*`）。下一空号 **N-210**。

**纪律**：`.l99-*` **只给 A**。不要占用 A 已用的暂停 / overlay / 首页横滑语义。

**撞车**：先合入 `game-1.3` 的为准。

**A 已占用（学习禁止再写这些义）**

| 号 | 义 |
| --- | --- |
| N-201 | 首页 `pan-x pan-y` + 暂停 `.btn` 58 |
| N-202 | 暂停弹窗矮横屏收边 |
| N-203 | 结算 overlay 可滚 |
| N-204 | overlay 矮档钉 CTA |

r48 学习号 N-204…206 让路，热区改挂本轮。

---

## 红线

1. C 只交两份 md。
2. **B 禁止修改 `level99.ts`、任何 `.l99-*`、暂停 overlay、`dialogs.ts`、`.home-screen` 的 `touch-action`。**
3. N-110…116 跳过。已合号只回归。
4. 在途：#94 / #96 / #99 / #104 / **#105～#121** / #107。
5. 勿改 `SKY_H`。勿抬 `.shr-toggle`（36）。勿改 fruit-stack `.fs-back`。

---

## 本轮工单

### N-207 · B · `.oa-back`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/orb-arena/index.ts` ≈L115；挂载 ≈L953 |
| 现状 | padding 7+字 14，无 min-height |
| 不做 | 不改暂停 / overlay / 横滑 / `.l99-*` |
| 验收 | 大厅返回 ≥44 |

### N-208 · B · fishing-star `.fs-back`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/fishing-star/index.ts` ≈L227；挂载 ≈L2035 |
| 现状 | padding 6+字 13，无 min-height |
| 不做 | 不改暂停弹窗（N-202）；不改 fruit-stack 同名类 |
| 验收 | 钓鱼「回选关」≥44 |

### N-209 · A · `*-back` 巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-back{` 可点块 min-height≥44 |
| **排除** | `.l99-*`（N-198）；不测 overlay 可滚（N-203）；不测暂停弹窗 |
| 本轮应变绿 | N-207 / N-208 |

---

## 建议顺序

1. A 守 N-201～N-204 壳层，并行 N-198 / N-209。
2. B 修 N-195、N-199、N-207、N-208，分 PR。
3. 回归：横滑仍 `pan-x pan-y`、暂停钮 58、overlay 可滚不回退。

---

## 本拍未覆盖

无头 Chrome 未跑。B 补测光球/钓鱼返回。A 勿把本轮号写进暂停/overlay/横滑闸。
