# 1.3 窗口 2 · 视觉收官 backlog（本窗专用）

> 建档：窗口 2 第 3 轮终验修复员（C 档）。只覆盖本窗 9 款（`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`）。
> 口径：登记「一般 / 建议级、本窗三轮内未修或不宜在终验轮扩面修」的项，供 1.3 收官后清理；不覆盖全库 backlog。阻断 / 严重级终态为 0，无此类条目。

## 一、待清理项（3 条）

| # | 级别 | 来源轮次 | 位置 | 现状 / 期望 | 改动面 |
| --- | --- | --- | --- | --- | --- |
| 1 | 建议（代码卫生，零玩家影响） | r3 fixer 读码自查（与已清零的 r1 遗留 1 同家族） | dark-chess `view.ts` `refresh()` 记牌面板：`s.textContent = line` 后紧跟 `s.setAttribute("aria-label", line)` 与 `s.innerHTML = counterIconsHTML(...)` | 面板 span 与已清零的格子桩同款双写模式：textContent 在真实 DOM 里必被 innerHTML 抹掉，读屏正文实际由同串 aria-label 承担，双写无害但口径冗余。期望：收官后与格子层同口径清理（删 textContent 写入，`smoke.test.ts` `lines()`/`art.test.ts:299` 两处断言换 aria-label 口径） | view.ts 1 行 + 2 个测试文件断言口径；A 档 r3 曾以 textContent+aria-label 双断言验证过该面板，终验轮不扩面动它 |
| 2 | 建议（B 档可选案，自述「不改也不算违和大项」） | r2 learner #7 可选案 → r2 fixer #14 登记本轮不动 | merge-2048 `index.ts` badge（角标） | 期望（可选）：badge 换 999px 圆丸统一家族圆角语汇 | 涉 `overflow-wrap:anywhere` 既有契约与 320 收窄断点回归面，改动收益低、回归面不小，维持登记 |
| 3 | 登记跟踪（共用件，不在本窗 9 款目录） | r1 tester 5-4 核准豁免 → r3 tester 备 1 沿用 | `src/games/level99.ts` `.l99-star` 12px（选关星标，各款 3 处可见） | 装饰字符豁免口径成立（非正文）；若后续全库统一字号红线口径，跨窗一并处理 | 共用件动一处牵全库 39 款，须由包工头层面统筹，不归本窗 |

## 二、已裁定不入册项（防复查重复立案）

- **dark-chess `setCellContent` textContent 测试桩**（r1 遗留 1 / r2 fixer 移交 / A 档 r3 对账 #12 合理降级）：**r3 fixer 已清零**（`166f926`，只动测试断言口径 + 最小绘制改动换 aria-label，用例只增不减），不再是 backlog 项。
- **merge-2048「方块静饰改圆点」**（r2 learner #7 前半）：r2 fixer 核实前提不成立（`.mg-wrap::before/::after` 带 clip-path 十顶点五角星，本就是星形角饰），**有据不采纳**，A 档 r3 对账 #10 复核维持，已闭环。
- **chess-garden `.cg-coord` 10.5px / `.cg-piece-tag` 12px**（r3 tester 备 2）：r1 A 5-3 核准的「40px 格内辅助标注」口径，非缺陷。
- **chess-garden 360px 格宽实测 40.4px 与 fixer 登记 41.4px 差 1px**（r3 tester 备 3）：壳卡圆角内衬取整，触控 ≥40px 红线保持、64/64 中心命中，无需处置。
