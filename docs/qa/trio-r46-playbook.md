# r46 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-198**（A）、**N-199**（B）。**N-200** = B 去修已开的 **N-195**，不新开选择器。下一空号 **N-201**。

**勘误**：`.l99-*` **只给 A**。r45 N-196 对 B 作废。

**撞车**：先合入 `game-1.3` 的为准。

---

## 红线

1. C 只交两份 md。
2. **B 禁止修改 `src/games/level99.ts` 以及任何 `.l99-*`。**
3. N-110…116 跳过。已合号只回归。
4. 勿重开 r45 §0 并号（除本轮点名的 `.bl-btn` 漏网）。
5. 在途：#94 / #96 / #99 / #104 / **#105～#118** / #107。
6. 勿改 `SKY_H`。勿抬 `.shr-toggle`（36）。

---

## 本轮工单

### N-198 · A · `.l99-*`

| 项 | 内容 |
| --- | --- |
| 文件 | 只 `src/games/level99.ts` |
| 含 | `.l99-continue` / `.l99-back` / `.l99-tool` / `.l99-tab` / `.l99-ov-btn` |
| 现状 | 均无 min-height（continue 估高 ≈34） |
| 不做 | 不改跳关逻辑 / 188 关表；不派给 B |
| 验收 | 壳层钮 ≥44；点了仍继续/返回/切工具 |

### N-199 · B · `.bl-btn`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/bowling-lane/index.ts` ≈L108 |
| 现状 | padding 6+字 13，无 min-height |
| 不做 | 不改 `.bl-pick`（已 44）/ 投球 |
| 验收 | HUD `.bl-btn` ≥44 |

### N-195 · B · `.shr-back`（r45 已开，本轮当第二单）

见 r45 playbook。不要写成新的 `.l99-*`。

---

## 建议顺序

1. A 立刻收 **N-198**（含原 N-196 的 continue）和 **N-197**。
2. B 修 **N-195**、**N-199**，分 PR。
3. 回归：N-134 toggle 仍 36、N-135 四款返回、N-173 `.pk-go`、N-192 地图格。

---

## 本拍未覆盖

无头 Chrome 未跑。A 必须真机量 l99 壳层钮；B 补测 `.bl-btn` 与 `.shr-back`。
