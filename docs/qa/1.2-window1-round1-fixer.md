# 窗口 1 · 第 1 轮 · 监督修复员报告

> 分支：`game-1.2-window1`　范围：窗口 1 平台层 + 12 款新游戏
> 本文件是修复员**独占**文件。tester / learner 报告不由本文件作者改动。

## 〇、修复计划（动代码前提交的那一版）

### 范围

- 平台：`src/ui/root12Contract.ts`、`src/ui/rootGate.ts`、`src/ui/home.ts`（手游/端游筛选）、
  `src/ui/homeFilters.ts`、`src/ui/mobileText.ts`、`src/engine/playModes.ts`、`src/engine/view25d.ts`、
  `src/games/level99.ts`、`src/games/quiz99.ts`。
- 12 款：`orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate`
  `hero-cards` `weiqi-garden` `flight-chess` `merge-2048` `mine-garden` `sudoku-petal`。
- **不碰**窗口 2–5 的游戏目录，不做点名五项（`gomoku` / `match-stars` / `rainbow-run` /
  `ocean-munch` / `xiangqi`，那是窗口 2 的活），不写 `docs/qa/1.2-roundN-*`。

### 开工基线

- 起点 SHA：`bbe8942`（`origin/game-1.2-window1`）。
- 基线 `npm test`：**232 文件 / 6378 用例全绿**。用例只增不减，以此为底线。

### 打算怎么查（本轮取证顺序）

1. **静态红线**：商标黑名单扫 12 款 + 平台层（排除黑名单守卫测试自身）；`destroy` 是否拆干净
   监听 / timer / rAF / AudioContext；`meta.modes` / `meta.platform` 是否填准；有没有偷偷引外部依赖。
2. **平台专项（窗口 1 必验）**：密码 `kangkang`、电话 `18438037080`、1 小时过期、可手动关、
   **密码不落盘**、直达第 N 关；1.1 家长算术门 `parentAuth.ts` / `parentGate.ts` 原样保留。
3. **真浏览器走查**：`google-chrome` + `puppeteer-core`（`--no-save`，不进 `package.json`），
   360×640 最窄屏进 dev server，逐款从**首页卡片**点进去，玩到真实胜负，战役试第 1 / 100 / 188 关，
   四种模式（战役 / 对战 / 无尽 / 双人同屏）各开到有画面，退回首页查 `destroy` 泄漏。
4. **learner 复核**：若 learner 已推上 diff，逐条核对——有没有破坏手感、有没有引外部依赖、
   量化结论能不能用固定 seed 复现。
5. 若远端出现 `docs/qa/1.2-window1-round1-tester.md`，按编号**从高到低**把阻断 + 严重清零。

### 修复纪律

- 每修一个：**先写复现测试（先红）→ 再改代码（后绿）→ 再全量回归**。
- 只在本窗口独占路径里改；越界的问题写进「遗留清单」交主管，不自己伸手。
- 收尾 `npm test && npm run build` 全绿，普通 `push`，不 `--force`。

### 待填

下面各节在走查结束后回填：修了哪些、回归测试、自查新发现、learner 复核、遗留清单、最终 SHA 与用例数。
