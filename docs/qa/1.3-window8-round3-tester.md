# 窗口 8 · 1.3 视觉升级 · 第 3 轮终验测试员报告

- 角色：窗口 8 三人验收组 · A 测试员（第 3 轮 / 共 3 轮 · **终验**）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只改 `*.test.ts` 与本报告；不改绘制实现代码；他人报告一字不碰；
  玩法 / 关卡数值 / 胜负 / parentAuth / parentGate 不碰；教育三连不改题目对错；禁止 three.js。

## 终验计划（动手前提交）

本轮是窗口 8 最后一轮验收，只回答一个问题：**本窗 12 款这套视觉能不能放行**。

1. 全量跑：`npm ci` → `npm test` → `npm run build`，记录用例总数并与第 2 轮合并态
   **745 文件 / 15559 例** 对比（只增不减）；给三轮用例数曲线。
2. 对账清零（本轮前置）：第 2 轮 A 档 `1.3-window8-round2-tester.md` 的新问题
   （W8R2-01 严重 / W8R2-02 一般 / W8R2-03 建议 + 跟催 2 条）× C 档
   `1.3-window8-round2-fixer.md` 修复清单（7 条全落地）逐条给结论——
   **已修复（附证据）/ 合理降级（引 C 档理由）/ 仍未修（升级为终验阻断项）**；
   C 档移交第 3 轮的 6 条遗留逐条核对状态与降级理由。
3. 机器化防线复核：`window8-round1-visual-scan.test.ts`（8 例）与
   `window8-round2-visual-scan.test.ts`（8 例）必须还在跑、全绿；被删或被 skip 按阻断处理。
   热区钉死断言、reduced 功能提示断言、教育数据（题面 / SHA）快照断言全绿是放行前提。
4. 终扫：**12 款 × 六大专项**（火柴人 / 收集物体积 / 双人可区分 / 2.5D 纵深 / 360px 布局 / 商标），
   不抽样，每款每项给「达标 / 不达标」二值结论，产出 12 行 × 6 列终验总表。
   2.5D 点名款 `red-blue-race` 不许退化；双人重点 `red-blue-*`。
5. 回归加严：抽 6 款（点名整改重点里挑）CPU 4× 节流下复测粒子高峰帧率 ≥30fps；
   `npx vite preview` 冒烟——首页 / `sw.js` / `manifest.webmanifest` 均 200，
   随机进 5 款各玩 30 秒无视觉崩坏。
6. 终验新发现问题（如有）量化登记，交本轮 C 档当轮清零。
7. 按需沉淀第 3 轮机器化用例（仅 `*.test.ts`），既有用例零改动。
8. 终验结论：**放行 / 带条件放行（列条件）/ 不放行（列理由）** 三选一，附依据。
9. 收尾：fetch → rebase（保留别人的文件）→ `npm test` / `npm run build` 全绿 →
   普通 push `origin game-1.3-window8`（禁止 force）。

---

## 一、全量跑数据与三轮用例数曲线

测试环境：headless Chrome（360×640 设备模拟 + CDP CPU 4× 节流，puppeteer-core `--no-save` 临时安装未入 manifest）、vitest；走查驱动脚本放 /tmp 与 node_modules 下未入库。
说明：第 2 轮 fixer 报告提交（`aefbf91d`）之后至本轮开工，分支上仅 3 个 docs 提交、**代码零改动**（`git log --name-only` 可验），第 2 轮全部浏览器实测证据在终验中直接有效；本轮仍独立重扫了 12 款。

| 项 | 第 2 轮合并态（fixer 登记） | 本轮开工态 | 本轮收尾态（含测试员新增 4 例） |
| --- | --- | --- | --- |
| `npm test` | 745 文件 / 15559 例 | **745 文件 / 15559 例全过**（175.3s），逐位一致 | **746 文件 / 15563 例全过**（见第六节收尾复跑） |
| `npm run build` | precache 192 项 / 5407.10 KiB | 绿（tsc --noEmit + vite build），precache **192 项 / 5407.10 KiB**，逐位一致 | 绿（收尾复跑） |
| `vite preview` 冒烟 | `/` `/sw.js` `/manifest.webmanifest` 均 200 | 三端点均 **200** | — |

**三轮用例数曲线（只增不减达标）**：
第 1 轮合并态 **736 / 15507** → 第 2 轮合并态 **745 / 15559**（+9 文件 / +52 例）→ 第 3 轮收尾态 **746 / 15563**（+1 文件 / +4 例，全部为本档终验新增）。

---

## 二、第 2 轮问题终验对账表（本轮前置）

对账输入：`1.3-window8-round2-tester.md` 新问题清单（阻断 0 / 严重 1 / 一般 1 / 建议 1 + 跟催 2）× `1.3-window8-round2-fixer.md` 修复清单（7 条全落地）。每条给三选一结论；验证手段 = 源码接线检视 + 配套用例复跑（7 文件 34 例全绿）+ 本轮浏览器独立实测（360×640 截图）。

### 2.1 第 2 轮 A 档新问题 + 跟催（5 条）

| 编号 | severity | 修复 commit | 终验结论 | 验证证据（量化） |
| --- | --- | --- | --- | --- |
| W8R2-01 red-blue-tap 点点裸 emoji | **严重** | `2aa5dbe` | **已修复** | ① 浏览器实锤：闯关第 1 关活点点 DOM = `<span class="rbt-dot-srglyph">🔵</span>` + `<span class="rbt-dot-face" aria-hidden><svg viewBox="0 0 48 48">`——sr-only 原 emoji + 贴纸 SVG 双结构在位，截图点点为带高光渐变球贴纸；② `index.ts:528` makeDot → `dotFace()` 接线，判定仍读 `d.kind`、62px 热区 CSS 零改动；③ `dotArt.test.ts` 7 例绿（SKINS 19 种符号覆盖率从源码现抓）；④ stickers 图集 143 张注册（终验清点） |
| W8R2-02 clock-house 小鸟遮连对徽章 | 一般 | `6f3b420` | **已修复** | ① `house.ts:359` `.clk-fx` right 8px → **120px** 一行改动；② 浏览器截图：小鸟落顶栏「第 n/4 题」与「连对 n」两徽章之间的空档，连对徽章完整可读，重叠 1144px² → **0**；③ `fxLayout.test.ts` 3 例绿（让位几何 / pointer-events:none / 动效零改动） |
| W8R2-03 clock-house ✨ emoji 粒子 | 建议 | `615cfc4` | **已修复** | ① `house.ts:301` cheer 粒子本体换 `sparkStarSVG()` 矢量四芒星（金主色+深描边+白星心），✨ 仅存于代码注释；② 粒子数 6 / 轨迹 / SPARK_MS 时序零改动；③ `sparkStar.test.ts` 2 例绿。12 款 emoji FX 粒子清零 |
| 跟催-1 = B 档 TOP9 闯关氛围底 | 建议 | `057fafd` | **已修复** | ① `skin.ts:201-205` `.rbt-arena` 5 个 radial-gradient 圆点（金 3 α=.10 / 蓝 2 α=.08，全部 ≤ .12 上限，走 `withAlpha(PASTEL.*)` token），纯静态 `background-image`；② 浏览器截图氛围底圆点隐约可见、不抢主体；③ `arenaAtmosphere.test.ts` 3 例绿 |
| 跟催-2 = B 档 TOP10 车票边条 | 建议 | `44378fa` | **已修复** | ① `scene.ts:93` ticketBg 边条 17–22px → **17–25px（8px 宽）**；② 浏览器截图 n/ü/r 三票声母橙 / 韵母青边条清晰可辨，文字与打孔圆点无一被吃；③ `ticketBand.test.ts` 3 例绿 |

**第 2 轮新问题对账统计：已修复 5 / 合理降级 0 / 仍未修 0。清零率 100%，阻断・严重级残留 0。**

C 档 7 条修复中另外 2 条属第 1 轮遗留处理，本轮同样复验通过：

- **W8R1-04 推进**（`7a545ae`）：boardArt 渲染层管线 + 整关门控 + 前 3 章 14 张图集亮灯。浏览器实锤第 1 关盘面 18 格全部为贴纸（葡萄 / 西瓜 / 香蕉 / 草莓 / 苹果，双色分面+深描边+左上高光）；题库 `visual25.test.ts` SHA-256 快照原样全绿；`boardArt.test.ts` 8 例绿（关闸回退与 1.2 逐字节一致）。
- **W8R1-07 清账**（`53f593a`）：faceLift 消费端换装。浏览器实锤第 1 关题面钟 = 胖橙时针 + 细青分针 + 铆钉轴心 + 加大刻度，与新钟面工艺一致；`faceLift.test.ts` 8 例绿（前 99 关全量扫 + 题库摘要零改动）。

### 2.2 C 档移交第 3 轮的 6 条遗留逐条核对

| 条目 | severity | 终验核对结果与裁决 |
| --- | --- | --- |
| W8R1-04 余量：find-diff 第 4–10 章贴纸 50 张 | 严重（推进中） | **合理降级 → 转带条件放行条件**。终验量化：章 1–3 缺 0（27 图案全配齐、亮灯）；章 4–10 缺口 **恰为 50 张**，与 C 档第四节设计稿逐位一致（章4 缺7 / 章5 缺4 / 章6 缺7 / 章7 缺9 / 章8 缺9 / 章9 缺5 / 章10 缺9）。降级理由充分：管线与整关门控已落地，未配齐章回退 1.2 逐字节一致输出、**永不混排**（用例钉死），当前视觉 = 1.2 已过全量验收的水位；设计稿 + FLIPPABLE 非对称契约 + 商标自查口径成文在案。**交本轮 C 档按稿实施 + 188 关全量回归**；本档已加终验 ratchet（round3 scan ②③：图集 ≥143 只增不减、前 3 章不许熄灯） |
| W8R1-08 find-diff 26px 盘面格触区 | 一般（超权限请裁决） | **终验裁决：维持现状，结案不再挂账**。依据：① 玩法层早有 W5R2-C-09 偏心热区——`hitRadius(26)=22px`，**44px 直径热区整个让给未找到的答案格**，普通格照旧就近判，`cellHotZone.test.ts` 9 例全绿，触达容错实质达到 44px 验收线；② 26px 是 `panelCellForRoom()` 矮屏下限，「两图同屏不滚动地比对」是这一款的玩法本身，撑大格子必破玩法且相邻热区无法各自 44px 互不重叠；③ 视觉尺寸本身非缺陷（终扫 16px 贴纸缩档仍可辨）。结论：触区问题已被玩法层机制消化，无需再改 |
| W8R1-09 壳层 12px 装饰星 / 顶栏 | 建议（非本窗） | **维持移交全局，账不丢**。`level99.ts` `.l99-star` 现状未变（grep 确认）；壳层统一件不属本窗 12 款，且本窗覆盖指令明令「移交全局，不要改壳」。顶栏游戏名 chip 的省略号截断同属此域（见第四节登记不判罚） |
| shape-kingdom 问答关城堡氛围帽 | 建议（B 档排期第 3 轮） | **仍未实施，交本轮 C 档**。grep + 截图双证：`castleSvg` 仅描金关（draw.ts:773）使用，问答关题卡区仍素。按 B 档修订清单第 7 条（`castleSvg(0)` 静态复用）本轮实施；建议级、非六大专项缺口，不构成放行阻断 |
| music-stars 星轨远端锚点星 | 建议（备档） | **未实施，备档维持**（grep 确认无锚点星实现）。星轨谱线 + 音符星现状终扫达标，备档项可后置 |
| pinyin-train 打孔圆点下同类色圆点副记号 | 建议（备档・增强） | **未实施，备档维持**（grep 确认）。主诉「360px 类别辨识弱」已由 8px 加宽方案（`44378fa`）解决并终验过检；副记号是可叠加的形状通道增强，可后置 |

**遗留对账统计：清零 2（W8R2 系 5 条全清 + W8R1-07 清账）· 结案 1（W8R1-08 本轮裁决）· 移交全局 1（W8R1-09）· 交本轮 C 档 2（W8R1-04 余量·严重 + shape-kingdom 氛围帽·建议）· 备档 2（music-stars / pinyin-train 增强项）。**

---

## 三、12 款 × 六大专项终验总表

方法：① 机器化——round1/round2/round3 三份扫描共 20 例全绿（fillText 直出 / 商标词 / 双人三通道 / 2.5D 透视 / canvas `.arc(` 直画 / 贴纸水位）；② 浏览器——12 款入口 + 局内 13 屏 360×640 实测（横向溢出全部 0、可见 emoji 仅存壳层 HUD 图标层）+ 结算屏样张；③ 肉眼——逐款截图目检。✅=达标 ❌=不达标（写明位置与现象）；③④ 对无双人 / 非点名款按「不适用=✅」计。

| 款 | ①火柴人/裸emoji | ②收集物体积 | ③双人可区分 | ④2.5D 纵深 | ⑤360px 布局 | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| red-blue-race | ✅ | ✅ | ✅（16px 17.6–22.7% / 32px 14.7–17.7% 用例钉死） | ✅（perspective 720px + rotateX18° 用例 + 截图近宽远窄，无退化） | ✅ | ✅ |
| red-blue-tap | ✅（W8R2-01 修复后：点点=贴纸+sr，浏览器实锤） | ✅ | ✅（对战场 jellyBtn 红蓝 + 信号形状双通道） | ✅ | ✅ | ✅（⭐👑💣⚡ 造型过 B 档盯防口径） |
| red-blue-tug | ✅ | ✅ | ✅（红头带 vs 蓝鸭舌帽 + 蓝队镜像，27.7–30.5%） | ✅ | ✅ | ✅ |
| clock-house | ✅（✨ 粒子换矢量星后 emoji FX 清零；faceLift 后两代钟面同工艺） | ✅ | ✅ | ✅ | ✅（W8R2-02 修复后小鸟与连对徽章重叠 0；失败结算屏样张过检） | ✅ |
| math-farm | ✅（计数贴纸浏览器实锤） | ✅ | ✅ | ✅ | ✅ | ✅ |
| pinyin-train | ✅（站台挥手小人有五官/衣形/圆帽臂线，非裸线段火柴人，属远景装饰） | ✅ | ✅ | ✅ | ✅（8px 边条实锤） | ✅ |
| word-garden | ✅（⛰️ 贴纸实锤） | ✅（花瓣 2 停渐变） | ✅ | ✅ | ✅ | ✅ |
| shape-kingdom | ✅ | ✅（gem 三停渐变契约用例） | ✅ | ✅ | ✅ | ✅ |
| find-diff | **❌**（第 4–10 章盘面 emoji 直出：`boardArt.ts` 门控回退态，缺 50 张贴纸；**前 3 章已亮灯达标**，浏览器实锤贴纸盘面。C 档书面降级在案 → 带条件放行条件） | ✅ | ✅ | ✅ | ✅ | ✅ |
| color-fun | ✅（图例 ●■▲★◆♥ 为几何字符合规） | ✅（paintBlob 7 处渐变） | ✅ | ✅ | ✅ | ✅ |
| music-stars | ✅ | ✅（glowStar 描边+星心用例） | ✅ | ✅ | ✅ | ✅ |
| kitty-care | ✅（道具贴纸实锤） | ✅ | ✅ | ✅ | ✅ | ✅（有嘴橙猫，无蝴蝶结+白身组合；测试词表含「汤姆猫 / hello kitty」是扫描名单非文案） |

**终验达标率：71/72 = 98.6%**（三轮曲线：R1 66/72=91.7% → R2 70/72=97.2% → R3 71/72=98.6%）。唯一不达标格 = find-diff ①（章 4–10 余量，严重级・推进中，降级理由与实施路径在案，见第二节裁决）。分专项：① 11/12，②③④⑤⑥ 均 12/12。

**商标终查**：round1 商标扫描用例（20 词表 × 12 款实现代码）持续全绿；本轮贴纸目检（点点信号章 14 张 / 找不同图鉴 14 张 / 四芒星）无知名角色相似造型；kitty 有嘴 + 无蝴蝶结、⭐🌟 无脸、👑 无盾无徽、💣 无明火，避开 B 档风险清单全部红线。

---

## 四、终验新发现问题

**0 条。** 另登记 2 条不判罚观察（均不属本窗 12 款问题域）：

1. 壳层顶栏游戏名 chip 在 360px 下按省略号截断（如「红…」）——所有款一致的壳层设计（`level99` 统一件），属 W8R1-09 同域壳层项，已随其移交全局，不在本窗新立。
2. pinyin-train 站台挥手小人为简笔造型——复核绘制源码（scene.ts:230-233）：有眼睛、微笑、成形衣身与 3.4px 圆帽臂线，不构成「无渐变无描边的裸 arc 圆或线段火柴人」，且属远景装饰非交互对象，与前两轮判定口径一致，判达标。

按终验标准「终验新发现交本轮 C 档当轮清零」：本轮新发现 0，无需清零动作。

---

## 五、回归防线复查（终验加严）

- **机器化防线全数在跑、全绿、零 skip**：round1 扫描 8 例 + round2 扫描 8 例 + 本轮新增 round3 扫描 4 例 = 20 例；修复配套 7 文件 34 例；热区钉死 / 题库 SHA / duoTrim / countArt / picArt / flowerShade / propArt / subLabel 共 8 文件 86 例。round3 scan 第 4 例把「防线文件不许删不许 skip」本身也钉成了用例。
- **热区**：find-diff `.fdf-cell` 26×26 原样 + W5R2-C-09 偏心热区 9 例绿；red-blue-tap `.rbt-dot` 62px CSS 原样（dotArt 只换可见层）。0 热区被挪动。
- **教育数据**：`git log aefbf91d..HEAD` 对 math-farm/levels.ts・gen.ts、word-garden/logic.ts、kitty-care/tasks.ts、find-diff/levels.ts、clock-house/levels.ts、pinyin-train/levels.ts **零提交**；两处 SHA-256 快照（find-diff 题库 / clock-house LEGACY_DIGEST）原样全绿。0 教育数据被改。
- **kit 纪律**：15 件冻结 kit 自第 2 轮后 `git log -- src/art/kit/` **零提交**；本窗自建件（stickers / runnerDuoTrim / dotArt / boardArt / faceLift）均只有 C 档提交。
- **reduced-motion**：round2 scan 第 8 例（12 款实现代码必须保有 `prefers-reduced-motion` 处理）持续全绿。
- **CPU 4× 节流帧率**（点名整改重点 6 款，CDP 节流 + 连续点按 3s rAF 采样）：red-blue-race **60fps**、red-blue-tap **60fps**、find-diff **60fps**、clock-house **60fps**、math-farm **60fps**、pinyin-train **60fps**——全部 ≥30fps 验收线（第 2 轮 12 款 60fps 水位保持，且代码零改动）。
- **preview 冒烟**：`/` `/sw.js` `/manifest.webmanifest` 均 **200**；12 款入口+局内 13 屏横向溢出全部 **0**、pageerror **0**；red-blue-tug / word-garden / shape-kingdom / color-fun / music-stars / kitty-care **6 款各随机点按 30 秒**（63–67 次触点/款）：JS 错误 0、横向溢出 0、页面存活、无空白无视觉崩坏（尾帧截图目检）；clock-house 失败结算屏样张过检（「就差一点点！」只鼓励口径、无遮挡无截断）。

---

## 六、测试员本轮新增用例与收尾复跑

`src/games/__tests__/window8-round3-visual-scan.test.ts`，**4 例**（全部为前两轮没有的新口径）：

1. 专项①补口径：12 款实现代码 0 处 canvas `.arc(` 直画（第 1 轮钉了 fillText，这里补钉裸圆的老底子路径）；
2. 贴纸图集水位 ratchet：stickers 注册数 ≥ 143 只增不减（给第 4–10 章 50 张实施护航）；
3. find-diff 已亮灯前 3 章 27 图案贴纸全配齐、不许熄灯回退；
4. 防线的防线：round1/round2 扫描文件必须在位且不许被 skip（终验规格「被删或被跳过按阻断处理」本身用例化）。

既有用例零改动。收尾复跑（rebase 后）：

- `npm test`：**746 文件 / 15563 例全过**（对第 2 轮合并态 745/15559 只增不减：+1 文件 / +4 例，全部为本档新增）；
- `npm run build`：绿（tsc --noEmit + vite build），precache **192 项 / 5407.10 KiB**；
- 三方合并态复验与合并态 SHA 由本轮 C 档执行，本档配合（本报告基于本分支 HEAD）。

---

## 七、终验结论

**带条件放行。**

**条件（2 条，均已有明确实施路径）：**

1. **（严重级余量・唯一）** find-diff 第 4–10 章 50 张贴纸由本轮 C 档按 `1.3-window8-round2-fixer.md` 第四节设计稿实施（含 FLIPPABLE 非对称契约与双胞胎区分位），补齐后跑 188 关全量回归确认无混排无漏网。本档 ratchet 用例（round3 scan ②③）已就位护航。
2. **（建议级排期）** shape-kingdom 问答关城堡氛围帽按 B 档修订清单第 7 条本轮实施；music-stars 锚点星与 pinyin-train 圆点副记号维持备档，可并轮可后置，不作放行前提。

**依据：**

- 阻断级 0；严重级唯一 1 条（W8R1-04 余量）有 C 档书面降级理由，且整关门控保证当前线上视觉 = 1.2 已过全量验收的回退态、永不混排——不构成「不放行」理由，但因其属六大专项①缺口，不满足无条件「放行」标准；
- 第 2 轮新问题清零率 100%（5/5 已修复，含严重级 W8R2-01），修复不彻底 0、修出回归 0；
- 六大专项终验达标率 71/72 = 98.6%，三轮单调上行（91.7% → 97.2% → 98.6%）；2.5D 点名款 red-blue-race 无退化，双人三连全部可区分；
- 机器化防线 20 + 34 + 86 = 140 例全绿零 skip，热区 / 教育数据 / kit 冻结 / reduced 四条纪律零违例；
- 点名 6 款 CPU 4× 节流 60fps，preview 冒烟与 30 秒玩局全绿；
- 分级红线复核：本轮全部截图无血 / 伤害 / 死亡描写；失败结算只鼓励（clock-house 样张实锤）；无广告无内购；商标 0 命中。

## 八、权限自查

本档本轮改动仅两个文件：`src/games/__tests__/window8-round3-visual-scan.test.ts`（新增）与本报告。绘制实现零触碰；他人报告只读未改；玩法 / 关卡数值 / 胜负 / parentAuth / parentGate 零触碰（浏览器实测中误触家长门仅点「不同意」关闭，未答题未放行）；教育三连题目对错零触碰；未使用 three.js。

- 实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
