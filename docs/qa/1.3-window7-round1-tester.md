# 1.3 窗口 7 · 第 1 轮视觉验收 · 测试员报告

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 1 轮视觉测试员（A 档）。只写本报告与本窗九款视觉 `*.test.ts`，不改绘制代码、不改玩法/数值/关卡。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 扫描计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积，对照第 20–22 步各工人宣称的「只增不减」。
2. 盘点：九款目录与其 import 的 `src/art/kit/` 文件归属；`git log --follow` 抽查有没有人改了别人的 kit 文件。
3. 对照 `docs/qa/1.3-window7-s2*-plan.md` 与实际提交，漏做/做了没推的逐款点名。
4. 六大专项（只扫本窗九款）：
   - ① 火柴人/草稿圆：九款 grep `fillText(` emoji 直出与纯圆两点眼；重点 `fruit-catch` 水果、`snake-snack` 虫、`poop-hero` 💨。
   - ② 金币体积：金币/星星/宝石/收集物/水果/牌面须 ≥2 停渐变 + 高光 + 描边或落影之一。
   - ③ 双人可区分：仅本窗确实双人的（`memory-cards` 双人模式等），16px 灰度剪影两通道可分辨。
   - ④ 2.5D 纵深：点名 `poop-hero` 章节视差/2.5D 层次；不验外窗游戏。
   - ⑤ 360px 布局：九款 360×640 与 320px 溢出/遮挡/字号<14px/触区<40px。
   - ⑥ 商标扫描：九款素材与文案无商标/官方角色高相似。
5. 横向一致性：kit 光照约定；`prefers-reduced-motion`（九款）；`destroy` 泄漏（九款 ×5 进出）；性能抽 `fruit-slice` / `fruit-catch` / `lianliankan` 等粒子较重款。
6. 产出：本文件六大专项各一节 + 「9 款 × 六专项」结果矩阵；问题量化到 文件+函数/现状/期望/严重度/建议责任文件。

（以下各节待扫描完成后填写。）
