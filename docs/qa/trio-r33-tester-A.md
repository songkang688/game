# 三人组 r33 测试员 A

> 同分支。不抢 N-159 字园花钮、N-160 `.clf-work`。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-161 | 静态：同规则 `button` / `cursor:pointer` 同时写 `width`+`height` 数字则均 ≥44。豁免 `pointer-events:none`、`.bc-knob` / `.fk-stick-dot`、`.mj-small` 展示牌。字园 `.wgd-garden-flower` 34 走 **N-159**。 |
