# 1.3 窗口 3 · 第 2 轮视觉验收 · 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 2 轮 · C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step28-C-fixer.md`（思路）、`docs/qa/1.3-window3-round2-tester.md`（本轮 A 档）、`docs/qa/1.3-window3-round1-fixer.md`（第 1 轮遗留）、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、修复计划（动手前提交）

开工时 `docs/qa/1.3-window3-round2-learner.md` 尚未产出：按提示词规则先按 A 档与 round1 遗留修，收尾 fetch/rebase 时若 B 档已出再合并跟进其一致性评审点。

修复顺序按铁律：

1. **第 1 轮「修复不彻底 / 修出回归」**：A 档对账结论为 0 条（13 笔修复全部驻留、0 回归）——无欠账。
2. **本轮阻断 / 严重 / 一般**：A 档均为 0 条——无。
3. **建议级落地**（A 档 N-01~N-04 + round1 遗留清单在职权内的部分）：
   - N-01：gomoku 难度菜单 🐣🐱🦊🐉🐘🌌 六档 / xiangqi 🐘 两档 emoji 前缀 → 画制小头像（复用 xiangqi `robotAvatarSVG` 材质套路），只动菜单渲染层不动 ai 逻辑；
   - N-03 + 遗留 #3：ocean-munch 对战 HUD 对手名牌 emoji（数据驱动）画制对手小头像；地图锁 / 对战节点 / 图鉴 / 提示行 / 增益标 / 深度计残留画布 emoji 换画制；
   - 遗留 #2：rainbow-run 战役地图锁 / 旗 / 表、结算行、无尽入口、岔路标 8 行画布 emoji 换画制；
   - 遗留 #4：fight-king 战斗 HUD 微标签 12–12.5px 携 360 实测评估（能提则提、会溢出则登记）；
   - 遗留 #5：fight-king 菜单模式卡 🥊🤖🏯🔥🎓 酌情画制；
   - 遗留 #6：hop-pads 画布「⏸ 已暂停」/ tap-tiles ⏸ 功能提示层酌情画制；
   - N-02：遗留台账按 rr 8 行 / om 9+2 行新口径接手（台账勘误，本报告落实）；
   - N-04：ocean-munch 48.3fps 预警登记，若本轮加饰层需复测（本轮不在 om 加常驻粒子）。
4. P-05（level99 `.l99-star`）非本窗职权，继续登记移交。
5. 每修一处一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减。
6. 收尾合并态复验：fetch → rebase（保留别人的报告）→ `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟 → 合并态 SHA / 用例总数 / 构建体积写回本报告。

红线重申：只动绘制函数 / CSS / 视觉常量 / 本款 `art.ts`；判定、数值、关卡、存档 key、玩法断言不碰；遵守各款 1.3 提示词既定规格（光源左上、描边 1.5–2px、粉彩色板、reduced 约定）；禁止 three.js；禁止 force push；不改 `level99.ts`；不碰别人的报告文件。

（以下各节修复完成后回填。）
