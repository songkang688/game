# 1.3 窗口 3 · 第 2 轮视觉验收 · 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 2 轮 · C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step28-C-fixer.md`（思路）、`docs/qa/1.3-window3-round2-tester.md`（本轮 A 档）、`docs/qa/1.3-window3-round2-learner.md`（本轮 B 档，开工后到达并已合并跟进）、`docs/qa/1.3-window3-round1-fixer.md`（第 1 轮遗留）、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、修复计划（动手前提交，commit `4461abb`）

开工时 `docs/qa/1.3-window3-round2-learner.md` 尚未产出：按提示词规则先按 A 档与 round1 遗留修，收尾 fetch/rebase 时 B 档已出（`e903520`），其一致性评审点名 5 款与修订建议 10 条已并入本轮执行（对照见第三节）。

修复顺序按铁律：

1. **第 1 轮「修复不彻底 / 修出回归」**：A 档对账结论为 0 条（13 笔修复全部驻留、0 回归）——无欠账。
2. **本轮阻断 / 严重 / 一般**：A 档均为 0 条——无。
3. **建议级落地**（A 档 N-01~N-04 + round1 遗留清单在职权内的部分 + B 档建议 1~8）。
4. P-05（level99 `.l99-star`）非本窗职权，继续登记移交。
5. 每修一处一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减。

红线重申：只动绘制函数 / CSS / 视觉常量 / 本款 `art.ts`；判定、数值、关卡、存档 key、玩法断言不碰；遵守各款 1.3 提示词既定规格（光源左上、描边 1.5–2px、粉彩色板、reduced 约定）；禁止 three.js；禁止 force push；不改 `level99.ts`；不碰别人的报告文件。

---

## 一、修复清单（来源 / 位置 / 修法 / 配套用例 / commit SHA）

本轮共 **9 笔修复 commit**，新增 **6 个 `visual-fix-r2.test.ts` / 60 个用例**，全部为建议级与 round1 遗留清理（本轮阻断 / 严重 / 一般均为 0）。

| # | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | 遗留 #3 + A 档 N-03 + B 档建议 1/5（素材层） | `ocean-munch/art.ts` | 新增九枚画制徽章画法：`drawPadlock`（挂锁）/ `drawClockBadge`（闹钟读秒）/ `drawDownChevron`（下潜箭）/ `drawBoltGlyph`（闪电）/ `drawVsBadge`（对战双鱼牌）/ `drawBookBadge`（图鉴书）/ `drawQuestBadge`（问号牌）/ `drawBulbBadge`（灯泡）/ `drawRivalChip`（对手小鱼头圆片，按 `rivalProfile` 主色渲染）——全部左上高光 + 1.5~2px 深描边 + 粉彩板，画在原点由调用方变换，save/restore 自净 | `ocean-munch/visual-fix-r2.test.ts`（与 #2 共 16 例） | `d9219c6` |
| 2 | 同上（接入层） | `ocean-munch/index.ts` 14 处 | 画布 `fillText` emoji 全部换画制：N-03 对手名牌 ×3（HUD 读秒行 ⏱+emoji → 闹钟+小鱼头、场上名牌 emoji → 小鱼头、档位卡 emoji → 按档缩放小鱼头）；遗留 #3（深度计 ⬇ / 图鉴 📖×2 / 电电草 ⚡ / 主题卡锁 🔒 / 地图节点锁 🔒 / 对战节点 ⚔ / 未收录 ❓ / 章节页 ⭐ / 结算 ⭐☆ / 提示 💡）——图鉴页 📖❓ 一并清账（B 档原判可留第 3 轮，提前完成） | 同上 | `a2ab0ef` |
| 3 | 遗留 #2 + B 档建议 2/4（素材层） | `rainbow-run/art.ts` | 新增七枚画制徽章画法：`drawMiniStar`（实/空两态迷你星）/ `drawPadlock`（挂锁）/ `drawFinishFlag`（终点三角旗）/ `drawStopwatchBadge`（秒表）/ `drawInfinityBadge`（无尽双环）/ `drawTargetBadge`（任务靶）/ `drawForkArrow`（岔路箭）——同库约定画在原点，左上高光 + 1.5~2px 描边；旗面纯色无字母徽记、锁体比例避开系统解锁图标标准样式（B 档 5.2 事前提醒逐条落实） | `rainbow-run/visual-fix-r2.test.ts`（与 #4 共 14 例） | `2f3d617` |
| 4 | 同上（接入层） | `rainbow-run/index.ts` 12 处 | 战役地图 / 结算层画布 emoji 全部换画制：主题卡锁 🔒 / 章节页行首 ⭐ / 地图节点锁 🔒 / 终点旗 🏁 / 限时表 ⏱ / 节点星 ⭐▫ / 结算星 ⭐☆ / 统计行 🍬⭐（**零新函数**复用既有 `drawCoinFrame` + 新 `drawMiniStar`，B 档建议 2 原样执行）/ 无尽 ♾️ / 任务靶 🎯×2 / 岔路 ◀▶——「进关前 vs 进关后」两世代观感台阶清账 | 同上 | `0ee65d8` |
| 5 | 遗留 #4 + B 档建议 7 | `fight-king/index.ts` CSS | 战斗 HUD 微标签字号 `.fk-ch-n` `.fk-name`×2 `.fk-clock-r` `.fk-pad-name` `.fk-fd` 12/12.5px → **14px**（B 档口径为「试提 13.5–14」，实取上限），`@media (max-width:380px)` 的 `.fk-name` 12px 覆写同步删除；360 安全性依据：名牌族全部自带 `white-space:nowrap` + `text-overflow:ellipsis` + JS 端 `shortName` 截断兜底，读秒/键标为定宽数字或单字，无溢出通道 | `fight-king/visual-fix-r2.test.ts` 字号组；`smoke.test.ts` 两处 12px 旧断言**收紧**为 14px（1.2 既定取舍被 1.3 新规格替代，属加严非削弱） | `7fdccc8` |
| 6 | 遗留 #5 + B 档建议 6 | `fight-king/art.ts` + `index.ts` 3 处 | 新增 `modeIconSVG(mode)`：菜单七款模式的内联 SVG 双色图标（拳套平放正视不构成格斗系列 logo 斜构图——B 档 5.2 提醒落实、机器人方头双天线、道场、连胜火、训练靶、双人、镜像），统一 1.5px 描边 + 左上高光；接入模式卡 `.fk-mode-emoji`、选关页标题栏、主菜单「训练场」标题三处，尾随文字包 `<span>` 保 textContent 可寻 | `fight-king/visual-fix-r2.test.ts` 图标组（文件共 8 例） | `115be33` |
| 7 | 遗留 #6a + B 档建议 8 | `hop-pads/art.ts` + `index.ts` 暂停层 | 新增 `drawPauseBars`：两根圆头粗竖条（lineCap=round，线宽 h×0.32 下限 3px，赭墨 #9A5A2C 同暂停层文字色），画在标题上方；画布 `fillText("⏸ 已暂停")` 改为 画制暂停牌 + 纯文字「已暂停」，aria 播报口径一字未动 | `hop-pads/visual-fix-r2.test.ts` 5 例 | `19bb3cc` |
| 8 | 遗留 #6b + B 档建议 8 | `tap-tiles/art.ts` + `index.ts` 4 处 | 新增 `pauseIconSVG`（两根圆角竖条 rect×2）/ `playIconSVG`（圆角小三角），`fill=currentColor` 跟随所在按钮 / 标题文字色一份图标全场通用；接入暂停按钮初始态 ⏸、切换两态 ▶️继续/⏸暂停、遮罩标题「⏸ 先歇一会儿」与「▶️ 继续玩」钮（`showCover` 按钮改走 innerHTML）；「开始 ▶」为纯排版三角保留（B 档允许类口径） | `tap-tiles/visual-fix-r2.test.ts` 7 例 | `5c82956` |
| 9 | A 档 N-01 + B 档建议 3（最低成本方案） | `gomoku/art.ts` + `index.ts` 座位条 + `view.ts` 1 行 CSS | 新增 `spiritAvatarSVG(tier)`：六档棋灵画制头像，与座位迷你棋子**同一套玉石材质**（投影 + 三档径向渐变圆面 + 左上 -32° 高光，同 `stoneIconSVG`），特征深木墨色两三笔勾出（苗=顶芽两叶 / 喵=立耳粉窝 w 嘴 / 狐=尖耳白吻 / 龙=金角短须 / 象=大耳卷鼻 / 渊=墨玉面星点闭目——星环徽记样式刻意避开，B 档 5.2 提醒落实）；对局座位 AI 一侧挂头像、座位名 emoji 前缀由头像承担（渲染层正则剥离）；**`DIFFICULTY_NAME` 常量与菜单按钮原文零改动**（`tiers.test` 逐字钉子未触碰） | `gomoku/visual-fix-r2.test.ts` 10 例（含「菜单六档原文仍逐字可寻」「双人局不挂头像」防误伤钉子） | `4f68090` |

未点名款检查：`hue-hand` `match-stars` `xiangqi` 三款本轮 A / B 两档均无待修项，未做改动（xiangqi 难度名文本前缀归入第四节决策项）。

---

## 二、第 1 轮遗留清单处理结果（6 条 → 清 5 条 / 移交 1 条）

| # | 内容 | 处理结果 |
| --- | --- | --- |
| 1 | 共享外壳 `level99.ts` `.l99-star` 装饰字符（P-05） | **继续移交**：非本窗职权，本轮一字未碰,登记待统筹/主管统一处理 |
| 2 | rainbow-run 战役地图/结算层约 6 行（实为 8 行）emoji | **清账**（`2f3d617` + `0ee65d8`）：8 行全画制,含 B 档原判「可留」的 ♾️🎯◀▶ 一并完成（N-02 台账勘误口径落实） |
| 3 | ocean-munch 地图/图鉴层约 8 行（实为 9+2 行）emoji | **清账**（`d9219c6` + `a2ab0ef`）：9 行 + 2 处数据驱动全画制,含 B 档原判「留第 3 轮」的图鉴 📖❓（N-02 台账勘误口径落实） |
| 4 | fight-king 战斗 HUD 微标签 12–12.5px | **清账**（`7fdccc8`）：提升至 14px 达宪法门槛,360 溢出防线核验见第一节 #5 |
| 5 | fight-king 菜单模式卡 🥊🤖🏯🔥🎓 emoji | **清账**（`115be33`）：`modeIconSVG` 七款内联 SVG 图标 |
| 6 | hop-pads 画布「⏸ 已暂停」/ tap-tiles ⏸ 提示层 emoji | **清账**（`19bb3cc` + `5c82956`）：hop 画制双竖条暂停牌、tt 暂停/继续双 SVG 图标 |

---

## 三、一致性统一化改动说明（对照 B 档第三节点名 5 款）

B 档四轴（色板 / 描边 / 光照 / 节奏）结论为「无违和款」，点名的族系问题全部是**菜单 / 地图 / 名牌装饰层 emoji 残留的界面世代差**。5 款点名全部按 B 档绘制规格级建议处理：

1. **ocean-munch（点名第 1）**：对手名牌画制化（复用鱼形按 `rivalProfile` 主色渲染小鱼头圆片 + 1.5px 深描边，B 档规格①原样执行）；地图锁 / 对战节点 ⚔ 画制（规格②执行,⚔ 实现为对战双鱼牌——本款语言里「对战」是两条鱼对视,比双短剑更贴题材且规避武器意象）；标题 emoji 前缀按规格③留决策（见下）。
2. **rainbow-run（点名第 2）**：结算行零新函数复用 / 地图锁旗画制,B 档规格逐条执行,「进关前 vs 进关后」世代差清账。
3. **gomoku（点名第 3）**：最低成本方案照做——只做对局座位一张（当前难度）,材质套路对齐 xiangqi `robotAvatarSVG`（面+侧壁光影+墨线剪影）但落在本款玉石渐变体系;菜单六张留第 3 轮（见第四节决策项）。
4. **fight-king（点名第 4）**：字号按「先实测再动」口径处理后提至 14px（B 档建议 13.5–14 区间上限）;模式卡图标画制,拳套平放正视。
5. **hop-pads / tap-tiles（点名第 5）**：⏸ 两根圆角竖条,两款同法同批完成。

B 档建议 10（负面清单）遵守情况：本轮**零新增常驻装饰层 / 粒子**（全部改动为 emoji→画制的等量替换或字号 / 图标替换）；`level99.ts` 未触碰；新增绘制函数全部服务于「减代差」not「铺新面」。

---

## 四、登记遗留（交第 3 轮终验裁决）

| # | 位置 | 内容 | 登记理由与本档立场 |
| --- | --- | --- | --- |
| 1 | `level99.ts` `.l99-star`（P-05） | 选关页签 10px「★」 | 非本窗职权,继续移交统筹 |
| 2 | 棋灵家族文本 emoji 前缀（B 档建议 9 决策项） | gomoku `DIFFICULTY_NAME` 六档 🐣🐱🦊🐲🐘🌌 前缀（菜单按钮 / 结算 headline）、xiangqi 两档难度名前缀、om「🐟」/ rr「🌈」画布标题前缀、fight-king 局内「🎓 训练场」battle 标题 | `tiers.test` 对 `DIFFICULTY_NAME` 逐字钉死,动文本必动玩法测试断言（红线）;本档立场：**建议第 3 轮裁决为「文本装饰允许类」并关账**——座位/名牌等「认人」场景已全部画制化,余下均为文本排版位,visual-bible 允许文本前缀 emoji;若第 3 轮裁决画制,gomoku 菜单可直接复用本轮 `spiritAvatarSVG` 六张现成头像,成本已探明 |
| 3 | ocean-munch 4× 节流 48.3fps（A 档 N-04） | 性能预警 | 本轮登记且执行 B 档禁令：未给 om 任何档位加常驻粒子/饰层;本轮 om 改动全部在地图/图鉴/结算等**非逐帧热路径**,局内热路径仅名牌行 emoji→小鱼头等量替换(每帧一次 fillText 换 ≤10 笔矢量,量级不变) |

---

## 五、性能回压

A 档 round2 深度走查 9 款 4× 节流全部 ≥30fps（8 款 60fps、om 48.3fps），**无 <30fps 款,本轮未做性能回压,无帧率对比数字**。om 预警处理见第四节 #3。

---

## 六、合并态复验（三人共同收尾，由 C 执行）

1. 三人均已 push：`git fetch origin game-1.3-window3` → rebase 本地修复至 B 档报告 `e903520` 之上（无冲突）→ 普通 push。确认三份 round2 报告（tester `63d4215` / learner `e903520` / fixer 本报告）与全部修复均在分支上。
2. 复验命令与结果（在合并态 `4f68090` 上执行）：
   - `npm ci`：全绿；
   - `npm test`：**全绿，747 文件 / 15438 用例**（对比 B 档登记的 741 文件 / 15378 例：+6 文件 / +60 例；对比第 1 轮合并态 `ef72248` 的 732 文件 / 15342 例：+15 文件 / +96 例，**只增不减**；本轮 C 档新增 6 个 `visual-fix-r2.test.ts` 共 60 例）；
   - `npm run build`：**全绿**（tsc --noEmit + vite build），PWA precache **188 entries / 5244.88 KiB**，`dist/` 总体积 5.6 MB（对比第 1 轮合并态 5226.06 KiB：**+18.8 KiB / +0.36%**，远低于 10% 说明线,增量即本轮 20 枚画制徽章/头像 + fk 图标组的矢量代码与 B 档 9 句文案；项数不变）；
   - `npx vite preview` 冒烟：`/` → 200，`/sw.js` → 200，`/manifest.webmanifest` → 200，**全绿**。
3. **合并态 SHA：`4f680909664093022da7264797487673026862d9`**（全部代码修复的最后一笔；本报告为其上的 docs-only 提交，不含代码改动）。

---

## 七、kit 纪律与越界自查

- 本窗无共享 `src/art/kit/`；本轮全部新增画法落在各款独占 `art.ts`（om 9 枚 / rr 7 枚 / fk 1 组 / hop 1 枚 / tt 2 枚 / gmk 1 组），无跨款引用、无引用窗口 1 kit。
- 本人 11 笔 commit（1 计划 + 9 修复 + 1 报告）触碰文件全集：6 款的 `index.ts` / `art.ts`、gomoku `view.ts` 1 行 CSS、6 个新增 `visual-fix-r2.test.ts`、`fight-king/smoke.test.ts` 2 处字号断言收紧、本报告。**未触碰**：`level99.ts`、引擎层、他窗目录、A/B 报告文件（只读）、任何玩法判定 / 数值 / 关卡 / 存档 key / 玩法测试断言（gomoku `tiers.test` 等逐字钉子原样保留并有新钉子反向守护）。
- 红线自查：本轮新图元零血 / 伤害 / 死亡意象（om「对战」用双鱼对视不用武器、fk 拳套为运动器具平放正视）；零商标 / 官方角色近似（B 档 5.2 事前提醒逐条落实：旗面无字母徽记、锁避开系统图标标准比例、龙档金角墨须无「圆鼻大眼」组合、渊档无星环徽记、拳套无斜构图 logo）；失败文案未动。

---

## 八、窗口 3 第 2 轮视觉验收结论

- **第 1 轮「不彻底 / 回归」：0 条，无需处理**（A 档对账 13 笔全驻留）。
- **阻断级：0 发现 / 0 残留**；**严重级：0 发现 / 0 残留**；**一般级：0 发现 / 0 残留**。
- **建议级：A 档 N-01（座位部分）/ N-03 修复，N-02 台账落实，N-04 登记；B 档建议 1~8 全部落地、9 登记决策、10 遵守**。
- **round1 遗留 6 条：清 5 条、移交 1 条（P-05）**；本轮新登记 3 条（第四节），无一属阻断 / 严重。
- `npm test` / `npm run build` / `vite preview` 冒烟全绿，用例只增不减（+96 例 vs 第 1 轮合并态），构建体积 +0.26%，合并态 SHA 已入报告。

**结论：窗口 3 第 2 轮视觉验收通过——阻断清零、严重清零、round1 遗留基本清账（仅 1 条非职权移交），装饰层 emoji 世代差在本窗职权内全部抹平，剩余 3 条登记项全部为决策类 / 移交类 / 预警类，移交第 3 轮终验。**
