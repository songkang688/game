# 窗口5 · 第3轮学习优化员（本窗收官）

> 范围仅限窗口 5 独占的 15 款游戏。不验 76 款全集，不改窗口 1 的平台文件。
> **实际模型 slug**：`claude-opus-5-thinking-high-fast`
> **分支**：`game-1.2-window5`（不推 `main`、不推 `game-1.2`、禁止 `--force`）
> **输入**：`docs/qa/1.2-window5-round3-tester.md` 的 `# 收官 · 全 15 款终检` 一节（同一执行者上一顶帽子写的），
> 以及第 1 / 2 轮的六份报告作为「有没有回潮」的底账。

## L0 · 这一轮盯的是什么

前两轮的学习优化条目大多在**调数值**（难度曲线、AI 强度、提示措辞）。
收官轮换一个角度：**孩子在真机上第一眼看到的那一屏，够不够他自己开始玩。**

理由是本轮测试员那 8 条现场读数里，有一条共同的形状：

> 元素**在 DOM 里、样式也对、`el.click()` 也绿**，
> 但它落在舞台裁切线以下 / 被常驻控件压着 / 落地的滚动位置刚好看不到它。

对大人这叫「滚一下就有了」；对五六岁的孩子，**屏幕上没有的东西就是不存在**。
所以这一轮的 10 条改进全部围绕一句话：**让「能玩」在落地那一帧就成立，而不是滚一下之后才成立。**

一条硬规矩贯穿始终：**收留白、收字号，绝不收热区。**
下面每一条落地项都单独有一条用例盯着 44px 下限，改动里一处都没有把它压下去。

## L1 · 十条改进（落地 8 条，不落地 2 条）

| # | 款 | 改什么 | 为什么这么改（孩子视角） | 状态 |
| --- | --- | --- | --- | --- |
| **L3-01** | `landlord-cards` | 提示行与按键统一叫「不出」，抽成 `PASS_WORD` / `PASS_BUTTON_LABEL` 两个常量，两处只能引用、不能各写各的 | 提示说「按不要」，键上印的是「不出」。大人一眼知道是同一件事，刚认字的孩子会在屏幕上逐个找「不要」——找不到就以为自己看错了 | **已落地**（`bf1f07a`） |
| **L3-02** | `poop-hero` | 画布收到底之后仍装不下时，本款壳层自己挂滚动条并钳住高度（`wrapRoomPx`，下限 240px） | 三个垃圾桶的名字是这一款的规则本身。320 宽的旧手机上整片压在屏外又滚不动，等于让孩子闭着眼分类 | **已落地**（`673b064`） |
| **L3-03** | `shape-kingdom` | 常驻控件摞按量收薄（`needsTightDock`）：只有「图形高 > 可视段 − dock 高」时才挂收薄档，三颗键从折两行改成排一行 | 画长方形要点对角两颗点。dock 占掉 174/332px 时，两颗点从来不在同一屏；点完一个角滚屏去找另一个，孩子就把刚才那个角忘了 | **已落地**（`7ae8a01`，dock 174→94） |
| **L3-04** | `shape-kingdom` | 钳完顺手把图形整张送进眼里（`showBoard`），滚**最小的那一段**，题面尽量留在屏上 | 「存在一个能看全的位置」和「一进关就在那个位置」是两回事。孩子不会先想到把屏幕往上推 | **已落地**（`d5cfaa9`，落地即 38/38 全可点） |
| **L3-05** | `word-garden` 组字工坊 | ① 窄屏把四选一收进一行（4×54 + 3×6 = 234 ≤ 252）；② 补接 `fitQuizHost` 钳位兜底；③ 钳住时同时松开 `min-height`（否则 `min-height` 赢 `max-height`，钳位整条空转） | 四选一少一个选项，正确答案可能就在够不着的那一颗上。而且第 ③ 点是真机才看得出来的：`maxHeight` 已经写成 301px，盒子却仍是 380px——**代码看着修了，屏幕上没修** | **已落地**（`0acf5b4`） |
| **L3-06** | `word-garden` 描红台 | 本款第三条入口补接同一套钳位（答题屏、组字工坊、描红台三条齐了） | 「田字格里按顺序描一描」0px 可见。规则看不见，孩子描错了顺序还以为是自己笨 | **已落地**（`d5fdc8d`） |
| **L3-07** | `kitty-care` | ① 粘在下沿的提示行改 `pointer-events:none`；② 挂上滚动条时把动手层送进眼里（`showPlayRow`），并**减掉粘住那一行的高度**再算滚多少 | 第 2 轮把提示行粘住是对的（「这一关要干什么」滚到哪儿都看得见），但它带着 `z-index:6` 压在所有交互层上。一句话不该吃掉孩子的手指。第 ② 点里减掉粘住那一行是关键：不减就会把托盘正好停在提示行底下，从「看得见摸不着」换成「看不见」 | **已落地**（`bca25bd`） |
| **L3-08** | `red-blue-tug` | 在「扣场地 → 减空隙」之后再加两档：**收字号 → 逐档扣两颗大按钮（底线 56px）**。按钮高度改写自定义属性 `--rbg-pull-h`，因为内联 `height` 会被下一次重绘覆盖 | 「按住蓄力、松手换气」是这一关唯一的玩法说明。看不见，孩子只会一下一下地戳，永远拉不过对面。这一款**故意不挂滚动条**——拔河是按住不放的玩法，手指一滑就松了力，比看不见半行更糟 | **已落地**（`f0635ba`，L117 提示行 1/36→33/33） |
| **L3-09** | `red-blue-tug` | 让机关胶囊按重要度可折叠（第 188 关三行 104px 是提示行被顶出去的根因） | 章节越靠后胶囊越多，是**无上限增长**；今天靠收字号压住的，下一批新机关又会撑破 | **不落地**：属玩法改动（要决定哪些胶囊可以折、折起来之后怎么让孩子知道），收官轮不动玩法。已写进测试员报告 R3-5 交主管 |
| **L3-10** | 平台 `quiz99.ts` | 同一题连错 2 次时不要直接指答案，改成「划掉一个错项」或再给一句方向 | 现在的行为是给正确选项挂 `qz-hint` 一闪一闪，还朗读「一闪一闪的那个就是答案」。这跟学习三连「提示不直接给答案」的红线直接冲突 | **不落地**：`src/games/quiz99.ts` 是跨窗口共用文件，**不在窗口 5 所有权范围**，一行不动。已交窗口 1 / 主管。**本窗口五款自己的三级提示（`hints.ts` / `safeHints`）另有 `noSpoiler` 用例守门，不给答案** |

## L2 · 落地那 8 条的用例账

每一条都**先红后绿**：新用例先在改动前的代码上真的失败，再补代码转绿。

| 文件 | `it(` 条数 | 盯的是什么 |
| --- | --- | --- |
| 落地项 | 测试文件 | 本轮新增 `it(` | 盯的是什么 |
| --- | --- | --- | --- |
| L3-01 | `landlord-cards/hint.test.ts` + `shell.test.ts` | **+3** | 提示行与按键必须引用同一个 `PASS_WORD`；源码里不许再出现散落的「不要」；牌桌那颗键的文案取自 `PASS_BUTTON_LABEL`，不是字面量 |
| L3-02 | `poop-hero/wrapScroll.test.ts`（新增文件） | **+8** | `wrapRoomPx` 的边界：装得下不挂滚动条、装不下钳到底线 240px、量不出数不动样式 |
| L3-03 | `shape-kingdom/dockTight.test.ts`（新增文件） | **+16** | `needsTightDock` / `applyTightDock` + 样式与接线巡检；含「先摘记号再量，否则一收就退不回去」与「收薄档里 44px 原样重申」 |
| L3-04 | `shape-kingdom/dockTight.test.ts`（追加） | **+10** | `scrollToShowPx` / `showBoard`；含反例「不减掉 dock 就当图形早看得见了，一格都不滚」 |
| L3-05 | `word-garden/buildCharFit.test.ts`（新增文件）+ `stageFit.test.ts` | **+12** | 四选一在 252px 预算里排得下（**含反例：改之前那一档确实排不下**）；钳住时必须同时松开 `min-height`，松回去成对还原 |
| L3-06 | `word-garden/buildCharFit.test.ts`（追加） | **+5** | 描红台三条入口齐全；描红台**不许**自动滚屏（按住画的玩法） |
| L3-07 | `kitty-care/playRow.test.ts`（新增文件） | **+12** | `scrollToShowPx` 减不减粘住那一行差 26px（**这条直接钉住缺陷本身**）、`showPlayRow` 六层选择器的回退、提示行 `pointer-events:none` |
| L3-08 | `red-blue-tug/tighterFit.test.ts`（新增文件） | **+16** | `needsTighter` / `pullRoomPx` 边界、`MIN_PULL_H ≥ 44` 的热区底线、四档顺序「扣场地 → 减空隙 → 收字号 → 扣按钮」、还原按钮高度用内联值而不是清空 |

**用例只增不减**：本轮新增 **82 个 `it(` 断言块**（含循环展开后由 `vitest` 实计更多）。
本窗口 15 款收尾水位（`vitest` 实计）：

| 款 | 用例 | 款 | 用例 | 款 | 用例 |
| --- | --- | --- | --- | --- | --- |
| `landlord-cards` | 338 | `red-blue-tug` | 125 | `shape-kingdom` | 216 |
| `fishing-star` | 278 | `clock-house` | 170 | `find-diff` | 113 |
| `poop-hero` | 152 | `math-farm` | 93 | `color-fun` | 128 |
| `red-blue-race` | 163 | `pinyin-train` | 104 | `music-stars` | 142 |
| `red-blue-tap` | 168 | `word-garden` | 128 | `kitty-care` | 169 |

**合计 2487 条**，每款都在「新游戏 ≥ 15 条」的下限之上一个数量级。

## L3 · 有意没做的事

- **没抽公共件**。五款各自存了一份「量舞台下沿 → 自己钳 → 自己滚」的实现（`poop-hero/runtime.ts`、
  `shape-kingdom/dockTight.ts`、`word-garden/fit.ts`、`kitty-care/runtime.ts`、`red-blue-tug/fit.ts`）。
  重复是**故意的**：抽出来得放 `src/games/` 根或 `src/ui/**`，那是跨窗口目录，本窗口不许动。
  每份文件的头注释里都写明了这一点，等平台侧愿意收的时候一次性合并。
- **没动 `src/styles.css`**。本轮 8 条缺陷里 6 条的根因是 `.game-stage{overflow:hidden}` 定高硬裁，
  但那是窗口 1 的文件。本窗口一律在自己的壳里兜住，**一个字节都没往公共文件里写**。
- **没调难度数值**。收官轮不改玩法平衡——第 2 轮刚做过一批，再动就没人复验得过来了。
- **没为了压像素动 44px 热区**。这是唯一一条我拒绝交易的东西：
  `red-blue-tug` 第 188 关宁可留 19px 提示行被裁，也不把按钮压到 56px 以下。

---

## 档B · 五款

（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`）

> 本节由**档B 学习优化员**独立填写。上面的 L0–L3 是并行执行者写的「收官学习优化员」正文，
> `## 档A · 五款` 与 `## 档C · 五款` 是并行的另外两名学习优化员写的，
> **三处一个字节都没动、没重排、没删**。档A 节若还不存在，也不由我代写。

### B0 · 调研与落地计划（动 `src/` 之前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 3 轮（收官）** · **档B 学习优化员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支；**不推 `main` / 不推 `game-1.2` / 禁止 `--force` / 不用 `gh` 开改 PR**）
**基线**：`0a1cc5f`（`git fetch origin game-1.2-window5` 后一格不差；即档B 测试员续作 B7/B8/B9 收官的那一条）
**没派任何云端子代理**——提示词里那句「请通过 Task 派生」是写给派发方看的，我就是执行者，活自己干完，也不派 fixer。

**开工前自己复跑的水位**（不照抄别人报告里的数字）：

```
$ npm test
 Test Files  710 passed (710)
      Tests  15157 passed (15157)
   Duration  176.93s
```

与测试员 B9 记的 710 / 15157 一格不差，说明它交卷之后到我开工之间本档没有别的改动落进来。

#### 读过的输入（逐条列出，不含糊）

| 文件 | 读的那一节 | 拿来干什么 |
| --- | --- | --- |
| `docs/qa/1.2-window5-round3-tester.md` 档B | **B4**（本轮新记 4 条 `W5R3-BT-01…04`） | **主输入**，本节落地项全部出自这四条 |
| 同上 | B3.1（`FB-01` 十一格「滚是滚得动了，落地那一眼还是不在手边」）、B3.4（`W5-B-08` 27 次上鱼不复现 + 逐帧坏帧归因） | 判断 BT-01 到底缺的是「能不能滚」还是「落地在不在眼前」；确认**不许再去动抛竿主路径** |
| 同上 | B2.4 `LB-13`（**一半真修**：L188 让得干净，L155 双声部章键排 `pan-x` 从键上起手竖划一分不动） | 第 5 条落地项的依据 |
| 同上 | B5.1 / B5.2（230 格静止扫描、真手指四趟救援定案表） | 只对「真手指也救不回来 / 落地就看不见」的动手，静止够不着但划一下就到的**不动** |
| 同上 | B2.1 / B2.2 / B2.3 / B2.5（前两轮 `W5-B-01…11`、`W5-L-11…21`、`W5-F-08…13`、`LB-01…13`、`FBS-01…04`、`FB-01…04` 逐条结案） | **复核第 1、2 轮落地项有没有回退**的底账 |
| 同上 | B6（交窗口1 的 5 条，只量不改） | 划清「不该我动」的边界 |
| 同上 | 文末 `## R3-0…R3-6` 收官节 | **只读，一个字节不动** |
| `docs/qa/1.2-window5-round3-learner.md` L0–L3 与 `## 档C · 五款` | 并行执行者与并行档C 学习优化员的落地清单（`showPad` / `showBoard` / `showPlayRow` / `showPull` 这一族「钳完把动手层送进眼里」的做法） | BT-01 直接照搬这套做法；同时避免把别人已修的条目改回去 |
| `docs/qa/1.2-window5-round3-fixer.md` | 已落地补丁清单与残留（`W5R3-B-01…03` 编号已被占用） | **只读不写**；不 revert 任何已落地补丁；我的条目沿用测试员的 `BT-` 号 |
| `docs/plan-1.2-step29-B-learner.md` | 第 3 轮学习优化员这一角色的思路（低风险高收益 / 先红后绿 / 至少落地若干条 / 不落地写明理由） | 角色口径。**路径 / 范围 / 分支全部覆盖为窗口 5 档B** |

#### 打算落地的（共 5 条，`BT-01` 必落）

| # | 级 | 款 | 打算怎么改（低风险方向） |
| --- | --- | --- | --- |
| `W5R3-BT-01` | **严重** | `fishing-star` | 横屏三档进关第一眼 `.fs-act` **整颗在可视段外**（`elementFromPoint` 拿回 `null`，11/11）。`FB-01` 把「滚不动」修好了，缺的是最后一步。方向：照搬档C `showPad` / 档A 壳层 `show*` 那一族——**钳完（`capWrap()`）之后自动 `scrollTop`，把抛竿键送进可视段**。`.game-stage{overflow:hidden}` 一个字不动 |
| `W5R3-BT-02` | 一般 | `fishing-star` | 320×568 上鱼后 `show` 相位，「👀 看看它」被自己的壳裁掉 30/44px。先在真机上量清楚「为什么滚 6 趟也救不回」再动手，方向仍在本款自己的壳里 |
| `W5R3-BT-03` | 一般 | `red-blue-tap` | 横屏矮档「本关新玩法」那行被切没了（`.rbt-msg` 22px 见 15/0，且**不在任何可滚容器里**——这一款故意不挂滚动条）。方向：本款那档收紧样式的门是 `max-width:420px`，横过来拿宽度上去了就整档失效；把它改成**按矮判**，字号留白收一档，热区 `.rbt-dot` 一分不动 |
| `W5R3-BT-04` | 建议 | `shape-kingdom` | 作图关主指令 `.shk-ask` 还写「**拖**两个点」，主路早已是「点两个点」（`W5-B-05` 八档 8/8）。改一句话 |
| `LB-13` 一半 | 建议 | `music-stars` | L155 双声部章：键身在矮屏那一档已经是 `pan-x pan-y`，可**键排 `.mst-keys-scroll` 自己还是 `pan-x`**——祖先只许横向，从键上起手竖划一分不动。方向：矮屏那一档里把键排也放成 `pan-x pan-y`。只让手势不动尺寸 |

#### 明确不落地的（写清理由）

| # | 级 | 为什么不落地 |
| --- | --- | --- |
| 平台 `.game-stage{overflow:hidden}` + 那圈 `border:4px` | — | 病灶在 `src/styles.css`，**跨窗口平台文件，本档禁改**，一行不动，原样交窗口1，也不写成已解决 |
| 选关地图末节点 / 章节页签够不着 | 一般 | 病灶在 `level99.ts`，同属禁改，交窗口1 |
| `quiz99` 连错闪答案 / 朗读接缝 / 兜底提示念两遍 | 建议 | 病灶在 `src/games/quiz99.ts`，同属跨窗口共用禁改文件，交窗口1 |
| `W5-B-08` 上鱼后再抛竿 | 原阻断 | 测试员**自己连做 27 次上鱼、21 次跃迁，21/21 够得着，判不复现**；我这一轮也没能复现。**不去动抛竿主路径**——收官轮为一条复现不了的账改主玩法，风险远大于收益 |
| `LB-08` / `09` / `10` / `12` | — | 第 2 轮 learner 当时已写明不落地，测试员本轮**没有新证据推翻**。没有新账就不改 |
| 钓场三个侧模式头上那颗 `◀ 回选关` 74×30 | 建议 | 测试员 B5.5 据实登记，**但它不在 B4 那四条里**，也没进过 1828 个热区的分母。本轮口径是「只结 B4 点名的账」，这一条原样留给下一棒，不在收官轮临时扩范围 |
| `math-farm` | — | 测试员本轮对它**没记新账**（B7 一览里「本轮新记」是空的），教育正确性独立复算 40/40 一致。**没有账就不改**，收官轮不主动动没坏的东西 |

#### 先红后绿的口径

1. 新用例**先在改动前的代码上真的跑一遍**，把失败原文（`AssertionError` 那几行）原样贴进本节的落地表；
2. 再改代码，同一条用例转绿；
3. **用例只增不减**——`it(` 块一个不删。既有断言里如果钉的正是缺陷本身，据实改判并在本节单独点名说明；
4. 真机口径与测试员同源：`npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖**；探针落 `/tmp/w5r3lb/`，**跑完即删、0 字节进提交**；
5. **热区不认 `el.click()`**，定案一律 `document.elementFromPoint(中心点)` 拿回它自己或它的后代；裁切线按 **padding box** 算（`rect.top + borderTopWidth + clientHeight`）。**`BT-01` 的横屏三档由我自己复测**，不引用测试员的数字。

#### 权限边界

- **只动本档五款自己的目录**：`src/games/{fishing-star,red-blue-tap,math-farm,shape-kingdom,music-stars}/**`。
- **一个字都不改**：`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css`。
- 不引入依赖、不动存档 key、CSS 前缀沿用 `fs-` / `fss-` / `rbt-` / `rte-` / `mtf-` / `shk-` / `mst-`、不派 fixer、不套娃。
- **报告只写本文件的 `## 档B · 五款` 这一节**；L0–L3、`## 档A · 五款`、`## 档C · 五款` 与 tester / fixer 报告一个字节不动，
  `docs/qa/1.2-window5-round2-fixer.md` 也一个字节不动。
- 不验 76 款全集、不改 README / 发布说明 / 1.3 基线 / 窗口 1 平台——那些是全局第 29 步，不是本窗档B。

---

## 档C · 五款

（`poop-hero` / `red-blue-tug` / `pinyin-train` / `find-diff` / `kitty-care`）

> 本节由**档C 学习优化员**独立填写。上面的 L0–L3 是并行执行者写的「收官学习优化员」正文，
> **一个字节都没动、没重排、没删**。档A / 档B 两节若还不存在，也不由我代写。

### C0 · 调研与落地计划（动 `src/` 之前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 3 轮（收官）** · **档C 学习优化员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支；**不推 `main` / 不推 `game-1.2` / 禁止 `--force` / 不用 `gh` 开改 PR**）
**基线**：`5a9378b`（`git fetch origin game-1.2-window5` 后一格不差；即档C 测试员续作 C15/C16/C17 收官的那一条）
**没派任何云端子代理**——提示词里那句「请通过 Task 派生」是写给派发方看的，我就是执行者，活自己干完。

**开工前自己复跑的水位**（不照抄别人报告里的数字）：见 C6 的登记，`npm test` / `npm run build` 各跑一遍。

#### 读过的输入（逐条列出，不含糊）

| 文件 | 读的那一节 | 拿来干什么 |
| --- | --- | --- |
| `docs/qa/1.2-window5-round3-tester.md` 档C | **C13**（本轮新记 5 条）、**C15**（五款一览 + 前两轮遗留最终结论）、**C16**（能否随窗口 5 提交 + 「带给下一棒」表） | **主输入**，本节落地项全部出自这三节点名的条目 |
| 同上 | C1 / C2（七档 × 五款 × 八关总账、慢拖重扫定案表） | 判断每一条到底是「静止够不着」还是「慢拖也救不回」——只对后者动手 |
| 同上 | C6（`W5-L-27` 已修 / `W5-L-36` 根因定位到 `renderCure()` 渲染闭包局部变量） | `W5-L-36` 的落地依据 |
| 同上 | C9 / C10（`W5R2-C-01…06` 与 `W5R2-FC-01…08` 真机复验） | **复核第 1、2 轮落地项有没有回退**的底账 |
| 同上 | C11（交窗口1 的 5 条，只量不改） | 划清「不该我动」的边界 |
| 同上 | 文末 `## R3-0…R3-6` 收官节 | **只读，一个字节不动** |
| `docs/qa/1.2-window5-round3-learner.md` L0–L3 | 并行执行者的收官正文（含 `W5R3-C-01` 壳层滚动条、`W5R3-C-02` 喂食托盘穿透两条**已修**） | 避免把已修的两条改回去；确认编号从 **C-03** 起 |
| `docs/qa/1.2-window5-round3-fixer.md` F0–F3 | 已落地补丁清单与残留 | **只读不写**；不 revert 任何已落地补丁 |
| `docs/plan-1.2-step29-B-learner.md` | 第 3 轮学习优化员这一角色的思路（低风险高收益 / 先红后绿 / 至少落地若干条 / 不落地写明理由） | 角色口径。**路径 / 范围 / 分支全部覆盖为窗口 5 档C** |

#### 打算落地的（严重优先，共 5 条）

| # | 级 | 款 | 打算怎么改（低风险方向） |
| --- | --- | --- | --- |
| `W5R3-C-03` | **严重** | `poop-hero` | 横屏两档 6 键 / 12 柄全部在裁切线以下且无可滚祖先。方向：**本款壳层自己钳到「真正看得见的那一段」并自己滚，再把手柄送进眼里**。不动 `.game-stage{overflow:hidden}` |
| `W5R3-C-04` | **严重** | `find-diff` / `red-blue-tug` / `kitty-care` | 同一档横屏的提示键 / 滑杆 / 拉绳钮 / 相册兑换钮 / 打扮件。方向同上，**一款一款在自己的壳里兜**，不抽公共件（跨窗口目录不许动） |
| `W5R3-C-05` | 一般 | `red-blue-tug` | 320×568 L181 `.rbg-msg` `vis 0/16`。四档收紧全用尽仍超出时，**最后一档兜底**让本款壳层自己滚，并用 `touch-action` 把「按住蓄力」的手指与滚动隔开 |
| `W5R3-C-06` | 一般 | `kitty-care` | 320×568 L117 双猫打扮件「滚到底仍露不全」。方向：粘在下沿那条提示行**吃掉了滚动口最后一段**，给滚动内容补一段等高的下留白，让最后一行滚得过它 |
| `W5-L-36` | 一般 | `kitty-care` | 躲纸箱出来护理进度清零。根因已由测试员定位：`renderCure()` 的 `let state` 是**渲染闭包里的局部变量**。方向：**把这一步的进度提到关卡实例上**，重渲染时接着上次的走 |

#### 明确不落地的（写清理由）

| # | 级 | 为什么不落地 |
| --- | --- | --- |
| `W5R3-C-07` | 一般 | 病灶在 `level99.ts`（选关地图 320×568 整张点不着 + 章节页签只剩 2–3 颗）。**那是窗口 1 的平台文件，本档禁改**，一行不动，原样交窗口1 |
| `quiz99` 连错闪答案 | 建议 | 病灶在 `src/games/quiz99.ts`，同属**跨窗口共用禁改文件**，交窗口1。本档五款自己的提示不给答案，另有守门用例 |
| `find-diff` 26×26 图格 | — | 维持第 2 轮 fixer C7·B「本档可修但不动」＋ 本轮测试员 C2「竖屏慢拖后 0 处够不着（`pickForgiving()` 44px 命中半径兜住）」的原判。**我没有低风险改法**：把格子撑到 44px 就得让两张图不能同屏对比，那是玩法改动，收官轮不动玩法 |
| `find-diff` 二级提示直给答案 | 建议 | 第 1 轮已判**有意设计**（`W5-L-34`），第 2 / 3 轮均只登记。改它属于玩法与教育口径的重新定调，**收官轮不动玩法** |
| `W5R2-C-11` 难度倒挂 | 建议 | 测试员 C14 已用量化判据**判关闭**，不再留给下一棒 |

#### 先红后绿的口径

1. 新用例**先在改动前的代码上真的跑一遍**，把失败原文（`AssertionError` 那几行）原样贴进本节的落地表；
2. 再改代码，同一条用例转绿；
3. **用例只增不减**——`it(` 块一个不删。既有断言里如果钉的正是缺陷本身，据实改判并在本节单独点名说明（本轮有 1 处，见 C3）；
4. 真机口径与测试员同源：`npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖**；探针落 `/tmp/w5r3lc/`，**跑完即删、0 字节进提交**；
5. **热区不认 `el.click()`**，定案一律 `document.elementFromPoint(中心点)` 拿回它自己或它的后代；裁切线按 **padding box** 算（`rect.top + borderTopWidth + clientHeight`）。

#### 权限边界

- **只动本档五款自己的目录**：`src/games/{poop-hero,red-blue-tug,pinyin-train,find-diff,kitty-care}/**`。
- **一个字都不改**：`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css`。
- **动物福利红线**：`kitty-care` 生产代码保持**零 `ctx.lose` / 零 `onLose`**，失败只鼓励；改完独立复扫一遍。
- 不引入依赖、不动存档 key、CSS 前缀沿用 `ph-` / `rbg-` / `qz-` / `fdf-` / `ktc-`、不派 fixer、不套娃。
- **报告只写本文件的 `## 档C · 五款` 这一节**；L0–L3 与 tester / fixer 报告一个字节不动。
- 不验 76 款全集、不改 README / 发布说明 / 1.3 基线 / 窗口 1 平台——那些是全局第 29 步，不是本窗档C。

---

### C1 · 落地表（计划 5 条，落地 5 条；严重 2 条全落）

一句话总账：**横过来拿手机，这五款里有四款连第一步都走不动**——不是难，是**手指够不到那颗键**。
四条横屏的病灶长得几乎一模一样：本款壳层「钳了」，可**钳出来的数比屏幕上真正看得见的那一段还大**，
于是滚动口自己有一截停在裁切线以下，滚到底也露不出来；或者干脆因为一条过时的下限**一格都没钳**，
2809px 的内容压在 `overflow:visible` 的盒子里，连个可滚祖先都没有。全部**只在本款自己的壳里兜**，
`.game-stage{overflow:hidden}` 一个字没动。

---

#### C1-1 · `W5R3-C-03` **严重** · `poop-hero` 横屏六颗方向键全在屏外，滚都滚不动

| 项 | 内容 |
| --- | --- |
| 来源 | 测试员 C13 `W5R3-C-03`（640×360 / 844×390 单人 6 键 + 双人 12 柄全部在裁切线以下且**无可滚祖先**） |
| 根因 | `WRAP_MIN_ROOM = 240`（画布底线 + 一盘手柄）被当**下限**用：`Math.max(240, 可视段)`。横屏 640×360 上可视段只有 **190px**，钳出来的滚动口却按 240px 写——**多出来的 50px 连同排在最后的六颗方向键一起停在裁切线以下**。滚动口自己有一截在屏外，滚到底也露不出来 |
| 做法 | ① `WRAP_MIN_ROOM` 改判为 **56**（一颗方向键的高，≥44px 热区），语义从「画布 + 手柄」改成「**滚动口最矮能矮到什么程度**」；② `wrapRoomPx()` 直接返回 `Math.floor(roomPx)`，**一步也不许超过可视段**，可视段比下限还矮就返回 `null`（干脆别钳）；③ 新增 `showPad()`，钳完顺手把 `.ph-pads` 滚进眼里——落地的 `scrollTop` 是 0，五六岁的孩子不会想到先把屏幕往上推；④ CSS 补一条 `@media (hover:none) and (max-height:480px){ .ph-pad-name{display:none;} }`——原来那条只按 `max-width:420px` 判，横屏宽度上去了说明行又冒出来白占 18px |
| 收益 | 640×360 / 844×390 / 320×568 三档横屏 `.ph-key` **6/6 落地即可点**，可滚祖先 `.ph-wrap`（max 177 / 147 / 62），真手指慢拖推得动；竖屏 360×640 / 390×844 **一个像素没变**（可滚祖先＝无，本来就装得下） |
| 风险 | 低。`wrapRoomPx()` 只在「画布已收到底线**仍然**装不下」这一路调用，高屏走不到；返回 `null` 的那一档原本就等于不钳。`showPad()` 滚**最小的那一段**（手柄下沿一进来就收手），画面下半截仍留在屏上，孩子按键时看得见人往哪儿走 |

**先红**（改动前，`src/games/poop-hero/landscapeReach.test.ts`）：

```
FAIL > 钳出来的滚动口绝不许高过可视段（W5R3-C-03） > 640×360 那一幕：可视段 190、整块 381 —— 钳到 190，不是 240
AssertionError: 钳成 240 就等于把口子的下半截钉在屏幕外面: expected 240 to be 190
FAIL > 钳出来的滚动口绝不许高过可视段（W5R3-C-03） > 844×390 那一幕：可视段 220 —— 钳到 220
AssertionError: expected 240 to be 220
FAIL > 钳出来的滚动口绝不许高过可视段（W5R3-C-03） > 矮到连一颗键的中心点都塞不进去才真的不值得钳
AssertionError: expected 240 to be null
FAIL > 接线与样式 > index.ts 挂上滚动条那一路真的叫了 showPad，而且排在写 overflow 之后
AssertionError: expected 'function fitCanvas(): void {\n    can…' to contain 'showPad(wrap)'
FAIL > 接线与样式 > 横过来拿的时候键盘说明行也该藏起来——那一档最挤，白占 18px
AssertionError: expected 'import { meta } from "./meta";\nexpor…' to contain '@media (hover:none) and (max-height:4…'
```
（同文件 `scrollToShowPx` / `showPad` 四组共 13 条一并红，`showPad` 当时还不存在。）

**后绿**（真机 CDP，`elementFromPoint` 定案，**不认 `el.click()`**）：

```
poop-hero L117 640×360  方向键 .ph-key: 落地 6/6，逐档滚动累计 6/6；可滚祖先=[{"cls":"ph-wrap","max":177}]
                        真手指慢拖一趟：scrollTop 118 → 177 ✅ 推得动；裁死的有字叶子：0
poop-hero L117 844×390  方向键 .ph-key: 落地 6/6，累计 6/6；可滚祖先=[{"cls":"ph-wrap","max":147}]
poop-hero L117 320×568  方向键 .ph-key: 落地 6/6，累计 6/6；可滚祖先=[{"cls":"ph-wrap","max":62}]
poop-hero L117 360×640  方向键 .ph-key: 落地 6/6，累计 6/6；可滚祖先=无（竖屏没多出滚动容器）
poop-hero L117 390×844  方向键 .ph-key: 落地 6/6，累计 6/6；可滚祖先=无
```

---

#### C1-2 · `W5R3-C-04` **严重** · 同档横屏 `find-diff` 工具条 / `red-blue-tug` 拉绳钮 / `kitty-care` 相册兑换钮

这一条测试员是**打包记的**，落地也就分三处，一款一款在自己的壳里兜，**没抽公共件**。

**(a) `red-blue-tug` 拉绳钮** —— 四档收紧（`rbg-tight` / `rbg-tighter` / …）全用尽仍旧超出时，
之前**没有第五档**，超出去多少就硬裁多少。新增 `SCROLL_MIN_ROOM = MIN_PULL_H`（56px）与
`needsScroll()`：四档之后仍溢出才给 `.rbg-wrap` 挂 `rbg-scroll` + `max-height` + `overflow-y:auto`，
再 `showPull()` 把拉绳钮送进眼里；CSS 配 `overscroll-behavior:contain`，翻到底不把外面那层也带着走。
**顺序不许换**——滚动永远排在四档收紧后面，能让的高度先让干净。

**(b) `find-diff` 提示键 / 滑杆 / 朗读** —— `viewport` 收到底线之后 `.fdf-wrap` 仍溢出时没有兜底。
新增 `WRAP_MIN_ROOM = TOOL_MIN_H`（44px）与 `wrapNeedsScroll()`，`fitViewport()` 里给 `root` 挂
`fdf-scroll` + `overflow-y:auto` 并把 `.fdf-tools` 滚进眼里。**外加一处只有真机才量得出来的坑**：
平台顶栏 `.l99-stagebar` 在窄屏上会折行，折之前和折之后这一屏的起点差好几像素，
量在折行**之前**就会以为「装得下」而整屏不钳——320×568 上实测正是这一幕（`.fdf-wrap` 382px、
可视段 330px，兜底一次都没触发）。补一次 `requestAnimationFrame` 下一帧重量；
拿不到 rAF（测试桩 / SSR）安静跳过，`destroy()` 里 `liveFit = false` 保证拆台之后那一帧不再动 DOM。

**(c) `kitty-care` 相册兑换钮** —— 病灶是 `LIST_MIN_ROOM = 160` 这条**早退**。
横屏 640×360 上 `.ktc-grid` 的可视段只有 **130px**，于是 `scrollIntoStage()` 直接 `return`：
`max-height:none` / `overflow:visible`，**2809px 的卡片墙一格都没钳，24 颗兑换钮 0/24 够得着，
一个可滚祖先都没有**，真手指慢拖八趟纹丝不动。844×390 的 159.x px 同样卡在这条线上。
改判为 **44**（一颗兑换钮的中心点）：130px 的滚动口一次只看得见半张卡片，确实不好看，
可「看得见半张、翻得到全部」和「一颗都点不着」不是同一个量级的事。

| 项 | 内容 |
| --- | --- |
| 收益 | 拉绳钮 640×360 / 844×390 / 320×568 **2/2 落地即可点**；工具条五档 **3/3 落地即可点**；相册兑换钮四档横竖屏**逐档滚动累计 24/24**，可滚祖先 `.ktc-grid`（max 1595 / 1565 / 2345 / 2273 / 2071）。五款五档「裁死的有字叶子（没有可滚祖先的）」**全 0** |
| 风险 | 低。三处都是**最后一档兜底**：装得下就一个像素不变（竖屏 360×640 / 390×844 实测可滚祖先＝无）。`overscroll-behavior:contain` 只作用于本款壳层 |

**先红**（改动前）：

```
FAIL src/games/red-blue-tug/scrollFallback.test.ts > 四档用尽还装不下就得兜底（needsScroll） > 640×360 第 181 关：这一屏 311、可视段 190 —— 得兜底
FAIL ... > 844×390 第 181 关：288 / 220 —— 得兜底
FAIL ... > 320×568 第 181 关：358 / 330，差的正是那 16px 提示行 —— 也得兜底
FAIL ... > 顺序不许换：滚动永远排在四档收紧后面
AssertionError: needsScroll( 这一档不见了: expected -1 to be greater than -1

FAIL src/games/find-diff/toolsReach.test.ts > 接线与样式 > 先钳两张图、再兜底整屏——顺序反了就白白浪费掉能让的那一截
AssertionError: expected 'function fitViewport(): void {\n    v…' to contain 'wrapNeedsScroll('
FAIL ... > 兜底那一档真的挂了滚动条，而且顺手滚了一次
AssertionError: expected 'function fitViewport(): void {\n    v…' to contain 'root.style.overflowY = "auto"'
FAIL ... > 翻到底不许把外面那层也带着走
AssertionError: 兜底那一档的样式没写: expected -1 to be greater than -1
FAIL ... > 下一帧还得再量一次——顶栏折行之前量到的是「装得下」，兜底就整个不触发
AssertionError: expected '  fitViewport();\n  const win = root.…' to contain 'requestAnimationFrame'

FAIL src/games/kitty-care/landscapeReach.test.ts > 相册在横屏上也得翻得动（W5R3-C-04） > 640×360 那一幕：可视段只有 130px —— 照样钳，照样挂滚动条
AssertionError: 早退了：2809px 的卡片墙一格没钳，24 颗兑换钮全点不着: expected '' to be '130px'
FAIL ... > 844×390 那一幕：可视段 159 —— 卡在 160 那条线上的正是它
AssertionError: expected '' to be '159px'
```

**后绿**（真机 CDP）：

```
red-blue-tug L181 640×360  拉绳钮 .rbg-pull: 落地 2/2，累计 2/2；可滚祖先=[{"cls":"rbg-wrap rbg-tight rbg-tighter rbg-scrol","max":121}]
                           真手指慢拖一趟：scrollTop 115 → 121 ✅ 推得动；裁死的有字叶子：0
red-blue-tug L181 844×390  拉绳钮 2/2；可滚祖先 max 68；慢拖 62 → 68 ✅
find-diff L63 640×360      提示键/滑杆/朗读 .fdf-tools: 落地 3/3，累计 3/3；裁死的有字叶子：0
find-diff L63 320×568      3/3；慢拖 0 → 165 ✅ 推得动
相册兑换钮 640×360: 落地 0/24，逐档滚动累计 24/24；可滚祖先=[{"cls":"ktc-grid","max":1595}]
相册兑换钮 844×390: 落地 0/24，累计 24/24；可滚祖先=[{"cls":"ktc-grid","max":1565}]
相册兑换钮 320×568: 落地 2/24，累计 24/24；可滚祖先=[{"cls":"ktc-grid","max":2345}]
相册兑换钮 360×640: 落地 2/24，累计 24/24；可滚祖先=[{"cls":"ktc-grid","max":2273}]
相册兑换钮 390×844: 落地 4/24，累计 24/24；可滚祖先=[{"cls":"ktc-grid","max":2071}]
```

> 相册这一格「落地 0/24」不是没修好：**24 张卡片本来就是一面翻着看的墙**，任何一屏都只放得下两三张。
> 关键是**从「0 处够得着、没有可滚祖先、慢拖八趟纹丝不动」变成「翻一下就到，24 颗一颗不落」**。

---

#### C1-3 · `W5R3-C-05` 一般 · `red-blue-tug` 320×568 L181 `.rbg-msg` `vis 0/16`

| 项 | 内容 |
| --- | --- |
| 来源 | 测试员 C13 `W5R3-C-05`（明确写了「不是 `FC-07` 回潮，再压一档才出来」） |
| 根因 | C1-2 的兜底只保按钮：`showPull()` 滚到拉绳钮下沿就收手，按钮下面还压着一条 `.rbg-msg`——「看到 🟢 才按住拉」那句**规则说明**。孩子看得见按钮，却不知道这一关要干什么 |
| 做法 | `showPull()` 里加一段：提示行跟按钮**一起装得进滚动口**时就连它一块儿送进来（多滚十几像素而已）；装不下才退回只保按钮 |
| 收益 | 320×568 / 640×360 / 844×390 三档 `.rbg-msg` **1/1 落地即可见**，`vis` 从 `0/16` 到全见 |
| 风险 | 极低。只在「两者跨度 ≤ 滚动口」时多滚一点；跨度超了就原样保按钮——**按钮一旦被顶出去这一关就没法玩了，两者不同价**，这条优先级在代码注释里钉死，另有一条用例守着 |

**先红**：

```
FAIL src/games/red-blue-tug/scrollFallback.test.ts > showPull > 提示行跟按钮一起装得下就连它一块儿送进来（W5R3-C-05）
AssertionError: 提示行还在口子外面——「这一关要干什么」就是看不见: expected 210 to be less than or equal to 190
```

**后绿**：

```
red-blue-tug L181 320×568  提示行 .rbg-msg: 落地 1/1，累计 1/1；可滚祖先=[{"cls":"rbg-wrap rbg-tight rbg-tighter rbg-scrol","max":28}]
red-blue-tug L181 640×360  提示行 .rbg-msg: 落地 1/1，累计 1/1
red-blue-tug L181 844×390  提示行 .rbg-msg: 落地 1/1，累计 1/1
```

---

#### C1-4 · `W5R3-C-06` 一般 · `kitty-care` 320×568 L117 双猫打扮件滚到底仍露不全

| 项 | 内容 |
| --- | --- |
| 来源 | 测试员 C13 `W5R3-C-06` |
| 根因 | `showPlayRow()` 挑的是 `PLAY_ROW_SELECTORS` 里**第一个找得到**的那层，也就是 `.ktc-play`——整个交互层的**外壳**。打扮 / 逗猫关里它还套着一整片场地，横屏 640×360 上量到 **214px > 滚动口 190px**。一段比滚动口还高，`scrollToShowPx()` 只能从它的**上沿**开始露，露出来的是场地，排在最底下的托盘照旧被切：四颗 58×58 打扮件只露出上半截，名字那一行 `.ktc-drag small` **`vis 0/15`，一个像素都看不见** |
| 做法 | 外壳装不进滚动口就**往里退一层**，挑真正要用手指去碰的那一排（`.ktc-tray` / `.ktc-btns`）。判定用的是「减掉粘在下沿那条 `.ktc-msg` 之后的净空间」，一层都不漏地往下找；全都装不下才退回最外层（`fallback`） |
| 收益 | 640×360 / 844×390 / 320×568 / 360×640 四档 `.ktc-drag` **落地 4/4 即可拖**，可滚祖先 `.ktc-wrap`（max 285 / 255 / 242 / 102）；390×844 本来就装得下，可滚祖先＝无，**一个像素没变** |
| 风险 | 低。装得进滚动口时行为与改前**完全一致**（第一层直接命中就 `break`）；只有「外壳比口子还高」这一种情况才往里退 |

**先红**：

```
FAIL src/games/kitty-care/landscapeReach.test.ts > 外壳装不进滚动口时得往里退一层（W5R3-C-06） > 640×360 第 117 关那一幕：.ktc-play 214 > 口子 190，就该改挑托盘
AssertionError: 还是挑了装不下的外壳：托盘只露上半截，名字一行 0 像素可见: expected 0 to be greater than 0
FAIL ... > 退一层之后仍旧减掉粘住的那一行，不然托盘正好停在提示行底下
AssertionError: 少滚了粘住那一行的高度: expected +0 to be 27
FAIL ... > 托盘也没有就往下找按钮排 / 场地，一层都不能漏
AssertionError: .ktc-tray 这一层没被认出来: expected 0 to be greater than 0
```

**后绿**：

```
kitty-care L117 640×360  打扮件 .ktc-drag: 落地 4/4，累计 4/4；可滚祖先=[{"cls":"ktc-wrap ktc-hasfield ktc-fit ktc-scroll","max":285}]
kitty-care L117 844×390  打扮件 4/4；可滚祖先 max 255
kitty-care L117 320×568  打扮件 4/4；可滚祖先 max 242
kitty-care L117 360×640  打扮件 4/4；可滚祖先 max 102；慢拖 84 → 102 ✅
kitty-care L117 390×844  打扮件 4/4；可滚祖先=无（装得下，没多出滚动容器）
```

> 640/844/320 三档那一行「慢拖推不动」是**对的**：手指落点正压在打扮件上，
> 被 `.ktc-drag` 自己的拖拽接管了（这一款就是靠拖件玩的）。四颗件**落地即 4/4 够得着**，不需要先滚。

---

#### C1-5 · `W5-L-36` 一般 · 躲纸箱出来护理进度清零

| 项 | 内容 |
| --- | --- |
| 来源 | 测试员 C6（根因已定位到 `renderCure()` 渲染闭包局部变量），C13 点名交给本棒 |
| 根因 | `renderCure()` 里的 `let state` 是**渲染闭包里的局部变量**。猫躲进纸箱、或者随便什么触发一次重画，整个闭包重建，`cureStart()` 从头再来——刚做对的两步凭空没了，孩子只能把同一步再做一遍 |
| 做法 | 把这一步的进度提到关卡实例上：`Arena` 新增 `private cure: { spec; state } \| null`。`renderCure()` 先看 `this.cure` 的 `spec` 是不是同一件事，是就接着上次的走，不是才 `cureStart()`；`curePick()` / `cureBack()` 统一走一个 `keep()` 出口写回——**进度只从一个入口写，写漏一处就又回到「重画即清零」**（专门有一条用例钉这个）。`startTask()` 开头 `this.cure = null`，换一件事必须清空 |
| 收益 | 重画之后第 1、2 步仍是绿的，护理台上摆的是**下一步**的工具；「↩ 退一步」照旧退得动，退完的位置也熬得过重画 |
| 风险 | 低，且**动物福利红线一格没踩**：这一修只搬了进度的存放位置，没碰任何判负路径。改完独立复扫：`src/games/kitty-care/**` 生产代码 `ctx.lose` / `onLose` 命中数 **0**，失败仍旧只鼓励 |

**先红**（`src/games/kitty-care/cureResume.test.ts`）：

```
FAIL > 护理进度熬得过一次重画（W5-L-36） > 做对一步之后重画（点一下猫）——第 1 步仍旧是绿的，不许退回「先看一看」
AssertionError: 重画一次就退回第 1 步了——刚做对的那一步凭空没了:
  expected '1.❓ 先看一看 → 2.· · · → 3.· · ·' to be '1.🪶 摸摸毛 → 2.❓ 动手照顾 → 3.· · ·'
FAIL > ... > 做对两步之后重画：两步都还在
AssertionError: expected '1.❓ 先看一看 → 2.· · · → 3.· · ·' to be '1.🪶 摸摸毛 → 2.🧤 戴手套梳毛 → 3.❓ 动手照顾'
FAIL > ... > 护理台上摆的也得是「下一步」的工具，不是又回到第 1 步那一批
AssertionError: 重画之后摆回了第 1 步的工具，孩子只能把同一步再做一遍:
  expected [ '轻轻拔小刺', '喝点温水', '摸摸毛', …(2) ] to include '戴手套梳毛'
FAIL > ... > 「↩ 退一步」照旧退得动，而且退完的位置也熬得过重画
FAIL > 动物福利红线：这一修一格没踩 > 进度只从一个入口写，写漏一处就又回到「重画即清零」
AssertionError: expected 'private renderCure(spec: TaskSpec): v…' to contain 'const keep = (next: CureState)'
FAIL > 动物福利红线：这一修一格没踩 > 换一件事必须清空，否则下一件事会顶着上一件的步数开局
AssertionError: expected 'startTask(spec: TaskSpec, onDone: () …' to contain 'this.cure = null'
```

**后绿**：`src/games/kitty-care/cureResume.test.ts` **9/9 通过**。

---

### C2 · 不落地表（逐条写清理由）

| # | 级 | 判定 | 理由 |
| --- | --- | --- | --- |
| `W5R3-C-07` | 一般 | **不落地，原样交窗口1** | 病灶在 `level99.ts`（选关地图 320×568 整张点不着 + 章节页签只剩 2–3 颗）。那是**窗口 1 的平台文件，本档禁改**，一行没动 |
| `quiz99` 连错闪答案 | 建议 | **不落地，交窗口1** | 病灶在 `src/games/quiz99.ts`，同属**跨窗口共用禁改文件**。本档五款自己的提示不给答案，另有守门用例 |
| `find-diff` 26×26 图格 | — | **维持原判，不动** | 第 2 轮 fixer C7·B 判「本档可修但不动」，本轮测试员 C2 复测「竖屏慢拖后 **0 处**够不着（`pickForgiving()` 44px 命中半径兜住）」。**我没有低风险改法**：把格子撑到 44px 就得让两张图不能同屏对比，那是**玩法改动**，收官轮不动玩法 |
| `find-diff` 二级提示直给答案 | 建议 | **不落地** | 第 1 轮已判**有意设计**（`W5-L-34`），第 2 / 3 轮均只登记。改它是玩法与教育口径的重新定调，收官轮不动玩法 |
| `W5R2-C-11` 难度倒挂 | 建议 | **不落地** | 测试员 C14 已用量化判据**判关闭**，不再留给下一棒 |
| `.game-stage{overflow:hidden}` | — | **一个字没动** | 这一条是横屏四款的共同上游，动它一行能省掉本节四处兜底。但它在 `src/styles.css` / `src/ui/**`，是**跨窗口平台文件**，且改它会同时影响 76 款。本档四处修法**全部收在自己的壳里** |
| `pinyin-train` | — | **本轮没动一行** | 测试员 C13 本轮对它**没记新账**，C15 一览里也没有横屏严重项。没有账就不改，收官轮不主动动没坏的东西 |

---

### C3 · 据实改判的既有断言（**逐条点名**，`it(` 块一个没删）

C0 里估的是 1 处，**实际 2 处**，都在这里，都是「原断言钉的正是缺陷本身」：

| 文件 | 原来钉的 | 为什么必须改判 | 现在钉的 |
| --- | --- | --- | --- |
| `src/games/poop-hero/wrapScroll.test.ts` | 「可视段 40px 也照样钳到 `WRAP_MIN_ROOM=240`」 | 那个下限**就是 `W5R3-C-03` 的病灶**——钳出来的数是滚动口自己的高，一旦超过可视段，多出来的一截连同里面的东西一起停在裁切线以下，滚到底也露不出来 | `wrapRoomPx(392, 40)` 返回 `null`；`WRAP_MIN_ROOM ≥ MIN_HOT` 且 `< MIN_CANVAS_H`（下限只能比可视段矮） |
| `src/games/kitty-care/albumScroll.test.ts` | 「可视段矮于 `LIST_MIN_ROOM=160` 就别钳」 | 那条早退**就是 `W5R3-C-04` 相册那一处的病灶**——640×360 上可视段 130px，于是一格没钳，24 颗兑换钮 0/24 够得着 | 只剩 22px 那种「一条缝」仍旧不钳；130px 那一档**照样钳、照样挂滚动条**；`LIST_MIN_ROOM ≥ 44` |

两处都**保留了原用例的名字语义与保护意图**（「再矮也不许压成一条缝」「矮到放不下一颗按钮就别钳」），
只是把那条线挪到真实的位置上，并在用例上方用块注释写明改判理由。
`git diff 5a9378b..HEAD` 里 **删除的 `it(` 行数 = 0**（这 2 行是同一条用例改了标题，不是删）。

---

### C4 · 本档第 1、2 轮落地项复核（没有回退）

| 轮次 | 落地项 | 守门用例 | 本轮状态 |
| --- | --- | --- | --- |
| 第 3 轮并行 | `W5R3-C-01` 壳层滚动条（`.ph-wrap` 自己滚） | `src/games/poop-hero/wrapScroll.test.ts` | ✅ 全绿。本轮**是在这条之上继续修**（把钳出来的数压回可视段），**没有改回去**：`.ph-wrap` 照旧自己挂滚动条 |
| 第 3 轮并行 | `W5R3-C-02` 喂食托盘穿透 | `src/games/kitty-care/fieldReach.test.ts` / `playRow.test.ts` | ✅ 全绿，一行没碰 |
| 第 2 轮 | `W5R2-C-03` 相册那一格自己挂滚动条 | `src/games/kitty-care/albumScroll.test.ts` | ✅ 全绿。本轮**把它的适用范围扩大到横屏**（`LIST_MIN_ROOM` 160→44），机制本身原封不动 |
| 第 2 轮 | `W5R2-C-01…06` / `W5R2-FC-01…08` | `modeBar.test.ts` × 4、`viewportFit.test.ts`、`stageFit.test.ts`、`scorePanel.test.ts`、`cellHotZone.test.ts`、`duoPad.test.ts` | ✅ 全绿 |
| 第 1 轮 | `W5-F-14…19` / `W5-L-22…36` / `W5C-K01` | 五款各自 test 目录 | ✅ 五款 **56 个 test 文件 / 744 条用例全绿**，一条没回潮 |
| 红线 | `kitty-care` 零 `ctx.lose` / 零 `onLose` | 独立复扫 | ✅ 生产代码命中数 **0**（`src/games/kitty-care/**/*.ts` 去掉 `*.test.ts`） |
| 红线 | 禁改公共文件 | `git diff --name-only 5a9378b..HEAD` | ✅ 改动**全部落在 `src/games/{poop-hero,red-blue-tug,find-diff,kitty-care}/`**；`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css` **一个字节没动** |

---

### C5 · 改动登记

**源码 8 个文件**（全在本档五款自己的目录里）：

| 文件 | 改了什么 | 对应 |
| --- | --- | --- |
| `src/games/poop-hero/runtime.ts` | `WRAP_MIN_ROOM` 240→56 并改判语义；`wrapRoomPx()` 不许超过可视段、太矮返回 `null`；新增 `scrollToShowPx()` / `showPad()` | `C-03` |
| `src/games/poop-hero/index.ts` | `fitCanvas()` 钳完调 `showPad(wrap)`；CSS 补 `@media (hover:none) and (max-height:480px)` 藏键盘说明行 | `C-03` |
| `src/games/red-blue-tug/fit.ts` | 新增 `SCROLL_MIN_ROOM` / `needsScroll()` / `scrollToShowPx()` / `showPull()` / `resetScroll()`；`relayout()` 末尾接第五档兜底；`showPull()` 顺带把 `.rbg-msg` 送进来 | `C-04` / `C-05` |
| `src/games/red-blue-tug/index.ts` | CSS `.rbg-wrap.rbg-scroll{overscroll-behavior:contain;}` | `C-04` |
| `src/games/find-diff/runtime.ts` | 新增 `WRAP_MIN_ROOM` / `wrapNeedsScroll()` / `scrollToShowPx()` | `C-04` |
| `src/games/find-diff/index.ts` | `fitViewport()` 接兜底并把 `.fdf-tools` 滚进眼里；顶栏折行后用 rAF 下一帧重量（`destroy()` 里 `liveFit=false`）；CSS `.fdf-wrap.fdf-scroll` | `C-04` |
| `src/games/kitty-care/runtime.ts` | `LIST_MIN_ROOM` 160→44 并改判语义；`showPlayRow()` 外壳装不下就往里退一层 | `C-04` / `C-06` |
| `src/games/kitty-care/arena.ts` | 新增 `private cure`；`renderCure()` 接着上次的走，`curePick()` / `cureBack()` 统一走 `keep()`；`startTask()` 清空 | `W5-L-36` |

**用例 7 个文件，只增不减**：

| 文件 | 新增 / 改动 | `it(` 条数 |
| --- | --- | --- |
| `src/games/poop-hero/landscapeReach.test.ts` | **新增** | 18 |
| `src/games/red-blue-tug/scrollFallback.test.ts` | **新增** | 19 |
| `src/games/find-diff/toolsReach.test.ts` | **新增** | 16 |
| `src/games/kitty-care/landscapeReach.test.ts` | **新增** | 13 |
| `src/games/kitty-care/cureResume.test.ts` | **新增** | 9 |
| `src/games/poop-hero/wrapScroll.test.ts` | 既有，1 条据实改判（见 C3），**没删** | 8（不变） |
| `src/games/kitty-care/albumScroll.test.ts` | 既有，1 条据实改判（见 C3），**没删** | 7（不变） |

**新增 5 个用例文件，共 75 条新用例**；既有用例**删除 0 条**。
文档只动本文件的 `## 档C · 五款` 一节；L0–L3 与 tester / fixer 报告**一个字节没动**。
**零新增依赖**（`package.json` / `package-lock.json` 没动），**没动存档 key**，没派 fixer，没套娃。

真机探针（CDP 直连、`elementFromPoint` 定案）跑在 `/tmp/w5r3lc/`，**跑完即弃，0 字节进提交**。

---

### C6 · 自跑水位

```
$ npm test
 Test Files  710 passed (710)
      Tests  15157 passed (15157)
   Duration  170.52s

$ npx vitest run src/games/{poop-hero,red-blue-tug,pinyin-train,find-diff,kitty-care}   # 本档五款
 Test Files  56 passed (56)
      Tests  744 passed (744)

$ npm run build
 ✓ built in 550ms
 PWA v1.3.0  precache 188 entries (5111.33 KiB)   # tsc 无报错，产物完整
```

真机口径：`npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带
`WebSocket` 直连 CDP，**零新增依赖**；五档视口 `640×360` / `844×390` / `320×568` / `360×640` / `390×844`。
四款五档「**裁死的有字叶子（没有可滚祖先的）：0**」。

**结论**：本档收官轮**落地 5 条**（严重 2 条全落，一般 3 条全落），**不落地 7 条**（逐条写明理由，
其中 2 条病灶在窗口 1 平台文件、2 条属玩法改动不在收官轮口径、1 条已由测试员判关闭）。
横屏那两条严重项从「纯触屏一步都走不动」变成「落地即可点 / 翻一下就到」，
五款可随窗口 5 提交进 `game-1.2`。
