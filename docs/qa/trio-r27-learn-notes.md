# 三人组第 27 轮 · 学习笔记（学习员 C / 1cd5，仅增量）

> 基线：`origin/game-1.3` @ `206d0522`。
> 前文：PR #95 r26（N-138…N-140）、#93 r25 … #90 r22、#87 r21；**PR #94** A 在途 N-117/118/120。
> 本轮 `src/**` 零改动。三视口 390×844 / 915×412 / 1024×768。
> **编号从 N-141 起**。撞车取主干先合版。

## 一、抽验方式

- 读 r22–r26。grep `min-height:4[0-3]px` 与无 `min-height` 的 `cursor:pointer` 菜单卡。
- 未开无头浏览器。

## 二、号段

N-100…N-109 主干；N-110…116 跳过；N-117…140 在途 PR 已占。本轮 **N-141 起**。

并号：N-94/101 dvs 40；N-104 ld-**back**；N-133 rbe-back；N-134 shr-back；N-135 四款返回；N-139 mp-open/rbv-foe 40；N-140 数独 `.sp-key` 40。

## 三、对账

- 主干无新 SHA。N-105 / N-12 族 / C-8 / N-90 只回归。
- **PR #94** 已开：N-117/118/120 源码在途，合入后 A **只回归**，勿第二套页签。
- 五子/象棋返回高度走 `MIN_HIT_PX=44` 父选择器，不开号。
- tap-tiles `.tt-btn-pause` 同时有 `.tt-btn`（44），不开号。

## 四、新抽验（N-141 起）

### N-141 🔧 B · landlord 叫分/出牌 `.ld-btn` 42px

`.ld-btn{min-height:42px}`（叫分、出牌、不出）。N-104 只覆盖 `.ld-back`。开局叫分行 r18 量过 h=48 的可能是另一套，**源码地板仍是 42**。抬到 44；两态（叫分+出牌）都量。勿改牌力/出牌判定。

### N-142 🔧 B · fight-king 模式卡 `.fk-mode` 无 min-height

选人/模式大卡 `padding:14px`、`cursor:pointer`，**没有** `min-height`。内容多时够高，空态或矮横屏收行后可能 <44。补 `min-height:44px`。≠ N-88 开打 CTA（已合）。勿改 `battle` 状态机。

### N-143 🔧 A · 可点元素 `min-height:40/42` 静态巡检

N-136 只扫 `*-back`。本号扫 **button / cursor:pointer** 块里写死 `min-height:40px|42px`（及 41–43）。豁免：非交互容器（`.ld-center`、`.shk-kingdom`、`.pyt-view` 题面）。N-141/139/140/94 修完后应变绿。

## 五、r22–r26 只派不换号

A：N-138 壳层；N-140 数独键；N-136/137/132；N-135 的 block-drop；N-117 对账 PR #94。  
B：N-139；N-135 三款；N-133/134；N-125/126/129；N-108 等。

## 六、纪律

只交本文 + `trio-r27-playbook.md`。下一空号 **N-144**。
