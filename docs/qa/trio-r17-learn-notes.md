# 三人组第 17 轮 · 学习优化员抽验笔记（本轮只学习、只记录，零代码改动）

> 基线：`origin/game-1.3 = 7d2bc1a3`（含 r16 笔记 N-87/N-88、r16 playbook 对账 r13 A / r14 A、r14 A 源码 N-63/N-68/N-73/C-6）。
> **不覆盖** `trio-r14*` / `trio-r15*` / `trio-r16*`。
> **编号红线**：N-58=暂停+跳关门套娃（**不是** 2048）；N-62=2048 四向；N-63…74=r13/r14；N-75…N-85=r15；N-86=brave-path 大厅（树上已有，**跳过**）；N-87/N-88=r16。
> **本工位新伤从 N-89 起。** `src/**` 一行未动。
> 避开：结算弹窗、color-fun 七关型、金钩商店、N-52…57 复测、收藏、麻将对局/连招双人/相册/射击双人/王子两人/仓鼠闯关/贪吃无尽/泡泡海/五子闯关/坦克雪仗闯关、duo-rush 菜单（N-87）、fight-king 双人选人开打（N-88）。
> 换面：连环找不同、时装关、照顾马拉松、绿芽传送关、dvs 2v2/合作课、格斗人机/无尽、音乐沙盒+双声部、射击单人、气球节、果盆闯关、拔河菜单、数独/记忆闯关、跳垫双人、地主对战、音符分轨、连招闯关、合成双人、冰火闯关、飞机闯关、保龄关内、残局学堂、无尽遗迹。主档 **915×412**。管理员 `kangkang`。
> 工装：preview `:4188` + puppeteer-core；`/tmp/trio-r17-audit.mjs` `/tmp/trio-r17-shots/` **不进库**。独立 `createBrowserContext()`。先写 `l99.<id>` 再换 hash 重挂。

## 一、抽验方式

量法同 r4–r16。进场水位待 `npm test` 实测。root 在同源 `#/game/*` 上种 `yiduo-yixing.root.v1` `{mode:"permanent"}`。

## 二、对账（草稿，交卷前用 915 数字替换）

见 playbook。N-86 不换号。N-87/N-88 不换号。

## 三、新抽验（N-89 起）

待浏览器数字。
