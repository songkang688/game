# 1.3 窗口 1 · 本窗三轮验收 · 第 1 轮 · 测试员报告

> 角色:窗口 1 第 1 轮视觉测试员。只写本报告与窗口 1 范围 `*.test.ts` 新增视觉用例,不改绘制实现。
> 范围:基建 `src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` + 九款
> `orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate` `hero-cards` `weiqi-garden` `flight-chess`。
> 依据:`docs/plan-1.3-visual-bible.md`(视觉宪法)、`docs/qa/1.3-window1-plan.md`(十二格登记)。
> 本轮只验视觉,不重开玩法验收。

## 0. 扫描计划(开工承诺)

1. **全量跑与盘点**:`npm ci` → `npx vitest run`(全仓)→ `npx tsc --noEmit` → `npm run build` / `vite preview` 冒烟;记录用例总数对照「只增不减」;清点 kit/motion/runner 与九款文件清单,对照 `docs/qa/1.3-window1-plan.md` 十二格提交 SHA。
2. **六大专项**(收缩到本窗 12 格,逐条给证据):火柴人/草稿圆、金币体积、多角色可区分、纵深/体积、360px 布局、商标扫描。
3. **横向一致性**:kit 光照约定、reduced-motion 降级、destroy 泄漏 ×5、粒子节流。
4. **产出**:本报告 + 机器化扫描用例 `src/games/window1-visual-r1.test.ts`。

---

## 一、全量跑数据与十二格盘点(第三节要求)

### 1.1 全量跑

| 项 | 结果 | 备注 |
| --- | --- | --- |
| `npm ci` | ✅ | 依赖装净 |
| `npx vitest run`(全仓) | ✅ 进场基线 **720 文件 / 15437 例**全绿;补测后收尾复跑 **721 文件 / 15504 例**全绿 | +67 为本轮新增机器化扫描 |
| 用例水位对照 | ✅ 只增不减 | 1.3 开工基线 14994(`docs/plan-1.3-tracker.md` 第二节)→ 进场 15437(+443)→ 本轮交卷 **15504(+67)** |
| `npx tsc --noEmit` | ✅ 0 错误 | |
| `npm run build`(tsc + vite build + PWA) | ✅ | 188 个预缓存条目,构建 698ms |
| `npx vite preview` 冒烟 | ✅ | `GET /` 200;九款 `#/game/<id>` 360px 视口逐款实开,主场景(canvas / SVG)全部渲染,**控制台 0 error** |

### 1.2 十二格 plan ↔ 提交对账

`docs/qa/1.3-window1-plan.md` 登记的 12 个 SHA 逐一 `git log` 验真,**全部存在、无漏做、无「做了没推」**:

| 格 | 登记 SHA | 验证 | 独占产物 |
| --- | --- | --- | --- |
| 1A kit | `9c345d4` | ✅ | `src/art/kit/`(palette/chars/props/fx/testing/index,6 源码 + 6 测试文件) |
| 1B 布局动效 | `dbb8bd4` | ✅ | `src/ui/motion.ts`(116 行)+ `motion.test.ts` |
| 1C runner | `6dea9e6` | ✅ | `src/art/runner/`(sky/track/sprites/speedfx/index,5 源码 + 5 测试文件) |
| 2A orb-arena | `cb2c65a` | ✅ | `art.ts` 892 行 + `art.test.ts` |
| 2B snake-royale | `310dd1d` | ✅ | `art.ts` 1177 行 + `art.test.ts` |
| 2C block-drop | `bb5e090` | ✅ | `art.ts` 728 行 + `art.test.ts` |
| 3A combo-clash | `58a2055` | ✅ | `art.ts` 1534 行 + `art.test.ts` |
| 3B mahjong-bloom | `3f03658` | ✅ | `tileart.ts` 401 行 + `tileart.test.ts` |
| 3C star-estate | `a5b446c` | ✅ | `art.ts` 423 行 + `art.test.ts` + `visual.test.ts` |
| 4A hero-cards | `7a07ddb` | ✅ | `cardart.ts` 618 行 + `cardart.test.ts` |
| 4B weiqi-garden | `0b1dc6b` | ✅ | `art.ts` 736 行(视觉契约 5 个 describe 在 `index.test.ts`,无独立 `art.test.ts`,见问题 G-7) |
| 4C flight-chess | `8575add` | ✅ | `art.ts` 410 行 + `art.test.ts` + `visual.test.ts` |

### 1.3 kit 独占约定抽查

`git log origin/game-1.3..HEAD -- src/art/kit/ src/art/runner/ src/ui/motion.ts`:三份基建自基线以来**各只有本格 owner 的一次提交**(`9c345d4` / `dbb8bd4` / `6dea9e6`),没人改别人的 kit 文件。✅

---

## 二、专项 ①:火柴人 / 草稿圆扫描

**方法**:九款 + kit/runner 全部非测试源码 grep `fillText(`(逐行核上下文),再 grep emoji 码位(`\p{Extended_Pictographic}`)分层登记;逐款人工读主体绘制函数,查「纯圆 + 两点眼」与无渐变纯圆。

**结果表**:

| 款 | canvas `fillText` 用途 | 主体画法 | 判定 |
| --- | --- | --- | --- |
| orb-arena | 名牌文字(`art.ts:718`)、结算名次/曲线标题(`:887-891`) | 果冻球 `drawJellyOrb`:径向渐变 + 瞳孔朝向 + 三种嘴型 + 头饰(星/月);刺球有渐变内芯 + 凶脸;糖豆三造型渐变 + 白描边 + 高光 | ✅ |
| snake-royale | 白底胶囊名牌(`art.ts:816`)、加成计数(`:1100`)、结算文案(`:1170-1175`) | 蛇头椭圆 + 表情 + 头饰,节点三层渐变 + 花纹族;星光豆渐变 + 光晕 + 高光 | ✅ |
| block-drop | 无障碍字母角标(`art.ts:106`,色弱辅助,画在 5 层块上) | 方块 5 层:底影/垂直渐变/顶面高光条/左亮右暗棱线/角标 | ✅ |
| combo-clash | 连击数、KO 横幅图形字(`art.ts:1339/1419`,双层描影) | 二头身 Q 版格斗家 8 层绘制 + 12 款头饰 + 5 种表情;落地软阴影随跳高缩放 | ✅ |
| mahjong-bloom | 无(全 SVG) | 筒=三层同心饼、条=竹节(1 条小鸟)、万=红汉字+黑万,传统三色 | ✅ |
| star-estate | 无(全 SVG) | 四种立牌(花/星/云/月剪影)+ 基座三层 + 五官层 | ✅ |
| hero-cards | 无(全 SVG) | 48×48 Q 版头像:软阴影 + 小身板 + 大脸 + 14 款头饰 + 大眼高光腮红 | ✅ |
| weiqi-garden | 测试桩内(`testkit.ts:269`,非渲染) | 棋子径向渐变(黑/白双色阶)+ 投影 + 左上 30% 高光 + sprite 缓存 | ✅ |
| flight-chess | 无(全 SVG) | Q 版飞机:渐变机身 + 座舱高光 + 双翼 + 螺旋桨 + 机身笑脸;四款尾翼剪影 | ✅ |
| kit | `fx.ts:178` 飞字特效(功能文字) | 朵朵/星星 2–3 头身、三阶光影、眨眼、五姿态、皮肤系统 | ✅ |

**canvas 层 emoji 直出:0 处**(新增用例 `window1-visual-r1.test.ts` ①组 10 例固化为契约)。

**装饰性 emoji 登记**(DOM 文案/HUD/关卡描述层,非测试源码,按码位计):orb-arena 44、snake-royale 38、block-drop 44、combo-clash 52、mahjong-bloom 38、star-estate 148、hero-cards 129、weiqi-garden 40、flight-chess 47。其中 star-estate 的地格主题图标(🧶/🌷/⛱️ 等)直接以 emoji 字符渲染在棋盘格按钮上,占比最高,登记为问题 G-6(一般)。

**问题清单**:严重 0、阻断 0;一般 1(G-6,见第八节)。

---

## 三、专项 ②:金币 / 收集物体积

**方法**:抽出本窗全部「金币/星星/宝石/收集物」绘制函数逐一读层次;判定标准 = ≥2 停渐变(或等效多色阶)+ 高光 + 描边/落影至少其一。

**结果表**:

| 款 | 收集物 | 层次证据 | 判定 |
| --- | --- | --- | --- |
| kit | `drawCoin` | 侧沿厚度暗阶 + 币面 + 内圈环描边 + 星形浮雕 + 左上高光斑(5 层,`props.ts:62-100`) | ✅ |
| kit | `drawStar`/`drawHeart`/`drawGem` | 底影厚度 + 主体 + 高光;宝石另有冠面亮阶 + 右下暗阶 + 四芒星光点 | ✅ |
| orb-arena | 星光糖 `paintStarCandy` | 2 停径向渐变 + 白描边 + 高光点,三种造型;离屏 sprite 缓存(`art.ts:314-390`) | ✅ |
| snake-royale | 星光豆 `drawStarBean` | 光晕 + 2 停线性渐变 + 圆角描边 + 高光(`art.ts:394-427`);sprite 缓存 | ✅ |
| snake-royale | 糖果宝石 `drawGemDrop` | 复用 kit `drawGem` + 价值三档配色 + 落地弹性(`art.ts:530-539`) | ✅ |
| block-drop | 发送星 `drawSentStar` | 尾迹双点 + sparkle 分层 + alpha 渐隐(`art.ts:561-576`) | ✅ |
| combo-clash | 投射球 `drawProjectileOrb` | 三段尾迹 + 光晕 + 主体 + 描边 + 高光 + 四芒星(`art.ts:1442-1469`);元气星徽三阶 | ✅ |
| mahjong-bloom | 开花节点 | 罗盘等级色 + 花纹微弧,契约测试 29 例(`3f03658`) | ✅ |
| star-estate | `coinSVG` | 3 停径向渐变 + 侧沿厚度 + 内圈亮环 + 星形压印 + 左上高光(`art.ts:203-220`);迷你币 `coinTagSVG` 三层 | ✅ |
| hero-cards | 体力心 `heartsSVG` | 实心 + 深描边 + 高光点/空心分层(`cardart.ts:406-421`) | ✅ |
| weiqi-garden | 提子花瓣 `drawPetalBurst` | 花瓣池回收 + 双色阶(契约在 `index.test.ts:592`) | ✅ |
| flight-chess | 塔顶星/着陆花环 | 星带描边 + 高光点;花环四色粉彩(`art.ts:106-116, 299-300`) | ✅ |

**「平涂圆内贴数字/星字符」:0 处**。严重 0、阻断 0。

---

## 四、专项 ③:双人 / 多角色可区分

**方法**:列出多角色款的区分通道(剪影/头饰/附件 vs 颜色),核实形状通道在灰度下仍可分辨;主色差异有既有契约测试兜底。

**结果表**:

| 款 | 角色集 | 形状通道(灰度可辨) | 颜色通道 | 判定 |
| --- | --- | --- | --- | --- |
| hero-cards | 14 英杰头像 | 14 款头饰剪影互异:郁金香冠/观星帽+星镜/将军盔+缨穗/云朵蓬蓬头/三色糖串/平顶石帽+青苔/闪电呆毛+发卡/豆芽/小鸡绒毛+橙嘴/星冠/五瓣花饰/霜叶/露珠呆毛/风铃发簪(`cardart.ts:428-528`);另有 `26 例` 契约(头像互异,`7a07ddb`) | 各英杰主色不同 | ✅ |
| hero-cards | 四门花色 | 五瓣花/双果一叶/叶片/圆石,四剪影完全不同(`cardart.ts:30-66`) | 红黑双通道(契约在 `cardart.test.ts`) | ✅ |
| flight-chess | 四色飞机 | 四款尾翼剪影:圆鳍/星鳍/双叉鳍/燕尾鳍(`art.ts:22-69`,`FIN_NAMES` 契约) | 四色 ink/soft | ✅ |
| combo-clash | 双方格斗家 | 12 款头饰(flower/star/snow/dango/cloud/bear/spark/sprout/chick/peach/…,`art.ts:538-700`)+ 姿态差分 | 双方主色不同(32 例资产契约,`58a2055`) | ✅ |
| star-estate | 四位玩家立牌 | 花/星/云/月四种脑袋剪影(`art.ts:56-111`) | 席位色基座 | ✅ |
| snake-royale | 多蛇同屏 | 花纹族 stripe/dot/gradient/plain + 人类头饰星/月 vs AI 无头饰(`art.ts:545-559`) | 皮肤主色 | ✅ |
| weiqi-garden | 黑白双子 | 双色阶渐变方向一致、明度两极;sprite 双子不同有契约(`0b1dc6b`) | 黑/白 | ✅ |
| kit | 朵朵 vs 星星 | 花瓣头饰 vs 五角星头 + 披风;`CHAR_COLORS` 主色不同色相(335° vs 47°) | 粉 vs 金 | ✅ |

**「只靠颜色区分」:0 款**。严重 0、阻断 0。

---

## 五、专项 ④:纵深 / 体积

**方法**:kit 三阶光影核查;九款按精 2D 定级逐款验体积层;grep three.js 三头(package.json / 九款 / 基建)。

**结果表**:

| 对象 | 体积证据 | 判定 |
| --- | --- | --- |
| kit 三阶 | 角色底色+暗部+高光(chars.ts 全员);道具底影+主体+高光(props.ts);高光统一左上(coin `-0.32r,-0.42r`、star `-0.28r,-0.3r`、朵朵左上两瓣提亮、星星左上星角高光) | ✅ |
| runner | 本窗九款均为棋牌/竞技精 2D,**未引用** `src/art/runner`(grep 0 命中);runner 作为基建自带 58 例契约,留给跑酷窗口使用,无退化风险面 | ✅(N/A) |
| mahjong-bloom 牌面 | 筒三层同心;牌背花纹微弧;桌面 radial+linear 双渐变木边框(`index.ts:101-104`) | ✅ |
| weiqi-garden 子 | 投影(y+1.5 下沉一圈)+ 径向渐变(左上 30% 高光)+ 兜底纯色路径;木盘渐变契约 | ✅ |
| block-drop 块 | 5 层:底影/垂直 3 停渐变/顶面高光条/左亮右暗棱线/角标;7 色 × 尺寸离屏贴图 | ✅ |
| star-estate 建筑 | 小房:方身+底沿暗阶+双层屋顶(暗顶+亮脊)+门窗;酒店:两层楼带+楼层暗带+金星招牌;骰子顶亮/侧暗两阶 | ✅ |
| orb-arena | 果冻球径向渐变 + 分裂拉伸形变;竞技场分区 `drawZone` 渐变 | ✅ |
| combo-clash | 舞台 `drawStage` 渐变 + 角色 8 层 + 落地影随高度缩放 | ✅ |
| hero-cards | 卡面三层(角标/中图/名牌)+ 牌堆三张叠置微旋 + 头像软阴影 | ✅ |
| flight-chess | 骰子顶面亮/侧面暗伪 3D 三面体;机身渐变 + 机腹浅色条 | ✅ |
| three.js | package.json 无 three 系依赖;九款与基建 0 import(新增用例 ④组 11 例固化) | ✅ |

严重 0、阻断 0。

---

## 六、专项 ⑤:360px 布局实测

**方法**:`npm run build` + `vite preview`,Chrome headless(148)CDP 实开九款,`Emulation.setDeviceMetricsOverride` 360×640 与 **320×640 极限**各跑一遍(共 18 组);每款「关卡页初测 → 点『开始冒险 ▶』进对局 → 复测」;量测:文档级横向溢出 px、越界元素清单、可见文本 <14px、交互元素 <40px 触区、控制台 error。原始数据 18 组 JSON 已核对。

**结果表**(初测/对局两阶段合并):

| 款 | 360px 溢出 | 320px 溢出 | 主场景渲染 | <14px 文本 | <40px 触区 | console error |
| --- | --- | --- | --- | --- | --- | --- |
| orb-arena | 0px | 0px | 3 canvas | 共享壳 `l99-star`(见 G-1) | `oa-open` 38px 高(G-5) | 0 |
| snake-royale | 0px | 0px | 1 canvas | 同上 | `sr-open` 38px、`sr-skin` 36px 高(G-5) | 0 |
| block-drop | 0px | 0px | 3 canvas | 同上 | `bd-open` 38px 高(G-5) | 0 |
| combo-clash | 0px | 0px | 5 canvas | 同上 | 无 | 0 |
| mahjong-bloom | 0px | 0px | 15 svg | 同上 + 牌面 SVG 图形字「万」13px(图形非正文,豁免) | `mj-tile` 30×42(G-2) | 0 |
| star-estate | 0px | 0px | 75 svg | 同上 | `se-tile` 23×23/20×20(G-3) | 0 |
| hero-cards | 0px | 0px | 19 svg | 同上 | 无(手牌 56px 宽 ≥44 高) | 0 |
| weiqi-garden | 0px | 0px | 1 canvas | 同上 | 无(棋盘 canvas 自带缩放/十字光标确认) | 0 |
| flight-chess | 0px | 0px | 21 svg | 同上 | `fc-token` 19×19/17×17,但有 `.fc-pick` ≥66×44 走子按钮兜底(G-4) | 0 |

**越界元素核对**:扫描报告里出现的「元素右缘超出视口」条目全部位于 `overflow-x:auto` 滚动容器内,属预期交互而非溢出——`l99-tabs`(`level99.ts:494`)、`mj-hand`(`mahjong-bloom/index.ts:124`)、`hc-hand`(`hero-cards/index.ts:133`)。**文档级 `scrollWidth − innerWidth` 在 18 组全部为 0**。

**1.3 回归判定**:上表全部小触区/小字号条目与 `origin/game-1.2` 的同名 CSS 逐字比对(`git show origin/game-1.2:…`)——`mj-tile{34×46;@media≤480px 30×42}`、`se-tile`、`fc-token`、`sr-skin`、`l99-node-stars{font-size:11px}` 在 1.2 完全相同。**视觉升级导致的新溢出:0(阻断 0)**;遗留项列入问题清单交 C 档酌处。

新增用例 ⑤组 9 例把「九款字号声明 ≥14px」固化为契约(canvas `font:` 与 DOM `font-size:` 双正则,现状全部 ≥14px)。

---

## 七、专项 ⑥:商标扫描

**方法**:宪法第八节口径 `rg -i "<黑名单>" src/ README.md --glob '!.cursor/skills/**'`;黑名单取 `plan-1.2-supervisor.md` 第八节全量 + 1.3 追加(任天堂/迪士尼/4399/Nintendo/Disney/Mario/Pokemon/Subway Surf)。

**结果**:

- **窗口 1 十二格(九款 + kit/runner/motion)非测试源码:0 命中**(新增用例 ⑥组 10 例固化,含注释)。
- 全仓命中逐条核销,全部属于三类豁免:
  1. 各游戏 QA 测试文件自带的黑名单数组(扫描器自身,如 `src/qa-window2/r3lib.ts`、`block-drop/levels.test.ts:25-53`、`snake-royale/levels.test.ts:33-61`、`orb-arena/levels.test.ts:153` 等)——断言「不出现」的反例词表,非孩子可见文案;
  2. `README.md:88/132` 的「斗地主」——1.2 主管文档第八节明文豁免的品类词(`landlord-cards` 条目,中文标题「朵朵抢地主」为原创名),且在窗口 1 范围外;
  3. `.cursor/skills/**` vendored 第三方文档(宪法明文排除,含 `character-sprite-maker/examples/mario-like-platformer.json` 上游示例文件名)。
- 造型侧:四色飞机/骰子/立牌/头像均为原创 Q 版剪影,未见对任何商业游戏角色剪影、标志性配色组合的复刻。

**阻断 0**。

---

## 八、横向一致性抽查

| 项 | 方法与结果 | 判定 |
| --- | --- | --- |
| kit 光照约定被九款遵守 | 高光统一左上(orb-arena `x-0.3r,y-0.34r` 径向渐变心、snake-royale 节点 `-0.25r,-0.3r`、weiqi `x-0.3r,y-0.3r`、star-estate `cx38%,cy32%`、combo-clash `-0.25r,-0.28r`);描边走 `Math.max(≈0.05–0.07×尺寸, 0.5–0.8)` 自适应,44px 主体量级落在 1.5–2px 约定带 | ✅ |
| import kit 而非抄第二份 | 九款全部 `from "../../art/kit"`;grep 全仓自造 `function shade/tint/mixToward`:命中仅在其他窗口款(alien-seek/sling-birds/rainbow-run/garden-guard/fruit-slice/monster-crisis/sprout-defense),**本窗九款 0 命中**(新增用例 ②组 18 例固化) | ✅(窗外命中已在报告备案,不越界处置) |
| prefers-reduced-motion | 运行时:CDP `setEmulatedMedia` 开 reduce 后实开 flight-chess/mahjong-bloom/star-estate/combo-clash/hero-cards——**无限循环装饰 CSS 动画 0 个**,功能按钮全部保留(42–44 个);源码:九款 reduced 引用 3–27 处/款,soft 分支契约已有测试(如 combo-clash `fxBudget(soft)→{rays:0,stars:0}`) | ✅ |
| destroy 泄漏 ×5 | CDP 实测 orb-arena/combo-clash/mahjong-bloom/flight-chess/weiqi-garden/star-estate 六款:进→退 ×5 后 `.view canvas` 0、`.game-screen` 0、再进正常挂载 1 屏、全程 console error 0 | ✅ |
| 粒子 CPU 节流 | combo-clash:命中粒子预算 `fxBudget`(正常 6–8 根射线,soft 0)、彩带 `CONFETTI_COUNT=20` 封顶;orb-arena/snake-royale/weiqi/block-drop:糖豆/星豆/棋子/方块全部离屏 sprite 缓存,主循环 `drawImage` 一笔贴上 | ✅ |

---

## 九、问题清单(全部量化)

**阻断:0 条 严重:0 条 一般:6 条 建议:1 条**

| 编号 | 位置(文件+函数/选择器) | 现状(量化) | 期望 | 严重度 | 1.3 回归? | 建议责任文件 |
| --- | --- | --- | --- | --- | --- | --- |
| G-1 | `src/games/level99.ts:512` `.l99-node-stars{font-size:11px}`(关卡节点 ★ 星级) | 装饰星形字符 11px(实测计算样式 10–12px),低于宪法 14px 文字线;为共享关卡壳,九款关卡页全部受累 | 星级改画形(SVG/伪元素)或提到 ≥14px;至少补 `aria-hidden` 语义确认 | 一般 | 否(1.2 逐字相同) | `src/games/level99.ts`(共享壳,建议统筹派本窗 C 档或壳层 owner 处理) |
| G-2 | `src/games/mahjong-bloom/index.ts:224` `@media(max-width:480px) .mj-tile{width:30px}` | 手牌按钮 30×42px,横向 30px < 44px 热区线;手牌区有 `overflow-x:auto`(`:124`)兜底不溢出 | 维持 42px 高度、用 padding/伪元素把可点区扩到 ≥40px 横向,或选中二段确认(已有 `mj-cur` 选中态,可视为二段确认——请 C 档裁决后登记结论) | 一般 | 否(1.2 逐字相同) | `src/games/mahjong-bloom/index.ts` |
| G-3 | `src/games/star-estate/index.ts:133` `.se-tile`(棋盘地格按钮) | 360px 下 23×23px、320px 下 20×20px,远低于 44px;但地格点击仅用于看地契说明(走子由骰子驱动),底部 `se-deed` 信息行 36px 高可达同一信息 | 地格信息入口改为 ≥44px 的替代控件(如地格列表),或地格 focus 环 + 说明浮层维持现状并登记豁免 | 一般 | 否(1.2 逐字相同) | `src/games/star-estate/index.ts` |
| G-4 | `src/games/flight-chess/index.ts:146` `.fc-token`(棋盘上的飞机按钮) | 360px 下 19×19px、320px 下 17×17px;**已有** `.fc-pick`(`:209`,min 66×44px)走子选择按钮兜底,token 为重复入口 | 保持 fc-pick 为主入口即可;token 可加 4px 透明外扩热区 | 一般 | 否(1.2 逐字相同) | `src/games/flight-chess/index.ts` |
| G-5 | `src/games/snake-royale/index.ts:135` `.sr-skin{min-height:36px}`;`oa-open`/`sr-open`/`bd-open` 双人模式按钮实测 38px 高 | 交互按钮 36–38px 高,低于 44px 线 6–8px | `min-height:44px` 对齐宪法 | 一般 | 否(1.2 逐字相同) | `src/games/{snake-royale,orb-arena,block-drop}/index.ts` |
| G-6 | `src/games/star-estate/index.ts`(地格渲染,配 `levels.ts` 地格 emoji 字段) | 地格主题图标以裸 emoji 字符渲染于棋盘格(🧶/🌷/⛱️ 等 24 格);全款非测试源码 emoji 计 148 个,为九款最高(其余款 38–129,主要在文案层) | 地格图标换 kit 风格矢量小图标(参照本款 `houseSVG` 已有画法),文案层 emoji 可留 | 一般 | 否(1.2 已如此,1.3 升级了立牌/骰/币/房但未覆盖地格图标) | `src/games/star-estate/art.ts` + `index.ts` |
| G-7 | `src/games/weiqi-garden/`(无独立 `art.test.ts`) | 视觉契约 5 个 describe 块内嵌在 `index.test.ts:538-756`,覆盖木盘渐变/sprite 双子/花瓣回收/reduced/标记形状/光标对比度,内容达标但文件组织与其余八款不一致 | 后续轮次可拆出 `art.test.ts` 对齐结构(纯组织问题,不扣视觉分) | 建议 | — | `src/games/weiqi-garden/art.test.ts`(新增) |

**交接**:G-1–G-6 移交本窗本轮 C 档(fixer)裁决/修复;G-7 供 B 档(learner)归档为结构约定。测试员按权限未改任何绘制代码。

---

## 十、本轮新增机器化扫描用例

新增 `src/games/window1-visual-r1.test.ts`:**67 例全绿**,把六大专项可机器化的部分固化为源码级契约:

| 组 | 用例数 | 内容 |
| --- | --- | --- |
| ① emoji 直出 | 10 | 九款 + 基建:含 `fillText(` 的源码行不携带 emoji 字面量 |
| ② kit 契约 | 18 | 九款均 import `art/kit`;九款不自造 shade/tint/mixToward |
| ③ reduced | 9 | 九款源码保留 reduced-motion 引用 |
| ④ 禁 three | 11 | package.json 无 three 系依赖;九款 + 基建不 import three |
| ⑤ 字号 | 9 | 九款 DOM `font-size:` 与 canvas `font:` 声明全部 ≥14px |
| ⑥ 商标 | 10 | 九款 + 基建非测试源码黑名单 0 命中(含注释) |

---

## 十一、结果矩阵(12 格 × 六专项)

| 格 | ①火柴人 | ②金币体积 | ③多角色 | ④纵深体积 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| kit(1A) | ✅ | ✅ | ✅ | ✅ | ✅(N/A 布局) | ✅ |
| motion(1B) | ✅(N/A 绘制) | ✅(N/A) | ✅(N/A) | ✅(N/A) | ✅(时序契约) | ✅ |
| runner(1C) | ✅ | ✅(N/A) | ✅(N/A) | ✅(基建自检 58 例) | ✅(N/A) | ✅ |
| orb-arena(2A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| snake-royale(2B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| block-drop(2C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| combo-clash(3A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mahjong-bloom(3B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| star-estate(3C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| hero-cards(4A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| weiqi-garden(4B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| flight-chess(4C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

注:⑤ 列 ✅ 指「文档级零溢出、零 console error、无 <14px 正文、1.3 零回归」;1.2 遗留小触区已按 G-1–G-5 单列,不改变格判定。矩阵 72 格全 ✅,阻断/严重 0 残留。

---

## 十二、验收口径核对(第八节要求)

- 测试全绿且只增不减:✅ 14994 → 15437(进场)→ 15504(本轮 +67,收尾复跑全绿);
- 构建 / tsc 全绿:✅;
- 六大专项阻断 0:✅(商标 0 命中、360px 新溢出 0);
- 严重级清单:0 条;一般 6 条 + 建议 1 条已列表移交 C 档;
- 测试员未改 `src/games/**`/`src/art/**`/`src/ui/**` 任何绘制实现:✅(diff 仅本报告 + `window1-visual-r1.test.ts`)。
