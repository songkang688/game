# 窗口 2 · 1.2 三轮验收 第 1 轮 · 监督修复员 · 包 A

> 角色：QA 监督修复员（把测试员记的阻断 / 严重清零，一般级登记）
> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 基线：`origin/game-1.2-window2`（`58e420b`）
> 模型：`claude-opus-5-thinking-high-fast`

## 〇、修复计划（动代码前先立此存照）

### 0.1 边界

- 只改上面这 5 个游戏目录。别人的 10 款与窗口 3/4/5 的款一个字不动。
- 不动窗口 1 的平台文件：`src/ui/home.ts` `src/ui/rootGate.ts` `src/ui/playModes.ts`；
  **不改 `src/engine/**`**。
- 不改测试员 / 学习优化员的报告原文（`docs/qa/1.2-window2-round1-tester*.md`、
  `docs/qa/1.2-window2-round1-learner*.md`），也不回滚学习优化员落地的 6 条 AI 改进
  （L-A-1…L-A-6）。
- 不写全局 `docs/qa/1.2-round1-fixer.md`；报告只写两处：本文件（独占）与
  `docs/qa/1.2-window2-round1-fixer.md` 的 `## Pack A` 一节（只追加）。

### 0.2 本轮必须清零的三条（阻断 0 / 严重 3）

| 编号 | 款 | 现象 | 打算怎么修 |
|---|---|---|---|
| PA-FS-1 | fruit-stack | 360px 上 `.fs-open` 32.2 / `.fs-btn` 27.6 / `.fs-back` / `.fs-pick` 都 < 44px；`.fs-key` 在 `@media (max-width:420px)` 里被压到 42px | 给四个选择器补 `min-height:44px`（按钮默认就把文字竖直居中，不改版式）；窄屏那段只缩字号与最小宽度，高度留 44px |
| PA-PS-1 | pool-stars | `Esc` 只挡住 rAF 与台面指针，暂停期间方向键仍改瞄准角、F 蓄力松手照样出杆、击球钮也拦不住 | `view.ts` 里 `nudge` / `startCharge` / `releaseCharge` 一律先看 `paused`；`onKeyDown` 除 `Escape` 外暂停时直接吞掉；进暂停时把正在蓄的那一杆取消掉；`spin` / `place` 两个钮也一起挡住 |
| PA-JQ-1 | junqi-camp | 双人同屏只有一个共用光标，WASD 与方向键走同一个 `moveCursor`，朵朵回合里星星按方向键照样拨得走 | `view.ts` 把 `cursor` 换成 `cursors: Record<Side, Pos>`，`moveCursor` / `activate` / `cancel` 都带 `side`；界面上只画「当前该走的那位真人」的光标；`index.ts` 把 WASD+F/G 派给朵朵、方向键+L/K 派给星星（单人局里两套都归朵朵，老键位不丢） |

顺手一起收的（与 PA-JQ-1 同根、不另开改动面）：

- **PA-JQ-2**（一般）：星星那套 `L` / `K` 一个都没接 —— 上面的按座位派发天然把它补上。

### 0.3 每条的做法

复现测试 → 修 → 绿。测试员标了「【已知问题】」的用例断言的是**修复前**的行为，
修完必须把断言翻成**修好后**的行为（先红后绿），标题里的「【已知问题】」一起摘掉，
这样「到底改没改」在 CI 上一眼看得出来。

### 0.4 一般级的处理

本轮不修的 13 条一般级（PA-DM-1/2/3、PA-FS-2/3、PA-PS-2/3、PA-JQ-3、PA-CG-1/2/3/4）
在本文件第三节逐条登记，留给第 2 轮。

### 0.5 收尾

fetch → rebase `origin/game-1.2-window2` → `npm test` 与 `npm run build` 全绿 → 普通 push。

## 一、修了什么

（动手后回填）

## 二、新增 / 翻面的回归测试

（动手后回填）

## 三、遗留

（动手后回填）
