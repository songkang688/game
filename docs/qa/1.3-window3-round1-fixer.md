# 1.3 窗口 3 · 第 1 轮视觉验收 · 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 1 轮 · C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`、`docs/plan-1.3-step27-C-fixer.md`（思路）、`docs/qa/1.3-window3-round1-tester.md`（A 档输入）、`docs/qa/1.3-window3-round1-learner.md`（B 档输入）。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、修复计划（动手前提交，commit `6c7086a`）

开工时 `docs/qa/1.3-window3-round1-learner.md` 尚未产出：按 step27-C 第三节先做自查修复 + A 档清单落地，learner 报告出来后再合并跟进（实际执行中 B 档报告如期产出，TOP10 建议全部落地，见下）。

1. **自查底线（step27-C 第四节，仅 9 款）**：商标黑名单 rg 复扫（含注释）；`fillText` emoji 直出复扫；360/320 宽复跑；reduced-motion 复验；kit 纪律核对（本窗无共享 kit，各款独占 `art.ts`，确认无人越界改窗口 1 的 kit）。
2. **A 档问题清单落地**（阻断 0 / 严重 0，一般 4 条 + 建议 3 条）：P-01~P-04 全修，P-06 / P-07 酌情落地，P-05 移交统筹。
3. 每修一处一个 commit（`fix(visual-r1): <款> <问题>`），配套视觉用例只增不减。
4. 收尾合并态复验：fetch → rebase → `npm test` → `npm run build` → `npx vite preview` 冒烟 → 合并态 SHA / 用例总数 / 构建体积写回本报告。

红线重申：只动绘制函数 / CSS / 视觉常量 / 本款 `art.ts`；判定、数值、关卡、存档 key、玩法断言不碰；遵守各款 1.3 提示词既定规格（光源左上 45°、描边 1.5–2px、粉彩色板、reduced 约定）；禁止 three.js；禁止 force push。

---

## 一、修复清单（14 commit：1 计划 + 13 修复）

修复顺序按铁律执行：本轮 A 档**阻断 0、严重 0**，故从「一般」4 条开修，再落 B 档 TOP10 建议。每条一个 commit，配套用例全部收进各款新增的 `visual-fix-r1.test.ts`（9 款共 9 个新测试文件、46 个新用例，只增不减）。

| # | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | A 档 P-03（一般）+ B 档 TOP10 之 3 | `fight-king/index.ts` 内嵌 CSS | `.fk-sub` 13→14px、`.fk-mode-d` 12.5→14px（A 档点名两处），顺带把同屏菜单/信息层 `.fk-swap` `.fk-info` `.fk-train-hint` `.fk-live` 及 520px 断点内 `.fk-padbtn` `.fk-combo` 全部提到 ≥14px；360px 视口复核模式卡不换行撑破 | `fight-king/visual-fix-r1.test.ts`（含媒体查询内字号断言） | `41c5992` |
| 2 | A 档 P-04（一般） | `xiangqi/art.ts` 新增 `robotAvatarSVG`、`index.ts` `avatarHTML()` | 「棋灵象」座位头像从 🐘 emoji 兜底换成画制 SVG 小象头 | `xiangqi/visual-fix-r1.test.ts` | `a59d7a0` |
| 3 | B 档 TOP10 之 4（P-04 重做对齐规格） | 同上 | 按 B 档规格重做：复用棋子 sprite 材质——`PIECE_FACE` 圆面 + `PIECE_WALL` 月牙侧壁 + `BLACK_INK` 2px 描边勾「圆头+大耳+卷鼻」，耳内 `RED_INK` 淡红；与 14 种精绘棋子同屏无代差 | 同上（断言材质常量复用与 SVG 结构） | `9223245` |
| 4 | A 档 P-06（建议）+ B 档 TOP10 之 5 | `hue-hand/art.ts` 新增 `botFaceSVG`、`index.ts` `BOT_FACES` | 三个电脑对手头像 🐰🐼🦊 DOM emoji 换成 34px 画制 SVG（兔=长耳粉耳窝 / 熊猫=黑耳黑眼斑 / 狐=尖耳白面颊），同卡面规格：对角渐变底 + 1.5px 深描边 + 左上 gloss | `hue-hand/visual-fix-r1.test.ts`（无 emoji、三只剪影互异、三档明暗） | `b152f9a` |
| 5 | A 档 P-01（一般）+ B 档 TOP10 之 1 | `ocean-munch/art.ts` 新增 `titleFitPx`、`index.ts` `drawHome`/`drawThemes`/`drawRivalPick` | 360 视口主页标题「海底大胃王」被图鉴徽章盖住 ≈34px → 标题按可用宽度量宽缩字号（下限 18px）+ 锚点让位；主题页窄屏改堆叠布局避按钮 | `ocean-munch/visual-fix-r1.test.ts`（`titleFitPx` 纯函数缩字/下限 + 集成断言） | `02e317c` |
| 6 | A 档 P-02（一般）+ B 档 TOP10 之 2 | `rainbow-run/art.ts` 新增 `titleFitPx`、`index.ts` 战役主页/章节页标题 | 标题「十二大世界」末字与 🎁 收藏册按钮重叠 ≈18px → 最大可用宽 `w-100` 量宽缩字号（下限 18px），章节页同法避返回钮 | `rainbow-run/visual-fix-r1.test.ts` | `9d1c128` |
| 7 | A 档 P-07 前半（建议）+ B 档 TOP10 之 6 | `rainbow-run/art.ts` 新增 `drawHeartPip` `drawNoteChip`、`index.ts` 局内 HUD | 局内 HUD 💗🤍🍬⭐♪ 换绘制小图标：星币复用 `drawCoinFrame`、星复用 `drawStarPickup`、心=两圆一角路径（实/空两态）、音符=画制音符片，1.5px 深描边对齐原字号盒 | `rainbow-run/visual-fix-r1.test.ts`（HUD 行无 emoji 静态断言） | `094537c` |
| 8 | A 档 P-07 后半（建议）+ B 档 TOP10 之 7 | `ocean-munch/art.ts` 新增 `drawHeartPip`、`index.ts` BOSS 条/玩家 HUD | BOSS 血心 ❤🤍 与玩家心心、护盾读秒 🛡 换绘制：心=双圆一角渐变路径（实/空），护盾复用 `drawShieldBadge` | `ocean-munch/visual-fix-r1.test.ts`（心形渐变/描边、无 fillText emoji） | `e0821cc` |
| 9 | 同上（P-07 后半续） | `ocean-munch/art.ts` 新增 `drawMiniStar`、`index.ts` 关卡地图 | 地图节点 👑 复用 `drawCrown` 缩放画制、⭐/☆ 星级换 `drawMiniStar`（实/空） | 同上 | `7894161` |
| 10 | B 档 TOP10 之 8（密度 -1） | `tap-tiles/index.ts` 背景段新增 `drawDriftSymbols` | 上半屏加一层漂浮列符号饰层：5 枚复用 `art.ts` `traceLaneSymbol`（半径 6–10px），`globalAlpha 0.07`，14–20s 周期缓慢上飘；reduced 静止定格；不遮判定线、不进命中区 | `tap-tiles/visual-fix-r1.test.ts`（透明度/复用/reduced/判定线以上） | `d85dfc0` |
| 11 | B 档 TOP10 之 9（密度 -1） | `match-stars/view.ts` 主题 CSS | 三主题背景各叠 1 层静态剪影饰（纯 background 多层，不加动画）：晨光 2 朵柔云影（白 ≤20%）/ 森林底部 3 座圆顶树影（深绿 ≤12%）/ 星夜 5 颗 2px 星点（白 20%） | `match-stars/visual-fix-r1.test.ts`（逐主题数量/alpha/静态断言） | `51fc06f` |
| 12 | B 档 TOP10 之 10 上（密度收尾） | `hop-pads/index.ts` 远景云段 | 天空云 3 团 → 5 团：新增两团同 `drawCloudPuff`、alpha 0.5/0.6、尺寸取现有区间，高度（0.26/0.58）与相位均与原云（0.34/0.5）错开不成排 | `hop-pads/visual-fix-r1.test.ts` | `88598e5` |
| 13 | B 档 TOP10 之 10 下（密度收尾） | `gomoku/view.ts` `drawGrid()` | 盘外一圈极淡径向暗角：圆心=盘心、内圈全透明、外圈 `rgba(74,50,32,0.06)`，一次填充画在精装框之前，不进动画循环 | `gomoku/visual-fix-r1.test.ts` | `ef72248` |

**A 档一般 4 条（P-01~P-04）全修；建议 3 条中 P-06 全修、P-07 修完最高可见度部分（局内 HUD / BOSS 条 / 地图节点）、P-05 非本窗职权移交统筹。B 档 TOP10 全部 10 条落地。**

---

## 二、自查结果（step27-C 第四节底线检查）

1. **商标扫描**：9 款 `rg -in` 黑名单（4399 / 马里奥 / 皮卡丘 / 托马斯 / Kitty / UNO / PopStar / Candy Crush / Doodle Jump / Subway Surfers / Temple Run / Piano Tiles / osu / Feeding Frenzy / Nemo / 索尼克 / 街霸角色名等）——**实际侵权 0 处**。命中项全部为误报：各款 `visual-scan.test.ts` / `copy.test.ts` / `smoke.test.ts` 的**禁词表本身**（扫描器自带的黑名单常量）、`hue-hand/art.ts` 渐变色值 `#5b4399`（十六进制颜色恰含数字串 4399）、`gomoku/session.test.ts` 变量名 `broken`（子串含 ken）。造型目检：fight-king 八位自创行头无街霸角色近似、xiangqi 楷体阴刻无协会徽记、rainbow-run 跑者无红帽字母徽——均合规。
2. **裸 emoji 主角**：`fillText(` emoji 直出复扫——核心资产（主角/棋子/收集物/障碍）0 处，与 A 档结论一致。装饰层残留见遗留清单第 2/3 条。
3. **360px / 320px**：A 档已实测 9 款硬溢出 0px；本轮修复全部为绘制层加法（量宽缩字、canvas 内画图标、CSS 叠层、canvas 饰层），不改任何布局宽度；`titleFitPx` 两处专治 A 档点名的 360 视口画布内遮挡（P-01/P-02）。fight-king 字号提升后经 520px 断点复核不撑破模式卡。**无新溢出。**
4. **reduced-motion**：本轮新增的动效只有 tap-tiles 漂浮饰层与 hop-pads 新云的视差漂移，前者 reduced 时定格（有用例钉住），后者复用既有 `reduced ? 0 : drift` 通道自然静止；match-stars 剪影饰与 gomoku 暗角为纯静态。既有 reduced 约定未被破坏（全库用例含各款 reduced 断言，全绿）。
5. **kit 纪律**：本窗**无共享 `src/art/kit/`**，9 款各自独占 `src/games/<id>/art.ts`；本人全部 commit 触碰的文件仅限 9 款的 `index.ts` / `view.ts` / `art.ts` / 新增 `visual-fix-r1.test.ts` 与本报告——**无越界改动，窗口 1 的 kit 未被触碰**（详见第五节）。

---

## 三、遗留清单（修不动 / 降级处理，交下一轮）

| # | 位置 | 内容 | 理由 |
| --- | --- | --- | --- |
| 1 | 共享外壳 `src/games/level99.ts` L513 `.l99-star` | 选关页签内 10px「★」装饰字符 | A 档 P-05 已判定：全 188 关共用外壳，**非本窗职权**，改动波及全库，移交统筹/主管统一处理 |
| 2 | `rainbow-run/index.ts` 战役地图/结算层（约 6 行） | 🔒🏁 地图节点、结算面板 🍬⭐、模式标签 ♾️🎯 | B 档 TOP10 之 6 明示「战役地图 5 行可留到下一轮」；均为菜单/结算装饰层（规格允许），本轮已修完最高可见度的局内 HUD，一轮改面过大风险高 |
| 3 | `ocean-munch/index.ts` 地图/图鉴层（约 8 行） | 🔒⚔ 地图节点、📖❓ 图鉴页、💡 提示行、⚡ 增益标 | B 档 TOP10 之 7 明示「其余登记留后续轮次」；本轮已修完心/星/皇冠/护盾最高频 4 类 |
| 4 | `fight-king/index.ts` 战斗 HUD 微标签 | `.fk-ch-n` `.fk-name` `.fk-clock-r` `.fk-pad-name` `.fk-fd` 仍为 12–12.5px | 这些是战斗 HUD 内为 360px 不溢出刻意收小的名牌/时钟/键标（1.2 既定取舍），A 档 P-03 未点名；贸然放大有把 HUD 挤溢出的回归风险，登记待下轮携 360 实测一并评估 |
| 5 | `fight-king/index.ts` 菜单模式卡 🥊🤖🏯🔥🎓 | DOM emoji 小图标 | A 档判定「按规格可留」（装饰层），未列入 P 项；下轮若做 fight-king 图标统一可顺带画制 |
| 6 | `hop-pads/index.ts` 画布暂停字「⏸ 已暂停」、`tap-tiles` 1 行装饰 emoji | 功能提示层 emoji | A 档登记为规格允许的提示层；换画制收益低，随下轮 HUD 统一处理 |

---

## 四、合并态复验（三人共同收尾，由 C 执行）

1. 三人均已 push：`git fetch origin game-1.3-window3` → rebase 本地 6 笔修复至 B 档合并态登记 `b5747a8` 之上（无冲突）→ 普通 push。确认三份报告（tester / learner / fixer）与全部修复均在分支上。
2. 复验命令与结果（在合并态 `ef72248` 上执行）：
   - `npm test`：**全绿，732 文件 / 15342 用例**（对比 B 档登记的合并态 `e0821cc` 728 文件 / 15324 例：+4 文件 / +18 例，只增不减；本轮 C 档累计新增 9 个 `visual-fix-r1.test.ts`、46 个用例）；
   - `npm run build`：**全绿**，PWA precache 188 entries / 5226.06 KiB，`dist/` 总体积 5.5 MB；
   - `npx vite preview` 冒烟：`/` → 200，`/sw.js` → 200，`/manifest.webmanifest` → 200，**全绿**。
3. **合并态 SHA：`ef722482813ab7c9b49518aaf5c7aad63610a52a`**（本报告为其上的 docs-only 提交，不含代码改动）。

---

## 五、kit 纪律检查结论

- 本窗无共享 `src/art/kit/`；逐款核对 9 款均独占 `src/games/<id>/art.ts`，无跨款引用他款 art、无引用窗口 1 kit 的路径。
- 本人 14 笔 commit 触碰文件全集：9 款的 `index.ts` / `view.ts` / `art.ts`、9 个新增 `visual-fix-r1.test.ts`、本报告。**未触碰**：共享外壳 `level99.ts`、引擎层、他窗游戏目录、A/B 两位的报告文件（只读未改）、任何玩法判定/数值/关卡/存档 key/玩法断言。
- 无越界改动需要恢复，无记名事项。

---

## 六、窗口 3 第 1 轮视觉验收结论

- **阻断级：0 发现 / 0 残留**（商标扫描实际侵权 0；360/320 硬溢出 0，无新溢出）。
- **严重级：0 发现 / 0 残留**（六大专项 A 档全过：核心资产无裸 emoji、无平涂圆、双人可分、2.5D 达标、金币有体积）。
- **一般级：4 条全修**（P-01~P-04，均为 1.2 遗留，本轮清零）。
- **建议级：P-06 全修、P-07 修完最高可见度部分、B 档 TOP10 全部落地**；遗留 6 条全部登记（上表），无一属阻断/严重。
- `npm test` / `npm run build` / `vite preview` 冒烟全绿，用例只增不减，合并态 SHA 已入报告。

**结论：窗口 3 第 1 轮视觉验收通过，阻断清零、严重清零，遗留清单移交第 2 轮。**
