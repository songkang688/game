# 1.3 窗口 3 · 第 1 轮视觉验收 · 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 1 轮 · C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`、`docs/plan-1.3-step27-C-fixer.md`（思路）、`docs/qa/1.3-window3-round1-tester.md`（A 档输入）。

## 〇、修复计划（动手前提交）

开工时 `docs/qa/1.3-window3-round1-learner.md` 尚未产出：按 step27-C 第三节先做自查修复 + A 档清单落地，learner 报告出来后再合并跟进。

1. **自查底线（step27-C 第四节，仅 9 款）**：商标黑名单 rg 复扫（含注释）；`fillText` emoji 直出复扫；360/320 宽复跑；reduced-motion 复验；kit 纪律核对（本窗无共享 kit，各款独占 `art.ts`，确认无人越界改窗口 1 的 kit）。
2. **A 档问题清单落地**（阻断 0 / 严重 0，一般 4 条 + 建议 3 条）：
   - P-01（一般）ocean-munch 360 视口主页标题被图鉴徽章遮挡 → 标题避让；
   - P-02（一般）rainbow-run 战役主页标题与收藏册按钮重叠 → 标题避让；
   - P-03（一般）fight-king `.fk-sub` 13px / `.fk-mode-d` 12.5px → 提到 ≥14px；
   - P-04（一般）xiangqi AI 头像 🐘 emoji 兜底 → 画制头像；
   - P-06（建议）hue-hand 对手头像 🐰🐼🦊 DOM emoji → 酌情换画制头像（对齐 fight-king 做法）；
   - P-07（建议）rainbow-run / ocean-munch 画布 HUD/地图 emoji 小图标 → 酌情逐步换绘制图标；
   - P-05（建议）共享外壳 `level99.ts` 10px「★」为全 188 关共用，**非本窗职权，不动**，仅登记移交统筹。
3. 每修一处一个 commit（`fix(visual-r1): <款> <问题>`），配套视觉用例只增不减。
4. 收尾合并态复验：fetch → rebase → `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟 → 合并态 SHA / 用例总数 / 构建体积写回本报告。

红线重申：只动绘制函数 / CSS / 视觉常量 / 本款 `art.ts`；判定、数值、关卡、存档 key、玩法断言不碰；遵守各款 1.3 提示词既定规格（光源左上 45°、描边 1.5–2px、粉彩色板、reduced 约定）；禁止 three.js；禁止 force push。

（修复清单、遗留清单、合并态复验数据见下文，随修复进度补齐。）
