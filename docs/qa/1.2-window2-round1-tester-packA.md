# 窗口 2 · 1.2 三轮验收 第 1 轮 · 测试员 · 包 A

> 角色：QA 测试员（只记录、不当修复员）
> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 基线：`origin/game-1.2-window2`
> 模型：`claude-opus-5-thinking-high-fast`

## 〇、走查计划（动代码前先立此存照）

### 0.1 边界
- 只读 / 只改上面这 5 个游戏目录；别人的 10 款（hue-hand hop-pads tap-tiles dark-chess
  gomoku match-stars rainbow-run ocean-munch xiangqi fight-king）与窗口 3/4/5 的款一个字不动。
- 不动窗口 1 的平台文件：`src/ui/home.ts` `src/ui/rootGate.ts` `src/ui/playModes.ts`
  `src/engine/view25d.ts`；不验 root 门。
- `src/styles.css` 若非改不可，只在文件末尾追加（本轮预期不需要动）。
- 报告只写两处：本文件（独占）与 `docs/qa/1.2-window2-round1-tester.md` 的 `## Pack A` 一节（只追加）。
- 不做第 27–29 步全集验收，不写 `docs/qa/1.2-round1-*.md` / `docs/plan-1.2-step27-*`。

### 0.2 每款必做的 8 条铁则
| # | 铁则 | 取证方式 |
|---|------|----------|
| 1 | 首页进入 → 真赢一次 + 真输一次 → 退出 → 再进 | headless 冒烟脚本驱动 `mount()`，断言结算浮层与二次 mount |
| 2 | 战役第 1 / 100 / 188 关 | 直开这三关，跑到 `win`/`lose` 回调 |
| 3 | 对战 / 无尽 / 双人每种模式玩到结算 | 菜单按钮点进去，推进到 `onWin` / `onLose` |
| 4 | 双人键盘 朵朵 `WASD+F+G` / 星星 `方向键+L+K` / `Esc` 暂停，互不抢占 | 派发 KeyboardEvent，断言只有对应一侧状态变化 |
| 5 | 360px 宽：能玩、不溢出、热区 ≥ 44px | 设 `innerWidth=360`，量画布/按钮尺寸与 CSS min-height |
| 6 | `meta.modes/levels/blurb/platform` 与实现一致 | meta 对照菜单实际按钮与 `CHAPTERS` 总和 |
| 7 | `destroy` 后监听 / rAF / timer 归零 | 计数 stub 包住 `addEventListener/rAF/setInterval` |
| 8 | 商标黑名单 0 命中；无血无死亡描写 | 全目录文本扫描 |

### 0.3 执行顺序
1. 本 commit：只落计划，不动代码。
2. 逐款走查：dot-maze → fruit-stack → pool-stars → junqi-camp → chess-garden。
3. 发现的问题按 阻断 / 严重 / 一般 分级记进本文件的问题表。
4. 需要复现证据的，只在**本包 5 个目录内**新增 `*.test.ts`（不改玩法源码）。
5. 收尾：fetch → rebase `origin/game-1.2-window2` → `npm test` + `npm run build` 全绿 → 普通 push。

### 0.4 商标黑名单（本轮扫描用词表）
吃豆人 / Pac-Man / Pacman / 俄罗斯方块 / Tetris / 2048 / Suika / 西瓜游戏 / 合成大西瓜 /
斯诺克官方名 / 8 Ball Pool / 军棋厂牌名 / Chess.com / Lichess / 任天堂 / Nintendo /
索尼 / Sony / 世嘉 / SEGA / 暴雪 / Blizzard / 米哈游 / 王者荣耀 / 和平精英 / 我的世界 / Minecraft。
血腥/死亡词表：血 / 血腥 / 死 / 死亡 / 尸 / 杀死 / 干掉 / 阵亡 / 击毙 / 残杀。

## 一、走查记录

（本节由后续 commit 逐款补齐）

## 二、问题表

（本节由后续 commit 逐款补齐）
