# 竖屏/平板全量走查 · 第 1 轮（云端子代理 #1，PT 系列独立编号）

> 基线:`origin/game-1.3 = 6982da7e`。本轮换面:历史 r1–r18 几乎全在 915×412 矮横屏上量,
> 本工位把 **手机竖屏 412×915 / 360×740、平板横屏 1180×820、桌面 1280×800** 全部过了一遍,
> 并用 CDP **真触摸滑动**(不是 grep sticky、不是只看 rect)验证「有时往下划不了」的用户反馈。
> 编号独立用 **PT-x**,不占 N-xx(本轮并行工位已用到 N-107)。
> 工装 `/tmp/qa-tools/*.mjs`、截图 `/tmp/qa-shots/**` 不进库。

## 一、测了什么

| 面 | 覆盖 | 方法 |
| --- | --- | --- |
| 竖屏 412×915 | **76/76 款**进场态 + 进关态(`.l99-continue`/首个可点关) | pageerror/console 收集;舞台与所有滚动容器 scrollTop 前后差;真触摸三横位下滑 |
| 竖屏 360×740 | 76/76 款进关态 | 同上 |
| 矮横屏 915×412 | 76/76 款进关态 | 同上(与历史轮口径对齐) |
| 平板 1180×820 | 76/76 款进关态 | 游戏根宽度占舞台比例、最大画布尺寸、截图走查 |
| 首页 | 五视口(412/360 竖、915 横、1180 平板、1280 桌面) | 触摸滚动、横向溢出、卡片网格、搜索空态 |
| 状态面 | 暂停面板、攻略抽屉、无效 id 路由、搜索空态(竖屏+平板) | 按钮热区 ≥44、弹窗溢出、回退路由 |
| 端到端 | mole-pop 第 1 关自动敲赢:结算层 → 星星到账(⭐3)→ 再玩一次 | 真点击流 |

## 二、发现 + 已修(本轮四条 PT + 一条主干抢修)

| # | 对象 | 实测(修前 → 修后) | 修法 |
| --- | --- | --- | --- |
| PT-1 | **bubble-aim 竖屏地图态「往下划不了」** | 412×915 舞台溢出 46px,真触摸 scrollTop **0 → 46**(修后) | `.ba-wrap` `touch-action: none → manipulation`。瞄准手势只挂画布、画布自留 none,拖瞄/发射零影响 |
| PT-2 | **bubble-aim 平板地图窄柱** | 1180×820 地图宽 **384 → 720**,四列 → 八列 | 照 candy-swing 已合的 `cs-view-map` 同款:地图态挂 `ba-view-map`,进关摘掉 |
| PT-3 | **ba/cs 平板关内画布小窗口** | 1180×820 关内 wrap **384/395 → 435**,画布 **360×480 / 371×494 → 411×548**,舞台零溢出;**915×412 实测 369/395 不变** | `@media (min-width:700px) and (min-height:700px)` 下 `max-width: clamp(400px, calc((100dvh−240px)*0.75), 520px)`;双条件保证矮横屏(N-29 账)与手机竖屏一像素不变 |
| PT-4 | **candy-swing 竖屏地图外壳被裁 18px** | 舞台溢出 **18 → 0** | `.cs-map` 高度预算 `100dvh−120px → −138px`(壳顶栏+边框+padding+msg 实账) |
| N-105 | **主干红灯**(进场时 2 文件 5 红) | 本工位曾独立做出同款修复并 915 复量(mj-goal 16px 在屏 146~166);r18-B `18c63e9d` **先合**,撞车取先合版,本分支让位、rebase 后不带第二套 | ✅ 已由先合版销账 |

守门测试:`src/games/portraitTablet.pt.test.ts`(8 例),防止 wrap 再被锁死、放宽档被误删。

## 三、量过是干净的(下轮不必重量)

- **触摸滚动**:76 款 × {412×915, 360×740, 915×412} 进场+进关,真触摸下滑全部能动或本就无溢出。
  之前疑似的 candy-swing(地图内滚 663px 正常)、find-diff(手指落图上 `.fdf-viewport` 0→81 正常,
  落工具区不滚是探针瞄偏)、sling-birds(地图 mapFocus 自动滚到当前关、已在底)都是工装误报。
- **首页**:五视口横向溢出 0、触摸滚动正常、搜索空态文案友好、无效 `#/game/xxx` 回首页不崩。
- **暂停面板**:竖屏+平板五钮全 44px 高、全在屏、弹窗自身零溢出。攻略抽屉全屏、正文内滚正常。
- **平板关内宽度**:除 ba/cs(已修)外,l99 框架游戏 99% 铺满;fight-king 75%、duo-vs-star 71%、
  clock-house/math-farm/shape-kingdom/word-garden 67%、brave-path/sling-birds 63% 属合理居中版式。
- **端到端**:mole-pop 赢局 → 3 星结算层 → 星星到账 → 「下一关/再玩一次/回地图」可点。

## 四、遗留给下一轮

1. **自带菜单游戏平板置顶留白**(外观):gold-hook(内容高仅 250px)、junqi-camp/dark-chess(~328px)
   菜单贴舞台顶、下方大片空白。全局 `justify-content: safe center` 有打破各游戏 JS 测高
   (fitCanvas/stageRoomPx 假设顶锚)的风险,建议逐款小修或壳层加 opt-in 类。
2. **duo-arena 平板菜单 43% 宽**(440px):观感尚可,若统一版式可与上一条一起做。
3. r18 playbook 在途单(N-94…N-107 等)归属另两工位,本轮未碰;N-105 已按「第零优先」抢修,
   若撞车取先合版。
4. 平板横屏**自定义双人对战开打后的关内态**(fight-king/duo-vs-star 等)本轮只走查了菜单/首关,
   开打后的键位在平板上的留白可再抽验。
5. iOS Safari 真机(橡皮筋滚动、safe-area 左右缺口)无法在本环境模拟,建议真机抽验
   bubble-aim/find-diff 两款触控密集游戏。

## 五、水位

- 基线 `6982da7e` 时全量:1194 文件中 2 红(主干 N-105)。rebase 到 `206d0522`
  (先合版已含 N-105 修复)后本分支全量:**1196 文件 / 19512 通过 + 2 跳过,0 红**。
- `npm run build`(tsc --noEmit + vite build):绿。
- 分支 `cursor/portrait-tablet-qa-e75a`,目标合入 `game-1.3`。
