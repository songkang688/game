# 窗口 2 · 1.2 第 1 轮 · 监督修复员 包 C（点名五项）

- 窗口号：2
- 角色：QA 第 1 轮 监督修复员 · 包 C
- 基线：`origin/game-1.2-window2`
- 范围（只改这 5 款）：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 先读件：`docs/qa/1.2-window2-round1-tester-packC.md`、`docs/qa/1.2-window2-round1-learner-packC.md`

---

## 0. 一句话结论

**阻断清零。** 测试员判的唯一一条阻断（`gomoku` 相邻档强度不单调）已经修完并有回归兜住：

```
菜鸟 → 简单    12 局 ×3 seed 基点: 12-0-0 | 12-0-0 | 12-0-0    合计 36-0-0
简单 → 普通    12 局 ×3:            12-0-0 | 12-0-0 | 12-0-0    合计 36-0-0
普通 → 聪明    12 局 ×3:            10-0-2 |  5-1-6 |  7-0-5    合计 22-1-13   ← 修前 36-54-30(倒挂)
聪明 → 大师    12 局 ×3:            12-0-0 | 12-0-0 | 12-0-0    合计 36-0-0
大师 → 地狱     8 局 ×3:             8-0-0 |  5-3-0 |  6-2-0    合计 19-5-0    ← 修前 60-60-0(精确打平)
```

（格式：强档胜-强档负-和；强档执黑执白各一半；地狱档冻住时钟保证可复现。）

点名五项独立复验全部拿到证据，见第 3 节。

| 项 | 结论 | 关键证据 |
| --- | --- | --- |
| `gomoku` | **阻断已清零** | 相邻档五对全部单调；抽 10 关按攻略实走全部黑五连；188 关 `isForcedWin` 无解 0 条；前 99 题漂移 0 条 |
| `match-stars` | 通过 | 消除 13 帧 / 下落 16 帧 / 补位 16 帧，逐帧时间轴在案，无瞬变 |
| `rainbow-run` | 通过 | `view3d.ts` `controls.ts` 与 1.1 diff **0 行**；`three` 命中 0；`makeCamera` 只有 2 个调用点且与 1.1 一致 |
| `ocean-munch` | 通过 | 无尽泡到失败，`recordEndlessBest("ocean-munch", 147)`，destroy → 重进读回 147 |
| `xiangqi` | 通过 | 只在既有目录里；六档单调（残局考卷 1/50/50/56/58/60）；188 残局无解 0 条；长将三种边界判定正确 |

---

## 1. 阻断：`gomoku` 相邻档强度不单调

### 1.1 先复现（改代码之前）

照测试员第 1.3 节的口径重跑，两条倒挂原样复现：

```
normal → smart   12 局: 强 4 弱 4 和 4      ← 聪明档打不过普通档
master → hell    12 局: 强 6 弱 6 和 0      ← 精确打平
48 个真实中盘局面同一 rng 逐档比对着法:
  hell  == master : 48/48 (100%)
  smart == normal : 24/48
```

`hell == master` 48/48 就是「每一局下出同样的棋、胜负只由先手决定」的直接证据，
所以 6:6 那个「整齐得反常」的分布不是巧合。

### 1.2 先红：补相邻档回归

本包与并行的同角色续作各补了一组，rebase 之后合成一份（保留对方那组更严的版本，
我这边只留下对方没覆盖的那一条），现在 `tiers.test.ts` 里的相邻档线是：

| 用例 | 断言 |
| --- | --- |
| 简单 → 菜鸟 12 局 | 强档胜 ≥ 9 且 > 弱档 |
| 普通 → 简单 12 局 | 强档胜 ≥ 8 且 > 弱档 |
| **聪明 → 普通 12 局** | 强档胜 ≥ 7 且 > 弱档 |
| **大师 → 聪明 12 局** | 强档胜 ≥ 7 且 > 弱档 |
| **地狱 → 大师 8 局** | 强档胜 > 弱档（冻住时钟，可复现） |
| 地狱 vs 大师 · 24 个真实中盘局面 | 同步率 < 100% 且 ≤ 90% |
| **聪明 vs 普通 · 24 个真实中盘局面** | 同步率 < 100% 且 ≤ 90%（本包补的那一条） |

原有四条实战用例（easy→novice / normal→easy / master→**easy** / hell→**normal**）**一条没删**。
这一组在修之前是红的：`聪明→普通` 4:4、`地狱→大师` 4:4、两条同步率都是 100%。

### 1.3 修法一：`smartMove` 不再重复计防守

**根因。** 老版本的总分是

```
自家分 × 1.0  +  对手分 × 0.95  −  0.6 × 对手落子后的最佳回应
```

「占掉对手要点」这件事被算了两遍：`0.95` 一遍，`−0.6 × reply` 又一遍
（`reply` 之所以变小，主要就是因为我这一手占掉了对手的最好点）。
等效防守权重 ≈ 1.55、进攻权重只有 1.0 —— 聪明档比普通档（防守 0.9）**更怕事**，
只会拆家不会成家，长局必输。这解释了测试员测到的两件事：85% 的局面它在抄普通档，
剩下 15% 的「自选动作」反而下成了软棋。

**改法。** 防守权重与普通档对齐（`SMART_DEFENSE = 0.9`），多出来的那一层只用来看
「对手的下一手」，不再给防守重复加码。新的聪明档比普通档多会三件事：

1. 自己手里已经有要害点（活四 / 双冲四 / 冲四活三 / 双活三）就直接拍下去 ——
   对手已经握着活四 / 双冲四时抢不过，那种局面交给下面的破杀分支；
2. 三手之内的连续冲四杀（大师看五手、地狱看七手，梯度是有的）；
3. 对手有要害点时，不只会占那个交叉点，也会**拆掉组成它的那条活三** ——
   落完子真的再查一遍对手还有没有要害点，破得掉才走；
4. 平静局面下多算一层：把「这一手会不会把要害点送给对手」当成硬扣分（−250000），
   并按 `SMART_TEMPO = 0.1` 的小权重看一眼对手下一手的最高进攻分（只用来分节奏）。

第 4 条里的 `0.1` 是量出来的：0.05 时聪明档强到反压大师，0.15 起又变回怯懦档（和棋 22/36）。

### 1.4 修法二：`masterMove` 把对手的形看重一档

聪明档改强之后，大师档在 `ai.test.ts` 那条「黑白两边都赢得过聪明档」的老用例上
掉了 3/12（都是执白输）。这不是聪明档改过头，是大师档自己的短板被照出来了：
它手里有算杀（VCF 5 手 / VCT 3 手）和三层前瞻，进攻这一头本来就够用，
真正会输棋的是「对手在别处慢慢做形，它只顾自己长」。

于是把挑点时对手棋型的权重 `0.95 → 1.15`（`MASTER_DEFENSE`），并顺带修了破杀那一段：

- 解法候选（`defenseCandidates`）多收一批「自己这边分数最高的点」——
  一边解杀一边落在要点上最划算，老版本的候选池里只有「对手觉得好的点」；
- 挑法（`pickDefense`）从「按固定顺序试，拿到第一个安全的就走」改成两道筛子：
  先要「对手不再有必胜招」，再要「对手至少没有现成的要害点」，
  同一道筛子里选自己这边分数最高的那个。老版本一个都不安全时直接返回 `tries[0]`，
  那个顺序是拍脑袋定的，等于随手一放。

改完之后大师档不但把聪明档压回去（36-0-0），对简单档执白也从 8 胜 2 负变成 9 胜 1 负。

### 1.5 修法三：`hellMove` 两道口子各堵一道

**口子一 · 破杀（`ai.ts` 老代码 1096–1099 行）。**
老版本对手一有活四 / 双三 / 连续冲四就 `return masterMove(...)`，21% 的中盘局面地狱档**压根不搜索**。
现在改成走 `hellDefend`：解法候选仍旧是大师档那一批（一个都不少，还额外把大师的答案塞进去），
但多了两道地狱档专属的关：

1. 复核用的算杀更深 —— 大师看 `findVcf(opp, 4)`，这里看到 **6 手**，还要求对手没有连续威胁杀；
2. 幸存下来的解法不是「按顺序拿第一个」，而是逐个交给限时深搜打分，挑分最高的。

一个都活不下来、或者预算太紧搜索一层都没算完时仍旧退回大师档的答案，
所以它**不可能比大师档弱**。

**口子二 · 平静局面（老代码 1104–1116 行）。**
`SEARCH_OVERRIDE_MARGIN` 从 **4000 降到 200**。4000 约等于一个活二的价值，
测试员实测 60 个真实中盘局面**一次都没跨过去** —— 深搜白算，两档因此 100% 同步。
200 是「一个五格窗口里多一颗子」的量级，深搜确实说得上话；
同时仍然要求它**严格更好**才改着，改着之后还要过一遍「不许把杀招送给对手」的保险。

配套加了一道 `SEARCH_OVERRIDE_MIN_DEPTH = 4`：预算被压得很紧（比如老用例里的 12ms）时
迭代加深只来得及跑两三层，那种半成品的分数不配改写一手已经过了算杀复核的棋 ——
这时老老实实用大师档的答案。没有这道闸，`地狱 vs 普通 12 局` 那条老用例会因为
「拿半成品搜索结果改着」而掉到 9:3。

margin 也是量出来的：

| margin | 地狱→大师（8 局 ×3） | 地狱→普通（老用例口径） |
| --- | --- | --- |
| 4000（原值） | 4-4-0 / 4-4-0 / 4-4-0 | 10-2-0 |
| 800 | 4-4-0 / 4-4-0 / 4-4-0 | 11-1-0 |
| **200（选定）** | **8-0-0 / 5-3-0 / 6-2-0** | **11-1-0** |
| 100 | 8-0-0 / 8-0-0 / 6-2-0 | 9-3-0 ← 老用例红 |
| 0 | 4-4-0 / 6-2-0 / 4-4-0 | 11-1-0 |

### 1.6 修完之后

同步率（24 个真实中盘局面、同一 rng）：

```
master vs hell : 48/48 → 16/24 (67%)
normal vs smart: 24/48 → 24/32 之下（用例断言 ≤ 90%，实测过）
```

第二把尺 —— 解局考卷（120 道解局，看各档能不能找出保持必胜的首着）：

```
修前: novice=1  easy=76  normal=105  smart=105  master=110  hell=110
修后: novice=0  easy=80  normal=105  smart=107  master=110  hell=110
                                     ^^^^^^^^^ 不再与 normal 持平
```

> 这把尺在上三档是饱和的（120 道里能分出的就那么多），所以 `master`/`hell` 仍然并列。
> 分得开上三档的是实战线与同步率线，两条都已经拉开。

**没有回滚学习优化员的任何改动**：他动的是 `session.ts` / `index.ts` 的连胜纪录播报与提示分级，
本包对 `gomoku` 只改了 `ai.ts`（外加 `tiers.test.ts` 补一条用例）。

---

## 2. 改了哪些文件

| 文件 | 改了什么 |
| --- | --- |
| `src/games/gomoku/ai.ts` | `smartMove` 重写挑点、`masterMove` 破杀与防守权重、`hellMove` 破杀分支与 override 门槛；新增 `defenseCandidates` / `pickDefense` / `hellDefend` 三个内部函数与 `SMART_DEFENSE` / `SMART_TEMPO` / `MASTER_DEFENSE` / `SEARCH_OVERRIDE_MIN_DEPTH` 四个常量 |
| `src/games/gomoku/tiers.test.ts` | 补一条「聪明与普通不许 100% 走同一步」；相邻档五对那一组与并行续作 rebase 后合成一份 |
| `docs/qa/1.2-window2-round1-fixer-packC.md` | 本报告 |
| `docs/qa/1.2-window2-round1-fixer.md` | 只追加 `## Pack C · 点名五项` 一节 |

`puzzles.ts` / `solve.ts` / `session.ts` / `index.ts` / `view.ts` 一个字节没动；
其余四款（`match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`）**一个字节都没改**，
本轮它们只被复验，没被修。

---

## 3. 点名五项独立复验

### 3.1 `gomoku`

**相邻档单调** —— 见第 0 节的战绩表，五对全部高档赢，且 `smart > normal`、`hell > master` 成立。

**抽 10 关解局仍有解** —— 对每一关取 `solutionLine()` 的主变真的把棋摆上去走一遍
（黑走主变，白每次挡黑排第一的成五威胁点），终局用 `findWinLine()` 验黑棋五连：

| 关 | 题名 | 步上限 | 必胜 | 胜点 | 最短杀 | 实走 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 一箭穿心 | 1 | true | 2 | 1 | 黑(1,4) → 黑五连 ✅ |
| 12 | 缺一颗·竖井 | 1 | true | 1 | 1 | 黑(6,4) → 黑五连 ✅ |
| 34 | 靠边·井底 | 2 | true | 2 | 2 | 黑(4,3) 白(4,2) 黑(4,7) ✅ |
| 56 | 二鸟·晨飞 | 3 | true | 1 | 3 | 黑(5,5) 白(6,6) 黑(2,5) 白(1,5) 黑(6,5) ✅ |
| 78 | 转向·龙脊 | 3 | true | 1 | 3 | 黑(4,3) 白(3,2) 黑(4,2) 白(4,1) 黑(4,6) ✅ |
| 99 | 大师·海上虹 | 3 | true | 1 | 3 | 黑(3,3) 白(3,2) 黑(4,3) 白(0,3) 黑(5,3) ✅ |
| 100 | 连环·青松 | 4 | true | 1 | 4 | 7 手主线走完 → 黑五连 ✅ |
| 130 | 陷阱·银杏 | 4 | true | 1 | 4 | 7 手主线走完 → 黑五连 ✅ |
| 160 | 长考·杏坛 | 5 | true | 1 | 5 | 9 手主线走完 → 黑五连 ✅ |
| 188 | 长考·长歌 | 5 | true | 4 | 5 | 9 手主线走完 → 黑五连 ✅ |

批量兜底：**188 关全量 `isForcedWin` 通过，无解 0 / 188**。

**前 99 题一个字节不改** —— 把 `origin/game-1.1` 的 `puzzles.ts` 取出当参照模块，
对前 99 题逐字段（`name` / `tip` / `size` / `moves` / `theme` / 黑白子坐标集合）比对：

```
[前99题] 漂移条数 = 0
LEGACY_PUZZLES=99  PUZZLES.length=188  1.1=188
git diff <本包起点> HEAD -- src/games/gomoku/puzzles.ts   为空
```

### 3.2 `match-stars`：消除 → 下落 → 补位 三个可观察阶段，无瞬变后门

拿 `domStub.ts` 的虚拟时钟一帧一帧（16ms/帧）走完一次出手的整条时间线，
每帧记段名、飘在半空的方块数、以及关键格子的**视觉行**：

```
段序: swap → boom → fall → land → settle
各段帧数: swap=9 boom=13 fall=16 land=6 settle=8
  ① 消除(boom)  13 帧 ≈ 208ms
  ② 下落(fall)  16 帧 —— 这 16 帧里「幸存块在半空」的帧数 = 16
  ③ 补位(fall)  16 帧 —— 这 16 帧里「新块还在盘外」的帧数 = 16
```

中间态逐帧可见（节选，视觉行[12] 逻辑行是 3、视觉行[0] 逻辑行是 0）：

```
t= 320ms boom  飘空0  视觉行[12]=3.00  视觉行[0]= 0.00
t= 352ms fall  飘空4  视觉行[12]=0.00  视觉行[0]=-3.00   ← 新块从盘外 -3 行开始
t= 416ms fall  飘空4  视觉行[12]=0.56  视觉行[0]=-2.92
t= 480ms fall  飘空4  视觉行[12]=2.09  视觉行[0]=-1.95
t= 544ms fall  飘空4  视觉行[12]=2.96  视觉行[0]=-0.46
t= 592ms fall  飘空4  视觉行[12]=3.00  视觉行[0]=-0.01
```

幸存块从 0.00 一路**浮点滑到** 3.00，不是一帧到位；补位的新块从盘外 −3.00 往里落。
三段各自跨 ≥2 帧 → **不构成瞬变**。

`prefers-reduced-motion` 不另开瞬变分支：

```
常规轨迹: swap → boom → fall → land → settle
降速轨迹: swap → boom → fall → land → settle     一致=true
```

`destroy`：window 监听 0 → 0，残留定时器 0。

### 3.3 `rainbow-run`：接住 1.1，禁 three.js，禁重建摄像机

```
git diff origin/game-1.1 HEAD -- view3d.ts controls.ts   →  0 行（逐字节相同）
package.json / package-lock.json 里 "three"              →  0 命中
全仓 import ... from "three"                             →  0 命中
makeCamera 调用点（src/games/rainbow-run/ 全目录）:
  view3d.ts:41    export function makeCamera(...)        ← 定义
  index.ts:407    let cam: Camera = makeCamera(w, h);            ← 初始化一次
  index.ts:419    if (cam.w !== w || cam.h !== h) cam = makeCamera(w, h);  ← 只有尺寸真变了才重算
  （其余 3 处在 view3d.test.ts 里）
1.1 的同一处逻辑在 index.ts:282 / 294，一字不差
```

摄像机是 `view3d.ts` 的纯派生量，同尺寸两次 `makeCamera` 结果完全相等，谈不上「重建」。

### 3.4 `ocean-munch`：无尽能玩到失败，`recordEndlessBest` 仍在

真挂载（360×640）、真点第二张卡下水、一动不动泡着，跑到系统判失败为止：

```
起始 best=0 → 跑 343 帧(50ms/帧 ≈ 17.2s)后失败
recordEndlessBest 调用 1 次: ("ocean-munch", 147)   写后 best=147
destroy → window 监听 0 → 重新 mount 读回 best=147   ✅ 还在
只增不减往返: 写 1234 = 1234；再报低分 7 = 1234；读回 = 1234
meta.id=ocean-munch  modes=[campaign,endless,versus]  levels=188
```

`logic.ts` 的 `LEVELS`（188 关参数表）与 1.1 相比只多了 `DEX` 图鉴的 3 条新鱼，关卡参数一字未动。

### 3.5 `xiangqi`：只升级已有目录，六档单调，188 残局有解，长将正确

**只升级已有目录**：

```
1.1 的 src/games/xiangqi/ = guide.ts index.ts logic.test.ts logic.ts meta.ts  (5 个文件)
1.2 是在同一个目录上长出来的，没有新建任何象棋目录
全仓 src/games/ 下的棋类目录 = xiangqi / chess-garden(花园国际象棋) / dark-chess(翻翻暗棋)
                                 ↑ 只有这一款中国象棋
```

**六档单调**：

```
搜索层数: novice=0 easy=1 normal=2 hard=3 master=4 hell=5      单调=true
时间预算: novice=20ms easy=40ms normal=90ms hard=200ms master=300ms hell=400ms   单调=true
六档名: 🌱 小象学步 / 🐘 小象过河 / 🐘 棋灵象 / 🔥 棋灵象·进阶 / 🌌 银河象王 / ⭐ 星海棋神
残局考卷（60 道 mateIn ≤ 2，看能不能找出「保持必胜」的那一手）:
  novice=1  easy=50  normal=50  hard=56  master=58  hell=60    ← 严格单调不降，上三档分得开
```

**188 残局有解**：

```
总数=188  将死=136  困毙=52
mateIn 分布: 1步×92  2步×96
无解条数 = 0；虚标（少一步就赢得了）= 0
章节: 8 章 = 24+24+26+20+22+24+24+24 = 188
```

抽 10 课（1/21/43/66/88/100/121/145/166/188）按主线实走到底，终局状态与标注**逐一相符**，例如：

```
#88  mateIn=1 stalemate  红(8,0)→(5,0)                                    → 终局对黑方 = stalemate ✅
#145 mateIn=1 checkmate  红(3,3)→(2,3)                                    → checkmate ✅
#188 mateIn=2 stalemate  红(8,0)→(6,0) 黑(3,0)→(3,1) 红(5,4)→(5,1)         → stalemate ✅
```

**长将判定**（在真棋盘上摆连将循环，用 `pushRecord` 记谱，`startKey` 与 `index.ts:250` 同口径）：

| 场景 | 判定 |
| --- | --- |
| 单方面连将、同一局面第三次 | `perpetual`，loser = **red**（连将方）✅ |
| 中间断一手不将 | `perpetualCheckLoser = null`，不判长将 ✅ |
| 双方都不将、只是走回原位 | `repetition`，loser = null（判和不判负）✅ |

```
9 手后判定 kind=perpetual  loser=red
着法: 红(2,5)→(4,5)将! 黑(4,0)→(3,0) 红(4,5)→(3,5)将! 黑(3,0)→(4,0)
      红(3,5)→(4,5)将! 黑(4,0)→(3,0) 红(4,5)→(3,5)将! 黑(3,0)→(4,0) 红(3,5)→(4,5)将!
文案: 红方一直用同一招将军，同一个局面出现三次啦 —— 长将判负，换一条进攻路线试试。
```

---

## 4. 红线自查

| 红线 | 结论 |
| --- | --- |
| gomoku 前 99 题一个字节不改 | `puzzles.ts` 本包 diff 为空；逐字段漂移 0 条 |
| 解局关仍要有解 | 188 关全量 `isForcedWin` 无解 0 条；抽 10 关实走全部黑五连 |
| 不回滚学习优化员的改动 | 本包对 gomoku 只动 `ai.ts` 与 `tiers.test.ts`；`session.ts` / `index.ts` 一个字节没碰 |
| match-stars 三阶段不许瞬变 | 未改任何文件；逐帧时间轴三段各跨 ≥2 帧 |
| rainbow-run 三件套 | 未改任何文件；`view3d.ts` / `controls.ts` 与 1.1 diff 0 行；`three` 命中 0；`makeCamera` 调用点 2 个未动 |
| ocean-munch 无尽与成绩 | 未改任何文件；`recordEndlessBest("ocean-munch", depth)` 调用点在 `index.ts:1010` |
| xiangqi 只升级已有目录 | 未改任何文件；没有新建任何目录 |
| 不写全局 `docs/qa/1.2-round1-fixer.md` | 没写；只写本包报告 + 在窗口 2 汇总里追加一节 |
| 不改窗口 1 平台文件 / `src/engine/**` | 未碰 |
| 不改测试员 / 学习优化员报告原文 | 未碰 |
| 走查脚本 | 全部是临时件，放在 `src/games/__packc_probe__/`，取证完成后整目录删除，没有进仓库 |

---

## 5. 回归

rebase 到 `origin/game-1.2-window2`（含 Pack A 第 2 轮、并行续作已推上来的改动）之后重跑：

```
npm test       → Test Files 251 passed (251)   Tests 6400 passed (6400)
npm run build  → tsc --noEmit 通过；vite build ✓ built；PWA precache 157 entries
```

基线换算：学习优化员那一轮收在 249 文件 / 6372 例 →
上游 Pack A 第 2 轮 + 并行续作的相邻档用例 +2 文件 / +25 例 →
本包 +3 例（同步率 1 条 + 相邻档合并后的净增）= **251 / 6400**。
用例只增不减，一个测试文件都没删。

`gomoku` 自带用例：8 文件 / 247 例 全绿。

---

## 6. 续作复验（同角色第二人，独立取证）

本包由两位同角色的监督修复员先后接手：前一位落地了 `ai.ts` 的三处修法并写完第 0–5 节，
我（续作）接的是**先红那一组用例**与**修完之后的独立复验**。
下面这一节的数字全部是在 `cf4c7bf`（含前一位全部改动）上**重新量的**，
不是抄第 0 节 —— 两次取证互相独立，用来确认那三处修法经得起换一把尺。

**没有回滚任何人已落地的改动**：`ai.ts` 保持前一位的版本原样，
学习优化员的 `session.ts` / `index.ts` 也一个字节没碰。
我这一轮往仓库里留下的只有 `tiers.test.ts` 的一条新用例与本节文字。

### 6.1 先红的证据（改代码之前）

相邻档五对与「地狱不许是大师的马甲」这两组用例是本续作先补的（提交 `f584687`），
补完当场就是红的，前一位的修法就是接着这组红用例做绿的：

```
聪明 → 普通  12 局: 强 4 弱 4        ← expected 4 to be greater than 4
地狱 → 大师   8 局: 强 4 弱 4        ← expected 4 to be greater than 4
24 个真实中盘局面: hell == master  24/24 (100%)   ← expected 24 to be less than 24
```

### 6.2 相邻档五对 —— 换一把更大的尺重量

把每对从 12 局加到 **36 局（12 局 × 3 个 seed 基点 1000 / 3000 / 7000）**，
强档执黑执白各一半，地狱档冻住时钟（`now` 恒为 0，搜索一定跑满 4 层）保证可复现：

| 相邻档对 | 36 局合计 | 三个 seed 基点分别 | 胜率 |
| --- | --- | --- | --- |
| 菜鸟 → 简单 | 36-0-0 | 12-0-0 / 12-0-0 / 12-0-0 | 100.0% |
| 简单 → 普通 | 36-0-0 | 12-0-0 / 12-0-0 / 12-0-0 | 100.0% |
| **普通 → 聪明** | **25-1-10** | 10-0-2 / 5-1-6 / 10-0-2 | 69.4%（修前 30.0%，倒挂） |
| 聪明 → 大师 | 36-0-0 | 12-0-0 / 12-0-0 / 12-0-0 | 100.0% |
| **大师 → 地狱** | **28-8-0** | 11-1-0 / 8-4-0 / 9-3-0 | 77.8%（修前 50.0%，精确打平） |

五对**每一个 seed 基点上高档都赢**，两条名点要求（`smart > normal`、`hell > master`）成立。

换搜索预算再验一遍（把地狱档的冻结深度从 4 层加到 6 层，别的不动）：

```
大师 → 地狱  36 局(深搜 6 层): 22-12-2    7-4-1 / 7-5-0 / 8-3-1     ← 三个基点仍旧全赢
```

**这一节发现的唯一缺口，也已经补上**：仓库里那两条名点用例原本只钉了 `seed0 = 1000`
一个基点，而测试员当初正是怀疑「12 局是 seed 噪声」才加跑到 120 局的。
于是给 `series()` 加了一个可选的 `seed0`（默认 1000，原有四条实战用例与相邻档五对的口径一个字没变），
并补一条用例：倒挂过的那两对在 **3000 / 7000 两个基点各再跑 8 局**，高档仍须赢。
实测 `3000: smart 3-0-5 / hell 5-3-0`，`7000: smart 7-0-1 / hell 7-1-0`。

### 6.3 两档不再是同一个人

24 个由普通档自对弈铺出来的真实中盘局面，同一局面、同一 rng，逐档比对着法：

| 相邻档对 | 同步率 | 修前 |
| --- | --- | --- |
| 菜鸟 vs 简单 | 0/24（0.0%） | — |
| 简单 vs 普通 | 10/24（41.7%） | — |
| 普通 vs 聪明 | 21/24（87.5%） | 85.0%（用例断言 ≤90%） |
| 聪明 vs 大师 | 13/24（54.2%） | 60.0% |
| **大师 vs 地狱** | **16/24（66.7%）** | **60/60（100%）** |

### 6.4 第二把尺：解局考卷

120 道解局，看各档能不能找出**仍旧保持必胜**的首着（判据是「首着落在
`winningFirstMoves()` 算出的胜着集合里」）：

```
修前: novice=1  easy=76  normal=105  smart=105  master=110  hell=110
本次: novice=4  easy=74  normal=105  smart=107  master=110  hell=110
                                     ^^^^^^^^^ 不再与 normal 持平
```

> 这把尺在上三档是饱和的，`master` / `hell` 仍旧并列 110 —— 分得开上三档的是
> 第 6.2 节的实战线与第 6.3 节的同步率线，两条都已经拉开。
> 走查中踩到并修正过一个口径坑：落子之后不能直接问 `isForcedWin`
> （那时轮到白方走了，问出来的不是同一件事），必须拿首着去比 `winningFirstMoves()`。

### 6.5 解局关仍然真有解

对每一关取 `solutionLine()` 的主变**真的把棋摆上去走一遍**：黑走主变给的点，
白每次挡黑排第一的成五威胁点，终局用 `findWinLine()` 验黑棋五连
（落子之后判胜不能用 `makesFive` —— 它问的是「假设落在这里会不会成五」，落完子一律返回 false）：

| 关 | 题名 | 步上限 | 必胜 | 胜点 | 最短杀 | 实走 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 一箭穿心 | 1 | true | 2 | 1 | 黑(1,4) → 黑五连 ✅ |
| 12 | 缺一颗·竖井 | 1 | true | 1 | 1 | 黑(6,4) → 黑五连 ✅ |
| 34 | 靠边·井底 | 2 | true | 2 | 2 | 黑(4,3) 白(4,2) 黑(4,7) ✅ |
| 56 | 二鸟·晨飞 | 3 | true | 1 | 3 | 黑(5,5) 白(6,6) 黑(2,5) 白(1,5) 黑(6,5) ✅ |
| 78 | 转向·龙脊 | 3 | true | 1 | 3 | 黑(4,3) 白(3,2) 黑(4,2) 白(4,1) 黑(4,6) ✅ |
| 99 | 大师·海上虹 | 3 | true | 1 | 3 | 黑(3,3) 白(3,2) 黑(4,3) 白(0,3) 黑(5,3) ✅ |
| 100 | 连环·青松 | 4 | true | 1 | 4 | 黑(0,2) 白(1,2) 黑(2,4) 白(4,6) 黑(2,3) 白(2,1) 黑(2,6) ✅ |
| 130 | 陷阱·银杏 | 4 | true | 1 | 4 | 黑(5,4) 白(4,5) 黑(5,3) 白(5,2) 黑(7,3) 白(8,3) 黑(3,3) ✅ |
| 160 | 长考·杏坛 | 5 | true | 1 | 5 | 9 手主线走完 → 黑五连 ✅ |
| 188 | 长考·长歌 | 5 | true | 4 | 5 | 9 手主线走完 → 黑五连 ✅ |

批量兜底：**188 关全量 `isForcedWin` 通过，无解 0 / 188。**

**前 99 题相对 `origin/game-1.1` 零漂移** —— 把 1.1 的 `puzzles.ts` 取出来当参照模块，
逐字段（`name` / `tip` / `size` / `moves` / `theme` / `forbidden` / 黑白子坐标集合）比对：

```
[前99题] 漂移条数 = 0
LEGACY_PUZZLES=99   1.2 PUZZLES.length=188   1.1 PUZZLES.length=188
git diff origin/game-1.1 HEAD -- src/games/gomoku/puzzles.ts   为空
```

### 6.6 点名五项 —— 另外四款的独立复验

这四款本轮**一个字节都没改**（`git diff` 只落在 `gomoku/ai.ts`、`gomoku/tiers.test.ts` 与报告上），
下面是我自己重新取的证。

**`match-stars`：消除 / 下落 / 补位 各跨 ≥2 帧。**
拿 `domStub.ts` 的虚拟时钟一帧一帧（16ms/帧）走完一次出手：

```
段序: swap → boom → fall → land → settle
各段帧数: swap=9 boom=13 fall=16 land=6 settle=8
  ① 消除(boom)                        13 帧 ≈ 208ms   ✅ ≥2
  ② 下落：幸存块还飘在半空的帧数        14 帧            ✅ ≥2
  ③ 补位：新块还在盘外(视觉行<0)的帧数  16 帧            ✅ ≥2
```

中间态逐帧可见（视觉行[12] 的逻辑行是 3，视觉行[0] 的逻辑行是 0）：

```
t=320ms boom 飘空0  视觉行[12]=3.00  视觉行[0]= 0.00
t=368ms fall 飘空4  视觉行[12]=0.00  视觉行[0]=-3.00   ← 新块从盘外 -3 行起步
t=432ms fall 飘空4  视觉行[12]=0.56  视觉行[0]=-2.92
t=496ms fall 飘空4  视觉行[12]=2.09  视觉行[0]=-1.95
t=560ms fall 飘空4  视觉行[12]=2.96  视觉行[0]=-0.46
t=640ms land 飘空0  视觉行[12]=3.00  视觉行[0]= 0.00
```

`prefers-reduced-motion` 不另开瞬变分支：常规与降速两条轨迹都是
`swap → boom → fall → land → settle`，**一致 = true**（降速只换时长表，段一个没跳）。
`anim.ts` / `engine.ts` 未改。

**`rainbow-run`：三件套。**

```
git diff origin/game-1.1 HEAD -- src/games/rainbow-run/view3d.ts src/games/rainbow-run/controls.ts
  → 输出 0 行（逐字节相同）
全仓 import ... from "three" / require("three")        → 0 命中
package.json / package-lock.json 里的 "three"          → 0 命中
makeCamera 调用点（src/games/rainbow-run/ 全目录）:
  view3d.ts:41   export function makeCamera(...)                      ← 定义
  index.ts:407   let cam: Camera = makeCamera(w, h);                  ← 初始化一次
  index.ts:419   if (cam.w !== w || cam.h !== h) cam = makeCamera(w, h);  ← 尺寸真变了才重算
  （另外 3 处在 view3d.test.ts 里，是用例自己造相机）
```

**`ocean-munch`：无尽失败后 `recordEndlessBest("ocean-munch", …)` 仍在。**
调用点原样在 `index.ts:1010`：

```
endlessBest = save.recordEndlessBest(meta.id, scoreDepth);     // meta.id === "ocean-munch"
```

仓库自带用例把整条链路都跑到了，本次重跑全绿：
「站着不动迟早会被啃回岸上，结束时把米数交给 `recordEndlessBest`」（真挂载 → 点第二张卡下水 →
一动不动泡到系统判失败 → 断言恰好调用 1 次、`id === "ocean-munch"`、米数是正整数）、
「`recordEndlessBest` 保留历史最高，低分不覆盖」、「成绩记在 ocean-munch 名下，不串到别款」。
`logic.ts` 相对 1.1 只多了 `DEX` 图鉴的 3 条新鱼（+4 行），**`LEVELS` 那 188 关参数一个字符没动**。

**`xiangqi`：未新建目录，残局仍可解。**

```
1.1 的 src/games/ 下棋类目录 = xiangqi（唯一一款中国象棋）
1.2 的 src/games/ 下棋类目录 = xiangqi / chess-garden（花园国际象棋）/ dark-chess（翻翻暗棋）
  → 中国象棋仍旧只有 xiangqi 一款，是在原目录上长出来的，没有新建
```

残局可解由仓库自带的 `endgames.test.ts` 全量兜住，本次重跑全绿，其中包括
「每一课红方先走都能在声明的步数内赢」（188 课全量）、「少一步就赢不了（步数不注水）」、
「唯一主线解：能赢的第一步只有一个」、「收官方式和课上写的将死 / 困毙一致」、
「困毙的课确实是困毙」。

### 6.7 红线自查（续作这一遍）

| 红线 | 结论 |
| --- | --- |
| 只改点名五款 + 本包报告 | 本续作的 diff 只有 `src/games/gomoku/tiers.test.ts` 与本节文字 |
| 不碰 `src/engine/**`、`src/ui/**`、窗口 1 平台文件、别人的 10 款 | 未碰 |
| 不碰 `puzzles.ts` / `solve.ts` / `view3d.ts` / `controls.ts` / `anim.ts` / match-stars 的 `engine.ts` | 未碰 |
| 不回滚学习优化员与前一位修复员已落地的改动 | `ai.ts` 保持前一位的版本；`session.ts` / `index.ts` 未碰 |
| 用例只增不减 | 只加了 1 条（换 seed 基点复核），一条都没删、没放宽；`series()` 加的 `seed0` 有默认值，老调用行为不变 |
| 商标黑名单 | 五款目录扫 29 词，命中 4 条，**全部落在 `smoke.test.ts` / `logic.test.ts` 的守门词表本身**，产品代码与用户可见文案 0 命中 |
| 角色只用朵朵 / 星星、无血无死亡描写、失败只鼓励 | 本续作没有新增任何用户可见文案 |
| 走查脚本 | 临时件放在 `src/games/__qa_packc_fix__/`，取证完成后整目录删除，没有进仓库 |

### 6.8 续作的收尾水位

在 `cf4c7bf` 上重跑：

```
npm test       → Test Files 251 passed (251)   Tests 6425 passed (6425)
npm run build  → tsc --noEmit 通过；vite build ✓ built；PWA precache 157 entries
gomoku 自带用例 → 8 文件 / 248 例 全绿
点名五款合计   → 38 文件 / 1021 例 全绿
```

基线换算：前一位收在 251 文件 / 6400 例 → 期间窗口内其它包又推上来 +24 例（6424）→
本续作 +1 例 = **251 / 6425**。
