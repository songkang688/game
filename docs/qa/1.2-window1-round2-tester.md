# 1.2 窗口 1 · 第 2 轮 · 测试员报告

> **这一轮有两位测试员并行走查，两份取证都留在这一个文件里，谁都没有覆盖谁。**
>
> | 份 | 走的路线 | 样本关 | 问题编号 |
> | --- | --- | --- | --- |
> | 第一份（下方「甲、」） | 难度曲线 / 竞态 / 教育正确性 / 无尽可持续性 | 24 · 96 · 164 | W1-R2-01 ~ W1-R2-04（已由第 2 轮修复员关闭） |
> | 第二份（下方「乙、」） | 平台五项 / 四模式矩阵双视口 / 双人分边 / 定 seed 复现 / 过关证据 / 窄屏字号 | 2 · 50 · 150 | W1-R2-05 ~ W1-R2-07（本次新增） |
>
> 两份的样本关与手段刻意错开，结论互不冲突；重合的只有「`npm test` 242 文件 / 6678 例全绿」
> 和「商标扫描 0 命中」这类全量水位，两边独立各量了一次，读数一致。

---

# 甲、第一份 · 难度曲线 / 竞态 / 教育正确性 / 无尽（样本关 24 · 96 · 164）

> 分支 `game-1.2-window1`，开工水位 `0626897`（第 1 轮监督修复员收工那一笔）。
> 范围仍是窗口 1 的 12 款新游戏与第 1 步的平台能力，不碰窗口 2–5 的目录。
>
> 第 2 轮的任务书写得很清楚：**换样本**。第 1 轮走的是第 1 / 100 / 188 关、
> 一步一等的「慢镜头」路线；这一轮换成第 24 / 96 / 164 关，并且把第 1 轮
> 没有正面查过的四件事补上 —— **难度曲线、竞态、教育正确性、无尽能不能一直走下去**。

## 〇、这一轮怎么测的

| # | 手段 | 脚本 / 命令 | 结果 |
| --- | --- | --- | --- |
| 1 | 整批走查（换第二批关卡 24 / 96 / 164） | `QA_ROUND=2 node scripts/qa-1.2-window1-sweep.mjs` | **144 / 144** |
| 2 | **竞态走查（本轮新写）** | `node scripts/qa-1.2-window1-r2-race.mjs` | **60 / 60** |
| 3 | 平台能力（root 门 / 设备芯片 / 无外链） | `node scripts/qa-1.2-window1-platform.mjs` | **26 / 26** |
| 4 | **难度曲线（本轮新查）** | 章内 / 章间数值抽样，12 款逐章 | 见第二节，**1 处回落** |
| 5 | **无尽可持续性（本轮新查）** | `endlessConfig(w)` 推到第 1…5000 波 | 见第三节，**2 款有硬墙** |
| 6 | 教育正确性 | `npx vitest run src/games/{sudoku-petal,mine-garden,weiqi-garden,mahjong-bloom}` | 26 文件 / **735 例** 全绿 |
| 7 | 商标扫描 | `.qa-tmp/scan-brand.mjs`，186 个文件 × 29 个词 | 出货代码 **0 命中** |
| 8 | 全量回归 | `npm test` | 242 文件 / **6678 例** 全绿 |

真浏览器一律是 Chrome + `puppeteer-core`，视口 360×640 竖屏、`isMobile` / `hasTouch` 打开，
胜负只认 188 关外壳真的弹出来的结算浮层，不读游戏内部变量、不开任何测试后门。

---

## 一、竞态走查：孩子不会「点一下等一等」

第 1 轮那份整批走查是慢镜头：点一下、睡 700ms、再点下一下。孩子不这么玩。
本轮新写了 `scripts/qa-1.2-window1-r2-race.mjs`，专门抢那些拍子，12 款每款五项：

| 抢的是什么拍子 | 判据 | 12 款结果 |
| --- | --- | --- |
| **Esc 连打**：40ms 一下连按 8 下 | 偶数下之后必须回到「没暂停」，外壳那层 `.dialog--pause` 一次都不许露头 | 12 / 12 |
| **模式入口连点**：同一个按钮不等待连点 3 下 | 舞台形状（可见 modebar 数、canvas 数、节点数）必须和老老实实点 1 下一模一样 | 12 / 12 |
| **模式切换风暴**：入口 → 返回 → 下一个入口，每步只给 200ms | 转完一圈不许翻出 `.game-error` | 12 / 12 |
| **动画中途退出**：进关只等 250ms（入场动画、假人首手都还在飞）就退回首页，连做 3 次 | `window` 监听 / `setInterval` / rAF 全部回到进游戏之前的基线 | 12 / 12 |
| 全程 | 无 `pageerror`、无 `console.error` | 12 / 12 |

**60 / 60 通过，本轮竞态没有查出缺陷。** 第 1 轮修掉的 Esc 双层暂停（W1-R1-01）在连打 8 下
的压力下也没有复发：`shellEverShowed` 12 款全是 `false`。

### 一处判据自纠（不是产品缺陷，记下来免得下一轮再踩）

竞态脚本第一版把「模式打开后页面上有两排 `-modebar`」当成了重复挂载，6 款报红
（`block-drop` `combo-clash` `mahjong-bloom` `star-estate` `hero-cards` `merge-2048`）。
逐款对照单点与三点的 DOM 之后确认是**判据写错了**：

- 模式开起来之后，188 关地图连同它那排入口会**留在 DOM 里只是藏起来**，按 `querySelectorAll`
  数当然是两排；
- 而且这 6 款把 `-modebar` 这个类名**复用成了「一排按钮」的通用行**，模式内部的档位选择器
  也叫这个名字（`merge-2048` 是 `mg-modebar:4` ＋ `mg-modebar:3`，`combo-clash` 是
  `cc-modebar:5` ＋ `cc-modebar:4`）。

改成「只数看得见的，并且拿点 1 下和点 3 下对比」之后 12 款全绿。类名复用这件事本身记成
**W1-R2-03**，见第五节。

---

## 二、难度曲线：一款回落

做法：每款挑一个「越大越难」的头号数值，先看**章内**是不是一路不降，再看**章间**。
188 关逐关取样，不抽签。

### 章内坡度

| 款 | 头号数值 | 八（十）章章内坡度 |
| --- | --- | --- |
| `orb-arena` | `targetMass` | 10 章全部不降（90→136 / 132→178 / …… / 468→514） |
| `snake-royale` | `targetLen` | 8 章全部不降（40→62 / …… / 152→174） |
| `block-drop` | `startLevel` | 8 章全部不降（0→2 / …… / 8→10） |
| `combo-clash` | `foeVigor` | 8 章全部不降（0.50→0.58 / …… / 0.85→0.93） |
| `star-estate` | `rounds` | 8 章全部不降 |
| `mine-garden` | `mines` | 8 章全部不降（3→10 / …… / 55→110） |
| `sudoku-petal` | `holes` | 8 章全部不降（8→12 / …… / 56→64） |
| `merge-2048` | `target` | 6 章不降、2 章持平；第 6 章 64/512 交替 —— 那一章叫「三乘三与五乘五」，交替的是盘面大小，属于设计 |
| `hero-cards` | 假人档位 | **第 7 章整章回落到菜鸟档，见 W1-R2-02** |
| `weiqi-garden` | `moveBudget` | 第 5–8 章按题型循环（提子 3 手 / 死活 8 手），不是坡度旋钮 |
| `flight-chess` | `refRolls` | 参考解的长度，随 seed 抖动，不是坡度旋钮 |
| `mahjong-bloom` | `targetPoints` | **不是难度旋钮**，见下 |

### 三条「看着像回落、核实后不是」的

1. **`mahjong-bloom` 的 `targetPoints` 在章内乱跳**（第 1 章 44 → 8 → 8 → 14 → …）。
   翻 `levels.ts` 第 549 行的注释与 `starsFor` 之后确认：这个字段记的是
   「按既定路线**能拿到**的番数」（三星线），**不是**过关门槛。真正的门槛是
   `CHAPTER_FLOORS = [1,2,4,6,6,6,8,8]`，逐章不降。既有用例
   `三星线记的是这条既定路线真能拿到的番数` 已经把这层关系钉住了。**非缺陷。**
2. **`merge-2048` 第 5 章目标从 2048 掉回 128**。第 5 章叫「障碍花」，新机制是
   滑不进去的格子，一行被切成两段 —— 换了机制就该把数字压回去重新起坡。**非缺陷。**
3. **`mine-garden` 第 5 章从 16×16 / 36 雷掉回 12×11 / 19 雷**。第 5 章整章有雾、
   第 6 章整章限时，同样是换机制重新起坡。`sudoku-petal` 第 6 章「对角花」也是同一回事。**非缺陷。**

判断口径统一成一句话：**章间的数字回落，只要伴随着章节介绍里写明的新机制，就算重新起坡；
不伴随新机制的回落才算缺陷。** 按这条口径，12 款里只有 `hero-cards` 一处不合格。

---

## 三、无尽能不能一直走下去：两款有硬墙

`weiqi-garden` 与 `mine-garden` 没有无尽档（`levels.ts` 里没有 `endless*` 出口，
`meta.modes` 也没声明），跳过。其余 10 款逐个把配置推到第 1 / 5 / 20 / 100 / 500 / 5000 波。

8 款没问题：`block-drop` `merge-2048` `sudoku-petal` 的 `endlessConfig` 收的是档位名
（马拉松 / 冲刺、混合 / 迷你）而不是波次，本身就没有「越来越大」的字段；
`combo-clash`（`foeVigor` 封顶 1.3）、`mahjong-bloom`（`floor` 封顶 8 番）、
`star-estate`（`cash` 900 / `rounds` 12 触底）、`hero-cards`、`flight-chess` 都有收敛点。

### W1-R2-01 · `orb-arena` / `snake-royale` 的无尽有三个旋钮没封顶

`orb-arena` 的 `endlessConfig` 里，`bots`（`Math.min(11, …)`）和 `viruses`（`Math.min(14, …)`）
都老老实实封了顶，**只有 `shrink`、`pellets`、`targetMass` 一路线性长下去**：

```
波   目标质量  收圈速度  收到底要多久  目标质量对应半径  豆子数
  1      195     4.4/s        223s            56          146
  5      415    10.0/s         98s            81          170
 10      690    17.0/s         58s           105          200
 20     1240    31.0/s         32s           141          260
 50     2890    73.0/s         13s           215          440
100     5640   143.0/s          7s           300          740
500    27640   703.0/s          1.4s         665         3140
```

（地图恒为 2000×2000，起圈半径 `min(w,h)*0.52 = 1040`；`shrinkZone` 收到 60 就不再收。）

两条后果，都能直接读出来：

- **收圈越到后面越不是「更难」，而是「没得玩」。** 第 20 波起，安全区 32 秒就收到 60 半径
  的底，而那时目标质量对应的圆半径已经 141 —— 安全区比圆圆自己还小一圈；
  第 100 波只剩 7 秒。`zoneDrain` 认的是**圆心**在不在圈里，所以不至于当场结束，
  但从落底那一刻起就只能在圈外硬扛每秒 9 点的流失，剩下的时间不是在玩，是在等结算。
- **豆子数无上限是纯粹的性能悬崖。** `index.ts` 的碰撞检测是每帧
  `for (let i = 0; i < pellets.length; i++)` 遍历一遍。第 500 波 3140 颗、
  第 5000 波 30140 颗，帧率会塌，而这件事**一点玩法收益都没有** —— 豆子被吃掉立刻在
  地图上随机重生，供给本来就是无限的，`pellets` 只决定同时铺在地上多少颗。

`snake-royale` 是同一份毛病，同一处写法：`food = 170 + w*8`（第 500 波 4170 颗）、
`shrink = 3 + w*1.3`（第 100 波 133/s），而 `bots` 一样封了顶（`Math.min(9, …)`）。

**定级：严重（不阻断发布）。** 孩子几乎走不到第 15 波，早期几波的手感完全正常
（第 1 轮的真胜真负取证也是在这几波里拿到的）；但「无尽」这个词写在 `meta.modes` 里，
说了能一直玩，就不该在第 20 波变成一个 32 秒的空转。修法在同一节第五条给出。

---

## 四、教育正确性：三款学习类逐题都有证明

这一项第 1 轮只做了抽样，本轮正面查了一遍现有覆盖，结论是**已经做到逐题证明，无需补测**：

| 款 | 教育正确性的硬要求 | 现有取证 |
| --- | --- | --- |
| `sudoku-petal` | 188 题**逐题**唯一解；逐题能**纯逻辑**推完，一题都不用猜；题面确实是完整解挖出来的（留着的格子一个没改）；异形宫的宫图合法；对角花的两条斜线也是一到九各一次 | `levels.test.ts` / `generate.test.ts` 共 5 条逐题遍历用例 |
| `mine-garden` | 第 49 关起**无猜**；抽样第 3–8 章每一关都真能生成「一路推到底」的图；换一批首点重来照样无猜；求解器可靠（说安全的一定不是刺种）；首点及 8 邻格永远干净 | `levels.test.ts` 3 条 + `solver.test.ts` 4 档 × 50 seed |
| `weiqi-garden` | 188 关**逐关**算得出至少一个正解，且按正解走能 `levelCleared` 且不超手数；提子 / 做眼 / 打劫 / 官子 / 标死子各自的规则逐题验过 | `levels.test.ts` 8 条 |
| `mahjong-bloom` | 每一关都**天生有解**（先造 14 张胡牌再抽一张塞进牌墙），`solveLevel` 照既定路线走一遍自检；番数与门槛对得上 | `levels.test.ts` 3 条 |

`npx vitest run` 这四款：**26 文件 / 735 例全绿**。另外抽查了文案：`weiqi-garden` 的
「提子」全部说成「请回篮子」，四款的失败句子扫过一遍只有鼓励、没有打击。

---

## 五、本轮记的问题

| 编号 | 定级 | 落在哪 | 一句话 |
| --- | --- | --- | --- |
| **W1-R2-01** | 严重 | `orb-arena/levels.ts` `snake-royale/levels.ts` | 无尽的 `shrink` / `pellets` / `food` 没有上限，第 20 波起安全区 32 秒收到底、第 500 波要铺 3000+ 颗豆子；同一个函数里 `bots` / `viruses` 都是封了顶的 |
| **W1-R2-02** | 一般 | `hero-cards/levels.ts` `chapter7` | 第 7 章「藏花残局」24 关**整章**假人都是 `rookie` 档，夹在第 6 章（normal 12 关 + pro 10 关）和第 8 章（pro 12 关 + hell 12 关）中间，是一段难度低谷。同一份文件里第 3 章用的是 `tier: step >= 2 ? "normal" : "rookie"`，写法现成 |
| **W1-R2-03** | 轻微 | 6 款的 `index.ts` | `-modebar` 被复用成「一排按钮」的通用类名，地图上的模式入口和模式内部的档位选择器同名，自动化走查分不清。和第 1 轮的 W1-06（返回键与模式入口同类名）是同一类问题 |
| **W1-R2-04** | 观察 · 跨窗口 | 平台 | 首页设备芯片区分度很低：全部 67 / 手游 66 / 端游 60。原因是 1.1 那 55 款缺省当 `both`，两边都露面。芯片本身工作正常（第 1 轮 W1-R1-04 修的是这个），要不要给老游戏补 `platform` 是主管的口径题，**窗口 1 不越界去改别人的 `meta.ts`** |

阻断级：**0 条**。

## 六、没变的那些（回归确认）

- 第 1 轮修掉的七条一条都没有复发：Esc 双层暂停（连打 8 下压测过）、360px 正文字号
  （第二批关卡重量一遍，12 款最小字号都是 14px 的「📖 攻略」，正文全部 ≥ 16px）、
  `smoke-1.2-step4-c` 的依赖、设备芯片筛选、`mg-` 前缀撞车、首页款数写死、麻将暂停遮罩。
- root 门 26 项逐条复验：密码 `kangkang`、电话 `18438037080`、一小时过期（偏差 52ms）、
  可手动关、连错 3 次锁 120 秒、密码翻遍 storage / cookie / URL / DOM 都搜不到、
  直达第 100 关落点正确、门关着时直达框消失、**家长算术门原样还在**。
- 无外链脚本 / 字体，全局没有 three.js。
- 商标扫描 186 个文件 × 29 个词，出货代码 0 命中（`*.test.ts` 里 105 处黑名单数组是正确用法）。

## 七、交给本轮学习优化员 / 监督修复员

- **修复员**：W1-R2-01（严重）必修，W1-R2-02（一般）必修。W1-R2-03 是类名规范，
  不改行为，可以顺手改也可以留到第 3 轮。W1-R2-04 不属于本窗口，原样交给主管。
- **学习优化员**：本轮已有一份平行落地的报告（`1.2-window1-round2-learner.md`，
  做的是读屏播报节流、`engine/index.ts` 补出口、7 份 `PLAN.md` 挪出源码目录）。
  刀口和本报告记的三条不重叠，不用回避。

---

# 乙、第二份 · 平台五项 / 四模式矩阵 / 双人分边 / 定 seed（样本关 2 · 50 · 150）

> 本份的问题编号从 **W1-R2-05** 起，避开第一份已占用的 01–04。

> 分支：从 `origin/game-1.2-window1` 切出。开工时上游 HEAD 是 `df966f8`，走查期间上游前进了三次：
> 先到 `0626897`（第 1 轮修复员/学习员的 26 个提交），再到 `e430a8b`（第一份第 2 轮报告 + 第 2 轮修复员的 W1-R2-01~03），
> 最后到 **`25e706a`**（含 `e4d0c31`，正好修掉本份 W1-R2-06 的三处之一）。
> **每次都跟上并把浏览器取证重跑一遍**，本份的所有数字都是 `25e706a` 这棵树上的读数。
> 全程没有从 `game-1.2` / `main` 重砍，没有 `--force`。
> 范围**仅限窗口 1**：第 1 步平台能力 + 12 款新游戏。窗口 2–5 点名的
> `gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi` 一律不碰。
> 本轮只记问题、只写只读走查脚本，**没有改任何 `src/games/**` 玩法代码，也没有动 `package.json` / lockfile / 既有测试**。

## 〇、这一轮换了什么样本、换了什么量法

第 1 轮的取证有两处天花板，这一轮是冲着它们去的。

| 维度 | 第 1 轮 | 第 2 轮 |
| --- | --- | --- |
| 战役关号 | 1 / 100 / 188 | **2 / 50 / 150**（12 款都是 188 关，没有越界，不用夹） |
| 视口 | 只有 360×640 | 360×640 **加一档 1280×800 宽屏对照** |
| 字号巡检 | 只到入口屏 | **入口屏 + 闯关屏 + 对战/无尽/双人三个二级界面 + 暂停屏**，另加一遍 `clamp()` 下界的源码扫描 |
| 双人取证 | 整屏指纹（`mine-garden` 因此误报过一次） | **按各款自己的分边容器切「座」**，再分 DOM / 画布分块比对；混战类改按玩家颜色算重心；回合制改读「第 N 手」这类只有真出手才会变的计数 |
| W1-10 / W1-11 | 偶发失败，没结论 | **把页面里的 `Math.random` 钉死成固定 seed** 复现，两条都给出了结论 |
| 六款过关证据 | 通用假人轻点按键，几乎打不动 | 假人改成**按住 0.5–1 秒**再松；另外独立复跑了上游第 1 轮修复员留下的专用假人 |

走查脚本：`scripts/smoke-1.2-window1-r2-tester.mjs`（本轮新增，只读；第 1 轮的
`scripts/smoke-1.2-window1-r1-tester.mjs` 原样保留没覆盖）。

跑法（`puppeteer-core` 是临时工具，用 `npm i --no-save` 装，没有进 lockfile）：

```bash
npm i --no-save puppeteer-core
npx vite --port 5186
PART=A node scripts/smoke-1.2-window1-r2-tester.mjs   # 平台五项 + 首页
PART=C node scripts/smoke-1.2-window1-r2-tester.mjs   # 12 款四模式矩阵 @360×640
PART=D node scripts/smoke-1.2-window1-r2-tester.mjs   # 同一套矩阵 @1280×800
PART=E node scripts/smoke-1.2-window1-r2-tester.mjs   # 双人键位分边取证
PART=F node scripts/smoke-1.2-window1-r2-tester.mjs   # W1-01 Esc 回归复核
PART=G node scripts/smoke-1.2-window1-r2-tester.mjs   # W1-10 固定 seed
PART=H node scripts/smoke-1.2-window1-r2-tester.mjs   # W1-11 固定 seed
PART=I node scripts/smoke-1.2-window1-r2-tester.mjs   # W1-12 补过关证据
PART=J node scripts/smoke-1.2-window1-r2-tester.mjs   # merge-2048 aria-live 观察
PART=K node scripts/smoke-1.2-window1-r2-tester.mjs   # 围子花园双人键位分不分人(本轮新增)
PART=L node scripts/smoke-1.2-window1-r2-tester.mjs   # 暂停屏字号 + clamp() 下界(本轮新增)
PART=M node scripts/smoke-1.2-window1-r2-tester.mjs   # 长蛇争霸「不操作」对照(本轮新增)
```

各段结果一览（均为跟上 `25e706a` 之后的重跑读数）：

| 段 | 内容 | 项数 | 通过 |
| --- | --- | --- | --- |
| A | 平台五项 + 首页（360×640） | 46 | **46** |
| C | 12 款 × 四模式矩阵（360×640） | 178 | 163（15 项失败全是字号，见第六节） |
| D | 同一套矩阵（1280×800 宽屏对照） | 107 | **107** |
| E | 双人键位分边取证 | 61 | 44（详见第四节，多数「失败」是量法上限） |
| F | W1-01 Esc 双暂停回归复核（5 款 × 第 50 关） | 5 | **5** |
| G | W1-10 围子花园固定 seed | 3 | **3** |
| H | W1-11 飞行棋固定 seed | 2 | **2** |
| I | W1-12 六款补过关证据 | 6 | 1（另外 5 款都玩到了结算） |
| J | `merge-2048` aria-live 观察 | 1 | **1** |
| K | 围子花园双人键位分不分人 | 2 | 0（**W1-R2-05**） |
| L | 暂停屏字号 + `clamp()` 下界 | 13 | 0（**W1-R2-06** 与旧账 W1-R1-08） |
| M | 长蛇争霸「不操作」对照（三批共 22 局） | 7 | 6（**W1-R2-07**） |

---

## 一、平台五项（PART=A，46 项全过）

### root 管理员门

| 硬要求 | 结果 | 本轮量到的原始数据 |
| --- | --- | --- |
| 首页有入口 | ✅ | `.icon-btn--admin`（🔑） |
| 电话文案原样 | ✅ | 弹窗正文含「要打开请联系管理员 **18438037080**」 |
| 密码框看不见字 | ✅ | `input.type === "password"` |
| 密码 `kangkang` 能开 | ✅ | 在弹窗里真敲一遍 + 回车，门开 |
| **只写** `yiduo-yixing.root.v1` | ✅ | `{"expiresAt":1787829092993}`，`Object.keys().length === 1` |
| **TTL 1 小时** | ✅ | 写进去的 `expiresAt - now = 60 分钟`；假时钟推到 1 小时后自动关门，且过期会话连存档一起清掉 |
| **密码永不落盘** | ✅ | 开门后把 `localStorage` + `sessionStorage` + `document.cookie` + `location.href` 拼起来搜 `kangkang`，**0 命中**；弹窗里的 `input.value` 也被抹成空串 |
| **`closeRoot()`** | ✅ | 调用前 `isRootOpen()===true`，调用后 `false`，`localStorage` 里那把 key 当场变 `null` |
| 弹窗 360px | ✅ | `doc+0`，最小字号 14px |

### 直达第 N 关（本轮样本 2 / 50 / 150）

| 硬要求 | 结果 | 证据 |
| --- | --- | --- |
| 门关着连控件都不生成 | ✅ | 选关页 `.l99-jump` 数量 **0**（不是 `hidden`，是根本没建 DOM） |
| 门开着才出现 | ✅ | `.l99-jump` 出现 1 个 |
| 直达 2 / 50 / 150 | ✅ | 以 `sudoku-petal` 取证：`🌱 四宫萌芽 · 第 2 关`（16 格）、`🌼 唯余九宫 · 第 50 关`（81 格）、`🧩 异形宫 · 第 150 关`（81 格），盘面都真画出来了 |
| **直达不发星** | ✅ | 直达完这三关，`yiduo-yixing.l99.sudoku-petal` 的第 2 / 50 / 150 关星级仍旧全是 `0` |
| 越界 / 乱输自动夹 | ✅ | `999→188`、`0→1`、`-5→1`、`abc→null`、`149.6→150` |

### 家长算术门（本轮**没有改动**，只做原样核对）

`src/ui/parentAuth.ts` / `parentGate.ts` 一个字都没被本轮碰过，也没被 root 门改坏：还是出算术题（本轮抽到 `5 × 5 = ?`），答对放行答错拦下；四个参数维持 1.1 口径 —— `AUTH_TTL_MS = 5 分钟`、`MAX_WRONG = 2`、`LOCK_MS = 90 秒`、`HIGH_NEED_CORRECT = 2`。

### 手游 / 端游筛选

三颗芯片（🌈全部 / 📱手游 / 💻端游）都在；`matchesPlatformChip` 对 `{}`、`{platform:"both"}`、`{platform:"脏值"}`、`{platform:undefined}` 一律双命中；只写 `mobile` / `desktop` 时只落对应那一颗。

### 2.5D（禁 three.js）

运行时没有全局 `THREE`、没有 three 的 `<script>`；自写透视 `z=1 → scale 0.857`、`z=40 → 0.130`；`flat` 档缩放恒 1、地平线落在画面里、脏输入（`NaN` / `fov=0` / 视口 `0×0`）一律给有限数。

---

## 二、必核第 1 轮已修项：**四条仍绿**

| 项 | 本轮怎么量的 | 结果 |
| --- | --- | --- |
| **W1-01 Esc 与游戏壳双暂停** | 换第 **50** 关重测 5 款（`orb-arena` `snake-royale` `block-drop` `combo-clash` `merge-2048`）：先确认画面在动 → 按一次 Esc → 看暂停提示 → 再按一次 Esc → 再确认画面又动起来 | ✅ **5/5 全绿，没有回归**。壳层面板 0 个（游戏自己拦住了 `Escape`，壳层不再重复弹） |
| **W1-02 拼音搜索** | 12 款标题的首字母逐条在首页搜索框里真敲；再把**全库 67 款**标题拿 `pinyinInitials()` 过一遍，看首字母串长度够不够汉字数 | ✅ **12 串全中**（`yydzz` `cszb` `fkddl` `lzdj` `hkmj` `dxdc` `yjl` `wzhy` `fxqly` `xxhc` `slhy` `sdht`，每次都收窄到 1 张）；**全库 67 款一款不缺** |
| **首页搜索框 360px 不被顶出 / 字号 16px 下限** | 量 `.home-search-input` 的实际矩形与 `computedStyle` | ✅ `x=61 → right=279`，视口 360，整条在屏内；字号 **17px ≥ 16px**（iOS 聚焦不会自动放大）。入口屏最小字号 14px，没有越线 |
| **root 密码不落盘** | 见第一节 | ✅ 四个落点全搜不到 |

---

## 三、12 款 × 四模式矩阵

四种模式一律以各款 `meta.modes` 为准。**11 款是 campaign / versus / endless / twoPlayer 四模式；`hero-cards` 按 meta 只有 campaign / versus / endless**（身份场两个人挤一屏会互相看光手牌），不算缺模式。

### 360×640（PART=C，178 项过 163）

| 检查项 | 结果 |
| --- | --- |
| 从首页点卡片进得去 | ✅ 12/12 |
| 模式条按 `meta.modes` 列齐 | ✅ 12/12 |
| **闯关第 2 / 50 / 150 关进得去、画得出、`doc+0` 不溢出** | ✅ **36/36** |
| **对战 / 无尽 / 双人进得去、画得出、`doc+0` 不溢出** | ✅ **35/35**（11 款 × 3 + `hero-cards` × 2） |
| `destroy` 不泄漏 | ✅ 12/12，退出后 `{listeners:4, intervals:1, frames:0}` 回到基线，一个不多 |
| 退出再进还能挂起来 | ✅ 12/12 |
| 全程无 `pageerror` / `console.error` | ✅ 12/12 |
| 二级界面 360px 字号下限 | ❌ 15 项，拆开看见第六节：**12 项**是平台旧账 W1-R1-08（`.l99-star` 12px，12 款闯关屏各一），**3 项**是 `star-estate` 的 `.se-token` 13px（对战/无尽/双人三屏，`clamp()` 下界，见 W1-R2-06） |

抽几条原文当样本（都是从 `.l99-stagetitle` 上读回来的）：

| id | 第 2 关 | 第 50 关 | 第 150 关 |
| --- | --- | --- | --- |
| `sudoku-petal` | 🌱 四宫萌芽 | 🌼 唯余九宫 | 🧩 异形宫 |
| `combo-clash` | 🌸 轻击学堂 | 🕊️ 跳入花园 | 🎲 起身猜拳 |
| `weiqi-garden` | 🌱 气的花园 | 🔁 打劫入门 | 🌾 十三路原野 |
| `mine-garden` | 🌱 小苗床 | 🎵 和弦课 | 🌾 高级田 |

### 1280×800 宽屏对照（PART=D）

同一套矩阵在宽屏上重跑：**107 项 107 全过**，一条不挂。窄屏挂掉的那 15 项在宽屏上全部达标，**字号问题是窄屏专属**。

### 商标黑名单

29 个词逐个扫 12 款目录 + 6 个平台文件的**出货源码**（排除 `*.test.ts`：测试文件里的黑名单数组本身是正确用法）以及 `dist/assets/` 打包产物：**两边都是 0 命中**。

---

## 四、双人键位：这一轮换的是**量法**

第 1 轮拿整屏指纹量，在 `mine-garden` 上误报过一次（两块盘同种子、长得一模一样，整屏指纹分辨不出第二个人）。这一轮改成三种按款式挑的量法，并且**每一轮都先采一遍「一根手指都不动」的空跑基线再做差集**，把自己会动的东西（动画、倒计时、对家 AI）先扣掉。

三种量法：

1. **分座（`split`）** —— 先找游戏自己的分边容器（`.bd-seats` / `.mg-seats` / `.mn-duo` / `.sp-seats` / `.oa-panes` / `.sr-panes`），它的每个直接子节点算一「座」，一座一份 DOM 摘要 + 一份画布分块像素摘要。判定标准是**存在只认朵朵的座、也存在只认星星的座**。
2. **按颜色分人（`realtime`）** —— 混战类两个人在同一个场地里，没有「左右两边」，改数朵朵的粉 `#F5A9C8` 和星星的蓝 `#A9C8F5` 各自的像素重心，做「按住左 → 按住右 → 按住左」三段来回，要求横坐标一去一回符号相反。
3. **看回合（`turn`）** —— 同一块盘轮流出手的四款，谁的回合只有谁能动，做差集没有意义。改成读「轮到谁」，按他自己那套键看吃不吃得进去；回合还在他手上时，再拿对家那套键探一下会不会串台。围子花园另外单开了 PART=K，改读**「第 N 手」**这个只有真落子才会 +1 的计数，避开「画布动了也可能只是光标挪了一格」的歧义。

### 结果

| id | 量法 | 朵朵 `WASD+F+G` | 星星 `方向键+L+K` | 分得开吗 | 原始读数 |
| --- | --- | --- | --- | --- | --- |
| `block-drop` | 分座 `bd-seats×2` | ✅ | ✅ | ✅ | 只认朵朵＝`座0.canvas0.B / 座0.dom / 座0.num`；只认星星＝`座1.canvas0.B / 座1.num` |
| `merge-2048` | 分座 `mg-seats mg-split×2` | ✅ | ✅ | ✅ | 只认朵朵＝`座0.dom,座0.num`；只认星星＝`座1.dom,座1.num` |
| `mine-garden` | 分座 `mn-duo×2` | ✅ | ✅ | ✅ | 只认朵朵＝`座0.dom`；只认星星＝`座1.dom`（第 1 轮那次误报，这一轮用独立量法再次确认是误报） |
| `sudoku-petal` | 分座 `sp-seats×2` | ✅ | ✅ | ✅ | 只认朵朵＝`座0.dom`；只认星星＝`座1.dom` |
| `mahjong-bloom` | 看回合 | ✅ 轮到朵朵时吃得进去 | ✅ 轮到星星时吃得进去 | ✅ 不串台 | 回合流转「朵朵 → 星星」；**轮到谁时按对家的键，毫无反应** |
| `flight-chess` | 看回合 | ✅ | ✅ | ✅ 不串台 | 回合流转「朵朵 → 星星」；两边的串台探针都是「毫无反应」 |
| `star-estate` | 看回合 | ✅ | ✅ | ✅ 不串台 | 回合流转「朵朵 → 星星」；轮到谁时按对家的键毫无反应 |
| `combo-clash` | 颜色 | ✅ | 🟡 | 🟡 部分 | 朵朵那团粉跟着 `A/D` 走了 `+160px / −122px` 一个来回；星星那一侧在画布上找不到足够的自有颜色像素（两个角色用的是同一套配色盘），**取证上限** |
| `weiqi-garden` | 看回合 + 读手数 | ✅ | ✅ | ❌ **会串台，两个方向都串** | 见 **W1-R2-05** |
| `orb-arena` | 颜色 | 🟡 | 🟡 | 🟡 | **取证上限**，见下 |
| `snake-royale` | 颜色 | 🟡 | 🟡 | 🟡 | **取证上限**，见下 |
| `hero-cards` | — | — | — | — | `meta` 里就没有 `twoPlayer`，跳过 |

### `orb-arena` / `snake-royale` 的取证上限（不判缺陷）

这两款双人同屏是**两块 pane、每块一台跟着自己那个人跑的摄像机**（`.oa-panes×2` / `.sr-panes sr-split×2`）。两件事叠在一起，浏览器侧的行为取证就做不下去了：

- 场地每一帧都在动，**空跑基线里所有区域都会变**（`座0.canvas0.*`、`座1.canvas0.*`、`外.dom`、`座*.dom` 全都在名单上），差集扣完什么都不剩；
- 摄像机锁在自己人身上，所以**自己那团颜色永远待在 pane 正中**（量到的 Δx 只有 ±1 到 ±11px），而对家那团颜色的位移是「对方在动」和「摄像机在动」两件事叠出来的，符号会翻。

本轮能拿到的证据是**结构侧 + 代码侧**的：

- 结构侧：双人同屏确实是两块 pane，pane0 里朵朵的粉色像素 4055 个、星星 0 个；pane1 里星星的蓝色像素 3585 个 —— 一人一块，摄像机各锁各的；
- 代码侧：两款都把两套键位绑到了**不同的 owner** 上，不是同一份表：

```275:276:src/games/orb-arena/index.ts
  const DUO_KEYS: Record<string, Vec> = { w: { x: 0, y: -1 }, s: { x: 0, y: 1 }, a: { x: -1, y: 0 }, d: { x: 1, y: 0 } };
  const STAR_KEYS: Record<string, Vec> = {
```

`applyKey(duo.id, DUO_KEYS[k], …)` 与 `applyKey(target.id, STAR_KEYS[e.key], …)` 走的是两个不同的 id，`snake-royale` 同构。

**所以这两款不进问题清单**：没有任何证据说它坏，但也请后续别把「双人键位已在真浏览器里逐款量过」这句话套到它们头上 —— 它们只有结构与代码两侧的证据。要补行为侧的硬证据，得让这两款在测试模式下把两个人的坐标暴露出来，那属于改玩法，本轮不做。

---

## 五、第 1 轮遗留：逐条结论（上游 26 个提交已经关掉大半）

开工时这些还挂着，走查过程中上游把其中五条修掉了。下面是**在 `25e706a` 上重新取证**的结果，不是照抄第 1 轮的话。

| 编号 | 本轮实测 | 状态 |
| --- | --- | --- |
| W1-03 `playModes.ts` 出货 0 引用 | `modeEntryKeys` / `compatFromMeta` 这批导出现在被 **12 款 `index.ts` 全部用上**，加 `src/engine/index.ts` 的统一出口共 13 个出货文件引用 | ✅ **已关** |
| W1-04 `view25d` 透视三份并存 | `engine/index.ts` 已经把 `view25d` 补进统一出口（第 52–53 行）。但 `project()` / `horizonY()` 那套透视数学**仍是三份**：`src/engine/view25d.ts`、`src/games/duo-rush/view25d.ts`、`src/games/rainbow-run/view3d.ts`；12 款里真正 `import` 共享模块的还是只引 `prefersReducedMotion` 这一个无障碍开关（`orb-arena` / `block-drop` / `flight-chess` / `merge-2048` 四款） | 🟡 **半关**：出口通了，复用没做（另两份属于窗口 2–5 的目录，本窗口无权动） |
| W1-05 `mg-` 前缀撞车 | `mine-garden` 已整体换成 `mn-`（`mn-wrap` / `mn-open` / `mn-back` …共 53 个类）。两款类名交集现在只剩 `aria-label` 这个标准属性，**实质撞车 0 个** | ✅ **已关** |
| W1-06 返回键与模式入口同类名 | `mine-garden` 的返回键已拆成 `.mn-back`。12 款逐个核过：11 款用 `<前缀>-back`（`mahjong-bloom` 是 `mj-back-btn`），`weiqi-garden` 的返回键走 `.wq-btn`，和模式入口 `.wq-open` 不是一个类。**没有一款再共用** | ✅ **已关** |
| W1-07 两套驱动 | 按 `import` 行数：**10 份 `playwright` + 23 份 `puppeteer-core`**（`smoke-1.2-step4-c.mjs` 已经改过来了，正文里只剩一句解释注释）。两个包都仍不在 `package.json` | ⬜ **仍在**（比第 1 轮少一份） |
| W1-08 `snake-royale` 皮肤 key | `src/games/__tests__/window1-storage-keys.test.ts` 新增，`SKIN_KEY` 已写进机器读得懂的硬约束清单 | ✅ **已关** |
| W1-09 `PLAN.md` 混在出货源码 | `src/games/*/PLAN.md` **0 份**，7 份都挪到 `docs/plan-1.2-window1-*.md` | ✅ **已关** |

### W1-10 · **结论：脚本挑点的问题，不是 AI 缺陷**（建议关掉）

把页面里的 `Math.random` 钉成固定 seed，跑 `smoke-1.2-step4-b` 那三个写死的落点 `(2,2) (6,6) (2,6)`，并且把**「我这一手落没落下去」和「AI 回没回」拆成两个独立判定**：

| seed | 手数 | (2,2) | (6,6) | (2,6) |
| --- | --- | --- | --- | --- |
| 1 | 0 → 6 | 我✓ / AI✓ | 我✓ / AI✓ | 我✓ / AI✓ |
| 7 | 0 → **4** | 我✓ / AI✓ | 我✓ / AI✓ | **我✗ / AI✗** |
| 20240601 | 0 → 6 | 我✓ / AI✓ | 我✓ / AI✓ | 我✓ / AI✓ |
| 424242 | 0 → 6 | 我✓ / AI✓ | 我✓ / AI✓ | 我✓ / AI✓ |
| 987654321 | 0 → **4** | 我✓ / AI✓ | 我✓ / AI✓ | **我✗ / AI✗** |

- **AI 缺回合的 seed 数：0/5** —— 只要我这一手落得下去，AI 每一次都在 4 秒内回了。
- 复现失败的 2/5 个 seed，挂的都是**我自己那一手就没落下去**：第三个写死的点 `(2,6)` 被前两手的应手占掉了，落子被判非法。

**修法建议给修复员**：`scripts/smoke-1.2-step4-b.mjs` 别用写死的三个点，改成每一手先读一下棋盘、挑个空交叉点再落；断言也拆成「我这一手落下去了」+「AI 回了一手」两条，失败时才看得出是谁的锅。

### W1-11 · **结论：固定 seed 下复现不出来，原失败是偶发**（建议关掉）

两步取证。先用纯函数看骰子流公不公平：`dice.ts` 的 `rollSeq(seed, 80)` 按四人轮流切开，八个 seed 下**每个座位都掷得出 6**（每座 1–8 次不等，合计 8–15 次），骰子流本身不偏心。

再把 `Math.random` 钉死（`flight-chess/index.ts` 的模式 seed 就是从它来的），在 **375×667** 真跑四人对战 90 秒：

| seed | 已掷 | 在路上 |
| --- | --- | --- |
| 1 | 0 → 79 | **9/16** |
| 20240611 | 0 → 73 | **9/16** |
| 987654321 | 0 → 79 | **10/16** |

`step4-c` 的阈值是「≥4 架在路上」，**三次全部远远越过**（9–10 架）。第 1 轮那次 `3/16` 复现不出来，判定为偶发，**不是缺陷**。

### W1-12 · 六款过关证据：**结算 6/6，赢一次 2/6**

这一条本轮有两份互相独立的证据。

**第一份是我自己的通用假人**（PART=I，第 2 关，每款最多 3 局 × 75s）。第 1 轮的假人是「轻点一下按键」，对圆圆、长蛇这种要**按住**才走得动的款几乎等于没动；这一轮改成「按住 0.5–1 秒再松」：

| id | 第 2 关目标 | 本轮结果 | 第 1 轮 |
| --- | --- | --- | --- |
| `star-estate` | 14 回合内把净资产做上去 | ✅ **过关**（第 1 局 · 64 次操作 · 9s） | ✅ |
| `orb-arena` | 长到 92 质量 | 🟡 玩到结算「就差一点点！」（81 次操作） | ❌ 连结算都没到 |
| `snake-royale` | 长到 41 长度 | 🟡 玩到结算（76 次操作） | ❌ |
| `block-drop` | 消 3 行 | 🟡 玩到结算（61 次操作） | ❌ |
| `combo-clash` | 三局两胜 | 🟡 玩到结算（50 次操作） | ❌ |
| `mahjong-bloom` | 平和（1 番起） | 🟡 玩到结算（12 次操作） | ❌ |

**六款现在都能玩到结算**（第 1 轮只有 1 款），通用假人这条线到此为止。

**第二份是上游第 1 轮修复员留下的专用假人** `scripts/qa-1.2-window1-winlose.mjs`。它在修复员报告里的结果一栏写的是「见第 2 轮补齐」—— 那就是这一轮的活。我独立跑了两遍：先整批跑一遍（2 把 × 120s），`star-estate` 和 `snake-royale` 在这一遍就赢了；没赢的四款再按脚本默认预算（**3 把 × 150s**）单独重跑，确认不是预算给少了：

| id | 第 1 关真操作打到过关 | 高关号放着不动 |
| --- | --- | --- |
| `star-estate` | ✅ **第 1 把就过** | 第 170 关超时没结算 |
| `snake-royale` | ✅ **第 2 把过** | 第 160 关**过关了**（见 W1-R2-07） |
| `orb-arena` | ❌ 足预算 3 把都没过 | 第 160 关「就差一点点」✅ |
| `block-drop` | ❌ 足预算 3 把都没过 | 第 170 关「就差一点点」✅ |
| `combo-clash` | ❌ 足预算 3 把都没过 | 第 170 关「就差一点点」✅ |
| `mahjong-bloom` | ❌ 足预算 3 把都没过 | 第 170 关超时没结算 |

上游 `e430a8b` 那批改动里有一条是「圆圆假人学会吃对手」，所以收工前又拿最新树把这两款单独复跑了一次：
`snake-royale` **第 3 把过关**（仍然赢得到），`orb-arena` **3 把依旧没过**；同一遍里第 160 关放着不动这次是「就差一点点」，
和 W1-R2-07 记的「大约六分之一」对得上。

合起来：**「赢一次」的硬证据从第 1 轮的 1/6 提到 2/6**（`star-estate` + `snake-royale`），其余四款仍是「玩到结算但没赢」。

顺带两条关于这份专用假人本身的意见，供修复员参考（都不是游戏的缺陷）：

- `mahjong-bloom` / `star-estate` 是回合制、没有倒计时，「放着不动」永远等不到结算，assert「一定会没过」对这两款不成立，建议这两款换个求负口径（例如故意打出必输的一手）；
- 第 160 关那条 assert 会随机变红，根因见 W1-R2-07。

**剩下四款为什么还赢不了**：不是打不通，是**取证手段够不着**。第 4、5 步那六款有过关硬证据，靠的是每款配一份「在页面里 import 自家 `ai.ts` / `solver.ts` 算出下一手，再用真鼠标真键盘落子」的脚本。这四款的 AI 吃的是内部状态模型，而现场盘面没有对外读取口：`orb-arena` 的 `aiSteer(view, tier)` 要一份 `AiView`（自己和所有对手的坐标、半径），`block-drop` 的 `choosePlacement(board, id, tier)` 要一份实时 `Board`，现场都只有画布像素。`weiqi-garden` / `sudoku-petal` / `mine-garden` 能做成，是因为它们的题面可以从 `levels.ts` 纯函数**重算**出来，不依赖运行时状态。

**建议给修复员 / 主管**（两条择一，都不必改玩法手感）：

1. 让这四款的 `mount()` 在 `import.meta.env.DEV` 下多导出一个只读的 `snapshot()`，冒烟脚本据此喂给自家 AI —— 最省事，且不影响生产包；
2. 或者给这四款各补一份 `levels.ts` 侧的纯函数「本关必胜序列」，冒烟脚本照 `step5-c` 的写法把序列敲进去。

在补上之前，请把这四款的过关能力记为**「未取证」**，不要记为「已验证」，也不要记为「打不通」。

### 观察项 · `merge-2048` 的 aria-live 已经加上节流

上游 `3cba7b1` 给读屏播报加了节流闸，本轮实测确认生效：连按 24 下方向键，`[aria-live]` 节点**只改写 3 次，最短间隔 900ms，间隔 <100ms 的 0 次**（第 1 轮同样的按法是 20 次 / 最短 116ms）。读屏跟得上，**这条观察项可以关掉**。

代价是播报变成隔几步报一次（末尾三句是「第 1 步…」「第 8 步…」「第 16 步…」），中间的步数不再念。对「知道现在几分、最大几」这个用途没影响，就不另外提问题了。

顺带记一笔：同一屏上仍同时挂着 **4 个 `aria-live="polite"` 区域**（`.chip.star-chip`、`.mg-msg`、`.mg-say`、`.sr-only`）。眼下没观察到互相打断，但四个 polite 区域同时活着，读屏要排队念，后面再往上加播报点之前值得先合并一下。**只做记录，不升严重度。**

---

## 六、本轮问题清单

| 编号 | 严重度 | 标题 | 范围 |
| --- | --- | --- | --- |
| W1-R2-05 | **严重** | `weiqi-garden` 双人同屏两套键位不分人，任一方都能替对方落子（两个方向都串） | `weiqi-garden` |
| W1-R2-06 | 一般 | `clamp()` 的字号下界绕过了 14px 下限那条单测；本轮量到 3 处 13px，收工前上游修掉 1 处，`star-estate` 两处仍在，其中 `.se-goal` 是真正文 | `star-estate` + `src/games/window1-mobile-text.test.ts` |
| W1-R2-07 | 一般 | `snake-royale` 第 160 关一根手指不动也有约 1/6 的概率过关 | `snake-royale` 第 160 关（第 7 章 `rank` 目标） |

> 第 1 轮已修且本轮仍绿的 W1-01、W1-02，以及第 1 轮已撤回的「`mine-garden` 星星键位失灵」，都**没有**再报成新问题。
> 上游本轮修掉的 W1-03 / W1-05 / W1-06 / W1-08 / W1-09 也**没有**再报。
> `orb-arena` / `snake-royale` / `combo-clash` 的双人行为证据、以及四款的「赢一次」，都记为**取证上限**，不进问题清单。
> **旧账 W1-R1-08（`.l99-star` 12px）本轮仍在**，但它属于平台文件 `src/ui/level99.ts`，第 1 轮修复员已写明「文件所有权不在本窗口」并交了主管，这里只做复核，不重新编号。

---

### W1-R2-05 · 严重 · `weiqi-garden` 双人同屏两套键位不分人

**决定性证据**（PART=K）。这一款自己会播报「第 N 手」，而手数**只有真落子才会 +1**，所以不用去猜画布上那一下是光标还是棋子：

```
开局读数:第 0 手 · 轮到 朵朵
轮到朵朵时,只按星星那套键(← ↓ L)  → 手数 0 → 1、轮次 朵朵 → 星星
轮到星星时,只按朵朵那套键(→ F)    → 手数 1 → 2、轮次 星星 → 朵朵
```

**两个方向都串**：谁的回合都不影响，另一套键照样把子落下去。

**根因**：`keyAction()` **不带玩家参数**（`keyAction.length === 1`），`WASD` 和方向键返回同一个动作，`F` 和 `L` 都是 `confirm`，`G` 和 `K` 都是 `pass`：

```116:150:src/games/weiqi-garden/index.ts
export function keyAction(key: string): KeyAction {
  switch (key) {
    case "ArrowUp":
    case "w":
    case "W":
      return "up";
    // …
    case "f":
    case "F":
    case "l":
    case "L":
    case "Enter":
      return "confirm";
    case "g":
    case "G":
    case "k":
    case "K":
      return "pass";
```

本轮实测的映射表：`{"w":"up","ArrowUp":"up","a":"left","ArrowLeft":"left","f":"confirm","l":"confirm","g":"pass","k":"pass"}` —— 两套键完全等价。

双人走的是自由对战那份 `mount()`（`mode === "duo"` 时 `white: "human"`），它的 `onKey` 直接用 `keyAction(e.key)`，中间没有任何「这个键是谁的」判断；两个人还共用同一个 `view.cursor`：

```856:869:src/games/weiqi-garden/index.ts
  function onKey(e: KeyboardEvent): void {
    const act = keyAction(e.key);
    if (!act) return;
    if (act === "pause") {
      e.preventDefault();
      togglePause();
      return;
    }
    if (paused) return;
    if (act === "confirm") {
      e.preventDefault();
      tap(view.cursor);
      return;
    }
```

**对照组**：同为共用一块盘的 `mahjong-bloom` / `star-estate` / `flight-chess` 三款，本轮的串台探针都是**「毫无反应」**；`mine-garden` 的 `keyAction(key, "p1" | "p2")` 更是直接带了玩家参数。所以这不是「共用盘就只能这样」，是这一款漏了。

**影响**：不崩不卡，单人闯关完全没事（闯关本来就只有一个人）。双人同屏下手快的那个可以替对方落子，两个小朋友抢一台设备就会吵架。定**严重**是因为它让「双人同屏」这个模式在这一款上事实上不成立 —— 不是画面问题，是规则被绕过去了。

**建议**：给 `keyAction` 加一个玩家参数（照 `mine-garden` 的 `keyAction(key, who)` 写），双人模式下按当前该谁走来收键；单人闯关维持「两套键都认」的现状，别把闯关的手感改掉。顺带把光标也拆成两个，不然两个人抢同一个光标位置照样打架。

---

### W1-R2-06 · 一般 · `clamp()` 的下界绕过了 14px 那条单测

**仓库自己定的规矩**（`src/ui/mobileText.ts`）：`MIN_BODY_PX = 16`、`MIN_CONTROL_PX = 14`。

上游新增的 `src/games/window1-mobile-text.test.ts` 有一条「没有一处写死的字号低于控件下限 14px」，它是这么找的（**截至收工仍是这样**）：

```38:41:src/games/window1-mobile-text.test.ts
/** 源码里所有写死的 `font-size:Npx`（不含 `var()` / `clamp()` / `max()` 这些兜底写法） */
function hardCodedSizes(src: string): number[] {
  return [...src.matchAll(/font-size:\s*([0-9.]+)px/g)].map((m) => Number(m[1]));
}
```

正则要求 `font-size:` 后面**紧跟数字**，所以 `font-size:clamp(13px,…)` 一律看不见；正文那一条断言里也写了 `if (/^(inherit|max\(|clamp\()/.test(value)) continue;`。**结果就是：只要把小字号写进 `clamp()` 的下界，这条下限规矩就形同虚设。**

12 款里这样的地方，本轮先后量到 **3 处**；收工前上游 `e4d0c31` 已经修掉其中一处（`flight-chess` 那条，并且把
`src/ui/mobileText.test.ts` 那把尺子对 `clamp()` 的读法从「取最大值」改成「取下限」）：

| 位置 | 声明 | 360px 上的实测 | 是不是正文 | 状态 |
| --- | --- | --- | --- | --- |
| `star-estate` `.se-goal` | `clamp(13px,2.4vw,14px)` | **13px** | ✅ **是**，双人暂停屏的键位说明 | ⬜ **仍在** |
| `star-estate` `.se-token` | `clamp(13px,2.1vw,15px)` | 13px | 否，地块上的 🌸 ⭐ 🍡 ☁️ | ⬜ **仍在** |
| `flight-chess` `.fc-token-face` | `clamp(13px,2.6vw,17px)` | 13px | 否，棋子上的 🌸 ⭐ | ✅ 上游 `e4d0c31` 已抬到 14px |

**但另一把尺子还是瞎的。** `e4d0c31` 修的是 `src/ui/mobileText.test.ts` 里的 `fontSizeOf`；
`src/games/window1-mobile-text.test.ts` 的 `hardCodedSizes` 没跟着改，仍旧只认 `font-size:<数字>px`。
拿它扫 `star-estate/index.ts`，看得见的最小值是 **14px**（合格），而实际渲染出 13px 的
`.se-goal` / `.se-token` 两条它一条都看不到。**同一个仓库里两把量字号的尺子，口径还是不一致。**

`.se-goal` 那条是**真的要读的中文**，本轮在真浏览器里量到了：

```
{ "cls": "se-goal", "px": "13px", "visible": true,
  "text": "朵朵：W A S D 选地块、F 掷骰、G 购买 / 建屋、M 抵押。星星：方向键选地块、L 掷骰、K 购买 / 建屋。" }
```

按 `MIN_BODY_PX` 它该是 ≥16px，实际 13px，而且它出现在**双人暂停屏**——PART=C 那一遍走的是四个模式入口，走不到暂停层，所以第 1 轮和本轮的矩阵都没抓到它。本轮为此新加了 PART=L，把「按一次 Esc」之后的暂停屏也纳入巡检。

另外两处是纯 emoji 棋子。上游修复员的走查脚本 `scripts/qa-1.2-window1-sweep.mjs` 里写了「纯 emoji 算装饰、不按正文要求」的过滤（第 201–203 行），但**这条豁免只活在那一份脚本里**，`src/ui/mobileText.ts` 和单测都没写。于是两份 QA 工装对同一个像素给出不同结论，谁也说服不了谁。

**建议**（三件事，都很机械）：

1. `.se-goal` 的下界抬到 16px（它是正文），`.se-token` 按装饰处理或抬到 14px；
2. 把 `src/games/window1-mobile-text.test.ts` 的 `hardCodedSizes` 照 `e4d0c31` 改 `mobileText.test.ts` 的同一个思路收一下 —— 连 `clamp(` / `max(` / `min(` 的**第一个参数**一起读，两把尺子口径对齐；
3. 把「纯 emoji / 纯符号豁免」写进 `src/ui/mobileText.ts`（例如一个 `DECORATIVE_TEXT_RE` 常量）。现在这条豁免只活在 `scripts/qa-1.2-window1-sweep.mjs` 第 201–203 行，共享约定里一个字都没有，于是不同工装对同一个像素给出不同结论。

---

### W1-R2-07 · 一般 · `snake-royale` 第 160 关不操作也可能过关

**怎么发现的**：跑上游那份 `qa-1.2-window1-winlose.mjs` 时，它拿「第 160 关放着不动」当**必输**的对照组，结果弹出「★★★ 第 160 关过关！第 1 名 · 长度 28」。

**独立复现**（PART=M，一根手指都不动，只等结算浮层）：

| 关号 | 目标 | 局数 | 过关次数 | 备注 |
| --- | --- | --- | --- | --- |
| 2 | 长到 40 长度 | 2 | 0 | |
| 50 | 拦下 N 条 | 2 | 0 | |
| 150 | 冲进第 **2** 名 | 2 | 0 | |
| **160** | **冲进第 1 名** | **12** | **1** | 8 局 90s + 4 局 150s（本关限时 125s，90s 会截断，所以又补了一轮足时的） |
| 185 | 冲进第 1 名 | 4 | 0 | 第 8 章同样是 `rank` 目标 |

加上上游那份假人跑出的那次，第 160 关目前是 **13 局里 2 局不操作过关**，量级大约六分之一。低关号和第 185 关一次都没有 —— 所以它既不是「所有 rank 关都这样」，也不是一次性的幻觉，是**低频但真实**。

**成因**（`src/games/snake-royale/levels.ts`）：第 160 关落在第 7 章（`夜色迷雾`，`ci=6`），`GOAL_BY_CHAPTER[6] === "rank"`，而 `targetRank` 在章节后段会塌到 1：

```83:83:src/games/snake-royale/levels.ts
    targetRank: Math.max(1, 4 - Math.floor(ci / 3) - (ramp > 0.7 ? 1 : 0)),
```

`ci=6`、`ramp≈0.87` → `targetRank = max(1, 4-2-1) = 1`。同一关 `bots=8`、`botsFight=true`（`ci>=3` 起 AI 之间互相拦）、`timeSec=125`。玩家的蛇不按键也会一直往前滑，于是只要这 125 秒里 8 条 AI 把彼此撞光，站着不动的玩家就是第 1 名，直接判过关。

**影响**：不崩不卡，也不影响别的关。但「冲进第 1 名」这类目标在 AI 会自相残杀的关里，可以靠运气白拿，孩子会发现「这关放着不管有时候就过了」。另外它让上游那份假人的 assert 随机变红，CI 会莫名其妙飘。

**建议**：`rank` 类目标别只看名次，加一条「自己得真参与」的下限 —— 例如同时要求长度超过开局值、或者要求至少吃到 N 颗豆。判定改一行就够，不动手感、不动关卡生成。

---

## 七、全量水位

### 单元测试

```
npm test
→ Test Files  244 passed (244)
        Tests  6718 passed (6718)
     Duration  64.47s
```

**244 文件 / 6718 用例，全绿**（`25e706a` 这棵树上的读数）。第 1 轮收尾时是 233 / 6445，上游这几批提交带来 +11 文件 / +273 用例，**只增不减**这条守住了。本份测试员没有增删任何测试源码，只增了一份只读走查脚本。

### 类型 + 构建

```
npm run build   （tsc --noEmit && vite build）
→ ✓ built in 492ms
→ PWA v1.3.0 · precache 165 entries (3551.61 KiB)
```

**通过，类型检查 0 错。** 唯一一条输出是 Rollup 的既有提示
`INEFFECTIVE_DYNAMIC_IMPORT: src/ui/parentAuth.ts is dynamically imported by src/ui/gameShell.ts but also statically imported by src/ui/parentGate.ts` ——
1.1 就在的分块提示，不是本窗口引入的，也不影响产物。

### 分支

- 开工起点 `df966f8`；上游中途前进三次，收工时是 **`25e706a`**。第一次（到 `0626897`）用 rebase 跟上，无冲突；第二次（到 `e430a8b`）因为**并行的第一份第 2 轮报告占了同一个文件名**，改用一次普通 merge 跟上，冲突只有报告这一处，**手工合成一份「甲、乙两份并存」的文件，谁的内容都没删**；第三次（到 `25e706a`）merge 无冲突。每次跟上之后都把浏览器取证重跑。
- 远端同名分支上还有第一次 rebase 前推过的旧提交，为了不 `--force`，也用普通 merge 收敛过一次（冲突只在我自己那份脚本上，取新版解决）。
- 本轮只碰两个文件：`docs/qa/1.2-window1-round2-tester.md`（本报告，与并行那份合并共存）、`scripts/smoke-1.2-window1-r2-tester.mjs`（走查脚本）。`git diff --name-only origin/game-1.2-window1...HEAD` 就这两行。
- `package.json` / `package-lock.json` 未改；`puppeteer-core` 用 `npm i --no-save` 装，没进 lockfile。
- 没有 `--force`，没有动 `main`，没有 `gh` 写操作，没有开 PR。

---

## 八、本轮结论

1. **平台五项全部复核通过**：root 门密码 `kangkang`、电话文案 `18438037080`、TTL 1 小时、密码四个落点全搜不到、`closeRoot()` 当场关门、直达 2/50/150 不发星、家长算术门原样、手游端游筛选、2.5D 禁 three.js —— 46 项 46 过。
2. **第 1 轮已修的四项仍绿，没有回归**：W1-01 Esc 双暂停（换第 50 关重测，5/5）、W1-02 拼音搜索（12 串 + 全库 67 款）、首页搜索框 360px 与 16px 字号、root 密码不落盘。
3. **12 款四模式矩阵**：换样本关 2/50/150 后，36 个战役关卡 + 35 个二级模式入口全部进得去、画得出、`doc+0` 不溢出；`destroy` 12 款不泄漏；商标扫描源码与 `dist` 双 0 命中；宽屏 1280×800 对照 **107 项全过**。窄屏 178 项里挂 15 项，全是字号，其中 12 项是平台旧账 W1-R1-08。
4. **第 1 轮的七条静态遗留，上游这一轮关掉了五条**（W1-03 / W1-05 / W1-06 / W1-08 / W1-09），W1-04 半关（统一出口通了、透视复用没做，另两份在别的窗口目录），W1-07 仍在（10 份 playwright vs 23 份 puppeteer-core）。这些都是**在 `e430a8b` 上重新取证**的，不是照抄。
5. **两条留给第 2 轮定 seed 的旧账都结清了**：W1-10 是脚本挑的写死落点被占（AI 缺回合 0/5），W1-11 固定 seed 三次全部 9–10/16 远超阈值、复现不出来。**建议都关掉**，W1-10 顺手把 `step4-b` 的挑点方式改一下。
6. **双人键位换量法之后，7 款拿到扎实证据**（`block-drop` / `merge-2048` / `mine-garden` / `sudoku-petal` 四款分座可辨，`mahjong-bloom` / `flight-chess` / `star-estate` 三款回合制不串台），`combo-clash` 只量到朵朵半边，并揪出一条**严重**问题 W1-R2-05（`weiqi-garden` 两套键位不分人，用「第 N 手」拿到决定性证据，两个方向都串）。`orb-arena` / `snake-royale` 因为「双摄像机 + 每帧都在动」够不着行为取证，如实记为取证上限，附结构侧与代码侧证据。
7. **W1-12 的缺口收窄了一半**：六款**全部能玩到结算**（第 1 轮只有 1 款），「赢一次」的硬证据到 **2/6**（`star-estate` 第 1 把过、`snake-royale` 第 2 把过，后者来自上游修复员留下的专用假人，本轮由我独立复跑）。剩下四款卡在「AI 吃内部状态、现场没有读取口」，报告里给了两条不改玩法手感的补法。
8. **本份新报 3 条**：1 严重（W1-R2-05）+ 2 一般（W1-R2-06 字号 `clamp()` 盲区、W1-R2-07 长蛇第 160 关不动也能过）。没有阻断。编号从 05 起，避开并行那一份已占用并已由第 2 轮修复员关闭的 W1-R2-01~04。
   最该先动的是 **W1-R2-05** —— 它让 `weiqi-garden` 的双人同屏名存实亡，而修法就是给 `keyAction` 加个玩家参数，`mine-garden` 已有现成写法可抄。
   W1-R2-06 在收工前已被上游 `e4d0c31` 修掉三分之一（`flight-chess` 那处 + `mobileText.test.ts` 的尺子），剩 `star-estate` 两处和 `window1-mobile-text.test.ts` 那把还没对齐的尺子。
