# 1.3 窗口 4 · 第 3 轮终验 · 监督修复员报告

> 分支 `game-1.3-window4`。规格模板 `docs/plan-1.3-step29-C-fixer.md`（收窄口径：范围仅本窗 9 款，
> 报告写本文件，backlog 写 `docs/qa/1.3-window4-backlog.md`）。
> 范围仅 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook /
> garden-guard / sprout-defense / monster-crisis。
> 只动绘制层（绘制函数 / CSS / 视觉常量）；不改玩法 / parentAuth / parentGate；禁 three.js；
> 用例只增不减；别人的报告只读不改。commit 口径 `fix(visual-r3): <款> <问题>`。
> 本轮收官声明是**窗口 4 终验**，不是全库 1.3 放行。

## 〇、清零计划（动手前提交）

开工基线：`22f2fc5`（A 档终验计划已交、B 档终验计划已交，两份正文均未产出）。
输入材料以已成文的为准：r2 fixer 遗留清单（`1.3-window4-round2-fixer.md`）、r2 学习优化员规格
（`1.3-window4-round2-learner.md` R2-TOP10）、r2 tester 对账表；A/B 终验正文若在本轮收尾前落地，
逐条跟进后再收工。

清零对象与打法（按优先级）：

1. **R2-TOP10 · sling-birds 结算星级仪式**（r2 唯一书面顺延项）。按「能安全做绘制层就做」拆两半：
   - **画布侧（本轮做）**：胜利判定后本就有 0.8s 演出窗口（`updateFlow` 中 `endT > 0.8` 才
     `finishWin()`，这条时序判定一字不动）。在 `drawQueue` 的既有「胜利全员跳」上升级成
     `endT` 驱动的**逐只波浪跃 + 金色星屑**仪式：省下的每只小鸟按 0.12s 间隔依次腾一个单程
     贝塞尔小弧，弧顶带金色四角星屑（纯 `endT` 函数，不改 `queue` / 物理 / 计分状态）；
     全部动作在 endT≤0.78 内收尾（`finishSent` 后 `simT`/`endT` 冻结，不留半空定格帧）；
     `reduceMotion` 维持现有静止口径。配套用例钉住修后状态。
   - **超绘制层的剩余部分（进 backlog）**：B 档全量规格里「DOM 面板亮出前再延 0.9s + 剩余小鸟
     飞出屏幕上缘 + 金色 +1000 飘字」需要推迟 `api.onWin` 的结算流程编排；「DOM 三星 0.3s 逐颗
     点亮」落在共享外壳 `src/ui/dialogs.ts`（全库 39+ 款共用，且外壳已有 0.18s 逐颗弹星 + 逐颗
     音效）。两者都出本窗 9 款绘制层边界，登记 backlog 交 1.4。
2. **W4R1-06 · DOM 次要文案字号 9–13px（约 18 处）**：两轮 tester 均明示不升级（1.2 存量 DOM
   取舍、无溢出无遮挡），A 档终验目前无升级动作 → 按派发口径登记 backlog，不动代码。
3. **新建 `docs/qa/1.3-window4-backlog.md`**：每条带「来源轮次 / 位置 / 期望 / 估算改动面」。
4. **A/B 终验正文跟进**：收尾前 fetch，若 A 档终验总表 / B 档终账点名新问题，按
   阻断/严重 → 一般 → 建议顺序逐条清；未落地则在报告写明输入状态。
5. **合并态复验**：fetch → rebase（保留别人文件）→ `npm ci` → `npm test`（用例只增不减，
   对照统筹水位 710 文件 / 15280 例）→ `npm run build`（体积对照 5246.26 KiB）→
   `npx vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 三项 200）。
6. **收官声明**：终态 SHA + 窗口 4 终验结论 + backlog 条数。普通 push `origin game-1.3-window4`；
   禁止 force、不改 main、不推 game-1.2 / game-1.2-kk、不开 PR。

（以下各节随执行填写。）
