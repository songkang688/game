# 1.3 窗口 4 · 第 3 轮终验 · 监督修复员报告

> 分支 `game-1.3-window4`。规格模板 `docs/plan-1.3-step29-C-fixer.md`（收窄口径：范围仅本窗 9 款，
> 报告写本文件，backlog 写 `docs/qa/1.3-window4-backlog.md`）。
> 范围仅 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook /
> garden-guard / sprout-defense / monster-crisis。
> 只动绘制层（绘制函数 / CSS / 视觉常量）；不改玩法 / parentAuth / parentGate；禁 three.js；
> 用例只增不减；别人的报告只读不改。commit 口径 `fix(visual-r3): <款> <问题>`。
> 本轮收官声明是**窗口 4 终验**，不是全库 1.3 放行。

## 〇、清零计划（动手前提交）

开工基线：`22f2fc5`（A 档终验计划已交、B 档终验计划已交，两份正文均未产出）。
输入材料以已成文的为准：r2 fixer 遗留清单（`1.3-window4-round2-fixer.md`）、r2 学习优化员规格
（`1.3-window4-round2-learner.md` R2-TOP10）、r2 tester 对账表；A/B 终验正文若在本轮收尾前落地，
逐条跟进后再收工。

清零对象与打法（按优先级）：

1. **R2-TOP10 · sling-birds 结算星级仪式**（r2 唯一书面顺延项）。按「能安全做绘制层就做」拆两半：
   - **画布侧（本轮做）**：胜利判定后本就有 0.8s 演出窗口（`updateFlow` 中 `endT > 0.8` 才
     `finishWin()`，这条时序判定一字不动）。在 `drawQueue` 的既有「胜利全员跳」上升级成
     `endT` 驱动的**逐只波浪跃 + 金色星屑**仪式：省下的每只小鸟按 0.12s 间隔依次腾一个单程
     贝塞尔小弧，弧顶带金色四角星屑（纯 `endT` 函数，不改 `queue` / 物理 / 计分状态）；
     全部动作在 endT≤0.78 内收尾（`finishSent` 后 `simT`/`endT` 冻结，不留半空定格帧）；
     `reduceMotion` 维持现有静止口径。配套用例钉住修后状态。
   - **超绘制层的剩余部分（进 backlog）**：B 档全量规格里「DOM 面板亮出前再延 0.9s + 剩余小鸟
     飞出屏幕上缘 + 金色 +1000 飘字」需要推迟 `api.onWin` 的结算流程编排；「DOM 三星 0.3s 逐颗
     点亮」落在共享外壳 `src/ui/dialogs.ts`（全库 39+ 款共用，且外壳已有 0.18s 逐颗弹星 + 逐颗
     音效）。两者都出本窗 9 款绘制层边界，登记 backlog 交 1.4。
2. **W4R1-06 · DOM 次要文案字号 9–13px（约 18 处）**：两轮 tester 均明示不升级（1.2 存量 DOM
   取舍、无溢出无遮挡），A 档终验目前无升级动作 → 按派发口径登记 backlog，不动代码。
3. **新建 `docs/qa/1.3-window4-backlog.md`**：每条带「来源轮次 / 位置 / 期望 / 估算改动面」。
4. **A/B 终验正文跟进**：收尾前 fetch，若 A 档终验总表 / B 档终账点名新问题，按
   阻断/严重 → 一般 → 建议顺序逐条清；未落地则在报告写明输入状态。
5. **合并态复验**：fetch → rebase（保留别人文件）→ `npm ci` → `npm test`（用例只增不减，
   对照统筹水位 710 文件 / 15280 例）→ `npm run build`（体积对照 5246.26 KiB）→
   `npx vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 三项 200）。
6. **收官声明**：终态 SHA + 窗口 4 终验结论 + backlog 条数。普通 push `origin game-1.3-window4`；
   禁止 force、不改 main、不推 game-1.2 / game-1.2-kk、不开 PR。

## 一、清零清单

| # | 来源 | 位置 | 修法（只动绘制层） | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | r2 唯一书面顺延项 R2-TOP10（B 档 r2 第四节规格 / r2 fixer 第三节顺延记录） | sling-birds `index.ts` `drawQueue` + `art.ts` | **绘制层子集落地**：胜利后省下的排队小鸟按 B 档 0.25s 间隔口径逐只腾一个小弧（`winLeapPhase` 纯函数，`endT` 驱动），弧上绕三粒金色四角星屑（`drawWinSparkle`，#FFD86B 与胜利星屑撒场同族 + 白芯）；腾跃中表情切 `fly`、翅膀加速扑动。硬约束全部满足：`endT > 0.8` 触发 `finishWin` 的时序判定一字不动，最后一跳 0.69s 落地（`finishSent` 后 `simT`/`endT` 冻结，不留半空定格帧）；`queue` / 物理 / 计分零改动；`reduceMotion` 维持既有静止口径 | art.test +4（节奏窗口 0.8s 内落地、错峰相位、金星屑资产零字符 + save/restore 配平 + alpha≤0 不画、drawQueue 接线源码钉住） | `d7b90ea` |
| 2 | 同上，R2-TOP10 超绘制层的剩余部分 | `index.ts` 结算流程编排 + 共享外壳 `src/ui/dialogs.ts` | **登记 backlog**（BL-W4-02）：0.9s 面板前置演出要推迟 `api.onWin`（流程编排非画笔）；「DOM 三星 0.3s 逐颗点亮」落在全库 39+ 款共用的 `showResultDialog`（且外壳已有 0.15s 起步、0.18s/颗逐颗弹星 + 逐颗音效，DOM 半边已部分覆盖）——两者都出本窗 9 款绘制层边界 | — | `b89e889` |
| 3 | r1 遗留 W4R1-06（DOM 次要文案字号 9–13px 约 18 处） | 9 款 `index.ts` 内嵌 CSS | **登记 backlog**（BL-W4-01）：r2 tester 复核明示「合理遗留、不升级」，r3 终验 A 档无升级动作，按派发口径（「除非 A 档升级否则 backlog」）不动代码 | — | `b89e889` |

除上述两笔既知遗留外，r2 fixer 报告的 16 笔修复无一被点名回归；A 档 r1 扫描 31 例、r2 扫描 9 例
在终态全部照跑全绿（`window4-visual-scan.test.ts` + `window4-visual-scan-r2.test.ts` 共 40 例）。

## 二、降级申请记录

无。阻断/严重级在 r2 已清零（W4R1-02），本轮无新增阻断/严重；两笔一般/建议级遗留均按
派发收窄口径处置（子集修复 + backlog / 直接 backlog），不需要 A 档裁决的书面降级。

## 三、backlog 条目统计

`docs/qa/1.3-window4-backlog.md`（本轮新建，commit `b89e889`）：共 **2 条**，
均为一般/建议级，每条带「来源轮次 / 位置 / 期望 / 估算改动面」：

- **BL-W4-01** DOM 次要文案字号 9–13px（约 18 处，待 1.4 划全库最小字号线后统一提）；
- **BL-W4-02** sling-birds 结算仪式全量方案剩余部分（结算流程编排 + 共享外壳
  `dialogs.ts`，建议 1.4 作「结算面板演出统一升级」专项）。

## 四、终态合并数据

- 收尾 `git fetch origin game-1.3-window4`：远端无他人新提交（A/B 终验正文截至收尾未产出），
  无需 rebase；三份 r1、三份 r2 报告与两份 r3 计划、`docs/plan-1.3-*.md` 全部保留，
  他人报告一字未改。
- `npm ci`：绿。
- `npm test`：**710 文件 / 15284 例全绿**。用例曲线只增不减：
  r1 合并态 709/15247 → r2 合并态 710/15280 → **本轮 710/15284**（+4，全部为本轮修复配套）。
- `npm run build`：绿（tsc --noEmit + vite；**PWA precache 188 entries / 5246.79 KiB**，
  较 r2 水位 5246.26 KiB **+0.53 KiB**，为仪式绘制代码的正常增量；对比 1.2 基线远低于 15% 红线）。
- `npx vite preview :4173` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200** ✅。
- 代码终态 SHA：**`d7b90ea`**（backlog 建档 `b89e889` 紧随其后，本报告 commit 收尾）。

## 五、红线自查

- 只动绘制层：本轮代码 diff 仅 `src/games/sling-birds/`（art.ts / index.ts / art.test.ts）；
  `endT > 0.8`、`calcStars`、`queue`、物理、存档 key 零改动；
- 未触碰 parentAuth / parentGate；无 three.js；用例只增不减、无 skip；
- 未 force、未改 main、未推 game-1.2 / game-1.2-kk、未开 PR、他人报告只读未改。

## 六、窗口 4 · 第 3 轮终验 · 修复侧收官声明

**1.3 窗口 4 终验（修复侧）收官**——本声明限窗口 4 的 9 款，**不是全库 1.3 放行**：

- 终态 SHA：**`d7b90ea`**（代码）/ `b89e889`（backlog 建档）；
- 阻断/严重级终态 **0**（W4R1-02 已在 r2 清零，本轮无新增）；r2 唯一书面顺延项
  R2-TOP10 已按「能安全做绘制层就做」拆分清零：画布侧仪式落地、超界部分建档 backlog；
  r1 遗留 W4R1-06 按派发口径入 backlog。修复侧问题账面**清零**；
- 遗留 backlog：**2 条**（均一般/建议级，已成文交 1.4）；
- 合并态 `npm test` 710 文件 / 15284 例全绿（三轮曲线 15247 → 15280 → 15284 只增不减）、
  `npm run build` 全绿（5246.79 KiB）、`vite preview` 冒烟三项 200；
- **待办移交**：A 档终验总表（9 行 × 6 专项）与「放行 / 带条件放行」结论、B 档终账
  截至本报告收尾未产出；两份正文落地后若点名新问题，按其严重度口径继续清（修复侧
  随叫随修）；窗口 4 的最终放行结论以 A 档终验裁决为准。

