# 三人组第 11 轮 · 测试修复员 A/B 任务清单（playbook）

> 依据：`trio-r11-learn-notes.md`（基线 `game-1.3 = 0a8c0344`）。
> **先读对账表**：r9 A/B 已合入主干的项全部 ✅，**禁止重做**（N-33/34/35/36/37/38/30/25/31关内/1/32/26/27/29+N-23、收藏册、L-2 faceLift）。
> **编号红线**：N-40…N-42 = r9 补测；N-43…N-45 = r10；N-46/N-47 = r10 并行补测（sky-squad 六键 / 模式芯片）。**本轮新伤 N-52…N-57。**
> 只列仍未落地的 🔧。A = 壳层+闯关学习，B = 休闲对战动手。
> 配方 A–C r4、D–F r5、G/H r6、I/J r8、K/L r9；本轮只补 L/H/I 笔。
> B 在途 `casual-duo-fit-r5-b-4683` 仍未进主干：rebase 到 `0a8c0344`，`[hidden]` 与守门测试取先合版，全库只留 `src/games/modebarHidden.guard.test.ts`。

## 通用纪律

- 不改存档 key：`yiduo-yixing.l99.<id>` / skip / `yiduo-yixing.root.v1` / 钱包 / `meta.id`。
- 不动题库/判定/seed。kit 已有文件只 import（L-3 扩 `stickers.ts` 例外）。
- 测试只增不减。开/关对照每档独立 context（或先 `localStorage.clear()`，必须在 `#/game/` 同源页上，不要在 `about:blank` 写 storage）。
- 宽屏 412×915 / 1280×800 零回归。禁 force。

## 测试步骤备忘

- `npm run build && npx vite preview --port 4173`；puppeteer-core + `/usr/local/bin/google-chrome`。
- 主档 **915×412**。duo-arena：先量菜单「开擂」，再点开擂量下半场。gold-hook 商店：闯关进关 → 🛒（模式选单没有商店）。alien-seek 推理关：进度 `Array(121).fill(1)`。fk 训练场：先量选人壳「开打」，再进关内（关内已绿）。

---

## 壳层（给 A）

### N-39 l99 蓝本首次进图/回地图不聚焦 🔧（r9 仍开，hop-pads 本轮 426–502 复证）

- 照 r9 playbook：`showMap(true)` 四处——初次 `:1100`、过关回地图 `:993`、失败回地图 `:1024`、关内选关 `:1053`。**切章 `:866` 保持 `showMap()`。**
- 验收：915×412 hop-pads 不滚可见当前关；过关/失败回地图同样；1024×768 零回归。

### S-4 扩容 `.qz-jump-input` 38→44 🔧（r9-A 明确没动；N-37 合入后仍露馅）

- `quiz99.ts:153` `min-height:38px` → 44。本轮 root×拼音 135 还量到直达钮高 **32**——同 PR 抬到 44。管理员面。

### N-33 / N-38 / 收藏册 / N-37

- 已合入。N-37 本轮 root×135 票在屏，**只复测不要重写** `:has(.l99-jump)`。

---

## 闯关学习（给 A）

### N-43 color-fun 全关型色盘线下 🔧（r10，配方 G/J）

- 照 `trio-r10-playbook.md`。七关型 × 915×412；limited 最重。

### N-44 math-farm 竖式答案钮线下 🔧（r10）

- 扩紧凑选择器或钳竖式插图高。第 1 关无线上插图须保持绿。

### N-16 走廊引擎三态 🔧（A r9 留下）

- 无尽遗迹 / 计时速通 / 闯关：`ak-pad` 勿挂 `advk-shell`（那是古堡 N-30）。配方 G，一次修三态。

### L-3 find-diff 贴纸 4–10 章 🔧

- 只扩 `stickers.ts` + `boardArt` 映射。题库 SHA 不动。L-2 已用 faceLift 销账，**不要改 clockSVG**。

### C-6 补笔（并进仍开账）

- alien-seek **推理关 121**：工具+D-pad 全线下、crop 608。修 C-6 时验收必须含 `isDeduceLevel`，不能只拿找物关结案。

---

## 休闲对战动手（给 B）

### N-52 duo-arena 915 菜单开擂 + 对局下半场 🔧（新，配方 L + C-8）

- **菜单**：开擂 top 527、怎么玩 471，crop 199。
- **对局**：上半场垫在屏；下半场整套 419+ 线下；暂停/规则/退出 549 线下。
- **改哪**：`src/games/duo-arena/` 布局 CSS。矮横屏：两套垫左右或上下各钳半屏；暂停 sticky 底。`match.ts` / 胜负零触碰。
- **验收**：915 不滚能点开擂；开打后两套垫+暂停可点；390 / 1280 零回归。不要为了下半场把上半场挤出屏。

### N-53 tank-battle 双人对战双垫线下 + 32px 热区 🔧（新，配方 G）

- 两套 D-pad/开火 607+ 全线下；画布出屏 113；暂停 32、回选关 33。
- **改哪**：对战壳矮横屏双栏或钳画布高把垫留在 412 内；`min-height:44` 暂停/回选关/芯片。关卡表/弹道零触碰。
- **验收**：915 对战不滚能开炮；合作/闯关零回归。

### N-54 hop-pads 双人同屏画布出屏 208 🔧（新；单人 r9 已绿勿回退）

- crop 261、折叠钮 0。按块钳高：`(余量 − 工具)/2`。
- **验收**：两块画布底 ≤412；单人 915 仍 crop 0。

### N-55 snow-fight 双人对战十二键线下 🔧（新，配方 G）

- 十二键 481–531 全线下。矮横屏两块操作牌并排（该款窄屏已有 3×2 牌，横屏没走这条）。
- 回合判定 / 灯笼逻辑零触碰。

### N-56 sky-squad 双人合作热区 🔧

- 暂停 33、判定点/手指上方开关 31、摇杆 36。折叠 0，**只抬热区**，勿重钳画布。

### N-57 fight-king 训练场选人「开打 ▶」线下 🔧（N-31 关内已绿）

- 开打 top 531、假人钮 38px。sticky 底或并排。**不要改** `.fk-train-shell` 关内已绿的键排。
- 验收：915 不滚能点开打；点进去后八键仍在屏（N-31 回归）。

### 仍开、原文不重抄

- **N-45** gold-hook 商店：本轮复证 接着挖 513 / buy 416 / 内滚 230。照 r10。
- **N-40** duo-rush `.dr-btns`；**N-41** 麻将 34；**N-42** puff 暂停 34。
- **N-10 / N-13 / N-14**（bumper 本轮 226/56/暂停 31 同 r9）。
- **N-15** bomb-buddies 915 对战六键（r10）。
- **N-17** prince 六键（教学=Boss 同刀）。
- **N-2/3/4**、**C-2…C-8**、C-6 含推理关。

---

## 完成定义

1. 新伤 N-52…N-57 或书面降级；r10 的 N-43…N-45 与 r9 的 N-39/40…42 仍开则继续或说明未做。
2. `npm test` / `npm run build` 水位只增不减；一份 modebar 守门。
3. 每条 915×412 留数字；duo-arena 必须菜单+对局两档；fk 训练必须选人壳+关内两档。
4. 撞车取先合版。
