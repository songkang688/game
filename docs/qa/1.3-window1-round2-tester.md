# 1.3 窗口 1 · 本窗三轮验收 · 第 2 轮 · 测试员报告

> 角色：窗口 1 第 2 轮视觉测试员（A 档）。只写本报告与窗口 1 范围 `*.test.ts` 新增视觉用例，不改绘制实现。
> 范围：基建 `src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` + 九款
> `orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate` `hero-cards` `weiqi-garden` `flight-chess`。
> 依据：`docs/plan-1.3-visual-bible.md`（视觉宪法）、`docs/qa/1.3-window1-plan.md`（十二格登记）、
> 第 1 轮三份报告（tester / learner / fixer，只读）。
> 本轮只验视觉，不重开玩法验收；重点是**第 1 轮 fixer 12 项落地的逐条回归**。

## 0. 扫描计划（开工承诺）

1. **全量跑与盘点**：`npm ci` → `npx vitest run`（全仓）→ `npx tsc --noEmit` → `npm run build` → preview 冒烟；
   对照第 1 轮 fixer 交卷水位 **724 文件 / 15558 例**（代码 SHA `3bc4df1`），本轮只增不减；
   对照 `docs/qa/1.3-window1-plan.md` 十二格 SHA 与 kit/motion/runner 独占约定 `git log` 抽查。
2. **第 1 轮修复逐条回归（本轮重点）**：P1–P10 + 自查 clamp 共 12 项，逐条给证据
   （代码检视 + 能跑的用例 + 画面描述），不抄 fixer 报告；
   G-2 / G-3 / G-4 三项豁免只复核裁决依据是否仍成立，不当新阻断；
   L-1–L-5 遗留登记现状（L-1 共享壳、L-4 他窗不碰）。
3. **回归猎手**：fixer 改过的绘制层查新溢出 / 新裸 emoji 主角 / 新商标构图 / kit 越权 / reduced-motion 破坏；
   360/320 对本窗九款再抽一遍。
4. **六大专项**（收缩到本窗 12 格）：火柴人/草稿圆、金币体积、多角色可区分、纵深/体积、360px 布局、商标扫描。
5. **横向一致性**：kit 光照约定、reduced-motion、destroy 泄漏 ×5、粒子节流观感。
6. **产出**：本报告 + 机器化扫描用例 `src/games/window1-visual-r2.test.ts`（只增不减）。

---

## 一、全量跑数据与十二格盘点

### 1.1 全量跑（进场基线，代码 = `6d9b8ef` 同源的合并态）

| 项 | 结果 | 备注 |
| --- | --- | --- |
| `npm ci` | ✅ | 依赖装净 |
| `npx vitest run`（全仓，进场） | ✅ **724 文件 / 15558 例全绿** | 与 fixer 交卷水位 724/15558 逐字持平，无人动过用例 |
| 本轮交卷复跑 | ✅ **725 文件 / 15575 例全绿**（见第七节收尾登记） | +1 文件（`window1-visual-r2.test.ts`）/ +17 例，**只增不减** |
| `npx tsc --noEmit` | ✅ 0 错误 | |
| `npm run build`（tsc + vite build + PWA） | ✅ | 主包 `dist/assets/index-DyjPnIMC.js` 110.70 kB（gzip 38.66 kB），precache **188 项 / 5291.01 KiB**，与 fixer 登记持平 |
| `npx vite preview` 冒烟 | ✅ | `GET /` 200；九款 `#/game/<id>` 360px 视口 Chrome headless（CDP）逐款实开进对局，主场景（canvas / SVG）全部渲染，**18 组 console error 全 0** |

### 1.2 十二格 plan ↔ 提交对账

`docs/qa/1.3-window1-plan.md` 登记的 12 格 SHA（`9c345d4` `dbb8bd4` `6dea9e6` `cb2c65a` `310dd1d`
`bb5e090` `58a2055` `3f03658` `a5b446c` `7a07ddb` `0b1dc6b` `8575add`）与三轮验收 SHA
（`040264a` `f278f62` `662e027` `3bc4df1` `d1ec97d` `6d9b8ef`）逐一 `git log` 验真，**全部存在**。✅

### 1.3 kit / motion / runner 独占纪律

`git log origin/game-1.3..HEAD -- src/art/kit/ src/ui/motion.ts src/art/runner/`：仍然只有三条
owner 创建提交（`9c345d4` / `dbb8bd4` / `6dea9e6`），`--diff-filter=M` **0 条**——自基线以来无任何人
修改过基建文件；fixer 的 r1 修复对 kit 仅 import。✅
另核 fixer 全部代码 diff（`f278f62..3bc4df1`，24 文件 +1239/-20）：改动面全部落在九款 `art.ts` /
`index.ts`（CSS 与装饰装配）与测试文件，art 层删除行仅 4 类（注释改写、土丘单笔平涂被三阶替换、
`DECOR_MIN_SIZE` 2.4→3.2、`paintCornerVine` 补 export），**收集物 / 角色绘制函数零触碰**（对
`paintStarCandy` / `drawStarBean` / `drawGemDrop` / `drawJellyOrb` / `drawSentStar` /
`drawProjectileOrb` / `stoneSprite` 等逐名 grep diff，0 命中）。

---

## 二、第 1 轮修复回归表（本轮重点，逐条独立取证）

证据口径：**代码检视**（本轮重读现源码，标注文件+函数/行）+ **能跑的用例**（既有契约复跑全绿 +
本轮新增 `window1-visual-r2.test.ts` 17 例钉子）+ **画面描述**（Chrome headless 360×640 实开实截）。

| 项 | 期望 | 回归证据 | 判定 |
| --- | --- | --- | --- |
| P1 = G-6 star-estate 地格矢量化 | 33 款 SVG，emoji 仅 aria-label，无「? 徽章」 | 代码：`art.ts:409-637` `TILE_ICONS` 33 键，与 `board.ts` 全部 `emoji:` 字段**双向核对无缺无余**（脚本逐键比对）；每款 20×20 viewBox、双色阶（`shade/tint` 派生）；机会=摩天轮 🎡→五彩轮辐、命运=爱心信封 💌→信封+心，`TILE_ICONS` 图形体 `<text>` 节点 0、无 `?` 字符。`index.ts:706` 地格插 `tileIconSVG(tile.emoji)`，`:720-725` emoji 只进 `aria-label`，全文件 `se-tile-emoji` 0 命中。画面：360px 实截棋盘 40 格全部为矢量小图标（郁金香/列车/卷浪/遮阳伞/书堆/满月等清晰可辨），无系统 emoji 字形 | ✅ 新增钉子 3 例 |
| P2 orb-arena 贴片层 | 中景具象三式、视差 0.3、与圆斑不同层 | 代码：`art.ts:148-150` `DECOR_CELL=176` / `DECOR_PARALLAX=0.3`；`decorAt`（`:173`）确定性哈希 40% 格出件、alpha 0.35–0.5 封顶；`paintArenaDecor`（`:186`）五瓣花/四芒星/糖果石子三式纯平涂；`drawArenaBackground` 内 `drawArenaDecorLayer` 调用位于第 2 步圆斑循环之后、第 3 步 6% 网格之前（`:319`「2.5)」独立层），与圆斑不同绘制层、同 0.3 系数不添第三档速度；离屏 sprite 缓存 + 240 件预算。画面：360px 实截糖豆平原可见淡粉具象小件散布，低于主体一阶 | ✅ 新增钉子 2 例 |
| P3 flight-chess 装饰 | 草簇×4 + 云×2，避格、aria-hidden | 代码：`index.ts:344-364` `decorArt()` `grassSpots` 恰 4 点（5.1/9.9 组合 = 机库内角，`COLOR_INFO[c].soft` 各随本座）+ `cloudSVG` ×2（6.45/8.55 塔台垫对角，远小近大 0.045/0.06）；`.fc-decor` `position:absolute;inset:0;pointer-events:none`（`:147`），挂载处 `aria-hidden="true"`（`:531`）；函数体零动画、零时间源。画面：360px 实截四象限四座机库内角各一簇同座色草地、塔台两侧两朵云，均不压格子与航线弧 | ✅ 新增钉子 1 例 |
| P4 = G-5 触区 44px | `.oa-open` / `.sr-open` / `.bd-open` / `.sr-skin` `min-height:44px` | 代码：四处声明逐一在位（`orb-arena/index.ts:108`、`snake-royale/index.ts:123` 与 `:136`、`block-drop/index.ts:142`）。**运行时实测**（CDP `getBoundingClientRect`，360px 进对局后）：`.oa-open` 119×44 ×3、`.sr-open` 119×44 ×3、`.sr-skin` 64–117×44 ×10、`.bd-open` 119–148×44 ×3，全部恰 44px | ✅ 新增钉子 1 例 |
| P5 combo-clash 土丘 | 顶亮弧 + 底暗带 | 代码：`art.ts:296-315` `nearMounds` 三笔齐全——base `rgba(th.near,0.9)` 半圆、底缘 `shade(th.near,0.2)` 4px 暗带、顶部 `tint(th.near,0.3)` 亮弧（半径 28/40=0.7 倍、弧段 π→1.72π 偏左上光源）。画面：360px 实截擂台围绳后粉色土丘带可见明暗阶 | ✅ 新增钉子 1 例 |
| P6 snake-royale 色岛 | 远景色岛一层、视差恰两档、贴片下限 3.2 | 代码：`art.ts:118-124` `DECOR_PARALLAX=0.85` / `ISLAND_CELL=900` / `ISLAND_PARALLAX=0.6` / `DECOR_MIN_SIZE=3.2`；全文件 `PARALLAX = 0.x` 声明恰 2 个（无第三档）；色岛实现（`:166-199`）每 900 格恰一枚、格心 ±0.35 抖动、直径 300–500 世界 px、`theme.decoB`、alpha `0.08 + (hsh>>>3)%5/100` = 0.08–0.12、单位圆+scale 兼容画法、48 枚预算；贴片 `Math.max(DECOR_MIN_SIZE, 5*zoom)`（`:223`）。画面：360px 实截绿野贴片小花/星散布正常 | ✅ 新增钉子 2 例 |
| P8 mahjong 织纹 | 毛毡 ≤4% 织纹 | 代码：`mahjong-bloom/index.ts:102` `.mj-board` 背景第一层 `repeating-linear-gradient(45deg,rgba(255,255,255,.03) 0 6px,transparent 6px 12px)`，叠于毛毡 radial 之上，**alpha 3% ≤ 4% 红线**。注：fixer 报告写的选择器名是 `.mj-table`，实际落点为 `.mj-board`（该款毛毡容器真名），效果与规格一致，登记备查不算缺陷 | ✅ 新增钉子 1 例 |
| P8 hero-cards 纸感 | 卡面米白纸感，不再平涂 `#fff` | 代码：`hero-cards/index.ts:136` `.hc-card{…background:linear-gradient(180deg,#fffdf8,#f6efe2)…}`，规则内无 `background:#fff` 平涂（其余 `#fff` 命中为徽章/席位卡等 1.2 既有面板底，非本项范围） | ✅ 新增钉子 1 例 |
| P9 block-drop 浮雕 | 井壁四主题浮雕，每 6 格一件，不进玩法区 | 代码：`art.ts:251` `RELIEF_H=12`；`paintWallRelief`（`:258-325`）四主题四款（wood=小花窗/ice=冰晶/dusk=果酱滴/night=星窗），双色阶只从 `wallLight` 派生（shade 0.32 底 + tint 0.5 亮点），件宽 ≤5.2px 收在 6px 壁内；调用点 `:397-400` `reliefStep=max(60,cell*6)` 纵向每 6 格、恰在左右壁中线 `ws/2` 与 `w-ws/2`，纵向止于 `innerH-cell*0.6`；走 `drawWellBackground` 离屏缓存。画面：360px 实截木箱主题井壁通高可见（浮雕件 6px 量级、细而不抢） | ✅ 新增钉子 1 例 |
| P10 = G-7 weiqi-garden 测试组织 | 独立 `art.test.ts` 在位，旧 describe 未迁走 | 文件在位：`art.test.ts` 4 个 describe / 11 例（花藤预算 / sprite 出血边 / washAlpha 边界 / 进度与配色）；`index.test.ts` 既有 5 个「1.3 视觉契约」describe（`:538/:592/:658/:727/:756`）原地未动 | ✅ 新增钉子 1 例 |
| 自查 clamp | `.se-tile-icon` 满足 360px 字号红线 | 代码：`star-estate/index.ts:138` `.se-tile-icon{font-size:clamp(15px,2.6vw,17px);width:1em;…}`，clamp 下限 15px ≥ 13px；`levels.test.ts` 360px 字号红线用例全绿 | ✅ 新增钉子 1 例 |

**豁免复核（G-2 / G-3 / G-4，只核依据、不当新阻断）**：

| 编号 | fixer 裁决 | 本轮复核 | 结论 |
| --- | --- | --- | --- |
| G-2 `mj-tile` 30px（≤480px） | 豁免 | `mj-cur` 选中态二段确认仍在（`index.ts:152/287`），`mj-hand` `overflow-x:auto` 兜底仍在（`:125`），30px 声明仍是 1.2 逐字遗留 | 依据成立，维持豁免 |
| G-3 `se-tile` 23×23 | 豁免 | 运行时实测 360px 仍 23×23；`.se-tile:focus-visible` 3px outline 在位（`:202`），`se-deed` 信息行可达同一信息（`:209`），走子仍由骰驱动 | 依据成立，维持豁免 |
| G-4 `fc-token` 19×19 | 豁免 | 运行时实测仍 19×19；`.fc-pick{min-width:66px;min-height:44px}` 主入口在位（`:211`） | 依据成立，维持豁免 |

**遗留 L-1–L-5 现状登记**：

| 项 | 现状（本轮实测） | 处置 |
| --- | --- | --- |
| L-1 共享壳 `level99.ts` ★ 11px | 仍在：`level99.ts:512` 11px、`:561` @媒体查询降 10px；本轮 18 组实测唯一 <14px 文本命中即 `span.l99-star` 10–12px，九款关卡页同源受累 | 未变成回归缺陷（1.2 逐字遗留）；仍属共享壳 owner / 统筹轮，本窗不抢。严重度维持「一般」 |
| L-2 `.hc-table` 织纹 | 仍未叠织纹（`hero-cards/index.ts:120` 无 repeating-linear-gradient）；主项卡面纸感已落地 | 观察项不升级；可随后续轮顺手做（与 3B 同规格 ≤4%） |
| L-3 kit `decor.ts` 供给缺口 | `src/art/kit/` 仍无 `decor.ts`；本窗九款需求已按「各游戏内自绘」满足（P2/P3/P6/P9 全部落地且不复制 kit） | 维持登记 kit owner 待办，本窗无受害面 |
| L-4 `bomb-buddies` `P_EMOJI` | 仍在（`index.ts:102` 座位标 🌸/⭐ 字符）| 他窗游戏，本窗零触碰，维持转交 |
| L-5 文案层 emoji | 仍在：combo-clash 选人卡 `<em>${c.emoji}</em>`（`index.ts:997`）、star-estate 地契列表行首 `${sel.emoji}`（`index.ts:730`）等 | 按 r1 tester 口径「文案层可留」，非六大专项失分面，维持观察 |

**回归猎手结论**：fixer 改动面 24 文件逐一审过——新增溢出 0（18 组实测零溢出）、新增裸 emoji 主角 0
（新增绘制段 emoji 码位扫描 0 命中，`se-tile-emoji` 清零保持）、新商标构图 0（TILE_ICONS 无 `<text>`
无 `?` 徽章、grassSVG/浮雕/贴片均抽象几何）、kit 越权 0（`--diff-filter=M` 空）、reduced-motion 破坏 0
（新增装饰全静态：装饰函数体内 `Date.now`/`performance.now`/`requestAnimationFrame`/`@keyframes` 0 命中，
且 reduce 模式实测 4 款无限循环动画 0 个）。**回归缺陷：0 条。**

---

## 三、专项 ①：火柴人 / 草稿圆扫描

**方法**：九款 + kit/runner/motion 全部非测试源码脚本扫描——逐行核 `fillText(` 行是否携带 emoji
字面量；`\p{Extended_Pictographic}` 码位分层计数；重点重读 fixer 新增绘制段（TILE_ICONS / grassSVG /
paintWallRelief / paintArenaDecor / nearMounds 三阶 / 色岛）查「纯圆+两点眼」与无渐变纯圆。

**结果表**：

| 对象 | canvas `fillText` 行数 | 其中 emoji 直出 | 非测试源码 emoji 字符数 | 判定 |
| --- | --- | --- | --- | --- |
| orb-arena | 3 | 0 | 41 | ✅ |
| snake-royale | 4 | 0 | 38 | ✅ |
| block-drop | 1 | 0 | 47 | ✅ |
| combo-clash | 4 | 0 | 45 | ✅ |
| mahjong-bloom | 0 | 0 | 57 | ✅ |
| star-estate | 0 | 0 | 166 | ✅（见下注） |
| hero-cards | 0 | 0 | 121 | ✅ |
| weiqi-garden | 1 | 0 | 41 | ✅ |
| flight-chess | 0 | 0 | 49 | ✅ |
| kit | 2 | 0 | 0 | ✅ |
| runner / motion | 0 | 0 | 0 | ✅ |

注：emoji 字符计数为本轮脚本口径（含 aria-label 字符串、TILE_ICONS 映射表键、guide 文案），与 r1
tester 的码位口径存在方法差，不作同比。**关键面**：star-estate 的 166 个中不再有任何主角级渲染——
地格按钮 DOM 实截确认全部为 SVG，emoji 只存在于映射表键与 aria-label / 地契列表文本（L-5 文案层）；
r1 修复未回退。fixer 新增装饰无一处纯圆无渐变主角（贴片糖石子为装饰件且有两阶高光，非角色）。

**问题清单**：严重 0、阻断 0。装饰性 emoji 数量已登记如上。

---

## 四、专项 ②：金币 / 收集物体积

**方法**：r1 已 72 格全查；本轮口径 = 「r1 结论 + 证明其检视对象未被改动」——对 fixer diff 逐名 grep
收集物绘制函数（0 触碰，见 1.3 节），再对每款主收集物重读现源码抽验层次。

| 款 | 收集物（本轮重读） | 层次证据 | 判定 |
| --- | --- | --- | --- |
| orb-arena | 星光糖 `paintStarCandy` | 2 停径向渐变 + 白描边 + 高光点、离屏 sprite；果冻球径向渐变心 `(-0.32r,-0.36r)` | ✅ |
| snake-royale | 星光豆 / 糖果宝石 | 光晕 + 渐变 + 描边 + 高光；宝石复用 kit `drawGem` | ✅ |
| block-drop | 发送星 `drawSentStar` | 尾迹双点 + sparkle 分层 + alpha 渐隐 | ✅ |
| combo-clash | 投射球 `drawProjectileOrb` | 尾迹 + 光晕 + 主体 + 描边 + 高光 + 四芒星 | ✅ |
| star-estate | `coinSVG` / `coinTagSVG` | 3 停径向渐变 + 侧沿厚度 + 内圈亮环 + 星形压印 + 高光斑（`art.ts:203-233`，本轮逐行重读） | ✅ |
| hero-cards | 体力心 `heartsSVG` | 实心 + 深描边 + 高光点 | ✅ |
| mahjong / weiqi / flight-chess | 开花节点 / 提子花瓣 / 塔顶星花环 | 各自既有契约用例全绿 | ✅ |
| kit | `drawCoin` / `drawStar` / `drawGem` | 5 层结构未动（基建 0 修改提交） | ✅ |

**「平涂圆内贴数字/星字符」：0 处**。严重 0、阻断 0。

---

## 五、专项 ③：双人 / 多角色可区分

**方法**：核对 r1 结论的形状通道资产是否被 fixer 改动（0 触碰）；重读区分通道常量；结合 360px 实截
（combo-clash 对局双方、flight-chess 四座、star-estate 四立牌均在画面里以剪影可辨）。

| 款 | 形状通道（灰度可辨） | 本轮证据 | 判定 |
| --- | --- | --- | --- |
| combo-clash（12 角色） | 12 款头饰 + 姿态差分 + 脚下双色光环 | `art.ts` 头饰段未被 diff 触碰；实截朵朵（粉花冠）vs 星星（金星头）同屏剪影互异 | ✅ |
| hero-cards | 14 款帽饰剪影互异 + 四门花色四剪影 | `cardart.ts` 本轮 0 改动；26 例头像互异契约全绿 | ✅ |
| flight-chess | 四款尾翼（圆鳍/星鳍/双叉鳍/燕尾鳍）+ 四色 | `FIN_NAMES` 契约全绿；实截四基地飞机尾翼形状不同 | ✅ |
| snake-royale | 头饰（蝴蝶结/棒球帽）+ 4 花纹族 | `accessoryFor` 未动；实截蛇头粉蝴蝶结可辨 | ✅ |
| orb-arena | 果冻球头饰金星/银月 + 表情 | `crestOf` 未动 | ✅ |
| star-estate | 花/星/云/月四种立牌脑袋 | `tokenHead` 本轮逐行重读（`art.ts:56-111`），四剪影完全不同构图 | ✅ |
| weiqi-garden | 黑白明度两极 + 白子暖描边 | 实截黑白子同屏，白子不融底 | ✅ |

**「只靠颜色区分」：0 款**。严重 0、阻断 0。

---

## 六、专项 ④：跑酷 / 竞速 / 纵深体积

**方法**：本轮点名五处逐一代码检视 + 实截（证据已在第二节展开，此处汇总）；grep three.js 三头。

| 点名对象 | 现状 | 判定 |
| --- | --- | --- |
| orb-arena 视差层 | 圆斑 0.3 + 具象贴片 0.3（同档不同层）+ 6% 网格 + 条纹墙，四层结构完整 | ✅ |
| snake-royale 双档视差 + 色岛 | 0.6（色岛）/0.85（贴片）恰两档，`PARALLAX` 常量恰 2 个；色岛 alpha 0.08–0.12 | ✅ |
| block-drop 井壁浮雕 | 四主题、每 6 格一件、只在 6px 壁内、离屏缓存；堆高后仍有装饰 | ✅ |
| combo-clash 土丘三阶 | base + 顶亮弧（0.7r、左上偏光）+ 底暗带（4px）三笔齐 | ✅ |
| flight-chess 装饰层 | 草簇×4 + 云×2 恰 6 件、避格、aria-hidden、全静态 | ✅ |
| runner 基建 | 本窗九款仍 0 引用（棋牌/竞技精 2D 定级正确），基建 58 例自检全绿 | ✅（N/A） |
| three.js | package.json 无 three 系依赖；九款 + 基建 0 import（r1 ④组 11 例契约全绿） | ✅ |

**r1 修后退化：0 处**。严重 0、阻断 0。

---

## 七、专项 ⑤：360px 布局实测

**方法**：`npm run build` + `vite preview`，Chrome headless（CDP / puppeteer-core，系统 Chrome）实开
九款，360×640 与 **320×640 极限**各一遍（18 组）；每款「关卡页初测 → 点『开始冒险 ▶』进对局 → 复测」；
量测：文档级 `scrollWidth − innerWidth`、可见文本 <14px（排除 aria-hidden 与 SVG 图形字）、
交互元素触区、console error。P4 四按钮另做 `getBoundingClientRect` 专项复测（结果见第二节）。

**结果表**（初测 / 对局合并）：

| 款 | 360px 溢出 | 320px 溢出 | 主场景渲染 | <14px 文本 | 小触区 | console error |
| --- | --- | --- | --- | --- | --- | --- |
| orb-arena | 0px | 0px | 3 canvas | 仅共享壳 `l99-star` 10–12px（L-1） | 无（`oa-open` 实测 44px ✅） | 0 |
| snake-royale | 0px | 0px | 1 canvas | 同上 | 无（`sr-open`/`sr-skin` 实测 44px ✅） | 0 |
| block-drop | 0px | 0px | 3 canvas | 同上 | 无（`bd-open` 实测 44px ✅） | 0 |
| combo-clash | 0px | 0px | 5 canvas | 同上 | 无 | 0 |
| mahjong-bloom | 0px | 0px | 15 svg | 同上 | `mj-tile` 30px 宽（G-2 豁免项，非新增） | 0 |
| star-estate | 0px | 0px | 115 svg | 同上 | `se-tile` 23×23（G-3 豁免项，非新增） | 0 |
| hero-cards | 0px | 0px | 19 svg | 同上 | 无 | 0 |
| weiqi-garden | 0px | 0px | 1 canvas | 同上 | 无 | 0 |
| flight-chess | 0px | 0px | 22 svg | 同上 | `fc-token` 19×19（G-4 豁免项，非新增） | 0 |

- **文档级溢出 18 组全 0**；star-estate 因 P1 矢量化 SVG 数由 r1 的 75 增至 115（33 地格图标上盘），
  未引起任何溢出或触区变化。
- <14px 唯一命中是共享壳 `l99-star`（10–12px）= L-1 遗留，本窗九款自身文本 0 命中。
- **视觉升级导致的新溢出：0（阻断 0）**；P4 的 44px 已复测通过；G-2/G-3/G-4 豁免项现状与裁决时一致。

---

## 八、专项 ⑥：商标扫描

**方法**：宪法第八节口径，对本窗九款 + kit + runner + motion 的**非测试源码**全量
`rg -i`（黑名单：4399 / 任天堂 / 迪士尼 / 马里奥 / 皮卡丘 / 宝可梦 / Hello Kitty / 托马斯 / Bomberman /
Tetris / 俄罗斯方块 / 球球大作战 / 贪吃蛇大作战 / slither / agar / Monopoly / 大富翁 / 三国杀 / 雀魂 /
野狐 / 蛋仔 / 地铁跑酷 / 超级玛丽 / Nintendo / Disney / Mario / Pokemon / Pikachu / Subway Surf）。
造型侧重点复核 fixer 新画资产。

**结果**：

- 非测试源码 **0 命中**（含注释）；测试文件内命中全部为各款 `levels.test.ts` 等自带的黑名单断言数组
  （「禁止出现」的反例词表本身），性质同 r1，非孩子可见文案。
- 造型侧：star-estate 33 款新图标逐款过目——机会/命运构图为摩天轮/信封（无「? 徽章」）、公用设施为
  水波纹（无灯泡+水龙头组合）、车站为原创 Q 版列车头；`TILE_ICONS` 图形体无 `<text>` 节点已固化为契约。
  flight-chess 草簇/云、block-drop 四主题浮雕、orb-arena 三式贴片均为抽象几何，无字母、无徽记、
  无航司式色带斜切构图。

**阻断 0**。

---

## 九、横向一致性抽查

| 项 | 方法与结果 | 判定 |
| --- | --- | --- |
| kit 光照约定（左上 45°、高光偏移、描边 1.5–2px） | 抽 fixer 新增件对照：orb 贴片糖石子高光 `(-0.26s,-0.3s)` 左上、combo 土丘亮弧 π→1.72π 偏左上、block 浮雕亮点居上、star-estate 图标高光椭圆全部落左上象限（毛线团 6.8,7 / 柠檬 7.4,9.2 / 满月 6.8,6 等）、snake 色岛为远景低 alpha 免高光合理；既有主体高光（orb `(-0.3r,-0.34r)`、coin `cx38%,cy32%`）未动 | ✅ |
| prefers-reduced-motion | 运行时：CDP `prefers-reduced-motion: reduce` 实开 flight-chess / star-estate / combo-clash / mahjong-bloom——**无限循环 CSS 动画 0 个**，功能按钮保留 43–44 个/款；源码：fixer 新增装饰段（贴片/色岛/浮雕/草云/织纹/纸感/土丘明暗）全静态，装饰函数体禁时间源已固化为契约 | ✅ |
| destroy 泄漏 ×5 | CDP 实测六款（orb-arena / combo-clash / star-estate / flight-chess / snake-royale / mahjong-bloom）：进→退 ×5 后 `.view canvas` 0、`.game-screen` 0、pageerror 0；再进正常渲染（18 组布局实测同批完成，主场景全在） | ✅ |
| 粒子节流（orb-arena / snake-royale / combo-clash） | **未取到帧率（headless 环境不做帧率声称），只做代码粒子上限抽查**：orb 贴片层 240 件/帧预算 + sprite 缓存一笔贴；snake 色岛 48 枚/帧预算 + 贴片 sprite 缓存；combo `fxBudget(soft)` 命中粒子归零 + `CONFETTI_COUNT=20` 封顶。CPU 4× 节流下 360px 实开三款对局均正常渲染无 console error（观感描述：镜头移动、贴片/色岛跟随平滑，无肉眼卡顿级异常） | ✅ |

---

## 十、问题清单（全部量化）

**阻断：0 条 严重：0 条 一般：1 条（遗留复登记） 建议：2 条**

| 编号 | 位置（文件+函数/选择器） | 现状（量化） | 期望 | 严重度 | 1.3 回归？ | 建议责任文件 |
| --- | --- | --- | --- | --- | --- | --- |
| R2-1（= L-1 = r1 G-1，复登记） | `src/games/level99.ts:512` `.l99-node-stars{font-size:11px}`、`:561` @媒体降 10px | 本轮 18 组实测唯一 <14px 命中（计算样式 10–12px），九款关卡页同源受累 | 星级改 12×12 内联 SVG 画形（learner P7 方案已给全） | 一般 | 否（1.2 逐字遗留） | `src/games/level99.ts`（共享壳 owner / 统筹轮，本窗不抢） |
| R2-2（= L-2） | `src/games/hero-cards/index.ts:120` `.hc-table` | 桌面仍无织物暗纹（卡面纸感主项已修） | 叠与 `.mj-board` 同规格 ≤4% 斜织纹，一行 CSS | 建议 | 否 | `src/games/hero-cards/index.ts`（交本窗 r2 C 档顺手做） |
| R2-3（新观察） | `docs/qa/1.3-window1-round1-fixer.md` 修复表 #5 | fixer 报告把织纹落点写作 `.mj-table`，实际选择器为 `.mj-board`（`mahjong-bloom/index.ts:102`）；实现本身达标 | 文档笔误级，C 档或统筹在后续文档提及时用真名即可，不必改历史报告 | 建议 | — | 无代码改动需求 |

（G-2 / G-3 / G-4 豁免项与 L-3 / L-4 / L-5 观察项不再重复占编号，处置见第二节两表。）

**交接**：R2-2 交本窗第 2 轮 C 档（成本一行 CSS）；R2-1 维持共享壳 owner 去向；R2-3 记录在案即可。
测试员按权限未改任何绘制代码（diff 仅本报告 + `window1-visual-r2.test.ts`）。

---

## 十一、本轮新增机器化回归用例

新增 `src/games/window1-visual-r2.test.ts`：**17 例全绿**，把 r1 十二项修复的落地状态钉成源码级契约：

| 组 | 用例数 | 内容 |
| --- | --- | --- |
| P1 钉子 | 4 | TILE_ICONS↔board 双向覆盖 / 图形体无 `<text>` 无「?」/ `se-tile-emoji` 清零 + SVG 接线 / clamp 下限 ≥13px |
| P2 钉子 | 2 | 视差 0.3 + 层序（圆斑后、网格前）/ 三式齐全 + alpha 封顶 0.5 |
| P3 钉子 | 1 | 恰 4 草 + 2 云 + aria-hidden + pointer-events:none |
| P5 钉子 | 1 | 土丘三笔（base / tint0.3 亮弧 / shade0.2 暗带） |
| P6 钉子 | 2 | 视差常量恰两档 + 下限 3.2 / 色岛 alpha 0.08–0.12 + 900 格布点 |
| P8 钉子 | 2 | 织纹在位且 alpha ≤4% / 卡面纸感不回退 #fff |
| P9 钉子 | 1 | 四主题 + RELIEF_H=12 + 每 6 格 + 只挂壁中线 |
| P10 钉子 | 1 | art.test.ts 在位 + 旧 describe 未迁走 |
| 回归猎手 | 3 | fixer 新增绘制段无 emoji 码位 / 装饰全静态（禁时间源）/ P4 四处 44px 在位 |

---

## 十二、结果矩阵（12 格 × 六专项）

| 格 | ①火柴人 | ②金币体积 | ③多角色 | ④纵深体积 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| kit（1A） | ✅ | ✅ | ✅ | ✅ | ✅(N/A 布局) | ✅ |
| motion（1B） | ✅(N/A 绘制) | ✅(N/A) | ✅(N/A) | ✅(N/A) | ✅(时序契约) | ✅ |
| runner（1C） | ✅ | ✅(N/A) | ✅(N/A) | ✅(基建自检) | ✅(N/A) | ✅ |
| orb-arena（2A） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| snake-royale（2B） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| block-drop（2C） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| combo-clash（3A） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mahjong-bloom（3B） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| star-estate（3C） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| hero-cards（4A） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| weiqi-garden（4B） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| flight-chess（4C） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

注：⑤ 列 ✅ 指「文档级零溢出、零 console error、无本款 <14px 正文、r1 修复复测通过、豁免项无恶化」；
共享壳 L-1 单列 R2-1 不改变格判定。矩阵 72 格全 ✅。

---

## 十三、验收口径核对与收尾登记

- 测试全绿且只增不减：✅ 进场 724 文件 / 15558 例（= fixer 水位）→ 交卷 **725 文件 / 15575 例**（+1 文件 / +17 例）；
- `npx tsc --noEmit` 0 错：✅；`npm run build` 全绿：✅（110.70 kB / gzip 38.66 kB，precache 188 项 / 5291.01 KiB）；
- 六大专项「阻断」级 0 残留：✅（商标非测试源码 0 命中、360/320 新溢出 0）；
- 「严重」级清单：0 条；一般 1 条（R2-1 遗留复登记）+ 建议 2 条已列表移交 C 档；
- 第 1 轮修复 12 项逐条回归：**12/12 通过**，豁免 3 项依据均成立，遗留 5 项现状已登记，回归缺陷 0；
- 测试员未改 `src/games/**`/`src/art/**`/`src/ui/**` 任何绘制实现：✅（diff 仅本报告 + `window1-visual-r2.test.ts`）；
- 合并态 SHA（rebase 后 HEAD）：见下方收尾登记。

**收尾登记**：fetch → rebase → `npx vitest run`（725 文件 / 15575 例全绿）→ `npx tsc --noEmit`（0 错）→
`npm run build`（全绿）→ 普通 push `origin game-1.3-window1`（无 force、未动 `main`/`game-1.2`/`game-1.2-kk`、未用 `gh` 开 PR）。
合并态 SHA 以推送 tip 为准（本报告 commit 与用例 commit 之上无他人新提交时 rebase no-op）。

本轮实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
