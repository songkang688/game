# 1.3 窗口 2 · 第 3 轮视觉验收（终验） · 测试员报告

> 角色：窗口 2 第 3 轮终验测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货合并态：`82bac8e`（r2 fixer 门禁宣称合并态 `c4abb03` · 737 文件 / 15285 例、build 绿）。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库、不碰其他窗口。
> 权限：只写本报告 + 本窗 9 款目录下新增测试用例（只增不减）。不改绘制代码、不改玩法数值、不改 `src/styles.css`、不改他人报告。
> 本轮定位：**终验**——只回答「本窗 9 款视觉能不能放行」，不重开玩法验收。
> 实测环境：node 22.14.0 / npm 10.9.7 / headless Chrome（`/usr/local/bin/google-chrome`，puppeteer-core `--no-save` 临时驱动，未入库）。探针脚本与截图全部只落 `/tmp`，不入库。

## 终验计划（动手前登记，`65f9177`）

1. **全量跑**：`npm ci` → `npm test` → `npm run build`，记录用例总数与 r2 合并态门禁（737 文件 / 15285 例、PWA 188 entries / 5215.41 KiB）的差值，只增不减；`npx vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 200 + 9 款深链各进一局）。
2. **对账清零**（本轮主责一）：r2 tester 问题清单 7 条（阻断 1 / 严重 1 / 一般 2 / 建议 3）× r2 fixer 14 commit × r2 learner 修订建议 10 条逐条对账——每条给三种结论之一：已修复（附验证证据）/ 合理降级（引 C 档书面理由）/ 仍未修（升级为终验阻断项）。阻断/严重必须为 0 或有 C 档书面降级理由。
3. **特别复验**：dark-chess 格互叠清零 + `::before` 扩展触区 + 32 格中心 elementFromPoint 命中自身全量（fixer 移交的浏览器级复测）；chess-garden 320px h 列可见可点；pool-stars `.ps-tip` 内联 13px 清零；HUD 字号尾巴（dc-sub/dc-tip/dc-pick/cg-pick/cg-over-s/dc-count/jq-empty/cg-log-row）；r2 fixer 遗留：dark-chess textContent 测试桩给终验结论。
4. **本窗终扫**（本轮主责二）：9 款 × 六大专项全走不抽样（火柴人/金币体积/双人可区分/精 2D/360&320 布局/商标），每款每项给「达标 / 不达标」，产出 9×6 终验总表。
5. **回归防线（终验加严）**：前两轮全部机器化用例（round1-visual-scan ×9、round1-fix ×8、round2-visual-verify ×9、round2-fix ×6、round1-packa ×5、learner copy ×2）逐一清点在跑、全绿，被删或 skip 按阻断处理；热区钉死、reduced 功能提示、玩法快照全绿；9 款 CPU 4× 节流高峰 ≥30fps。
6. **产出**：本报告五节（全量跑+三轮用例曲线 / r2 对账 / 9×6 总表 / 终验新问题量化立案 / 终验结论三选一，只覆盖本窗 9 款）+ 第 3 轮终验契约用例（只增不减）。
7. 收尾：fetch → rebase（保留 `docs/plan-1.3-*.md`、`.cursor/skills/**`、`docs/qa/1.3-window2-plan.md` 与他人全部文件）→ `npm test` / `npm run build` 全绿 → 普通 push（禁 force），合并态 SHA 写进报告。

---

## 一、全量跑数据与三轮用例曲线

| 项 | 结果 |
| --- | --- |
| `npm ci` | ✅ 通过 |
| `npm test`（收货基线 `82bac8e`） | ✅ **737 文件 / 15285 例全绿**（174.6s）——与 r2 fixer 门禁宣称逐字一致，只增不减基线成立，**中间零丢失** |
| `npm test`（本轮收尾，含新增用例） | ✅ **746 文件 / 15312 例全绿**（+9 文件 / +27 例，见新增用例清单） |
| `npm run build` | ✅ 全绿（`tsc --noEmit` + vite build，727 模块）；PWA precache **188 entries / 5215.41 KiB**（与 r2 门禁逐字一致） |
| `npx vite preview` 冒烟 | ✅ `/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**；9 款深链 360/320 两档全部进到第 1 关盘面，**0 条 pageerror**、**0 横向溢出**（首页两档同测 0 溢出 0 错误） |

**三轮用例曲线**：r1 收货 703/15178 → r1 合并态 721/15247 → r2 tester 730/15261 → r2 learner 731/15265 → r2 fixer 合并态 **737/15285** → r3 收货实测 **737/15285（差值 0，无丢失）** → r3 收尾 **746/15312**。三轮累计 +43 文件 / +134 例，全部为视觉契约与文案快照，只增不减全程成立。

**前两轮机器化用例清点**（被删或 skip 按阻断处理——**零删除、零 skip**，`rg "\.skip|\.todo|\.only"` 全部 round*/learner-copy 测试 0 命中）：

| 组 | 文件数 | 例数 | 定向重跑 |
| --- | --- | --- | --- |
| `round1-visual-scan.test.ts` ×9 + `round1-fix.test.ts` ×8 | 17 | 65 | ✅ 全绿（993ms） |
| `round2-visual-verify.test.ts` ×9 + `round2-fix.test.ts` ×6 + `round1-packa.test.ts` ×5 + learner copy ×2 | 22 | 234 | ✅ 全绿（7.5s） |

本轮新增用例（**9 文件 / 27 例**，每款一个 `round3-final-verify.test.ts`，全部锁「已验证达标性质」）：三道终验闸统一扩到**全目录非测试源码**口径（较 r2 单文件契约加严）——① 精 2D 零透视关键字（perspective/rotate3d/matrix3d/translateZ/three.js，B 档 r2 建议的「扩到全部 9 款」落地）；② 商标黑名单零命中；③ 字号红线（源码全部 `font-size:*px` 声明 ≥14px，把 r2-3~r2-7 修复钉成「永不回降」总闸）。

---

## 二、第 2 轮问题终验对账（本轮主责）

对账口径：r2 tester 立案 7 条 + r2 fixer B 档一致性 4 条 + r1 遗留 2 条处置 + fixer 移交浏览器级复测 1 条，共 14 行。三种结论：**已修复（附证据）/ 合理降级（引 C 档理由）/ 仍未修（升级终验阻断）**。

| # | 来源·级别 | 款·问题 | 终验结论 | 验证证据（本轮实测） |
| --- | --- | --- | --- | --- |
| 1 | r2-1 · 严重（r1 修复引入回归） | dark-chess 44×44 固定格互叠 + 末列出框 | ✅ **已修复** | 源码：`.dc-cell{aspect-ratio:1/1;min-width:0;min-height:0}` 零最小尺寸，`.dc-cell:not(.dc-empty)::before{inset:min(0px,calc((100% - 44px)/2))}`。浏览器实测：**360px 格宽 31.02px / 320px 27.13px 完全跟随轨道；行内最大互叠 0px（两档）；末列右缘距棋盘内衬右缘 -3px（框内）**；`::before` 实测 inset -6.48px@360 / -8.44px@320，名义热区 **44.0×44.0px** 两档钉死；空格无扩展区。round2-fix 3 例 + 既有 3 处断言更新（it 数不变）在跑 |
| 2 | r2-2 · 阻断（1.2 存量·r1 漏检） | chess-garden 320px h 列被裁 12px 不可点 | ✅ **已修复** | 源码：styles.css 末尾追加 `.cg-wrap .cg-sq{min-width:0;min-height:0}`。浏览器实测：**320px 格宽 36.5px、h 列完整可见（h 列右缘 306px = 板箱可视右缘，可见宽 36.5px = 全宽）**；360px 格宽 40.4px（触控 ≥40px 红线保持；与 fixer 登记估算 41.4px 差 1px，为壳卡圆角内衬取整，不影响红线）；**64 格中心 elementFromPoint 逐格滚动后命中自身 64/64（360 与 320 两档）** |
| 3 | r2-3 · 一般（r1 5-4 尾巴①） | pool-stars `.ps-tip` 被运行时内联 13px 架空 | ✅ **已修复** | 源码：`view.ts` `fontPx: 14`（删视口分支，带 r2-3 注释）。浏览器实测 computed + 内联双查：**320/360/380/768 四档全部 14px、内联值 "14px"**；round2-fix 八档契约在跑 |
| 4 | r2-4 · 一般（r1 5-4 尾巴②） | dark-chess `.dc-sub/.dc-tip/.dc-pick`、chess-garden `.cg-pick/.cg-over-s` <14px | ✅ **已修复** | 菜单屏浏览器实测 computed：dc-sub **14px**、dc-tip **14px**、dc-pick **14px**、cg-pick **14px**；cg-over-s（结算屏元素，第 1 关未实例化）读注入后 CSSOM 声明值 **14px**；round2-fix 契约在跑 |
| 5 | r2-5 · 建议 | dark-chess `.dc-count span` 12px | ✅ **已修复** | 源码 14px；盘内注入后 CSSOM 实测声明值 **14px** |
| 6 | r2-6 · 建议 | junqi-camp `.jq-empty .jq-face` 13px | ✅ **已修复** | 源码 14px；盘内注入后 CSSOM 实测声明值 **14px** |
| 7 | r2-7 · 建议 | chess-garden `.cg-log-row/.cg-log-sum` 13px | ✅ **已修复** | styles.css 末尾追加 `.cg-wrap .cg-log-sum,.cg-wrap .cg-log-row{font-size:14px}`；层叠实证：360px（落在 420px 回降断点内）`.cg-log-sum` **computed 14px**——特异性 0,2,0 压住基础与断点 13px 规则；cg-log-row 同块同口径（第 1 关未走子无实例，CSSOM 三条规则序核对追加段在末） |
| 8 | B 档 r2 一致性①②③ | pool-stars / fruit-stack / chess-garden 壳卡 | ✅ **已修复（采纳落地）** | 源码三张壳卡渐变 + 圆角在位（ps 淡草绿 6px 侧衬 / fs 粉白 0 侧衬 / cg 草绿 ≤420px 侧衬归零）；截图目检 360/320 家族卡片语汇归队；**壳卡未动摇盘面几何**——#1/#2 的格宽实测即为证（36.5/40.4/31.0/27.1）；round2-fix 各 2 例契约在跑 |
| 9 | B 档 r2 #8 · 可选微调 | junqi-camp 星星面底停压一档 | ✅ **已修复（采纳）** | 源码 `#93B2DE`；灰度复算：顶停 Δ**21.0**、底停 Δ**42.8**（≥40 达 B 档原始建议），与 fixer 宣称逐字一致；12 兵种双方角标契约在跑 |
| 10 | B 档 r2 #7 · 不采纳 | merge-2048「方块静饰改圆点」 | ☑ **合理降级（有据不采纳）** | 复核 fixer 理由成立：`.mg-wrap::before/::after` 带 `clip-path:polygon(...)` 十顶点五角星，本就是星形角饰，`border-radius` 在 clip-path 下无效；诉求已由现状满足。终验商标扫描该处零风险 |
| 11 | r1 遗留 2（sudoku-petal 行列轻反馈） | B 档 #10 · fixer 按「只动渲染层」落地 | ✅ **已修复（遗留清零）** | 源码 `sp-linefx` 纯 fx 子节点 + reduced 块 `display:none`（index.ts:369,446,758）；round2-fix 2 例行为契约在跑；reduced 仿真实测 sudoku-petal 进盘 0 错误、功能提示 20 处可见 |
| 12 | r1 遗留 1（dark-chess textContent 测试桩） | fixer 维持遗留、移交本轮 | ☑ **合理降级（终验结论：维持遗留，不阻塞放行）** | 终验裁定：该文本层为测试桩契约 + 无 SVG 环境降级（view.ts 注释明示），真实 DOM 中被 `innerHTML` 整体接管，**零渲染、零性能、零玩家影响，非视觉缺陷**；清理需改绘制代码与既有断言口径（`art.test.ts:73` 等），超出终验测试员「只写报告与补测试、用例只增不减」权限。按 C 档两轮书面理由降级成立，登记为 1.3 收官后的清理项（换 aria-label 口径由绘制侧与测试侧同轮协同） |
| 13 | fixer 移交 · 浏览器级复测 | dark-chess 32 格「中心 elementFromPoint 命中自身」全量（node 桩无法覆盖） | ✅ **已验证** | 360×640 与 320×568 两档实测：**32/32 全量命中自身（含 `::before` 扩展区仲裁后）**，非只测首行；B 档 #1 提醒的「相邻扩展区互叠按 DOM 序仲裁」未产生任何中心点误吞 |
| 14 | r2 学习员修订建议 #1–#10 | 采纳情况终查 | ✅ **对账闭环** | #1–#3（支持 A 档立案）与 #4–#6（壳卡）、#8（灰度）已由 fixer 落地（本表 #1–#9）；#7（方块改圆点）有据不采纳（本表 #10）；#9/#10 遗留跟踪（本表 #11/#12）；「零透视契约扩到 9 款」由本轮 round3-final-verify 落地 |

**对账统计：已修复 / 已验证 12 · 合理降级（有据）2 · 仍未修 0。阻断级、严重级终验残留 = 0。**

---

## 三、9 款 × 六大专项终验总表（全走不抽样）

口径：每款「深链 → 模式 → 第 1 关盘面」360×640 与 320×568 双档浏览器实测 + 全目录源码扫描 + 前两轮机器化契约在跑佐证。**达标 / 不达标**二值结论；带括号为登记备注（不降级）。

| 款 \ 专项 | ①火柴人/草稿圆 | ②金币/道具体积 | ③双人可区分 | ④精 2D | ⑤360/320 布局 | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| merge-2048 | **达标**（块面三停渐变+星角标 5 档契约在跑） | **达标**（左上提亮+box-shadow+高光点；16px 色阶+角标双通道契约） | **达标**（双人同屏：双盘+名字标签+位置通道） | **达标**（零透视契约·全目录） | **达标**（两档 0 溢出 0 错误；HUD ≥14px） | **达标**（黑名单 0 命中；星形角饰复核无「四格方块」方言） |
| mine-garden | **达标**（盛开花 5+5 层瓣+花芯高光契约） | **达标**（旗三角旗面+褶皱、种子数量×形状双通道） | **达标**（轮流制同屏，不适用口径登记） | **达标**（mnflip 纯压扁；零透视契约） | **达标**（小地图 [hidden] 生效契约+drawMini 收敛上下限契约在跑；两档 0 溢出） | **达标**（0 命中） |
| sudoku-petal | **达标**（花瓣三层灰度递增契约） | **达标**（三层实心层叠等效；零 defs/id 契约） | **达标**（轮流制，不适用口径登记） | **达标**（r2 契约 + r3 全目录契约双看守） | **达标**（两档 0 溢出；sp-linefx reduced 兜底实测） | **达标**（0 命中） |
| dot-maze | **达标**（三尖裙边+呆毛+单大眼；r=8 不退化契约） | **达标**（豆/能量豆 2–3 停径向+halo 契约） | **达标**（同屏双盘轮流，不适用口径登记） | **达标**（canvas 正投影；零透视契约） | **达标**（两档 0 溢出；dmz-chip ≥14px 实测） | **达标**（四色淡彩每通道 ≥120 契约在跑；剪影原创组合契约） |
| fruit-stack | **达标**（果卡三停径向+梗叶+双态脸契约） | **达标**（hi→主色→edge 三停 + 11 hi 色互异契约） | **达标**（双盆：盆沿 3px 座位色内衬 #a8306a/#28568f 与座位条同源契约 + 名字标签；灰度下内衬明度差可辨） | **达标**（零透视契约） | **达标**（两档 0 溢出；壳卡侧衬 0 不动双盆几何契约） | **达标**（0 命中） |
| pool-stars | **达标**（球三停+spot 高光+压印契约） | **达标**（球径 ≥14px 三档契约；16px 圆点 vs 菱形退化契约） | **达标**（暖冷组灰度同档故形状通道钉死：花印 vs 星印、退化形互异契约） | **达标**（零透视契约） | **达标**（两档 0 溢出；`.ps-tip` 四档 14px 实测——r2-3 清零） | **达标**（0 命中；`d324399` SHA 误报已排除口径沿用） |
| junqi-camp | **达标**（军衔条金星描边+角标不遮字契约） | **达标**（面板渐变+落影+inset 描边） | **达标**（形状通道：12 兵种×双方 `jq-mark` 去色互异契约；颜色通道：顶停 Δ21.0 / 底停 Δ42.8 复算实测；旗形 crest/hq/hoist 互异契约） | **达标**（jq-flip 纯压扁；零透视契约） | **达标**（两档 0 溢出；jq-empty 14px CSSOM 实测） | **达标**（0 命中；抽象几何旗无军徽制式） |
| chess-garden | **达标**（duoTone 三层：描边→暗纱→顶部内缩亮层，6 兵种契约） | **达标**（高光椭圆左上+底座投影+16px 六种头部剪影） | **达标**（白黑主体灰度 Δ≈174 契约；同形制式明暗可分） | **达标**（零透视契约） | **达标**（**320 h 列完整可见 36.5px、64/64 中心命中**——r2-2 清零；360 格宽 40.4px ≥40 红线；两档 0 溢出；cg-coord 10.5px/cg-piece-tag 12px 为 r1 核准辅助标注口径登记） | **达标**（0 命中；代数记谱公有领域） |
| dark-chess | **达标**（牌背暗字印章+双线框、textContent 桩为非渲染层登记——见对账 #12） | **达标**（象牙渐变+阵营环+战力点；32 格渐变 id 互异配对契约） | **达标**（14 汉字两两互异 + 金钩 vs 银钩 + 环色双通道契约） | **达标**（dcFlipIn 纯压扁；零透视契约） | **达标**（**互叠 0px、末列框内、32/32 中心命中、名义热区 44px**——r2-1 清零；两档 0 溢出；局外屏三类 14px 实测） | **达标**（0 命中；记牌面板「圆环+汉字」无麻将纹样） |

**六大专项终验达标率：54/54（100%）**。三轮曲线：r1 51/54 → r2 52/54 → **r3 54/54**——r2 的两个 ❌（r2-1 dark-chess 互叠、r2-2 chess-garden h 列）全部清零并经浏览器级全量复测坐实。

---

## 四、终验新问题清单

**阻断 0 · 严重 0 · 一般 0 · 建议 0——本轮终扫未发现新问题。**

登记备注 3 条（均为既有核准口径的沿用确认，不立案、不交 C 档）：

| # | 位置 | 现状 | 口径 |
| --- | --- | --- | --- |
| 备 1 | `l99-star` 12px（选关星标，`src/games/level99.ts` 共用件） | 360/320 盘面小字终扫唯一 <14px 可见项（各款 3 处） | r1 5-4 即核准的装饰字符豁免；共用件不在本窗 9 款目录，维持登记 |
| 备 2 | `.cg-coord` 10.5px / `.cg-piece-tag` 12px（styles.css cg- 段） | 360/320 实测 computed 与 r1 修复值一致 | r1 A 5-3 修复时核准的「40px 格内辅助标注」口径（坐标 ≥10.5px 加粗、角标 ≥12px），非正文，维持登记 |
| 备 3 | chess-garden 360px 格宽实测 40.4px（fixer 登记估算 41.4px） | 差 1px，源于壳卡圆角内衬取整 | 触控 ≥40px 红线保持、64/64 中心命中，实测值以本报告为准，无需处置 |

---

## 五、回归防线（终验加严）实测

- **热区钉死**：dark-chess 32 格名义热区 44.0px（`::before` 公式两档实算）+ 中心命中 32/32；chess-garden 64 格中心命中 64/64（两档）；两款热区契约（round2-fix）在跑。r2 fixer 全部 14 commit `git diff --stat` 审计：只碰 9 款 view/index/art/guide、styles.css 末尾追加与测试文件，判定/数值/关卡/存档 key/parentAuth **零触碰**；3 处既有断言更新逐一核对 `it()` 数量不变（25/4/21），用例只增不减成立。
- **reduced 功能提示**：`prefers-reduced-motion: reduce` 仿真下 9 款全部进第 1 关盘面，**0 JS 错误**，功能提示（tip/note/chip/msg/hud 类）**9/9 可见**（每款 2–20 处实测计数）。
- **玩法快照**：全量 15312 例全绿即含全部玩法快照与教育数据契约，零红灯。
- **CPU 4× 节流性能**（360×640 dpr2，rAF 计帧 2s，红线 ≥30fps）：

| 款 | 静置 | 高峰（连发交互） |
| --- | --- | --- |
| merge-2048 | 60.1 | 60.1（方向键连发） |
| mine-garden | 60.1 | 60.1（连点翻格） |
| sudoku-petal | 60.2 | 60.1（连点选格） |
| dot-maze | 60.4 | 60.1（方向键连发） |
| fruit-stack | 60.1 | 60.1（连续投果） |
| pool-stars | 60.2 | 60.1（拖杆出杆） |
| junqi-camp | 60.4 | 60.0（连点走子） |
| chess-garden | 60.1 | 60.0（连点选子） |
| dark-chess | 60.0 | 60.1（连点翻子） |

- **preview 冒烟**：`/`、`/sw.js`、`/manifest.webmanifest` 三端点 200；9 款深链 360/320 两档进第 1 关盘面截图逐张目检**无视觉崩坏**（dark-chess 320 八列全在木框内、chess-garden 320 h 列完整、fruit-stack 盆沿色边可见、junqi 地形件与旗徽正常）。
- **kit 纪律与红线**：`src/art/` 目录仍不存在，无人拷入窗口 1 文件；本轮测试员未改绘制代码、未改 `src/styles.css`；商标黑名单 9 款非测试代码 grep **0 命中**（r3 契约已把该扫描钉成用例）；主角只有朵朵/星星；无血/无伤害/失败只鼓励复查无回退。

---

## 六、终验结论（只覆盖本窗 9 款）

> **放行。**

依据：

1. **对账清零**：r2 阻断 1 条（chess-garden 320 h 列）、严重 1 条（dark-chess 格互叠）全部修复并经浏览器级全量复测坐实（64/64、32/32 中心命中）；一般 2 条、建议 3 条全部修复；合理降级 2 条（merge-2048 星形角饰不采纳、dark-chess textContent 测试桩维持遗留）均有 C 档两轮书面理由且为建议级、零玩家影响。**阻断/严重终验残留 = 0。**
2. **本窗终扫**：9 款 × 6 专项 **54/54 达标**，无一格不达标。
3. **回归防线**：三轮机器化用例 48 文件（39 前两轮 + 9 本轮）零删零 skip 全绿；746 文件 / 15312 例全量绿；build 绿；preview 冒烟绿；reduced 9/9；CPU 4× 全 60fps。
4. 本结论**不替全库放行**，不覆盖他窗 30 款；dark-chess textContent 桩清理登记为收官后事项（不构成放行条件）。

## 附：收尾登记

- 收尾：fetch → rebase（保留 `docs/plan-1.3-*.md`、`.cursor/skills/**`、`docs/qa/1.3-window2-plan.md` 与包工头/他人全部文件）→ `npm test` **746 / 15312 全绿** → `npm run build` 全绿（188 entries / 5215.41 KiB）→ 普通 push，未用 force。
- 提交链：`65f9177`（终验计划）→ `40c0489`（r3 终验契约 9 文件 27 例，rebase 过包工头 `c019226` 派工 commit 后普通 push）→ 本报告收尾 commit 落于其上。
- 合并态 SHA：见收尾 commit（收货 `82bac8e`；全量验证跑于收货态与含契约的收尾态各一次）。
