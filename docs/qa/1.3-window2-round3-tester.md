# 1.3 窗口 2 · 第 3 轮视觉验收（终验） · 测试员报告

> 角色：窗口 2 第 3 轮终验测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货合并态：`82bac8e`（r2 fixer 门禁宣称合并态 `c4abb03` · 737 文件 / 15285 例、build 绿）。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库、不碰其他窗口。
> 权限：只写本报告 + 本窗 9 款目录下新增测试用例（只增不减）。不改绘制代码、不改玩法数值、不改 `src/styles.css`、不改他人报告。
> 本轮定位：**终验**——只回答「本窗 9 款视觉能不能放行」，不重开玩法验收。

## 终验计划（动手前登记）

1. **全量跑**：`npm ci` → `npm test` → `npm run build`，记录用例总数与 r2 合并态门禁（737 文件 / 15285 例、PWA 188 entries / 5215.41 KiB）的差值，只增不减；`npx vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 200 + 9 款深链各进一局）。
2. **对账清零**（本轮主责一）：r2 tester 问题清单 7 条（阻断 1 / 严重 1 / 一般 2 / 建议 3）× r2 fixer 14 commit × r2 learner 修订建议 10 条逐条对账——每条给三种结论之一：已修复（附验证证据）/ 合理降级（引 C 档书面理由）/ 仍未修（升级为终验阻断项）。阻断/严重必须为 0 或有 C 档书面降级理由。
3. **特别复验**：dark-chess 格互叠清零 + `::before` 扩展触区 + 32 格中心 elementFromPoint 命中自身全量（fixer 移交的浏览器级复测）；chess-garden 320px h 列可见可点；pool-stars `.ps-tip` 内联 13px 清零；HUD 字号尾巴（dc-sub/dc-tip/dc-pick/cg-pick/cg-over-s/dc-count/jq-empty/cg-log-row）；r2 fixer 遗留：dark-chess textContent 测试桩给终验结论。
4. **本窗终扫**（本轮主责二）：9 款 × 六大专项全走不抽样（火柴人/金币体积/双人可区分/精 2D/360&320 布局/商标），每款每项给「达标 / 不达标」，产出 9×6 终验总表。
5. **回归防线（终验加严）**：前两轮全部机器化用例（round1-visual-scan ×9、round1-fix ×8、round2-visual-verify ×9、round2-fix ×6、round1-packa ×5、learner copy ×2）逐一清点在跑、全绿，被删或 skip 按阻断处理；热区钉死、reduced 功能提示、玩法快照全绿；9 款 CPU 4× 节流高峰 ≥30fps。
6. **产出**：本报告五节（全量跑+三轮用例曲线 / r2 对账 / 9×6 总表 / 终验新问题量化立案 / 终验结论三选一，只覆盖本窗 9 款）+ 第 3 轮终验契约用例（只增不减）。
7. 收尾：fetch → rebase（保留 `docs/plan-1.3-*.md`、`.cursor/skills/**`、`docs/qa/1.3-window2-plan.md` 与他人全部文件）→ `npm test` / `npm run build` 全绿 → 普通 push（禁 force），合并态 SHA 写进报告。

（正文五节随终验推进补齐。）
