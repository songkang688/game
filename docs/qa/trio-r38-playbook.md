# r38 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**（r19 B 已合 N-94～N-108）。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-174**、**N-175**、**N-176**。下一空号 **N-177**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r37 学习 PR 未合主干时，本轮仍从 N-174 起编；若主干已占用 N-174+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r37 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r38-learn-notes.md` §0。尤其 **不要**再开 `.dvs-pick`（N-94）、`.bc-pick`（N-102）、`.xq-start` 出屏（N-95）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / #105～**#110** / **#107**（坦克双垫 / 消消乐·地鼠钳高 / 底栏 840）。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-174 · B · `.rbg-pick`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/red-blue-tug/index.ts` ≈L206；挂载 ≈L935 |
| 现状 | 选对手 `<button>`，`padding:10px 14px`，无 min-height |
| 不做 | 不改 `.rbg-open`/`.rbg-back`/`.rbg-toggle`（已绿）；不改 `TUG12` / AI 档表；不并 N-145/169 |
| 验收 | 选对手卡高 ≥44；点档仍进对战/双人 |

### N-175 · B · `.rbg-btn`

| 项 | 内容 |
| --- | --- |
| 文件 | 同文件 ≈L202 |
| 现状 | 结算 CTA，padding 12 + 字号 16，无 min-height；估高 ≈43.2 |
| 不做 | 不改 `.dvs-over button`（N-157）；不改冲刺菜单（N-87） |
| 验收 | 再来/回选钮高 ≥44；点按仍换屏 |

### N-176 · A · `*-pick` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-pick` 可点块必须 `min-height`≥44 或 TOUCH/TOGGLE/SWATCH/CHIP/MIN_HIT 插值 |
| 现状红 | `.rbg-pick`（N-174）；`.pfb-pick`（N-169，可红到那号合入） |
| 对照绿 | `.dvs-pick` N-94、`.bc-pick` N-102、`.ps-pick` 等 |
| 豁免 | `*-picks` / `.fk-pick` / `.cc-pick` 容器、非交互 |
| 不做 | 不替代 N-169/170/145；不把已销的 N-148 再当红灯 |

---

## 建议顺序

1. A 先合 **N-176**（或与 B 并行）。
2. B 修 **N-174**，再修 **N-175**（同一文件）。
3. 回归：主干 N-94/102 pick、N-171 tally、N-173 go、N-87 冲刺菜单、N-99/97/100、拔河 open/back/toggle（已绿勿回退）。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-174/175 的点击矩形，不能只看 CSS 行。
