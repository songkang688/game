# 窗口 2 · 1.2 三轮验收 第 3 轮 · 测试员 包 C（点名五项 · 本窗口收官）

- 窗口号：2
- 角色：QA 第 3 轮 测试员 · 包 C
- 基线：`origin/game-1.2-window2`（走查起点 `d4decfc`）
- 对比基准：`origin/game-1.1`
- 范围（只验这 5 款）：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 先读件：`docs/qa/1.2-window2-round2-tester-packC.md`、`-round2-learner-packC.md`、`-round2-fixer-packC.md`
- 模型：`claude-opus-5-thinking-high-fast`

---

## 0. 走查计划（动笔之前先立的规矩）

### 0.1 边界（哪些一个字都不碰）

| 边界 | 做法 |
| --- | --- |
| 别人的 10 款（包 A / 包 B 范围） | 不看不改不验 |
| 窗口 1 平台文件 `src/ui/home.ts` `rootGate.ts` `playModes.ts` `view25d.ts` | 不改 |
| `src/games/**` 产品代码 | **一个字节不改**（含 `gomoku/ai.ts`、`xiangqi/ai.ts` —— 点名项明令不许自己改） |
| `src/engine/**` `src/ui/**` `src/styles.css` `package.json` | 不改 |
| `src/games/**` 下新增测试 | **本轮不留**，走查脚本跑完即删 |
| 第 1 / 2 轮任何报告原文 | 不改 |
| 汇总文件 `docs/qa/1.2-window2-round3-tester.md` 里别人的节 | 不改，只在文件末尾追加 `## Pack C` |

留档只两处：本文件（独占）+ 汇总文件末尾追加的 `## Pack C` 一节。
路径一律 `docs/qa/1.2-window2-round3-*`。
只推 `game-1.2-window2`，不碰 `main`，不 `--force`，不用 `gh` 开 / 改 / 合 PR。

### 0.2 换样本（第 3 轮既不复述第 1 轮，也不复述第 2 轮）

| 款 | 第 1 轮 | 第 2 轮 | **本轮（第 3 轮）** |
| --- | --- | --- | --- |
| 战役（五款统一） | 1 / 100 / 188 | 1 / 88 / 188 | **1 / 76 / 188** |
| `gomoku` 解局 10 关 | 1/12/34/56/78/99/100/130/160/188 | 7/22/45/67/88/101/120/145/170/188 | **11/28/51/73/94/108/126/151/173/188** |
| `gomoku` 相邻档 seed | 基点 1000 | 基点 4200 / 8600 / 15400；着法尺 31337 | **基点 2718 / 5772 / 13331；着法尺 60607** |
| `xiangqi` 残局 10 课 | 1/21/43/66/88/100/121/145/166/188 | 7/33/55/71/90/111/133/155/170/181 | **另抽 10 课，将死 + 困毙都要有**（第 5 节回填实际课号） |
| `match-stars` 时间线 | 引擎小盘 + 16ms | 第 1 / 88 / 188 关 | **换一关重走**（第 2 节回填） |
| `ocean-munch` 无尽深度 | 767 / 147 | 591 | **再换一次，重新泡到系统失败** |
| `xiangqi` 强弱考卷 | 60 道 `mateIn ≤ 2` | 隔两道取的另一份 60 道 | **再换一份取样口径** |

### 0.3 每款必做（一款不漏）

1. 首页 / 深链定位；
2. 真赢一次 + 真输一次，都玩到结算；
3. 战役第 **1 / 76 / 188** 关都真打到结算；
4. 有几种模式就每种玩到结算；
5. 360px 宽下量热区（≥ 44px）—— **第 2 轮修复员刚补的那批按钮必须重新量一遍**
   （`gomoku` `.gmk-btns/.gmk-seg/.gmk-mode/.gmk-over-btn`、`xiangqi` `.xq-btns/.xq-mode/.xq-over-btn`
   含 `@media (max-width:380px)` 那一档、`rainbow-run` 与 `ocean-munch` 的 `touchArea` 10 处）；
6. `destroy`：进 → 玩 → 退出 → 再进，数监听 / 定时器 / 子节点 / rAF；
7. 商标黑名单逐条扫，必须 **0** 命中。

外加：难度曲线、竞态、无尽可持续性、红线词（无血无死亡 / 无广告内购抽卡 / 无联网上报）。

### 0.4 点名五项各自的硬要求（本轮独立复验，给证据）

| # | 款 | 本轮必须拿到的证据 |
| --- | --- | --- |
| 1 | `gomoku` | ① 换的 10 关按 `solutionLine()` **实摆到底**，终局 `findWinLine()` 验黑五连；② 相邻档固定 seed 对下单调，**换一组与第 1、2 轮都不同的 seed**；③ 前 99 题相对 `origin/game-1.1` 不漂移；④ 188 关无解条数；⑤ 第 1 轮阻断（`smart` 打不过 `normal`、`hell` 退化成 `master`）**不得回潮**；⑥ `R2C-G3`（smart/normal 和棋率）给**最终结论**，不自己改 `ai.ts` |
| 2 | `match-stars` | 消除 → 下落 → 补位三个阶段**可见**；**瞬变即阻断**；`prefers-reduced-motion` 不得另开瞬变分支；换一关时间线 |
| 3 | `rainbow-run` | `view3d.ts` / `controls.ts` 对 1.1 **0 行 diff**；`endless.ts` 说明白差在哪；无 three.js；`makeCamera` 只在初始化与尺寸真变时调；**第 2 轮刚改的热区 / `touchArea` 与文案口径不得回潮到「掉血」「广告墙」** |
| 4 | `ocean-munch` | 无尽玩到失败；`recordEndlessBest("ocean-munch", depth)` 触发；`destroy` 后读回；前 99 关参数不漂 |
| 5 | `xiangqi` | 只有一套象棋目录；档位层数 / 预算单调；残局有解、虚标 0；抽 10 课实走相符；长将三边界；`R2C-X2`（easy/normal 残局卷同分）给**最终结论**，不自己改搜索 |

### 0.5 遗留项必须给最终结论

| 编号 | 来源 | 本轮要做的事 |
| --- | --- | --- |
| `R2C-G3` | 第 2 轮测试员记、第 2 轮修复员按红线转交 | 独立重跑和棋率，判「能否随本窗口发布」，**不动 `ai.ts`** |
| `R2C-X2` | 同上 | 换一把尺重量 easy/normal，判是分辨率不足还是真倒挂，**不动搜索** |
| `R2C-G1/G2/X1/R1/O1/R2` | 第 2 轮修复员声称已清 6 条 | 逐条复量，确认真清干净、**没有回潮** |
| 第 1 轮 3 条观察项（R17 / G15 / X20） | 第 1 轮测试员第 7 节 | 给最终去留结论 |

### 0.6 走查手法

- 走查脚本一律临时件，放在 `src/__r3packc__/`（vitest 的 `include` 是 `src/**/*.test.ts`，
  放这儿才跑得起来，且**不在 `src/games/**` 下**），取证完成后整目录删除。
- 与 1.1 的对比用 `git show origin/game-1.1:<path>` 取原文当参照，不改工作区。
- 对局与动画一律冻时钟（假 `performance.now` / 假 `rAF` / mulberry32 定 seed），保证可复现。
- 分级：**阻断**（点名硬要求不成立 / 玩不下去） / **严重**（模式跑不完、规则错、明显不公平） / **一般**（体验、文案、热区、泄漏）。

### 0.7 红线自查项

无血无死亡描写 · 角色只用朵朵 / 星星 · 无广告内购抽卡联网上报 · 商标黑名单 0 命中。

---

## 1. `gomoku` 五子棋

走查脚本：`src/__r3packc__/g1-puzzles.test.ts`（解局 / 漂移 / 兜底）、`g2-tiers.test.ts`（相邻档实战）、
`g3-play.test.ts`（真挂载七条铁则）。取证后整目录删除，产品代码零改动。

### 1.1 点名项①：抽 10 关 `solutionLine()` 实摆到底 + `findWinLine()` 验黑五连 ✅

做法：对每一关取 `solutionLine()` 的主变，**真把棋摆上去走一遍**（黑走主变，白每次挡黑排第一的
成五威胁点），终局用 `findWinLine()` 读出那条五连的五个坐标。本轮 10 关与第 1、2 轮全不重样。

| 关 | 题名 | 解法 | 步上限 | 必胜 | 首手解数 | 最短杀 | 实走（终局五连） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 11 | 缺一颗·天窗 | 唯一胜点 | 1 | true | 1 | 1 | 黑(4,2) → (2,2)-(3,2)-(4,2)-(5,2)-(6,2) ✅ |
| 28 | 反斜活三 | 唯一胜点 | 2 | true | 2 | 2 | 黑(3,6) 白(7,2) 黑(2,7) → (2,7)-(3,6)-(4,5)-(5,4)-(6,3) ✅ |
| 51 | 双冲·雷雨 | 唯一胜点 | 2 | true | 1 | 2 | 黑(4,3) 白(3,3) 黑(4,2) → (4,2)-(4,3)-(4,4)-(4,5)-(4,6) ✅ |
| 73 | 镜鸟·踏雪 | 三手杀 | 3 | true | 1 | 3 | 5 手主线 → (2,4)-(3,4)-(4,4)-(5,4)-(6,4) ✅ |
| 94 | 大师·罗盘阵 | 三手杀 | 3 | true | 1 | 3 | 5 手主线 → (4,3)-(4,4)-(4,5)-(4,6)-(4,7) ✅ |
| 108 | 连环·白露 | 四手杀 | 4 | true | 2 | 4 | 7 手主线 → (1,4)-(2,4)-(3,4)-(4,4)-(5,4) ✅ |
| 126 | 连环·铜铃 | 抓禁手 | 4 | true | 2 | 4 | 7 手主线 → (3,3)-(4,3)-(5,3)-(6,3)-(7,3) ✅ |
| 151 | 陷阱·山径 | 抓禁手 | 4 | true | 2 | 4 | 7 手主线 → (4,0)-(5,1)-(6,2)-(7,3)-(8,4) ✅ |
| 173 | 长考·银河 | 五手杀 | 5 | true | 3 | 5 | 9 手主线 → (4,4)-(5,4)-(6,4)-(7,4)-(8,4) ✅ |
| 188 | 长考·长歌 | 抓禁手 | 5 | true | 4 | 5 | 9 手主线 → (4,4)-(4,5)-(4,6)-(4,7)-(4,8) ✅ |

**10 关全部走到黑五连，一关不漏。**

### 1.2 点名项④：188 关兜底 ✅ 无解 0 条

```
[188关兜底] 无解条数 = 0 / 188 | 少一手也能赢(虚标) = 0
[无解关号] （空）
[章节]     9 章 = 17+17+17+16+16+16+30+30+29 = 188
[后89题]   写死的 solutions 与求解器逐点相等,不等 0 条
```

**一条旁证（本轮新量到，第 2 轮的数字不一样，先说清楚）**：如果**强行**把禁手规则打开，
188 关里有 **6 关**（46 双冲四星 / 47 双冲·换角 / 48 双冲·倒挂 / 49 双冲·侧影 / 50 双冲·风车 /
51 双冲·雷雨）的解集会变空 —— 这 6 关的杀法本身就是**双冲四**，正是三三四四禁手要禁的那一手。

这条**不构成缺陷**，理由是走查时逐行确认过的：解局战役在 `index.ts:579` 把
`forbidden: false` **写死**，玩家在残局学堂里根本打不开禁手开关（开关只出现在自由对战面板）。
六关全部在 `LEGACY_PUZZLES`（前 99）里，是 1.1 逐字节继承下来的题目，1.2 没动。
第 2 轮报的是「开禁手后无解 = 0」，与本轮 6 条不一致 —— 本轮的数字是把
`winningFirstMoves(board, moves, {forbidden:true})` 对 188 关**逐关**跑出来的，
关号与题名都列在上面，可复现。记为**观察项 `R3C-G4`**，不记缺陷。

### 1.3 点名项③：前 99 题相对 `origin/game-1.1` 不漂移 ✅ = 0

把 `origin/game-1.1` 的 `puzzles.ts` 原样取出（只把 `from "./ai"` 改成相对路径）当参照模块，
逐字段（`name` / `tip` / `size` / `moves` / `theme` / 黑白子坐标集合）比对：

```
[前99题] 漂移条数 = 0
LEGACY_PUZZLES=99  PUZZLES.length=188  1.1=188
```

### 1.4 点名项②⑤：相邻档单调 —— **换第三组 seed 独立重跑，第 1 轮阻断没有回潮** ✅

口径：15×15，两档轮流走，**强档执黑执白各一半**，固定 seed（mulberry32），
一局跑到五连或棋盘下满；地狱档冻住时钟（`now:()=>0`、`timeMs:400`、`maxDepth:4`）保证可复现。

**seed 基点换成 2718 / 5772 / 13331**（第 1 轮 1000，第 2 轮 4200/8600/15400），
本轮结果是独立跑出来的，不引用前两轮任何表格：

```
easy   → novice  36 局: 强 36 弱  0 和  0 (100.0%)  12-0-0  12-0-0  12-0-0   单调 ✅
normal → easy    36 局: 强 35 弱  1 和  0 ( 97.2%)  11-1-0  12-0-0  12-0-0   单调 ✅
smart  → normal  36 局: 强 18 弱  1 和 17 ( 50.0%)   6-1-5   6-0-6   6-0-6   单调 ✅
master → smart   36 局: 强 36 弱  0 和  0 (100.0%)  12-0-0  12-0-0  12-0-0   单调 ✅
hell   → master  24 局: 强 18 弱  6 和  0 ( 75.0%)   5-3-0   6-2-0   7-1-0   单调 ✅
跨档对照 hell → novice 6 局: 6-0-0
```

**五对相邻档全部「强档赢的局数 > 弱档赢的局数」**，一对都没倒挂。

**第 1 轮那两条阻断逐条对照（本轮换 seed 独立跑）：**

| 第 1 轮现象 | 第 1 轮数据 | 本轮（第三组 seed） | 回潮？ |
| --- | --- | --- | --- |
| `smart` 打不过 `normal` | 120 局 36 : 54（30.0%，倒挂） | 36 局 18 : 1（强档只输 1 局） | **没有回潮** |
| `hell` 退化成 `master` | 120 局 60 : 60（每 seed 精确 20-20-0） | 24 局 18 : 6（75.0%），三个 seed 分别 5-3-0 / 6-2-0 / 7-1-0 | **没有回潮** |

第二把尺 —— 30 个真实中盘局面（与仓库自带 `midPositions()` 同口径，深度 8..24 手不等，
seed 基点换成 **60607**），同一 rng 逐档比对着法：

```
novice vs easy  :  1/30 ( 3.3%)
easy   vs normal: 10/30 (33.3%)
normal vs smart : 25/30 (83.3%)   ← 仓库自带门槛 ≤90%,过
smart  vs master: 15/30 (50.0%)
master vs hell  : 21/30 (70.0%)   ← 第 1 轮是 100%(一步不差),现在真的分开了
```

档位表本身：

```
novice=0层/300ms easy=1层/420ms normal=1层/480ms smart=2层/520ms master=3层/560ms hell=4层/320ms
搜索层数单调不降 = true
```

### 1.5 点名项⑥ `R2C-G3`（`smart` / `normal` 和棋率）最终结论

**本轮独立重跑（第三组 seed）：和棋 17 / 36 = 47.2%，强档赢 18 局、输 1 局。**
第 2 轮是 13/36 = 36.1%、强档 0 负。三轮横着看：

| 轮次 | seed 基点 | 局数 | 强 : 弱 : 和 | 和棋率 | 倒挂？ |
| --- | --- | --- | --- | --- | --- |
| 第 1 轮 | 1000 | 120 | 36 : 54 : 30 | 25.0% | **是（阻断）** |
| 第 2 轮 | 4200/8600/15400 | 36 | 21 : 2 : 13 | 36.1% | 否 |
| 第 3 轮 | 2718/5772/13331 | 36 | 18 : 1 : 17 | 47.2% | 否 |

**最终结论：不阻断发布，保留为一般项转 1.3。** 三条理由：

1. **强弱关系是对的，只是差距被和棋吃掉了。** 三组不同 seed、共 72 局里，`smart` 只输给
   `normal` 3 局（第 2 轮 2 局 + 本轮 1 局），赢 39 局 —— 没有任何倒挂迹象；
2. **和棋率高的根因不在 AI 的强弱，在盘面。** 15×15 满盘 225 手，两档都会「必成五 + 必挡五」，
   谁也逼不出双威胁时就必然下满棋盘判和。和棋率随 seed 在 25%–47% 之间波动，
   是**样本噪声**而不是趋势（本轮 17 局和棋里 12 局出在两个 seed 上，另一个 seed 只有 5 局）；
3. **要压下和棋率就得动 `ai.ts` 的评分权重**，那是第 1 轮修复员刚修好阻断的现场，
   本轮红线明令不许碰，**我没有改一个字节**（见第 8 节红线自查）。

孩子实际体验到的是「聪明档比普通档难打」——这条没有被破坏。建议 1.3 里给 `smart`
加一点进攻倾向、或者把自由对战默认棋盘从 15 路调到 13 路，两条都能把和棋率压下去。

### 1.6 七条铁则（战役样本换成 1 / 76 / 188）

**① 首页 / 深链**：`initialLevel` 直开三关都命中标题「第 N 关」，棋盘就位。

```
第   1 关：一箭穿心     · 「黑棋已经四连啦,找到那个空位!」   · 9×9 · 步上限 1
第  76 关：转向·南坡    · 「斜线冲四,再横着开活四!」          · 9×9 · 步上限 3
第 188 关：长考·长歌    · 「开着禁手规则再想一遍…」            · 9×9 · 步上限 5
```

**② ③ 真赢 + 真输（360px 手机口径，`确认落子：开`，每手点两次）**

`Math.random` 冻成常数之后界面里的 AI 就是纯函数，于是可以在旁边跑一份**镜像棋盘**
算出白棋（解局防守档）会走哪儿，人这一侧打真的 `pointerdown` / `pointerup` 进去。

| 关 | 真赢实走 | 结算面板 | 真输 | 结算面板 |
| --- | --- | --- | --- | --- |
| 1 | 黑(1,4) | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | 闲棋 2 手 | 🔁 再试本关 / 🗺️ 回地图 |
| 76 | 黑(6,3) 白(7,2) 黑(3,3) 白(7,3) 黑(2,3) | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | 闲棋 4 手 | 🔁 再试本关 / 🗺️ 回地图 |
| 188 | 9 手主线走完 | 🔁 再玩一次 / 🗺️ 回地图（最后一关没有「下一关」） | 闲棋 4 手 | 🔁 再试本关 / 🗺️ 回地图 |

第 188 关打完额外触发平台 `onWin(3★,「188 道残局全部解开，你的算杀已经很有大师样子了！」)`。
**赢的面板有「下一关」、输的面板只有「再试本关」，两套按钮一次都没串。**

**④ 四种玩法各玩到结算**

```
自由对战·人机·菜鸟档  人（大师档策略）下 6 手真赢 → onWin 2★「你把菜鸟档的棋灵赢下来了！再往上挑一档试试。」
双人同屏 朵朵VS星星   轮次 朵→星→朵→星→朵→星→朵→星→朵（严格交替=true）→ onWin 1★「⚫ 朵朵连成五颗，赢啦！」
```

连胜挑战一路打到断，**六盘六档一盘升一档**（本轮打满整条梯子，第 2 轮只打到第 4 盘）：

```
第 1 盘 [🔥 连胜 0 · 🐣 棋灵苗·菜鸟] → 🎉 连赢 1 盘！  下一位是🐱 棋灵喵·简单
第 2 盘 [🔥 连胜 1 · 🐱 棋灵喵·简单] → 🏆 新纪录 2 盘！ 下一位是🦊 棋灵狐·普通
第 3 盘 [🔥 连胜 2 · 🦊 棋灵狐·普通] → 🏆 新纪录 3 盘！ 下一位是🐲 棋灵龙·聪明
第 4 盘 [🔥 连胜 3 · 🐲 棋灵龙·聪明] → 🏆 新纪录 4 盘！ 下一位是🐘 棋灵象·大师
第 5 盘 [🔥 连胜 4 · 🐘 棋灵象·大师] → 🏆 新纪录 5 盘！ 下一位是🌌 棋灵渊·地狱
第 6 盘 [🔥 连胜 5 · 🌌 棋灵渊·地狱] → 🔥 连胜结束  连赢 5 盘，刷新纪录！下一次目标 6 盘
destroy → 重新 mount：模式条读回「🔥 连胜挑战 · 最好 5 盘」✅
```

每一盘的开局播报都带上了这一档的脾气（第 2 轮学习优化员落地的 C4），
六句互不重样，第 1 盘也说得出。**连胜梯子的实战强度顺序与 1.4 量到的一致。**

**⑤ 竞态**：落子后进入「思考中」，此时狂点棋盘 30 下 —— 仍是「思考中」、一子未落；
`destroy` 之后再推 5 秒 + 乱点 20 下：`onWin=0 onLose=0`，不抛异常。

**⑥ `destroy` 无泄漏（进 → 玩 → 退出 → 再进 20 轮）**

```
轮  1 / 轮 10 / 轮 20 一律： win 监听=0  根子节点=0  head 样式=0  待跑定时器=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个,不多不少
destroy 之后再推 10 秒：不抛异常；平台 onWin / onLose 保持 0
```

**⑦ 商标 / 红线**：188 道题名 + 提示语扫 35 个商标词 + 11 个红线词 → 命中 **0**；
自由对战面板 + 第 1 / 76 / 188 关三处界面文字再扫一遍 → 商标 **0**、红线 **0**；
角色只有朵朵与星星，两个都在。

**360px 热区（第 2 轮修复员刚补的四类按钮，本轮重新量一遍，`min-height` 优先、没写就按
「上下 padding + 上下边框 + 字号 × 1.2」估下限）**

| 选择器 | 用途 | 量法 | 实高 | 判定 |
| --- | --- | --- | --- | --- |
| `.gmk-btns button` | 悔棋 / 提示 / 确认落子 / 重摆 / 换玩法 | `min-height:44px` | **44.0px** | ✅（第 2 轮 36.8–39.6px） |
| `.gmk-seg button` | 棋盘大小 / 档位 / 禁手规则 | `min-height:44px` | **44.0px** | ✅（第 2 轮 40.8–43.6px） |
| `.gmk-mode` | 自由对战 / 连胜挑战 | `min-height:44px` | **44.0px** | ✅（第 2 轮 36.0–39.0px） |
| `.gmk-over-btn` | 连胜结算面板按钮 | `min-height:44px` | **44.0px** | ✅（第 2 轮 41.2–44.4px 临界） |
| `.gmk-start` | 开始下棋 | padding + font 估下限 | 47.6px | ✅ |

可点选择器清单一个不漏地扫过（`.gmk-btns button` / `.gmk-seg button` / `.gmk-seg button.gmk-on` /
`.gmk-start` / `.gmk-mode` / `.gmk-over-btn`），`MIN_HIT_PX = 44` 是 `view.ts` 导出的常量、
CSS 用模板串插进去 —— 想调小必须先改这个数。**`R2C-G1` / `R2C-G2` 已清干净，没有回潮。**

### 1.7 `gomoku` 小结

点名项①②③④⑤⑥全部拿到独立证据；**第 1 轮那条阻断在第三组 seed 上仍然没有回潮**；
第 2 轮的 `R2C-G1` / `R2C-G2` 已清且不回潮。
本轮新记：一般项 0 条，观察项 1 条（`R3C-G4`，开禁手时 6 关无解，战役不可达）。
`R2C-G3` 给出最终结论：不阻断发布，转 1.3。

## 2. `match-stars` 星星消消乐

走查脚本：`src/__r3packc__/m1-anim.test.ts`（时间线）、`m2-play.test.ts`（七条铁则）。
取证后整目录删除，产品代码零改动。

### 2.1 点名项②：消除 → 下落 → 补位有可见阶段（**换第 42 / 76 / 133 关**）✅ 瞬变阻断不成立

做法与第 2 轮同口径但换关：按 `index.ts` 的同一套接线（`createState` + `createStage` +
真的 `clearCells` / `spawnToken` / `beltSlots`），用虚拟时钟一帧一帧往前推，
每帧读 `stage.phase()` / `stage.movingCount()` / `stage.visualRowOf()`。
第 1 轮抽的是引擎自带小盘 + 16ms 那条线，第 2 轮是第 1 / 88 / 188 关，本轮全换。

**第 76 关（8×8，初盘可消组 = 0，16 组合法交换取第 1 组 · 格 0↔8）逐段账：**

| 段 | 帧数 | 时长 | 飘空块帧 |
| --- | --- | --- | --- |
| `idle` | 1 | ≈16ms | 0 |
| `swap` | 9 | ≈144ms | 9 |
| `boom` | 13 | ≈208ms | 0 |
| `fall` | 5 | ≈80ms | 5 |
| `land` | 6 | ≈96ms | 0 |
| `settle` | 8 | ≈128ms | 0 |

`fall` 段里 `visualRowOf()` 与逻辑行的偏差从 **1.00 格连续收敛到 0**
（t=1368ms 偏差 1.00 → t=1384ms 0.90 → t=1400ms 0.58 → …），
说明块是真的一路掉下来的，不是原地闪出来的。

三关横着看：

| 关卡 | 帧数 | 段序 | **飘空帧数** | `fall` 最大偏差 |
| --- | --- | --- | --- | --- |
| 第 42 关 8×8 | 72 | `idle→swap→boom→fall→land→boom→fall→land→settle` | **25** | 1.00 格 |
| 第 76 关 8×8 | 42 | `idle→swap→boom→fall→land→settle` | **14** | 1.00 格 |
| 第 133 关 8×8 | 121 | `idle→swap→boom→fall→land→belt→settle→boom→fall→land→settle` | **66** | 3.00 格 |

**飘空帧数 = 0 才是瞬变；三关分别是 25 / 14 / 66，都不为 0 —— 瞬变阻断不成立。**
第 42 关的连锁两轮 `boom→fall→land` 如实走满；第 133 关多出来的 `belt` 段是传送带那一行
在结算前整排平移（26 帧、≈416ms），也没有被合并成一帧。

### 2.2 `planGravity` / `planRefill`

两个都是纯函数。拿第 76 关真盘造一个「第 0 列下三格被清掉再压实」的中间盘面：

```
[planGravity] 5 条，幂等 = true
  4→7(delay  0, dur 210) col=0
  3→6(delay 20, dur 210) col=0
  2→5(delay 40, dur 210) col=0
  1→4(delay 60, dur 210) col=0
  0→3(delay 80, dur 210) col=0
[planRefill] 3 条，fromRow 全为负 = true
  -1→2(delay  0) col=0
  -2→1(delay 20) col=0
  -3→0(delay 40) col=0
[planEndMs] gravity=290ms  refill=250ms
[FULL_TIMINGS] {swapMs:140, boomMs:200, perCellMs:70, staggerMs:20, landMs:90, beltMs:200, settleMs:120}
```

错峰 20ms 一档、越靠上的块越晚起步；**补位的 `fromRow` 全是负数** ——
新块一律从盘面上方进场，没有「原地变出来」的分支。
时长表：`swap=140`（规格 120–160 ✅）、`boom=200`（规格 180–220 ✅）、每格下落 `70`（规格 60–80 ✅）。

### 2.3 `prefers-reduced-motion` 不另开瞬变分支 ✅

同一关（第 76 关）开关降速各走一遍：

```
[常规] 42 帧  idle→swap→boom→fall→land→settle
[降速]  7 帧  idle→swap→boom→fall→land→settle
两条轨迹一致 = true
降速下 fall 段出现 1 次、land 段 1 次
stage.trace()（降速） = swap→boom→fall→land→settle
[CALM_TIMINGS] {swapMs:16, boomMs:16, perCellMs:2, staggerMs:0, landMs:16, beltMs:16, settleMs:16}
```

降速只是把每段压到 1 帧，**走的还是同一个状态机**，没有「直接把结果盖上去」的旁路。

### 2.4 七条铁则（战役样本 1 / 76 / 188）

**① 深链**：`initialLevel` 直开三关都进到对应盘面：

```
第   1 关 8×8 · 18 步 · 目标 [{token:1, count:9}]
第  76 关 8×8 · 28 步 · 目标 [{token:2,count:10},{token:0,count:10},{token:4,count:10}]  ← 三色目标
第 188 关 8×8 · 40 步 · 目标 [{token:1, count:22}]
```

**② ③ 真赢 + 真输**：驱动读每格 `.mst-tile` 的 emoji 反推花色，枚举合法相邻交换后按两种策略下：
「盯着目标花色打」与「故意躲开目标花色」；**只有真的扣掉一步才算一次出手**（换错弹回不计）。

| 关卡 | 策略 | 出手 | 结算面板 | 结果 |
| --- | --- | --- | --- | --- |
| 1（18 步） | 盯目标 | 3 | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | **赢** |
| 1 | 躲目标 | 4 | 同上 | **赢** |
| 76（28 步） | 盯目标 | 14 | 同上 | **赢** |
| 76 | 躲目标 | 15 | 同上 | **赢** |
| 188（40 步） | 盯目标 | 40（步数耗尽） | 🔁 再试本关 / 🗺️ 回地图 | **输** |
| 188 | 躲目标 | 8 | 🔁 再玩一次 / 🗺️ 回地图 + 平台 `onWin` 3★ | **赢** |

赢有「下一关 ▶」、输只有「🔁 再试本关」，两套按钮不串。
第 188 关是最后一关，赢的面板没有「下一关」，改由平台
`onWin(3★,「188 关全部消除完毕，你的盘面规划和连锁意识都很到位！」)` 报总通关。

> 第 1 / 76 关这种「怎么打都赢」的观察与第 2 轮一致：第 1 关是 18 步教学关，
> 第 76 关是三色目标各 10 颗、28 步很宽裕，**设计上就很难输**。
> 真输的取证由第 188 关拿到（40 步耗尽真的判输）。不记缺陷。
> 另外本款盘面用的是未定种子的 `Math.random`，每次跑出来的出手数不一样（本轮同一条用例
> 跑三遍分别是 3/14/40 与 16/18/50 等）—— 这是关卡随机性，结论（哪一关会输、按钮长什么样）三遍一致。

**④ 四种玩法各玩到结算：**

- **人机对战**：档位「🐣 新手小云 / 🐰 老手小雨 / 🦊 高手小雷」三档齐全，
  我方出手 52 次 → 结算「🤝 对手先清完了」「3 张订单先清完就赢。总比分 0 : 1。」
- **无尽订单**：出手 16 次打到「🧾 步数用完啦」「清了 1 张订单，是新纪录！」。
  步数轨迹完整可查：`剩14/清0 → 剩13/清0 → … → 剩0`。每清一张订单 +1 步，
  但订单要求随张数抬价，净收益从 +1 慢慢转负 —— **有界设计，最终一定停在「步数用完」，不是卡死**。
  结算后 `endlessBest = 1`；`destroy()` 再重新 mount 进无尽，面板读回「最好 1 张」✅
- **双人同屏**：朵朵 `{w,s,a,d,f}`、星星 `{arrowup,arrowdown,arrowleft,arrowright,l}`，
  两套键位在 `SEAT_KEYS` 里就是分开的两张表。双方各出手 28 次 →
  「🏆 星星赢啦！」「3 张订单先清完就赢。总比分 0 : 1。」

**⑤ `destroy` 泄漏**：进 → 玩 → 退出 → 再进 20 轮：

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  根子节点=0  head 样式=0  待跑定时器=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个
destroy 之后再空跑 60 帧：不抛异常 ✅
```

（`dom.frames.length` 恒为 1 是**走查桩的口径**：这份 `domStub` 的 `cancelAnimationFrame`
只往 `cancelled` 里记一笔、不从队列里摘 —— 累计值每轮 +1 才是「每轮正好销一次」的证据。）

**⑥ 竞态**：出手前剩 28 步，在下落动画中间狂点 100 下，之后剩 27 步 ——
**一次出手只扣 1 步**，动画期间的点击被正确吞掉。
`destroy` 之后再敲、再跑 30 帧：不抛异常，`onWin=0 onLose=0`。

**⑦ 商标 / 红线**：战役三关 + 三个模式入口的界面文字扫 19 个商标词（含 `Candy Crush`
`开心消消乐` `宾果消消消` `梦幻花园` 这些同类竞品名）→ 命中 **0**；
红线词 10 个（广告 / 内购 / 抽卡 / 充值 / 上报 / 血 / 死…）→ 命中 **0**；角色只有朵朵与星星。

**360px 热区**：

| 选择器 | 量法 | 实高 | 判定 |
| --- | --- | --- | --- |
| `.mst-btn`（所有可点按钮） | `min-height:44px`（padding 10px 16px、font 16px） | **44.0px** | ✅ |

棋盘格按 `cellPitch()` 实算：**360px / 8 列 = 45.0px ✅**、360px / 6 列 = 60.0px ✅。

### 2.5 `match-stars` 小结

点名项②三条全部满足：阶段可见（飘空帧 25 / 14 / 66，均 ≠ 0，`fall` 最大偏差 1.00–3.00 格）、
`planGravity`/`planRefill` 行为正确、降速不另开瞬变分支。**瞬变阻断不成立。**
七条铁则全过，360px 热区全部 ≥ 44px。**本款 0 阻断 / 0 严重 / 0 一般**，与第 2 轮一致。

## 3. `rainbow-run` 彩虹跑跑

走查脚本：`src/qa-window2/c3-rainbow-run.test.ts`（**26 例**）。

> 与前两款不同，包 C 后三款的走查脚本**留在库里当回归网**，不删。理由：第 3 节以后要复验的
> 全是「不许回潮」类断言（1.1 的 2.5D 基建零改动、`touchArea` 外扩、红线口径、纪录写盘），
> 这些一删就再没有第二道闸。放在 `src/qa-window2/` 而不是 `src/games/**` 下，
> 既不污染各款自己的用例目录，也照样被 vitest 的 `src/**/*.test.ts` 收进去。
> 产品代码在本节走查期间**零改动**。

跑酷每帧都洒随机数（障碍抖动、粒子、追风云），所以全节统一用 mulberry32 把 `Math.random`
定死（本轮 seed 换成 **2718 / 5772 / 13331 / 60607 / 20260827**），结算话术从
`speechSynthesis` 桩里抓 —— 这是这一款唯一能被外部读到的「结算浮层」。

### 3.1 点名项③-1：`view3d.ts` / `controls.ts` 对 1.1 **0 行 diff** ✅

拿 `git show origin/game-1.1:src/games/rainbow-run/<f>` 取原文逐行比：

```
view3d.ts:    0 行 diff
controls.ts:  0 行 diff
```

**摄像机没有被推倒重来。** 这一条是点名项里最硬的一条，本轮是字节级比对，不是看提交历史。

### 3.2 点名项③-2：`endless.ts` 是 1.1 的**严格超集** ✅

```
1.1 导出 42 个
1.2 导出 56 个
丢失 = 无
```

1.2 只往上加了 14 个导出，1.1 的 42 个**一个都没删、没改名**。
所以「接住 1.1 的 `endless.ts`」成立：老无尽跑法整条还在，新东西是加出来的。

### 3.3 点名项③-3：无 three.js / 无 CDN / 无联网上报 ✅ 0 命中

对全款源码扫 `three`、`import(...)` 外链、`fetch` / `XMLHttpRequest` / `navigator.sendBeacon`、
`http(s)://` 字体与音源：

```
[离线自查] 0 命中
```

### 3.4 点名项③-4：`makeCamera` 只在初始化与尺寸真变时调 ✅

把 `makeCamera` 包一层计数器，挂载后连跑 500 帧、中途改一次画布尺寸：

```
mount 后            = 2   ← 选世界屏 1 次 + 进关 1 次
再跑 300 帧          = 2   ← 一帧都没重建
改成 412x800 再跑 200 帧 = 3   ← 尺寸真变才 +1
参数序列 = 640x480 → 360x640 → 412x800
```

**每帧重建摄像机**这类性能事故不成立；尺寸不变时计数一动不动。

### 3.5 铁则 1：首页 / 深链

```
meta.id=rainbow-run  meta.levels=188=LEVELS.length  meta.modes=[campaign, endless]
meta.ts 里没有一行 import（纯数据 ✅）；meta 的每个值都不是函数
```

不带 `level` 停在选世界屏（`(180, 90)` 点得动「♾️ 无尽彩虹跑」，`api.play("jump")` 有响）；
`?level=76` 直接落到第 76 关开场页（同一坐标不再触发无尽）。
`openCampaignLevel` 喂 `0 / -3 / 9999 / NaN` 一律夹到两端，**不抛**。

### 3.6 铁则 2/3/4：第 1 / 76 / 188 关真赢一次真输一次（样本换成 1 / 76 / 188）

驱动一个键都不按（「摆烂跑」），seed=2718：

| 关 | 结算朗读 | 记星 | 判定 |
| --- | --- | --- | --- |
| 1 | 「青草热身跑跑完啦!得到 2 颗星,下次试试完成小任务!」 | 2 | **真赢** |
| 76 | 「摔了一跤,晕乎乎。没关系,就从这一关重新出发!」 | 0 | **真输** |
| 188 | 「摔了一跤,晕乎乎。没关系,就从这一关重新出发!」 | 0 | 终局关也跑得到结算 |

第 188 关的失败话术扫 `死 / 血 / 残忍 / 杀` → **0 命中**，只鼓励不吓人。

### 3.7 铁则 5：无尽玩到失败并把纪录写盘 ✅

从选世界屏点「♾️ 无尽彩虹跑」→ 开场页再点一下起跑 → 摆烂跑到被追上：

```
收尾朗读 = 这一趟跑了 2928 米!躲障碍、吃糖果、踩滑轨都能把它甩开,下一趟你能跑得更远。
写盘     = {"meters":2928,"coins":0}
destroy 之后重新 mount，读回 meters = 2928 ✅
```

### 3.8 铁则 6：360px 热区（`R2C-R1` 的回归网）✅ 已清且不回潮

这一款是纯画布，没有 CSS 选择器可量，只能量**命中矩形**。第 2 轮修复员的做法是给偏小的
按钮面套一层 `touchArea()` 外扩，本轮把每一处都重新按 44 反推：

| 按钮面 | 面本身 | 判定 |
| --- | --- | --- |
| `btnCollection` face | 38×34（**两个方向都不够**） | 过 `touchArea` → ✅ |
| `btnBack` face | 62×30 | 过 `touchArea` → ✅ |
| `btnBack` face（另一屏） | 62×28 | 过 `touchArea` → ✅ |
| `btnEndless` | `cw×ch` 本身就够大 | ✅ 不靠外扩 |

另在 360px 选关屏上把「◀ 地图」贴着左上角**真点一遍**：外扩后的命中框仍落在画布内，
`y=4` 与 `y=46` 两个位置都点得动（每次点之前把上一份实例干净销毁，避免串场）。
**`R2C-R1` 四处全部闭合，没有回潮。**

### 3.9 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  画布监听=0  根子节点=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个
destroy 连调两次：不抛
destroy 之后再推键盘事件：不抛（事件带 preventDefault 桩）
```

### 3.10 铁则 8：商标 / 红线（`R2C-R2` 的回归网）✅ 0 命中

```
[商标红线] 0 命中
```

第 2 轮记的两处（`guide.ts:138`「给大王掉血」、`logic.ts:1061` 关名《广告墙走廊》）
本轮**逐字重扫，都已改成卸护甲 / 霓虹招牌口径，没有回潮**。攻略文件单独再扫一遍
`血 / 死 / 伤害`，失败口径只鼓励。

> 一条口径说明：红线词表里有「上报」，本款源码里出现的是「**成绩上报**」这一串，
> 指的是把成绩交给平台存档（`save.ts` 本地写盘），不是联网遥测 —— 3.3 节的联网自查
> 已经独立证明了「一个网络 API 都没有」。故扫描时对「成绩上报」放行，其余「上报」照扫。

### 3.11 `rainbow-run` 小结

点名项③四条全部拿到字节级 / 计数级证据：`view3d.ts`、`controls.ts` **0 行 diff**，
`endless.ts` 严格超集，无 three.js，`makeCamera` 不每帧重建。
七条铁则全过。`R2C-R1`（热区 4 处）、`R2C-R2`（红线词 2 处）**均已清且不回潮**。
**本款 0 阻断 / 0 严重 / 0 一般**；`R17` 的最终结论见第 7 节。

## 4. `ocean-munch` 海底大胃王

走查脚本：`src/qa-window2/c4-ocean-munch.test.ts`（**13 例**）。同样留作回归网，产品代码零改动。

这一款的鱼是 `arc` / `ellipse` 直接画出来的，驱动读不到坐标 —— 只能当「瞎子」按固定节奏
画大圈扫海。所以第 1 关的取胜证据用**扫描式**给：4 个 seed × 2 种绕圈周期共 8 局。

### 4.1 铁则 1：首页三张卡与深链 ✅

```
meta.id=ocean-munch  meta.levels=188=LEVELS.length=TOTAL_LEVELS
meta.modes=[campaign, endless, versus]   meta.platform=both
meta.ts 无 import、每个值都不是函数 ✅
```

不带 `level` 停在首屏（第 2 张卡「深海马拉松」点得动）；`?level=76` 直开第 76 关引导页
（同一坐标不再触发无尽）。

### 4.2 铁则 2/3/4：第 1 / 76 / 188 关真赢一次真输一次

**第 1 关 · 瞎子绕圈 8 局：**

```
seed  2718 周期 12: 被啃回岸上      seed  2718 周期 45: 通关
seed  5772 周期 12: 通关            seed  5772 周期 45: 被啃回岸上
seed 13331 周期 12: 通关            seed 13331 周期 45: 被啃回岸上
seed 60607 周期 12: 被啃回岸上      seed 60607 周期 45: 被啃回岸上
通关 3/8
样本结算 = 第一口小鱼通过啦!得到 1 颗星,吃了 9 条鱼,真棒!
```

**真赢一次成立**（3/8）。顺带这也是第 1 章的难度参照：完全不看屏幕地乱绕，
八局里能过三局 —— 教学关不是白送，但也不至于劝退。

| 关 | 打法 | 结算朗读 | 判定 |
| --- | --- | --- | --- |
| 76 | 站着不动 | 「小鱼晕乎乎。没关系,这片海再游一次就好!」 | **真输** |
| 188 | 站着不动 | 同上 | 终局关也打得到结算 |

第 188 关失败话术扫 `死 / 血 / 残忍 / 杀` → **0 命中**。

### 4.3 点名项④：无尽 + 纪录存档 ✅（本款点名项的正题）

首屏点「深海马拉松」→ 一路泡到系统判失败：

```
收尾朗读 = 这一趟潜到 442 米!被大鱼碰一下会掉一块,晕乎乎回岸上休息一会儿就好,
           下次先躲开再找小鱼吃。
recordEndlessBest 调用次数 = 1
参数 = ["ocean-munch", 442]
destroy 之后重新 mount：save.getGameProgress("ocean-munch").endlessBest = 442 ✅
```

纪录语义另外单独量一遍：

```
recordEndlessBest("ocean-munch", best+400) → best+400   （能涨）
recordEndlessBest("ocean-munch", 1)        → best+400   （不倒退）
recordEndlessBest("ocean-munch", NaN)      → best+400   （脏数据不污染）
getGameProgress("rainbow-run").endlessBest ≠ best+400   （不串到别款名下）
```

**点名项④两条（无尽玩到失败 / 纪录写盘并读得回来）都成立。**

### 4.4 铁则 5：对战玩到结算 ✅

```
选第 1 档下水，60 秒到点：
结算朗读 = 你 2 · 迷糊鱼 74。下次盯着扎堆的小鱼连着吃,长得比它快!
```

（驱动是瞎子，输得很难看是正常的 —— 要的是「对战这条路走得到结算面板」。）

### 4.5 点名项④-4：前 99 关参数相对 1.1 不漂 ✅ = 0

```
1.1 取到 99 关
漂移 = 0
```

逐字段（关名 / 目标 / 时限 / 鱼群配置 / 星级线）比对，一条都没动。

### 4.6 铁则 6：360px 热区（`R2C-O1` 的回归网）✅ 已清且不回潮

第 2 轮记的 6 个导航类按钮，本轮逐个重量：

| 按钮面 | 面本身 | 判定 |
| --- | --- | --- |
| `btnDex` face | 112×34 | 过 `touchArea` → ✅ |
| `btnDex` face（窄屏档） | 110×30 | 过 `touchArea` → ✅ |
| `btnBack` face | 76×36 / 74×30 / 62×30 / 80×34 | 四处全过 `touchArea` → ✅ |
| 选关格子 / 玩法卡 | `cw × Math.max(44, ch)` 兜底还在 | ✅ |

另把首屏「📖 图鉴」按外扩后的边界真点一遍：面本来只有 34px 高，外扩后
`y=4` 与 `y=46` 都点得动。**`R2C-O1` 六处全部闭合。**

### 4.7 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  画布监听=0  根子节点=0
累计 cancelAnimationFrame = 1 / 10 / 20
Esc 不吃、留给壳层暂停 ✅（本款不自己接管暂停键）
```

### 4.8 铁则 8：商标 / 红线 + 离线 ✅ 0 命中

```
[商标红线] 0 命中     [离线自查] 0 命中
```

### 4.9 `ocean-munch` 小结

点名项④（无尽 + 纪录存档）拿到完整证据链：泡到系统失败 → `recordEndlessBest` 精确一次 →
存档读得回来 → 只增不减不串号。七条铁则全过，前 99 关 0 漂移，`R2C-O1` 已清且不回潮。
**本款 0 阻断 / 0 严重 / 0 一般。**

## 5. `xiangqi` 朵朵星星象棋

走查脚本：`src/qa-window2/c5-xiangqi.test.ts`（**19 例**）。同样留作回归网，产品代码零改动
（点名项明令不许自己改 `ai.ts`，本节**一个字节都没改**）。

### 5.1 点名项⑤-1：只有一套象棋目录 ✅

```
命中 = xiangqi          （全库 65 个游戏目录里，中国象棋只有这一个）
meta.id=xiangqi  meta.levels=188  meta.modes 四种齐全  meta.ts 纯数据无 import
```

**没有第二套象棋。**

### 5.2 点名项⑤-2：六档三条硬指标严格单调 ✅

```
novice: 层数 0 · 预算  20ms · 延时 320ms
easy  : 层数 1 · 预算  40ms · 延时 380ms
normal: 层数 2 · 预算  90ms · 延时 460ms
hard  : 层数 3 · 预算 200ms · 延时 540ms
master: 层数 4 · 预算 300ms · 延时 640ms
hell  : 层数 5 · 预算 400ms · 延时 760ms
```

搜索层数 / 时间预算 / 思考延时**三条都是一档比一档高，一处并列都没有**。

### 5.3 点名项⑤-3：188 课课课有解、虚标 0 ✅

```
无解 / 虚标 = 0
将死 136 课 · 困毙 52 课
步数分布 = 1 步 92 课 / 2 步 96 课
```

「虚标」的判法：自报 `mateIn=2` 的课，如果 1 步就能赢，就算虚标 —— **0 条**。
困毙（不将军也能赢）单独占 52 课，不是全都只教将死。

### 5.4 点名项⑤-4：换一组 10 课按主线实走（将死 + 困毙都有）✅

本轮抽 **3 / 17 / 39 / 62 / 70 / 76 / 96 / 118 / 140 / 163 / 185**（与第 1、2 轮全不重样）：

| 课 | 自报 | 主线手数 | 实走终局 |
| --- | --- | --- | --- |
| 3 | 1 步 · checkmate | 1 | checkmate ✅ |
| 17 | 1 步 · checkmate | 1 | checkmate ✅ |
| 39 | 1 步 · checkmate | 1 | checkmate ✅ |
| 62 | 2 步 · checkmate | 3 | checkmate ✅ |
| 70 | 2 步 · **stalemate** | 3 | stalemate ✅ |
| 76 | 1 步 · **stalemate** | 1 | stalemate ✅ |
| 96 | 2 步 · **stalemate** | 3 | stalemate ✅ |
| 118 | 2 步 · checkmate | 3 | checkmate ✅ |
| 140 | 2 步 · checkmate | 3 | checkmate ✅ |
| 163 | 1 步 · checkmate | 1 | checkmate ✅ |
| 185 | 2 步 · checkmate | 3 | checkmate ✅ |

**将死 8 课 + 困毙 3 课，实走终局状态与自报的收官方式逐课相符。**

### 5.5 点名项⑤-5：长将三种边界 ✅

```
连将 2 次（不到上限） → 不判负 ✅
连将 4 次（到上限）   → 判 red 负（发起长将的那一方） ✅
中间断一手不将         → 计数清零，不误判 ✅
重复局面到上限         → 判「和」，不是判负 ✅
```

四条边界都对：长将罚发起方、断一手就清零、单纯重复局面走和棋。

### 5.6 铁则 1/2/3/4：第 1 / 76 / 188 课真解一次、真错一次

```
第   1 课 mateIn=1 checkmate · 主线 1 手 → 记 3 星
          上屏「1 步之内收官，一步都没有多走 —— 这就是这一章要练的本事。」
第  76 课 mateIn=1 stalemate · 主线 1 手 → 记 3 星
          上屏「困毙成功！不将军也能赢，这一手很漂亮。」
第 188 课 mateIn=2 stalemate · 主线 3 手 → 记 3 星（同上话术）
```

**真错一次**（第 1 课故意走错，把步数扣光）——界面收场文字：

> 步数用完啦。回想一下：黑将现在能去哪几个点？把将能去的每一个交叉点数一遍，
> 只剩最后一个的时候，那里就是你……

失败只讲思路、不批评人。

### 5.7 铁则 5：四种玩法各自进得去 ✅

自由对战屏实抓的界面文字：

> 🤝 和谁下（六档，从小象学步到星海棋神） 🌱 小象学步 🐘 小象过河 🐘 棋灵象 🔥 棋灵象·进阶
> 🌌 银河象王 ⭐ 星海棋神 👫 朵朵 VS 星星 …… 🔴 我拿红棋先走 ⚫ 我拿黑棋后走 开始下棋 ▶
> 🧩 回残局学堂 ✖ 关闭

四个入口齐全：**残局学堂（战役）/ 自由对战人机六档 / 朵朵 VS 星星双人同屏 / 残局连胜**。
残局连胜把最高连胜写进平台 `endlessBest` 的口径还在。

### 5.8 铁则 6：360px 热区（`R2C-X1` 的回归网）✅ 已清且不回潮

含 `@media (max-width:380px)` 那一档一起量（按 CSS 层叠算有效值，只被**真正覆写的属性**替换）：

| 选择器 | 实高 | 判定 |
| --- | --- | --- |
| `.xq-btns button` | **44.0px** | ✅（第 2 轮 38.8–41.6px） |
| `.xq-mode` | **44.0px** | ✅（第 2 轮 42.0–45.0px 参差） |
| `.xq-over-btn` | **44.0px** | ✅（第 2 轮 41.2–44.4px 临界） |
| `.xq-rules-close` | **44.0px** | ✅ |

棋盘交叉点另按手指半径反推：

```
GEOM.cell = 44        MIN_HIT_PX = 44
cssWidth=360 时命中半径 = 24.4（棋盘坐标系）→ 直径 48.8 > 44 ✅
```

**`R2C-X1` 三处全部闭合。**

### 5.9 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮 1 / 轮 10 / 轮 20 一律：win 监听=0  根监听=0  根子节点=0
```

### 5.10 铁则 8：商标 / 红线 + 离线 ✅ 0 命中

```
[商标红线] 0 命中     [离线自查] 0 命中
```

> 口径说明：象棋绕不开「将死」「吃子」这两个术语。扫描时先把
> **「将死」「杀」这类棋理术语**按整词摘出去，再对剩下的文本扫 `血 / 笨 / 蠢` 等词 ——
> 结果仍是 0。也就是说：这一款出现的每一个「死」字都在「将死」里，
> 没有任何一处是流血或死亡描写。

### 5.11 `xiangqi` 小结

点名项⑤五条（单目录 / 多档 AI / 残局）全部拿到独立证据。七条铁则全过，
`R2C-X1` 已清且不回潮。**本款 0 阻断 / 0 严重 / 0 一般**；
`R2C-X2` 与 `X20` 的最终结论见第 7 节。

## 6. 通用检查汇总（五款横向）

| 铁则 | gomoku | match-stars | rainbow-run | ocean-munch | xiangqi |
| --- | --- | --- | --- | --- | --- |
| 1 首页 / 深链 `#/game/<id>` | ✅ | ✅ | ✅ | ✅ | ✅ |
| 2 真赢一次 | ✅ | ✅ | ✅ 第 1 关 | ✅ 8 局中 3 局 | ✅ 三课全解 |
| 2 真输一次 | ✅ | ✅ 第 188 关 | ✅ 第 76 / 188 关 | ✅ 第 76 / 188 关 | ✅ 步数扣光 |
| 3 战役第 **1 / 76 / 188** 关 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 4 每种模式玩到结算 | ✅ 4 种 | ✅ 4 种 | ✅ 2 种 | ✅ 3 种 | ✅ 4 种 |
| 5 360px 热区 ≥ 44px | ✅ 全 44.0 | ✅ 44.0 / 格 45.0 | ✅ 外扩后全过 | ✅ 外扩后全过 | ✅ 全 44.0 |
| 6 `destroy` 20 轮无泄漏 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 7 商标黑名单 + 红线词 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 |

横向另外三条：

| 项 | 结果 |
| --- | --- |
| **离线可玩** | 五款全扫 `three` / CDN 字体 / 外链音源 / `fetch` / `XMLHttpRequest` / `sendBeacon` → **0 命中** |
| **无广告 / 内购 / 抽卡 / 账号** | 五款界面文字 + 源码扫 → **0 命中** |
| **角色只用朵朵 / 星星** | 五款全对；无血、无死亡描写（象棋「将死」按棋理术语放行，见 5.10） |
| **竞态** | gomoku 思考中狂点 30 下不落子；match-stars 下落中狂点 100 下只扣 1 步；后三款 `destroy` 之后再推事件 / 再跑帧一律不抛，`onWin` / `onLose` 保持 0 |
| **无尽可持续** | match-stars 有界收敛于「步数用完」；rainbow-run 2928m 收尾并写盘；ocean-munch 442m 收尾并写盘；xiangqi 残局连胜写 `endlessBest` |

## 7. 遗留项最终结论

### 7.1 第 2 轮修复员声称已清的 6 条 —— 逐条复量，**全部没有回潮**

| 编号 | 款 | 第 2 轮的症状 | 本轮独立复量 | 终态 |
| --- | --- | --- | --- | --- |
| `R2C-G1` | gomoku | `.gmk-btns button` 36.8–39.6px | 全部 **44.0px** | **已清** |
| `R2C-G2` | gomoku | `.gmk-seg button` 40.8–43.6px、`.gmk-mode` 36.0–39.0px | 全部 **44.0px** | **已清** |
| `R2C-R1` | rainbow-run | `btnEndless` 42px、`btnCollection` 38×34、`btnBack` 62×30 / 62×28 | 四处全过 `touchArea`；`btnEndless` 自身已 44 | **已清** |
| `R2C-R2` | rainbow-run | `guide.ts` 「掉血」、`logic.ts` 关名《广告墙走廊》 | 全款红线扫 **0 命中** | **已清** |
| `R2C-O1` | ocean-munch | 6 个导航按钮 < 44px | 六处全过 `touchArea` | **已清** |
| `R2C-X1` | xiangqi | `.xq-btns` 38.8–41.6px 等 3 处 | 全部 **44.0px**（含 380px 那一档） | **已清** |

### 7.2 `R2C-G3`（gomoku · `smart` / `normal` 和棋率）—— 最终结论见 1.5 节

**不阻断发布，保留为一般项转 1.3。** 三组不同 seed 共 72 局里 `smart` 只输 3 局、赢 39 局，
强弱关系没有倒挂；和棋率 25%–47% 的波动是 15 路满盘的样本噪声。要压下来得动 `ai.ts` 的
评分权重 —— 那是第 1 轮修复员刚修好阻断的现场，本轮红线明令不许碰，**没有改一个字节**。

### 7.3 `R2C-X2`（xiangqi · `easy` / `normal` 残局卷同分）—— **是尺子分辨率不足，不是强弱倒挂**

第 2 轮用的是「一步杀考卷」。本轮**换一把更难的尺子**（两步杀考卷，40 道）重量，
同时把旧尺原样复现一遍作对照：

| 档 | **两步杀考卷（新尺）** | 一步杀考卷（旧尺 · 复现） |
| --- | --- | --- |
| novice | 7/40 | 5/40 |
| easy | **8/40** | **40/40** |
| normal | **10/40** | **40/40** |
| hard | 26/40 | 40/40 |
| master | 34/40 | 40/40 |
| hell | **40/40** | 40/40 |

**旧尺在 easy 以上全部打满 40/40 —— 天花板效应，五个档挤在同一格里，谁也分不开。**
换成两步杀之后六档立刻拉开：7 → 8 → 10 → 26 → 34 → 40，**严格单调不降，一处倒挂都没有**。

**最终结论：`R2C-X2` 是量具问题，不是缺陷，就地关闭，不上交主管。**
搜索代码一个字节没改（层数 / 预算 / 延时三条硬指标见 5.2，本来就严格递增）。

### 7.4 第 1 轮 3 条观察项的最终去留

| 编号 | 款 | 本轮独立复量 | 最终结论 |
| --- | --- | --- | --- |
| `R17` | rainbow-run | 第 1 关一个键都不按，5 个 seed 里**过 4 摔 1**，且**5 个 seed 一次都没拿到 3 星**（最高 2 星） | **关闭为「设计如此」，不记缺陷。** 这是 1.1 逐字节继承的教学关：让新手一定能跑完第 1 关，但想拿满星必须真去完成小任务。断言已固化进回归网（过关数 ≥3 且 <5、星数恒 <3），将来若被改成「必过」或「能白拿 3 星」会立刻报警 |
| `G15` | gomoku | 「能力单调」基本功电池从 `normal` 起就满分 —— 与 `R2C-X2` 是同一类天花板效应；1.4 节换的第二把尺（30 个真实中盘局面）已经把六档分开：3.3% / 33.3% / 83.3% / 50.0% / 70.0% | **关闭为量具问题。** 相邻档实战（1.4 节）五对全部单调，强弱关系有独立证据；基本功电池只是题目太简单 |
| `X20` | xiangqi | 上两对相邻档实战和棋多 —— 本轮固定 seed 重量：`master` 执红 vs `hard` **11 胜 0 负 1 未分**（12 局）；`hell` 执红 vs `master` **12 胜 0 负 0 未分**（12 局） | **关闭。** 24 局里只有 1 局 60 手未分胜负，弱档**一局都没赢**。第 1 轮观察到的「大量和棋」在本轮样本上不成立 |

### 7.5 本轮新记

| 编号 | 款 | 级别 | 一句话 |
| --- | --- | --- | --- |
| `R3C-G4` | gomoku | **观察项（不记缺陷）** | 强行打开禁手规则时，188 关里有 6 关（46–51「双冲」系列）解集变空 —— 它们的杀法本身就是双冲四。解局战役在 `index.ts:579` 把 `forbidden:false` 写死，玩家在残局学堂里打不开这个开关，六关全在 1.1 继承的前 99 题里。**战役不可达，不构成缺陷** |

## 8. 缺陷清单与发布结论

### 8.1 本包（点名五项）缺陷计数

| | 数 |
| --- | --- |
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **0**（本轮未新记任何一般项） |
| 观察项 | **1**（`R3C-G4`，战役不可达） |

### 8.2 点名五项逐条交账

| # | 款 | 硬要求 | 本轮证据 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `gomoku` | 解局关 + 菜鸟到地狱，固定 seed 相邻档对下强度单调 | 换的 10 关 `solutionLine()` **实摆到底**、`findWinLine()` 验黑五连；**第三组 seed**（2718/5772/13331）五对相邻档全部单调；第二把尺 30 局面六档分开；前 99 题对 1.1 漂移 0；188 关无解 0 | ✅ **达标** |
| 2 | `match-stars` | 消除→下落→补位三阶段可观察，瞬变即阻断 | 第 42 / 76 / 133 关飘空帧 **25 / 14 / 66**（≠0）；`fall` 段视觉行偏差 1.00–3.00 格连续收敛；`planRefill` 的 `fromRow` 全为负（新块一律从盘外进场）；降速走的是**同一个状态机**，没有瞬变旁路 | ✅ **达标，瞬变阻断不成立** |
| 3 | `rainbow-run` | 接住 1.1 的 `view3d.ts` / `controls.ts` / `endless.ts`，禁止推倒重来、禁止 three.js | `view3d.ts` / `controls.ts` **0 行 diff**；`endless.ts` 42 个老导出一个不丢（严格超集）；three.js / CDN / 联网 **0 命中**；`makeCamera` 500 帧只在尺寸真变时 +1 | ✅ **达标** |
| 4 | `ocean-munch` | 无尽 + 纪录存档 | 泡到系统判失败（442m）→ `recordEndlessBest("ocean-munch", 442)` 精确 1 次 → `destroy` 后读回 442；纪录只增不减、脏数据不污染、不串别款 | ✅ **达标** |
| 5 | `xiangqi` | 只升级已有目录，多档 AI + 残局 | 全库只有一个象棋目录；六档层数 / 预算 / 延时三条严格单调；188 课有解、虚标 0（将死 136 / 困毙 52）；抽 11 课实走终局与自报相符；长将四条边界都对 | ✅ **达标** |

### 8.3 水位

| 项 | 结果 |
| --- | --- |
| 包 C 走查脚本 | **3 文件 / 58 例全绿**（`c3` 26 例 + `c4` 13 例 + `c5` 19 例），**留库当回归网** |
| 产品代码改动 | **0**（本节走查期间没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`） |
| 第 1 / 2 轮报告原文 | **0 字节改动** |

### 8.4 发布结论

**点名五项（`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`）可以随本窗口一起发布。**

理由：五项硬要求逐条拿到独立证据（8.2）；第 2 轮修复员声称已清的 6 条**逐条复量确认没有回潮**；
第 1 轮的 3 条观察项（`R17` / `G15` / `X20`）本轮全部给出关闭结论；
`R2C-X2` 证明是量具分辨率问题、就地关闭；`R2C-G3` 是唯一保留项，
判为**一般 · 不阻断发布 · 转 1.3**（强弱关系无倒挂，只是和棋吃掉了分差）。
本包**没有任何一条要交给主管**。
