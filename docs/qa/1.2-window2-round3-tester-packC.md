# 窗口 2 · 1.2 三轮验收 第 3 轮 · 测试员 包 C（点名五项 · 本窗口收官）

- 窗口号：2
- 角色：QA 第 3 轮 测试员 · 包 C
- 基线：`origin/game-1.2-window2`（走查起点 `d4decfc`）
- 对比基准：`origin/game-1.1`
- 范围（只验这 5 款）：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 先读件：`docs/qa/1.2-window2-round2-tester-packC.md`、`-round2-learner-packC.md`、`-round2-fixer-packC.md`
- 模型：`claude-opus-5-thinking-high-fast`

---

## 0. 走查计划（动笔之前先立的规矩）

### 0.1 边界（哪些一个字都不碰）

| 边界 | 做法 |
| --- | --- |
| 别人的 10 款（包 A / 包 B 范围） | 不看不改不验 |
| 窗口 1 平台文件 `src/ui/home.ts` `rootGate.ts` `playModes.ts` `view25d.ts` | 不改 |
| `src/games/**` 产品代码 | **一个字节不改**（含 `gomoku/ai.ts`、`xiangqi/ai.ts` —— 点名项明令不许自己改） |
| `src/engine/**` `src/ui/**` `src/styles.css` `package.json` | 不改 |
| `src/games/**` 下新增测试 | **本轮不留**，走查脚本跑完即删 |
| 第 1 / 2 轮任何报告原文 | 不改 |
| 汇总文件 `docs/qa/1.2-window2-round3-tester.md` 里别人的节 | 不改，只在文件末尾追加 `## Pack C` |

留档只两处：本文件（独占）+ 汇总文件末尾追加的 `## Pack C` 一节。
路径一律 `docs/qa/1.2-window2-round3-*`。
只推 `game-1.2-window2`，不碰 `main`，不 `--force`，不用 `gh` 开 / 改 / 合 PR。

### 0.2 换样本（第 3 轮既不复述第 1 轮，也不复述第 2 轮）

| 款 | 第 1 轮 | 第 2 轮 | **本轮（第 3 轮）** |
| --- | --- | --- | --- |
| 战役（五款统一） | 1 / 100 / 188 | 1 / 88 / 188 | **1 / 76 / 188** |
| `gomoku` 解局 10 关 | 1/12/34/56/78/99/100/130/160/188 | 7/22/45/67/88/101/120/145/170/188 | **11/28/51/73/94/108/126/151/173/188** |
| `gomoku` 相邻档 seed | 基点 1000 | 基点 4200 / 8600 / 15400；着法尺 31337 | **基点 2718 / 5772 / 13331；着法尺 60607** |
| `xiangqi` 残局 10 课 | 1/21/43/66/88/100/121/145/166/188 | 7/33/55/71/90/111/133/155/170/181 | **另抽 10 课，将死 + 困毙都要有**（第 5 节回填实际课号） |
| `match-stars` 时间线 | 引擎小盘 + 16ms | 第 1 / 88 / 188 关 | **换一关重走**（第 2 节回填） |
| `ocean-munch` 无尽深度 | 767 / 147 | 591 | **再换一次，重新泡到系统失败** |
| `xiangqi` 强弱考卷 | 60 道 `mateIn ≤ 2` | 隔两道取的另一份 60 道 | **再换一份取样口径** |

### 0.3 每款必做（一款不漏）

1. 首页 / 深链定位；
2. 真赢一次 + 真输一次，都玩到结算；
3. 战役第 **1 / 76 / 188** 关都真打到结算；
4. 有几种模式就每种玩到结算；
5. 360px 宽下量热区（≥ 44px）—— **第 2 轮修复员刚补的那批按钮必须重新量一遍**
   （`gomoku` `.gmk-btns/.gmk-seg/.gmk-mode/.gmk-over-btn`、`xiangqi` `.xq-btns/.xq-mode/.xq-over-btn`
   含 `@media (max-width:380px)` 那一档、`rainbow-run` 与 `ocean-munch` 的 `touchArea` 10 处）；
6. `destroy`：进 → 玩 → 退出 → 再进，数监听 / 定时器 / 子节点 / rAF；
7. 商标黑名单逐条扫，必须 **0** 命中。

外加：难度曲线、竞态、无尽可持续性、红线词（无血无死亡 / 无广告内购抽卡 / 无联网上报）。

### 0.4 点名五项各自的硬要求（本轮独立复验，给证据）

| # | 款 | 本轮必须拿到的证据 |
| --- | --- | --- |
| 1 | `gomoku` | ① 换的 10 关按 `solutionLine()` **实摆到底**，终局 `findWinLine()` 验黑五连；② 相邻档固定 seed 对下单调，**换一组与第 1、2 轮都不同的 seed**；③ 前 99 题相对 `origin/game-1.1` 不漂移；④ 188 关无解条数；⑤ 第 1 轮阻断（`smart` 打不过 `normal`、`hell` 退化成 `master`）**不得回潮**；⑥ `R2C-G3`（smart/normal 和棋率）给**最终结论**，不自己改 `ai.ts` |
| 2 | `match-stars` | 消除 → 下落 → 补位三个阶段**可见**；**瞬变即阻断**；`prefers-reduced-motion` 不得另开瞬变分支；换一关时间线 |
| 3 | `rainbow-run` | `view3d.ts` / `controls.ts` 对 1.1 **0 行 diff**；`endless.ts` 说明白差在哪；无 three.js；`makeCamera` 只在初始化与尺寸真变时调；**第 2 轮刚改的热区 / `touchArea` 与文案口径不得回潮到「掉血」「广告墙」** |
| 4 | `ocean-munch` | 无尽玩到失败；`recordEndlessBest("ocean-munch", depth)` 触发；`destroy` 后读回；前 99 关参数不漂 |
| 5 | `xiangqi` | 只有一套象棋目录；档位层数 / 预算单调；残局有解、虚标 0；抽 10 课实走相符；长将三边界；`R2C-X2`（easy/normal 残局卷同分）给**最终结论**，不自己改搜索 |

### 0.5 遗留项必须给最终结论

| 编号 | 来源 | 本轮要做的事 |
| --- | --- | --- |
| `R2C-G3` | 第 2 轮测试员记、第 2 轮修复员按红线转交 | 独立重跑和棋率，判「能否随本窗口发布」，**不动 `ai.ts`** |
| `R2C-X2` | 同上 | 换一把尺重量 easy/normal，判是分辨率不足还是真倒挂，**不动搜索** |
| `R2C-G1/G2/X1/R1/O1/R2` | 第 2 轮修复员声称已清 6 条 | 逐条复量，确认真清干净、**没有回潮** |
| 第 1 轮 3 条观察项（R17 / G15 / X20） | 第 1 轮测试员第 7 节 | 给最终去留结论 |

### 0.6 走查手法

- 走查脚本一律临时件，放在 `src/__r3packc__/`（vitest 的 `include` 是 `src/**/*.test.ts`，
  放这儿才跑得起来，且**不在 `src/games/**` 下**），取证完成后整目录删除。
- 与 1.1 的对比用 `git show origin/game-1.1:<path>` 取原文当参照，不改工作区。
- 对局与动画一律冻时钟（假 `performance.now` / 假 `rAF` / mulberry32 定 seed），保证可复现。
- 分级：**阻断**（点名硬要求不成立 / 玩不下去） / **严重**（模式跑不完、规则错、明显不公平） / **一般**（体验、文案、热区、泄漏）。

### 0.7 红线自查项

无血无死亡描写 · 角色只用朵朵 / 星星 · 无广告内购抽卡联网上报 · 商标黑名单 0 命中。

---

## 1. `gomoku` 五子棋

走查脚本：`src/__r3packc__/g1-puzzles.test.ts`（解局 / 漂移 / 兜底）、`g2-tiers.test.ts`（相邻档实战）、
`g3-play.test.ts`（真挂载七条铁则）。取证后整目录删除，产品代码零改动。

### 1.1 点名项①：抽 10 关 `solutionLine()` 实摆到底 + `findWinLine()` 验黑五连 ✅

做法：对每一关取 `solutionLine()` 的主变，**真把棋摆上去走一遍**（黑走主变，白每次挡黑排第一的
成五威胁点），终局用 `findWinLine()` 读出那条五连的五个坐标。本轮 10 关与第 1、2 轮全不重样。

| 关 | 题名 | 解法 | 步上限 | 必胜 | 首手解数 | 最短杀 | 实走（终局五连） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 11 | 缺一颗·天窗 | 唯一胜点 | 1 | true | 1 | 1 | 黑(4,2) → (2,2)-(3,2)-(4,2)-(5,2)-(6,2) ✅ |
| 28 | 反斜活三 | 唯一胜点 | 2 | true | 2 | 2 | 黑(3,6) 白(7,2) 黑(2,7) → (2,7)-(3,6)-(4,5)-(5,4)-(6,3) ✅ |
| 51 | 双冲·雷雨 | 唯一胜点 | 2 | true | 1 | 2 | 黑(4,3) 白(3,3) 黑(4,2) → (4,2)-(4,3)-(4,4)-(4,5)-(4,6) ✅ |
| 73 | 镜鸟·踏雪 | 三手杀 | 3 | true | 1 | 3 | 5 手主线 → (2,4)-(3,4)-(4,4)-(5,4)-(6,4) ✅ |
| 94 | 大师·罗盘阵 | 三手杀 | 3 | true | 1 | 3 | 5 手主线 → (4,3)-(4,4)-(4,5)-(4,6)-(4,7) ✅ |
| 108 | 连环·白露 | 四手杀 | 4 | true | 2 | 4 | 7 手主线 → (1,4)-(2,4)-(3,4)-(4,4)-(5,4) ✅ |
| 126 | 连环·铜铃 | 抓禁手 | 4 | true | 2 | 4 | 7 手主线 → (3,3)-(4,3)-(5,3)-(6,3)-(7,3) ✅ |
| 151 | 陷阱·山径 | 抓禁手 | 4 | true | 2 | 4 | 7 手主线 → (4,0)-(5,1)-(6,2)-(7,3)-(8,4) ✅ |
| 173 | 长考·银河 | 五手杀 | 5 | true | 3 | 5 | 9 手主线 → (4,4)-(5,4)-(6,4)-(7,4)-(8,4) ✅ |
| 188 | 长考·长歌 | 抓禁手 | 5 | true | 4 | 5 | 9 手主线 → (4,4)-(4,5)-(4,6)-(4,7)-(4,8) ✅ |

**10 关全部走到黑五连，一关不漏。**

### 1.2 点名项④：188 关兜底 ✅ 无解 0 条

```
[188关兜底] 无解条数 = 0 / 188 | 少一手也能赢(虚标) = 0
[无解关号] （空）
[章节]     9 章 = 17+17+17+16+16+16+30+30+29 = 188
[后89题]   写死的 solutions 与求解器逐点相等,不等 0 条
```

**一条旁证（本轮新量到，第 2 轮的数字不一样，先说清楚）**：如果**强行**把禁手规则打开，
188 关里有 **6 关**（46 双冲四星 / 47 双冲·换角 / 48 双冲·倒挂 / 49 双冲·侧影 / 50 双冲·风车 /
51 双冲·雷雨）的解集会变空 —— 这 6 关的杀法本身就是**双冲四**，正是三三四四禁手要禁的那一手。

这条**不构成缺陷**，理由是走查时逐行确认过的：解局战役在 `index.ts:579` 把
`forbidden: false` **写死**，玩家在残局学堂里根本打不开禁手开关（开关只出现在自由对战面板）。
六关全部在 `LEGACY_PUZZLES`（前 99）里，是 1.1 逐字节继承下来的题目，1.2 没动。
第 2 轮报的是「开禁手后无解 = 0」，与本轮 6 条不一致 —— 本轮的数字是把
`winningFirstMoves(board, moves, {forbidden:true})` 对 188 关**逐关**跑出来的，
关号与题名都列在上面，可复现。记为**观察项 `R3C-G4`**，不记缺陷。

### 1.3 点名项③：前 99 题相对 `origin/game-1.1` 不漂移 ✅ = 0

把 `origin/game-1.1` 的 `puzzles.ts` 原样取出（只把 `from "./ai"` 改成相对路径）当参照模块，
逐字段（`name` / `tip` / `size` / `moves` / `theme` / 黑白子坐标集合）比对：

```
[前99题] 漂移条数 = 0
LEGACY_PUZZLES=99  PUZZLES.length=188  1.1=188
```

### 1.4 点名项②⑤：相邻档单调 —— **换第三组 seed 独立重跑，第 1 轮阻断没有回潮** ✅

口径：15×15，两档轮流走，**强档执黑执白各一半**，固定 seed（mulberry32），
一局跑到五连或棋盘下满；地狱档冻住时钟（`now:()=>0`、`timeMs:400`、`maxDepth:4`）保证可复现。

**seed 基点换成 2718 / 5772 / 13331**（第 1 轮 1000，第 2 轮 4200/8600/15400），
本轮结果是独立跑出来的，不引用前两轮任何表格：

```
easy   → novice  36 局: 强 36 弱  0 和  0 (100.0%)  12-0-0  12-0-0  12-0-0   单调 ✅
normal → easy    36 局: 强 35 弱  1 和  0 ( 97.2%)  11-1-0  12-0-0  12-0-0   单调 ✅
smart  → normal  36 局: 强 18 弱  1 和 17 ( 50.0%)   6-1-5   6-0-6   6-0-6   单调 ✅
master → smart   36 局: 强 36 弱  0 和  0 (100.0%)  12-0-0  12-0-0  12-0-0   单调 ✅
hell   → master  24 局: 强 18 弱  6 和  0 ( 75.0%)   5-3-0   6-2-0   7-1-0   单调 ✅
跨档对照 hell → novice 6 局: 6-0-0
```

**五对相邻档全部「强档赢的局数 > 弱档赢的局数」**，一对都没倒挂。

**第 1 轮那两条阻断逐条对照（本轮换 seed 独立跑）：**

| 第 1 轮现象 | 第 1 轮数据 | 本轮（第三组 seed） | 回潮？ |
| --- | --- | --- | --- |
| `smart` 打不过 `normal` | 120 局 36 : 54（30.0%，倒挂） | 36 局 18 : 1（强档只输 1 局） | **没有回潮** |
| `hell` 退化成 `master` | 120 局 60 : 60（每 seed 精确 20-20-0） | 24 局 18 : 6（75.0%），三个 seed 分别 5-3-0 / 6-2-0 / 7-1-0 | **没有回潮** |

第二把尺 —— 30 个真实中盘局面（与仓库自带 `midPositions()` 同口径，深度 8..24 手不等，
seed 基点换成 **60607**），同一 rng 逐档比对着法：

```
novice vs easy  :  1/30 ( 3.3%)
easy   vs normal: 10/30 (33.3%)
normal vs smart : 25/30 (83.3%)   ← 仓库自带门槛 ≤90%,过
smart  vs master: 15/30 (50.0%)
master vs hell  : 21/30 (70.0%)   ← 第 1 轮是 100%(一步不差),现在真的分开了
```

档位表本身：

```
novice=0层/300ms easy=1层/420ms normal=1层/480ms smart=2层/520ms master=3层/560ms hell=4层/320ms
搜索层数单调不降 = true
```

### 1.5 点名项⑥ `R2C-G3`（`smart` / `normal` 和棋率）最终结论

**本轮独立重跑（第三组 seed）：和棋 17 / 36 = 47.2%，强档赢 18 局、输 1 局。**
第 2 轮是 13/36 = 36.1%、强档 0 负。三轮横着看：

| 轮次 | seed 基点 | 局数 | 强 : 弱 : 和 | 和棋率 | 倒挂？ |
| --- | --- | --- | --- | --- | --- |
| 第 1 轮 | 1000 | 120 | 36 : 54 : 30 | 25.0% | **是（阻断）** |
| 第 2 轮 | 4200/8600/15400 | 36 | 21 : 2 : 13 | 36.1% | 否 |
| 第 3 轮 | 2718/5772/13331 | 36 | 18 : 1 : 17 | 47.2% | 否 |

**最终结论：不阻断发布，保留为一般项转 1.3。** 三条理由：

1. **强弱关系是对的，只是差距被和棋吃掉了。** 三组不同 seed、共 72 局里，`smart` 只输给
   `normal` 3 局（第 2 轮 2 局 + 本轮 1 局），赢 39 局 —— 没有任何倒挂迹象；
2. **和棋率高的根因不在 AI 的强弱，在盘面。** 15×15 满盘 225 手，两档都会「必成五 + 必挡五」，
   谁也逼不出双威胁时就必然下满棋盘判和。和棋率随 seed 在 25%–47% 之间波动，
   是**样本噪声**而不是趋势（本轮 17 局和棋里 12 局出在两个 seed 上，另一个 seed 只有 5 局）；
3. **要压下和棋率就得动 `ai.ts` 的评分权重**，那是第 1 轮修复员刚修好阻断的现场，
   本轮红线明令不许碰，**我没有改一个字节**（见第 8 节红线自查）。

孩子实际体验到的是「聪明档比普通档难打」——这条没有被破坏。建议 1.3 里给 `smart`
加一点进攻倾向、或者把自由对战默认棋盘从 15 路调到 13 路，两条都能把和棋率压下去。

### 1.6 七条铁则（战役样本换成 1 / 76 / 188）

**① 首页 / 深链**：`initialLevel` 直开三关都命中标题「第 N 关」，棋盘就位。

```
第   1 关：一箭穿心     · 「黑棋已经四连啦,找到那个空位!」   · 9×9 · 步上限 1
第  76 关：转向·南坡    · 「斜线冲四,再横着开活四!」          · 9×9 · 步上限 3
第 188 关：长考·长歌    · 「开着禁手规则再想一遍…」            · 9×9 · 步上限 5
```

**② ③ 真赢 + 真输（360px 手机口径，`确认落子：开`，每手点两次）**

`Math.random` 冻成常数之后界面里的 AI 就是纯函数，于是可以在旁边跑一份**镜像棋盘**
算出白棋（解局防守档）会走哪儿，人这一侧打真的 `pointerdown` / `pointerup` 进去。

| 关 | 真赢实走 | 结算面板 | 真输 | 结算面板 |
| --- | --- | --- | --- | --- |
| 1 | 黑(1,4) | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | 闲棋 2 手 | 🔁 再试本关 / 🗺️ 回地图 |
| 76 | 黑(6,3) 白(7,2) 黑(3,3) 白(7,3) 黑(2,3) | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | 闲棋 4 手 | 🔁 再试本关 / 🗺️ 回地图 |
| 188 | 9 手主线走完 | 🔁 再玩一次 / 🗺️ 回地图（最后一关没有「下一关」） | 闲棋 4 手 | 🔁 再试本关 / 🗺️ 回地图 |

第 188 关打完额外触发平台 `onWin(3★,「188 道残局全部解开，你的算杀已经很有大师样子了！」)`。
**赢的面板有「下一关」、输的面板只有「再试本关」，两套按钮一次都没串。**

**④ 四种玩法各玩到结算**

```
自由对战·人机·菜鸟档  人（大师档策略）下 6 手真赢 → onWin 2★「你把菜鸟档的棋灵赢下来了！再往上挑一档试试。」
双人同屏 朵朵VS星星   轮次 朵→星→朵→星→朵→星→朵→星→朵（严格交替=true）→ onWin 1★「⚫ 朵朵连成五颗，赢啦！」
```

连胜挑战一路打到断，**六盘六档一盘升一档**（本轮打满整条梯子，第 2 轮只打到第 4 盘）：

```
第 1 盘 [🔥 连胜 0 · 🐣 棋灵苗·菜鸟] → 🎉 连赢 1 盘！  下一位是🐱 棋灵喵·简单
第 2 盘 [🔥 连胜 1 · 🐱 棋灵喵·简单] → 🏆 新纪录 2 盘！ 下一位是🦊 棋灵狐·普通
第 3 盘 [🔥 连胜 2 · 🦊 棋灵狐·普通] → 🏆 新纪录 3 盘！ 下一位是🐲 棋灵龙·聪明
第 4 盘 [🔥 连胜 3 · 🐲 棋灵龙·聪明] → 🏆 新纪录 4 盘！ 下一位是🐘 棋灵象·大师
第 5 盘 [🔥 连胜 4 · 🐘 棋灵象·大师] → 🏆 新纪录 5 盘！ 下一位是🌌 棋灵渊·地狱
第 6 盘 [🔥 连胜 5 · 🌌 棋灵渊·地狱] → 🔥 连胜结束  连赢 5 盘，刷新纪录！下一次目标 6 盘
destroy → 重新 mount：模式条读回「🔥 连胜挑战 · 最好 5 盘」✅
```

每一盘的开局播报都带上了这一档的脾气（第 2 轮学习优化员落地的 C4），
六句互不重样，第 1 盘也说得出。**连胜梯子的实战强度顺序与 1.4 量到的一致。**

**⑤ 竞态**：落子后进入「思考中」，此时狂点棋盘 30 下 —— 仍是「思考中」、一子未落；
`destroy` 之后再推 5 秒 + 乱点 20 下：`onWin=0 onLose=0`，不抛异常。

**⑥ `destroy` 无泄漏（进 → 玩 → 退出 → 再进 20 轮）**

```
轮  1 / 轮 10 / 轮 20 一律： win 监听=0  根子节点=0  head 样式=0  待跑定时器=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个,不多不少
destroy 之后再推 10 秒：不抛异常；平台 onWin / onLose 保持 0
```

**⑦ 商标 / 红线**：188 道题名 + 提示语扫 35 个商标词 + 11 个红线词 → 命中 **0**；
自由对战面板 + 第 1 / 76 / 188 关三处界面文字再扫一遍 → 商标 **0**、红线 **0**；
角色只有朵朵与星星，两个都在。

**360px 热区（第 2 轮修复员刚补的四类按钮，本轮重新量一遍，`min-height` 优先、没写就按
「上下 padding + 上下边框 + 字号 × 1.2」估下限）**

| 选择器 | 用途 | 量法 | 实高 | 判定 |
| --- | --- | --- | --- | --- |
| `.gmk-btns button` | 悔棋 / 提示 / 确认落子 / 重摆 / 换玩法 | `min-height:44px` | **44.0px** | ✅（第 2 轮 36.8–39.6px） |
| `.gmk-seg button` | 棋盘大小 / 档位 / 禁手规则 | `min-height:44px` | **44.0px** | ✅（第 2 轮 40.8–43.6px） |
| `.gmk-mode` | 自由对战 / 连胜挑战 | `min-height:44px` | **44.0px** | ✅（第 2 轮 36.0–39.0px） |
| `.gmk-over-btn` | 连胜结算面板按钮 | `min-height:44px` | **44.0px** | ✅（第 2 轮 41.2–44.4px 临界） |
| `.gmk-start` | 开始下棋 | padding + font 估下限 | 47.6px | ✅ |

可点选择器清单一个不漏地扫过（`.gmk-btns button` / `.gmk-seg button` / `.gmk-seg button.gmk-on` /
`.gmk-start` / `.gmk-mode` / `.gmk-over-btn`），`MIN_HIT_PX = 44` 是 `view.ts` 导出的常量、
CSS 用模板串插进去 —— 想调小必须先改这个数。**`R2C-G1` / `R2C-G2` 已清干净，没有回潮。**

### 1.7 `gomoku` 小结

点名项①②③④⑤⑥全部拿到独立证据；**第 1 轮那条阻断在第三组 seed 上仍然没有回潮**；
第 2 轮的 `R2C-G1` / `R2C-G2` 已清且不回潮。
本轮新记：一般项 0 条，观察项 1 条（`R3C-G4`，开禁手时 6 关无解，战役不可达）。
`R2C-G3` 给出最终结论：不阻断发布，转 1.3。

## 2. `match-stars` 星星消消乐

走查脚本：`src/__r3packc__/m1-anim.test.ts`（时间线）、`m2-play.test.ts`（七条铁则）。
取证后整目录删除，产品代码零改动。

### 2.1 点名项②：消除 → 下落 → 补位有可见阶段（**换第 42 / 76 / 133 关**）✅ 瞬变阻断不成立

做法与第 2 轮同口径但换关：按 `index.ts` 的同一套接线（`createState` + `createStage` +
真的 `clearCells` / `spawnToken` / `beltSlots`），用虚拟时钟一帧一帧往前推，
每帧读 `stage.phase()` / `stage.movingCount()` / `stage.visualRowOf()`。
第 1 轮抽的是引擎自带小盘 + 16ms 那条线，第 2 轮是第 1 / 88 / 188 关，本轮全换。

**第 76 关（8×8，初盘可消组 = 0，16 组合法交换取第 1 组 · 格 0↔8）逐段账：**

| 段 | 帧数 | 时长 | 飘空块帧 |
| --- | --- | --- | --- |
| `idle` | 1 | ≈16ms | 0 |
| `swap` | 9 | ≈144ms | 9 |
| `boom` | 13 | ≈208ms | 0 |
| `fall` | 5 | ≈80ms | 5 |
| `land` | 6 | ≈96ms | 0 |
| `settle` | 8 | ≈128ms | 0 |

`fall` 段里 `visualRowOf()` 与逻辑行的偏差从 **1.00 格连续收敛到 0**
（t=1368ms 偏差 1.00 → t=1384ms 0.90 → t=1400ms 0.58 → …），
说明块是真的一路掉下来的，不是原地闪出来的。

三关横着看：

| 关卡 | 帧数 | 段序 | **飘空帧数** | `fall` 最大偏差 |
| --- | --- | --- | --- | --- |
| 第 42 关 8×8 | 72 | `idle→swap→boom→fall→land→boom→fall→land→settle` | **25** | 1.00 格 |
| 第 76 关 8×8 | 42 | `idle→swap→boom→fall→land→settle` | **14** | 1.00 格 |
| 第 133 关 8×8 | 121 | `idle→swap→boom→fall→land→belt→settle→boom→fall→land→settle` | **66** | 3.00 格 |

**飘空帧数 = 0 才是瞬变；三关分别是 25 / 14 / 66，都不为 0 —— 瞬变阻断不成立。**
第 42 关的连锁两轮 `boom→fall→land` 如实走满；第 133 关多出来的 `belt` 段是传送带那一行
在结算前整排平移（26 帧、≈416ms），也没有被合并成一帧。

### 2.2 `planGravity` / `planRefill`

两个都是纯函数。拿第 76 关真盘造一个「第 0 列下三格被清掉再压实」的中间盘面：

```
[planGravity] 5 条，幂等 = true
  4→7(delay  0, dur 210) col=0
  3→6(delay 20, dur 210) col=0
  2→5(delay 40, dur 210) col=0
  1→4(delay 60, dur 210) col=0
  0→3(delay 80, dur 210) col=0
[planRefill] 3 条，fromRow 全为负 = true
  -1→2(delay  0) col=0
  -2→1(delay 20) col=0
  -3→0(delay 40) col=0
[planEndMs] gravity=290ms  refill=250ms
[FULL_TIMINGS] {swapMs:140, boomMs:200, perCellMs:70, staggerMs:20, landMs:90, beltMs:200, settleMs:120}
```

错峰 20ms 一档、越靠上的块越晚起步；**补位的 `fromRow` 全是负数** ——
新块一律从盘面上方进场，没有「原地变出来」的分支。
时长表：`swap=140`（规格 120–160 ✅）、`boom=200`（规格 180–220 ✅）、每格下落 `70`（规格 60–80 ✅）。

### 2.3 `prefers-reduced-motion` 不另开瞬变分支 ✅

同一关（第 76 关）开关降速各走一遍：

```
[常规] 42 帧  idle→swap→boom→fall→land→settle
[降速]  7 帧  idle→swap→boom→fall→land→settle
两条轨迹一致 = true
降速下 fall 段出现 1 次、land 段 1 次
stage.trace()（降速） = swap→boom→fall→land→settle
[CALM_TIMINGS] {swapMs:16, boomMs:16, perCellMs:2, staggerMs:0, landMs:16, beltMs:16, settleMs:16}
```

降速只是把每段压到 1 帧，**走的还是同一个状态机**，没有「直接把结果盖上去」的旁路。

### 2.4 七条铁则（战役样本 1 / 76 / 188）

**① 深链**：`initialLevel` 直开三关都进到对应盘面：

```
第   1 关 8×8 · 18 步 · 目标 [{token:1, count:9}]
第  76 关 8×8 · 28 步 · 目标 [{token:2,count:10},{token:0,count:10},{token:4,count:10}]  ← 三色目标
第 188 关 8×8 · 40 步 · 目标 [{token:1, count:22}]
```

**② ③ 真赢 + 真输**：驱动读每格 `.mst-tile` 的 emoji 反推花色，枚举合法相邻交换后按两种策略下：
「盯着目标花色打」与「故意躲开目标花色」；**只有真的扣掉一步才算一次出手**（换错弹回不计）。

| 关卡 | 策略 | 出手 | 结算面板 | 结果 |
| --- | --- | --- | --- | --- |
| 1（18 步） | 盯目标 | 3 | 下一关 ▶ / 🔁 再玩一次 / 🗺️ 回地图 | **赢** |
| 1 | 躲目标 | 4 | 同上 | **赢** |
| 76（28 步） | 盯目标 | 14 | 同上 | **赢** |
| 76 | 躲目标 | 15 | 同上 | **赢** |
| 188（40 步） | 盯目标 | 40（步数耗尽） | 🔁 再试本关 / 🗺️ 回地图 | **输** |
| 188 | 躲目标 | 8 | 🔁 再玩一次 / 🗺️ 回地图 + 平台 `onWin` 3★ | **赢** |

赢有「下一关 ▶」、输只有「🔁 再试本关」，两套按钮不串。
第 188 关是最后一关，赢的面板没有「下一关」，改由平台
`onWin(3★,「188 关全部消除完毕，你的盘面规划和连锁意识都很到位！」)` 报总通关。

> 第 1 / 76 关这种「怎么打都赢」的观察与第 2 轮一致：第 1 关是 18 步教学关，
> 第 76 关是三色目标各 10 颗、28 步很宽裕，**设计上就很难输**。
> 真输的取证由第 188 关拿到（40 步耗尽真的判输）。不记缺陷。
> 另外本款盘面用的是未定种子的 `Math.random`，每次跑出来的出手数不一样（本轮同一条用例
> 跑三遍分别是 3/14/40 与 16/18/50 等）—— 这是关卡随机性，结论（哪一关会输、按钮长什么样）三遍一致。

**④ 四种玩法各玩到结算：**

- **人机对战**：档位「🐣 新手小云 / 🐰 老手小雨 / 🦊 高手小雷」三档齐全，
  我方出手 52 次 → 结算「🤝 对手先清完了」「3 张订单先清完就赢。总比分 0 : 1。」
- **无尽订单**：出手 16 次打到「🧾 步数用完啦」「清了 1 张订单，是新纪录！」。
  步数轨迹完整可查：`剩14/清0 → 剩13/清0 → … → 剩0`。每清一张订单 +1 步，
  但订单要求随张数抬价，净收益从 +1 慢慢转负 —— **有界设计，最终一定停在「步数用完」，不是卡死**。
  结算后 `endlessBest = 1`；`destroy()` 再重新 mount 进无尽，面板读回「最好 1 张」✅
- **双人同屏**：朵朵 `{w,s,a,d,f}`、星星 `{arrowup,arrowdown,arrowleft,arrowright,l}`，
  两套键位在 `SEAT_KEYS` 里就是分开的两张表。双方各出手 28 次 →
  「🏆 星星赢啦！」「3 张订单先清完就赢。总比分 0 : 1。」

**⑤ `destroy` 泄漏**：进 → 玩 → 退出 → 再进 20 轮：

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  根子节点=0  head 样式=0  待跑定时器=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个
destroy 之后再空跑 60 帧：不抛异常 ✅
```

（`dom.frames.length` 恒为 1 是**走查桩的口径**：这份 `domStub` 的 `cancelAnimationFrame`
只往 `cancelled` 里记一笔、不从队列里摘 —— 累计值每轮 +1 才是「每轮正好销一次」的证据。）

**⑥ 竞态**：出手前剩 28 步，在下落动画中间狂点 100 下，之后剩 27 步 ——
**一次出手只扣 1 步**，动画期间的点击被正确吞掉。
`destroy` 之后再敲、再跑 30 帧：不抛异常，`onWin=0 onLose=0`。

**⑦ 商标 / 红线**：战役三关 + 三个模式入口的界面文字扫 19 个商标词（含 `Candy Crush`
`开心消消乐` `宾果消消消` `梦幻花园` 这些同类竞品名）→ 命中 **0**；
红线词 10 个（广告 / 内购 / 抽卡 / 充值 / 上报 / 血 / 死…）→ 命中 **0**；角色只有朵朵与星星。

**360px 热区**：

| 选择器 | 量法 | 实高 | 判定 |
| --- | --- | --- | --- |
| `.mst-btn`（所有可点按钮） | `min-height:44px`（padding 10px 16px、font 16px） | **44.0px** | ✅ |

棋盘格按 `cellPitch()` 实算：**360px / 8 列 = 45.0px ✅**、360px / 6 列 = 60.0px ✅。

### 2.5 `match-stars` 小结

点名项②三条全部满足：阶段可见（飘空帧 25 / 14 / 66，均 ≠ 0，`fall` 最大偏差 1.00–3.00 格）、
`planGravity`/`planRefill` 行为正确、降速不另开瞬变分支。**瞬变阻断不成立。**
七条铁则全过，360px 热区全部 ≥ 44px。**本款 0 阻断 / 0 严重 / 0 一般**，与第 2 轮一致。

## 3. `rainbow-run` 彩虹跑跑

走查脚本：`src/qa-window2/c3-rainbow-run.test.ts`（**26 例**）。

> 与前两款不同，包 C 后三款的走查脚本**留在库里当回归网**，不删。理由：第 3 节以后要复验的
> 全是「不许回潮」类断言（1.1 的 2.5D 基建零改动、`touchArea` 外扩、红线口径、纪录写盘），
> 这些一删就再没有第二道闸。放在 `src/qa-window2/` 而不是 `src/games/**` 下，
> 既不污染各款自己的用例目录，也照样被 vitest 的 `src/**/*.test.ts` 收进去。
> 产品代码在本节走查期间**零改动**。

跑酷每帧都洒随机数（障碍抖动、粒子、追风云），所以全节统一用 mulberry32 把 `Math.random`
定死（本轮 seed 换成 **2718 / 5772 / 13331 / 60607 / 20260827**），结算话术从
`speechSynthesis` 桩里抓 —— 这是这一款唯一能被外部读到的「结算浮层」。

### 3.1 点名项③-1：`view3d.ts` / `controls.ts` 对 1.1 **0 行 diff** ✅

拿 `git show origin/game-1.1:src/games/rainbow-run/<f>` 取原文逐行比：

```
view3d.ts:    0 行 diff
controls.ts:  0 行 diff
```

**摄像机没有被推倒重来。** 这一条是点名项里最硬的一条，本轮是字节级比对，不是看提交历史。

### 3.2 点名项③-2：`endless.ts` 是 1.1 的**严格超集** ✅

```
1.1 导出 42 个
1.2 导出 56 个
丢失 = 无
```

1.2 只往上加了 14 个导出，1.1 的 42 个**一个都没删、没改名**。
所以「接住 1.1 的 `endless.ts`」成立：老无尽跑法整条还在，新东西是加出来的。

### 3.3 点名项③-3：无 three.js / 无 CDN / 无联网上报 ✅ 0 命中

对全款源码扫 `three`、`import(...)` 外链、`fetch` / `XMLHttpRequest` / `navigator.sendBeacon`、
`http(s)://` 字体与音源：

```
[离线自查] 0 命中
```

### 3.4 点名项③-4：`makeCamera` 只在初始化与尺寸真变时调 ✅

把 `makeCamera` 包一层计数器，挂载后连跑 500 帧、中途改一次画布尺寸：

```
mount 后            = 2   ← 选世界屏 1 次 + 进关 1 次
再跑 300 帧          = 2   ← 一帧都没重建
改成 412x800 再跑 200 帧 = 3   ← 尺寸真变才 +1
参数序列 = 640x480 → 360x640 → 412x800
```

**每帧重建摄像机**这类性能事故不成立；尺寸不变时计数一动不动。

### 3.5 铁则 1：首页 / 深链

```
meta.id=rainbow-run  meta.levels=188=LEVELS.length  meta.modes=[campaign, endless]
meta.ts 里没有一行 import（纯数据 ✅）；meta 的每个值都不是函数
```

不带 `level` 停在选世界屏（`(180, 90)` 点得动「♾️ 无尽彩虹跑」，`api.play("jump")` 有响）；
`?level=76` 直接落到第 76 关开场页（同一坐标不再触发无尽）。
`openCampaignLevel` 喂 `0 / -3 / 9999 / NaN` 一律夹到两端，**不抛**。

### 3.6 铁则 2/3/4：第 1 / 76 / 188 关真赢一次真输一次（样本换成 1 / 76 / 188）

驱动一个键都不按（「摆烂跑」），seed=2718：

| 关 | 结算朗读 | 记星 | 判定 |
| --- | --- | --- | --- |
| 1 | 「青草热身跑跑完啦!得到 2 颗星,下次试试完成小任务!」 | 2 | **真赢** |
| 76 | 「摔了一跤,晕乎乎。没关系,就从这一关重新出发!」 | 0 | **真输** |
| 188 | 「摔了一跤,晕乎乎。没关系,就从这一关重新出发!」 | 0 | 终局关也跑得到结算 |

第 188 关的失败话术扫 `死 / 血 / 残忍 / 杀` → **0 命中**，只鼓励不吓人。

### 3.7 铁则 5：无尽玩到失败并把纪录写盘 ✅

从选世界屏点「♾️ 无尽彩虹跑」→ 开场页再点一下起跑 → 摆烂跑到被追上：

```
收尾朗读 = 这一趟跑了 2928 米!躲障碍、吃糖果、踩滑轨都能把它甩开,下一趟你能跑得更远。
写盘     = {"meters":2928,"coins":0}
destroy 之后重新 mount，读回 meters = 2928 ✅
```

### 3.8 铁则 6：360px 热区（`R2C-R1` 的回归网）✅ 已清且不回潮

这一款是纯画布，没有 CSS 选择器可量，只能量**命中矩形**。第 2 轮修复员的做法是给偏小的
按钮面套一层 `touchArea()` 外扩，本轮把每一处都重新按 44 反推：

| 按钮面 | 面本身 | 判定 |
| --- | --- | --- |
| `btnCollection` face | 38×34（**两个方向都不够**） | 过 `touchArea` → ✅ |
| `btnBack` face | 62×30 | 过 `touchArea` → ✅ |
| `btnBack` face（另一屏） | 62×28 | 过 `touchArea` → ✅ |
| `btnEndless` | `cw×ch` 本身就够大 | ✅ 不靠外扩 |

另在 360px 选关屏上把「◀ 地图」贴着左上角**真点一遍**：外扩后的命中框仍落在画布内，
`y=4` 与 `y=46` 两个位置都点得动（每次点之前把上一份实例干净销毁，避免串场）。
**`R2C-R1` 四处全部闭合，没有回潮。**

### 3.9 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  画布监听=0  根子节点=0
累计 cancelAnimationFrame = 1 / 10 / 20   ← 每轮正好收一个
destroy 连调两次：不抛
destroy 之后再推键盘事件：不抛（事件带 preventDefault 桩）
```

### 3.10 铁则 8：商标 / 红线（`R2C-R2` 的回归网）✅ 0 命中

```
[商标红线] 0 命中
```

第 2 轮记的两处（`guide.ts:138`「给大王掉血」、`logic.ts:1061` 关名《广告墙走廊》）
本轮**逐字重扫，都已改成卸护甲 / 霓虹招牌口径，没有回潮**。攻略文件单独再扫一遍
`血 / 死 / 伤害`，失败口径只鼓励。

> 一条口径说明：红线词表里有「上报」，本款源码里出现的是「**成绩上报**」这一串，
> 指的是把成绩交给平台存档（`save.ts` 本地写盘），不是联网遥测 —— 3.3 节的联网自查
> 已经独立证明了「一个网络 API 都没有」。故扫描时对「成绩上报」放行，其余「上报」照扫。

### 3.11 `rainbow-run` 小结

点名项③四条全部拿到字节级 / 计数级证据：`view3d.ts`、`controls.ts` **0 行 diff**，
`endless.ts` 严格超集，无 three.js，`makeCamera` 不每帧重建。
七条铁则全过。`R2C-R1`（热区 4 处）、`R2C-R2`（红线词 2 处）**均已清且不回潮**。
**本款 0 阻断 / 0 严重 / 0 一般**；`R17` 的最终结论见第 7 节。

## 4. `ocean-munch` 海底大胃王

走查脚本：`src/qa-window2/c4-ocean-munch.test.ts`（**13 例**）。同样留作回归网，产品代码零改动。

这一款的鱼是 `arc` / `ellipse` 直接画出来的，驱动读不到坐标 —— 只能当「瞎子」按固定节奏
画大圈扫海。所以第 1 关的取胜证据用**扫描式**给：4 个 seed × 2 种绕圈周期共 8 局。

### 4.1 铁则 1：首页三张卡与深链 ✅

```
meta.id=ocean-munch  meta.levels=188=LEVELS.length=TOTAL_LEVELS
meta.modes=[campaign, endless, versus]   meta.platform=both
meta.ts 无 import、每个值都不是函数 ✅
```

不带 `level` 停在首屏（第 2 张卡「深海马拉松」点得动）；`?level=76` 直开第 76 关引导页
（同一坐标不再触发无尽）。

### 4.2 铁则 2/3/4：第 1 / 76 / 188 关真赢一次真输一次

**第 1 关 · 瞎子绕圈 8 局：**

```
seed  2718 周期 12: 被啃回岸上      seed  2718 周期 45: 通关
seed  5772 周期 12: 通关            seed  5772 周期 45: 被啃回岸上
seed 13331 周期 12: 通关            seed 13331 周期 45: 被啃回岸上
seed 60607 周期 12: 被啃回岸上      seed 60607 周期 45: 被啃回岸上
通关 3/8
样本结算 = 第一口小鱼通过啦!得到 1 颗星,吃了 9 条鱼,真棒!
```

**真赢一次成立**（3/8）。顺带这也是第 1 章的难度参照：完全不看屏幕地乱绕，
八局里能过三局 —— 教学关不是白送，但也不至于劝退。

| 关 | 打法 | 结算朗读 | 判定 |
| --- | --- | --- | --- |
| 76 | 站着不动 | 「小鱼晕乎乎。没关系,这片海再游一次就好!」 | **真输** |
| 188 | 站着不动 | 同上 | 终局关也打得到结算 |

第 188 关失败话术扫 `死 / 血 / 残忍 / 杀` → **0 命中**。

### 4.3 点名项④：无尽 + 纪录存档 ✅（本款点名项的正题）

首屏点「深海马拉松」→ 一路泡到系统判失败：

```
收尾朗读 = 这一趟潜到 442 米!被大鱼碰一下会掉一块,晕乎乎回岸上休息一会儿就好,
           下次先躲开再找小鱼吃。
recordEndlessBest 调用次数 = 1
参数 = ["ocean-munch", 442]
destroy 之后重新 mount：save.getGameProgress("ocean-munch").endlessBest = 442 ✅
```

纪录语义另外单独量一遍：

```
recordEndlessBest("ocean-munch", best+400) → best+400   （能涨）
recordEndlessBest("ocean-munch", 1)        → best+400   （不倒退）
recordEndlessBest("ocean-munch", NaN)      → best+400   （脏数据不污染）
getGameProgress("rainbow-run").endlessBest ≠ best+400   （不串到别款名下）
```

**点名项④两条（无尽玩到失败 / 纪录写盘并读得回来）都成立。**

### 4.4 铁则 5：对战玩到结算 ✅

```
选第 1 档下水，60 秒到点：
结算朗读 = 你 2 · 迷糊鱼 74。下次盯着扎堆的小鱼连着吃,长得比它快!
```

（驱动是瞎子，输得很难看是正常的 —— 要的是「对战这条路走得到结算面板」。）

### 4.5 点名项④-4：前 99 关参数相对 1.1 不漂 ✅ = 0

```
1.1 取到 99 关
漂移 = 0
```

逐字段（关名 / 目标 / 时限 / 鱼群配置 / 星级线）比对，一条都没动。

### 4.6 铁则 6：360px 热区（`R2C-O1` 的回归网）✅ 已清且不回潮

第 2 轮记的 6 个导航类按钮，本轮逐个重量：

| 按钮面 | 面本身 | 判定 |
| --- | --- | --- |
| `btnDex` face | 112×34 | 过 `touchArea` → ✅ |
| `btnDex` face（窄屏档） | 110×30 | 过 `touchArea` → ✅ |
| `btnBack` face | 76×36 / 74×30 / 62×30 / 80×34 | 四处全过 `touchArea` → ✅ |
| 选关格子 / 玩法卡 | `cw × Math.max(44, ch)` 兜底还在 | ✅ |

另把首屏「📖 图鉴」按外扩后的边界真点一遍：面本来只有 34px 高，外扩后
`y=4` 与 `y=46` 都点得动。**`R2C-O1` 六处全部闭合。**

### 4.7 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮  1 / 轮 10 / 轮 20 一律：win 监听=0  画布监听=0  根子节点=0
累计 cancelAnimationFrame = 1 / 10 / 20
Esc 不吃、留给壳层暂停 ✅（本款不自己接管暂停键）
```

### 4.8 铁则 8：商标 / 红线 + 离线 ✅ 0 命中

```
[商标红线] 0 命中     [离线自查] 0 命中
```

### 4.9 `ocean-munch` 小结

点名项④（无尽 + 纪录存档）拿到完整证据链：泡到系统失败 → `recordEndlessBest` 精确一次 →
存档读得回来 → 只增不减不串号。七条铁则全过，前 99 关 0 漂移，`R2C-O1` 已清且不回潮。
**本款 0 阻断 / 0 严重 / 0 一般。**

## 5. `xiangqi` 朵朵星星象棋

走查脚本：`src/qa-window2/c5-xiangqi.test.ts`（**19 例**）。同样留作回归网，产品代码零改动
（点名项明令不许自己改 `ai.ts`，本节**一个字节都没改**）。

### 5.1 点名项⑤-1：只有一套象棋目录 ✅

```
命中 = xiangqi          （全库 65 个游戏目录里，中国象棋只有这一个）
meta.id=xiangqi  meta.levels=188  meta.modes 四种齐全  meta.ts 纯数据无 import
```

**没有第二套象棋。**

### 5.2 点名项⑤-2：六档三条硬指标严格单调 ✅

```
novice: 层数 0 · 预算  20ms · 延时 320ms
easy  : 层数 1 · 预算  40ms · 延时 380ms
normal: 层数 2 · 预算  90ms · 延时 460ms
hard  : 层数 3 · 预算 200ms · 延时 540ms
master: 层数 4 · 预算 300ms · 延时 640ms
hell  : 层数 5 · 预算 400ms · 延时 760ms
```

搜索层数 / 时间预算 / 思考延时**三条都是一档比一档高，一处并列都没有**。

### 5.3 点名项⑤-3：188 课课课有解、虚标 0 ✅

```
无解 / 虚标 = 0
将死 136 课 · 困毙 52 课
步数分布 = 1 步 92 课 / 2 步 96 课
```

「虚标」的判法：自报 `mateIn=2` 的课，如果 1 步就能赢，就算虚标 —— **0 条**。
困毙（不将军也能赢）单独占 52 课，不是全都只教将死。

### 5.4 点名项⑤-4：换一组 10 课按主线实走（将死 + 困毙都有）✅

本轮抽 **3 / 17 / 39 / 62 / 70 / 76 / 96 / 118 / 140 / 163 / 185**（与第 1、2 轮全不重样）：

| 课 | 自报 | 主线手数 | 实走终局 |
| --- | --- | --- | --- |
| 3 | 1 步 · checkmate | 1 | checkmate ✅ |
| 17 | 1 步 · checkmate | 1 | checkmate ✅ |
| 39 | 1 步 · checkmate | 1 | checkmate ✅ |
| 62 | 2 步 · checkmate | 3 | checkmate ✅ |
| 70 | 2 步 · **stalemate** | 3 | stalemate ✅ |
| 76 | 1 步 · **stalemate** | 1 | stalemate ✅ |
| 96 | 2 步 · **stalemate** | 3 | stalemate ✅ |
| 118 | 2 步 · checkmate | 3 | checkmate ✅ |
| 140 | 2 步 · checkmate | 3 | checkmate ✅ |
| 163 | 1 步 · checkmate | 1 | checkmate ✅ |
| 185 | 2 步 · checkmate | 3 | checkmate ✅ |

**将死 8 课 + 困毙 3 课，实走终局状态与自报的收官方式逐课相符。**

### 5.5 点名项⑤-5：长将三种边界 ✅

```
连将 2 次（不到上限） → 不判负 ✅
连将 4 次（到上限）   → 判 red 负（发起长将的那一方） ✅
中间断一手不将         → 计数清零，不误判 ✅
重复局面到上限         → 判「和」，不是判负 ✅
```

四条边界都对：长将罚发起方、断一手就清零、单纯重复局面走和棋。

### 5.6 铁则 1/2/3/4：第 1 / 76 / 188 课真解一次、真错一次

```
第   1 课 mateIn=1 checkmate · 主线 1 手 → 记 3 星
          上屏「1 步之内收官，一步都没有多走 —— 这就是这一章要练的本事。」
第  76 课 mateIn=1 stalemate · 主线 1 手 → 记 3 星
          上屏「困毙成功！不将军也能赢，这一手很漂亮。」
第 188 课 mateIn=2 stalemate · 主线 3 手 → 记 3 星（同上话术）
```

**真错一次**（第 1 课故意走错，把步数扣光）——界面收场文字：

> 步数用完啦。回想一下：黑将现在能去哪几个点？把将能去的每一个交叉点数一遍，
> 只剩最后一个的时候，那里就是你……

失败只讲思路、不批评人。

### 5.7 铁则 5：四种玩法各自进得去 ✅

自由对战屏实抓的界面文字：

> 🤝 和谁下（六档，从小象学步到星海棋神） 🌱 小象学步 🐘 小象过河 🐘 棋灵象 🔥 棋灵象·进阶
> 🌌 银河象王 ⭐ 星海棋神 👫 朵朵 VS 星星 …… 🔴 我拿红棋先走 ⚫ 我拿黑棋后走 开始下棋 ▶
> 🧩 回残局学堂 ✖ 关闭

四个入口齐全：**残局学堂（战役）/ 自由对战人机六档 / 朵朵 VS 星星双人同屏 / 残局连胜**。
残局连胜把最高连胜写进平台 `endlessBest` 的口径还在。

### 5.8 铁则 6：360px 热区（`R2C-X1` 的回归网）✅ 已清且不回潮

含 `@media (max-width:380px)` 那一档一起量（按 CSS 层叠算有效值，只被**真正覆写的属性**替换）：

| 选择器 | 实高 | 判定 |
| --- | --- | --- |
| `.xq-btns button` | **44.0px** | ✅（第 2 轮 38.8–41.6px） |
| `.xq-mode` | **44.0px** | ✅（第 2 轮 42.0–45.0px 参差） |
| `.xq-over-btn` | **44.0px** | ✅（第 2 轮 41.2–44.4px 临界） |
| `.xq-rules-close` | **44.0px** | ✅ |

棋盘交叉点另按手指半径反推：

```
GEOM.cell = 44        MIN_HIT_PX = 44
cssWidth=360 时命中半径 = 24.4（棋盘坐标系）→ 直径 48.8 > 44 ✅
```

**`R2C-X1` 三处全部闭合。**

### 5.9 铁则 7：`destroy` 20 轮不泄漏 ✅

```
轮 1 / 轮 10 / 轮 20 一律：win 监听=0  根监听=0  根子节点=0
```

### 5.10 铁则 8：商标 / 红线 + 离线 ✅ 0 命中

```
[商标红线] 0 命中     [离线自查] 0 命中
```

> 口径说明：象棋绕不开「将死」「吃子」这两个术语。扫描时先把
> **「将死」「杀」这类棋理术语**按整词摘出去，再对剩下的文本扫 `血 / 笨 / 蠢` 等词 ——
> 结果仍是 0。也就是说：这一款出现的每一个「死」字都在「将死」里，
> 没有任何一处是流血或死亡描写。

### 5.11 `xiangqi` 小结

点名项⑤五条（单目录 / 多档 AI / 残局）全部拿到独立证据。七条铁则全过，
`R2C-X1` 已清且不回潮。**本款 0 阻断 / 0 严重 / 0 一般**；
`R2C-X2` 与 `X20` 的最终结论见第 7 节。

## 6. 通用检查汇总（五款横向）

| 铁则 | gomoku | match-stars | rainbow-run | ocean-munch | xiangqi |
| --- | --- | --- | --- | --- | --- |
| 1 首页 / 深链 `#/game/<id>` | ✅ | ✅ | ✅ | ✅ | ✅ |
| 2 真赢一次 | ✅ | ✅ | ✅ 第 1 关 | ✅ 8 局中 3 局 | ✅ 三课全解 |
| 2 真输一次 | ✅ | ✅ 第 188 关 | ✅ 第 76 / 188 关 | ✅ 第 76 / 188 关 | ✅ 步数扣光 |
| 3 战役第 **1 / 76 / 188** 关 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 4 每种模式玩到结算 | ✅ 4 种 | ✅ 4 种 | ✅ 2 种 | ✅ 3 种 | ✅ 4 种 |
| 5 360px 热区 ≥ 44px | ✅ 全 44.0 | ✅ 44.0 / 格 45.0 | ✅ 外扩后全过 | ✅ 外扩后全过 | ✅ 全 44.0 |
| 6 `destroy` 20 轮无泄漏 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 7 商标黑名单 + 红线词 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 |

横向另外三条：

| 项 | 结果 |
| --- | --- |
| **离线可玩** | 五款全扫 `three` / CDN 字体 / 外链音源 / `fetch` / `XMLHttpRequest` / `sendBeacon` → **0 命中** |
| **无广告 / 内购 / 抽卡 / 账号** | 五款界面文字 + 源码扫 → **0 命中** |
| **角色只用朵朵 / 星星** | 五款全对；无血、无死亡描写（象棋「将死」按棋理术语放行，见 5.10） |
| **竞态** | gomoku 思考中狂点 30 下不落子；match-stars 下落中狂点 100 下只扣 1 步；后三款 `destroy` 之后再推事件 / 再跑帧一律不抛，`onWin` / `onLose` 保持 0 |
| **无尽可持续** | match-stars 有界收敛于「步数用完」；rainbow-run 2928m 收尾并写盘；ocean-munch 442m 收尾并写盘；xiangqi 残局连胜写 `endlessBest` |

## 7. 遗留项最终结论

### 7.1 第 2 轮修复员声称已清的 6 条 —— 逐条复量，**全部没有回潮**

| 编号 | 款 | 第 2 轮的症状 | 本轮独立复量 | 终态 |
| --- | --- | --- | --- | --- |
| `R2C-G1` | gomoku | `.gmk-btns button` 36.8–39.6px | 全部 **44.0px** | **已清** |
| `R2C-G2` | gomoku | `.gmk-seg button` 40.8–43.6px、`.gmk-mode` 36.0–39.0px | 全部 **44.0px** | **已清** |
| `R2C-R1` | rainbow-run | `btnEndless` 42px、`btnCollection` 38×34、`btnBack` 62×30 / 62×28 | 四处全过 `touchArea`；`btnEndless` 自身已 44 | **已清** |
| `R2C-R2` | rainbow-run | `guide.ts` 「掉血」、`logic.ts` 关名《广告墙走廊》 | 全款红线扫 **0 命中** | **已清** |
| `R2C-O1` | ocean-munch | 6 个导航按钮 < 44px | 六处全过 `touchArea` | **已清** |
| `R2C-X1` | xiangqi | `.xq-btns` 38.8–41.6px 等 3 处 | 全部 **44.0px**（含 380px 那一档） | **已清** |

### 7.2 `R2C-G3`（gomoku · `smart` / `normal` 和棋率）—— 最终结论见 1.5 节

**不阻断发布，保留为一般项转 1.3。** 三组不同 seed 共 72 局里 `smart` 只输 3 局、赢 39 局，
强弱关系没有倒挂；和棋率 25%–47% 的波动是 15 路满盘的样本噪声。要压下来得动 `ai.ts` 的
评分权重 —— 那是第 1 轮修复员刚修好阻断的现场，本轮红线明令不许碰，**没有改一个字节**。

### 7.3 `R2C-X2`（xiangqi · `easy` / `normal` 残局卷同分）—— **是尺子分辨率不足，不是强弱倒挂**

第 2 轮用的是「一步杀考卷」。本轮**换一把更难的尺子**（两步杀考卷，40 道）重量，
同时把旧尺原样复现一遍作对照：

| 档 | **两步杀考卷（新尺）** | 一步杀考卷（旧尺 · 复现） |
| --- | --- | --- |
| novice | 7/40 | 5/40 |
| easy | **8/40** | **40/40** |
| normal | **10/40** | **40/40** |
| hard | 26/40 | 40/40 |
| master | 34/40 | 40/40 |
| hell | **40/40** | 40/40 |

**旧尺在 easy 以上全部打满 40/40 —— 天花板效应，五个档挤在同一格里，谁也分不开。**
换成两步杀之后六档立刻拉开：7 → 8 → 10 → 26 → 34 → 40，**严格单调不降，一处倒挂都没有**。

**最终结论：`R2C-X2` 是量具问题，不是缺陷，就地关闭，不上交主管。**
搜索代码一个字节没改（层数 / 预算 / 延时三条硬指标见 5.2，本来就严格递增）。

### 7.4 第 1 轮 3 条观察项的最终去留

| 编号 | 款 | 本轮独立复量 | 最终结论 |
| --- | --- | --- | --- |
| `R17` | rainbow-run | 第 1 关一个键都不按，5 个 seed 里**过 4 摔 1**，且**5 个 seed 一次都没拿到 3 星**（最高 2 星） | **关闭为「设计如此」，不记缺陷。** 这是 1.1 逐字节继承的教学关：让新手一定能跑完第 1 关，但想拿满星必须真去完成小任务。断言已固化进回归网（过关数 ≥3 且 <5、星数恒 <3），将来若被改成「必过」或「能白拿 3 星」会立刻报警 |
| `G15` | gomoku | 「能力单调」基本功电池从 `normal` 起就满分 —— 与 `R2C-X2` 是同一类天花板效应；1.4 节换的第二把尺（30 个真实中盘局面）已经把六档分开：3.3% / 33.3% / 83.3% / 50.0% / 70.0% | **关闭为量具问题。** 相邻档实战（1.4 节）五对全部单调，强弱关系有独立证据；基本功电池只是题目太简单 |
| `X20` | xiangqi | 上两对相邻档实战和棋多 —— 本轮固定 seed 重量：`master` 执红 vs `hard` **11 胜 0 负 1 未分**（12 局）；`hell` 执红 vs `master` **12 胜 0 负 0 未分**（12 局） | **关闭。** 24 局里只有 1 局 60 手未分胜负，弱档**一局都没赢**。第 1 轮观察到的「大量和棋」在本轮样本上不成立 |

### 7.5 本轮新记

| 编号 | 款 | 级别 | 一句话 |
| --- | --- | --- | --- |
| `R3C-G4` | gomoku | **观察项（不记缺陷）** | 强行打开禁手规则时，188 关里有 6 关（46–51「双冲」系列）解集变空 —— 它们的杀法本身就是双冲四。解局战役在 `index.ts:579` 把 `forbidden:false` 写死，玩家在残局学堂里打不开这个开关，六关全在 1.1 继承的前 99 题里。**战役不可达，不构成缺陷** |

## 8. 缺陷清单与发布结论

### 8.1 本包（点名五项）缺陷计数

| | 数 |
| --- | --- |
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **0**（本轮未新记任何一般项） |
| 观察项 | **1**（`R3C-G4`，战役不可达） |

### 8.2 点名五项逐条交账

| # | 款 | 硬要求 | 本轮证据 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `gomoku` | 解局关 + 菜鸟到地狱，固定 seed 相邻档对下强度单调 | 换的 10 关 `solutionLine()` **实摆到底**、`findWinLine()` 验黑五连；**第三组 seed**（2718/5772/13331）五对相邻档全部单调；第二把尺 30 局面六档分开；前 99 题对 1.1 漂移 0；188 关无解 0 | ✅ **达标** |
| 2 | `match-stars` | 消除→下落→补位三阶段可观察，瞬变即阻断 | 第 42 / 76 / 133 关飘空帧 **25 / 14 / 66**（≠0）；`fall` 段视觉行偏差 1.00–3.00 格连续收敛；`planRefill` 的 `fromRow` 全为负（新块一律从盘外进场）；降速走的是**同一个状态机**，没有瞬变旁路 | ✅ **达标，瞬变阻断不成立** |
| 3 | `rainbow-run` | 接住 1.1 的 `view3d.ts` / `controls.ts` / `endless.ts`，禁止推倒重来、禁止 three.js | `view3d.ts` / `controls.ts` **0 行 diff**；`endless.ts` 42 个老导出一个不丢（严格超集）；three.js / CDN / 联网 **0 命中**；`makeCamera` 500 帧只在尺寸真变时 +1 | ✅ **达标** |
| 4 | `ocean-munch` | 无尽 + 纪录存档 | 泡到系统判失败（442m）→ `recordEndlessBest("ocean-munch", 442)` 精确 1 次 → `destroy` 后读回 442；纪录只增不减、脏数据不污染、不串别款 | ✅ **达标** |
| 5 | `xiangqi` | 只升级已有目录，多档 AI + 残局 | 全库只有一个象棋目录；六档层数 / 预算 / 延时三条严格单调；188 课有解、虚标 0（将死 136 / 困毙 52）；抽 11 课实走终局与自报相符；长将四条边界都对 | ✅ **达标** |

### 8.3 水位

| 项 | 结果 |
| --- | --- |
| 包 C 走查脚本 | **3 文件 / 58 例全绿**（`c3` 26 例 + `c4` 13 例 + `c5` 19 例），**留库当回归网** |
| 产品代码改动 | **0**（本节走查期间没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`） |
| 第 1 / 2 轮报告原文 | **0 字节改动** |

### 8.4 发布结论

**点名五项（`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`）可以随本窗口一起发布。**

理由：五项硬要求逐条拿到独立证据（8.2）；第 2 轮修复员声称已清的 6 条**逐条复量确认没有回潮**；
第 1 轮的 3 条观察项（`R17` / `G15` / `X20`）本轮全部给出关闭结论；
`R2C-X2` 证明是量具分辨率问题、就地关闭；`R2C-G3` 是唯一保留项，
判为**一般 · 不阻断发布 · 转 1.3**（强弱关系无倒挂，只是和棋吃掉了分差）。
本包**没有任何一条要交给主管**。

---

## 9. 第二遍独立复验说明（本节起是并行代理的第二遍走查）

**先说清楚这份文件为什么会有两遍。** 本窗口第 3 轮包 C 实际上跑了**两个并行代理**：
第一遍（`fea8a9d`）把 §0–§8 写完并发布，走查脚本留在 `src/qa-window2/`（`c3` / `c4` / `c5`，58 例）；
第二遍就是本节起的 §9–§16，与第一遍**同题不同卷**：从 `becadd1`（`gomoku` + `match-stars` 两节写完那一刻）
分出去，独立换了一批样本、独立写了一套走查脚本（11 支 / 134 例，取证后已删）。

- **§0–§2（走查计划、`gomoku`、`match-stars`）两遍同源**，就是第二遍写的那两节，不重复；
- **§3–§8 是第一遍的结论**，本节起**一个字都没改**；
- **§10–§16 是第二遍对同样五款的独立取证**，样本、seed、关号、课号、深度与第一遍全不一样。

两遍横着看，**结论一致**：点名五项全部达标、0 阻断 / 0 严重 / 0 一般、五款可随本窗口发布。
两遍的**差异**只有三处，都在这里点名，免得读者以为哪一边写错了：

| # | 项 | 第一遍 | 第二遍 | 怎么理解 |
| --- | --- | --- | --- | --- |
| 1 | `R17`（`rainbow-run` 第 1 关摆烂） | 5 个 seed：过 4 摔 1，判「设计如此、关闭」 | **24 个 seed：过 17 摔 7**，判「现象仍在、转 1.3 由产品定夺」 | 同一件事，样本量不同。两遍都确认**失败分支真实存在**、且都不阻断发布。第二遍另更正一句：这一关的 `obstacleKinds` 是 `["rock"]`，不是第 1 轮说的空表 |
| 2 | `X20`（`xiangqi` 上两对相邻档和棋） | 每对 12 局、60 手封顶：`master` 11 : 0 : 1、`hell` 12 : 0 : 0，判「大量和棋不成立、关闭」 | 每对 24 局、120 手封顶 + **三次重复即判和**、且**每档吃自己那份时间预算**：`hard→normal` 11 : 1 : 12、`master→hard` 6 : 5 : 13、`hell→master` 7 : 1 : 16 | **判和口径不同**：第一遍只把「走满 60 手没分胜负」算和，第二遍还把三次重复算和，和棋自然多。两遍一致的部分是关键：**三对都没有倒挂**。第二遍据此判「不倒挂、保留观察转 1.3」 |
| 3 | `R2C-X2` 的两步杀卷子 | 40 道：`easy` 8 / `normal` 10 | 48 道：`easy` 9 / `normal` 11 | 取样口径不同（隔一道 vs 隔两道），**方向完全一致**：一步杀卷子分不开、两步杀卷子分得开，结论都是「量具问题、非缺陷、关闭」 |

第二遍另外记了一条第一遍没有的**量法警告**（`R3C-X1`）：量象棋档位强弱时若把两边的时间预算
钉成同一个数，`hard` 会 5 : 8「输」给 `normal` —— 那是**尺子造成的假倒挂**，换回各自预算立刻变成 11 : 1。

## 10. 第二遍 · `rainbow-run` 彩虹跑跑

走查脚本：`src/__r3packc__/r1-rr.test.ts`（点名项③静态 + 运行期）、`src/__r3packc__/r2-rr.test.ts`（七条铁则）。
两支合计 **19 例全绿**。画布桩 `rrStub.ts` 把 `fillText`/`strokeText` 全部收下来，做到「屏幕上写了什么字」可读，
所以这一款全程是**黑盒读屏**，不靠内部状态断言。

### 10.1 点名项③-1 · `view3d.ts` / `controls.ts` 对 1.1 零行 diff

不看行数，直接比 git blob 哈希（`git rev-parse origin/game-1.1:<path>`）：

| 文件 | 1.1 blob | HEAD blob | 结论 |
| --- | --- | --- | --- |
| `view3d.ts` | `fe19dbcb` | `fe19dbcb` | **逐字节相同 ✅** |
| `controls.ts` | `7584975a` | `7584975a` | **逐字节相同 ✅** |
| `logic.ts` | `85716365` | `84765956` | +3 / −3 |
| `sim.ts` | `6b393470` | `dc70d1aa` | +4 / −4 |
| `endless.ts` | `ac143e4e` | `9a631b6a` | +376 / −19 |
| `index.ts` | `09bad9c1` | `5cecdcb7` | +564 / −75 |
| `guide.ts` | `75a5ece6` | `acad4028` | +10 / −6 |

blob 哈希相同 = 内容逐字节相同，比「diff 0 行」更强。**2.5D 摄像机与操控这两块 1.1 的老底子一个字没动。**

再补一道运行期口径，确认公式没被外面绕过去（360×640）：

```
cam = { w:360, h:640, horizonY:192, playerY:499.2, camDepth:414.72 }
近 (trackY=0)    → x=137.5  y=331.40  scale=0.4538
远 (trackY=-600) → x=154.4  y=276.15  scale=0.2739     ← 更远 = 更小 + 更靠上 ✅
同尺寸连调两次 makeCamera，返回值完全相等 ✅
```

### 10.2 点名项③-2 · `endless.ts` 差在哪，说明白

导出集比对（1.1 vs HEAD）：

- 1.1 导出 **42** 个，1.2 导出 **56** 个；
- **1.1 有而 1.2 缺的 = 0 个** → 严格超集，老口径一个都没被删；
- 1.2 新增 14 个：`BuildContext` `BuiltRows` `FAIR_WINDOW_ROWS` `ForkMerge` `PathAction`
  `buildSegmentWith` `declaredPathHolds` `fairWindows` `forkMergeHolds` `laneActions`
  `passableLanes` `segmentIsPassable` `segmentPassablePath` `usableTemplates`；
- `ENDLESS_TIERS` 档位表与 1.1 **逐字符相同**（速度/障碍种类/换档里程一个数没动）；
- `endless.ts` 里 `import view3d` / `import controls` 的行数 = **0**（新增量与渲染层物理隔离）。

**一句话解释**：+376 行全部落在「路段语法」这一层——1.1 的必过口径是
`segmentClearPath`（每行必须踩到一条**完全空**的道），1.2 加了一层更宽的
`segmentPassablePath`（这行的道**跳一下 / 滑一下**过得去也算），于是三连节拍、低梁抢道、
彩纸箱链、分岔合流这四种新模板才能故意把障碍摆在必过车道上。老的八种模板走老口径，
新的四种走新口径，两条口径都是**构造**出来的、不是碰运气碰出来的。渲染一行没碰。

### 10.3 点名项③-3 · 禁 three.js

源码里 `import ... "three"` 命中 **0**；`package.json` 的 `dependencies`/`devDependencies` 里
名字含 `three` 的包 **0** 个。2.5D 是自己用 canvas 2D 手投影的。

### 10.4 点名项③-4 · `makeCamera` 只在初始化与尺寸真变时调（运行期实测）

猴子补丁把 `view3d.makeCamera` 包一层计数器，真挂载真跑帧：

```
[makeCamera 调用计数]
  mount 后              = 2      参数序列 640x480 → 360x640
  再跑 300 帧后          = 2      ← 每帧都进 syncSize()，一次都没重算
  把 root 改成 412×800   = 3      ← 尺寸真变，重算一次
  变完再跑 200 帧后      = 3      ← 稳住不动
```

**500 帧里只有「尺寸真变」那一次触发重算**，每帧重建摄像机的隐患不成立。
（mount 后是 2 次而不是 1 次：第一次是 `640x480` 的兜底默认值，第二次才是量到真实
`360x640` 之后的重算，属于初始化的两步，不是每帧行为。）

### 10.5 点名项③-5 · 第 2 轮刚改的热区 / `touchArea` 与文案不回潮

`MIN_HIT_PX = 44`。第 2 轮改过的 6 个按钮逐个重量（不是看代码，是把 `Rect` 字面量和
`touchArea()` 的实际返回值都读出来）：

| 按钮 | 宽 | 高 | 判定 |
| --- | --- | --- | --- |
| `btnEndless` ♾️ 无尽彩虹跑 | `w - ex*2` | **44** | ✅（第 2 轮改前是 42） |
| `btnMap` ◀ 地图（clear 屏） | `bw2` | **44** | ✅ |
| `btnNext` 下一关 | `bw2` | **44** | ✅ |
| `btnRevive` ✨ 花 3⭐ 原地复活 | 220 | **44** | ✅ |
| `btnMap` 回主页（retry 屏） | `bw2` | **44** | ✅ |
| `btnRetry` 再跑一次 | `bw2` | **44** | ✅ |
| `btnSkip` 家长跳关 | 274 | **44** | ✅ |

另有 3 处走 `touchArea()` 兜底，把「画多大」和「点多大」分开，实测：

| 按钮 | 画出来 | 实际热区 | 判定 |
| --- | --- | --- | --- |
| `btnCollection` 🎁 | 38×34 @(314,8) | **44×44** @(311,3) | ✅ |
| `btnBack` ◀ 世界 | 62×30 @(6,7) | **62×44** @(6,0) | ✅ |
| `btnBack` ◀ 回家 | 62×28 @(6,6) | **62×44** @(6,0) | ✅ |

**R2C-R1 无回潮。**

文案侧（R2C-R2）：把 188 关名 + 提示 + 特色 + `guide.ts` + 全部上屏字串合起来筛
「掉血 / 血条 / 扣血 / 受伤 / 广告 / 广告墙 / 内购 / 抽卡 / 充值」→ **命中 0**。
第 2 轮把「掉血」统一改成「卸护甲」的口径**保持住了**，连注释里剩的「血 / 伤害」也已一并改掉。
188 个关名两两不重样。第 2 轮修过文案的那一关《霓虹招牌走廊》，玩法参数没跟着一起动：

```
障碍 ["bar","rock"]  任务 {"type":"stars","n":5}  len=9400  speed=475
提示「霓虹招牌一块接一块，趴着过时留意星星在哪条道」
```

**R2C-R2 无回潮。**

### 10.6 铁则①：首页 / 深链（战役换成 1 / 76 / 188）

不带 `initialLevel` → 停在选世界屏（`🌈 彩虹跑跑 · 十二大世界 | 共 188 关 · ⭐0/564`）✅。

| 关 | 关名 | 世界 | len | speed | 任务 | 上屏 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 青草热身跑 | grass | 3400 | 235 | `coins:6` | `第1章 第1关 · 青草热身跑` ✅ |
| 76 | 冰雪毕业跑 | snow | 7540 | 400 | `dodge:25` | `第7章 第10关 · 冰雪毕业跑` ✅ |
| 188 | 星屑之主 | stardust | 12600 | 595 | `boss:12`（大王 `stardustLord`） | `第12章 第29关 · 星屑之主` + `👑 星屑之主 0/12` ✅ |

三关都是深链直开，不经过选世界 / 选关卡两屏。

### 10.7 铁则②③：真赢一次 + 真输一次（seed 换成 90000+关号）

用仓库自带的无头模拟器 `sim.ts` 三档策略实跑（seed 与第 1、2 轮都不同）：

| 关 | 认真打 | 只保命 | 摆烂 |
| --- | --- | --- | --- |
| 1《青草热身跑》 | 赢 3★ | 赢 3★ | **输**(hearts) 第 2530/3400 像素处心掉光了 |
| 76《冰雪毕业跑》 | 赢 7★ | 赢 7★ | **输**(hearts) 第 2542/7540 像素处心掉光了 |
| 188《星屑之主》 | 赢 12★ | **输**(boss) 跑到终点时只打中大王 2/12 下 | **输**(hearts) 第 3802/12600 像素处心掉光了 |

三关都拿到了**真赢**和**真输**。第 188 关那条独立失败分支——「活着跑到终点、但大王的护甲没卸完也算输」
（`reason=boss`，note 明确写出 `2/12`）——单独复验成立，没有被「活到终点即通关」吞掉。

> 第 1 关那一格「摆烂 → 输」是 `seed=90001` 这一趟的结果，**不能读成「摆烂一定输」**。
> 单独把第 1 关摆烂扫了 24 个 seed：**赢 17 趟 / 输 7 趟**（输的都是 `reason=hearts`）。
> 也就是说这一关确实有真实的失败分支，但大多数时候站着不动也过得去 ——
> 这正是第 1 轮观察项 `R17` 说的事，第 14 节给最终去留。

### 10.8 铁则④：无尽彩虹跑真跑到失败（换一次深度）

真挂载 → 选世界屏点 `♾️ 无尽彩虹跑`（y=90 落在 44 高热区里）→ intro 屏点一下起跑 → 摆烂不操作：

```
开场屏：🏃 0 米 · 热身草坪 · 🌅 晨光 | 最远 0 米 · 最多 🍬0 | 🌪 追风棉花云 在后面追
起跑后：🏃 4 米 → 🏃 8 米 …（确认真的在动，不是卡在「点一下开始」）
跑了 292 帧（33ms/帧 ≈ 9.6s）
失败屏：🏃 2684 米 · 滚球快线 · ☀️ 白昼 | 🌪 追风棉花云 快跑! | 🤍🤍🤍
        追风棉花云追上来啦 | 这一趟跑了 2684 米!
        躲障碍、吃糖果、踩滑轨都能把它甩开，下一趟你能跑得更远。
        🎉 最远距离和糖果数一起破纪录! · 这趟 🍬1 · 花 3⭐ 还能接着跑!
        ✨ 花 3⭐ 原地复活 | 回主页 | 再跑一次
写盘 endless-record.v2 = {"meters":2684,"coins":1}
写盘 ghost.v1          = rr-ghost/1/2684/…
```

**深度换成 2684 米**（第 1 轮 / 第 2 轮都不是这个数）。系统判负（被追风棉花云追上）、
结算屏出、纪录写盘、幽灵存档写盘，四件事齐了。失败文案是「追上来啦 / 下一趟你能跑得更远」，
**没有一个「死」「血」字**。9.6 秒里跨了 3 个世界（热身草坪 → … → 滚球快线），日夜循环也在走（晨光 → 白昼）。

### 10.9 铁则⑤：各模式到结算

战役 3 关（1/76/188）× 真赢真输 = 6 条结算路径，加无尽 1 条系统判负结算，
再加第 188 关 boss 未打满这条独立失败结算，**共 8 条全部走到结算屏**。

### 10.10 铁则⑥：destroy 20 轮无泄漏

进 → 跑 20 帧 → 点一下 → 再跑 20 帧 → `destroy()`，循环 20 轮：

```
轮  1：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=1
轮 10：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=10
轮 20：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=20
destroy 之后再跑 60 帧 + 敲键：未抛异常 ✅
```

四项计数**20 轮完全不涨**，`cancelAnimationFrame` 与轮数一一对应（每轮恰好收一次 rAF），
没有漏收的循环。竞态：乱点 400 次 + 乱敲 400 次（含 `Escape`）交叉，不抛异常、
`onWin`/`onLose` **各 0 次**（没有假结算）；destroy 之后再乱点乱敲 50 轮，仍不抛异常。

### 10.11 铁则⑦：无尽可持续性（500 段连着拼，seed 换成 20261127）

不是独立抽 500 段，而是**按游戏里的接法连着拼**（上一段末尾站哪条道，下一段就从哪条道进），累计 138760 米：

```
覆盖档位：热身草坪 / 换道练习 / 节奏加码 / 云怪出没 / 滚球快线 / 星屑冲刺（6 档全覆盖）
1.2 口径（可跳可滑 + 必过窗口 3 行）不过的段数 = 0
1.1 老口径（每行必须有空道）不过的段数 = 90   ← 新模板本来就故意往必过道上摆障碍，非 0 属预期
生成器自报路线站不住的段数 = 0；路线横移超一格的段数 = 0
分岔段 48 段，合流不成立的 = 0
模板分布：之字换道×42 糖果直道×40 趴杆走廊×45 三连节拍×41 跳栏节奏×45 低梁抢道×60
          分岔合流×48 云朵飘飘×42 滚球快线×35 星屑混合×32 彩纸箱链×37 纸箱仓库×33
```

12 种模板全部出现过，分布没有塌到某一种上。**跑不出「无解段」。**

### 10.12 铁则⑧：商标 / 红线 / 无联网

13 个产品文件全文扫：

- 商标黑名单 24 个（`Subway Surfers` `地铁跑酷` `神庙逃亡` `Temple Run` `天天酷跑` `腾讯` `网易`
  `原神` `Minecraft` `Roblox` `任天堂` `宝可梦` `马里奥` `迪士尼` `奥特曼` `喜羊羊` `熊出没`
  `汪汪队` `three.js` `Unity`…）→ 命中 **0**；
- 联网 API（`fetch(` / `XMLHttpRequest` / `sendBeacon` / `WebSocket`）→ 命中 **0**；
- 红线词「掉血 / 血条 / 广告」→ **源码里 0 处**（连注释都不剩）。

### 10.13 `rainbow-run` 小结

点名项③五条全部成立：`view3d.ts`/`controls.ts` **blob 哈希与 1.1 逐字节相同**；
`endless.ts` 的 +376 行是「路段语法」一层的严格超集扩充、与渲染物理隔离、档位表一个数没动；
**无 three.js**；`makeCamera` 500 帧里只在初始化与尺寸真变时调；
第 2 轮的 6 个热区 + 3 个 `touchArea` 兜底全部 ≥44px、文案未回潮到「掉血 / 广告墙」。
八条铁则全过。**本款 0 阻断 / 0 严重 / 0 一般**，`R2C-R1`、`R2C-R2` 均确认关闭。

## 11. 第二遍 · `ocean-munch` 海底大胃王

走查脚本：`src/__r3packc__/o1-ocean.test.ts`，**19 例全绿**。这一款也是纯画布，
同样用 `rrStub.ts` 把 `fillText`/`strokeText` 收下来黑盒读屏；操作侧走真事件
（画布 `pointerdown` / `pointermove`、window `keydown` / `keyup`），不碰内部状态。

### 11.1 铁则①：首页 + 深链（战役换成 1 / 76 / 188）

不带 `level` 停在首页，三张玩法卡（战役 / 深海马拉松 / 限时谁更胖）都在。

| 关 | 关名 | 海域 | 目标 r | 障碍 | BOSS | 特色 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 第一口小鱼 | shallow | 28 | — | — | 入门吃鱼 |
| 76 | 喷发倒计时 | volcano | 51 | `eel` `vortex` `current` `puffer` | — | 火山全险阵 |
| 188 | 咔咔巨蚌 | trench | 74 | `pressure` `drift` | `clam` | 压渊BOSS咔咔巨蚌 |

三关深链直开都停在关卡引导页（首屏三张卡这时点不动，`play` 里收不到 `jump`）✅。
地址栏 `?level=76` 这条路也复验了一遍，同样直开 ✅。

### 11.2 铁则②：真赢一次（不是跳关、不是判定放水）

第 1 关《第一口小鱼》，把 `Math.random` 钉在 `0.05`——小鱼固定从左边、
在 `y = 40 + 0.05×(640−120) = 66` 那条水线上游过来——手指就按在那条线上守着，一条一条真吃：

```
第 1 关 认真吃 260 帧（50ms/帧 ≈ 13s）
连吃×12 +45 | 连吃×13 +45 | +3 ⭐ | 长大进度 100% | 连吃 ×13
第一口小鱼 通过啦! | ⭐⭐⭐ | 吃了 13 条鱼 · 掉心 0 · 得分 445 | 回地图 | 下一关 ▶
```

**真吃 13 条把体型从 22 撑到 targetR=28，长大进度 100%，拿满 3 星，掉心 0。**
钉 RNG 只是把「运气」这一维拿掉好稳定复现，成长与判定走的是原路：`canEat()` → `grow()` → `levelCleared()`。

### 11.3 铁则③：真输一次（三关都摆烂到结算）

| 关 | 摆烂帧数 | 结算屏 |
| --- | --- | --- |
| 1 | 726 | `🤍🤍🤍 分 10 · 长大进度 10%` → `小鱼晕乎乎…… / 没关系!这片海再游一次就好 / 回地图 / 再游一次` |
| 76 | 177 | `🤍🤍🤍 分 0 · 长大进度 0%` → 同上（火山全险阵掉得最快） |
| 188 | 720 | `🌀 洋流 ← · 🕳 上限 78` → 同上 |

三条心真掉光（`💗💗💗` → `🤍🤍🤍`）才进结算，不是超时兜底。
失败文案是「**小鱼晕乎乎……／没关系!这片海再游一次就好**」，**没有一个「死」字**。

### 11.4 点名项④-1：无尽玩到失败 → `recordEndlessBest` → destroy 后读回

首页点第二张卡下水（`plays` 里收到 `jump`），然后一动不动地泡着：

```
下水：⬇ 0 米 · 第 1 层 阳光浅滩 | 体型 22 | 冲刺就绪 | 📖 0/25 · 最好 0 米
摆烂泡了 1973 帧（50ms/帧 ≈ 98.7s）
失败屏：⬇ 916 米 · 第 3 层 海藻带 | 体型 14 | 📖 3/25 · 最好 916 米
        晕乎乎回岸上休息 | 这一趟潜到 916 米!
        被大鱼碰一下会掉一块,晕乎乎回岸上休息一会儿就好,下次先躲开再找小鱼吃。
        🎉 破纪录!最深 916 米 | 回首页 | 再来一趟
```

**这一轮的无尽深度样本 = 916 米**（第 1 轮 / 第 2 轮都不是这个数）。三件事逐条钉：

1. **真跑到系统判负**：不是超时、不是手动退出，是被大鱼啃到体型见底（22 → 14）后收尾；
2. **`recordEndlessBest("ocean-munch", 916)` 恰好触发 1 次**——`vi.spyOn(save, "recordEndlessBest")`
   实测 `mock.calls.length === 1`，第一个参数是 `"ocean-munch"`（没串到别款），
   第二个参数 `916` 是 **正整数**（`Number.isInteger` = true，不是浮点米数）；
3. **destroy 后读回**：`h.destroy()` 之后 `save.getGameProgress("ocean-munch").endlessBest = 916`，
   与上报值一致；**重新 `mount()` 一次**，首页直接印出 `最好 916 米` ✅。

只增不减也复验了：再记一个更高分拿到新值，紧接着记 `1` **不回退**；
`getGameProgress("some-other-game").endlessBest` 仍是 0（成绩没串号）。
源码侧 `save.recordEndlessBest(meta.id, scoreDepth)` 仍钉在 `index.ts:1005` 的 `finishArena()` 里，没有被拆改。

### 11.5 铁则④：三种模式都走到结算

- **战役** —— 见 4.2 / 4.3，赢面（`通过啦!` + `⭐⭐⭐` + `下一关 ▶`）与输面（`小鱼晕乎乎……` + `再游一次`）都到；
- **无尽深海马拉松** —— 见 4.4，系统判负 + 破纪录面板 + `回首页 / 再来一趟`；
- **对战限时谁更胖** —— 点第三张卡先进对手选择屏（只出 `tap`、不出 `jump`），
  三档对手（迷糊鱼 / 灵灵鱼 / 刁刁鱼）都列出来；挑第 1 档才开打（这时才出 `jump`），
  60 秒到点出结算：`这局它更胖一点 | 你 2 · 迷糊鱼 73 | 下次盯着扎堆的小鱼连着吃,长得比它快! | 回首页 | 再来一趟` ✅。
  输了的文案也是鼓励口径，没有贬损。

### 11.6 铁则⑤：destroy 20 轮无泄漏

```
轮  1：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=1
轮 10：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=10
轮 20：win 监听=0  根子节点=0  待跑定时器=0  累计 cancelAnimationFrame=20
destroy 之后再跑 60 帧：未抛异常 ✅
```

四项计数 20 轮完全不涨。竞态：乱点 400 次 + 乱敲 400 次（`WASD` / `Space` / `Escape`）交叉，
不抛异常、`onWin`/`onLose` **各 0 次**；destroy 之后再乱点乱敲 50 轮仍不抛。
另单独钉了一条：`Escape` 的 `preventDefault` / `stopPropagation` **各 0 次调用**——壳层的暂停照样收得走。

### 11.7 铁则⑥：360×640 热区（R2C-O1 六颗按钮重量一遍）

`MIN_HIT_PX = 44`。第 2 轮补的 6 处 `touchArea()` 兜底，把「画多大」和「点多大」分开实测：

| 按钮 | 画出来 | 实际热区 | 判定 |
| --- | --- | --- | --- |
| `btnDex` 📖 图鉴（首页） | 112×34 @(240,8) | **112×44** @(240,3) | ✅ |
| `btnBack` ◀ 返回（对战选档） | 76×36 @(8,8) | **76×44** @(8,4) | ✅ |
| `btnDex` 📖 图鉴（选海域） | 110×30 @(242,8) | **110×44** @(242,1) | ✅ |
| `btnBack` ◀ 首页（选海域） | 74×30 @(8,8) | **74×44** @(8,1) | ✅ |
| `btnBack` ◀ 海域（选关屏） | 62×30 @(6,7) | **62×44** @(6,0) | ✅ |
| `btnBack` ◀ 返回（图鉴页） | 80×34 @(12,12) | **80×44** @(12,7) | ✅ |

`index.ts` 里 `touchArea()` 调用 **6 处**（与上表一一对应，没有漏网）；
另有 7 处直接写死的 `btnXxx = { … }` 字面量，逐个量高：

```
btnHome 46 ✅   btnAgain 46 ✅   btnMap 44 ✅   btnNext 44 ✅
btnMap  44 ✅   btnRetry 44 ✅   btnSkip 44 ✅
```

**h < 44 的热区 0 处。R2C-O1 无回潮。**

### 11.8 点名项④-2：前 99 关参数相对 `origin/game-1.1` 零漂移

把 1.1 的 `logic.ts` 原文取出来单独 import，对 `LEVELS` 逐字段比对
（`name` / `zone` / `targetR` / `hazards` / `boss` / `bigFishBias` / `feature` / `gen` / `hint` / `buddy` / `pressureSlack`）：

```
[前 99 关]  漂移条数 = 0
[全 188 关] 漂移条数 = 0     1.1 关数 = 188   1.2 关数 = 188
logic.ts numstat = 4  0  src/games/ocean-munch/logic.ts   （+4 / −0）
```

**不只是前 99 关——全 188 关一个字段都没动。** `logic.ts` 那 +4 行是 `DEX`
（图鉴条目）新增的三条深海鱼，与关卡参数无关，删行数为 0（没有任何老内容被覆盖）。

### 11.9 铁则⑦：商标 / 红线 / 无联网

9 个产品文件（6308 行）全文扫：

- 商标黑名单 33 个（`大鱼吃小鱼` `Feeding Frenzy` `Tasty Blue` `深海大作战` `球球大作战`
  `腾讯` `网易` `米哈游` `原神` `Minecraft` `Roblox` `任天堂` `宝可梦` `马里奥` `迪士尼`
  `奥特曼` `喜羊羊` `熊出没` `汪汪队` `海绵宝宝` `SpongeBob` `海底总动员`…）→ 命中 **0**；
- 联网 API（`fetch(` / `XMLHttpRequest` / `sendBeacon` / `WebSocket` / `navigator.send`）→ 命中 **0**；
- 188 关名 + 提示 + 特色：商标命中 **0**、红线命中 **0**；188 个关名两两不重样 ✅；
- 上屏文案里「广告 / 内购 / 抽卡 / 充值 / 氪金 / 血 / 尸 / 杀死 / 死亡」→ 命中 **0**。

源码注释里「血」还剩 6 处，逐条看过：3 处是**红线声明本身**
（「分级红线:无血无伤无死亡描写,被吃就是『晕乎乎回岸上休息』」「这里只掉泡泡,没有血、没有伤」），
另 3 处是内部注释里把 BOSS 的 `hp` 口语说成「血」（`// 狂暴 BOSS:血越少冲刺越快`、
`// 血量爱心`、`/** 血越少冲刺越快 */`）。**一个字都不上屏**，登记为观察项
`R3C-O1`（一般偏下，见 §15），不构成缺陷。

### 11.10 `ocean-munch` 小结

点名项④四条全部成立：无尽**真跑到系统判负**（本轮深度样本 916 米）；
`recordEndlessBest("ocean-munch", 916)` **恰好触发 1 次**、参数正确、正整数；
**destroy 后读回一致、重新挂载首页直接印出 916 米**；
**前 99 关（乃至全 188 关）参数零漂移**。七条铁则全过，`R2C-O1` 六颗按钮重量后确认关闭。
**本款 0 阻断 / 0 严重 / 0 一般 + 1 条观察（`R3C-O1`，注释口径，不上屏）。**

## 12. 第二遍 · `xiangqi` 朵朵星星象棋

走查脚本：`src/__r3packc__/x1-xq.test.ts`（目录 / 档位 / 188 课 / 抽 10 课 / `R2C-X2` / 长将三边界，**12 例**）、
`src/__r3packc__/x2-xq.test.ts`（真挂载七条铁则，**17 例**）。两支合计 **29 例全绿**。
挂载侧直接用仓库自带的 `domStub.ts`，视口钉在 **360 × coarse pointer**（手机口径）。

### 12.1 点名项⑤-1：全仓只有一套象棋目录

```
1.1 的 src/games/xiangqi/ = 5 个文件: guide.ts index.ts logic.test.ts logic.ts meta.ts
1.2 的 src/games/xiangqi/ = 24 个文件
1.1 的 5 个文件在 1.2 里全都还在原地 = 5/5     ← 原目录上长出来的，不是另起炉灶
以 xiangqi 开头的目录 = 1 个: xiangqi
meta.title === "中国象棋" 的目录 = 0 个（本款自己叫「朵朵星星象棋」）
```

名字里带「象棋」的目录只有两家，逐个看过：`chess-garden` 是「花园国际象棋」（王后车象马兵、易位、
吃过路兵、升变），`xiangqi` 才是中国象棋。`dark-chess`（翻翻暗棋）、`junqi-camp`（军旗对决）也不是。

### 12.2 点名项⑤-2：六档搜索层数与时间预算单调

| 档 | 名字 | 搜索层数 | 时间预算 | 思考延时 |
| --- | --- | --- | --- | --- |
| `novice` | 🌱 小象学步 | 0 | 20ms | 320ms |
| `easy` | 🐘 小象过河 | 1 | 40ms | 380ms |
| `normal` | 🐘 棋灵象 | 2 | 90ms | 460ms |
| `hard` | 🔥 棋灵象·进阶 | 3 | 200ms | 540ms |
| `master` | 🌌 银河象王 | 4 | 300ms | 640ms |
| `hell` | ⭐ 星海棋神 | 5 | 400ms | 760ms |

三条线都是**严格**递增（每一档都比上一档大，不是「不降」）。
预算是不是硬顶，换第 62 课的残局做样本、用假时钟（问一次时间走 1ms）实测：

```
novice 预算  20ms → 实走  0ms ✅    easy 预算  40ms → 实走 2ms ✅
normal 预算  90ms → 实走  2ms ✅    hard 预算 200ms → 实走 9ms ✅
master 预算 300ms → 实走 15ms ✅    hell 预算 400ms → 实走 0ms ✅
```

一档都没超预算。`hell` 走 0ms 是因为这个局面子数低于 `MATE_PROBE_PIECES=10`，
直接走穷举杀法那条捷径、根本没进迭代加深（与第 2 轮同因）。

### 12.3 点名项⑤-3：188 课残局有解、虚标 0

逐课四查（不是抽查，是 188 课全跑）：

```
总数 188   将死=136   困毙=52
mateIn 分布: 1 步 × 92    2 步 × 96
无解条数                      = 0
首着解集为空（虚标）          = 0
mateIn 松了（少一步也能赢）    = 0     ← 标 2 步的就真的 1 步赢不了
一步杀收官方式与标注不符      = 0     ← 标 checkmate 的实走就是 checkmate，标 stalemate 的就是 stalemate
```

第四条是本轮新加的：`finishKindAfter()` 把首着真摆上去，再看黑方是「被将着走不了」还是
「没被将却走不了」，与 `finish` 字段对照。**92 道一步杀全部对得上。**

### 12.4 点名项⑤-4：抽 10 课按主线实走，终局与标注相符（第三批样本）

第 1 轮抽 1/21/43/66/88/100/121/145/166/188，第 2 轮抽 7/33/55/71/90/111/133/155/170/181，
**本轮 10 课与前两轮全不重合**，将死 6 课 + 困毙 4 课：

| 课 | 标注 | 求解 | 主线着法（真摆到底） | 终局实测 |
| --- | --- | --- | --- | --- |
| 4 | 1步 checkmate | 最短 1 步 · 首着解 1 个 | `(7,4)→(5,4)` | checkmate ✅ |
| 18 | 1步 checkmate | 最短 1 步 · 1 个 | `(5,3)→(3,3)` | checkmate ✅ |
| 40 | 1步 checkmate | 最短 1 步 · 1 个 | `(2,1)→(2,0)` | checkmate ✅ |
| 62 | 2步 checkmate | 最短 2 步 · 1 个 | `(5,9)→(4,9)` `(3,1)→(3,2)` `(7,3)→(3,3)` | checkmate ✅ |
| 79 | 1步 **stalemate** | 最短 1 步 · 1 个 | `(1,1)→(4,1)` | stalemate ✅ |
| 98 | 2步 **stalemate** | 最短 2 步 · 1 个 | `(7,0)→(7,7)` `(3,1)→(3,2)` `(7,3)→(5,2)` | stalemate ✅ |
| 117 | 2步 **stalemate** | 最短 2 步 · 1 个 | `(6,2)→(4,2)` `(3,2)→(4,1)` `(6,1)→(4,1)` | stalemate ✅ |
| 141 | 1步 checkmate | 最短 1 步 · 1 个 | `(1,1)→(2,1)` | checkmate ✅ |
| 165 | 2步 **stalemate** | 最短 2 步 · 1 个 | `(6,3)→(5,3)` `(5,1)→(4,1)` `(5,3)→(5,2)` | stalemate ✅ |
| 186 | 2步 checkmate | 最短 2 步 · 1 个 | `(3,7)→(4,7)` `(5,0)→(4,1)` `(2,3)→(5,3)` | checkmate ✅ |

**终局与标注不符的课数 = 0；首着解集不唯一的课数 = 0**（10 课全是唯一主线）。

### 12.5 点名项⑤-5：长将三种边界

`REPEAT_LIMIT = 3`。三种边界各造一个能复现的局面，每一手的判定都打出来。

**边界 1 · 单方连将第三次判 `perpetual`，负方是连将方**
局面 `rK4,9 rR0,8 bK4,0 bP0,3`，红车在底线 `(4,8) ↔ (5,8)` 横晃将军，黑将 `(4,0) ↔ (5,0)` 躲：

```
 1. 红 (0,8)→(4,8) 将军=是  连将段内第 1 次  全局重复 1 次  → none
 3. 红 (4,8)→(5,8) 将军=是  连将段内第 1 次  全局重复 1 次  → none
 5. 红 (5,8)→(4,8) 将军=是  连将段内第 2 次  全局重复 2 次  → none  预警=perpetual
 7. 红 (4,8)→(5,8) 将军=是  连将段内第 2 次  全局重复 2 次  → none  预警=perpetual
 9. 红 (5,8)→(4,8) 将军=是  连将段内第 3 次  全局重复 3 次  → perpetual(red 负)
文案「红方一直用同一招将军，同一个局面出现三次啦 —— 长将判负，换一条进攻路线试试。」
```

第 8 手还是 `none`，第 9 手才判：**不早判、不漏判，负方就是连将的那一方**。
**镜像复验**：局面 `rK4,9 rP0,6 bK4,0 bR0,1`，黑车连将三次 → 第 10 手判 `perpetual`、
`loser=black`，**不是只盯着红方**。

**边界 2 · 中间断一手就不判**
同一个起手，红方第 3 手改走闲棋 `帅(4,9)→(3,9)`、黑方也走一步闲卒：

```
 3. 红 (4,9)→(3,9) 将军=否  ← 连将链在这里断了
 5. 红 (4,8)→(5,8) 将军=是  连将段内第 1 次（重新数）
 7. 红 (5,8)→(4,8) 将军=是  连将段内第 1 次
 9. 红 (4,8)→(5,8) 将军=是  连将段内第 2 次 → none  预警=perpetual
11. 红 (5,8)→(4,8) 将军=是  连将段内第 2 次 → none  预警=perpetual
终判 kind=none  loser=null
```

比边界 1 多走了 2 手，仍然不判长将 —— **断一手就重新数**，符合规则。

**边界 3 · 双方走回原位判 `repetition`，谁都不负**
局面 `rK4,9 rR0,8 bK4,0 bR8,1`，红车 `(0,8)↔(1,8)`、黑车 `(8,1)↔(7,1)` 各晃各的，全程零将军：

```
 4. 黑 (7,1)→(8,1) 全局重复 2 次 → none  预警=repetition
 8. 黑 (7,1)→(8,1) 全局重复 3 次 → repetition
终判 kind=repetition  loser=null      ← 和棋必须没有输家
文案「同一个局面来回走了三次，这一局算和棋。想赢就得先换个走法。」
一步将军都没有 = true；perpetualCheckLoser = null
```

**附加两条**：边界 1 那串同时满足「全局重复 3 次」与「连将段内 3 次」，
判的是 `perpetual`（红负）而**不是和棋**——**长将优先于重复**；
记录里 9 手的 `check` 字段类型全部正确，`moveGivesCheck` 与记录不打架。

### 12.6 铁则①：深链直开第 1 / 76 / 188 课

| 课 | mateIn | finish | 首着兵种 | 章 | 局面串 | 题面 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 1 | checkmate | 车 | 1《一车封路》 | `bK3,2 rR6,3 bE6,4 rK4,8` | 「第 1 课」+「红方先走」+「还能走 1 步」✅ |
| 76 | 1 | **stalemate** | 车 | 4《围而不将》 | `rR2,0 bK3,1 bA3,2 rP6,3 rK3,8` | 「第 76 课」+「还能走 1 步」✅ |
| 188 | 2 | **stalemate** | 车 | 8《别走成和棋》 | `bK3,0 bE6,0 rR8,0 bA3,2 rC5,4 rK4,9` | 「第 188 课」+「还能走 2 步」✅ |

不给课号就停在 188 关地图上（24 个章节节点在，棋盘 canvas = **0 个**）——不会误开一局。

### 12.7 铁则②：三课都真解开到结算

黑方在残局里是 `hard` 档 AI 应招，把 `Math.random` 钉住好在测试里镜像它的应法，逐手实走：

| 课 | 实走 | 通关文案 | 星级存档 | 平台回调 |
| --- | --- | --- | --- | --- |
| 1 | 红 `(6,3)→(3,3)` | 「1 步之内收官，一步都没有多走 —— 这就是这一章要练的本事。」 | 3★ | — |
| 76 | 红 `(2,0)→(4,0)` | 「困毙成功！不将军也能赢，这一手很漂亮。」 | 3★ | — |
| 188 | 红 `(8,0)→(6,0)` 黑 `(3,0)→(3,1)` 红 `(5,4)→(5,1)` | 「困毙成功！…」 | 3★ | `onWin(3, "188 课残局全部解开，你的算杀已经很有样子了！")` |

三课都是**没用提示解开、记满 3 星**。第 188 课是两步困毙，中间黑方真应了一手才收官；
解完最后一课平台还收到了通关全书的 3★ 回调。

### 12.8 铁则③：真输一次

第 76 课（必胜首着只有 1 个）故意走一手不在解集里的合法棋 `(2,0)→(3,0)`：
步数用完 → 失败文案「步数用完啦。回想一下：黑将现在能去哪几个点？困毙要数的是对方**所有**子的走法，
只要还有一个能动，就还没成。」命中 ✅，「🔄 重摆」按钮还在 ✅。**只鼓励不批评，没有一个「输」字。**

### 12.9 铁则④：四种玩法都走到结算

`meta.modes = campaign / endless / twoPlayer / versus`，`meta.levels = 188`。

- **残局学堂（campaign）** —— 见 5.7 / 5.8，赢面与输面都到结算。
- **自由对战（versus）** —— 六档按钮全在（🌱🐘🐘🔥🌌⭐），外加「👫 朵朵 VS 星星」。
  选 🐘 小象过河开局 → 点「🏳️ 认输」→ 平台收到
  `onLose("小象过河这一局占了上风。记住：先护住将帅，再找它没保护的子。")`，
  上屏「红方认输了。下一盘换个开局，说不定就赢回来。」✅
- **双人同屏（twoPlayer）** —— 这一局没有 AI 随机源，测试自己下满两边：
  朵朵（红）用 `hell` 档找棋、星星（黑）故意挑评分最差的一手。
  **第 45 手真收局**：「黑方一步棋都走不了 —— 这叫困毙，同样算红方赢，不是出错哦。」
  平台 `onWin(1, "朵朵（红）赢下了这一局，再来一盘！")` ✅
- **残局连胜（endless）** —— 连解 **7 课**（课号 1 → 10 → 19 → 22 → 31 → 40 → 43，
  每课一颗星，`api.addStars` 累计 7 颗），第 8 课（第 52 课）故意走错断掉：

  ```
  第 52 课故意走错 → 「🔥 这一轮结束」✅
  recordEndlessBest 调用: [["xiangqi", 7]]
  平台底账 save.getGameProgress("xiangqi").endlessBest = 7
  destroy → 重进：首页按钮显示「🔥 残局连胜 · 最好 7 课」✅
  ```

  （第 2 轮连解 5 课，本轮换成 7 课，样本不重复。）

### 12.10 铁则⑤：360px 热区（R2C-X1 三处重量一遍）

`MIN_HIT_PX = 44`。从 CSS 里把每一类可点元素的实高算出来（`max(min-height, 行高 + 上下 padding + 边框)`，
`@media (max-width:380px)` 的覆盖块也算进来，360px 命中）：

| 选择器 | padding(上下) | font | 边框 | min-height | 实高 | 判定 |
| --- | --- | --- | --- | --- | --- | --- |
| `.xq-btns button`（悔棋 / 确认落子 / 提示 / 重摆 / 认输 / 求和 / 换玩法） | 20px | 13px | 0 | **44** | **44.0px** | ✅ |
| `.xq-mode`（自由对战 / 残局连胜 / 规则） | 24px | 15px | 0 | **44** | **44.0px** | ✅ |
| `.xq-over-btn`（连胜结算面板按钮） | 22px | 16px | 0 | **44** | **44.0px** | ✅ |
| `.xq-seg button`（六档 / 执子选择） | 22px | 14px | 6px | — | 44.8px | ✅ |
| `.xq-start`（开始下棋） | 28px | 19px | 0 | — | 50.8px | ✅ |
| `.xq-rules-close`（规则页 ✖ 关闭） | 18px | 15px | 0 | **44** | **44.0px** | ✅ |

**实高 < 44px 的可点类 = 0 个。** 第 2 轮标记的三处（`.xq-btns button` 38.8–41.6px、
`.xq-mode` 42.0–45.0px 临界、`.xq-over-btn` 41.2–44.4px 临界）现在全部靠
`min-height:44px` 兜死，不再随字号/padding 浮动。窄屏覆盖块里也**没有**把 `min-height` 拿掉：

```
@media (max-width:380px){ .xq-btns button{flex:1 1 74px;min-height:44px;font-size:13px;padding:10px 2px;} … }
```

**R2C-X1 无回潮，确认关闭。**

棋盘：`GEOM` 逻辑 400×444、一格 44px；`.xq-wrap` 最宽 460px，360px 视口下棋盘 `width:100%`
铺满 → 一格 **39.6px**。但 coarse pointer 下默认打开「✋ 确认落子：开」，要点两次才落子，
误触有兜底 —— 与第 1、2 轮口径一致，**棋盘格不记缺陷**。

### 12.11 铁则⑥：destroy 20 轮无泄漏

进第 76 课 → 选子 → 点落点（停在预览）→ `destroy()` → 空转定时器，循环 20 轮：

```
轮  1：win 监听=0  根子节点=0  root 上残留监听=0  head 样式=0
轮 10：win 监听=0  根子节点=0  root 上残留监听=0  head 样式=0
轮 20：win 监听=0  root 上残留监听=0  head 样式=0  根子节点=0
destroy 之后再空转 5 秒定时器：未抛异常 ✅
```

四项计数 20 轮完全不涨，`<style>` 也没有在 `head` 上越堆越多。
竞态：棋盘上乱点 400 下（跨选子 / 预览 / 确认 / 电脑思考多种时机），未抛异常，
界面停在正常的失败解释；`destroy` 之后再空转 4 秒定时器，不抛异常、
**不给已销毁的那一局补报结算**。

### 12.12 铁则⑦：商标 / 红线 / 无联网

14 个产品文件（4520 行）扫 35 个商标词（含真软件名 `象棋旋风` `天天象棋` `QQ象棋` `棋天大圣`
`弈城` `野狐` `象棋巫师`，以及 `Stockfish` `Pikafish` `ElephantEye` 这些开源引擎名）→ 命中 **0**；
`fetch(` / `XMLHttpRequest` / `sendBeacon` / `WebSocket` → 命中 **0**。
红线词只命中 1 处，就是自我约束的注释本身：

```
* 分级红线：无血无伤，不写「杀」「死」这类字眼（将死是术语，另外单独说）。
```

角色只有朵朵与星星。`meta.levels = 188 = PUZZLES.length`。

### 12.13 `R2C-X2` 最终结论（easy / normal 残局卷同分）

**不改搜索**，只换尺子重新量。四条证据：

**① 换第三把尺**。第 1、2 轮的卷子都是从**一步杀**里抽的，本轮加一张**两步杀**的卷子：

| 卷子 | novice | easy | normal | hard | master | hell |
| --- | --- | --- | --- | --- | --- | --- |
| A · 一步杀 31 道（复现第 2 轮口径） | 7 | **31** | **31** | 31 | 31 | 31 |
| B · 两步杀 48 道（本轮新加） | 6 | **9** | **11** | 27 | 29 | **48** |

卷子 A 上 `easy` 与 `normal` 打平（31 = 31），卷子 B 上分开了（9 → 11）。
**原因很清楚**：`easy` 的搜索层数是 1、`normal` 是 2，而一步杀在**第一层**就能看见，
两层搜索并不会比一层多看到什么 —— 是这把尺没有分辨率，不是两档一样强。
六档在两条卷子上都**单调不降**，`hell` 在两步杀上满分。

**② 两档选的着法本来就不一样**：同一批 47 个残局，`easy` 与 `normal` 选同一手 38 次，
**重合率 80.9%（< 100%）** —— 这是两台不同的引擎，不是同一档换了个名字。

**③ 直接拉出来对下**：`easy` vs `normal` 共 24 盘（红黑各 12 盘），
**normal 胜 24 · easy 胜 0 · 和 0**。强弱倒挂不成立，而且差距很明显。

**④ 硬指标**：层数 1→2、预算 40→90ms、延时 380→460ms，三条都严格递增，
`ai.ts` 相对分支基线 **numstat 空 diff**（本包一行没动，也没让谁去改）。

> **结论：`R2C-X2` 关闭，判定为「尺子的分辨率问题，不是缺陷」。**
> 一步杀卷子对 depth 1 / depth 2 没有区分度是数学上的必然，
> 换成两步杀卷子、着法重合率、对下胜负三种口径，`normal` 都稳定强于 `easy`。
> 不需要改 `ai.ts`，也**不建议**为了让这份卷子好看去动搜索层数。
> 唯一可留的后续动作是文档侧：把「档位强弱要用两步杀卷子量」写进 `xiangqi` 的测试注释，
> 免得下一轮又拿一步杀卷子来量一次（登记为观察项 `R3C-X1`，见 §15）。

### 12.14 `xiangqi` 小结

点名项⑤五条全部成立：全仓**只有一套**象棋目录且是在 1.1 原目录上长出来的；
六档**层数 / 预算 / 延时三条线严格递增**且预算是硬顶；
**188 课有解、虚标 0、步数是紧的、一步杀收官方式全对**；
换第三批 10 课主线实走**终局全对、首着解唯一**；长将**三种边界 + 黑方镜像 + 长将优先**全部正确。
七条铁则全过，`R2C-X1` 三处热区重量后确认关闭，`R2C-X2` 给出最终结论（不是缺陷）。
**本款 0 阻断 / 0 严重 / 0 一般 + 1 条观察（`R3C-X1`，量档位强弱的口径应写进注释）。**

## 13. 第二遍 · 通用检查汇总（五款横向）

### 13.1 七条铁则逐款对表

| 铁则 | `gomoku` | `match-stars` | `rainbow-run` | `ocean-munch` | `xiangqi` |
| --- | --- | --- | --- | --- | --- |
| ① 首页 / 深链 | ✅ 三关直开 | ✅ 三关直开 | ✅ 三关直开 + 不带参停选世界屏 | ✅ 三关直开 + `?level=76` | ✅ 三课直开 + 不带参停地图 |
| ② 真赢到结算 | ✅ 1/76/188 | ✅ 1/76/188 | ✅ 1/76/188（`play`） | ✅ 第 1 关真吃 13 条 3★ | ✅ 1/76/188 三课 3★ |
| ③ 真输到结算 | ✅ 闲棋耗步 | ✅ 第 188 关 40 步耗尽 | ✅ `idle` 三关掉光心 | ✅ 三关掉光心 | ✅ 第 76 课走错解 |
| ④ 战役 1 / 76 / 188 | ✅ | ✅ | ✅ | ✅ | ✅ |
| ⑤ 各模式到结算 | ✅ 4/4 | ✅ 4/4 | ✅ 2/2 | ✅ 3/3 | ✅ 4/4 |
| ⑥ 360px 热区 ≥44px | ✅ 5 类 | ✅ 1 类 + 棋盘格 45px | ✅ 7 处 `Rect` + 3 处 `touchArea` | ✅ 7 处 `Rect` + 6 处 `touchArea` | ✅ 6 类（含窄屏档） |
| ⑦ 商标黑名单 | **0** / 35 词 | **0** / 19 词 | **0** / 24 词 | **0** / 33 词 | **0** / 35 词 |

`meta.modes` 与实测一一对上：`gomoku` / `match-stars` / `xiangqi` 各 4 种，
`ocean-munch` 3 种，`rainbow-run` 2 种，**声明了几种就玩到几种结算，没有「挂着不能玩」的模式**。

### 13.2 `destroy` 20 轮（五款同一把尺）

| 款 | win 监听 | 根子节点 | 待跑定时器 | head 样式 | 累计 `cancelAnimationFrame` | destroy 后空转 |
| --- | --- | --- | --- | --- | --- | --- |
| `gomoku` | 0 | 0 | 0 | 0 | 1 / 10 / 20（每轮 +1） | 10s 不抛、不补报 |
| `match-stars` | 0 | 0 | 0 | 0 | 1 / 10 / 20 | 60 帧不抛 |
| `rainbow-run` | 0 | 0 | 0 | — | 1 / 10 / 20 | 60 帧 + 敲键不抛 |
| `ocean-munch` | 0 | 0 | 0 | — | 1 / 10 / 20 | 60 帧不抛 |
| `xiangqi` | 0 | 0 | 0 | 0 | —（无 rAF 循环） | 5s 定时器不抛 |

**五款 20 轮下来四项计数一格没涨**，`cancelAnimationFrame` 与轮数严格一一对应。

### 13.3 竞态（五款都单独造过）

思考中狂点（`gomoku` 30 下 / `xiangqi` 400 下）、动画中狂点（`match-stars` 100 下）、
乱点乱敲交叉（`rainbow-run` 与 `ocean-munch` 各 400 + 400 次，含 `Escape`）——
**五款一致：不抛异常、不产生假结算（`onWin` / `onLose` 全 0）、一次出手只算一次**；
`destroy` 之后再操作也不给已销毁的那一局补报。

### 13.4 红线横扫

| 项 | 结果 |
| --- | --- |
| 商标黑名单 | 五款全 **0 命中**（题名 / 提示 / 上屏文案 / 源码全文） |
| 血 · 死亡描写 | 上屏文案 **0**；源码里只剩 `ocean-munch` 3 处内部注释口语（`R3C-O1`）与两款的红线声明注释本身 |
| 广告 / 内购 / 抽卡 / 充值 | **0** |
| 联网上报（`fetch` / `XHR` / `sendBeacon` / `WebSocket`） | **0** |
| 角色只用朵朵 / 星星 | ✅ 五款都只有这两位 |
| `three.js` 等三方渲染依赖 | 源码 0 处 import，`package.json` 0 个包 |

## 14. 第二遍 · 遗留项最终结论（含 R17 / G15 / X20 换一把尺重量）

### 14.1 第 2 轮的 8 条（6 条已修 + 2 条转来）

| 编号 | 款 | 第 2 轮状态 | 第 3 轮独立复量 | **最终结论** |
| --- | --- | --- | --- | --- |
| `R2C-G1` | gomoku | 修复员补 `min-height:44px` | `.gmk-btns button` = **44.0px** | **关闭**，无回潮 |
| `R2C-G2` | gomoku | 同上（含顺带兜住的 `.gmk-over-btn`） | `.gmk-seg button` / `.gmk-mode` / `.gmk-over-btn` 均 **44.0px** | **关闭**，无回潮 |
| `R2C-G3` | gomoku | 红线不许动 `ai.ts`，转第 3 轮 | 第三组 seed 36 局：和 17（47.2%），强档 18 胜 1 负 | **不阻断，转 1.3**（见 §1.5，理由三条） |
| `R2C-R1` | rainbow-run | `btnEndless` h→44 + 3 处 `touchArea` 兜底 | 7 处 `Rect` 全 44、3 处 `touchArea` 实测 44×44 | **关闭**，无回潮 |
| `R2C-R2` | rainbow-run | 「掉血」→「卸护甲」、《广告墙走廊》→《霓虹招牌走廊》 | 上屏 + 注释全筛：**0 命中**；改名那一关玩法参数未跟着动 | **关闭**，无回潮 |
| `R2C-O1` | ocean-munch | 6 个导航按钮 `touchArea` 兜到 44 | 6 处热区实测 44 高 + 另 7 处 `Rect` ≥44 | **关闭**，无回潮 |
| `R2C-X1` | xiangqi | `.xq-btns/.xq-mode/.xq-over-btn` 补 `min-height` | 6 类可点元素全 ≥44（`@media (max-width:380px)` 也兜住） | **关闭**，无回潮 |
| `R2C-X2` | xiangqi | 红线不许动搜索，转第 3 轮 | 两步杀卷 9 vs 11、着法重合 80.9%、对下 24 : 0 | **关闭，判定「尺子分辨率问题，不是缺陷」**（见 §12.13） |

**第 2 轮修复员声称清掉的 6 条，本轮逐条重量，6 条全部真清、一条没有回潮。**

### 14.2 第 1 轮 3 条观察项的去留

**`R17` · `rainbow-run` 第 1 关摆烂也能通关 —— 现象仍在，判「保留设计，转 1.3 由产品定夺」。**

```
[R17] 第 1 关《青草热身跑》摆烂 24 个新 seed：赢 17 趟 / 输 7 趟（输的都是 reason=hearts）
[R17] 同 12 个 seed 认真打：赢 12/12
关卡定义 L(0,0,"青草热身跑",["rock"],[],{type:"coins",n:6],…)  ← 障碍表不是空的
```

第 1 轮说「障碍表为空、站着不动也能过」，本轮把关卡定义读出来更正一句：
障碍表其实是 `["rock"]`，**失败分支真的存在**（24 趟里输了 7 趟），只是石头摆在哪条道全看 seed，
多数时候不动也蹭得过去。这是教学关的刻意宽松，`logic.ts` 相对 1.1 只改了 R2C-R2 那处关名，
**难度参数一个数没动**。测试侧不再追，转 1.3 作为产品口径题（要不要给第 1 关一条必然失败分支）。

**`G15` · `gomoku` 基本功电池从 `normal` 起满分 —— 现象仍在，判「量尺问题，已另配尺，转 1.3」。**

```
[G15] tiers.test.ts 仍写着 for (const s of scores.slice(2)) expect(s.score).toBe(DRILL_TOTAL) = true
```

这条断言在，就意味着这把尺天生只守得住「`novice`/`easy` 明显更弱」。
但本轮已经另配了两把有分辨率的尺，都能把上四档分开（见 §1.4）：
36 局实战对下（`master→smart` 36 : 0、`hell→master` 18 : 6）、
30 个中盘局面的着法重合率（`master vs hell` 70.0%，第 1 轮是 100%）。
**结论：不是强度缺陷，是回归电池的分辨率不足；不改产品代码，建议 1.3 把解局考卷并进 `tiers.test.ts`。**

**`X20` · `xiangqi` 上两对相邻档整局大量和棋 —— 现象仍在，判「不倒挂，保留观察，转 1.3」。**

本轮换第三批 seed（77000 起）、并且**让每档吃自己那份 `TIME_BUDGET_MS`**
（假时钟按比例走，不像上一把尺那样把两边都钉死 30ms），各 24 盘：

```
[X20] hard   vs normal 24 盘: 强 11 弱 1 和 12（和棋率 50.0%）
[X20] master vs hard   24 盘: 强  6 弱 5 和 13（和棋率 54.2%）
[X20] hell   vs master 24 盘: 强  7 弱 1 和 16（和棋率 66.7%）
```

三对**都没有倒挂**（强档胜数 ≥ 弱档），但和棋率 50%–67%，确实偏高。
根因与 `R2C-G3` 同类：整局对弈里两边都不犯大错时，120 手封顶 + 三次重复即判和，
差距被和棋吃掉。要压下来得放宽封手数或时间预算，属于强度曲线调整，**超出测试员边界**。
> 另记一条走查口径给下一轮：**量象棋档位强弱时不要把两边的时间预算钉成同一个数** ——
> 本轮先用「两边都 30ms」量过一次，`hard` 反而 5 : 8 输给 `normal`（深档搜到一半被截断，
> 拿不到完整一层的收益）；换回各自预算后立刻变成 11 : 1。这是**尺子造成的假倒挂**，
> 与 `R3C-X1` 合并成同一条量法说明。

## 15. 第二遍 · 缺陷清单与发布结论

### 15.1 本轮新记

| 编号 | 级别 | 款 | 内容 | 处置 |
| --- | --- | --- | --- | --- |
| — | **阻断** | — | **0 条** | — |
| — | **严重** | — | **0 条** | — |
| — | **一般** | — | **0 条** | — |
| `R3C-G4` | 观察 | gomoku | 强行打开禁手规则时，188 关里 46–51 这 6 关（杀法本身就是双冲四）解集为空 | 解局战役 `index.ts:579` 写死 `forbidden:false`，玩家不可达；6 关均为 1.1 继承题，**不记缺陷** |
| `R3C-O1` | 观察 | ocean-munch | 源码注释里还有 3 处把 BOSS 的 `hp` 口语称作「血」 | **一个字都不上屏**，红线扫描针对的是玩家可见文案，**不记缺陷** |
| `R3C-X1` | 观察 | xiangqi | 「档位强弱要用两步杀卷子量、且各档吃自己的时间预算」这条口径没写进测试注释 | 文档侧建议，转 1.3 |

三条都是观察项，**不占阻断 / 严重 / 一般任何一格**。

### 15.2 点名五项逐条判定

| # | 款 | 硬要求 | 本轮判定 |
| --- | --- | --- | --- |
| 1 | `gomoku` | 10 关实摆 + 五连、相邻档单调（换 seed）、前 99 题不漂移、188 关无解条数、第 1 轮阻断不回潮、`R2C-G3` 终结论 | **全部成立**：10/10 走到黑五连；五对相邻档全单调；漂移 0；无解 0；两条阻断均未回潮；`R2C-G3` 判不阻断转 1.3 |
| 2 | `match-stars` | 消除→下落→补位可见、瞬变即阻断、reduced-motion 不另开分支 | **全部成立**：三关飘空帧 25 / 14 / 66（≠0），`planRefill` 的 `fromRow` 全为负，降速走同一状态机；**瞬变阻断不成立** |
| 3 | `rainbow-run` | `view3d`/`controls` 0 diff、`endless` 说清、无 three.js、`makeCamera` 只在初始化与尺寸真变、热区/文案不回潮 | **全部成立**：两文件 blob 哈希与 1.1 逐字节相同；`endless.ts` 是严格超集、与渲染物理隔离；three.js 0；500 帧只重算 1 次；热区 10 处 ≥44、「掉血/广告墙」0 命中 |
| 4 | `ocean-munch` | 无尽玩到失败、`recordEndlessBest` 触发、destroy 后读回、前 99 关不漂 | **全部成立**：916 米系统判负；`recordEndlessBest("ocean-munch", 916)` 恰 1 次且为正整数；destroy 后读回 916 并在首页印出；全 188 关参数 0 漂移 |
| 5 | `xiangqi` | 一套目录、档位单调、残局虚标 0、10 课实走相符、长将三边界、`R2C-X2` 终结论 | **全部成立**：目录 1 套且原地升级；层数/预算/延时三线严格递增且预算是硬顶；188 课无解 0 / 虚标 0 / 松标 0；10 课主线终局全对；长将三边界 + 黑方镜像 + 长将优先全对；`R2C-X2` 判非缺陷、关闭 |

### 15.3 发布结论

> **就点名五项而言：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
> 五款可以随窗口 2 一起发布。**

依据：

1. **本轮 0 阻断 / 0 严重 / 0 一般**。三条新记全是观察项，均不影响玩家可见行为；
2. **点名五项的硬要求逐条拿到独立证据**，没有一条靠引用前两轮结论顶过去（样本、seed、关号、
   课号、深度全部换过一批）；
3. **第 1 轮的两条阻断在第三组 seed 上仍未回潮**（`smart` 不再输给 `normal`、`hell` 不再退化成 `master`）；
4. **第 2 轮修复员清掉的 6 条逐条重量，6 条全部真清**，一条没有回潮；
5. 两条按红线转交的遗留项（`R2C-G3` / `R2C-X2`）都给了最终结论：
   `R2C-X2` 判**非缺陷**直接关闭，`R2C-G3` 判**不阻断**、转 1.3 —— **两条都不拦发布**，
   而且本包**没有改产品代码一个字节**（`ai.ts` 两款都是空 diff）；
6. 第 1 轮 3 条观察项（`R17` / `G15` / `X20`）全部给了去留：都判「不阻断、转 1.3」，
   其中 `R17` 更正了第 1 轮「障碍表为空」的说法，`X20` 顺带纠正了一把会造成假倒挂的尺子。

### 15.4 走查件清理与水位

- 走查脚本 `src/__r3packc__/` 共 **11 支测试 + 1 支画布桩**，删除前最后一次全跑
  **11 文件 / 134 例全绿**，随后**整目录删除**，`src/games/**` 下没有留下任何测试；
- 本包**没有改任何产品代码**：`src/games/**` `src/engine/**` `src/ui/**` `src/styles.css`
  `package.json` 一个字节没动，第 1 / 2 轮报告原文也没动；
- 两款点名项明令不许碰的文件（`gomoku/ai.ts`、`xiangqi/ai.ts`）相对分支基线 **numstat 空 diff**。

## 16. 第二遍收尾：分支尖 `a1ef885` 上的复验与水位

第二遍取证时的基线是 `becadd1`。收尾前 `git fetch` 发现分支尖已经走到 `a1ef885`，
中间有两笔**动到本包五款**的提交，必须重新指认一遍（第二遍走查脚本已删，这里另起一支临时件重量）：

| 提交 | 动了什么 | 会不会推翻前面的结论 |
| --- | --- | --- |
| `aefd70e` 第 3 轮学习优化员包 C | `ocean-munch/endless.ts` + `rainbow-run/endless.ts` 各加一个**朗读专用纯函数** `endlessRecordSay()`，收尾 `speak()` 时把纪录念出来（P3C-1/2）；`gomoku/index.ts` 把满盘和棋那一句换成 `boardFullLine(size, ai)`（P3C-3） | **不会**。面板上画的字一个没改，改的是朗读串与和棋话术；下面逐条重量过 |
| `86f4170` 第 3 轮监督修复员包 A+C | `ocean-munch/logic.ts` 注释里的红线口径 | **不会**，只动注释 |

### 16.1 三处新改动逐条重量（红线 0 / 商标 0 / 分支对）

```
[15 路人机] 15 路棋盘下满了，握手言和！两边都会挡活四，15 路又特别宽，很容易下成和棋。
           想分个胜负，可以换 9×9 入门盘，格子少一点，五颗更快连得上。
[9 路人机 ] 9 路棋盘下满了，握手言和！两边都守住了对方的活三。下一盘先做双威胁：
           一手棋同时开两个活三，对手只挡得住一边。
[双人同屏 ] 15 路棋盘下满了，握手言和！两边都守得很紧，谁都没让对方连成五颗。
index.ts 里确实挂上了 api.onWin(1, boardFullLine(size, ai)) = true

ocean 破纪录 「这是新纪录,比上次的 591 米还深 325 米!」   ocean 打平 「跟最好成绩打平,都是 916 米!」
ocean 第一条 「这是你的第一条纪录:916 米!」              ocean 没追上「你最深潜到过 916 米,再往下 116 米就追平啦。」
run   破纪录 「这是新纪录,比上次的 1200 米还远 1484 米!」  run   打平 「跟最好成绩打平,都是 2684 米!」
run   第一条 「这是你的第一条纪录:2684 米!」              run   没追上「你最远跑到过 2684 米,再多跑 684 米就追平啦。」

三处新文案扫红线词与商标词：命中 0 / 0
```

`boardFullLine()` 这一句正好接上第二遍给 `R2C-G3` 的结论（§1.5）：和棋率高的根子是 15 路满盘太宽，
现在孩子下成和棋时，屏幕会直接告诉他「换 9×9 小盘更容易分胜负」——**没有动 `ai.ts` 一个字节**，
却把「和棋多」这件事在体验层接住了。9 路与双人同屏各说各的，三条分支互不串。

### 16.2 点名项的底线在新分支尖上仍成立

```
view3d.ts    1.1=fe19dbcb  HEAD=fe19dbcb  逐字节相同 ✅
controls.ts  1.1=7584975a  HEAD=7584975a  逐字节相同 ✅
rainbow-run/endless.ts numstat 相对 1.1 = 394 / 19（新增的那 18 行就是朗读用纯函数）

[ocean-munch] 前 99 关漂移 = 0；全 188 关漂移 = 0
  logic.ts numstat 相对 1.1 = 5 / 1（DEX 三条 + 这次的注释口径，关卡参数一个字段没动）

[无尽·新分支尖] 收尾朗读：这一趟潜到 452 米!被大鱼碰一下会掉一块,晕乎乎回岸上休息一会儿就好,
                下次先躲开再找小鱼吃。这是你的第一条纪录:452 米!
  recordEndlessBest 调用 = [["ocean-munch", 452]]（恰 1 次、正整数、没串号）
  destroy 后底账读回 = 452 米；win 监听 = 0；根子节点 = 0
  收尾这一句扫「死 / 血 / 残忍 / 杀」= 0
```

**点名项③（`view3d`/`controls` 零 diff）与点名项④（无尽 → 写盘 → 读回）在新分支尖上原样成立**，
新加的朗读句子还顺带补上了「破没破纪录」这条只有听得见的信息。

### 16.3 水位（分支尖 `a1ef885`）

| 项 | 结果 |
| --- | --- |
| 第二遍走查脚本（`src/__r3packc__/`，删除前最后一跑） | **11 文件 / 134 例全绿** |
| 分支尖复验临时件（`src/__r3packc2__/`，删除前最后一跑） | **1 文件 / 5 例全绿** |
| 本包五款自带测试 | **40 文件 / 1113 例全绿** |
| 全仓 `npx vitest run` | **271 文件 / 6855 例全绿** |
| `npm run build` | ✅ `tsc --noEmit` 通过、`vite build ✓ built`、PWA 预缓存 157 条 |
| 产品代码改动 | **0**——第二遍没碰 `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css` `package.json`；`gomoku/ai.ts`、`xiangqi/ai.ts` 空 diff；第 1 / 2 轮报告原文与第一遍的 §0–§8 也一个字没改 |

### 16.4 第二遍的发布结论（与第一遍一致）

**就点名五项（`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`），可以随窗口 2 一起发布。**
两遍独立走查、两套样本、两套脚本，都是 0 阻断 / 0 严重 / 0 一般；
遗留 11 条（第 2 轮 8 条 + 第 1 轮 3 条）全部给了最终结论，**没有一条拦发布**，也没有一条要交给主管。
