# r36 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`17356717`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-168**、**N-169**、**N-170**。下一空号 **N-171**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r35 学习 PR 未合主干时，本轮仍从 N-168 起编；若主干已占用 N-168+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r35）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、N-108 无尽画廊、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r36-learn-notes.md` §0。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / #105 / **#106** / **#107**（坦克双垫 / 消消乐·地鼠钳高）/ **#108**（N-165…167）。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-168 · B · `.bvp-opt`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/brave-path/index.ts` ≈L239 |
| 现状 | `padding:13px`，无 min-height；内图标 `.bvp-opt-em` 34×34 |
| 不做 | 不改 `.bvp-btn` / `.bvp-act`（N-150）；不改大厅 `.bvp-mode`；不改 34px 图标本身（抬整钮即可） |
| 验收 | 岔路口选项钮高 ≥44；点选仍进战斗 |

### N-169 · B · `.pfb-pick`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/puff-bros/index.ts` ≈L251 |
| 现状 | `padding:12px 16px; min-width:132px`，无 min-height |
| 不做 | 不改 `.pfb-veil-btn`（N-152）；不改 `.pfb-open` / 摇杆 `--k`；不并 N-145/148 选车/选档 |
| 验收 | 选角色卡高 ≥44；开局仍按所选角色 |

### N-170 · A · `*-opt` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-opt` 可点块必须 `min-height`≥44 或 TOUCH/MIN_HIT 插值 |
| 豁免 | `*-optbar` 容器、`*-opt-em` 图标、非交互 |
| 不做 | 不替代 N-149/150/167；不扫 `*-pick`（那是 N-145/148/169） |

---

## 建议顺序

1. A 先合 **N-170**（或与 B 并行）。
2. B 修 **N-168**，再修 **N-169**。
3. 回归：N-150 顶栏、N-87 冲刺菜单、N-165/166 规则关闭/继续、N-99/97/100。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-168/169 的点击矩形，不能只看 CSS 行。
