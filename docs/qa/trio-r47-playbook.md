# r47 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-201**（B `.oa-back`）、**N-202**（B 钓鱼 `.fs-back`）、**N-203**（A `*-back` 巡检，**不含 `.l99-*`**）。下一空号 **N-204**。

**纪律**：`.l99-*` **只给 A**（N-198）。r45 N-196 对 B 作废。

**撞车**：先合入 `game-1.3` 的为准。

---

## 红线

1. C 只交两份 md。
2. **B 禁止修改 `src/games/level99.ts` 以及任何 `.l99-*`。**
3. N-110…116 跳过。已合号只回归。
4. 勿重开 r46 §0 并号（除本轮点名的两颗 N-135 余量）。
5. 在途：#94 / #96 / #99 / #104 / **#105～#119** / #107。
6. 勿改 `SKY_H`。勿抬 `.shr-toggle`（36）。勿把 fruit-stack `.fs-back` 当钓鱼那颗改。

---

## 本轮工单

### N-201 · B · `.oa-back`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/orb-arena/index.ts` ≈L115；挂载 ≈L953 |
| 现状 | padding 7+字 14，无 min-height |
| 不做 | 不改 `.oa-open` / `.oa-btn` / 技能 |
| 验收 | 大厅返回 ≥44；点了仍回选关 |

### N-202 · B · fishing-star `.fs-back`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/fishing-star/index.ts` ≈L227；挂载 ≈L2035 |
| 现状 | padding 6+字 13，无 min-height |
| 不做 | 不改 fruit-stack 同名 `.fs-back`（已 44）/ `.fs-act`（N-157）/ `.fs-open` |
| 验收 | 钓鱼「回选关」≥44 |

### N-203 · A · `*-back` 巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-back{` 可点块必须 min-height≥44 |
| **排除** | **全部 `.l99-*`（N-198）** |
| 本轮应变绿 | N-201 / N-202 |
| 仍可红 | `.bl-back` / `.bd-back`（N-135） |

---

## 建议顺序

1. A 立刻收 **N-198**（含 continue）和 **N-197**、**N-203**。
2. B 修 **N-195**、**N-199**、**N-201**、**N-202**，分 PR（钓鱼与光球不要和 bowling 捆一单）。
3. 回归：N-134 toggle 仍 36、N-173 `.pk-go`、N-192 地图格、bowling `.bl-btn` 走 N-199。

---

## 本拍未覆盖

无头 Chrome 未跑。A 必须真机量 l99 壳层钮（不要只看 styles.css 补丁）。B 补测 `.oa-back` 与钓鱼 `.fs-back`。
