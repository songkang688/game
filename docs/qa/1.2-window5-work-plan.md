# 窗口 5 · 监督派发计划

- 窗口：5（监督，不写 `src/games/**`）
- 工作分支：`game-1.2-window5`
- 基线：`origin/game-1.2`
- 实现步：第 22–26 步，每步 A/B/C **三个都要全派**
- 验收：本窗三轮 × 测试员 / 学习优化员 / 监督修复员，三个角色都要派
- 过检后：把 `game-1.2-window5` 合进 `origin/game-1.2`
- 独占目录：`landlord-cards` `fishing-star` `poop-hero` `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 不做：全局第 27–29 步；不改窗口 1 平台文件；不改其他窗口游戏目录

## 派发顺序

1. 第 22 步：同时派 A `landlord-cards` / B `fishing-star` / C `poop-hero`
2. 谁交卷过检，立刻补第 23 步空档，始终保持 3 个实现子代理在跑
3. 第 23 → 24 → 25 → 26，每步 A/B/C 全派全收
4. 15 格全绿后再派本窗三轮验收（每轮 3 人）
5. 9 份报告齐、测试构建全绿 → merge 进 `game-1.2`
