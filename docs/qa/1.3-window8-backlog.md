# 窗口 8 · 1.3 视觉升级 · 遗留 backlog（1.4 的输入）

- 建档：C 监督修复员（第 3 轮终验收官），按终验规格「一般 / 建议级能修则修，不能修的登记进 backlog」。
- 口径：每条带「来源轮次 / 位置 / 期望 / 估算改动面」。本窗 12 款范围内能修的都已在三轮内修完
  （第 3 轮连两条备档增强项 music-stars 锚点星、pinyin-train 圆点副记号也已落地），
  以下条目全部因**超出本窗权限或范围**而移交，非「修不动」。

## 条目（共 2 条，均建议级）

### BL-01 · 壳层 12px 装饰星 / 顶栏统一件（W8R1-09，移交全局）

- **来源轮次**：第 1 轮 A 档 W8R1-09（建议级）→ 三轮维持 → 第 3 轮 A 档终验裁决「维持移交全局，账不丢」；
  本窗覆盖指令明令「壳层移交全局，不要改 `level99.ts` 壳」。
- **位置**：`src/games/level99.ts` 壳层 `.l99-star` 12px 装饰星。同域一并移交：
  ① 顶栏游戏名 chip 在 360px 下按省略号截断（如「红…」，第 3 轮 A 档第四节登记不判罚）；
  ② 壳层卡片 emoji 图标层若做矢量化，可顺手错开 tap 入口卡 🎈 与 tug 玩局加油点 🎈
  撞 emoji（第 3 轮 B 档 4.1 观察，不判罚不移交、仅顺手项）。
- **期望**：壳层装饰星按 1.3 金币三件套口径升级（≥2 停渐变 + 高光 + 描边 / 落影，
  参照 `docs/qa/1.3-window8-style-baseline.md` B-24~B-28）；顶栏 chip 截断给出全库统一的
  缩字 / 让宽策略。
- **估算改动面**：`level99.ts` 壳层 CSS 与装配段一处 + 全库所有款回归（壳层是公共件）——
  超出本窗 12 款范围与三人组权限，须由全局统一窗执行。

### BL-02 · kit `train.ts` 文件头注释含品牌词（B 档商标终审移交）

- **来源轮次**：第 3 轮 B 档商标终审 3.1（注释级残留 · 唯一 1 条 · 零用户可见风险：
  `vite build` 产物剥离注释，B 档 dist 全文复扫 0 命中）。
- **位置**：`src/art/kit/train.ts:10` 文件头注释「不给车头画鼻子嘴巴照搬托马斯」——
  规避说明性注释含品牌词，与各款测试里的商标扫描名单同性质（说的是「不许像谁」）。
- **期望**：二选一——① 注释措辞中性化（如「不给车头画立体人脸浮雕照搬任何知名火车角色」）；
  ② 商标扫描口径明确豁免「规避说明注释」并把 `src/art/kit/` 纳入扫描范围（堵住本次的盲区）。
- **估算改动面**：方案① 一行注释改动，但 `train.ts` 属冻结 kit、归属另一维护人，
  本窗 C 档权限内不可改（「其余冻结 kit 只 import 不改」）；方案② 为 round1 scan 用例
  扫描范围一处调整（属测试档权限）。两案任一不影响放行（A 档「商标 0 命中」口径为
  12 款实现代码，此条不在其内；孩子端不可达）。

## 统计

| 维度 | 数 |
| --- | --- |
| 总条数 | 2 |
| 按严重度 | 阻断 0 · 严重 0 · 一般 0 · 建议 2 |
| 按去向 | 移交全局统一窗 1（BL-01）· 移交 kit 维护人或测试档 1（BL-02） |

三轮验收的其余账目均已闭环：阻断 / 严重级终态 0；W8R1-08 由 A 档终验裁决结案（非 backlog）；
两条放行条件（find-diff 章 4–10 贴纸、shape-kingdom 氛围帽）已在第 3 轮 C 档落地关闭，
见 `docs/qa/1.3-window8-round3-fixer.md`。
