# 三人组 r32 测试员 A

> 同分支。`--k` 仍走 N-155，本拍不并号。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-158 | 静态扫 `*-pad` / `*-keys` 上 `grid-auto-rows:\d+px` 与 `repeat(N,Npx)` ≥44。`var(--k)` 不在本号重复判。仓鼠 `@media (max-height:500px)` 的 40 走 **N-156**，本拍不改该文件。 |
