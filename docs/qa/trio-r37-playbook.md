# r37 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`17356717`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-171**、**N-172**、**N-173**。下一空号 **N-174**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r36 学习 PR 未合主干时，本轮仍从 N-171 起编；若主干已占用 N-171+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r36）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、N-108 无尽画廊、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r37-learn-notes.md` §0。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / #105 / **#106** / **#107**（坦克双垫 / 消消乐·地鼠钳高）/ **#108**（N-165…167）/ **#109**（N-168…170）。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-171 · B · `.gdh-tally`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/gold-hook/style.ts` ≈L108；点击 `index.ts` ≈L1035/L1064 |
| 现状 | 结算跳数 **div**，`font-size:26px;line-height:1.2`，无 min-height；提示「点一下」 |
| 不做 | 不改 `.gdh-card`/买钮（已 TOUCH_MIN）；不改 `TALLY_MS` / 钩索；不把画布 `.gdh-tally-fly` 当热区号 |
| 验收 | 数字行高 ≥44；点一下仍立刻停在终值；`prefers-reduced-motion` 仍直接终值 |

### N-172 · B · `.ak-card`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/adventure-king/index.ts` ≈L158；挂载 ≈L1435 |
| 现状 | 选章 `<button>`，`padding:10px`，无 min-height |
| 不做 | 不改走廊物理 / 章节表；不并 N-28 modebar；不改 `.advk-pad2` |
| 验收 | 选章卡高 ≥44；点章仍进对应关；390 两列 / 560 四列不裁字 |

### N-173 · A · `*-go` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-go` 可点块必须 `min-height`≥44 或 TOUCH/CHIP/MIN_HIT 插值 |
| 现状红 | `.pk-go`（pickAll.ts ≈L70）、`.dvs-go`（duo-vs-star/index.ts ≈L192） |
| 对照绿 | `.pyt-go` 已 CHIP_MIN |
| 豁免 | `.mj-btn.mj-go` / `.jq-btn.jq-go` 修饰类；非交互 |
| 不做 | 不替代 N-87（冲刺菜单）/ N-149（open/mode）/ N-170（opt）；不扫 `.dvs-lessonbtn` |

---

## 建议顺序

1. A 先合 **N-173**（或与 B 并行）。
2. B 修 **N-171**，再修 **N-172**。
3. 回归：N-168/169 选项/选角、N-87 冲刺菜单、N-165/166 规则关闭/继续、N-99/97/100、gold-hook 买道具与模式卡（已绿勿回退）。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-171/172 的点击矩形，不能只看 CSS 行。
