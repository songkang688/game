# 窗口 2 · 1.2 三轮验收 第 3 轮 · 监督修复员 · 包 A（本窗口收官）

> 角色：QA 第 3 轮监督修复员 · 包 A
> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 基线：`origin/game-1.2-window2` @ `82bdd3c`
> 模型：`claude-opus-5-thinking-high-fast`

## 〇、复验计划（动代码前先立此存照）

### 0.1 我进场时水位已经不是原始水位

本轮开工前，`game-1.2-window2` 上已经有两拨人动过本包的代码，**他们的 commit 一条不回滚**：

| SHA | 谁 | 落了什么 |
|---|---|---|
| `d324399` | 第 3 轮监督修复员（前手） | 给 `stepPhysics` 加物理看门狗收掉两条严重项（`R3-PA-FS-1` / `FS-2`）；另修 `R3-PA-FS-3` `R3-PA-PS-1` `R3-PA-DM-1` 三条一般项 |
| `82bdd3c`（含前面 9 个 commit） | 第 3 轮学习优化员包 A | 列 17 条落地 13 条（暂停口径、触屏暂停入口、读屏说明、结算文案等），并与 `d324399` 做过三处撞车处置 |

所以本轮**不是「照着缺陷单从头修一遍」**，而是：

1. 拿当前 HEAD 把两位测试员记的每一条**独立复验**，已消失的点名是谁清的；
2. 只修**复验之后仍然在**的那些；
3. 盯住第 1 轮三条严重项没有回潮、商标 0、`destroy` 无泄漏；
4. 给「本包五款能否随本窗口一起发布」一个最终结论，遗留一条不许悬着。

### 0.2 硬边界（红线）

- **只改这 5 款**：`src/games/{dot-maze,fruit-stack,pool-stars,junqi-camp,chess-garden}/`。
  别人的 10 款、`src/engine/**`、窗口 1 的平台文件（`src/ui/**`）一个字不动；
  `src/styles.css` 只许碰本包自己的选择器。
- **不重做已经落地的修复**：`d324399` 与 `82bdd3c` 的 commit **一条不回滚、不双写**。
  两条严重项的 `stepPhysics` 看门狗**一个字节不碰**，除非复验证明它还红。
- **不拆 `R2-PA-3` 的棋盘格例外**（`.cg-sq` 41.4px），除非那条过期提醒断言已经报红。
- 不改测试员 / 学习优化员的报告原文。
- 用例只增不减：既有断言只翻面、不删除、不放宽。
- 无血无死亡描写；角色只用朵朵 / 星星；商标黑名单 0 命中；不引 three.js、不引任何外部运行时依赖。
- 报告两处：本文件（独占）与 `docs/qa/1.2-window2-round3-fixer.md` 的 `## Pack A` 一节（**只追加**）。
- 只推 `origin/game-1.2-window2`，被拒就 fetch + rebase 再普通 push，禁止 `--force`，
  不用 `gh` 开 / 改 / 合 PR。

### 0.3 复验台账（两位测试员合计 6 条编号）

| 编号 | 款 | 记录人 | 原级别 | 复验要回答什么 |
|---|---|---|---|---|
| `R3-PA-1` = `R3-PA-FS-1` | fruit-stack | 第 1 名（一般）/ 第 2 名（建议重定严重） | 严重 | 密堆极限环还收不收得了场？按第 2 名那三组取证（随机落点 20 盆、界面真按、第 188 关）重跑 |
| `R3-PA-FS-2` | fruit-stack | 第 2 名 | 严重 | 人机对战里电脑会不会永久停手 |
| `R3-PA-FS-3` | fruit-stack | 第 2 名 | 一般 | 同帧双达标判不判平、对家堆爆记不记成自己过关 |
| `R3-PA-PS-1` | pool-stars | 第 2 名 | 一般 | 结算浮层还会不会把同一句说两遍（5 条 reason 逐条过） |
| `R3-PA-DM-1` | dot-maze | 第 2 名 | 一般 | 无尽破纪录说不说「新纪录」 |
| `R2-PA-3` | chess-garden | 第 1/2 名 | 一般（记录在案的例外） | 例外前提是否过期（过期提醒断言报没报红） |

另加三条不带编号的回归口径：

- 第 1 轮三条严重项无回潮：fruit-stack 44px（`PA-FS-1`）、pool-stars 暂停吞输入（`PA-PS-1`，
  学习优化员另修了「暂停时 AI 仍出杆」）、junqi-camp 双人光标（`PA-JQ-1` / `PA-JQ-2`）；
- 商标黑名单 0 命中；
- `destroy` 进 → 玩 → 退出 → 再进无泄漏。

### 0.4 执行顺序

1. 本 commit：只落复验计划，不动任何代码。
2. 写一份**一次性复验脚本**逐条重跑上面 6 条 + 3 条回归口径（脚本跑完即删，不进仓库）。
3. 确有未修项的，每条一个独立 commit + 测试（用例只增不减）。
4. 回填本报告与 `docs/qa/1.2-window2-round3-fixer.md` 的 `## Pack A`。
5. fetch + rebase `origin/game-1.2-window2` → `npm test` + `npm run build` 全绿 → 普通 push。
