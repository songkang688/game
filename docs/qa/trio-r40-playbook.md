# r40 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-180**、**N-181**、**N-182**。下一空号 **N-183**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r39 学习 PR 未合主干时，本轮仍从 N-180 起编；若主干已占用 N-180+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r39 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r40-learn-notes.md` §0。尤其 **不要**再开 `.sn-back`（N-147）、`.sks-mode`（N-149）、`.dvs-lessonbtn`（N-177）、`.bl-btn`（N-145）、`.frc-open`（N-121）、`.xq-start`（N-95）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#112** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 矮横屏 `.dr-menu-cta`。

---

## 本轮工单

### N-180 · B · `.sn-open`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/snake-snack/index.ts` ≈L114；挂载 ≈L842 / 结算 ≈L775 |
| 现状 | 大厅无尽入口 + 结算再来，`padding:9px 18px`，无 min-height |
| 不做 | 不改 `.sn-back`（N-147）/ `.snk-toggle`（已 44）/ 蛇逻辑 |
| 验收 | 无尽钮与结算再来 ≥44；进无尽花园仍正常 |

### N-181 · B · `.dr-softbtn`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/duo-rush/index.ts` ≈L380；挂载 ≈L477–478 |
| 现状 | 基规则无 min-height；仅 `max-height:500` 的 N-87 块有 44 |
| 不做 | 不回退 N-87 sticky；不改 `.dr-btns`；不并 N-165/166 |
| 验收 | 390×844 怎么玩 / 收藏册 ≥44；915×412 仍钉在菜单顶 |

### N-182 · A · `*-softbtn` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-softbtn` 可点块必须 `min-height`≥44 或 TOUCH 插值 |
| 现状红 | 仅 `.dr-softbtn` |
| 不做 | 不扫 `.dr-start`；不替代 N-87/181 |

---

## 建议顺序

1. A 先合 **N-182**（或与 B 并行）。
2. B 修 **N-180**（snake-snack）、**N-181**（duo-rush），可分 PR。
3. 回归：主干 N-94/101、N-87 冲刺菜单、N-147 `.sn-back` 仍红属旧号、N-177/178 教案与模式卡、N-174/175 拔河、N-99/97/100。bowling `.bl-btn` 仍走 N-145。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-180/181 的点击矩形，不能只看 CSS 行。
