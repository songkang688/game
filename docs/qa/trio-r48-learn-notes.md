# r48 学习笔记 · 测试员 C

**角色**：只交本文 + `trio-r48-playbook.md`。**零改 `src/**`**。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。中文简体。

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。r20–r47 学习文档均未合入主干。

**本轮抽验**：2026-08-28。无头 Chrome 本拍未跑。读 **r38**～**r47** 与 tester A r47–r50 交卷。

**纪律**：**凡 `.l99-*` 只给 A**。B 禁止改 `level99.ts`。

---

## 0. 号段权威（先合版）

| 段 | 权威 | r48 动作 |
| --- | --- | --- |
| N-94～N-108 / C-5 / N-29 | 主干 r19 B | 只回归 |
| N-99 / 97 / 100 / 109 | 主干 r19 A | 只回归 |
| N-110…116 | 永久跳过 | 勿开 |
| N-117…124 | #87 / #94 / #96 | 勿重做 |
| N-125…197 | #90～#118 | 勿重开 |
| N-198 | #119 r46 | A `.l99-*` 热区地板，仍有效 |
| N-199 | #119 r46 | B `.bl-btn`，仍有效 |
| N-200 | #119 r46 | = B 修 N-195，不新开选择器 |
| **N-201** | **tester A r47** | **首页 `pan-x pan-y` + 暂停 `.btn` 58**。r47 学习把本号写成 `.oa-back` **作废** |
| **N-202** | **tester A r48** | **矮横屏暂停弹窗收边**。r47 学习「钓鱼 `.fs-back`」**禁止再派此义 / 此号** |
| **N-203** | **tester A r49** | **结算 overlay 可滚**。r47 学习「`*-back` 巡检」**作废此号** |
| N-204 起（A r50 文档） | tester A 曾用 N-204 写 overlay 矮档钉 CTA | **学习不派暂停/overlay 可滚/钉 CTA 三义**；本轮热区号仍从 **N-204** 编，撞车取先合 |
| **N-204 起（本轮学习）** | **本轮新开** | 见 §2，全是游戏内大厅返回热区，不是弹窗 |

**并号（勿新开）**：r47 §0 并号仍适用。**`.l99-*` → 只 A（N-138 / N-198）**。N-201/202/203 **不要**再写成返回钮或 `*-back` 巡检。`.shr-toggle` → N-134（36）。`.pk-go` → N-173。`.ba-lv` → N-192。`.bl-btn` → **N-199**。`.shr-back` → **N-195**。`.bl-back` → 仍 **N-135**（本轮不拆第三颗）。fruit-stack `.fs-back` 主干已 44。

**在途勿重做**：PR **#94** / **#96** / **#99** / **#104** / **#105**～**#120** / **#107**。tester A 同分支暂停/overlay 闸（`shell.r47`～`r50.test.ts`）勿回退、勿重开同义号。

---

## 1. 读 r38–r47 与主干

### 1.1 勘误（优先）

| 号 | r47 学习曾写 | 先合 / tester A | 本轮 |
| --- | --- | --- | --- |
| N-201 | B `.oa-back` | A：首页横滑 + 暂停钮 58 | **学习义作废**；光球返回改 **N-204** |
| N-202 | B 钓鱼 `.fs-back` | A：**暂停弹窗** | **禁止再派**；钓鱼返回改 **N-205** |
| N-203 | A `*-back` 巡检 | A：**结算 overlay 可滚** | **禁止再派**；巡检改 **N-206** |

### 1.2 r38–r47 学习交卷（均未合主干）

| 轮 | PR | 号 |
| --- | --- | --- |
| r38…r44 | #111～#117 | N-174…194 |
| r45 | #118 | N-195…197 |
| r46 | #119 | N-198…200 |
| r47 | #120 | 自称 N-201…203；**其中 201–203 与 tester A 撞号** |

### 1.3 本轮新扫到、但**不开号**

| 选择器 / 主题 | 理由 |
| --- | --- |
| 全部 `.l99-*` | N-198；overlay 可滚 / 矮档钉 CTA 是 **N-203 / A 的 N-204**，只 A |
| 暂停弹窗收边 / `.btn` 58 | **N-202 / N-201**，只 A |
| `.shr-back` / `.bl-btn` | N-195 / N-199 |
| `.bl-back` | N-135 |
| fruit-stack `.fs-back` | 主干已 44 |
| `.dc-mode` 族 | padding 14 |
| 坦克双垫 | #107 |

---

## 2. 本轮新开（N-204 起）

**不是**暂停弹窗，**不是**结算 overlay 可滚。

### N-204 · B · orb-arena `.oa-back`

r47 误挂在 N-201 上的那颗。**证据**：`src/games/orb-arena/index.ts` ≈L115 `padding:7px 13px;font-size:14px`，无 min-height，估高 ≈30.8。挂载 ≈L953。`.oa-open` / `.oa-btn` 已 ≥44。

**边界**：N-135 捆四款返回；本号只打光球。N-201 是首页/暂停壳层。若 N-135 先合且已含 `.oa-back` 44，本号销。若 tester A 的 overlay 矮档先占用「N-204」号段，**本热区顺延到尚未占用的下一号**，勿改 overlay。

**建议**：`.oa-back { min-height: 44px; }`。勿改技能 / `level99.ts`。

### N-205 · B · fishing-star `.fs-back`

r47 误挂在 N-202 上的那颗。**证据**：`src/games/fishing-star/index.ts` ≈L227 `padding:6px 12px;font-size:13px`，无 min-height，估高 ≈27.6。挂载 ≈L2035「◀ 回选关」。

**边界**：**不要**写成暂停弹窗。`fruit-stack` 同名 `.fs-back` 已 44，禁止改那份。`.fs-act` → N-157。若 N-135 先合已钉钓鱼返回，本号销。

**建议**：`.fs-back { min-height: 44px; }`。勿改收线。

### N-206 · A · 游戏内大厅 `*-back` 巡检（不含 `.l99-*`）

r47 误挂在 N-203 上的那颗。**禁止**把本号写成 overlay `overflow-y:auto`。

扫 `src/games/**/*.{css,ts}` 的 `.-back{` 可点块须 `min-height`≥44。**排除全部 `.l99-*`**（N-198）。`.l99-overlay` 可滚是 **N-203**，本号不碰。

本轮应变绿：N-204 / N-205。仍可红：`.bl-back` / `.bd-back`（N-135）。绿对照：`.sr-back` / `.as-back` / `.tkb-back` / fruit-stack `.fs-back`。

---

## 3. 给 A / B

- **A**：壳层继续 N-198 / N-201～N-203（及 A 文档里的 overlay 矮档）。另收 **N-206** 巡检。不要等 B。
- **B**：N-195、N-199、**N-204**（`.oa-back`）、**N-205**（钓鱼 `.fs-back`）。**零改 `level99.ts` / `dialogs.ts` / 暂停 overlay。** 不要改 `SKY_H`。不要抬 `.shr-toggle`。
- **C-8** 禁改 `SKY_H`。N-105 禁第四份 14→16 hunk。

---

## 4. 下一空号

**N-207**。N-204…N-206 本轮占用（热区义）。若 A 的 overlay 矮档先合占用 N-204 号，学习热区从空号顺延，**语义仍不得写成暂停/overlay 可滚**。

---

## 5. 开 PR

`https://github.com/songkang688/game/pull/new/cursor/trio-r48-learn-notes-1cd5`
