# 窗口5 · 第1轮监督修复员

> 本文件由三名监督修复员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏；平台文件（`src/ui/**`、`src/engine/**`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`）与 76 款全集口径都不在本轮范围内。
> 原计划第 27–29 步的全局三人组已取消，本文只按窗口 5 的口径写。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 修复计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · 第 1 轮 · 档A 监督修复员（本档这一轮的收口人）
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`51f99ae`（对齐 `origin/game-1.2-window5`）。开工前自己复跑：`npm test` **198 文件 / 5399 用例全绿**，与学习优化员 A3 节登记的数字一致。

**输入**

| 来源 | 档A 结论 |
| --- | --- |
| `docs/qa/1.2-window5-round1-tester.md` 档A 节 | 阻断 0 / 严重 0 / 一般 5（W5-A-01…05）/ 建议 2（W5-A-06、W5-A-07，均交窗口1） |
| `docs/qa/1.2-window5-round1-learner.md` 档A 节 | 十条改进，本轮落地 5 条（W5-L-01…05），SHA `3bc5fcf` / `e738590` / `d380045` / `4e17521`，报告 `51f99ae` |

**权限边界**：只改 `src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**` 与本文件。不改测试员 / 学习优化员的报告（只读），不改平台文件，不改本档以外的游戏，不引入任何外部运行时依赖，CSS 前缀沿用各款已有前缀，用例总数只增不减。窗口 2 点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`）**跳过不验**。

**这一轮要干的六件事**

1. **阻断与严重清零**：测试员记的是 0；我自己独立复审，若新发现阻断 / 严重级问题，必须当轮修掉并带回归测试（先红后绿）。
2. **独立复审学习优化员的 5 条 diff**：逐条查①有没有破坏既有手感与断言、②有没有引入外部依赖、③报告里的量化数字对不对得上。对不上就在本报告里指出；有漏修就补修。
3. **补测漏口**：崩溃入口、快速连点 / 切游戏 / 暂停恢复、存档损坏、`destroy` 泄漏。漏了就补测并记录。
4. **商标扫描**：本档五款文案与注释扫到 0 命中。
5. **窗口边界**：窗口 1 的平台问题只登记「交给窗口1」，一行 `src/ui/**` 都不动。
6. **不开第 2 轮**。

### A1 · 复审手法

复审不只看 diff，三条通道都走了一遍：

1. **静态复核**：逐条读学习优化员改的 8 个源码文件与 5 个用例文件，再把量化数字用一次性脚本在本地重算（脚本跑完即删，未进提交）。
2. **真机复核**：`npm run build` 的产物用 `vite preview` 起起来，**Chrome 148 headless + CDP** 驱动真实浏览器，360×720 视口逐款点。仓库里没有 puppeteer / playwright，本档也不许引依赖——用的是 **Node 22 自带的 `WebSocket` 全局直接说 CDP 协议**，`package.json` / `package-lock.json` 零改动。
3. **前后对照**：把学习优化员收尾那一版（`51f99ae`）单独 `git worktree` 出来另建一份产物、另起一个 preview 端口，**同一段操作分别打在修复前后两个产物上**，本轮判为缺陷的每一条都拿到了「修复前确实坏、修复后确实好」的对照证据，而不是只有我一句话。

### A2 · 复审结论：学习优化员那 5 条

**总结论：5 条方向都对、都真的落到了地、没有破坏既有手感、没有引入任何依赖，量化数字我全部复算过、逐条对得上。**
但**题干自指那条（W5-L-02）漏了一半**，另有**一条 a11y 改动在最常见的操作路径上不生效（W5-L-05）**——两条都由我在 A3 补修。

| 学习优化员的条目 | 我的复核动作 | 结论 |
| --- | --- | --- |
| **W5-L-01** clock-house 题面钟面标签不泄答案 | 遍历 188 关把 `promptHTML` + `choices` 里**所有** `aria-label` 扫出来逐条判：共 **813** 个，带数字的 **0** 个，包含本题答案的 **0** 个。813 这个数也和源码对得上：题面 `read` 413 + `readMin` 33 + `next` 19 = 465，选项 `set` 96×3 = 288，`setMin` 15×(1 拨盘 + 3 选项) = 60，合计 813。真机上进第 1 关看 DOM：题面钟面 `aria-label="钟面，自己读一读"`，三个选项是「6 点 / 4 点 / 2 点」——**答案确实读不出来了** | ✅ 落地属实，446 这个数对得上 |
| **W5-L-01** 摘要锁「不放水」的说法 | 重点查了 `asLegacyHtml()`：它只把 `<svg data-h data-q … role="img" aria-label="…">` 这一种形状还原成 1.1 写法，还原完的 `LEGACY_DIGEST` 仍然是**原来那串** `5a314f…296cf`，一个字节没换。也就是说前 99 关只要有第二处改动，这条锁照样会红 | ✅ 没放水。**一点余量登记在案**：这个还原是「按 `data-h`/`data-q` 重算标签」，所以标签本身换成另一句不含数字的话（比如「钟面」），摘要锁不会响——挡这一格的是同一轮新加的「标签不许带数字」那条，两条合起来才严密，不能只留一条 |
| **W5-L-02** word-garden 题干自指 | 成语三条释义、偏旁 `radicalTargets()` 都复核无误；被剔字的卡确实是 **4** 张（`艹/花草植物`、`木/树木`、`讠/说话`、`目/眼睛`），剔完各剩 4–5 个字，和报告一致。`pick()` 只消耗一次随机数、前 99 关摘要 `465a6c3b` 未变，确定性没被破坏 | ⚠️ **半条漏修**：守门用例只扫了 `ask`，没扫 `promptHTML`。我把两者一起扫，又掉出 2 处共 8 道题——见 **W5-F-02 / W5-F-03** |
| **W5-L-03** 三处 44px 热区 | CSS 逐条看过（`.ld-open` / `.rbe-open` 各加 `min-height:44px` + `inline-flex` 居中，`.rbr-chip-btn` 30→44），只动尺寸没动配色圆角。**真机 360px 复量五款**：`<44px` 的可点热区 **0 个**，横向溢出 **0**——学习优化员「5 个降到 0 个」的说法成立，我这一遍是独立量的 | ✅ 落地属实 |
| **W5-L-04** color-fun 画室 `inert` + 焦点接管 | 真机验：开画室后背后两层（`.clf-tools` 模式条与选关地图）**都挂上了 `inert` + `aria-hidden="true"`**，host 里画室之外**还能聚焦的控件 0 个**（原来 11 个），初始焦点直接落在「✖ 关上画室」，关画室后 `[inert]` 节点数归 0、焦点回到「🎨 自由涂色（不计分）」。`muteBehind()` 按原值还原、重复调用安全 | ✅ 落地属实，量化对得上 |
| **W5-L-05** color-fun 画室 Esc 关闭 | 逻辑对，也确认了不会和平台打架：`gameShell.ts` 的全局 Esc 是**推迟一个宏任务再看 `defaultPrevented`**，画室这一下既 `preventDefault` 又 `stopPropagation`，真机上按 Esc 只关画室、平台暂停面板没被顶出来 | ⚠️ **最常见的那条路上不生效**：`keydown` 挂在 `.clf-sheet` 上，焦点不在画室里就收不到；线稿色块是不可聚焦的 SVG 图形，**点一下涂个颜色**（进画室要做的第一件事）焦点就被交到画室外面去了。修复前真机实测：涂完一笔按 Esc，画室**关不掉**。见 **W5-F-04** |
| 依赖与越界 | `package.json` / `package-lock.json` 零改动；`git show --stat` 逐条看过 4 个 commit，动的文件全在本档五款目录内，没碰 `src/ui/**`、`src/engine/**`、任何共用文件、任何别的游戏、任何别人的报告 | ✅ 边界干净 |
| 既有断言与手感 | 全量 `npm test` 在基线上就是全绿；`landlord-cards` 的「1.2 新增块只许 `ldc-` 前缀」、`word-garden` 的「成语题答案唯一」「答案能从字词库反查」、`color-fun` 的「涂色这件事上没有输」、`clock-house` 的四条题目正确性用例都还在绿着，没有一条被改宽或删掉 | ✅ 没有推翻既有手感 |

### A3 · 补修与新增回归

四条，全部**先红后绿**：先让新用例在旧代码上真的失败（下面每条都记了失败信息原文），再改代码转绿。

---

**W5-F-01 · 严重 · `color-fun` 画室读到坏存档会崩，而且崩完整间画室就废了**

- **怎么找到的**：按本轮口径补测「存档损坏 / 崩溃入口」时顺着沙盒存档层往上读出来的。
- **成因**：`sandbox.ts` 的 `normalizeWork()` 只管线稿下标是不是个 **≥0 的有限数**，它**不认识 `PICTURES` 有几幅**——存档被改坏、或者从装过新版（线稿更多）的机器退回旧版，都会留下一个指不到画的下标。画廊那一格本来就写着 `?? PICTURES[0]`，缩略图看着好好的；可**「点开旧作接着涂」**那条路没有兜底：`onWork()` 直接把它赋给 `picIndex`，`renderCanvas()` 再去读 `PICTURES[picIndex].regions`。
- **真机证据（修复前，`51f99ae` 单独构建的产物）**：往 `yiduo-yixing.clf.sandbox.v1` 写一张 `{pic: 999}`，开画室、点那张作品 →
  `TypeError: Cannot read properties of undefined (reading 'regions')`。
  更要紧的是**崩完还回不去**：接着再涂一笔 / 撤销 / 清空 / 换线稿，又抛 2 次同样的异常——`picIndex` 已经停在 999，画室从此点什么都没反应，只能退出游戏重进。这就是判**严重**而不是一般的理由（可复现的崩溃 + 功能不可用，只是要先有一份坏存档）。
- **修法**：`sandboxUi.ts` 新增纯函数 `safePicIndex(i)`，越界 / 负数 / 小数 / `NaN` 一律回到第一幅；`onWork()` 与 `renderGallery()` 都走它，两处不再各写各的兜底。**不动存档层**——`sandbox.ts` 故意不认识 `PICTURES`，把游戏数据塞进存档层反而是退步。
- **新增回归**：`src/games/color-fun/sandboxRobust.test.ts`。第一条同时钉两件事——存档层**确实会放行**越界下标（证明这个入口不是我编的），以及不收口**真的会抛**；后面几条钉收口行为与两处接线。
- **先红**：`AssertionError: expected [Function] to not throw an error but 'TypeError: Cannot read properties …' was thrown`（4 条全红，`safePicIndex` 当时还不存在）。
- **修复后真机复测**：同一段操作，JS 异常 **0** 个，画布正常换成第一幅、8 块区域都画得出来，后续涂 / 撤销 / 清空 / 换线稿也都 0 异常。

---

**W5-F-02 · 一般 · `word-garden`「百发百中」的题面自己把答案摆出来了（6 道题）**

- **成因**：这条成语里「百」出现两次，`blank: 2` 挖的是**后面**那个，题面渲染成「**百**发□中」——前面那个「百」明晃晃留在题上，孩子照着抄就行。学习优化员那一轮把三条**释义**的自指修干净了，但守门用例只扫 `ask`（释义句），没扫 `promptHTML`（挖了空的成语本身），所以这一条一直没露头。「每一次都正中目标」这句释义本身是干净的。
- **修法**：`blank: 2 → 1`，改挖「发」，题面变成「百□百中」。释义**一个字都没动**（「每一次都正中目标」不含「发」）。挑 1 不挑 3 是因为挖「中」的话释义里的「正中目标」又会撞上。
- **影响面**：188 关里这条卡出 **6** 道题。前 99 关摘要锁 `465a6c3b` 未变（成语题在第 7 章往后，都在 99 关之后）。

---

**W5-F-03 · 一般 · `word-garden`「操场」的形近字提示写着答案字（2 道题）**

- **成因**：`{ char: "操", word: "操场", hint: "学校里跑步做**操**的地方" }`。题目渲染成「学校里跑步做操的地方，「□」里填哪个字？」，选项是 操 / 燥 / 躁——提示句里就写着「操」。同一条规矩字卡那边早就有（`bank.test.ts` 的「不把答案字或示例词直接抄进去」），形近字这批是 1.1 后加的，一直没人钉。
- **修法**：提示换成「学校里上体育课的空地」。这里踩到一个既有约束值得记一笔：`levels188.test.ts` 钉着「每道题的 `ask` 汉字数 ≤ 15」，我第一版写「学校里上体育课的大空地」（11 字）就把 `ask` 顶到 16 直接红了——**说明这条既有断言是活的**。改成 10 字后 `ask` 仍是 15，压着上限过。
- **影响面**：188 关里 **2** 道题。
- **W5-F-02 / W5-F-03 的守门**：`noSpoiler.test.ts` 加一组 3 条——①成语挖空以后**露在外面**的那几个字里不许再出现答案字；②形近字提示句不许含答案字；③188 关的成语题 / 偏旁题 / **形近字题**三类，`promptHTML` 与 `ask` **一起读**都读不出答案（覆盖 300+ 道，带 `checked > 300` 自检防空转）。同时在 `IdiomCard.blank` 与 `LookalikeItem.hint` 的注释里把规矩写死，免得后面加数据的人再踩。
- **先红**：三条同时红，报文分别是
  `「百发百中」挖第 2 位，题面「百发中」里还留着答案字「百」`、
  `「操场」的提示「学校里跑步做操的地方」写着答案字「操」`、
  `第 113 关 lookalike 的题面读得出答案：□场 学校里跑步做操的地方，「□」里填哪个字？ → 操`。

---

**W5-F-04 · 一般 · `color-fun` 画室「涂过一笔之后 Esc 就不灵了」（补 W5-L-05 的另一半）**

- **成因**：`keydown` 挂在 `.clf-sheet` 上，只有焦点还留在画室里才收得到。线稿上的色块是 SVG 图形、不可聚焦，点它一下浏览器会把焦点交给**最近的可聚焦祖先**；画室没有 `tabindex`，焦点就一路交到画室外面去了，Esc 从此冒不到画室上——偏偏「点一下涂个颜色」就是进画室要做的第一件事。
- **真机前后对照**：修复前，涂完一笔 `document.activeElement` 是 `DIV`（没有 `clf-sheet` 类），按 Esc **画室还在**；修复后 `activeElement` 是 `DIV.clf-sheet`，按 Esc **画室关掉了**，焦点回到「🎨 自由涂色（不计分）」，平台暂停面板没有被同一下 Esc 顶出来。
- **修法**：给 `.clf-sheet` 补 `tabindex="-1"`。用 `-1` 是为了只接鼠标点击带来的焦点、**不往 Tab 序里插一站**——这也是 `role="dialog"` 容器的通行写法，和 W5-L-04 已经做的初始焦点 / 焦点归位是同一套东西的最后一件。
- **守门**：`sandboxRobust.test.ts` 加一条源码巡检，钉 `role="dialog"` 与 `tabindex="-1"` 一起在位。

---

**另补一处覆盖漏口（不是缺陷，是没人看着）**

- 本档五款里，`landlord-cards` / `clock-house` / `word-garden` 各有 `shell.test.ts`、`color-fun` 有 `upgrade12.test.ts`，四款都钉着「destroy 归零」与「整个目录一个商标都不沾」；**只有 `red-blue-race` 一条都没有**——偏偏它是五款里唯一常驻 rAF（关内约 60fps）、还挂着两套全局键盘监听的那个，最经不起漏一次清理。
- 新增 `src/games/red-blue-race/shell.test.ts`（12 条）：三处入口的 `destroy` 都收 runtime；计时器 / 动画帧 / 监听只从 `createRuntime()` 一个登记簿出去、`dispose()` 一次全收、全库无 `setInterval`；全局键盘只有 `bindKeys()` 一个口子且三处调用都进 `rt.own()`；卸载器重复调用安全（拿桩真跑，不是巡检）；不建 `AudioContext`；侧场入口连点只开得出一个；换对手先 `dispose` 上一局；**暂停 / 切后台回来那一大跳的 `dt` 被三条主循环一致夹在 0.05s**；退出后排队中的回调不补跑；整个目录（注释也算）零商标、无流血受伤赌博说法，外加一条筛子自检防空转。

### A4 · 补测漏口：真机结果

产物 = `npm run build` 的 `dist`，`vite preview` 起在 127.0.0.1:4173，Chrome 148 headless + CDP，360×720，全部从真实 DOM 读、真实事件驱动。

| 口子 | 做法 | 结果 |
| --- | --- | --- |
| **崩溃入口** | 五款逐款进 / 退，全程收 `Runtime.exceptionThrown` 与 `console.error`；另外把画室的坏存档路径单独打了一遍 | 五款 **0 个 pageerror / 0 个 console.error**；坏存档那条修复前抛 `TypeError`、修复后 0 异常（见 W5-F-01） |
| **`destroy` 泄漏** | 每款进→退 5 轮，退出后留 1.2s 静默窗口，统计 `requestAnimationFrame` / `setTimeout` / `setInterval` 的**新增调用数**、`AudioContext` 累计数、`window` 上加了没摘的监听 | 五款**全部 rAF=0 / setTimeout=0 / setInterval=0**，`AudioContext` 累计 **0**（声音都走平台 sfx），`window` 上未摘的监听 **{}**（一个不剩） |
| **DOM 是否逐轮叠加** | `landlord-cards` 进退 5 轮，逐轮量文档元素总数 | **589 → 589 → 589 → 589 → 589**，一轮不涨 |
| **快速连点** | `red-blue-race` 对战场入口连点 12 下 | 侧场只开出 **1** 层（`openSide()` 的 `if (side) return` 兜住了） |
| **切游戏** | 每 220ms 硬切一款、**不等加载完**，连切 5 款再回首页 | 无异常，DOM 回到首页水位；五款各自的 `destroy` 都能在半路被打断而不报错 |
| **暂停恢复** | 三条主循环的 `dt` 上限 | 闯关 / 对战场 / 无尽**三条一致夹在 0.05s**，暂停面板或切后台停几秒回来不会一帧冲线；已写成用例钉住 |
| **存档损坏** | 沙盒作品存档写成 `{pic:999}`；另复核了 `normalizeWork` / `normalizeWorks` / `loadWorks` 对非数组、非对象、坏 `fills`、超上限的处理 | 存档层本身够硬（读不出来一律当没有）；唯一漏的是**下标越界**这一格，已修（W5-F-01） |
| **360px 热区 / 溢出** | 五款逐款量所有可见可点元素的盒子 | `<44px` 的热区 **0 个**，横向溢出 **0**（`scrollWidth - clientWidth == 0`） |

### A5 · 商标扫描

- **口径**：本档五款目录下**全部 54 个生产 `.ts` 文件**（`*.test.ts` 除外），面向孩子的文案**和代码注释一起扫**。
- **词表**：仓库 `src/games/copy.test.ts` 的 `BRAND_WORDS`（含「迪士尼」「宝可梦」「超级马里奥」等）**并上**本窗口提示词点名的那 29 个词（「愤怒的小鸟」「植物大战僵尸」「地铁跑酷」「屁王兄弟」「Tetris」「Minecraft」「Pac-Man」「斗地主」「蛋仔」…），去重后 **108 个词**。
- **结果**：**0 命中**。特别确认「斗地主」在 `landlord-cards`（朵朵抢地主）的生产文件里一次都没出现。
- 测试员登记过的 `red-blue-race/logic.ts:252` 那句注释「只是坐一下，**不掉血**、不判输」我复看了：它是明写着「不会发生什么」的说明，不是伤害描写，与测试员判断一致，**不算命中**。本轮给 `red-blue-race` 新加的红线用例也是按整词查「流血 / 受伤 / 骨折 / 打死 / 杀死 / 死亡 / 赌注 / 下注 / 输钱 / 赢钱」，不收「掉血」这种会误伤该句的写法。
- 这一遍不止是跑一次：`red-blue-race` 原来没有自己的商标守门，本轮已经补上（见 A3 末尾），五款现在**各有各的常驻扫描**，不用靠人记得跑。

### A6 · 遗留清单

**本档 A 节现在的水位：阻断 0、严重 0。** 严重那一条（W5-F-01）是我这轮自己发现、自己修完并带回归的，不是留给下一轮的。

| 编号 | 级别 | 一句话 | 去哪 |
| --- | --- | --- | --- |
| W5-A-01…05 | 一般 ×5 | 测试员第 1 轮记的五条 | **已清**（学习优化员 W5-L-01…04 落地，我复审通过） |
| W5-F-01 | 严重 | 画室读到坏存档会崩 | **本轮已修**（`1092b93`） |
| W5-F-02 / F-03 | 一般 ×2 | 「百发百中」题面、「操场」提示仍写着答案 | **本轮已修**（`a323fb5`） |
| W5-F-04 | 一般 | 涂过一笔之后 Esc 关不上画室 | **本轮已修**（`c032f33`） |
| W5-A-06 | 建议 | `parentAuth.ts` 被 `parentGate.ts` 静态引用，`gameShell.ts` 那处动态 import 失效，家长门代码回落进主包 | **交给窗口1**。本轮构建期这条 `[INEFFECTIVE_DYNAMIC_IMPORT]` 告警仍在，属预期，`src/ui/**` 一行没动 |
| W5-A-07 | 建议 | `package.json` 的 `version` 还是 `1.1.0`、`description` 还写「1.1 —— 55 款」 | **交给窗口1 / 发布环节**。另登记一处同源的：`vite.config.ts` 的 PWA `manifest.description` 也还写着「一朵一星 1.1 —— 55 款原创小游戏合集」，改的时候两处一起改 |
| W5-L-06 | 建议 | 把「题面不许泄答案」抽成全库共用的守门工具 | **交给窗口1**（跨窗口共用文件）。本轮我这两组用例又把这条规矩写了第三遍，**抽取的必要性比学习优化员写报告时更强了**：`clock-house` 盯 `aria-label`、`word-garden` 盯 `promptHTML` + `ask`，同一条规矩已经有三种写法。抽的时候建议直接按「题面渲染出来的**任何**文本表示（`ask` / `promptHTML` / `aria-label` / `title`）都不得包含 `answer`」一次写死 |
| W5-L-07 | 建议 | 44px 热区抽成平台级 CSS 变量 / 工具类 | **交给窗口1**（要动 `src/styles.css` 与 `src/ui/**`） |
| W5-L-08 | 建议 | `red-blue-race` 战役对手的局内动态难度 | **留第 2 轮**。直接动玩法手感，要先补一批 `runAiLane` 的对局分布回归，还要和已有的「🤝 让分」开关定位一起想清楚 |
| W5-L-09 | 建议 | `clock-house` 错题回顾「统计 → 回顾题型」这条链做成可回放的种子用例 | **留第 2 轮**。是覆盖率不是缺陷，本档目录内即可完成 |
| W5-L-10 | 建议 | `landlord-cards` 三档牌力提示的「不泄底」守门 | **留第 2 轮**。要先定义清楚「最强那一档本来就建议出哪一手」算不算泄底，口径没定之前写断言是拍脑袋 |
| W5-F-05 | 建议 | `word-garden` 有 13 道题的**指令语**里带着答案字：`count` 题「数**一**数，用哪个汉字表示？」而答案是「一」（10 道），`py2char` 题「读**一**读拼音…」答案是「一」（3 道） | **留第 2 轮**，并附一条硬约束：这两类题都在**前 99 关**，`levels188.test.ts` 的 `465a6c3b` 摘要锁钉着「前 99 关一字未改」，动指令语就得连锁一起处理，不是改句文案那么简单。我判它比 W5-F-02 弱得多——「数一数 / 读一读」是通用指令套话不是线索，孩子要靠数图上有几个才答得对；登记在案由第 2 轮定口径 |
| W5-F-06 | 建议 | `clock-house` 的摘要锁现在靠「把标签按 `data-h`/`data-q` 还原后比对」，**挡不住把标签换成另一句不含数字的话**（挡这一格的是同轮新加的「标签不许带数字」那条） | **留第 2 轮**，两条合起来才严密，不能只留一条。不是缺陷，是登记这条锁的边界 |

**明确跳过 / 没做的**：窗口 2 点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`）一律未验；76 款全集口径已取消，未验；档B / 档C 五款不属本档，一行没看；**第 2 轮没有开始**。

### A7 · 改动文件登记

| 文件 | 改了什么 | 对应 |
| --- | --- | --- |
| `src/games/color-fun/sandboxUi.ts` | 新增导出纯函数 `safePicIndex()`，`onWork()` 与 `renderGallery()` 都走它；`.clf-sheet` 补 `tabindex="-1"` | W5-F-01 / W5-F-04 |
| `src/games/color-fun/sandboxRobust.test.ts`（新增） | 5 条：坏存档不许崩（含「不收口真的会抛」的反证）、越界收口、缩略图与画布都画得出、Esc 焦点前提、两处接线巡检 | W5-F-01 / W5-F-04 的守门 |
| `src/games/word-garden/logic.ts` | 「百发百中」`blank: 2 → 1`；「操场」`hint` 换成「学校里上体育课的空地」；`IdiomCard.blank` 与 `LookalikeItem.hint` 的注释各把规矩写死 | W5-F-02 / W5-F-03 |
| `src/games/word-garden/noSpoiler.test.ts` | 加一组 3 条（成语挖空后露出的字、形近字提示句、188 关三类题的 `promptHTML` + `ask` 一起读） | W5-F-02 / W5-F-03 的守门 |
| `src/games/red-blue-race/shell.test.ts`（新增） | 12 条：destroy 归零、快速连点 / 切模式 / 暂停恢复、文案红线 | 覆盖漏口 |
| `docs/qa/1.2-window5-round1-fixer.md` | 本报告（只写档A 节） | 本轮产出 |

**没碰**：`src/ui/**`、`src/engine/**`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`、`package.json` / `package-lock.json`、`vite.config.ts`、本档以外的任何游戏目录、`docs/qa/1.2-window5-round1-tester.md`、`docs/qa/1.2-window5-round1-learner.md`、本文件的档B / 档C 节。**没有引入任何依赖**——真机复核用的是 Node 22 自带的 `WebSocket` 直接说 CDP，没装 puppeteer / playwright。**没有动任何存档 key**。

### A8 · 验收

| 项 | 学习优化员收尾（`51f99ae`） | 本轮收尾 | 结论 |
| --- | --- | --- | --- |
| `npm test` | 198 文件 / 5399 用例全绿 | **200 文件 / 5419 用例全绿**，0 失败 0 跳过，35.7s | ✅ 只增不减（+2 文件 / **+20 条**） |
| `npm run build` | 全绿 | **全绿**，`tsc --noEmit` 0 错，`✓ built in 379ms`，PWA precache 139 项 | ✅ |
| 主包 `index-*.js` | 86.80 kB（gzip 30.50） | **86.80 kB（gzip 30.50）** | ✅ 一个字节没涨 |
| `clock-house` chunk | 29.18 kB | 29.18 kB | ✅ 未动 |
| `red-blue-race` chunk | 46.16 kB | 46.16 kB | ✅ 未动（只加了用例） |
| `landlord-cards` chunk | 54.71 kB | 54.71 kB | ✅ 未动 |
| `color-fun` chunk | 59.10 kB | 59.18 kB（+0.08） | ✅ `safePicIndex` + `tabindex` + 注释的全部代价 |
| `word-garden` chunk | 67.38 kB | 67.38 kB | ✅ 只换了两处数据字面量 |
| 真机 360px `<44px` 热区 | — | 五款各 **0 个** | ✅ |
| 真机 `destroy` 5 轮 | — | 五款 rAF / setTimeout / setInterval 增量全 **0**，`AudioContext` **0**，`window` 未摘监听 **{}**，DOM 恒定 | ✅ |
| 真机 pageerror / console.error | — | 五款全程 **0** | ✅ |
| 商标扫描 | — | 54 个生产文件 × 108 个词 = **0 命中** | ✅ |

新增 20 条用例的分布：`color-fun` +5（新文件）、`red-blue-race` +12（新文件）、`word-garden` +3（并进既有的 `noSpoiler.test.ts`）。

**本轮不开第 2 轮。**

## 档B · 五款（待填写）

## 档C · 五款（待填写）
