# 1.3 窗口 1 · 第 2 轮学习优化员报告

> 状态：学习计划已立项，评估进行中。

## 学习计划（动手前立项）

1. **只读必读文档**：`1.3-window1-round2-tester.md`（水位 725/15575，P1–P10 回归 12/12，R2-1/R2-2/R2-3）、第 1 轮 learner/fixer/tester 三份报告、`plan-1.3-visual-bible.md`、`1.3-window1-plan.md`。
2. **外部学习**（只记结论文字，不下载、不截图、不描摹）：4399 及同类平台同题材头部作品，只学两件事——画面密度（背景层数/装饰物件数/动效通道数）与角色剪影（16px 可辨识依据）。九题材基准表基于第 1 轮更新「本库现状」列。
3. **对照评估本窗 12 格**（1A kit / 1B motion / 1C runner + orb-arena、snake-royale、block-drop、combo-clash、mahjong-bloom、star-estate、hero-cards、weiqi-garden、flight-chess）：每格四字段——密度差距（数字化）/ 剪影达标 / 观感短板 TOP1（点名文件与函数）/ 建议动作。已回归通过的格子不编造短板。
4. **建议清单**：按「严重度 × 修复成本」排序，绘制规格级；R2-2（`.hc-table` ≤4% 斜织纹）必入；R2-1（共享壳 `.l99-star`）写明跨窗去向；R2-3 提醒 C 档用真名 `.mj-board`；L-3/L-4/L-5 维持第 1 轮去向。
5. **文案与引导**（唯一可动代码范围）：抽查九款 `guide.ts` / `meta.ts` blurb 是否已覆盖第 1 轮 fixer 新落地信号（地格 SVG 图标、中景贴片、井壁浮雕、远景色岛、土丘明暗、四象限草云、卡面纸感、毛毡织纹等）；只补「修复后仍没讲到」的亮点，改动逐条登记并补契约测试（只增不减）。
6. **红线**：不改绘制代码、不改 kit、不改判定/数值/关卡、不碰别人的报告与 plan；无商标无官方角色；收尾 rebase + `npx vitest run` / `npx tsc --noEmit` / `npm run build` 全绿后普通 push。

（以下各节评估完成后填写。）
