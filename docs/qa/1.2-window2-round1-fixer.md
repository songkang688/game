# 窗口 2 · 1.2 三轮验收 第 1 轮 · 监督修复员 · 汇总

> 每个 Pack 只追加自己那一节，不动别人的段落。
> 完整明细各自在 `docs/qa/1.2-window2-round1-fixer-pack*.md` 里。

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 详版：[`docs/qa/1.2-window2-round1-fixer-packB.md`](./1.2-window2-round1-fixer-packB.md)
- 输入：`docs/qa/1.2-window2-round1-tester-packB.md`（阻断 0 · 严重 2 · 一般 10）、`docs/qa/1.2-window2-round1-learner-packB.md`
- 结论：**阻断 0 条（本来就是 0）· 严重 2 条全部清零 · 一般 10 条全部登记留第 2 轮。**

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| B-2 | 严重 | `tap-tiles` | 同谱对战判定从两态改三态：赢仍旧是严格大于，`<` 才是对手赢，同分就是平局。平局不记给任何一方（另记 `draws`，顶栏写成 `你 0 : 0 地狱 · 平 1`），结算换成 `🤝 打成平手!`，屏幕上的两个数字和结论终于是一回事 |
| B-6 | 严重 | `hop-pads` | 双人同屏触屏按手指号分台：`onDown` 把 `pointerId` / `changedTouches[].identifier` 记进本台账本，`onUp`（`pointerup` / `pointercancel` / `touchend` 共用）先对账再决定放不放。一个人抬手不再把两个人一起弹出去 |

先红后绿都留了证据：B-2 的红是「你 4200 分 · 对手 4200 分」配「🤖 这局对手分高一点」，B-6 的红是只抬一根手指却读出 `['flying','flying']`。

**新增回归 2 个文件 / 13 条**：`src/games/tap-tiles/versus.test.ts` 4 条、`src/games/hop-pads/duo.test.ts` 9 条。
既有用例一条没删、没放宽；`dark-chess` / `hue-hand` / `fight-king` 本轮一个字节没动。

**遗留 10 条**（全是一般级，明细见详版第三节）：其中 B-1 / B-4 / B-5 / B-8 已由学习优化员在本轮落地闭合，B-7 / B-9 / B-10 各闭合一半，
真正留给第 2 轮的是 **B-3**（`菜鸟 / 地狱` 档名）、**B-7 / B-9** 的键位口径、**B-10 / B-11**（格斗塔后段的墙在角色适配，B-11 优先）；
**B-12** 训练场进不了 `meta.modes` 是 `src/engine/types.ts` 的平台枚举，按纪律不碰窗口 1 的文件。

`npm test` rebase 后 **251 文件 / 6386 用例全绿**（本包 +2 文件 / +13 用例）；`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
三张黑名单 0 命中；没引外部依赖、没碰 `src/engine/**` 与窗口 1 平台文件、没回滚学习优化员的任何改动。
