# 窗口 2 · 1.2 三轮验收 第 1 轮 · 监督修复员 · 汇总

> 每个 Pack 只追加自己那一节，不动别人的段落。
> 完整明细各自在 `docs/qa/1.2-window2-round1-fixer-pack*.md` 里。

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 详版：[`docs/qa/1.2-window2-round1-fixer-packB.md`](./1.2-window2-round1-fixer-packB.md)
- 输入：`docs/qa/1.2-window2-round1-tester-packB.md`（阻断 0 · 严重 2 · 一般 10）、`docs/qa/1.2-window2-round1-learner-packB.md`
- 结论：**阻断 0 条（本来就是 0）· 严重 2 条全部清零 · 一般 10 条全部登记留第 2 轮。**

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| B-2 | 严重 | `tap-tiles` | 同谱对战判定从两态改三态：赢仍旧是严格大于，`<` 才是对手赢，同分就是平局。平局不记给任何一方（另记 `draws`，顶栏写成 `你 0 : 0 地狱 · 平 1`），结算换成 `🤝 打成平手!`，屏幕上的两个数字和结论终于是一回事 |
| B-6 | 严重 | `hop-pads` | 双人同屏触屏按手指号分台：`onDown` 把 `pointerId` / `changedTouches[].identifier` 记进本台账本，`onUp`（`pointerup` / `pointercancel` / `touchend` 共用）先对账再决定放不放。一个人抬手不再把两个人一起弹出去 |

先红后绿都留了证据：B-2 的红是「你 4200 分 · 对手 4200 分」配「🤖 这局对手分高一点」，B-6 的红是只抬一根手指却读出 `['flying','flying']`。

**新增回归 2 个文件 / 13 条**：`src/games/tap-tiles/versus.test.ts` 4 条、`src/games/hop-pads/duo.test.ts` 9 条。
既有用例一条没删、没放宽；`dark-chess` / `hue-hand` / `fight-king` 本轮一个字节没动。

**遗留 10 条**（全是一般级，明细见详版第三节）：其中 B-1 / B-4 / B-5 / B-8 已由学习优化员在本轮落地闭合，B-7 / B-9 / B-10 各闭合一半，
真正留给第 2 轮的是 **B-3**（`菜鸟 / 地狱` 档名）、**B-7 / B-9** 的键位口径、**B-10 / B-11**（格斗塔后段的墙在角色适配，B-11 优先）；
**B-12** 训练场进不了 `meta.modes` 是 `src/engine/types.ts` 的平台枚举，按纪律不碰窗口 1 的文件。

`npm test` rebase 后 **251 文件 / 6389 用例全绿**（本包 +2 文件 / +13 用例）；`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
三张黑名单 0 命中；没引外部依赖、没碰 `src/engine/**` 与窗口 1 平台文件、没回滚学习优化员的任何改动。

---

## Pack A

- 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
- 详版：[`docs/qa/1.2-window2-round1-fixer-packA.md`](./1.2-window2-round1-fixer-packA.md)
- 输入：`docs/qa/1.2-window2-round1-tester-packA.md`（阻断 0 · 严重 3 · 一般 13）、`docs/qa/1.2-window2-round1-learner-packA.md`
- 结论：**阻断 0 条（本来就是 0）· 严重 3 条全部清零 · 顺手收掉同根的 PA-JQ-2 · 一般 12 条全部登记留第 2 轮。**

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| PA-FS-1 | 严重 | `fruit-stack` | 360px 上 `.fs-btn` / `.fs-back` / `.fs-open` / `.fs-pick` 各补 `min-height:44px` 并改成竖直居中（`padding` 与版式不动）；`@media (max-width:420px)` 里的 `.fs-key` 从 `height:42px` 改回 `44px` —— 窄屏只缩字号与最小宽度，不缩高度 |
| PA-PS-1 | 严重 | `pool-stars` | `Escape` 提到 `onKeyDown` 最前面并紧跟 `if (paused) return`，`onKeyUp` 同样先看；进暂停时把蓄到一半的那一杆作废；`startCharge` / `releaseCharge` / `nudge` 与旋转 / 摆球 / 叫袋钮各加一道 `paused` 闸；暂停期间按钮直接置灰。恢复之后一切照旧 |
| PA-JQ-1 | 严重 | `junqi-camp` | `view.ts` 的 `let cursor` 拆成 `cursors: Record<Side, Pos>`，`moveCursor` / `activate` / `cancel` / `cursor` 全带座位，回合外那一套键只挪得动自己的光标、选不了子；画面上仍只画当前该走的那一位那一个虚线框 |
| PA-JQ-2 | 一般（同根） | `junqi-camp` | `index.ts` 的 `onKey` 拆成朵朵（WASD + F/G）与星星（方向键 + L/K）两套；单人局里两套都归朵朵，老键位一条不丢。工具条键名与攻略同步 |

先红后绿都留了证据：PA-FS-1 改样式前 5 例红；PA-PS-1 拿修复前的 `view.ts` 跑现在这一组 6 例红；
PA-JQ-1 拿修复前的 `view.ts` / `index.ts` 跑 5 例红（方向键那条一开始写成「上下左右各按一次」净位移是 0，旧代码能蒙混，已改成同方向连按两下才真红）。

**回归只增不减**：3 个既有文件里 9 例【已知问题】翻成修好后的行为，另新增 8 例
（`fruit-stack` 7→8、`pool-stars` 6→9、`junqi-camp` 26→30）。本包 5 款 36 文件从 719 例增到 **727 例**，既有断言一条没删、没放宽。
`dot-maze` / `chess-garden` 本轮一个字节没动。

**遗留 12 条**（全是一般级，逐条理由见详版第三节）：`PA-DM-1/2/3`、`PA-FS-2/3`、`PA-PS-2/3`、`PA-JQ-3`、`PA-CG-1/2/3/4`。
其中 `PA-PS-3` 与 `PA-CG-3` 是同一个「`document.head` 里的样式拆不掉」，
`PA-DM-3` / `PA-FS-2` / `PA-CG-2` 是同一类「规格里的动作键还没给语义」，第 2 轮各并成一条改更省事。

`npm test` rebase 后 **251 文件 / 6393 用例全绿**（本包 +8 用例）；`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
没引外部依赖、没碰 `src/engine/**` 与窗口 1 平台文件、没改 `src/styles.css`、没回滚学习优化员落地的 6 条 AI 改进。

---

## Pack C · 点名五项

- 范围：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 详版：[`docs/qa/1.2-window2-round1-fixer-packC.md`](./1.2-window2-round1-fixer-packC.md)
- 输入：`docs/qa/1.2-window2-round1-tester-packC.md`（阻断 1 · 严重 0 · 观察 3）、`docs/qa/1.2-window2-round1-learner-packC.md`
- 结论：**阻断 1 条清零 · 严重 0 条 · 点名五项独立复验全部通过。**

唯一那条阻断是 `gomoku` 的「菜鸟→地狱相邻档强度不单调」，两个根因都在 `ai.ts`：

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| C-G-1 | 阻断 | `gomoku` | `smartMove` 的总分把「占掉对手要点」算了两遍（`0.95×对手分` 一遍、`−0.6×对手最佳回应` 又一遍），等效防守权重 ≈1.55 而进攻只有 1.0 —— 聪明档比普通档更怕事，只会拆家不会成家。防守权重与普通档对齐（0.9），多出来的那一层改成只看「对手的下一手」：自己有要害点就抢先手、对手有要害点就真的把那条活三拆掉（落完子再查一遍破没破掉）、送要害点给对手硬扣分 |
| C-G-2 | 阻断（同根） | `gomoku` | `hellMove` 两道口子把答案原样交回 `masterMove`：① 对手一有威胁就 `return masterMove(...)`，21% 的局面压根不搜；② 平静局面要赢过 `SEARCH_OVERRIDE_MARGIN = 4000` 才改着，实测一次都没跨过去。①改成 `hellDefend`：候选与大师档同一批，但复核用的算杀从 4 手加深到 6 手且要求对手没有连续威胁杀，幸存者再交给深搜排座次；②门槛降到 200，并配一道「深搜没跑够 4 层就不许改着」的闸 |
| C-G-3 | 顺带 | `gomoku` | 聪明档改强之后大师档在老用例上掉了 3/12（都是执白输）。把挑点时对手棋型的权重 0.95→1.15，破杀候选池补上「自己这边分数最高的点」，挑法从「按顺序拿第一个安全的」改成「两道筛子里各选自己分最高的」。大师档对简单档执白也从 8 胜 2 负变成 9 胜 1 负 |

先红后绿都留了证据：改之前 `聪明→普通` 4:4、`地狱→大师` 4:4，48 个真实中盘局面上 `hell == master` **48/48**。
修完之后相邻档五对（强档执黑执白各一半、固定 seed、地狱档冻住时钟）：

```
菜鸟→简单 36-0-0   简单→普通 36-0-0   普通→聪明 22-1-13
聪明→大师 36-0-0   大师→地狱 19-5-0                     （格式：强档胜-负-和，12 局 ×3 seed 基点；地狱那对是 8 局 ×3）
```

**回归只增不减**：`tiers.test.ts` 补一条「聪明与普通不许 100% 走同一步」，
相邻档五对那一组与并行的同角色续作 rebase 后合成一份（保留对方更严的版本，不重复跑）。
原有四条实战用例（挑的是 master→**easy**、hell→**normal**，恰好跳过两对相邻档）一条没删、没放宽。

**点名五项独立复验**（逐项证据见详版第 3 节）：
`gomoku` 相邻档单调 ✅ + 抽 10 关按攻略实走全部黑五连、188 关无解 0 条、前 99 题漂移 0 条 ✅；
`match-stars` 消除 13 帧 / 下落 16 帧 / 补位 16 帧，视觉行浮点滑动，降速轨迹一致，无瞬变 ✅；
`rainbow-run` `view3d.ts` `controls.ts` 与 1.1 diff **0 行**、`three` 0 命中、`makeCamera` 仍是「初始化一次 + 尺寸真变了才重算」两个调用点 ✅；
`ocean-munch` 无尽泡到失败、`recordEndlessBest("ocean-munch", 147)`、destroy 后重进读回 147 ✅；
`xiangqi` 只在既有目录、六档单调（残局考卷 1/50/50/56/58/60）、188 残局无解 0 条、长将三种边界判定正确 ✅。

本轮除 `gomoku/ai.ts` 与 `gomoku/tiers.test.ts` 外，其余四款**一个字节都没改**，只被复验。

`npm test` rebase 后 **251 文件 / 6400 用例全绿**；`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
`puzzles.ts` / `solve.ts` / `session.ts` / `index.ts` 未碰，学习优化员落地的 6 条改进一条没回滚；
没碰 `src/engine/**` 与窗口 1 平台文件，没写全局 `docs/qa/1.2-round1-fixer.md`，走查脚本跑完即删、没进仓库。
### Pack C 续作 · 同角色第二人的独立复验

本包由两位同角色的监督修复员先后接手：前一位落地 `ai.ts` 的三处修法，
**先红那一组用例与修完之后的独立复验由续作补齐**（详版第 6 节）。
下面的数字是在 `cf4c7bf` 上重新量的，与上面那组互相独立。

先红留痕（提交 `f584687`，修法就是接着这组红用例做绿的）：
`聪明→普通 4:4`、`地狱→大师 4:4`、24 个真实中盘局面 `hell == master` **24/24**。

把每对从 12 局加到 **36 局（12 局 × 3 个 seed 基点）**重量，五对在**每一个基点上**高档都赢：

```
菜鸟→简单 36-0-0   简单→普通 36-0-0   普通→聪明 25-1-10(69.4%)
聪明→大师 36-0-0   大师→地狱 28-8-0(77.8%)      地狱档换 6 层深搜再验: 22-12-2,三个基点仍全赢
同步率:  master vs hell 16/24(修前 100%)   normal vs smart 21/24
解局考卷: novice=4 easy=74 normal=105 smart=107 master=110 hell=110   ← smart 不再与 normal 持平
```

续作发现并补上的唯一缺口：那两条名点用例原本只钉 `seed0 = 1000` 一个基点，
而测试员当初正是怀疑「12 局是 seed 噪声」才加跑到 120 局的。
于是给 `series()` 加了可选的 `seed0`（默认 1000，老调用一个字没变），
补一条「倒挂过的两对在 3000 / 7000 两个基点各再跑 8 局，高档仍须赢」
（实测 `3000: 3-0-5 / 5-3-0`，`7000: 7-0-1 / 7-1-0`）。**用例只增不减，只加了这 1 条。**

点名五项续作也各自重取了一遍证：`gomoku` 抽 10 关按主变实走全部黑五连、188 关无解 0 条、
前 99 题相对 1.1 逐字段漂移 0 条；`match-stars` 消除 13 帧 / 幸存块在半空 14 帧 / 新块在盘外 16 帧，
降速轨迹与常规一致；`rainbow-run` `view3d.ts` `controls.ts` diff **0 行**、`three` 0 命中、
`makeCamera` 仍是 `index.ts:407/419` 两个调用点；`ocean-munch` `recordEndlessBest(meta.id, scoreDepth)`
原样在 `index.ts:1010`，自带的「泡到失败 → 恰好上报 1 次」用例全绿；
`xiangqi` 中国象棋仍只有 `xiangqi` 一款目录，188 课残局全量可解用例全绿。

续作只改了 `src/games/gomoku/tiers.test.ts` 一个源文件，
`ai.ts` 保持前一位的版本、学习优化员的 `session.ts` / `index.ts` 一个字节没碰。
收尾水位：最后一次 rebase 到 `b2650f3` 之后，**`npm test` 251 文件 / 6431 用例全绿**
（前一位 6400 + 窗口内其它包 + 本续作 1），点名五款 38 文件 / 1021 例全绿，
`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）。
