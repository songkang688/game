# 1.3 窗口 7 · 第 3 轮（终验）视觉验收 · 测试员报告

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 3 轮终验测试员（A 档）。只写本报告与 `*.test.ts`，不改绘制代码、不改玩法/数值/关卡，不碰前两轮 A/B/C 六份报告与他人文件。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。
> 扫描环境：node 22 / vitest 4.1.11 / vite 8.2.2；node 环境无浏览器，动态验证以「可执行用例 + 源码定量扫描 + commit diff 审计」落地，凡以代码预算替代真机观测处均已注明。
> 本轮定位：**终验**，只回答「这套视觉能不能放行」，结论三选一见第五节。

## 〇、终验计划（开工首 commit，`069b045`）

1. 全量跑数据（对账基线 + 收尾合并态），用例只增不减；
2. 第 2 轮 A 档新发现 × C 档修复 × 遗留清单逐条对账，每条三选一：已修复 / 合理降级 / 仍未修（升级为终验阻断项）；
3. 前两轮机器化守门用例存活性全查（被删 / 被 skip 按阻断）；
4. 九款 × 六专项终扫，产出 9 行 × 6 列二值总表；
5. 回归防线终验加严 + `vite preview` 冒烟；
6. 终验守门用例（qaW7r3）+ 终验结论。

## 一、全量跑数据 + 三轮用例数曲线

| 项目 | 结果 |
| --- | --- |
| `npm ci` | ✅ 无报错 |
| `npm test`（开工态，对账基线） | ✅ **741 文件 / 15437 用例全绿**，179.0s —— 与第 2 轮合并态（C 档报告 741/15437 @ `7fc6da2`）**逐位一致，Δ=0**（其间只有 docs 提交），只增不减 ✅ |
| `npm run build`（tsc --noEmit + vite build） | ✅ 全绿，637ms；PWA 预缓存 **190 项 / 5250.39 KiB**（与 R2 合并态逐位一致，本轮 0 绘制改动，体积零增长） |
| 本轮新增用例 | **+1 文件 / +6 条**（`src/games/qaW7r3.test.ts`，见第四节末），收尾态 **742 文件 / 15443 用例全绿** |
| `npx vite preview` 冒烟 | ✅ `/`、`/sw.js`、`/manifest.webmanifest` 全部 **200** |

**三轮用例数曲线（只增不减，全程单调递增 ✅）：**

```
R1 开工基线 698/14996（origin/game-1.3）
→ R1 合并态   735/15384（fixer 报告 @ f8b944a 链）
→ R2 A 档     736/15397（+qaW7r2 13 条）
→ R2 合并态   741/15437（+copyW7r2 +qaW7r2Fix ×4 @ 7fc6da2）
→ R3 开工态   741/15437（Δ=0，docs-only 区间）
→ R3 收尾态   742/15443（+qaW7r3 6 条）
```

**前两轮机器化守门用例存活性全查：** 9 个 `qaW7r1.test.ts` + 8 个 `qaW7r1Fix.test.ts` + `qaW7r2.test.ts` + 4 个 `qaW7r2Fix.test.ts` + `copyW7r1.test.ts` + `copyW7r2.test.ts` + `art/kit/pattern.test.ts` **共 25 个文件全部在盘、全部在跑、全绿**；全库 `*.test.ts` 扫描 `.skip( / .only( / .todo(` **0 命中**（此前 grep 报的一处命中是 `portalExit(` 撞 `xit(` 的假阳性，人工核清）。本轮起该存活性检查本身已由 `qaW7r3` 机器化——守门文件被删或被 skip 会直接红。

## 二、第 2 轮问题终验对账表（本轮前置）

结论口径三选一：**已修复（附证据）/ 合理降级（引 C 档书面理由）/ 仍未修（升级为终验阻断项）**。全部逐条重验代码现状与 commit diff，不采信报告自述。

### 2.1 R2 A 档新发现 5 条（N-1–N-5）× C 档修复（F1–F6）

| # | 级别 | 问题 | C 档处置 | 终验结论 | 本轮实测证据 |
| --- | --- | --- | --- | --- | --- |
| N-1 | 一般 | landlord `.ldv-kribbon` 9px「大/小王」缎带（1.3 唯一新增 <14px 样式） | F1 `3a73660` | **已修复** | 缎带改 `kribbonBadgeSvg` 图形徽记：大王五瓣花徽 `#E2648F` / 小王五角星徽 `#5C79C4`，形状+色相双通道、无 `<text>` 节点；CSS `line-height:0` 去全部字属性；`LDV_CSS` 全量扫描 <14px **0 处**；窄牌 `EMBOSS_MIN_W` 降级逻辑原样；`qaW7r2Fix` 3 条钉死在跑 |
| N-2 | 一般 | poop-hero 开场横幅 12–13px 功能文字两处 | F2 `9d4ccff` | **已修复** | index.ts 1757/1761 均为 `Math.max(14, Math.round(13|12 * Math.max(0.85, scale)))`，写法照抄门帘计数（A-11 修法）；本轮九款画布字面量全量再扫 <14px **0 处**（见 3.⑤） |
| N-3 | 登记 | 九款样式表 <14px 84 处（全部 1.1/1.2 遗留） | 维持登记（遗留 a） | **合理降级** | 本轮宽口径复点（含 12.5/13.5 半像素档与 `font:` 简写）**86 处**，量级一致；关键差异 landlord 32→**31**——正是 F1 清掉的那处 1.3 新增 9px。剩余全部 pre-1.3 遗留（R2 blame 抽查在案），非六大专项范围，等「360px 阅读性」专项统一排格，降级理由成立 |
| N-4 | 建议 | 同名 `shade` 三份实现三种量纲 | F4 `92c0070` | **已修复** | poop-hero / memory-cards 两款 `visual.ts` 均已 `import { shade as kitShade } from "../../art/kit/fruit"` 改薄适配层（各保原量纲+大写输出），`parseInt(hex` 两款清零；kit `fruit.ts` 0 提交（归属纪律）；qaW7r2Fix 4 条等价网格断言在跑。fruit-slice 两份 ±255 加法量纲**数学不等价**，当轮收编=改实际渲染色，F6 `7fc6da2` 已按 B 档最低要求钉死量纲守门（2 条），实现收编顺延「shade 全库归一」——该部分为**合理降级**（B/C 双方书面理由一致） |
| N-5 | 登记 | fruit-slice 菜单条件行 13px（=R1 A-13 补录） | F3 `519bcb3` | **已修复** | index.ts 2612 现为 `14px sans-serif` 并带「不许低于 14px」注释；qaW7r2Fix 2 条（13px 清场 + 字面量全量扫描）在跑 |

### 2.2 R2 C 档遗留清单（维持 4 + 新登记 a–f）

| # | 事项 | 终验结论 | 依据 |
| --- | --- | --- | --- |
| R1-遗留 1 | plan 过程文档缺档 ×4（20-A/20-C/21-A/21-C） | **合理降级（维持）** | 本轮复查 `docs/qa/` 四格 plan 仍缺；C 档「不代写他人 plan」理由两轮一致；纯过程文档，不属六大专项，不影响放行 |
| R1-遗留 2 | puzzle-tiles blurb 文案跟进 | **已修复（结案）** | `meta.ts` blurb 现为「每关拼的都是一整幅手绘场景画…」，B 档 R2 落地、copyW7r2 快照钉死 |
| R1-遗留 3 | HUD/菜单文案级装饰 emoji | **合理降级（维持）** | 验收口径「装饰性可留」两轮一致；本轮复扫九款画布 emoji 直出：**仅 fruit-slice 17 处**，逐处定性全部是菜单标题/章节卡图标/地图标记/HUD 横幅的**文案级前缀**（含 2463 `st.emoji` 章节卡图标），玩法物件 0 处——玩法层 emoji 平涂维持清零 |
| R1-遗留 4 | destroy 真机「进出 ×5」 | **合理降级（维持）** | node 无浏览器；本轮加做 R2 全部 6 个修复 commit（F1–F6）diff 静态复审：**新增 setTimeout/setInterval/addEventListener/rAF 0 处**，泄漏面零扩大；R1 12 commit 复审结论（0 处）沿用 |
| a | N-3 样式表 84 处 | **合理降级** | 见 2.1 N-3 行 |
| b | fruit-slice `index.ts:2467` 章节卡 blurb `Math.min(12,…)` 封顶 | **合理降级（本轮加固）** | 1.2 遗留；直接提 14px 有 360px 双列卡溢出风险需配套排版，降级理由成立。本轮 `qaW7r3` 把「全窗表达式低封顶只许这 1 处」钉成断言——**新增低封顶即红**，遗留面不会再悄悄扩大 |
| c | fruit-slice `shadeColor` / 局部 `shade`（±255 加法量纲） | **合理降级** | 见 2.1 N-4 行；F6 守门用例在跑，收编列入「shade 全库归一」专项 |
| d | snake-snack `#4A3A2C`/`#8A5A38`「墨色孤例」纠偏（C 档交 round3 复核） | **合理降级（复核成立，结案）** | 本轮逐处读码复核：`#4A3A2C` 是刺猬眼/鼻/嘴线（visual13.ts 473/482），`#8A5A38` 是苹果梗与饼干巧克力豆（529/550）——**物件本色而非场景收边**；场景件（栅栏/门）走 `catShade` 家族本就合规。改半透明紫灰会洗掉角色五官，B 档原点名判据与代码用途不符，C 档纠偏成立 |
| e | memory-cards `mcShadow=rgba(60,50,90,.18)` 双支 | **合理降级** | 本轮确认值原样在位、被 21-C 规格用例（`visual21.test.ts` 一字不差断言）钉死；单方面改=改他人规格断言，须与规格归属格协调，属跨窗一致性打磨，不属六大专项，不影响放行 |
| f | kit `GOLD_EDGE` token / fruit-slice 果渍留底 / memory 四角角饰 / poop-hero 近景前景层 | **合理降级** | B 档自标「下轮/可选」的增强项（非缺陷），原样登记 |

**对账统计：已修复 5 条（N-1/N-2/N-5 + N-4 主体 + R1-遗留 2 结案）· 合理降级 8 项（全部有 C/B 档书面理由，且无一属六大专项缺陷）· 仍未修→升级终验阻断 0 条。第 2 轮阻断级/严重级本就为 0，终态维持 0。**

## 三、九款 × 六专项终验总表（每格二值：达标 ✅ / 不达标 ❌）

扫描方法：专项①②③以绘制源码逐函数走查 + 渐变/描边/落影工序核对 + 既有量化用例链为证据；④以视差系数与图层序常量+用例为证据；⑤以画布字号双口径全量扫描（字面量 + 表达式低封顶）为证据；⑥以品牌词黑名单全源码扫描 + 新素材造型点检为证据。「─」= 该款无此题材面，按达标计且注明。

| 款 | ① 火柴人/草稿圆 | ② 金币/道具体积 | ③ 双人可区分 | ④ 2.5D 纵深 | ⑤ 360px 布局 | ⑥ 商标 |
| --- | --- | --- | --- | --- | --- | --- |
| fruit-catch | ✅ kit 六剪影 3 停渐变+1.5px 描边，篮子编织工序，emoji 直出 0 | ✅ 水果三件套齐：+18% 左上高光 / −14% 侧缘暗面 / `drawFruitShadow` 落影（着地帧）；<18px 细节降级、16px 靠剪影通道 | ✅ 双人：自绘小花/小星标名牌（形状通道）+14px 名字+左右半屏空间分隔 | ─（接物玩法无纵深题材；云两档视差 0.1/0.18 作氛围） | ✅ 画布字面量/低封顶双口径 0 处；样式表仅 1 处 1.2 遗留在登记账 | ✅ 黑名单 0 命中 |
| fruit-slice | ✅ 五果专属剪影+果王皇冠（三尖齿渐变+珠色三分）；画布 emoji 17 处全为菜单/HUD 文案级，玩法物件 0 | ✅ 果身亮心 −0.35r/−0.4r 左上+≥1.5px 描边+切半截面工序；液滴/星花短寿命 | ─（单人玩法） | ─（砧板题材基准即薄背景） | ✅ 字面量 0；唯一表达式低封顶（2467 blurb）在登记账并被 qaW7r3 钉死唯一性 | ✅ 0 命中（含「水果忍者」黑名单词） |
| snake-snack | ✅ `ball()` 三停径向渐变+kit caterpillar 单源；头身比 0.42 大眼白黑瞳；食物全自绘小果子（emoji 只当 kind 钥匙不上屏） | ✅ 果子渐变+籽点/叶冠细节+棋盘底色差；吞咽金闪 1 帧 step | ✅ 双虫：粉虫深梅 `#9B4E86` 蝴蝶结（剪影级，不吃 12px 触角门槛，对头色灰阶差 Δ57）+绿虫不戴，亮度+几何双通道 | ─（俯视棋盘题材） | ✅ 字面量/低封顶 0 处；样式表 2 处遗留在账 | ✅ 0 命中（含「贪吃蛇大作战」黑名单词） |
| lianliankan | ✅ kit icons 16 枚一形一色：双色渐变+1.5px 描边+左上高光；面具唯一走 `maskFaceSvg` | ✅ 牌面单位精致工序（B 档棋牌「少而精」基准带内）；流星光带沿判定折线 1:1 只读映射 | ─（单人） | ─（桌面题材） | ✅ 字面量/低封顶 0 处；样式表 0 处 | ✅ 0 命中（含「开心消消乐/天天爱消除」） |
| puzzle-tiles | ✅ 牌面=场景画（三层构图+确定性 PRNG+每格贴纸）；`tile.emoji` 上屏 0 处（pattern.test 9 条+qaW7r1Fix 8 条在跑） | ✅ 齿边皮肤 clipPath 凸齿上也有画；<40px 齿径 18%→14% 降档（qaW7r2 对表在跑） | ─（单人） | ─（拼图题材） | ✅ 字面量/低封顶 0 处；样式表 3 处遗留在账 | ✅ 0 命中（pattern 七种几何原语无角色/城堡剪影，R2 造型点检结论维持） |
| memory-cards | ✅ 牌背双环 0.62/0.78+金线+徽章；图标 drawIconDeluxe 描边+高光；shade 已收敛 kit 单源 | ✅ <48px 角标降级门槛（`MC_CORNER_MIN_PX` 用例在跑）；配对星屑+星星印章事件残留 | ✅ 双人轮流翻:具名座席徽章（`SEAT_NAMES` 朵朵/星星）粉蓝双档+**当前手 3px 白描边圈**（结构通道，灰度可辨）+文字通道 | ─（桌面题材） | ✅ 字面量/低封顶 0 处；样式表 3 处遗留在账 | ✅ 0 命中 |
| landlord-cards | ✅ 牌面七道工序;大小王金银框+立绘+角标+图形徽记四通道;AI 头像 `botFaceSvg` 2 停渐变+1.5px 描边+耳形几何差 | ✅ 徽记/花色左上小高光;炸弹星屑环 RING_STARS=8 封顶;深影三支收敛 `rgba(90,74,110,α)` 单家族（F5） | ✅ AI 双头像 16px 耳形几何差可分（长耳 vs 圆耳+眼周深块，qaW7r1Fix 在跑） | ─（桌面题材） | ✅ 字面量/低封顶 0 处；LDV_CSS（1.3 新增块）<14px 清零（F1 后），样式表 31 处 1.1/1.2 遗留在账（−1 即 F1） | ✅ 0 命中（含「欢乐斗地主」） |
| fishing-star | ✅ kit fishArt 纺锤三停（背深腹浅）25 鱼参数化;花纹 15px 门槛+深水去饱和 | ✅ 浮标红白功能剪影+点头 160ms/3px 双信号（reduced 静态满位移保留，qaW7r2 在跑）;气泡 ≤6/波光 2 条预算 | ─（单人） | ─（垂钓题材;天空远山+云两档视差+深水渐变光柱四层作层次） | ✅ 字面量/低封顶 0 处;样式表 30 处遗留在账（宽口径含 12.5/13.5 半像素档） | ✅ 0 命中（含「捕鱼达人」） |
| **poop-hero（点名）** | ✅ 超人十道工序（渐变披风/菱形高光/腮红/4 帧腿摆）;18 款垃圾条目 ≥2 停渐变+1.7px 有效描边+左上 gleam+落影;香香星 `traceStar`+3 停渐变+描边;`emoji()` 工具函数已连根删除 | ✅ 香香星/垃圾条目/分类桶图标三件套规格齐;残影硬顶 6+尾流硬顶 6（PhFx 用例在跑） | ✅ 双人披风 Rec.601 灰度 **Δ≈29 ≥25**（qaW7r1Fix 恒跑）+星星发卡 `max(2, headR*0.55)` 恒画改变剪影（不吃 showDetail 门槛） | ✅ **章节视差远层 `camX*0.16` / 中层 `camX*0.45` / 玩法 1.0 三档在位**（qaW7r1 源码级断言在跑）;图层序 `PH_LAYERS` 背景→残影→超人→HUD 钉死 | ✅ 字面量/低封顶 0 处（N-2 修后）;样式表 16 处遗留在账 | ✅ 0 命中 |

**终验达标率：9 款 × 6 专项 = 54 格，54/54 全达标（其中 15 格为「该款无此题材面」按达标计，均已注明）。不达标 0 格。**

## 四、终验新发现问题（本轮增量扫描结果）

**阻断 0 · 严重 0 · 一般 0 · 建议 0 —— 本轮终验扫描（画布字号双口径 / emoji 直出 / 商标黑名单 / 渐变体积底线 / 守门存活性五组机器化扫描 + 九款人工走查）未翻出任何新问题，无需交本轮 C 档清零。**

两处口径级说明（非问题，记录在案）：

- N-3 样式表遗留量本轮宽口径复点为 86 处（R2 报 84），差异来自 12.5/13.5px 半像素档与 `font:` 简写的扫描口径，非新增回归——landlord 一款净减 1 处（F1）可交叉验证；
- fruit-slice 画布 emoji 17 处文案级前缀全部落在既有「装饰性可留」口径内，本轮逐处定性后维持不立项。

**本轮新增守门用例：`src/games/qaW7r3.test.ts`（4 describe / 6 it，全绿，commit `b410809`）**——① 前两轮 25 个守门文件存活+无 skip（「被删/被 skip 按阻断」自动执行）；② 九款画布字面量 <14px 聚合清零 + 表达式低封顶全窗唯一性钉死；③ 商标黑名单全源码扫描（copyW7r1 只查新增文案句，本条查九款+kit 全部源码，扫描面不同）；④ 每款渐变调用 ≥2 的体积感粗粒度底线。

**回归防线终验三查（放行前提，全部满足）：**

- **热区钉死**：puzzle 齿边皮肤 `pointer-events:none` 绝对定位不占盒模型、lianliankan 流星折线 1:1 只读映射、landlord 头像/徽记 SVG `pointer-events:none`——R1/R2 断言全部在跑全绿；R2 六个修复 commit diff 复审 0 处热区触碰；
- **reduced 功能提示保留**：fruit-catch 警告红圈（calm 只停脉动）、fishing-star 浮标点头静态满位移、puzzle 接缝白光、landlord 星屑环静态、poop-hero 扫帚弧静态帧——五处断言全绿；
- **玩法/教育数据零改动**：15437 条既有断言全绿；本轮自身 0 行绘制/玩法改动（只加 test 与本报告）。

**粒子高峰（node 无浏览器，以代码预算+用例为准并注明）**：fruit-catch 星屑 4×280ms 自灭 / fruit-slice 稳态液滴 ≈9–12 颗双裁剪 / poop-hero 残影 ≤6+尾流 ≤6（PhFx 溢出断言在跑）/ landlord RING_STARS=8 封顶 / fishing-star 气泡 ≤6+波光 2 条 / lianliankan 星尘 3×240ms——全部维持 R2 预算，本轮零绘制改动无新增通道。

## 五、终验结论

> **放行。**

依据：

1. **阻断级 / 严重级终态 0**：R1 六条严重级修复在 R2 已 100% 确认，R2 新发现（一般 2 / 建议 1 / 登记 2）本轮对账全部「已修复」或「合理降级」，**无一条升级为终验阻断项**；本轮终扫新问题 0；
2. **六大专项 9 款 × 6 项 54/54 全达标**，点名项（poop-hero 2.5D 三档视差、水果零 emoji 平涂）均有源码级断言恒跑；
3. **三道机器防线全绿**：`npm test` 742 文件 / 15443 用例（三轮曲线只增不减）、`npm run build` 全绿（体积与 R2 合并态逐位一致）、`vite preview` 三点位 200；
4. **遗留账清爽且全部钉死**：8 项合理降级各有 B/C 档书面理由、各有去向（360px 阅读性专项 / shade 全库归一 / 跨窗规格协调 / 可选增强），无一属六大专项缺陷；且 qaW7r3 已把「遗留面不许悄悄扩大」（低封顶唯一性、守门文件存活性）钉成断言；
5. 分级红线复核：失败文案全鼓励、无血/伤害/死亡、无广告内购、商标黑名单全源码 0 命中（copyW7r1/r2 + qaW7r3 三层用例在跑）。

放行不附带条件；已登记遗留按各自去向在后续专项处理，不构成本窗视觉放行的前置。

## 六、收尾与交接

- 本轮改动面：本报告 + `src/games/qaW7r3.test.ts`（6 条用例），**绘制代码 0 行、玩法 0 行、他人报告 0 字**。
- 交本轮 B 档：无新文案缺口（R1 遗留 2 已结案）。
- 交本轮 C 档：本轮新问题 0，无当轮修复项；合并态复验由 C 档执行，合并态 SHA 由 C 档写入其报告，本档已把全绿基线（742/15443 + build + preview 全绿）钉在分支上配合复验。
- 全程普通 push `origin game-1.3-window7`，未 force、未动 `main` / `game-1.2`、未开 PR。
