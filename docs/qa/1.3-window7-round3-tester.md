# 1.3 窗口 7 · 第 3 轮（终验）视觉验收 · 测试员报告

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 3 轮终验测试员（A 档）。只写本报告与 `*.test.ts`，不改绘制代码、不改玩法/数值/关卡，不碰前两轮 A/B/C 六份报告与他人文件。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。
> 本轮定位：**终验**，只回答「这套视觉能不能放行」，结论三选一：放行 / 带条件放行 / 不放行。

## 〇、终验计划（开工首 commit）

1. **全量跑数据**：`npm ci` → `npm test` → `npm run build`，记录用例总数与第 2 轮合并态（`7fc6da2`，741 文件 / 15437 用例）差值，只增不减。
2. **对账清零**（前置）：第 2 轮 A 档 5 条新发现（N-1–N-5）× C 档修复清单（F1–F6）× 遗留清单（维持 4 + 新登记 a–f）逐条对账，每条三选一：已修复（附证据）/ 合理降级（引 C 档理由）/ 仍未修（升级为终验阻断项）。阻断级 / 严重级必须终态为 0。
3. **机器化用例存活性全查**：`qaW7r1*.test.ts` / `qaW7r1Fix.test.ts` / `qaW7r2.test.ts` / `qaW7r2Fix.test.ts` / `copyW7r1/r2.test.ts` / `pattern.test.ts` 等全部在跑、全绿；被删或被 skip 按阻断处理。
4. **九款 × 六专项终扫**：火柴人/草稿圆、金币/道具体积、双人可区分、2.5D 纵深（点名 poop-hero）、360px 布局、商标扫描——每款每项给「达标 / 不达标」二值结论，产出 9 行 × 6 列终验总表。
5. **回归防线（终验加严）**：热区钉死、reduced 功能提示、玩法断言全绿为放行前提；粒子高峰以代码预算/用例为准（node 无浏览器，注明）；`npx vite preview` 冒烟三点位 200。
6. **守门用例**：补 `qaW7r3` 终验守门用例（只增不减），钉死终验现状防回退。
7. **终验结论**：五节报告 + 三选一正式结论；配合本轮 C 档合并态复验。

（以下各节由本轮实测回填。）
