# 窗口 4 · 第 1 轮 · 监督修复员

## 档C

> 范围：`alien-seek` / `box-hamster` / `bubble-aim` / `snake-snack` / `memory-cards` 五款。
> 状态：**续跑中**（修复计划已登记，结论随后补写在本节）。

### 本轮收口项

1. 本轮阻断 / 严重问题清零，每修一条先红后绿；
2. 商标黑名单 0 命中；
3. `destroy` 拆干净监听 / timer / rAF / AudioContext；
4. 存档 key 只增不改，音效只走 `api.play`；
5. `npm test && npm run build` 全绿才进第 2 轮。
