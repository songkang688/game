# 窗口 4 · 第 3 轮 · 测试员

## 档C

范围：`alien-seek` / `box-hamster` / `bubble-aim` / `snake-snack` / `memory-cards` 五款。
基线：第 2 轮收口 `3e4fbda`。收口：**243 文件 / 6136 用例**全绿。

### 一、本轮走查计划（收官）

前两轮打的是**样本**：第 1 轮点了第 1 / 100 / 188 关，第 2 轮每款另换 10 关。
样本走查能抓住「这一类关有问题」，抓不住「第 137 关那一关有问题」。
第 3 轮是发布前最后一遍，所以全部改成**全量**——五款各自的 188 关**一关不漏**地过一遍，
再把每款 meta 声明的模式**一个不漏**地各造一局，最后逐关做一次存档往返。

新增用例全部落在各款的 `qaC3.test.ts`，一条不改旧用例。

### 二、全量走查了什么

| 游戏 | 188 关逐关验的东西 | 模式（`meta.modes` 全覆盖） | 存档 |
| --- | --- | --- | --- |
| `alien-seek` | 藏身点不重叠 / 不出画、颜色+种类不重名、目标指得到人、推理题解唯一、限时按「键盘挪光标」这种最慢玩法算还留 6 秒、360px 上目标直径 ≥24px、文案无洋文无丧气话、同一关反复取一模一样 | 战役 / 无尽 60 轮 / 对战 20 局 / 双人 | 188 关逐关往返 + 只增不减 + 坏档 |
| `box-hamster` | 箱子数=脚印数、不压墙不叠人、照参考解推得完、开局不是死局、至少推 3 下、三星门槛够得着、360px 格子戳得准、文案干净、关号越界夹得回来 | 战役 / 无尽 60 仓 | 188 关逐关往返 + 只增不减 + 坏档 |
| `bubble-aim` | 行长 8/9 交替、字符全在册、开局不悬空不越警戒线、子弹预算 >1/3、机关标签与布局逐关对得上、云和黑洞不糊墙不挡发射台、子弹分档、360px 满铺 | 战役 / 无尽 | 188 位新档 + 1.0 的 99 位老档 + 坏档 + 往返两趟不漂 |
| `snake-snack` | 墙在格子里 / 不压出生位 / 不堵路、全园连通、空格够吃完目标、速度不穿地板、星门成对、小刺猬走在格子里、窄门有上限、石头不压墙、双身位镜像摆得开、文案干净 | 战役 / 无尽 60 座 | 188 关逐关往返 + 只增不减 + 坏档 |
| `memory-cards` | 参数在册、真发一副牌（张数 / 每组齐 / 独苗确实没同伴）、算式得数两两不同、记性完美的孩子逐关过得去、限时关留三成富余、360px 每张牌 ≥56px、老章不长新机关、图案号不越界、卡背只看槽位不泄底、评星单调 | 战役 / 无尽 300 轮 / 双人 / 记忆辅助 | 188 关**真打一遍**再往返 + 1.0 老档 + key 没改 |

### 三、新增用例数

| 游戏 | 文件 | 新增用例 |
| --- | --- | --- |
| `alien-seek` | `src/games/alien-seek/qaC3.test.ts` | 15 |
| `box-hamster` | `src/games/box-hamster/qaC3.test.ts` | 16 |
| `bubble-aim` | `src/games/bubble-aim/qaC3.test.ts` | 19 |
| `snake-snack` | `src/games/snake-snack/qaC3.test.ts` | 17 |
| `memory-cards` | `src/games/memory-cards/qaC3.test.ts` | 21 |
| **合计** | | **88** |

### 四、走查结果表

| 游戏 | 188 关全量 | 模式全覆盖 | 360px | 存档往返 | 文案红线 |
| --- | --- | --- | --- | --- | --- |
| `alien-seek` | 通过 | 4/4 通过 | 通过 | 通过 | 通过 |
| `box-hamster` | 通过 | 2/2 通过 | 通过 | 通过 | 通过 |
| `bubble-aim` | 通过 | 2/2 通过 | 通过 | 通过 | 通过 |
| `snake-snack` | 通过（有 C3-01 数据脏点） | 2/2 通过 | 通过 | 通过 | 通过 |
| `memory-cards` | 通过 | 4/4 通过 | 通过 | 通过 | 通过 |

前两轮登记的问题这一轮复查：C1 的 5 条、C2-01 / C2-02 / C2-03 全部**没有复发**
（C2-02 的两种奇偶补货在 `qaC2fix.test.ts` 里仍逐轮跑）。

### 五、问题清单

#### C3-01　迷宫贪吃：1.0 老章的镜像关把同一格墙写了两遍　**【轻微 · 不影响玩法】**

- **复现**：不用进游戏——`LEVELS[51].walls`（第 52 关）里 `[x, y]` 有 4 组是重复的。
- **范围**：**25 关 / 88 个重复格**，全落在 1.0 的 99 关以内：第 52–67 关（第 4 章「双子藤园」
  一带的镜像生成）与第 85–98 关（第 6 章）。1.1 / 1.2 追加的第 100–188 关一关都没有。
- **根因**：`levels.ts` 里本来就有 `dedupe()`，而且第 100 关往后的分支全都过了它
  （`dedupe(safeTwin(walls))` / `dedupe(safe(walls))`）；1.0 那几章的镜像分支
  `walls.push([x, y], [x, y + 1], [GRID - 1 - x, y], [GRID - 1 - x, y + 1])` 在 `x` 落到中轴附近时
  左右两半会撞到同一格，却只过了 `safe()` 没过 `dedupe()`。
- **玩起来看不看得出来**：**看不出来**。碰撞判定走的是 `wallSet()`（Set），
  画墙也是同一格画两遍、像素完全一致；`canvas.dataset.walls` 取的也是 Set。
  所以这是**数据脏**而不是玩法坏——但 `walls.length` 比真实墙数多，
  「墙占比」「墙太多爬不开」这类阈值判断会被它带偏，是个该在发布前擦干净的角。
- **建议责任文件**：`src/games/snake-snack/levels.ts`（1.0 六章的 `return` 补上 `dedupe()`）。
- **登记方式**：`qaC3.test.ts` 里那条 `【C3-01 待修】` 先把现状钉成断言
  （25 关 / 88 格 / 全在老章以内 / 去重后集合不变），本轮修复员改成「一格重复都没有」。

除此之外，本轮全量走查**没有发现新的阻断 / 严重 / 一般问题**。

### 六、给最终结论的输入

- 五款 × 188 关 = **940 关**逐关验过，没有一关摆不开、推不完、找不完、吃不完、配不完；
- 五款 meta 声明的模式合计 **14 个**，全部造得出局、走得到结算；
- 存档：五款都做了 188 关逐关往返，`memory-cards` 还把 188 关**真打了一遍**再存；
  1.0 的 99 位老档在 `bubble-aim` / `memory-cards` 上都验过「老成绩不丢、新关补 0」；
- 唯一未清的是 C3-01（轻微、不影响玩法），交本轮修复员。

## 档A

范围：`brave-path` / `brick-break` / `balloon-pop` / `fruit-catch` / `lianliankan` 五款。
基线：第 2 轮收口 `e280d2d`。收口：**259 文件 / 6356 用例**全绿，`npm run build` 通过。

### 一、这一轮换了把尺子：从「抽样」改成「全量」

前两轮量的是样本。第 1 轮点了第 1 / 100 / 188 关，第 2 轮每款另换 11–13 关。
样本能告诉你「这一类关有问题」，告诉不了你「第 89 关那一关有问题」——
而发布前要回答的恰恰是后一个问题。

所以第 3 轮全部改成全量：五款各自的 **188 关一关不漏**，每关再换三个种子跑；
无尽 / 对战 / 双人一个模式不落地各玩到结算；360px 重走；
前两轮记过账的 17 条逐条给最终结论。

新增用例全部落在各款新建的 `walkthrough.r3.test.ts`，**一条旧用例都没有改**，
玩法代码**一行都没有动**（本轮新发现的两条交本轮修复员）。

### 二、每款全量验了什么

| 游戏 | 188 关逐关验的东西 | 模式（各玩到结算） | 竞态复核 |
| --- | --- | --- | --- |
| `brave-path` | 达标勇者 3 种子通关、Boss 关门口有整装石、路上不缺歇脚、精英数值不越档 | 战役 / 无尽（含守关）/ 擂台 / 竞速 | 同帧双技能、守势与暴击叠加、判定血比 |
| `brick-break` | 3 种子打通、砖阵有得打不出界、钢砖不堵关、章内砖数只增不减 | 战役 / 无尽砖塔（四档手速 × 五种子） | 一帧一砖只结算一次、球拍反弹留角度、道具同帧取用与到期 |
| `balloon-pop` | 3 种子过关、四类目标（数数 / 挑颜色 / 按顺序 / 护礼物）逐类核 | 战役 / 气球节（戳到手滑为止） | 连锁窗口内收完、礼物同屏上限、上升速度按自己的波次 |
| `fruit-catch` | 3 种子接下来、下落表条条赶得到、碰不得的擦不着、同帧两颗不叠 | 战役 / 双人赛跑 / 水果雨（续五段） | 篮口边界与磁铁加宽、冰冻+磁铁并存、沉水果压慢 1.2 秒回速 |
| `lianliankan` | 3 种子全清、牌能两两配对、窄屏排得下、开局走得动 | 战役 / 无尽（连到第 40 盘） | 连线动画里再点、同格点两下、消完再点原地、连不上只摇不吃牌 |

三处口径值得单说：

- **`brick-break` 的「打得越快活得越久」**：W4A-07 修完之后，下压速度只跟**在场时间**挂钩，
  不再跟清了几行挂钩。本轮拿四档手速 × 五个种子交叉验，结论稳定——手快的孩子活得更久。
  奖励方向是正的，这正是第 2 轮那条严重问题的反面。
- **`fruit-catch` 的水果雨**：连续续了**五段**（六段共 1900+ 颗），每段接缝都验
  「不倒流 + 必接的赶得到」，并确认 `mountRain` 里已经没有「排完就收」那条老路。
  （双人赛跑仍留着这条兜底——那本来就是一场有终点的比赛。）
- **`brave-path` 的 W4A-15 复核**：种子从 6 个加到 **40 个**。原来那 5 关都稳在 98% 以上，
  但更长的尺子照出了新的一批，见下面的 W4A-17。

### 三、新增用例数

| 游戏 | 文件 | 新增用例 |
| --- | --- | --- |
| `brave-path` | `src/games/brave-path/walkthrough.r3.test.ts` | 19 |
| `brick-break` | `src/games/brick-break/walkthrough.r3.test.ts` | 19 |
| `balloon-pop` | `src/games/balloon-pop/walkthrough.r3.test.ts` | 22 |
| `fruit-catch` | `src/games/fruit-catch/walkthrough.r3.test.ts` | 26 |
| `lianliankan` | `src/games/lianliankan/walkthrough.r3.test.ts` | 29 |
| **合计** | | **115** |

### 四、走查结果表

| 游戏 | 188 关全量 | 模式全覆盖 | 赢一次 + 输一次 | 360px | 文案红线 | destroy |
| --- | --- | --- | --- | --- | --- | --- |
| `brave-path` | 通过（有 W4A-17） | 4/4 | 通过 | 通过 | 通过 | 通过 |
| `brick-break` | 通过 | 2/2 | 通过 | 通过 | 通过 | 通过 |
| `balloon-pop` | 通过 | 2/2 | 通过 | 通过 | 通过 | 通过 |
| `fruit-catch` | 通过 | 3/3 | 通过 | 通过 | 通过 | 通过 |
| `lianliankan` | 通过（有 W4A-18） | 2/2 | 通过 | 通过 | 通过 | 通过 |

### 五、前两轮 16 条的最终复核

| 编号 | 游戏 | 第 3 轮结论 |
| --- | --- | --- |
| W4A-01 严重 | `balloon-pop` | 已修，未复发 |
| W4A-02 轻微 | `lianliankan` | 已修，14 张牌的同色系轮廓两两不同 |
| W4A-03 轻微 | `balloon-pop` | 已修，连锁在窗口内收完 |
| W4A-04 建议 | `fruit-catch` | 已落地 |
| W4A-05 轻微 | `brave-path` | 已修，向导词里一处「血」都没有 |
| W4A-06 轻微 | `fruit-catch` | 已修，讲解与漏球话术只说「爱心」 |
| W4A-07 **严重** | `brick-break` | 已修，四档手速 × 五种子都撑得住，且手快活得更久 |
| W4A-08 中等 | `lianliankan` | 已修，一盘拧了几个旋钮就报几条 |
| W4A-09 轻微 | `fruit-catch` | 已修，要的手速一章一章往上抬 |
| W4A-10 建议 | `brave-path` | 已修，第一位守关在第 4 层、浅层薄第 16 层满厚 |
| W4A-11 中等 | `fruit-catch` | 已修，水果雨续了五段仍然接得下去 |
| W4A-12 中等 | `balloon-pop` | 已修，气球节续得下去，天空不空 |
| W4A-13 轻微 | `balloon-pop` | 已修，上升速度按气球自己的出场波次算 |
| W4A-14 轻微 | `balloon-pop` | 已修，末章的坡紧过第 7 章 |
| W4A-15 中等 | `brave-path` | 已修，原来那 5 关在四十种子下都在 98% 以上 |
| W4A-16 轻微 | `brave-path` | 已修，技能栏卸不空 |

十六条全部**未复发**，且每一条都在 `walkthrough.r3.test.ts` 里留了一条守门用例。

### 六、本轮新发现的两条

#### W4A-17　勇者小路：「半路撞上精英」的关，通关率停在 93%　**【轻微】**

- **怎么量出来的**：把 W4A-15 的尺子从 6 个种子加长到 **40 个**，口径是
  「达标勇者、一次道具都不用」——这是下限口径，不是正常玩法。
- **现象**：188 关里有 **10 关**不是 100%，落在 93%~98%：
  第 117 / 121 / 124 / 127 / 128 / 135 / 139 / 140 / 159 / 185 关。
- **共同点**：这 10 关**关关带精英**，而且其中 8 关的精英**不在最后一步**。
  第 2 轮落地的 `easeClimaxElite` 只照顾「收尾那只」，照顾不到半路上这只；
  半路那只打完后面还接着一两场小怪，中间没有歇脚石：
  第 121 关是「小怪 → 精英 → 小怪」，第 124 / 140 / 185 关是「精英 → 小怪 → 小怪」。
- **定性轻微**：没有一关是墙（最低 93%，且这已是下限口径），重来一次或练高一级就过得去。
- **登记方式**：`brave-path/walkthrough.r3.test.ts` 里那条 `W4A-17 特征化`
  先把现状钉成断言（全 188 关 ≥90%、掉队的 10 关关关带精英），交本轮修复员定夺。

#### W4A-18　连连看：照发出去的洗牌次数玩，第六章那几关会走进死胡同　**【中等】**

- **复现**：第 **89** 关——**用它发出去的那个固定种子**，一路挑「眼前能连的」往下消，
  会把盘面消成一个谁也连不上的死局，而这时两次洗牌已经用光。
- **范围**：全量扫 188 关 × 60 种子，会走进死胡同的有 **7 关**：
  第 36 / 51 / 64 / 86 / 89 / 95 / 98 关，最高的第 89 关是 8/60。
  七关**全是 1.0 的老关**，共同点是「洗牌次数 ≤3 + 会收拢」；
  1.1 / 1.2 追加的第 100–188 关一关都没有——新章都配了免费重排，本来就没有这个坑。
- **孩子那头是什么感受**：`index.ts` 判这一关没过，还捎一句
  「洗牌是应急用的，下一局多留一次就够翻盘啦」。可孩子并没有乱花洗牌，
  是这一手消除顺序刚好把自己堵死了——这是**没犯错却输了**，还挨了一句不该挨的提点。
- **好修**：`fairShuffle` 本来就保证「重排完一定还走得动」；而且**开局那一次死盘救场
  已经是免费的**（`index.ts` 第 608 行 `doShuffle(true, true)`）。
  把同样的待遇给到中途的死局就完了，前 99 关的关表一个字节都不用动。
- **登记方式**：`lianliankan/walkthrough.r3.test.ts` 里那条 `W4A-18（本轮新发现）`
  先把现状钉成断言（7 关名单 / 全在老章以内 / 第 89 关连官方种子都会死 / 重排救得回来），
  交本轮修复员。

除这两条外，本轮全量走查**没有发现新的阻断或严重问题**。

### 七、给最终结论的输入

- 五款 × 188 关 × 3 种子 = **2820 局**逐关跑过，没有一关是打不过去的死关；
- 五款声明的模式合计 **13 个**，全部造得出局、走得到结算，赢一次输一次都验过；
- 无尽三条命脉都撑得住：砖塔（时间驱动、越快越久）、水果雨（续五段）、
  气球节（续得下去）、无尽连连看（连到第 40 盘）；
- 360px 五款重走一遍：画布满铺、按钮 ≥44px、最挤的棋盘格子仍有 32px；
- 红线：商标 0 命中、无血无死亡、失败只鼓励、不联网不存本地、声音只走 `api.play`；
- 未清的是 W4A-17（轻微）与 W4A-18（中等），交本轮修复员。

## 档B

范围：`adventure-king` / `mole-pop` / `bubble-pop` / `fruit-slice` / `puzzle-tiles` 五款。
上一轮收口：`d94e4a0`（第 2 轮三段全绿）。本段新增 **46 条**用例，收口 **254 文件 / 6230 用例**全绿。

### 一、这一轮为什么不再抽样

第 1 轮点的是第 1 / 100 / 188 关，第 2 轮每款另换 4 关。抽样能抓住「这一类关有毛病」，
抓不住「第 137 关那一关有毛病」。第 3 轮的题目是「五款不漏」，所以全部改成**全量**：

- 五款各自的 **188 关一关不落**，能让机器人打的就真让机器人打完；
- 每款的无尽玩法从「开得起来」加长到 **200～600 轮**，直到它自己走到结算；
- 360px 的走查口径从「只翻 `index.ts`」扩到**每款每一份源码都翻**，
  这样本档这两轮新添的 `qaSolver.ts` / `explore.ts` 也在闸门里，以后再添文件会自动被扫到。

新增用例全部落在各款的 `qa-b-r3.test.ts`，一条旧用例都没改、没删。

### 二、五款各自全量验了什么

| 游戏 | 188 关逐关验的东西 | 无尽玩法（全部跑到自己结算） |
| --- | --- | --- |
| `adventure-king` | 机器人逐关打通且三件神器一件不缺、地形没有跨不过去的坑、神器不悬空、每条缝要么跳得过要么有钩点、逐关拿得到 ≥1 星、目标时间比全速跑更宽 | 无尽遗迹 **120 层**、无尽古堡 **200 间 × 2 seed**（顺带复验 B2-01 不再连着重样）、计时速通 **8 条赛道**（两次生成一模一样） |
| `mole-pop` | 逐关排得出谱面且满分打法过得了线、不超台面预算、同一刻同一个洞不挤两只、出洞时间线露头/停留/缩回三段首尾相接、评星恒落 1~3 星、掉心的关掉的只有心 | 无尽夜市 **200 摊**：摊摊排得出、不超预算、够得着目标分，且 200 摊里没有一刻挤洞 |
| `bubble-pop` | 每关 **6 个种子**跑贪心机器人、整体过关率 ≥90% 且没有一关全军覆没、开局都有可点的一手、过关线留得下消不掉的石头、盘面装得下所有道具、一步不点必输且剩余数等于满盘 | 无尽泡泡海：边推边消 **200 推**，验「好好玩能撑很久、但潮水终究会赢」这条无尽结算条件 |
| `fruit-slice` | 188 回合都够得着目标分（没有数学上的死局）、参数合法（时长为正 / 抛数区间成立 / 炸弹率不到一半）、评星只看掉了几颗心且恒 1~3 星、**每回合 20 次随机抛在 360×640 上都够得着且不出屏**、果王的耐打刀数与体型奖励都合法 | 禅宗 **600 秒**、街机连打到 **600 分**、水果暴风 **300 波**，外加选关地图 **12 章 × 5 档屏宽**节点不出界不重叠 |
| `puzzle-tiles` | 按各自玩法（滑块 / 转向 / 填格）逐关拼得完且在步数上限内、评星线留在步数上限内、板子不超 6×6、360px 上吸附地板不大过半格 | 无尽画廊 **300 幅**：幅幅排得出、幅幅在 6×6 以内、幅幅拼得完，且 300 幅全部 seeded 可复现 |

存档这一轮单独立了一节（`puzzle-tiles`）：188 关各写一次再读回来一关不走样、脏档一律当没存过绝不崩、
朝向表往返一模一样、360px 上每一格的中心都吸得回它自己。

### 三、新增用例数

| 游戏 | 文件 | 新增用例 |
| --- | --- | --- |
| `adventure-king` | `src/games/adventure-king/qa-b-r3.test.ts` | 11（含五款横扫 4 条） |
| `mole-pop` | `src/games/mole-pop/qa-b-r3.test.ts` | 8 |
| `bubble-pop` | `src/games/bubble-pop/qa-b-r3.test.ts` | 7 |
| `fruit-slice` | `src/games/fruit-slice/qa-b-r3.test.ts` | 10 |
| `puzzle-tiles` | `src/games/puzzle-tiles/qa-b-r3.test.ts` | 10 |
| **合计** | | **46** |

### 四、走查结果表

| 游戏 | 188 关全量 | 无尽全量 | 360px | 存档 | 文案红线 |
| --- | --- | --- | --- | --- | --- |
| `adventure-king` | 通过 | 3/3 通过 | 通过 | 通过 | 通过 |
| `mole-pop` | 通过 | 1/1 通过 | 通过 | 通过 | 通过 |
| `bubble-pop` | 通过 | 1/1 通过 | 通过 | 通过 | 通过 |
| `fruit-slice` | 通过 | 3/3 通过 | 通过（**但 B3-01**） | 通过 | 通过 |
| `puzzle-tiles` | 通过 | 1/1 通过 | 通过 | 通过 | 通过 |

前两轮登记的问题这一轮复查：第 1 轮那 5 条、第 2 轮 B2-01 ~ B2-04 全部**没有复发**
（B2-01「古堡连着重样」这一轮直接用 200 间 × 2 seed 的全量跑复验）。
B2-05（切水果快闪回合密度偏低）第 2 轮判定为**不修**，理由见下面第六节。

### 五、问题清单

#### B3-01　水果切切乐：五款里只有它一份源码都没提 `prefers-reduced-motion`　**【一般 · 无障碍】**

- **复现**：不用进游戏——`src/games/fruit-slice/` 下 6 份源码里 grep 不到 `prefers-reduced-motion`，
  另外四款都有（`adventure-king` / `mole-pop` / `bubble-pop` / `puzzle-tiles` 的内联样式里都写了 `@media`）。
- **为什么前两轮没查出来**：第 1、2 轮的窄屏 / 动效走查只翻各款 `index.ts` 的**内联 CSS**。
  切水果整块是 canvas，所有动效都在 `requestAnimationFrame` 里用 JS 画，压根没有 CSS 动画可写 `@media`，
  于是「没有 CSS 动画」被当成了「没有动效」放过去。实际上果块抛飞、刀光拖尾、切开的迸溅都是真动效。
- **影响**：系统开了「减弱动态效果」的孩子，进别的四款都会安静下来，进切水果照样满屏飞溅。
  不影响玩法正确性，是无障碍口径上的不一致。
- **建议责任文件**：`src/games/fruit-slice/index.ts`（读一次 `matchMedia("(prefers-reduced-motion: reduce)")`，
  照 `puzzle-tiles/index.ts:141` 那个现成写法，把刀光拖尾和迸溅粒子压到最短）。
- **登记方式**：`adventure-king/qa-b-r3.test.ts` 里那条横扫用例先把现状钉成断言
  （缺口集合恰好等于 `["fruit-slice"]`），本轮修复员落地后翻成「五款全过」。

除此之外，本轮全量走查**没有发现新的阻断 / 严重问题**。

### 六、给最终结论的输入

- 五款 × 188 关 = **940 关**逐关验过，没有一关打不通、排不出、消不完、切不够、拼不完；
- 五款的无尽玩法合计 **9 种**，全部跑到自己的结算条件（不是「跑了几轮没崩」）；
- 360px 走查这一轮扩到每款每一份源码，固定宽度撑破容器的规则**一条都没有**；
- 未清项：B3-01（一般，交本轮修复员）；
- 留给最终结论的两条**已知不修**：
  - **B2-05**　切水果第 70 / 81 / 92 三个快闪回合比同章最难的正课回合更松。
    第 1~99 回合的参数是 FNV 哈希锁死的老档兼容面，改密度会让 1.0 老玩家的成绩线整体漂移，
    收益（三个回合松一点）远小于代价，**判定不修**，理由已写进 `fruit-slice/qa-b-r2-learn.test.ts` 的专门用例里。
  - **B2-L12**　五款无尽都没有「第 50 / 100 轮」这类里程碑反馈。
    要动 `recordEndlessBest` 和平台层 UI，越出本档 5 个目录的独占范围，**本轮不做**，留作后续窗口的输入。
