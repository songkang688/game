# 1.3 窗口 1 · 本窗三轮验收 · 第 1 轮 · 监督修复员报告

> 角色：窗口 1 第 1 轮监督修复员（C 档）。落地 A 档（tester）问题清单与 B 档（learner）建议清单的高优先级项，
> 并执行合并态复验。只动绘制层（绘制函数 / CSS / 视觉常量），判定、数值、关卡数据、存档 key、玩法测试断言一律不碰。
> 输入：`docs/qa/1.3-window1-round1-tester.md`（阻断 0 / 严重 0 / 一般 6 / 建议 1）、
> `docs/qa/1.3-window1-round1-learner.md`（P1–P10 建议清单）。
> 依据：`docs/plan-1.3-visual-bible.md`（视觉宪法）、`docs/qa/1.3-window1-plan.md`（十二格登记）。

## 0. 修复计划（开工承诺）

修复顺序铁律：阻断（0 条，自查复核）→ 严重（0 条，自查复核）→ 一般 → 建议。

1. **自查底线**（不等报告也做）：商标 grep、裸 emoji 主角 grep、360px 抽查、reduced-motion 抽查、kit 纪律 `git log` 查。
2. **一般级修复**（对应 learner P1–P10 与 tester G-2–G-6）：
   - P1 = G-6：star-estate 地格图标矢量化——`art.ts` 新增 `tileIconSVG`（20×20 viewBox、双色阶、KIT_PALETTE 派生色、
     款数覆盖 `board.ts` 全部去重 emoji），`index.ts` 地格 emoji 节点换 SVG、emoji 转入 aria-label；契约测试 ≥ 12 例；
   - P2：orb-arena `drawArenaBackground` 圆斑层与网格层之间加确定性哈希具象贴片（三式、视差 0.3、alpha ≤ 0.5、sprite 缓存）；
   - P3：flight-chess 四象限静态装饰（草地簇 ×4 + 塔台旁云 ×2，`aria-hidden`、`pointer-events:none`、避开格子）；
   - P4 = G-5：`oa-open` / `sr-open` / `bd-open` / `sr-skin` 触区补到 `min-height:44px`；
   - P5：combo-clash `nearMounds` 土丘补顶部亮弧 + 底缘暗带两阶；
   - P6：snake-royale 远景色岛层（视差 0.6，全场保持两档视差）+ 贴片尺寸下限 2.4 → 3.2；
   - P8：mahjong-bloom `.mj-table` ≤ 4% 织纹、hero-cards `.hc-card` 米白纸感渐变；
   - P9：block-drop 井壁主题浮雕（挂 `WELL_WALL` 井壁、离屏缓存，中后期不被方块盖住）；
   - P10 = G-7：weiqi-garden 新增 `art.test.ts` 补充用例（旧例原地保留，只增不减）。
3. **裁决登记**（tester 移交）：G-2（`mj-tile` 选中态二段确认豁免）、G-3（`se-tile` 信息入口豁免 + focus 环已有）、
   G-4（`.fc-pick` 主入口兜底豁免）——逐条给结论；G-1 = P7（共享壳 `src/games/level99.ts` 星级画形）属壳层 owner，
   按窗口纪律写入遗留清单，不抢他窗文件。
4. 每修一处一个 commit（`fix(visual-r1): <款> <问题>`）；每处修复配套测试，用例只增不减
   （learner 交卷水位 722 文件 / 15514 例）。
5. 收尾合并态复验：fetch → rebase → `npx vitest run` → `npx tsc --noEmit` → `npm run build` → preview 冒烟 →
   合并态 SHA、用例总数、构建体积写进本报告。

---

## 一、修复清单（已全部落地，一处一 commit）

修复统计：**阻断 0 条（无输入，自查亦未发现）；严重 0 条（无输入，自查亦未发现）；一般 / 建议共 10 项落地**。
每条格式：来源 / 位置 / 修法 / 配套用例 / commit。

| # | 来源 | 位置 | 修法 | 配套用例（只增不减） | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | learner P1 = tester G-6 | `star-estate/art.ts` + `index.ts` | 新增 `TILE_ICONS`（`board.ts` 全部 33 款去重 emoji 逐一映射）与 `tileIconSVG()`：20×20 viewBox、四周留白 ≥ 2px、每款 ≤ 8 图元、双色阶（KIT_PALETTE 主色 + shade/tint 派生）、未知 emoji 落金色四芒星兜底；`index.ts` 地格节点 `se-tile-emoji` 换 `se-tile-icon` 插 SVG，emoji 字符转入按钮 `aria-label`，机会/命运类构图避开「? 徽章」（用信封 / 星卡，见 learner 商标风险表） | `star-estate/art.test.ts` +12 例（全覆盖 / 结构 / 无 emoji 码位 / 款款互异 / 双色阶 / 图元数 2–8 / 确定性 / 兜底 / 风格同族）、`visual.test.ts` +1 例（DOM 插 SVG 且 emoji 进 aria-label） | `f5bae48` |
| 2 | learner P2 | `orb-arena/art.ts` | `drawArenaBackground` 圆斑层之后、网格层之前加确定性哈希具象贴片层：三式（五瓣小花 / 四芒星 / 糖果石子）、格边 176 世界 px、约 40% 格出件、视差 0.3 与圆斑同步、alpha ≤ 0.5、只用平涂不加渐变（径向渐变计数契约不受扰动）、离屏 sprite 缓存一笔贴 | `orb-arena/art.test.ts` +3 例（`decorAt` 确定性与值域 / 密度与常量 / 贴片只平涂且背景确定性不变） | `2ebf319` |
| 3 | learner P3 | `flight-chess/art.ts` + `index.ts` | `art.ts` 新增 `grassSVG`（叠圆草地簇 + 短草叶，双色阶）；`index.ts` 新增 `decorArt` 静态装饰层：机库内角草地簇 ×4（各随本座 `COLOR_INFO[c].soft` 派生色）+ 塔台旁 `cloudSVG` ×2，恰 6 件、全部避开格子与航线坐标、`aria-hidden` + `pointer-events:none`、零动画 | `flight-chess/art.test.ts` +2 例（grassSVG 结构 / 双色阶与互异）、`visual.test.ts` +3 例（件数与静态 / 不与 RING_XY·HOME_XY 重叠 / aria-hidden） | `fc4a372` |
| 4 | learner P4 = tester G-5 | `orb-arena/index.ts`、`snake-royale/index.ts`、`block-drop/index.ts` | `.oa-open` / `.sr-open` / `.bd-open` / `.sr-skin` 四处补 `min-height:44px`（tester 实测 36–38px），只改高度不动布局 | `window1-visual-r1-fix.test.ts` 源码级契约 3 例（三款逐一钉住 44px） | `8cfa597` |
| 5 | learner P8（3B 格） | `mahjong-bloom/index.ts` | 毛毡底 radial 渐变之上、花徽之下叠 `repeating-linear-gradient(45deg, rgba(255,255,255,.03) …)` 斜织纹，alpha 3% ≤ 4% 红线，眯眼测试不抢牌面 | `window1-visual-r1-fix.test.ts` 契约 2 例（织纹存在 + alpha ≤ 4%） | `a94239f` |
| 6 | learner P8（4A 格） | `hero-cards/index.ts` | `.hc-card` 卡面由 `#fff` 平涂改 `linear-gradient(180deg,#fffdf8,#f6efe2)` 米白纸感，选中态描边不动，`cardart.ts` 不动 | `window1-visual-r1-fix.test.ts` 契约 1 例（纸感渐变存在且不再平涂 #fff） | `282d3e1` |
| 7 | 自查（钉住 #4/#5/#6 与 #1 的修后状态） | `src/games/window1-visual-r1-fix.test.ts`（新文件） | C 档修复源码级契约集中钉子：44px×3 / 织纹×2 / 纸感×1 / star-estate 地格矢量化（`se-tile-emoji` 清零 + `tileIconSVG` 在位 + emoji 进 aria-label） | 新文件共 +6 例 | `37d59f3` |
| 8 | learner P5（3A 格） | `combo-clash/art.ts` | `nearMounds` 每座土丘补两笔：顶部 `tint(near,0.3)` 亮弧（0.7 倍半径上半弧）+ 底缘 `shade(near,0.2)` 暗带（4px），五层背景唯一的一阶平涂补齐为三阶，静态 | `combo-clash/art.test.ts` +1 例（各主题 base/亮弧/暗带三色齐上） | `8ebc15b` |
| 9 | learner P6（2B 格） | `snake-royale/art.ts` | `drawFieldBackground` 网格层与贴片层之间加「原野色岛」远景：`ISLAND_CELL=900` 世界格恰一枚（格心 ±0.35 格哈希抖动，不成棋盘也不留多屏空档）、直径 300–500 世界 px 椭圆（save+scale+单位圆，兼容线不用 ellipse）、`theme.decoB`、alpha 0.08–0.12、视差 `ISLAND_PARALLAX=0.6`，与贴片层 0.85 恰两档不出第三档；同时贴片尺寸下限 `DECOR_MIN_SIZE` 2.4 → 3.2（低 zoom 不再近噪点） | `snake-royale/art.test.ts` +3 例（布点与结构逐笔 / 双档视差与镜头平移可复现 / 尺寸下限行为） | `fe5de02` |
| 10 | learner P9（2C 格） | `block-drop/art.ts` | 装饰改挂井壁：`paintWallRelief` 左右壁各一列、纵向每 6 格一件、6×12 内主题小件（wood=小花窗 / ice=冰晶 / dusk=果酱滴 / night=星窗）、双色阶只从 `wallLight` 同族派生（shade 深底 + tint 亮点，条纹明暗两段都读得出）；仍走 `drawWellBackground` 离屏缓存零逐帧成本，堆高过半后仍有装饰可看 | `block-drop/art.test.ts` +2 例（四主题四款且收在 6×12 内 / 上墙位置·间隔·不进玩法区） | `c1aa9dc` |
| 11 | learner P10 = tester G-7 | `src/games/weiqi-garden/art.test.ts`（新文件） | 补独立 `art.test.ts` 对齐其余八款的文件组织：**只新写补充用例**（花藤笔数 ≤ 12 / 花藤配色与确定性 / 整盘四枝与木纹 5–8% / 木纹种子确定性 / sprite 出血边两条 / washAlpha 边界两条 / 振纹·花瓣进度边界 / 配色合法性），`index.test.ts` 既有 5 个视觉 describe 原地保留一字不迁；`art.ts` 仅将 `paintCornerVine` 补 export（可测性，无行为变化） | 新文件共 +11 例 | `d3fcc6a` |
| 12 | 自查（合并态复验发现） | `star-estate/index.ts` | `.se-tile-icon` 尺寸由 `width:clamp(…)` 改 `font-size:clamp(15px,2.6vw,17px)` + `width:1em`：渲染结果不变，恢复 `levels.test.ts` 360px 字号红线所需的第 4 处 `font-size:clamp` 下限（≥13px）——#1 换掉 emoji 字号 clamp 时少了一处 | 复用既有红线用例（`levels.test.ts`「360px 窄屏有兜底」恢复全绿），用例数不变 | `3bc4df1` |

### 裁决登记（tester 移交的 G-2 / G-3 / G-4，均豁免并留痕）

| 编号 | 裁决 | 依据 |
| --- | --- | --- |
| G-2 `mj-tile` 30px 宽 | **豁免，不改** | 手牌选中态 `mj-cur` 构成二段确认（点错第一下不出牌）；手牌区 `overflow-x:auto` 兜底不溢出。tester 已给出此豁免路线，learner 认同。1.2 逐字遗留，非 1.3 回归 |
| G-3 `se-tile` 23×23 | **豁免，不改** | 走子由骰驱动，地格点击仅是信息入口；`se-deed` 信息行可达同一信息；`.se-tile:focus-visible` 3px outline 已在（learner「补 focus 环即可」的前提已满足）。为 23px 重排 11×11 棋盘风险远大于收益 |
| G-4 `fc-token` 19×19 | **豁免，不改** | `.fc-pick`（≥66×44）是走子主入口，token 为重复入口；本轮已在其盘面加装饰层，不再叠加热区外扩改动，留观 |

---

## 二、自查修复结论（第四节底线检查）

1. **商标扫描**：对本窗九款 + `src/art/kit` + `src/ui/motion.ts` + `src/art/runner` 全量 grep
   黑名单词（4399 / 马里奥 / 皮卡丘 / 托马斯 / Kitty / Tetris / 俄罗斯方块 / 球球·贪吃蛇大作战 / slither / agar /
   Monopoly / 大富翁 / 三国杀 / 雀魂 / 野狐 等）：**命中全部位于各款 `levels.test.ts` 等黑名单断言用例内**
   （即「测试禁止出现这些词」的清单本身），文案与代码注释零违规。新画的 33 款地格图标按 learner 商标风险表
   规避了「? 徽章 / 灯泡+水龙头 / 礼帽+单片眼镜」等构图。**阻断 0**。
2. **裸 emoji 主角残留**：本窗九款 `fillText` 直出 emoji 为 0；`P_EMOJI` 命中仅 `bomb-buddies`（他窗游戏，不碰，
   已在遗留清单登记转交）。本窗残余 emoji 均为文案层（star-estate 地契列表行文本、combo-clash 选人卡字符、
   各款 guide 文案），非棋盘 / 画布主角渲染，tester 口径「文案层 emoji 可留」。
3. **360px / 320px 溢出**：本轮全部修复不改横向布局——canvas 内绘制层（#2/#8/#9/#10）零布局影响；
   `.fc-decor` 为 `position:absolute;inset:0;pointer-events:none`；`.se-tile-icon` 为 clamp 收敛尺寸；
   44px 修复只加高度。tester 的 360/320 实测矩阵（九款横竖零溢出）没有被本轮任何改动触碰的失效面；
   `levels.test.ts` 各款 360px 红线用例全绿（含 #12 恢复的 star-estate 字号红线）。**新增溢出 0**。
4. **reduced-motion**：本轮新增装饰全部为静态件（贴片 / 色岛 / 浮雕 / 草地簇 / 云 / 织纹 / 纸感 / 土丘明暗），
   零动画、`soft` 无差别，天然满足 reduced 约定；抽查 flight-chess `prefersReducedMotion` 路径
   （骰面直落 / 走子直线滑 / 金边只亮不闪）未受影响，各款 reduced 契约用例全绿。
5. **kit 纪律**：`git log origin/game-1.3..HEAD -- src/art/kit/ src/ui/motion.ts src/art/runner/` 只有三条
   1.3 第 1 步 owner 的创建 commit（全部为新增文件 A，无后续 M），**无人改动别人的 kit 文件，无需恢复、无记名**。
   本轮 C 档对 kit 仅 import（`KIT_PALETTE` / `shade` / `tint` / `cloudSVG` / `drawSparkle` 等），
   未新增也未修改任何 kit 文件，未复制粘贴整份 kit。

---

## 三、遗留清单（修不动 / 不该本窗动的，交下一轮）

| 项 | 内容 | 理由与去向 |
| --- | --- | --- |
| L-1（tester G-1 = learner P7） | 共享壳 `src/games/level99.ts:512` `.l99-node-stars` ★ 字符 11px，建议改 12×12 内联 SVG 星形 | 共享关卡壳属壳层 owner，跨窗文件本窗不抢（窗口纪律）。修法 learner 已给全（SVG 星形走现有金色变量），转交壳层 owner 或统筹轮 |
| L-2（learner P8 进阶半项） | `.hc-table` 再叠 ≤4% 斜织纹 | 本轮已完成 `.hc-card` 纸感（主项）；桌面织纹属「锦上添花之上再添花」，与 3B 织纹同规格可随下轮顺手做,不值一个独立风险面 |
| L-3（learner 1A 格登记） | `src/art/kit/` 增补 `decor.ts` 中景三件套（软云 / 花草簇 / 星簇，Canvas + SVG 双出口） | kit 为 1A owner 独占（本窗约定 C 档不动 kit 供给面）；本轮九款需求已按「各游戏内自绘」方案满足（P2/P3/P6/P9 落地），供给缺口登记给 kit owner 待办 |
| L-4（自查） | `bomb-buddies/index.ts` `P_EMOJI` 裸 emoji 座位标 | 他窗游戏,本窗禁改;转交对应窗口 fixer |
| L-5（观察项） | combo-clash 选人卡 `<em>${c.emoji}</em>`、star-estate 地契列表行首 emoji 等文案层字符 | tester 口径「文案层 emoji 可留」,非六大专项失分面;若后续轮次要求文案层也画形,工作量为 12 角色徽记 + 列表图标复用 `tileIconSVG`,留档备查 |

---

## 四、合并态复验（第六节）

1. **git**：`git fetch origin game-1.3-window1` → 本地 rebase 于 `origin/game-1.3-window1` 之上 →
   确认 tester / learner 两份报告与十二格产物全部在位（他人文件零改动，`docs/plan-1.3-*.md`、`.cursor/skills/**` 原样）。
   推送方式：普通 push，无 force，未动 `main` / `game-1.2` / `1.2-kk`，未用 `gh` 开 PR。
2. **全量验证（合并态,代码=下表 SHA）**：
   - `npx vitest run`：**724 文件 / 15558 例全绿**（tester 水位 721/15504 → learner 722/15514 → C 档 724/15558，
     +2 文件 / +44 例，**只增不减** ✅）；
   - `npx tsc --noEmit`：**0 错误** ✅；
   - `npm run build`：**全绿** ✅，产物主包 `dist/assets/index-*.js` 110.70 kB（gzip 38.66 kB），
     PWA precache **188 项 / 5291.01 KiB**，`sw.js` / `manifest.webmanifest` 正常生成（vite preview 环境无浏览器,
     以产物文件存在性 + PWA 生成日志作冒烟等价物）。
3. **合并态 SHA**：**`3bc4df1`**（最后一个代码 commit;其后仅本报告与 SHA 登记两个 docs-only commit，
   不含任何代码改动,全绿结论对推送 tip 同样成立。最终推送 tip 见 git log 顶端）。
4. **收尾登记**：报告 commit `662e027` 之上再次执行 fetch → rebase（origin 无新提交,rebase no-op）→
   `npx vitest run`（724 文件 / 15558 例全绿,含报告在内的合并态复跑）→ `npx tsc --noEmit`（0 错误）→
   `npm run build`（全绿,precache 188 项 / 5291.01 KiB）。三人产物齐全,普通 push 交卷。

## 五、kit 纪律检查结论

见第二节第 5 条：**通过**。`src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` 自建立以来无任何越权改动，
本轮亦未新增 kit 文件；九款对 kit 的使用全部是 import,无复制粘贴。

## 六、窗口 1 · 第 1 轮视觉验收结论

- **阻断级：0 残留**（商标扫描零违规、360/320 零新增溢出，当轮清零 ✅）；
- **严重级：0 残留**（六大专项无不达标输入,自查亦未发现:裸 emoji 主角 0、平涂圆已在各步清账、
  双人可分 / 2.5D / 金币体积按 tester 72 格矩阵全 ✅）；
- **一般级**：输入 6 条（G-1–G-6）——修复 2 条（G-5、G-6），豁免 3 条（G-2、G-3、G-4，依据见裁决登记），
  遗留 1 条（G-1，共享壳跨窗，L-1）；learner P1–P10 落地 9 项、遗留 1 项（P7 = G-1）；
- **测试水位**：724 文件 / 15558 例全绿，较 learner 交卷 +2 文件 / +44 例，只增不减；
- 遗留清单 5 项（L-1–L-5）已逐条写明理由与去向，交第 2 轮。

**结论：窗口 1 第 1 轮视觉验收通过（阻断 0 / 严重 0 / 一般全部处置完毕）。**
