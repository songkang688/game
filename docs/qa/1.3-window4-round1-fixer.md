# 1.3 窗口 4 · 第 1 轮监督修复员报告

分支：`game-1.3-window4`。规格模板：`docs/plan-1.3-step27-C-fixer.md`（适配窗口 4）。
范围仅本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。

## 修复计划（开工 commit）

开工时 `docs/qa/1.3-window4-round1-tester.md` 与 `docs/qa/1.3-window4-round1-learner.md` 均未产出，按模板第四节先做自查修复，收尾 rebase 后跟进两份报告（若届时已 push）。

1. **商标扫描**：grep 全库文案与 9 款代码里的品牌词（4399 / 马里奥 / 皮卡丘 / 托马斯 / Kitty / 愤怒的小鸟 / 黄金矿工等易踩线题材词），造型相似度目检 sling-birds（弹弓鸟）与 gold-hook（矿工钩金）两款高危题材。
2. **裸 emoji 主角残留**：grep 9 款 `fillText` 直出 emoji、`*.emoji` 字段直绘、`P_EMOJI` 等 1.2 时代残留，漏网的按各步 1.3 提示词规格补画。
3. **六大专项抽查**：平涂圆主角、双人不可分（duo 三款 P1/P2 轮廓差异）、2.5D 缺失、金币无体积（gold-hook）、结算星形制、光源左上 45°/描边 1.5–2px 一致性。
4. **reduced-motion**：9 款验证装饰动画停、功能提示留（静态检查 `prefers-reduced-motion` / reduced 通道）。
5. **kit 纪律**：`git log src/art/kit/` 查有没有人改别人的文件。
6. 修复只落 `src/games/<id>/**` 绘制层 + 配套 `*.test.ts`；每处修复一个 commit（`fix(visual-r1): <款> <问题>`），配套用例只增不减。
7. 收尾：fetch → rebase → `npm test` → `npm run build` → 合并态 SHA 写入本报告。

（以下各节在修复完成后填写。）
