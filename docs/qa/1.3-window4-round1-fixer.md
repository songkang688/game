# 1.3 窗口 4 · 第 1 轮监督修复员报告

分支：`game-1.3-window4`。规格模板：`docs/plan-1.3-step27-C-fixer.md`（范围收窄为窗口 4 的 9 款）。
范围仅本窗 9 款：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
修复只落 `src/games/<id>/**` 绘制层与配套 `*.test.ts`；玩法数值 / 判定 / 关卡数据零改动。

## 修复计划（开工 commit `2670ef2`）

开工时 A 档（tester）与 B 档（learner）报告均未产出（A 档收尾时仍处「扫描计划已提交，执行中」状态，无可跟进条目；B 档文件未建）。按模板第四节执行自查修复，收尾 rebase 后复核两份报告状态如上。

## 一、自查结论

| 检查项 | 结论 |
| --- | --- |
| 商标扫描 | **通过**。9 款非测试代码零命中（马里奥/皮卡丘/Angry/4399/黄金矿工 等）；全库命中均为各款测试里的封禁词表（防护用，正常）。sling-birds（弹弓鸟）与 gold-hook（矿钩）题材目检：鸟为五色圆身原创家族、矿工钩为手绘 HUD 图标（`iconCanvas`），无造型抄袭。 |
| 裸 emoji 主角 | **通过**。9 款主角/核心道具全部为绘制资产（duo-rush 绘制跑者、duo-arena 朵朵星星双主角、garden-guard 八塔+怪物家族、sprout-defense 苗/虫、monster-crisis 五怪序列等，均有契约测试钉住）。 |
| 画布层残留 emoji（一般级） | **发现 3 款共 18 处，全部修复**（见修复清单）。其余款画布层干净；DOM 文案层 emoji（按钮/规则页/HUD chip）属既有容忍约定，登记不修。 |
| 360px 溢出 | 静态核查通过：9 款均有窄屏分支（garden-guard `hud12` 窄屏爱心写法、gold-hook 420px 收字、candy-swing `.cds-top` 换行等）。本轮无绘制改动触碰布局宽度；实测截图归 A 档专项⑤。 |
| reduced-motion | 9 款均接入（`prefers-reduced-motion` / `reduceMotion` / `particleBudget(reduced)`），gold-hook / sprout-defense / monster-crisis 接入点较少但均有 CSS 或粒子预算兜底；行为级验证归 A 档。 |
| kit 纪律 | `src/art/kit/` **尚未建立**（git log 零记录），各款资产自持于 `src/games/<id>/art.ts`，duo-rush art.ts 已留「待上移共享 kit」注记。无跨款改动违纪。 |
| 六大专项抽查 | 金币体积（duo-rush coinFrames 旋转帧、duo-arena drawKitCoin 径向渐变、gold-hook 手绘金块）、双人可分（duo 三款形状+颜色双通道）、2.5D（duo-rush view25d）、渐变星（garden-guard drawGoldStar、sprout-defense 绘制金星）均有既有契约测试且全绿。 |

## 二、修复清单（来源均为自查）

| # | 位置 | 问题 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | `src/games/garden-guard/{art,index}.ts` | 画布层 emoji 直出 13 处：浮字/成本/卖出/升级标签 🌸×9、标题 🌼×2、主题卡片 `st.emoji`×2、首页卡片 🗺️/🛡️、无尽标题 🛡️、提示 💡×2、天气 `wSpec.emoji`×2、⚡ | art.ts 新增 `drawPetalIcon`（五瓣花瓣币）/`drawBulbIcon`/`drawShieldIcon`/`drawMapScrollIcon`/`drawThemeBadge`（13 章徽章两两互异）；index.ts 全部改「文字 + 手绘图标」合成（`drawPetalLabel`/`drawIconTitle`），天气改用名字 | `art.test.ts` +5 例：源码 🌸/🌼/💡/🛡/🗺/⚡ 与 `.emoji` 插值清零、花瓣币含径向渐变零 fillText、13 徽章两两互异、灯泡/盾牌/卷轴有填充有渐变、徽章确定性 | `cd187a9` |
| 2 | `src/games/duo-arena/index.ts` | 画布 emoji 4 处：技能浮字 `${spec.emoji}`×2、人机徽章 `AI_SPECS[...].emoji`（🐣🙂😎🔥）、让分徽章 🤝 | 浮字带技能 id 画白底手绘 `drawSkillIcon`；人机难度改 1–4 颗 `drawMiniStar` 星阶（`AI_PIPS` 视觉常量）；让分改 `drawMicroFlower` 小花 | `art.test.ts` +3 例：pushFloat 不再拼 emoji 且浮字分支接 drawSkillIcon、AI 徽章走星阶、画布 hLabel 不再贴 🤝（DOM 文案不在此列） | `f7af94d` |
| 3 | `src/games/sling-birds/index.ts` | 关卡横幅画布标题拼章节 emoji（`CHAPTERS[...].emoji` / ♾️） | 标题去 emoji，两端既有的全绘制 `drawBannerBadge` 章节角标承担主题辨识 | `art.test.ts` +1 例：drawBanner 函数体无 `.emoji` 与 ♾ 且仍接 drawBannerBadge | `a91fb0e` |

修复统计：**阻断 0 条（无发现）/ 严重 0 条（无发现）/ 一般 3 款 18 处（全部清零）**。用例只增不减：+9 例。

## 三、遗留清单

1. **garden-guard 战内 HUD 行仍是 emoji 字符串**（`hud12.ts` 的 🌸 花瓣数 / 💗🤍 爱心，经 `drawHud` fillText 上画布）。不动理由：hud12 是 1.2 冻结契约（窄屏「💗×N」写法与整套宽度测量被 `upgrade12`/`defense12` 测试钉死，art.test 也明示「HUD 爱心是 hud12 旧契约,不在此列」），改造需整条 HUD 布局測量联动重设计，超出第 1 轮「只修视觉小步走」的安全边界。交下一轮与 A/B 档合议。
2. **DOM 文案层 emoji**（9 款按钮 / 规则页 / DOM chip，如 duo-vs-star 触控板 🤝✋💥、gold-hook 🛒⬇️、candy-swing 🍬⭐ 徽章等）。属全库既有容忍约定（emoji 直出红线针对画布资产），本轮不动，登记备查。
3. **360px 实测与 reduced-motion 行为级验证**：本轮仅静态核查，实测归 A 档专项⑤/横向抽查；若 A 档后续报出新溢出，下一轮修复员按阻断优先处理。

## 四、合并态复验（第六节）

- 收尾 `git fetch origin game-1.3-window4` → rebase：上游无新提交（A 档扫描计划 `9d33ac5` 已包含,B 档未开工）；三份报告状态：tester=计划中 / learner=未建 / fixer=本文件。
- `npm ci`：干净安装通过。
- `npm test`：**707 文件 / 15207 例全绿**（统筹基线 15198 + 本轮新增 9 例,只增不减）。
- `npm run build`：全绿。PWA precache **188 entries / 5235.12 KiB**，`dist/assets/index-jOMducE5.js` 107.12 kB (gzip 37.56 kB)。
- `npx vite preview` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**。
- 合并态 SHA：见本报告收尾 commit（推送后的分支头即合并态;本文件所在 commit 与其一致）。

## 五、kit 纪律检查结论

`src/art/kit/` 目录尚未建立，无人越权改动他人 kit 文件（git log 零记录）；本轮新增图标均落在各款自己的 `art.ts`，未跨款引用。

## 六、第 1 轮视觉验收结论（修复员视角）

- 阻断级：**0 残留**（商标零命中；无新 360 溢出证据）。
- 严重级：**0 残留**（六大专项契约测试全绿;裸 emoji 主角零）。
- 一般级：画布层 emoji 直出本轮清零 18 处；遗留 3 项如上（1 项旧契约冻结、1 项容忍约定、1 项归 A 档实测）。
- 等 A 档六专项矩阵与 B 档建议清单产出后，如有高优先项由第 2 轮修复员跟进。
