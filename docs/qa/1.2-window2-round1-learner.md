# 窗口 2 · 1.2 三轮验收 第 1 轮 · 学习优化员 · 汇总

> 每个 Pack 只追加自己那一节，不动别人的段落。
> 完整明细各自在 `docs/qa/1.2-window2-round1-learner-pack*.md` 里。

## Pack A

- 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
- 完整明细：[`docs/qa/1.2-window2-round1-learner-packA.md`](./1.2-window2-round1-learner-packA.md)
- 基线：`origin/game-1.2-window2`
- 结论：**列了 12 条，本轮落地 6 条，5 款每款都动到了。**
- 落地的全部是纯逻辑模块（`ghosts.ts` `logic.ts` `ai.ts` `search.ts`），
  一行 DOM / CSS / `meta` / 上屏文案都没改；测试员记的 16 条 PA-* 一条都没抢（那是修复员的活）。
- 新增 16 个用例，既有断言一条没删、没放宽；本包 5 款 36 个文件合计 719 例全绿。

### 改进清单

| # | 款 | 改进 | 本轮落地 | 收益（实测） |
|---|---|---|---|---|
| L-A-1 | dot-maze | 巡游 ↔ 追击换段时全体掉头 | ✅ | 节奏换段有了看得见的信号；被操纵的那只不跟着转 |
| L-A-2 | dot-maze | 惊吓时四只各回各的角落，不再排成一串 | ✅ | 连击要真的绕路去凑，不是顺着队伍吃到底 |
| L-A-3 | chess-garden | 置换表按局面做键，杀分不当缓存值 | ✅ | 同一手同一分，节点 −21% ~ −41%；200ms 预算内多算一层 |
| L-A-4 | junqi-camp | 高手 / 地狱档不再来回拉锯 | ✅ | 同一对格子从蹭 6 趟降到 1 趟；胜率断言不变 |
| L-A-5 | pool-stars | 库边球算真实碰库点，两段路都要通 | ✅ | 只能打库边的局面里，高手档从犯规改成先碰己组球 |
| L-A-6 | fruit-stack | 普通档不往顶到警戒线的那一摞上压 | ✅ | 八个种子总分 +6.4%，多撑 7% 的球 |
| L-A-7 | dot-maze | 小幽灵错峰出巢 | ❌ | 要重标难度曲线与三星门槛 |
| L-A-8 | dot-maze | 能量豆按路口 / 角落布点 | ❌ | 动生成器会改掉全部 188 关地图 |
| L-A-9 | pool-stars | 高手档加一次「母球会不会掉袋」的轻预演 | ❌ | 会抬高高手档实力，胜率门槛要重标 |
| L-A-10 | junqi-camp | 把踩出来的地雷记进 `Knowledge` | ❌ | 要改三处共用的 `Knowledge` 结构 |
| L-A-11 | chess-garden | 静态搜索加 SEE / delta 剪枝 | ❌ | SEE 是一块要单独写测试的新逻辑 |
| L-A-12 | fruit-stack | 地狱档「第二眼」撒点按下一颗半径自适应 | ❌ | 会改变每一步选择，要重跑重标 |

### 改了哪些文件

`dot-maze/{ghosts,logic}.ts` · `chess-garden/search.ts` · `junqi-camp/ai.ts` ·
`pool-stars/ai.ts` · `fruit-stack/ai.ts`，外加这 5 款各自的 `*.test.ts`。
`src/engine/**` 与窗口 1 的平台文件（`home.ts` `rootGate.ts` `playModes.ts` `view25d.ts`）一个字没碰。

### 红线

- 外部运行时依赖 0 新增（`package.json` / `package-lock.json` 未改）。
- 商标黑名单 0 命中；无血无死亡描写（`dot-maze/copy.test.ts` 的全目录扫描连注释一起查，绿）。
- 玩法没被推翻：五处改动都是在既有决策函数里加一项判据 / 换一个键。

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 详版：`docs/qa/1.2-window2-round1-learner-packB.md`
- 输入：`docs/qa/1.2-window2-round1-tester-packB.md`（阻断 0 · 严重 2 · 一般 10）
- 列出 12 条可落地改进，**落地 8 条**，其余 4 条记录在案。

| 编号 | 游戏 | 改进 | 本轮 |
| --- | --- | --- | --- |
| L-1 | `dark-chess` | 棋格热区 40px → 44px，窄屏格间距收到 3px | 落地 |
| L-2 | `hue-hand` | 键位提示补全 `W A S D` / `← → ↑ ↓` | 落地 |
| L-3 | `hue-hand` | 手牌横滚跟随光标（360px 上七张牌摆不下） | 落地 |
| L-4 | `hop-pads` | 加收力键：朵朵 `G`、星星 `K`，蓄过头能反悔 | 落地 |
| L-5 | `hop-pads` | `.hp-back` 返回钮 40px → 44px | 落地 |
| L-6 | `tap-tiles` | 双人分轨补别名键 `D` / `← →`，同轨双键当一个键使 | 落地 |
| L-7 | `fight-king` | 格斗塔台阶错开：两回合制从第 6 章推到第 7 章 | 落地 |
| L-8 | `fight-king` | 后段增益缓收，最高档是更会打不是更耐打 | 落地 |
| L-9 | `dark-chess` / `hop-pads` | `菜鸟 / 地狱` 档名对低龄偏冲 | 记录 |
| L-10 | `fight-king` | 格斗塔换人提示（后段真正的墙是角色适配） | 记录 |
| L-11 | `hop-pads` | 蓄力条满力提示 | 记录 |
| L-12 | `dark-chess` | 和棋倒数（二十手不吃不翻）没有 HUD 计数 | 记录 |

关键复测数据：格斗塔第 132 关用 lv4 的手真打十局，**2/10 → 5/10**，测试员点名的断崖平掉了；第 160 / 176 关仍是 0/10，墙在角色适配（L-10），改前改后一致。

`npm test` 249 文件 / 6330 用例全绿（基线 248 / 6312，+1 文件 / +18 用例，一条没删）；`npm run build` 通过；三张黑名单 0 命中。
两条**严重**（`tap-tiles` 同谱平局判输、`hop-pads` 双人触屏串台）按分工留给修复员，本轮未动。
