# 窗口 2 · 1.2 第 1 轮 · 测试员 包 C（点名五项）

- 窗口号：2
- 角色：QA 第 1 轮 测试员 · 包 C
- 基线：`origin/game-1.2-window2`
- 范围（只验这 5 款，别人的游戏一个字都不改）：
  `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`

---

## 走查计划（动代码前先立此为据）

### 0. 通用铁则（每款都要做）

| # | 检查项 | 判定方式 |
| --- | --- | --- |
| G1 | 首页进入 → 真实赢一次 / 输一次 → 退出再进 | 驱动 `mount()` 走到结算，`destroy()` 后重新 `mount()` |
| G2 | 战役第 1 / 100 / 188 关 | 直开该关，跑到通关或确认可解 |
| G3 | 对战 / 无尽 / 双人每种模式玩到结算 | 逐模式驱动到结算面板 |
| G4 | 360px 宽 | 以 360×640 尺寸挂载，量关键热区与溢出 |
| G5 | 双人键位互不抢 | 两侧键位集合求交集必须为空 |
| G6 | `Esc` 暂停 | 壳层/游戏内 Esc 行为一致，不与游戏键冲突 |
| G7 | `meta` 与实现一致 | `modes` / `levels` / `platform` 对齐 index.ts 实际入口 |
| G8 | `destroy` 无泄漏 | 计时器 / RAF / 事件监听在 destroy 后清零 |
| G9 | 商标 0 命中 | 全目录扫已知商标词表 |

### 1. 点名五项专项（失败即阻断）

| 项 | 硬要求 | 取证方式 |
| --- | --- | --- |
| `gomoku` | 解局关真有解：抽 10 关按攻略走；菜鸟→地狱固定 seed 相邻档对下，胜率单调；前 99 题未改 | 直接执行 `puzzles.ts` 的解法序列并验证终局；`ai.ts` 六档两两对弈统计；`git diff origin/game-1.1` 查前 99 题 |
| `match-stars` | 消除→下落→补位三个**可观察**阶段，逐帧/时间轴证明，**瞬变即阻断** | 按帧推进 `anim.ts` 时间轴，记录每阶段起止 ms 与中间帧快照 |
| `rainbow-run` | 接住 1.1 的 `view3d.ts` / `controls.ts` / `endless.ts`；对比 `origin/game-1.1`；禁 three.js、禁重建摄像机 | `git diff origin/game-1.1 -- src/games/rainbow-run/{view3d,controls,endless}.ts`；扫 three 依赖与摄像机重建点 |
| `ocean-munch` | 无尽「深海马拉松」能玩到失败；`save.recordEndlessBest("ocean-munch", depth)` 退出重进还在；前 99 关参数未改 | 驱动无尽到 game over；查 `recordEndlessBest` 调用与 storage 往返；`git diff origin/game-1.1` 查前 99 关 |
| `xiangqi` | 只升级已有目录（没有第二款象棋）；六档 AI 强度单调；188 残局有解；长将判定正确 | `ls src/games` 查重；六档对弈统计；批量求解 188 局；长将用例 |

### 2. 取证纪律

- 每条结论都要落一段可复现的命令或脚本输出，不接受"看代码觉得没问题"。
- 一次性走查脚本放 `/tmp`，不进仓库，不污染别人的目录。
- 报告只写两份：本文件（独占）与 `docs/qa/1.2-window2-round1-tester.md` 的 `## Pack C · 点名五项` 一节（只追加）。

### 3. 结论区（走查后回填）

待回填。
