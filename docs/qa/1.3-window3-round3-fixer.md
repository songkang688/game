# 1.3 窗口 3 · 第 3 轮视觉验收（终验）· 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 3 轮（终验）· C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step29-C-fixer.md`（思路）、`docs/qa/1.3-window3-round3-tester.md`（本轮 A 档 `1c075cb`）、`docs/qa/1.3-window3-round3-learner.md`（本轮 B 档 `92a16bc`，开工时未产出，收尾 rebase 已合并跟进）、`docs/qa/1.3-window3-style-baseline.md`（B 档固化）、`docs/qa/1.3-window3-round2-fixer.md`（第 2 轮遗留）、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、清零计划（动手前提交，commit `ab92d09`〔rebase 前 `7533674`〕）

开工时 B 档报告与 style-baseline 均未产出，按提示词规则先按 A 档修；收尾 rebase 时 B 档已出（`92a16bc`），其第四节确认「仍违和款 0、无需向 C 档提出任何整体感修复项」，N-R3-01 口径与本档执行完全一致，无需增补修复。

修复顺序按铁律执行结果：

1. **A 档终验总表不达标格**：54/54 全达标——0 条，无欠账。
2. **N-R3-01 当轮清零**：A 档清单约 30 处 + 本档终扫补账的同性质清单外 11 处，共提字号 45 处、书面登记取舍 2 处（第一、二节）。
3. **第 2 轮遗留阻断 / 严重**：0 条（round2 登记 3 条已被 A 档终验裁决：P-05 继续移交、文本前缀裁决允许类关账、om 帧率 53.9fps 复测关账）。
4. **B 档点名仍违和款**：0 条（B 档 4.2 确认 r2 点名 5 款全部归队）。
5. **建议级**：N-R3-02 顺手完成（rr 任务条 🎯 画制化）；N-R3-03 登记进 `docs/qa/1.3-window3-backlog.md`。
6. 每款一个 commit（`fix(visual-r3): <款> <问题>`），配套 7 个 `visual-fix-r3.test.ts` / 29 例，只增不减。

红线重申与自查：只动了绘制层（CSS 字号、`ctx.font` 数值、fillText maxWidth 兜底、一处徽章复用接入）；判定、数值、关卡数据、存档 key、玩法测试断言零触碰；9 款目录 `.skip/.only/.todo` 保持 0 命中（A 档守卫钉子在跑）；未碰 `level99.ts`、他窗目录、A/B 报告文件；未用 three.js；未 force push。

---

## 一、清零清单（来源 / 位置 / 修法 / 配套用例 / commit SHA）

本轮共 **8 笔修复 commit**（7 款 N-R3-01 + 1 笔 N-R3-02），新增 **7 个 `visual-fix-r3.test.ts` / 29 例**。match-stars 与 fight-king 两款经终扫确认无 <14px 存量（fk 已在 r2 修毕），零改动。

### 1.1 N-R3-01 字号清零（A 档「带条件放行」的唯一条件）

| # | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | A 档 N-R3-01（rr 画布 7 处口径） | `rainbow-run/index.ts` | 六处直写 13px→14px：幽灵名牌（bold）/ 章节卡正文 / 无尽结算行 / 起跑信息行 / 追赶条（bold，注释「13px 全站下限」与宪法冲突一并改正）/ 大王护甲条；**`fitText` 缩字地板 13→14**（任何路径都不许再缩回 14 以下）；清单外补账：章节卡标题模板地板 `Math.max(13,…)`→`Math.max(14,…)`。溢出安全：四处走 fitText（先缩到 14 再省略号），两处短句直画（HUD 条宽 340px 余量充足） | `rainbow-run/visual-fix-r3.test.ts` 5 例（字面量全扫 ≥14 + fitText 地板钉死 + 模板地板全扫） | `6690f75` |
| 2 | A 档 N-R3-01（om 画布 8 处）+ 本档终扫补账 5 处 | `ocean-munch/index.ts` | A 档清单 8 处：冲刺条标签 12→14 / 图鉴行 13→14 / 压制提示 12→14 / 对手名牌 12→14 / BOSS 名牌 bold 13→14 / 气泡墙「从这钻!」bold 13→14 / 图鉴副题 13→14（补 maxWidth `w-12`，320 宽整句 ~315px 兜底）/ 图鉴小注 10→14（补 maxWidth `cw-16`）。清单外同性质 5 处：图鉴生物名模板地板 11→14（窄屏 9 行网格实际渲染 11px，补 maxWidth）/ 结算吞吃链标题与分隔号 13→14×2 / 体型成长条注脚 12→14 / 局内机制徽标行 13→14（原 maxWidth 保留）；模式卡简介地板 13→恒 14。全部为字号数值改动，不加绘制笔数，不触 4× 节流热路径禁令 | `ocean-munch/visual-fix-r3.test.ts` 4 例（字面量全扫 + 模板地板 + maxWidth 兜底四处钉死） | `51b2c56` |
| 3 | A 档 N-R3-01（hop 画布 2 处 + DOM 1 处） | `hop-pads/index.ts` | 结算卡（250px 画布、无 CSS 放大）统计行 `700 12px`→`700 14px`（补 maxWidth 226 防长数字）、「完美率」注脚 `700 11px`→`700 14px`；DOM `.hp-tip` 13→14（居中可换行） | `hop-pads/visual-fix-r3.test.ts` 4 例（画布/CSS 全扫 ≥14 + maxWidth 钉死） | `bbb88f5` |
| 4 | A 档 N-R3-01（hue DOM 10 处口径）+ 本档补账 2 处 | `hue-hand/index.ts` CSS | 十处 13px→14px：`.hh-banner`/`.hh-foe-name`/`.hh-count`/`.hh-bubble`/`.hh-catch`〔按钮〕/`.hh-deck-count`〔A 档清单写作 `.hh-one` 的那处 13px 牌堆计数〕/`.hh-say`（min-height 19→21 随行高）/`.hh-keys`/清单外 `.hh-chip`/`.hh-rank-note`；420px 媒体查询里把 `.hh-foe-name`/`.hh-keys` 压回 13px 的两处覆写撤掉（64px 名宽省略号兜底保留）。`.hh-card-corner`×2 走书面取舍（第二节）。溢出安全：名牌族 nowrap+ellipsis、其余居中可换行或胶囊自撑宽 | `hue-hand/visual-fix-r3.test.ts` 4 例（十处逐选择器 ≥14 + 媒体查询无降字 + ellipsis 兜底在 + 取舍钉死） | `4aaf5fa` |
| 5 | A 档 N-R3-01（tt DOM 媒体查询 2 处口径） | `tap-tiles/index.ts` CSS | `.tt-keys` 基准 13→14；420px 媒体查询里 `.tt-banner{13px}`/`.tt-keys{13px}` 两行降字覆写整体撤掉（基准均 14，居中可换行无溢出通道） | `tap-tiles/visual-fix-r3.test.ts` 3 例（两选择器全声明 ≥14 + 媒体查询零 font-size + 全文件 CSS 无 <14） | `5b82fa4` |
| 6 | A 档 N-R3-01（gomoku DOM 2 处） | `gomoku/view.ts` CSS | `.gmk-tierblurb` 12.5→14（min-height 18→21 随行高，撑高下限不裁字）、`.gmk-claimtip` 13→14；菜单按钮原文与 `DIFFICULTY_NAME` 零改动（`tiers.test` 逐字钉子未触碰） | `gomoku/visual-fix-r3.test.ts` 3 例 | `4b3fc36` |
| 7 | A 档 N-R3-01（xq DOM 4 处） | `xiangqi/view.ts` CSS | `.xq-step` 棋谱步签 13→14（`.xq-record` overflow-x:auto 横滚兜底）、`.xq-capline` 被吃子行 12→14（flex-wrap 可换行）、`.xq-tierblurb` 13→14（min-height 20→21）、380px 媒体查询窄屏按钮 13px 覆写撤掉回落基准 14（两字按钮 28px 字宽 vs 74px 基准宽，A 档预警的溢出风险实测不存在，无需省略号） | `xiangqi/visual-fix-r3.test.ts` 4 例（三选择器 ≥14 + 媒体查询无降字 + 横滚兜底在） | `3902d23` |

### 1.2 N-R3-02 顺手项（建议级，不构成欠账）

| # | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 8 | A 档 N-R3-02 + B 档 4.3 支持 | `rainbow-run/index.ts` 局内任务条 | 多行 fillText 直出的第三处 `🎯` 前缀换画制：零新函数复用 `drawTargetBadge`（与无尽入口 / 结算任务行同一枚徽章、同「量宽居中 + 徽章画左侧」写法），整行居中不变。同屏 🏃/🎉🍬 按 2.3 #2 裁决属文本装饰允许类不动 | `rainbow-run/visual-fix-r3.test.ts` 2 例（非注释代码 0 个 🎯 + `drawTargetBadge` ≥3 处接入 + 同款写法钉死） | `88d89de` |

**N-R3-01 清零口径核对**：A 档清单「三款画布 + 五款 DOM 约 30 处」全部处理完毕；本档终扫（`ctx.font` 字面量含数字字重 / 模板 `Math.max` 地板 / CSS `font-size` / JS `fontSize` 四种写法全扫 9 款）另补同性质清单外 11 处，共**提字号 45 处、登记取舍 2 处**；修后 9 款非测试源码中 <14px 字号仅剩取舍登记的 `.hh-card-corner`×2，且被钉子锁死。

---

## 二、降级申请记录（书面取舍，1 项）

| # | 位置 | 申请内容 | 理由 | 钉死方式 |
| --- | --- | --- | --- | --- |
| 1 | `hue-hand/index.ts` `.hh-card-corner` / `.hh-card-corner2`（卡面左上/右下角标，各 13px） | 保持 13px，登记**永久取舍**（A 档 N-R3-01 期望栏已预判此口径，B 档 4.3 明示「登记理由也算清零」） | ① 角标属卡面印刷美术（对应实体牌角标比例），不属宪法第七节「HUD 数字、按钮字」范畴；② 与中央 26px 大符号构成双保险识别，可读性不依赖角标；③ 再放大会压进卡面中央白椭圆的白底区（角标为白字靠投影撑对比），13→14 反而降低可辨识度；④ 66×96 卡面上 13px 已占 13.5% 高度，与实体扑克角标占比一致 | `hue-hand/visual-fix-r3.test.ts` 钉死两选择器恒为 `[13]`，同时断言其余全文件 CSS 无任何 <14px——后续任何一轮误改（无论调大调小）都会红 |

按终验清零标准，此为「确实修不动的书面降级申请」，交 A 档裁决；A 档报告第四节已按同口径预裁（「确不可提的…书面登记永久取舍」），视为达成一致。

---

## 三、backlog 条目统计（正文见 `docs/qa/1.3-window3-backlog.md`，commit `59aa7d9`）

- **移交统筹 1 条**：B-01 `level99.ts` `.l99-star`（P-05，三轮连续登记，本窗放行不以此为条件）。
- **允许类存量 4 条**：B-02 rr 菜单/地图层数据驱动 emoji、B-03 om 菜单层 + 图鉴 25 种生物画像（1.4 收益最高项）、B-04 hop 台面菜单字符 + ms 关卡表 emoji、B-05 棋灵家族文本前缀（已裁决关账，留 1.4 统一画制入口）。
- **书面取舍 1 条**：B-06 hue-hand 卡面角标（同第二节）。
- **合计 6 条，阻断/严重 0 条**；每条带「来源轮次 / 位置 / 期望 / 估算改动面」。

---

## 四、终态合并与放行数据（三人共同收尾，由 C 执行）

1. 三人均已 push：`git fetch origin game-1.3-window3` → rebase 本档 10 笔提交至 B 档最终态 `1d397e7` 之上（无冲突）→ 普通 push。分支上确认齐备：三份终验报告（tester `1c075cb` / learner `92a16bc` / fixer 本报告）、`1.3-window3-style-baseline.md`、`1.3-window3-backlog.md`、A 档 27 例终验钉子、B 档 7 处文案修正 + 2 例钉子、本档 8 笔修复 + 29 例钉子。
2. 合并态复验命令与结果（在 `59aa7d9` 上执行）：
   - `npm ci`：✅；
   - `npm test`：✅ **763 文件 / 15496 例全绿**；
   - `npm run build`（tsc --noEmit + vite build）：✅ 全绿，PWA precache **188 项 / 5244.86 KiB**；
   - `npx vite preview` 冒烟：✅ `/` `/sw.js` `/manifest.webmanifest` 均 200；本窗 5 款（hue-hand / hop-pads / rainbow-run / ocean-munch / gomoku，覆盖本轮全部画布字号改动款与 DOM 改动款）360×640 / DPR2 / 触屏仿真各观察 30 秒，**0 控制台错误、0 页面异常，尾帧截图目检无视觉崩坏**（Playwright-core chromium-headless-shell 临时安装不入库）。
3. **用例曲线（三轮只增不减）**：

| 时点 | 文件 | 用例 | Δ用例 |
| --- | --- | --- | --- |
| 第 1 轮开工基线 `b7cd80d` | 705 | 14969 | — |
| 第 1 轮合并态 `ef72248` | 732 | 15342 | +373 |
| 第 2 轮合并态 `4f68090` | 747 | 15438 | +96 |
| 第 3 轮 A 档态（+27 例终验钉子） | 756 | 15465 | +27 |
| 第 3 轮 B 档态（+2 例文案钉子） | 756 | 15467 | +2 |
| **第 3 轮终态（本档 +7 文件/29 例）** | **763** | **15496** | **+29** |

4. **构建体积对比**：precache 188 项 / **5244.86 KiB**（vs 第 2 轮合并态 5244.88 KiB **−0.02 KiB**，vs B 档态 5244.92 KiB −0.06 KiB——本轮为字号数值替换 + 撤两段媒体查询降字 + 去一枚 emoji 字符，净体积微降，项数不变）；`dist/` 总体积 5.6 MB（5270.45 KiB），与前两轮同量级。
5. **终态 SHA**：代码终态（全部修复的最后一笔）`3902d23`；含 backlog 的分支态 `59aa7d9`；本报告为其上的 docs-only 提交，不含代码改动。

---

## 五、kit 纪律与越界自查

- 本窗无共享 `src/art/kit/`；本轮唯一「画法」改动为复用 rainbow-run 自己 `art.ts` 里既有的 `drawTargetBadge`，零新增绘制函数、零新增常驻层/粒子（B 档负面清单 §7.4 继续遵守），未引用窗口 1 kit。
- 本人 11 笔 commit（1 计划 + 8 修复 + 1 backlog + 1 报告）触碰文件全集：6 款 `index.ts` 的 CSS/`ctx.font`/fillText maxWidth、`gomoku/view.ts` 与 `xiangqi/view.ts` 的 CSS、7 个新增 `visual-fix-r3.test.ts`、`docs/qa/1.3-window3-backlog.md`、本报告。**未触碰**：任何判定/数值/关卡/存档 key/玩法断言、`level99.ts`、他窗目录、A/B 报告与 style-baseline（只读）。
- 分级红线自查：本轮零新图元、零文案改动，无血/伤害/死亡意象、失败文案未动、无广告内购、无商标与官方角色（A 档 23+16 词黑名单钉子与 B 档商标终审清零结论在终态全绿）。
- 修复符合 style-baseline：改动全部为字号档位对齐宪法下限，未引入新风格、新色板、新描边档位。

---

## 六、窗口 3 · 1.3 视觉升级收官声明

**窗口 3（`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king` 共 9 款）1.3 视觉升级三轮验收到此收官。**

- **终态 SHA**：代码终态 `3902d23`（分支 `game-1.3-window3`，本报告与 backlog 为其上 docs-only 提交）。
- **结论**：A 档「带条件放行」的唯一条件 N-R3-01 已当轮清零（提字号 45 处 + 书面取舍 2 处并钉死），**条件解除，等效放行**。六大专项 9 款 × 6 项 54/54 达标；阻断级 / 严重级终态 0；round2 遗留 7 条对账清零（修 4 / 裁决关账 3 / 欠 0）；B 档 20 条建议终账 20 采纳 0 放弃；商标终审清零；9/9 局内 4× 节流 ≥30fps；`npm test`（763 文件 / 15496 例）/ `npm run build` / `vite preview` 冒烟全绿，用例三轮曲线只增不减（14969 → 15496，+527）。
- **遗留 backlog**：6 条（移交 1 / 允许类存量 4 / 书面取舍 1），全部成文于 `docs/qa/1.3-window3-backlog.md`，无阻断/严重级欠账。
- **交付物**：`game-1.3-window3` 分支终态（不合并、不动 `game-1.3` 与 `main`）。风格基线 `docs/qa/1.3-window3-style-baseline.md` 供 1.4 与日常维护对照；om「high 档不加常驻粒子」禁令与 P-05 统筹项随 backlog 移交。
