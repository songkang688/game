# 1.3 窗口 3 · 第 3 轮视觉验收（终验）· 监督修复员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 3 轮（终验）· C 监督修复员。
> 分支：`game-1.3-window3`。只修视觉绘制层，一行玩法逻辑不动。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step29-C-fixer.md`（思路）、`docs/qa/1.3-window3-round3-tester.md`（本轮 A 档）、`docs/qa/1.3-window3-round3-learner.md`（本轮 B 档，开工时未产出，若后到则合并跟进）、`docs/qa/1.3-window3-round2-fixer.md`（第 2 轮遗留）、各款 `docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、清零计划（动手前提交）

开工时 `docs/qa/1.3-window3-round3-learner.md` 与 `docs/qa/1.3-window3-style-baseline.md` 均尚未产出：按提示词规则先按 A 档（`1c075cb`）修；收尾 fetch/rebase 时若 B 档已到，则合并跟进其点名项。

A 档终验结论为**带条件放行**，唯一条件即 N-R3-01。修复顺序按铁律：

1. **A 档终验总表不达标格**：54/54 全达标——0 条，无欠账。
2. **N-R3-01 当轮清零**（约 30 处 <14px 局内 HUD/按钮字号）：画布（rr 7 处 / om 8 处 / hop 2 处）+ DOM（hue 10 处 / hop 1 / tt 1 / gomoku 2 / xq 4）——能提则提至 ≥14px（360/320 复测不溢出），确有取舍必要的逐处书面登记（A 档已预判 `.hh-card-corner` 属卡面美术双保险）。
3. **第 2 轮遗留阻断 / 严重**：0 条（round2-fixer 登记 3 条已被 A 档终验全部裁决关账 / 移交：P-05 继续移交、文本前缀裁决为允许类、om 帧率复测 53.9fps 关账）。
4. **B 档点名仍违和款**：开工时无 B 档，收尾 rebase 后核对。
5. **一般 / 建议**：N-R3-02（rr 局内任务条 🎯 多行 fillText，顺手对齐 `drawTargetBadge`，加分项）；N-R3-03（菜单层数据驱动 emoji 存量）登记进 `docs/qa/1.3-window3-backlog.md`。
6. 每修一处一个 commit（`fix(visual-r3): <款> <问题>`），配套用例只增不减。
7. 终态合并与放行流程：三人 push 齐后 fetch → rebase → `npm ci` → `npm test` → `npm run build` → `vite preview` 冒烟 → 记录终态 SHA / 用例曲线 / 体积 → 收官声明 → 普通 push。

红线重申：只动绘制函数 / CSS / 视觉常量 / 本款 `art.ts`；判定、数值、关卡、存档 key、玩法测试断言一律不碰；被 skip 的历史用例一律恢复（A 档已核 9 款目录 `.skip/.only/.todo` 0 命中）；禁止 three.js；禁止 force push；不改 `level99.ts`（P-05 继续移交）；不碰别人的报告文件；不动 main / game-1.2 / 1.2-kk / game-1.3 主线。

（以下各节随修复进度补齐。）
