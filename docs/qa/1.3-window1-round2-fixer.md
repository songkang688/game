# 1.3 窗口 1 · 本窗三轮验收 · 第 2 轮 · 监督修复员报告

> 角色：窗口 1 第 2 轮监督修复员（C 档）。落地 A 档（r2 tester）问题清单与 B 档（r2 learner）建议清单的
> 可动手项，并执行合并态复验。只动绘制层（绘制函数 / CSS / 视觉常量），判定、数值、关卡数据、存档 key、
> 玩法测试断言一律不碰。
> 输入：`docs/qa/1.3-window1-round2-tester.md`（阻断 0 / 严重 0 / 一般 R2-1 / 建议 R2-2、R2-3；P1–P10 回归 12/12）、
> `docs/qa/1.3-window1-round2-learner.md`（11 格达标 / 1 格待改 4A；C 档清单第三节）。
> 依据：`docs/plan-1.3-visual-bible.md`（视觉宪法）、`docs/qa/1.3-window1-plan.md`（十二格登记）、
> `docs/qa/1.3-window1-round1-fixer.md`（r1 修复与遗留基线）。

## 0. 修复计划（开工承诺，commit `b2d0dd0`）

修复顺序铁律：阻断（本轮输入 0 条，自查复核）→ 严重（0 条，自查复核）→ 一般 → 建议。

1. **本轮唯一动手项 R2-2（= L-2，建议级）**：`hero-cards/index.ts` `.hc-table` 叠斜织纹。
   按 learner 第三节第 1 条规格执行：在现有 `linear-gradient(180deg,#FBEBD8,#F5DCC2)` 之前
   （多重背景第一层）加 `repeating-linear-gradient(45deg,rgba(170,120,80,.04) 0 6px,transparent 6px 12px)`——
   与 `.mj-board` 同 45° 角、同 6px/12px 节距；织纹色不用白纹（浅暖米底上 3% 白纹不可见），
   改用从木沿色 `#EBD2B4` 向深派生的暖褐 `rgba(170,120,80,.04)`，alpha 恰 4% 顶格不越线。
   一行 CSS + 契约测试 ≥1 例（新文件 `src/games/window1-visual-r2-fix.test.ts`）。
   红线：只动 `.hc-table` 一条规则；`.hc-card` / `cardart.ts` / 判定零触碰。
2. **本轮不修、写入遗留**：R2-1（= L-1，共享壳）、R2-3（零代码）、L-3（kit owner）、L-4（他窗）、L-5（观察项）。
3. **自查底线（第四节，收缩到本窗）**：商标 grep、裸 emoji 主角、360/320、reduced-motion、kit 纪律。
4. 每修一处一个 commit（`fix(visual-r2): <款> <问题>`）；用例只增不减（进场水位 726 文件 / 15577 例，
   learner 代码 SHA `19710a8`，tip `372e7a7`）。
5. 收尾合并态复验：fetch → rebase → `npx vitest run` → `npx tsc --noEmit` → `npm run build` → preview 冒烟 →
   合并态 SHA、用例总数、构建体积写进本报告。普通 push，禁 force。

---

## 一、修复清单（一处一 commit）

修复统计：**阻断 0 条（无输入，自查亦未发现）；严重 0 条（无输入，自查亦未发现）；建议 1 项落地（R2-2）**。
本轮 tester / learner 输入没有阻断与严重项，一般级唯一一条（R2-1）为跨窗共享壳禁改文件（见遗留清单），
故本轮动手面恰为 learner 明示交给 C 档的 R2-2 一条——不为「显得有活干」往达标格加装饰（learner 总提示）。

| # | 来源 | 位置 | 修法 | 配套用例（只增不减） | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | tester R2-2 = learner 清单第 1 条（= r1 遗留 L-2） | `src/games/hero-cards/index.ts` `.hc-table` 规则（原 `:120-122`） | `background` 改多重背景：第一层（织纹在上）加 `repeating-linear-gradient(45deg,rgba(170,120,80,.04) 0 6px,transparent 6px 12px)`，第二层保持原纸色 `linear-gradient(180deg,#FBEBD8,#F5DCC2)` 不动。与 `.mj-board`（`mahjong-bloom/index.ts:102`，真名见 R2-3）同 45° 角、同 6px/12px 节距，两款棋牌桌面同一套织物语言；织纹色按 learner 规格弃白纹改**暖褐** `rgba(170,120,80,.04)`（从木沿色 `#EBD2B4` 向深派生，不引入新色相），alpha 恰 4% 顶格不越线；纯静态无动画。眯眼测试：织纹低于牌堆 / 头像 / 体力标一阶 | 新文件 `src/games/window1-visual-r2-fix.test.ts` +2 例：① 织纹存在且色值 / 节距逐字符合规格、alpha 正则钳制 ≤0.04；② 织纹为多重背景第一层（`indexOf` 序在纸色渐变之前）且底渐变未被改动 | `8ca41a8` |

**修复红线自查**：本轮代码 diff 仅 `hero-cards/index.ts`（+3 行 CSS 注释与背景层，-1 行原背景）与新测试文件；
`.hc-card` / `cardart.ts` / 判定 / 数值 / 关卡数据 / 存档 key / 玩法测试断言零触碰；
`parentAuth.ts` / `parentGate.ts` 零触碰；无 three.js；未动 `src/art/kit/`、`src/games/level99.ts`。

---

## 二、无法修复的遗留清单与理由

| 项 | 内容 | 理由与去向 |
| --- | --- | --- |
| R2-1（= L-1 = r1 G-1，一般级） | 共享壳 `src/games/level99.ts:512` `.l99-node-stars` ★ 字符 11px（@媒体降 10px），九款关卡页同源受累，本轮实测唯一 <14px 命中 | 共享关卡壳属壳层 owner / 统筹轮，跨窗文件**本窗禁改**（窗口纪律 + 本轮任务书明令）。修法 r1 learner P7 已给全（★ 改 12×12 内联 SVG 星形，双态 fill `#ffb937`/`#e3ddef`，连带 `:538 .l99-beststars` 与 `:548 .l99-ov-stars` 一次改净）。转交壳层 owner / 第 3 轮统筹；严重度维持「一般」（1.2 逐字遗留非 1.3 回归） |
| R2-3（建议级，零代码） | r1 fixer 报告修复表 #5 把麻将织纹落点写作 `.mj-table`，实际选择器真名为 **`.mj-board`**（`mahjong-bloom/index.ts:102`） | 文档笔误级，实现本身达标；历史报告按纪律不改。本报告与本轮修复（R2-2 参照织纹规格处）全程使用真名 `.mj-board`，后续轮次文档引用照此 |
| L-3 | `src/art/kit/` 增补 `decor.ts` 中景三件套供给缺口 | kit 为 1A owner 独占（本窗约定 C 档不动 kit 供给面）；本窗九款需求已按「各游戏内自绘」满足（r1 P2/P3/P6/P9），无受害面。维持登记 kit owner 待办 |
| L-4 | `bomb-buddies/index.ts` `P_EMOJI` 裸 emoji 座位标 | 他窗游戏，本窗禁改，维持转交对应窗口 fixer。本轮 `P_EMOJI` 全库登记（只登记不改）：`bomb-buddies` / `pool-stars`（index.ts 与 rules.ts）/ `bumper-cars` / `bowling-lane` / `monster-crisis`（logic.ts）/ `fruit-stack`，均为他窗 |
| L-5 | 文案层 emoji（combo-clash 选人卡 `<em>${c.emoji}</em>`、star-estate 地契列表行首等） | 维持「文案层可留」口径，观察项不抬成阻断、不做大改（本轮任务书明令）；若全局轮要求画形，工作量估算已在 r1 fixer L-5 留档 |

---

## 三、自查修复结论（底线检查，收缩到本窗）

1. **商标扫描**：对本窗九款 + `src/art/kit` + `src/ui/motion.ts` + `src/art/runner` 的**非测试源码**全量
   `rg -i` 黑名单（4399 / 任天堂 / 迪士尼 / 马里奥 / 皮卡丘 / 宝可梦 / Hello Kitty / 托马斯 / Bomberman /
   Tetris / 俄罗斯方块 / 球球大作战 / 贪吃蛇大作战 / slither / agar / Monopoly / 大富翁 / 三国杀 / 雀魂 /
   野狐 / 蛋仔 / 地铁跑酷 / 超级玛丽 / Nintendo / Disney / Mario / Pokemon / Pikachu / Subway Surf）：
   **0 命中**。star-estate `art.ts` 无「? 徽章」（图标图形体无 `?` 字符；全文件仅有的两处 `<text>` 为
   「抵押中 / 已收摊」中文状态标签，属信息文本非图标图形体，tester P1 钉子「TILE_ICONS 图形体无 `<text>`」
   契约同批复跑全绿）。本轮新增织纹为无图案 45° 平行线族，无字母 / 徽记 / 花型织入（learner 商标风险表第 1 行规避项）。**阻断 0**。
2. **裸 emoji 主角**：本窗九款非测试源码 `fillText(` 行逐款扫描 emoji 码位——**九款全部 0 直出**；
   他窗 `P_EMOJI` 只登记不改（清单见遗留 L-4 行）。**阻断 0**。
3. **360/320**：本轮唯一改动是 `.hc-table` 的 `background` 多重背景层——纯颜料层，不改任何盒模型 /
   宽高 / 布局属性，r2 tester 的 18 组（九款 × 360/320）零溢出实测无失效面；hero-cards 360×640
   preview 冒烟复核页面正常渲染（见第五节）。豁免项 G-2/G-3/G-4 未碰热区（任务书明令）。**新增溢出 0**。
4. **reduced-motion**：本轮新增为纯静态 CSS 织纹，零动画、零时间源、无新增 `@keyframes` / `animation` /
   `transition`；tester「装饰全静态（禁时间源）」钉子契约同批复跑全绿。✅
5. **kit 纪律**：`git log --diff-filter=M origin/game-1.3..HEAD -- src/art/kit/ src/ui/motion.ts src/art/runner/`
   ——**0 条**，全部历史仍只有三条 owner 创建提交（`9c345d4` / `dbb8bd4` / `6dea9e6`），无人改过别人的 kit；
   本轮 C 档对 kit 零触碰（连 import 都未新增）。✅

---

## 四、kit 纪律检查结论

**通过**。`src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` 自基线 `origin/game-1.3` 以来无任何修改型
提交（`--diff-filter=M` 空），仅三条 1.3 第 1 步 owner 创建 commit；本轮修复未新增、未修改任何 kit 文件，
未复制粘贴 kit 代码（R2-2 织纹色为 `.hc-table` 本款木沿色派生，非 kit 供给面）。

---

## 五、合并态复验（由 C 档执行）

1. **git**：`git fetch origin game-1.3-window1` → rebase（origin 无新提交，rebase no-op）→ 确认三轮六份
   报告（round1 三份 + round2 tester / learner + 本报告）、`docs/plan-1.3-*.md`、`.cursor/skills/**`、
   十二格产物全部在位，他人文件零改动。推送方式：普通 push，无 force，未动 `main` / `game-1.2` / `game-1.2-kk`，
   未用 `gh` 开 PR。
2. **进场水位复核**：本轮进场先全量复跑一次——**726 文件 / 15577 例全绿**，与 learner 交卷水位
   （代码 SHA `19710a8`，tip `372e7a7`）逐字持平，无人动过用例。
3. **全量验证（合并态，代码 = `8ca41a8`）**：
   - `npx vitest run`：**727 文件 / 15579 例全绿**（进场 726/15577 → +1 文件（`window1-visual-r2-fix.test.ts`）
     / +2 例，**只增不减** ✅）；
   - `npx tsc --noEmit`：**0 错误** ✅；
   - `npm run build`（tsc + vite build + PWA）：**全绿** ✅，主包 `dist/assets/index-*.js` 110.70 kB
     （gzip 38.67 kB），hero-cards 分包正常产出，PWA precache **188 项 / 5291.55 KiB**，
     `sw.js` / `manifest.webmanifest` 正常生成；
   - `npx vite preview` 冒烟：`GET /` 200；Chrome headless（系统 Chrome，`--headless=new`，360×640）
     实开 `#/game/hero-cards`——关卡壳正常渲染（`l99-node` ×91、`开始冒险` 在位），页面级 JS 错误
     （Uncaught / TypeError / SyntaxError）**0 条**，且运行时页面注入的样式中
     `repeating-linear-gradient(45deg,rgba(170,120,80,.04)…)` 织纹层**确认在位**（产物级验证，非只看源码）。
4. **合并态代码 SHA：`8ca41a8`**（本轮唯一代码 commit；其后仅本报告与收尾登记 docs-only commit，
   全绿结论对推送 tip 同样成立，最终 tip 见收尾复验登记）。

---

## 六、窗口 1 · 第 2 轮视觉验收结论

- **阻断级：0 残留**（本轮输入 0 条；自查商标非测试源码 0 命中、360/320 新增溢出 0、裸 emoji 主角 0，当轮保持清零 ✅）；
- **严重级：0 残留**（本轮输入 0 条，自查亦未发现）；
- **一般级**：输入 1 条（R2-1 共享壳星级，跨窗禁改文件）——按纪律不抢，写入遗留清单转交壳层 owner / 统筹轮；
- **建议级**：输入 2 条——R2-2 已落地（本轮唯一动手项，commit `8ca41a8`）；R2-3 零代码，本报告全程用真名 `.mj-board` 完成处置；
- **测试水位**：727 文件 / 15579 例全绿，较 learner 交卷 +1 文件 / +2 例，只增不减；
- 遗留清单 5 项（R2-1、R2-3、L-3、L-4、L-5）逐条写明理由与去向，交第 3 轮。

**结论：窗口 1 第 2 轮视觉验收通过（阻断 0 / 严重 0 / 一般 1 条按纪律移交 / 建议全部处置完毕）。**

本轮实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
