# 1.3 窗口 1 · 本窗三轮验收 · 第 2 轮 · 监督修复员报告

> 角色：窗口 1 第 2 轮监督修复员（C 档）。落地 A 档（r2 tester）问题清单与 B 档（r2 learner）建议清单的
> 可动手项，并执行合并态复验。只动绘制层（绘制函数 / CSS / 视觉常量），判定、数值、关卡数据、存档 key、
> 玩法测试断言一律不碰。
> 输入：`docs/qa/1.3-window1-round2-tester.md`（阻断 0 / 严重 0 / 一般 R2-1 / 建议 R2-2、R2-3；P1–P10 回归 12/12）、
> `docs/qa/1.3-window1-round2-learner.md`（11 格达标 / 1 格待改 4A；C 档清单第三节）。
> 依据：`docs/plan-1.3-visual-bible.md`（视觉宪法）、`docs/qa/1.3-window1-plan.md`（十二格登记）、
> `docs/qa/1.3-window1-round1-fixer.md`（r1 修复与遗留基线）。

## 0. 修复计划（开工承诺）

修复顺序铁律：阻断（本轮输入 0 条，自查复核）→ 严重（0 条，自查复核）→ 一般 → 建议。

1. **本轮唯一动手项 R2-2（= L-2，建议级）**：`hero-cards/index.ts` `.hc-table` 叠斜织纹。
   按 learner 第三节第 1 条规格执行：在现有 `linear-gradient(180deg,#FBEBD8,#F5DCC2)` 之前
   （多重背景第一层）加 `repeating-linear-gradient(45deg,rgba(170,120,80,.04) 0 6px,transparent 6px 12px)`——
   与 `.mj-board` 同 45° 角、同 6px/12px 节距；织纹色不用白纹（浅暖米底上 3% 白纹不可见），
   改用从木沿色 `#EBD2B4` 向深派生的暖褐 `rgba(170,120,80,.04)`，alpha 恰 4% 顶格不越线。
   一行 CSS + 契约测试 ≥1 例（新文件 `src/games/window1-visual-r2-fix.test.ts`，仿
   `window1-visual-r1-fix.test.ts` 的源码级钉子写法：织纹存在 / alpha ≤4% / 层序在 linear-gradient 之前）。
   红线：只动 `.hc-table` 一条规则；`.hc-card` / `cardart.ts` / 判定零触碰。
2. **本轮不修、写入遗留**：R2-1（= L-1，`level99.ts` 星级 10–12px，共享壳 owner，本窗禁改此文件）、
   R2-3（文档笔误零代码，本报告全程用真名 `.mj-board`）、L-3（kit `decor.ts`，1A owner，不抢 kit）、
   L-4（`bomb-buddies` 他窗不改）、L-5（文案层 emoji 观察项不抬级）。
3. **自查底线（第四节，收缩到本窗）**：商标 grep（九款 + kit + motion + runner）、裸 emoji 主角
   `fillText` 直出扫描、360/320 溢出面核查（本轮只加静态织纹应零布局影响）、reduced-motion
   （静态织纹无动画）、kit 纪律 `git log` 查（无人改别人的 kit，自己也不改 kit）。
   若自查发现本窗新的阻断 / 严重（商标、360 新溢出、裸 emoji 主角），当轮清零。
4. 每修一处一个 commit（`fix(visual-r2): <款> <问题>`）；每处修复配套测试，用例只增不减
   （进场水位 726 文件 / 15577 例，learner 代码 SHA `19710a8`，tip `372e7a7`）。
5. 收尾合并态复验：`git fetch origin game-1.3-window1` → rebase（保留他人文件）→ `npx vitest run` →
   `npx tsc --noEmit` → `npm run build` → preview 冒烟 → 合并态 SHA、用例总数、构建体积写进本报告，
   给「第 2 轮视觉验收结论」。普通 push，禁 force，不动 `main` / `game-1.2` / `game-1.2-kk`，不用 `gh` 开 PR。

（修复清单、遗留清单、自查结论、合并态复验数据见后续章节，随修复逐条回填。）
