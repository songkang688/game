# 1.3 窗口 6 · 视觉升级 backlog（三轮验收收官登记）

由第 3 轮终验修复员（C 档）成文。收录本窗口三轮验收结束后仍登记在案的一般 / 建议级事项与移交统筹事项。**全部条目均不属六大专项判据、不构成放行条件**（A 档终验报告 §六放行附注同判）。本窗口范围：`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`。

## 条目（共 3 条）

### BL-W6-01 · badge.ts 底圆 2 停渐变 + 冻结老 kit 头注补描边分级约定（待统筹解冻决策）

- **来源轮次**：第 1 轮 A 档 W6R1-11（建议级）+ W6R1-13 残余（第 2 轮 C 档修复 #9 部分清偿）；第 2 轮 C 档移交、第 3 轮 A 档终验裁决「合理降级（移交统筹解冻决策）」。
- **位置**：`src/art/kit/badge.ts` 底圆填充（现为平涂）；同批冻结老 kit（`badge.ts` / `hamsterSvg.ts` / `moleSvg.ts` 等）文件头注。
- **期望**：badge 底圆改 2 停径向渐变（左上光源），使 brave-path 花徽 vs 星徽 16px 灰度差从 1.6% 抬到 ≥3% 家规线；解冻同批为老 kit 头注补「按渲染尺寸取档」的描边宽度分级约定注释（约定文本已在 `src/art/kit/hamsterAccents.ts` 头注立约）。
- **估算改动面**：`badge.ts` 单文件 1 处填充函数 + `badge.test.ts` 快照更新；badge 族被多窗口多款引用，需全库回归各款 16px 灰度用例。头注部分为纯注释，每文件约 5 行，零运行时风险。**前置条件：统筹解冻 badge.ts 等冻结 kit**（叠加 kit 方案对「改底色」需求叠不上去——叠加层画在底圆之上会盖掉徽记本体，第 2 轮 C 档 §2 已论证）。兜底在位：颜色 + 图形双通道可分，专项③按整体剪影判仍达标。

### BL-W6-02 · l99 共享壳层触区 / 字号统一抬（待统筹跨窗口派单）

- **来源轮次**：第 1 轮 A 档 W6R1-09/10 残余；第 1 轮 C 档、第 2 轮 B 档 §4-7、第 3 轮 A 档终验三方同判「移交统筹跨窗口派单」。
- **位置**：`src/ui/` 共享壳层（`l99-*` 类名族：战役选关 / 共享结算屏等）。
- **期望**：共享壳层按钮触区 min-height ≥40px / min-width ≥44px、正文 ≥14px，与本窗口 kit `src/art/kit/uiTouch.ts`（`touchUpliftCss` / `bodyFontUpliftCss`）同规格。
- **估算改动面**：`src/ui/` 内 1–2 个文件的 CSS 常量；但共享壳层横跨全库 76 款，单窗口改会造成跨窗口不一致，须统筹统一派单 + 全库回归。本窗口 9 款**自有**壳层已全部达标（第 2 轮修复 #1，`window6.r2.fixer.uplift.test.ts` 12 条持续守护），不受此条影响。

### BL-W6-03 · W6R1-08 bubble-pop 360px 触区终验结案记录（交统筹归档，无需改动）

- **来源轮次**：第 1 轮 A 档 W6R1-08；第 2 轮 C 档书面理由；第 3 轮 A 档终验裁决「**合理降级（现方案即最优，毋需再改）**」，结案。
- **位置**：`src/games/bubble-pop` 泡泡盘 `.bp-cell`。
- **期望**：无改动期望。结案依据：40px 触区需 8×40+7×4=348px 盘宽，360 屏扣池壁页边后物理上限约 303px，数学上放不下；六大专项⑤判据（无横滚 / 无重叠 / 无截断）实测达标，8 列全可见。宽屏 `.bp-cell min-width:36px` 热区「一个像素不动」断言持续在跑（`visual.fixer.r1/r2.test.ts`）。
- **估算改动面**：0（登记仅为结案归档，供统筹核销 W6R1-08 编号）。

## 统计

- 条目总数：**3**（移交统筹决策 2 条：BL-W6-01 / BL-W6-02；结案归档 1 条：BL-W6-03）。
- 阻断 / 严重级遗留：**0**。本窗口三轮全部阻断 / 严重 / 一般级问题已闭环（第 1 轮 13 条 + 第 2 轮 1 条：已修复或合理降级，A 档终验对账「仍未修 0」）。
- 三条均有兜底或已结案，不阻塞窗口 6 视觉升级放行。
