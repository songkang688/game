# 1.3 窗口 7 · 第 3 轮（终验）视觉验收 · 监督修复员报告（C 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 3 轮终验修复员（C 档）。只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 新增或本窗归属文件），不碰判定、数值、关卡数据、存档 key、玩法测试断言；他人报告只读不改。
> 输入：A 档终验 `docs/qa/1.3-window7-round3-tester.md`（已读：54/54 达标、新问题 0、结论「放行」）；B 档 `docs/qa/1.3-window7-round3-learner.md` 与风格基线 `docs/qa/1.3-window7-style-baseline.md`（开工时尚未产出，先按 A 档 + 第 2 轮遗留动手，B 出稿后跟进）；第 2 轮遗留清单 `docs/qa/1.3-window7-round2-fixer.md`。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 〇、清零计划（开工首 commit）

修复顺序铁律逐档核对：

1. **A 档终验总表不达标格**：0 格（54/54 全达标）——无此档工作量；
2. **第 2 轮遗留阻断 / 严重**：0 条（A 档对账「仍未修→升级终验阻断 0 条」）——无此档工作量；
3. **B 档点名仍违和款**：B 档本轮未出稿，开工时无点名；收尾前 fetch 复查，出稿则跟进；
4. **一般 / 建议级遗留（能修则修）**：逐条评估第 2 轮遗留 8 项合理降级——
   - **可修 1 项**：遗留 b（fruit-slice `index.ts:2467` 章节卡 blurb `Math.min(12,…)` 低封顶）。A 档已在 `qaW7r3.test.ts` 内留书面通道（「清掉后把这条断言改成 0」），修法=提字号 ≥14px + 配套测宽省略号排版（R2 降级理由正是缺配套排版），随修收紧 A 档预授权的唯一性断言 1→0；
   - **不可修 7 项**：各有明确不可修理由（不代写他人 plan / node 无浏览器 / pre-1.3 大盘等专项 / 收编改渲染色等专项 / 他人规格断言需协调 / 可选增强不许终验轮引入新风格），全部登记进 `docs/qa/1.3-window7-backlog.md`，每条带来源轮次 / 位置 / 期望 / 估算改动面。

每修一处一个 commit（`fix(visual-r3): <款> <问题>`），配套用例只增不减。不为「有活干」去改已达标绘制：除遗留 b 外九款绘制零触碰。

kit 终态整理预检：C 档三轮新增文件为 `art/kit/pattern.ts`（R1）与各款 `qaW7r*Fix.test.ts`——`pattern.ts` 被 puzzle-tiles 两处 import 在用，守门用例被 qaW7r3 存活性断言钉死，**无死文件可删，kit 目录本轮预计 0 提交**。

收尾流程：三人 push 后 fetch → rebase → `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟（`/`、`/sw.js`、`/manifest.webmanifest` 200）→ 记录终态 SHA / 用例曲线（对比 A 档基线 742/15443 只增不减）/ 体积对比 → 写收官声明 → 普通 push `origin game-1.3-window7`。禁止 force、不动 `main` / `game-1.2`、不开 PR、不合并到 `game-1.3`。

## 一、清零清单（逐条落地）

本轮 A 档终验总表 0 格不达标、新问题 0、遗留升级阻断 0——**当轮必修档为空**。按「一般 / 建议级能修则修」落地 1 条，配套用例只增不减（+1 文件 / +3 条）。

| # | 级别 | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | 一般（R2 登记遗留） | 第 2 轮遗留 b（`docs/qa/1.3-window7-round2-fixer.md` 新登记 b；A 档终验对账 2.2-b「合理降级（本轮加固）」并在 `qaW7r3.test.ts` 留书面通道「清掉后把这条断言改成 0」） | fruit-slice `index.ts` `drawThemes()` 章节卡 blurb（原 2467 行） | ① 字号 `Math.min(12, Math.round(ch*0.16))` 低封顶改 `Math.max(14, …)` 地板（≥14px，ch≤96 时实际 14–15px，随卡缩放通道保留）；② 补 R2 降级理由点名的「配套排版」：新增 `fitLine` 测宽省略号截断（`measureText` 装不下 `cw-20` 内宽即截短补「…」），blurb 与进度行两处走 fitLine——360px 双列卡（cw≈153px）原本 12px 整句就超宽 100+px，靠相邻卡片盖住假装截断，修后为有意截断可读；③ 按 A 档预授权把 `qaW7r3.test.ts` 低封顶唯一性断言 1→0 收紧（全窗清零，新增即红），文件头注释同步 | 新增 `fruit-slice/qaW7r3Fix.test.ts` 3 条（低封顶清场 + ≥14px 地板钉死 / blurb 与进度行走 fitLine 源码级 / fitLine 测宽+省略号语义）；`qaW7r3` 聚合断言收紧后照绿 | `d673e05` |

**红线自查**：只动 `drawThemes` 绘制函数与新增纯排版工具 `fitLine`，判定 / 数值 / 关卡数据 / 存档 key / 玩法断言 0 接触；无新风格引入（省略号截断是排版手段非风格资产）；热区 `themeCards` push 的 rect 一行未动；`fitLine` 稳态成本约 12 卡 × ≤20 次 `measureText`，与该场景既有 HUD 测宽同量级，无粒子 / 图层 / 计时器新增。窗外款同模式代码（garden-guard / ocean-munch 各 1 处）按「不改外窗游戏」纪律不碰。

**不为「有活干」改达标绘制**：除 F1 外九款绘制源码零触碰；kit 目录 0 提交。

## 二、降级申请记录与裁决

- 本轮 C 档**新降级申请 0 条**（唯一可修项 F1 已修，无「修不动」项）；
- 沿用 A 档终验对账既有裁决：合理降级 8 项（A 档报告第二节逐条载明 B/C 档书面理由），其中遗留 d（snake-snack 墨色点名）经 A 档复核纠偏成立**结案**，其余按去向入 backlog；
- B 档本轮整体感终评未点名新的仍违和款（其文案终查 2 处由 B 自修自测，见 `66be861`，属文案格非 C 档修复面）。

## 三、backlog 条目统计

`docs/qa/1.3-window7-backlog.md` 已成文（commit `06f412e`），共 **10 条**：验证类 1（destroy 真机进出 ×5）· 文档类 1（plan 缺档 ×4）· 口径类 1（装饰 emoji）· 专项待办 3（360px 阅读性 86 处 / shade 全库归一 / mcShadow 跨窗规格协调）· 可选增强 4（GOLD_EDGE token / 果渍留底 / 四角角饰 / 近景前景层）。**缺陷类（阻断 / 严重 / 一般未修）0 条。** 每条带来源轮次 / 位置 / 期望 / 估算改动面。已清项（遗留 b）与已结案项（R1 遗留 2、R2 遗留 d）注明不入账。

（终态合并数据与收官声明随收尾回填。）
