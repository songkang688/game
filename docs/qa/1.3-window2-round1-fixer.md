# 1.3 窗口 2 · 第 1 轮视觉验收 · 监督修复员报告

> 角色：窗口 2 第 1 轮视觉监督修复员（C 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。
> 范围：只修本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 红线：只动绘制层（绘制函数 / CSS / 视觉常量 / 各款本地 art.ts）。判定、数值、关卡、存档 key、玩法断言一律不碰。不复制窗口 1 的 `src/art/kit/`。用例只增不减。

## 修复计划（动手前登记）

输入：A 档报告 `docs/qa/1.3-window2-round1-tester.md`（SHA `40e9801`）已读；B 档报告开工时尚未落地，先按 A 档 + 自查修，B 档出来后 rebase 跟进。

按「阻断 → 严重 → 一般 → 建议」排序，每修一处一个 commit（`fix(visual-r1): <款> <问题>`），配套用例只增不减：

| 序 | 级别 | 来源 | 款 | 问题 | 预定修法 |
| --- | --- | --- | --- | --- | --- |
| 1 | 阻断 | A 5-1 | mine-garden | `.mn-mini{display:block}` 覆盖 `[hidden]`，空底板常驻 + 320px 越界 14px | 补 `.mn-mini[hidden]{display:none}`；`drawMini` 300px 基准改按容器宽收敛 |
| 2 | 阻断 | A 5-2 | dark-chess | `.dc-cell{min-height:44px}`×8 列最小内容宽 373px，360/320 裁切约 1.7–2.5 列 | 触区与列宽解耦：列改 `minmax(0,1fr)` + `.dc-cell{min-width:0}`，保留 44px 触控高度 |
| 3 | 严重 | A 3-1 | junqi-camp | 双方棋面/徽标只差颜色（面板灰度 Δ≈2–3.6），形状通道缺位 | art.ts 新增双方徽记（朵朵小花 / 星星五角星），棋面角落上形状角标；crestSVG/hqSVG 旗形与旗徽双方互异；同步翻转 A 档钉证据的断言为钉修后状态 |
| 4 | 一般 | A 2-1 | chess-garden | 12 张棋子主体平涂 0 渐变 | 参照 sudoku-petal 层叠法：主体路径叠「描边底层 + 半透明暗色 + 向顶部内缩亮层」两停明暗，免 defs/id |
| 5 | 一般 | A 2-2 | dark-chess | `backSVG`/`pieceFaceSVG` 的 `<defs>` 固定 id `dcWood`/`dcIvory`，32 格内联重复 id | id 加格位后缀：`backSVG` 用 seedIdx，`pieceFaceSVG` 增加可选格位参数（视图传入格号） |
| 6 | 一般 | A 5-3 | chess-garden | `.cg-coord` 9px / `.cg-piece-tag` 11px（<14px 红线） | 坐标提至 ≥10.5px 加粗、角标 ≥12px（40px 格内辅助标注按 A 档建议口径） |
| 7 | 一般 | A 5-4 | dot-maze / fruit-stack / pool-stars / junqi-camp / dark-chess / chess-garden | HUD chips / tip / note / 按钮 12–13.5px | 统一提至 ≥14px；`l99-star` 装饰字符豁免登记（首页共用件，非本窗 9 款目录） |
| 8 | 建议 | A 1-1 | dark-chess | `renderCell` 里 `textContent` 赋值紧跟 `innerHTML` 覆盖，emoji 死代码 | 核对测试契约后清理或改注释口径；有契约依赖则登记遗留 |
| 9 | 自查 | 规格四节 | 全 9 款 | 商标 grep、裸 emoji 主角残留 grep、360/320 复跑、reduced-motion 抽验、kit 纪律 `git log` 核查 | 逐项做，问题直接修 |

建议级 A 6-1（dot-maze 幽灵剪影差异化）按 A 档口径留第 2 轮跟踪，登记遗留清单。

收尾：三人 push 后 fetch → rebase → `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟，合并态 SHA、用例总数、构建体积写进本报告。

---

## 一、修复清单（15 commit，全部已落地）

B 档报告（`docs/qa/1.3-window2-round1-learner.md`）在本档开工后落地，其 TOP10 #1–#3 与 A 档立案重合（按 B 的绘制规格执行），#4–#10 已全部跟进。每条一 commit，SHA 为 rebase 后的最终值。

| # | 级别 | 来源 | 款 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 阻断 | A 5-1 / B #2 | mine-garden | `index.ts` `.mn-mini` 与 `drawMini` | 补 `.mn-mini[hidden]{display:none}` 恢复 UA 语义；画布加 `max-width:100%` 兜底；`drawMini` 300px 基准改按容器宽收敛（`clamp(96, 容器宽−4, 300)`） | `mine-garden/round1-fix.test.ts` 2 例（CSS 契约 + 容器 240px 时画布收敛） | `0fa590d` |
| 2 | 阻断 | A 5-2 / B #3 | dark-chess | `view.ts` `.dc-board`/`.dc-cell` | 触区与列宽解耦：列模板 `repeat(8,1fr)` → `repeat(8,minmax(0,1fr))`，`.dc-cell` 加 `min-width:0`；44px 触控高度保留（格子整格可点，窄屏格宽 ≥35px） | `dark-chess/round1-fix.test.ts` 1 例；`smoke.test.ts` 既有断言更新为钉修后模板（未删用例） | `f0930a0` |
| 3 | 严重 | A 3-1 / B #1① | junqi-camp | `art.ts` + `view.ts` | 形状第二通道：新增 `sideMarkSVG`（朵朵=五瓣圆花 / 星星=五角尖星），翻开棋面左下角上 `jq-mark` 角标（牌背不带）；`crestSVG`/`hqSVG`/`hoistSVG` 旗形互异（三角尖旗+花徽 vs 燕尾旗+星徽）；A 档钉证据的「去色后逐字节相同」断言翻转为钉修后互异 | `junqi-camp/round1-fix.test.ts` 4 例 + `round1-visual-scan.test.ts` 断言翻转 | `9811355` |
| 4 | 严重跟进 | B #1② | junqi-camp | `view.ts` 面板渐变 | 星星面板底渐变压暗一档，双方 16px 灰度差从 Δ≈2–3.6 拉开到 ≥20/255（配合形状角标双通道） | `round1-fix.test.ts` 灰度差计算用例 1 例 | `147f48f` |
| 5 | 一般 | A 2-1 / B #4 | chess-garden | `art.ts` 六个 `*Inner()` | `duoTone` 层叠两停法：body path 原样描边 + 半透明暗纱层 + 向顶部内缩 92% 亮层（`rgba(255,255,255,.14)`，黑方 .10），光源左上约定不变，免 defs/id | `chess-garden/round1-fix.test.ts` 2 例（12 张全查 + 亮层锚点在顶） | `cf38adc` |
| 6 | 一般 | A 2-2 / B #5 | dark-chess | `art.ts` | `backSVG` 改三停层叠实心（零 defs/id，32 张除木纹相位外逐字节一致的既有契约保持）；`pieceFaceSVG` 渐变 id 拼格号（`dcIvory-<色>-<种>-<格>`），视图传入格位 | `dark-chess/round1-fix.test.ts` 2 例 | `8accc7d` |
| 7 | 一般 | A 5-3 + 5-4 | chess-garden | `src/styles.css` 末尾追加（cg- 前缀） | `.cg-coord` 9→10.5px 加粗、`.cg-piece-tag` 11→12px、`.cg-tool`/`.cg-seat`/`.cg-tip` 13–13.5→14px | `chess-garden/round1-fix.test.ts` 2 例 | `b451f0a` |
| 8 | 一般 | A 5-4 / B #7 | dot-maze | `index.ts` | `dmz-chip`/`dmz-note`/`dmz-sub`/`dmz-tip` 提至 14px，窄屏媒体查询里的 13px 回降删除 | `dot-maze/round1-fix.test.ts` 1 例 | `452f490` |
| 9 | 一般 | A 5-4 / B #7 | fruit-stack | `index.ts` | `fs-chip`/`fs-btn`/`fs-bowlname`/`fs-next`/`fs-result-slot`/`fs-tip`/`fs-veil-s`/`fs-back`/`fs-open`/`fs-pick` 十类全部 ≥14px，窄屏不回降 | `fruit-stack/round1-fix.test.ts` 1 例 | `0f5a698` |
| 10 | 一般 | A 5-4 / B #7 | pool-stars | `index.ts` + `view.ts` | `ps-chip`/`ps-power-tag`/`ps-power-val`/`ps-tip`/`ps-veil-s`/`ps-pick`/`ps-over-s`/`ps-recap-t` 全部 ≥14px，窄屏不回降 | `pool-stars/round1-fix.test.ts` 2 例 | `3fc7a05` |
| 11 | 一般 | A 5-4 / B #7 | junqi-camp | `index.ts` + `view.ts` | `jq-sub`/`jq-tip`/`jq-pick`/`jq-chip`/`jq-note`/`jq-crest`/`jq-legend` 全部 ≥14px | `junqi-camp/round1-fix.test.ts` 1 例 | `f296ef2` |
| 12 | 一般 | A 5-4 / B #7 | dark-chess | `view.ts` | `dc-chip`/`dc-note` 提至 14px | `dark-chess/round1-fix.test.ts` 1 例 | `fd86713` |
| 13 | 建议 | A 6-1 / B #6 | dot-maze | `art.ts` `drawGhostFigure` | 裙边 4 齿改 3 齿（`tooth=r/1.5`），头顶补与玩家同款小呆毛（quadraticCurve 一笔、身体色加深 20% 描边），normal/fright 同步、eyes 态不带；三态互异既有契约通过 | `dot-maze/round1-fix.test.ts` 3 例 | `f44dbbb` |
| 14 | 建议 | B #9 | fruit-stack | `index.ts` 画盆函数 | 双盆口两侧内沿各刷 3px 座位色内衬（朵朵 `#a8306a` / 星星 `#28568f`，与座位条字色同源），纯视觉、碰撞盒不变 | `fruit-stack/round1-fix.test.ts` 1 例（录制 canvas 代理验证两色都真的刷上）；`domStub.ts` 补 `document.addEventListener` 空实现（engine/audio 模块顶层监听所需，测试桩只增能力） | `c7ac5c8` |
| 15 | 建议 | B #10 | merge-2048 | `index.ts` `.mg-wrap` | 背景栈追加两颗淡星点 `radial-gradient`（12% 18% 3px 淡金 `#FFD75E14`、88% 72% 2px 淡紫 `#D0A9F512`），列于不透明线性渐变之前保证可见；零 DOM 零动效，克制条款只加两颗 | `merge-2048/round1-fix.test.ts` 3 例（含 radial 总数恰为 6 的克制断言） | `c130a35` |

**统计：阻断 2/2 清零；严重 1/1 清零（含 B 档灰度跟进）；一般 4/4 清零（5-4 拆 6 款 6 commit）；建议/B 档新增 4/4 落地。** 新增契约用例 27 例（8 个 `round1-fix.test.ts`），另有 2 处既有断言更新为钉修后状态（`dark-chess/smoke.test.ts` 列模板、`junqi-camp/round1-visual-scan.test.ts` 证据翻转），用例只增不减。

## 二、自查修复结论（规格第四节）

| 项 | 方法 | 结论 |
| --- | --- | --- |
| 商标扫描 | 全库 grep 黑名单（4399/马里奥/皮卡丘/托马斯/Kitty/Battle City 等）+ 本窗 9 款造型目检 | 9 款零命中。命中处均为 QA 扫描工具自身的词表（`src/qa-window2/r3lib.ts`、`adventure-king/qaAudit.ts`，用途即黑名单）与 A/B 报告正文；`pool-stars/lose-line.test.ts` 的 `d324399` 为 git SHA 子串误报（A 档已排除）。本轮改造型处（幽灵三尖裙边、junqi 角标、棋子提亮层）均按 B 档第四节规避要点执行：幽灵未用官方四色、junqi 无肩章轮廓、chess-garden 无猫脸蝴蝶结 |
| 裸 emoji 主角残留 | grep `fillText("🐹"`/`fillText(car.emoji`/`P_EMOJI`/`fish.emoji` 等 | canvas fillText 直出主角 0 处。fruit-stack/pool-stars 的 `P_EMOJI`（🌸/⭐）仅入 DOM 文本座位标识，属 A 档专项①已核可的文字通道用法 |
| 360/320 溢出 | 修复后以契约用例钉住（mine-garden 容器收敛、dark-chess minmax 收缩、各款既有 360px 用例全量跑） | 两条阻断修后无新增溢出；15247 例全绿含全部 360px 契约 |
| reduced-motion | 逐款 grep reduced 分支 | 9/9 款均有处理（每款 5–15 处），本轮新增视觉全部为静饰或既有动效通道内改绘，未新增动效，无需新分支 |
| kit 纪律 | `ls src/art` + `git log` | 本分支不存在 `src/art/kit/`（目录未合入），无人拷入窗口 1 文件，无冲突改动。全部修复用各款本地 `art.ts`，纪律保持 |

## 三、遗留清单（交第 2 轮）

| # | 来源 | 款 | 问题 | 不修理由 |
| --- | --- | --- | --- | --- |
| 1 | A 1-1（建议） | dark-chess | `setCellContent` 里 `textContent` 赋值紧跟 `innerHTML` 覆盖，字符层不渲染 | won't-fix：`view.ts` 注释已明示该文本层是 1.2 既有测试的契约与 SVG 失效时的降级兜底，删除需减既有用例，违反「用例只增不减」。建议第 2 轮由测试员评估改 aria-label 口径后再动 |
| 2 | B 档第二节（未入 TOP10） | sudoku-petal | 行/列/宫刚好填满缺一条 120ms 轻反馈 | 需在玩法状态机上加「刚好完成」的判定钩子，超出「只动绘制层」红线；B 档亦未列入 TOP10。交第 2 轮与玩法侧协同评估 |

## 四、合并态复验（规格第六节）

三人报告均已在分支上（tester `40e9801`、learner `d470829` 起、本报告随最终 commit 落地），rebase 保留他人全部文件（含 `docs/plan-1.3-*.md` 与监督登记 `0b673da`）。

- **合并态 SHA：`c130a35`**（复验基准；本报告收尾 commit 在其上）
- `npm ci`：绿
- `npm test`：**721 个测试文件 / 15247 例全绿**（本轮三人净增：A 档 38 例 + B 档 4 例 + C 档 27 例，另 2 处断言口径更新）
- `npm run build`：绿；PWA precache **188 entries / 5212.27 KiB**；`dist/sw.js`、`workbox-9c191d2f.js` 生成
- `npx vite preview` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**

## 五、第 1 轮视觉验收结论

- **阻断清零：是**（A 5-1 mine-garden 小地图、A 5-2 dark-chess 八列裁切均修复并有契约用例钉住）；
- **严重残留：0**（A 3-1 junqi-camp 双方可分性以「形状角标 + 旗形互异 + 灰度差 ≥20」三管修复）；
- 一般 4 条全部修复；建议级除遗留清单 2 条外全部落地；
- 遗留清单 2 条（见第三节），均为低风险建议级，已登记理由交第 2 轮；
- kit 纪律：本分支无 kit，无违规；`src/styles.css` 仅末尾追加 cg- 前缀规则一处。
