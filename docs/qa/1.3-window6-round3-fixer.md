# 1.3 窗口 6 · C 档 · 第 3 轮视觉监督修复员报告（终验）

- 角色：窗口 6 视觉验收三人组 第 3 轮（终验）· C 监督修复员（清零收官 + 合并态守门 + 收官声明）
- 范围：`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`（共 9 款）
- 输入：本轮 A 档 `docs/qa/1.3-window6-round3-tester.md`（**放行**，SHA `79c02cc`：对账清零 已修复 1 + 复验 9 + 合理降级 3 / 仍未修 0；六大专项 54/54；终验新问题 0）+ 第 2 轮 C 档 `docs/qa/1.3-window6-round2-fixer.md`（遗留 3 项移交）+ 本轮 B 档 `docs/qa/1.3-window6-round3-learner.md` 与风格基线 `docs/qa/1.3-window6-style-baseline.md`（本档收尾时仍未 push，按步骤书预案执行：计划 commit + 自查 + 合并态复验 + backlog 成文，B 档到达后由其 rebase 于本终态之上，其角色产出为报告与只增用例，不影响本放行判定）
- 状态：**收官完成。本轮零绘制修复（依据 A 档放行 + 逐项自查复核）；backlog 成文 3 条；合并态 752 文件 / 15597 用例全绿；构建 0 增量；冒烟全绿。窗口 6 视觉升级收官声明见 §六。**

## 〇、清零计划（动手前提交，commit `03bc8a7`）

修复顺序铁律：A 档终验总表不达标格最优先 → 第 2 轮遗留阻断/严重 → B 档点名仍违和款 → 一般 → 建议。对照输入逐档核对：① A 档终验总表 54/54 全达标，不达标格 0；② A 档终验新发现 0 条，「当轮清零」条款无需动作；③ 第 2 轮遗留 3 项 A 档已终验裁决合理降级；④ B 档未到，到达后跟进。依据以上对账，本轮预计零绘制修复（A 档总表已全达标且新问题 0 时，不为交差硬改已进好的画面——步骤书原文），动作收敛为自查 / 合并态流程 / backlog 成文 / 收官声明。

红线自检（三轮一致，最后重申）：只动绘制层（本轮实际零改动）；判定、数值、关卡数据、存档 key、玩法测试断言一律不碰；用例只增不减、skip 一律恢复；kit 只增不改（badge.ts 冻结）；不碰 parentAuth / parentGate；不引入 three.js；别人的报告只读不改；禁 force、不改 main、不推 game-1.3、不用 gh 开 PR。

## 一、清零清单（来源 / 位置 / 修法 / 配套用例 / commit SHA）

**本轮零绘制修复，依据 A 档放行。**逐项核对过程与证据：

| # | 来源 | 核对结论 |
| --- | --- | --- |
| 1 | A 档终验总表（最优先） | 9 款 × 6 项 = **54/54 全达标，不达标格 0**（A 档 §三），无可修项 |
| 2 | A 档终验新发现（当轮清零条款） | **0 条**（阻断 0 · 严重 0 · 一般 0 · 建议 0，A 档 §四），无清零动作 |
| 3 | 第 2 轮遗留阻断 / 严重 | 第 2 轮输入即无阻断 / 严重；遗留 3 项均为登记级且 A 档终验已裁决合理降级（见 §二），不升级 |
| 4 | B 档点名仍违和款 | 第 2 轮 B 档点名 5 款已全部在第 2 轮处理（r2 fixer §三）；本轮 B 档报告未到，无新增点名 |
| 5 | 一般 / 建议级 | 存量一般 / 建议级全部闭环（A 档对账「仍未修 0」）；不可修项 3 条登记 backlog（见 §三） |

自查复核（本档独立执行，不只转录 A 档）：

- **skip 清零**：全库 `it/test/describe` 的 `.skip/.only/.todo` grep **0 命中**；
- **机器化防线在位**：7 个窗口级扫描文件（`window6.r1.fixer.scan` / `r1.learner.copy` / `r2.qa` / `r2.fixer.uplift` / `r2.learner.copy` / `r3.qa` + `lighting.qa1`）+ 9 款 `visual.qa1.test.ts` + 13 个逐款 fixer 钉子文件（7 r1 + 6 r2）全部在位，随全量跑全绿；
- **kit 终态整理**：本窗口三轮 fixer 新增 kit 共 3 支（`moleAccents.ts` r1 / `uiTouch.ts` r2 / `hamsterAccents.ts` r2），逐一引用核查——均被各款 `index.ts` / `visual.ts` 活代码引用（uiTouch 9 款接线、hamsterAccents 入 box-hamster/visual、moleAccents 入 mole-pop/visual），**无死文件，零删除**；badge.ts 等冻结件未动，公共导出未动；
- **代码态核实**：自 A 档钉子 commit `d84890d` 起至本终态，`git diff --stat -- src/ package.json package-lock.json` 为**空**（其后全部为 docs 提交），本档全部测试 / 构建 / 冒烟审计的即终态代码。

## 二、降级申请记录（引 A 档已接受的 3 项）

本轮无新增降级申请。存量 3 项引 A 档终验裁决（`docs/qa/1.3-window6-round3-tester.md` §2.3），全部为**已接受的合理降级**，且均不属六大专项判据：

| 项 | A 档终验裁决 | 兜底 |
| --- | --- | --- |
| W6R1-08 bubble-pop 360px 下 40px 触区 | **合理降级（现方案即最优，毋需再改），结案** | 40px 触区需 348px 盘宽 > 360 屏物理上限约 303px，数学上放不下；专项⑤判据实测达标；宽屏 36px 热区断言持续在跑 |
| W6R1-11 badge 底圆 2 停渐变（花徽 vs 星徽 16px 1.6%） | **合理降级（移交统筹解冻决策）** | badge.ts 冻结，叠加 kit 对改底色叠不上去；颜色 + 图形双通道可分，专项③整体剪影达标 |
| l99 共享壳层触区 / 字号（W6R1-09/10 残余） | **合理降级（移交统筹跨窗口派单）** | 共享壳层横跨 76 款，单窗口改致跨窗口不一致（三方同判）；本窗口 9 款自有壳层已全部达标且有用例守护 |

## 三、backlog 条目统计

`docs/qa/1.3-window6-backlog.md` 已成文（commit `329365e`），每条带来源轮次 / 位置 / 期望 / 估算改动面：

- **条目总数 3**：BL-W6-01 badge 底圆渐变 + 冻结老 kit 头注补注释（待统筹解冻决策）；BL-W6-02 l99 共享壳层触区 / 字号统一抬（待统筹跨窗口派单）；BL-W6-03 W6R1-08 终验结案归档（无改动期望，供统筹核销编号）；
- 阻断 / 严重级遗留 **0**；三条均有兜底或已结案，不阻塞放行。

## 四、终态合并数据（SHA / 用例曲线 / 体积对比）

合并态流程实录：`git fetch origin game-1.3-window6` → 本地建立于 `origin/game-1.3-window6`（`6b2f37a`，含 A 档终验报告与统筹计划更新；三份报告中 A 档 + 本档在位，B 档未到见头部说明）→ `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟 → 普通 push。

| 项 | 结果 |
| --- | --- |
| 终态 SHA | **`329365e`**（backlog 成文 commit；本报告 commit 紧随其后，除本报告外无任何改动。代码态与 A 档放行点 `d84890d`/`79c02cc` 逐字节一致——其后全部为 docs 提交） |
| `npm ci` | 通过 |
| `npm test` | **全绿，752 文件 / 15597 用例**（182.5s），0 fail / 0 skip |
| 用例三轮曲线（只增不减 ✅） | 第 1 轮基线 15379（723 文件）→ 第 1 轮合并态 `cbaff1e` 15499（742）→ 第 2 轮合并态 `5824758` 15580（751）→ **本终态 15597（752）**；三轮累计 +218 条 / +29 文件，无一轮回落 |
| `npm run build` | 全绿（tsc + vite 749ms）；PWA precache **190 项 / 5255.58 KiB** |
| 体积对比 | 第 1 轮基线 189 项 / 5217.14 KiB → 第 1 轮合并态 5232.41 → 第 2 轮合并态 5255.58 → **本终态 5255.58（较第 2 轮 0 增量）**；三轮总涨幅 **+38.44 KiB = +0.74%**，远低于 15% 说明线 |
| `vite preview` 冒烟 | 首页 `/` **200** · `/sw.js` **200** · `/manifest.webmanifest` **200** |
| 5 款 × 30 秒冒烟 | adventure-king / brick-break / mole-pop / balloon-pop / bubble-aim（与 A 档冒烟 5 款互补，合计双轮覆盖 9 款去重后 8 款）360×640@2x touch 持续点击 30 秒：pageerror / console.error 合计 **0**，末帧截图逐张肉眼查无视觉崩坏（mole-pop 天线星鼠 / 2.5D 洞穴、bubble-aim 渐变泡泡 + 色觉标记 + 彩虹薄膜球均健康渲染） |

（冒烟用 puppeteer-core 为 `--no-save` 临时装，与 A 档同法，package.json / package-lock.json 零改动，git status 已核实干净。）

## 五、红线复述

全库分级红线终态复核随行：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；商标三层词表（qa1 + r1 扩展 + r3 补充 29 组）扫描全绿（`window6.r1.fixer.scan.test.ts` + `window6.r3.qa.test.ts` 随全量跑通过）。本轮零绘制改动，无新增风险面。

## 六、窗口 6 视觉升级收官声明

**窗口 6（`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim` 共 9 款）1.3 视觉升级三轮验收收官。**

- **终态 SHA：`329365e`**（分支 `game-1.3-window6`；本报告 commit 紧随其后，除报告文档无任何改动）。
- **结论：放行。**A 档终验结论「放行」（54/54 达标、终验新问题 0、三轮问题闭环无一悬空）；本档合并态复验全绿背书：752 文件 / 15597 用例 0 fail 0 skip、build 0 增量（三轮总涨 +0.74%）、preview 冒烟三端点 200、5 款 30 秒零错误零崩坏；阻断 / 严重级终态 **0**。
- **遗留 backlog：3 条**（`docs/qa/1.3-window6-backlog.md`：移交统筹决策 2 + 结案归档 1），均非放行条件、均有兜底。
- 交付物即 `game-1.3-window6` 分支终态：不合并、不动 `game-1.3` 与 `main`、未 force、未用 gh 开 PR。
- 附注：本轮 B 档报告与风格基线截至本声明成文仍未 push（统筹计划中标记 RUNNING）；其到达后 rebase 于本终态之上，按三人组规则其产出为独立报告与只增用例，不改变本声明的放行判定与阻断 / 严重终态 0 的结论。
