# 1.3 窗口 6 · C 档 · 第 3 轮视觉监督修复员报告（终验）

- 角色：窗口 6 视觉验收三人组 第 3 轮（终验）· C 监督修复员（清零收官 + 合并态守门 + 收官声明）
- 范围：`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`（共 9 款）
- 输入：本轮 A 档 `docs/qa/1.3-window6-round3-tester.md`（放行，SHA `79c02cc`：对账清零 已修复 1 + 复验 9 + 合理降级 3 / 仍未修 0；六大专项 54/54；终验新问题 0）+ 第 2 轮 C 档 `docs/qa/1.3-window6-round2-fixer.md`（遗留 3 项移交）+ 本轮 B 档 `docs/qa/1.3-window6-round3-learner.md` 与风格基线 `docs/qa/1.3-window6-style-baseline.md`（开工时未到，先自查 + 合并态预跑 + backlog 草稿；B 档 push 后 rebase 跟进）
- 状态：**清零计划（动手前提交）。**

## 〇、清零计划（动手前提交）

修复顺序铁律：A 档终验总表不达标格最优先 → 第 2 轮遗留阻断/严重 → B 档点名仍违和款 → 一般 → 建议。对照输入逐档核对：

1. **A 档终验总表**：9 款 × 6 项 = 54/54 全达标，不达标格 **0**；
2. **A 档终验新发现**：阻断 0 · 严重 0 · 一般 0 · 建议 0，「终验新发现交本轮 C 档当轮清零」条款**本轮无需动作**；
3. **第 2 轮遗留 3 项**（W6R1-08 / W6R1-11 / l99 共享壳层）：A 档终验已逐项裁决为**合理降级**（书面理由成立，且均不属六大专项判据），不升级为阻断；
4. **B 档本轮报告**：未到。到达后 rebase，若点名仍违和款且属绘制层可修，则按铁律补修；否则登记 backlog。

依据以上对账，本轮预计**零绘制修复**（A 档总表全达标且新问题 0 时，不为交差硬改已进好的画面——步骤书原文）。本档动作收敛为：

- 自查：全库 skip/only/todo 清零复核、本窗口三轮 kit 新增文件引用核查（只删自己新增且无人引用的死文件——预检结果见 §2）、机器化扫描用例在位复核；
- 合并态流程：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟（首页 / `sw.js` / `manifest.webmanifest` 200 + 本窗口 5 款各 30 秒无视觉崩坏）；
- 成文 `docs/qa/1.3-window6-backlog.md`（每条带来源轮次 / 位置 / 期望 / 估算改动面）；
- 记录终态 SHA / 用例三轮曲线（只增不减）/ 构建体积对比（超 15% 逐项说明）；
- 收官声明。

红线自检（三轮一致，最后重申）：只动绘制层（本轮预计零改动）；判定、数值、关卡数据、存档 key、玩法测试断言一律不碰；用例只增不减、skip 一律恢复；kit 只增不改（badge.ts 冻结）；不碰 parentAuth / parentGate；不引入 three.js；别人的报告只读不改；禁 force、不改 main、不推 game-1.3、不用 gh 开 PR。

（以下各节随执行补齐。）
