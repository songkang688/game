# 1.3 窗口 7 · 第 2 轮视觉验收 · 监督修复员报告（C 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 2 轮监督修复员（C 档）。只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 新增或本窗归属文件），不碰判定、数值、关卡数据、存档 key、玩法测试断言；他人报告只读不改。
> 输入：A 档 `docs/qa/1.3-window7-round2-tester.md`（已读）；B 档 `docs/qa/1.3-window7-round2-learner.md`（开工时尚未产出，先按 A 档新问题动手，B 出稿后跟进）；第 1 轮遗留清单 `docs/qa/1.3-window7-round1-fixer.md`。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 〇、修复计划（开工首 commit）

修复顺序铁律：第 1 轮「修复不彻底 / 修出回归」最优先（**A 档本轮判定 0 条**，无此档）→ 本轮阻断（**0 条**）→ 严重（**0 条**）→ B 档一致性点名（未出稿，出稿后跟进）→ 一般（N-1 / N-2 + A-13 补录清理）→ 建议（N-4）。每修一处一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减。

| # | 级别 | 来源 | 款 / 位置 | 计划修法 |
| --- | --- | --- | --- | --- |
| F1 | 一般 | A 档 N-1 | landlord-cards `visual.ts:292` `.ldv-kribbon` 9px 缎带 | 文字改图形徽记（大王=金花徽 / 小王=银星徽，沿用立绘「花冠 vs 星冠」身份语言），消灭 1.3 新增唯一 <14px 样式；窄牌砍缎带的降级逻辑原样保留 |
| F2 | 一般 | A 档 N-2 | poop-hero `index.ts:1756/1760` 开场横幅 12–13px | 照抄同款门帘计数 `Math.max(14, …)` 写法，两处提到 ≥14px，缩放通道保留 |
| F3 | 登记清理 | A 档 N-5（= R1 A-13 补录） | fruit-slice `index.ts:2611` 菜单条件行 13px | `13px → 14px`，顺手全量扫描钉死画布字面量字号 ≥14px |
| F4 | 建议 | A 档 N-4 | `poop-hero/visual.ts:19` 与 `memory-cards/visual.ts:53` 的同名 `shade` | 混色引擎收敛到 kit 单源（`art/kit/fruit.shade` import 只读），两款保持各自公开量纲（poop-hero 百分比 / memory 小数）与大写输出为薄适配层——行为逐位等价（已验算 memory 钉死的三个精确色值与 poop-hero ±100 端点），A 档 qaW7r2 三量纲行为断言不动照绿；**kit `fruit.ts`（20-A 归属）一字不碰** |

不修待登记（写入遗留清单）：plan 缺档 ×4（不代写）、N-3 样式表 84 处 1.1/1.2 遗留（等「360px 阅读性」专项）、自查新发现 fruit-slice `index.ts:2467` 章节卡 blurb `Math.min(12,…)` 封顶（1.2 遗留，提字号有 360px 溢出风险需配套排版，降级登记）、fruit-slice `index.ts:1469` 函数内局部 `shade`（20-B 归属格代码，非 N-4 点名面，登记）。

性能回压：A 档九款深度走查性能列 9/9 全 ✅，本轮无粒子高峰点名款，不做减法（避免无据裁剪）。

## 一、修复清单（逐条落地）

每条一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减，全部收在各款 `qaW7r2Fix.test.ts`（3 个新文件）+ 跨款 `src/games/qaW7r2Fix.test.ts`（1 个新文件），合计 **+4 文件 / +11 条**。

| # | 级别 | 来源 | 款 / 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | 一般 | A 档 N-1 | landlord-cards `visual.ts` `.ldv-kribbon` | 9px「大/小王」缎带小字改**图形徽记** `kribbonBadgeSvg`：大王=五瓣花徽（暖粉 `#E2648F`）、小王=五角星徽（冷蓝 `#5C79C4`），沿用立绘「花冠 vs 星冠」身份语言，形状+色相双通道；CSS 去掉全部字属性（`line-height:0`），徽记footprint（24×16）比原 9px 缎带（≈30×15.5）还小、零新溢出；窄牌 `EMBOSS_MIN_W` 砍缎带的降级逻辑原样；身份文字仍由 ≥14px 角标承担。LDV_CSS（1.3 新增样式块）<14px 清零 | `landlord-cards/qaW7r2Fix.test.ts` 3 条（LDV_CSS 全量字号扫描 / 徽记无 `<text>` 两形两色 / 宽窄两档降级） | `3a73660` |
| F2 | 一般 | A 档 N-2 | poop-hero `index.ts:1756/1760` | 开场横幅关卡提示（13px 基准）与卫生小知识（12px 基准）两处装 `Math.max(14,…)` 地板，写法照抄同款门帘计数（A-11 修法）；大屏缩放通道保留；横幅三行 14px 下 24 字 ≈336px<360 不溢出 | `poop-hero/qaW7r2Fix.test.ts` 2 条（两处点名钉死 + 画布字体全量扫描：字面量 / max(14,…) / 0.85 缩放下限三种写法一律 ≥14px） | `9d4ccff` |
| F3 | 登记清理 | R1 遗留 A-13（A 档 N-5 补录） | fruit-slice `index.ts:2611` | 菜单回合卡条件说明行 `13px → 14px`（1.2 遗留，本轮顺手清）；wrapText 行高 18px 容 14px 字 | `fruit-slice/qaW7r2Fix.test.ts` 2 条（13px 清场 + 画布字面量字号全量扫描 ≥14px） | `519bcb3` |
| F4 | 建议 | A 档 N-4 | `poop-hero/visual.ts:19` / `memory-cards/visual.ts:53` | 同名 `shade` 混色引擎收敛 **kit 单源**（`art/kit/fruit.shade`，import 只读，**kit 文件一字未动**）：两款改薄适配层——poop-hero 保百分比量纲（`pct/100` 换算）、memory-cards 保小数量纲，均保大写输出。行为逐位等价：kit 与 memory 的混色公式数学同构（线性插值向白/黑），旧钉值（`#C59C56`/`#626272`/`rgba` 原样返回/±100 端点/`PH_TOKENS` 派生色）全部不变；A 档 qaW7r2 三量纲行为断言原样照绿 | 跨款 `src/games/qaW7r2Fix.test.ts` 4 条（两款 × 采样网格逐位等价 / 旧钉值不变 / 源码级钉死 import kit 引擎、`parseInt(hex` 清零） | `92c0070` |

**修复统计（按来源 × 严重度）：A 档本轮新发现 3 条全处理（一般 2：N-1、N-2；建议 1：N-4）+ 第 1 轮遗留 1 条清掉（A-13/N-5，登记级）。阻断 0、严重 0（A 档本轮判定即为 0，无此档工作量）；「修复不彻底 / 修出回归」0 条（A 档对账 13/13 已修复，无返工项）。**

### 一之补、kit 收敛自查

- 自己 R1 新增的文件零重复轮子：`art/kit/pattern.ts` 首建时即 import kit `shade`；`poop-hero/trashArt.ts` 经 visual 适配层间接单源（F4 后传导到 kit）。无需新增 kit 文件；
- 本轮 kit 目录 **0 提交**（`git log -- src/art/kit/` 无新增），import 替换只动了自己修的两款（poop-hero / memory-cards），别人的 kit 文件未碰。

## 二、第 1 轮遗留清单处理结果（清 1 · 维持 4 · 新登记 3）

| # | 事项 | 本轮处理 |
| --- | --- | --- |
| R1-遗留 1 | plan 过程文档缺档 ×4（20-A/20-C/21-A/21-C） | **维持遗留**（C 档不代写他人 plan，理由与 R1 一致；A 档对账也判合理遗留） |
| R1-遗留 2 | puzzle-tiles blurb 文案跟进 | **移交本轮 B 档**（B 档计划第六节已接手，其文案 commit `45c5bb5` 已开始落地；以 B 档报告为准） |
| R1-遗留 3 | HUD/菜单文案级装饰 emoji | **维持遗留**（验收口径「装饰性可留」不变） |
| R1-遗留 4 | destroy 真机「进出 ×5」 | **维持遗留**(node 无浏览器；A 档本轮已做 12 commit 静态复审:新增计时器/监听器 0 处) |
| A-13（N-5 补录） | fruit-slice 菜单说明行 13px | **本轮清掉**（F3，`519bcb3`） |
| 新登记 a | N-3:九款样式表 <14px 84 处（全部 1.1/1.2 遗留） | 维持登记，等「360px 阅读性」专项统一排格（A 档口径） |
| 新登记 b（自查） | fruit-slice `index.ts:2467` 章节卡 blurb `Math.min(12,…)px` 封顶 | 1.2 遗留（R1/R2 A 档 canvas 扫描均未点名,模式是 `Math.min` 封顶非字面量）；直接提 14px 会让 blurb 在 360px 双列卡（cw≈153px）溢出,需配套排版（换行/截断）,**降级登记**交第 3 轮或 360px 专项 |
| 新登记 c（自查） | fruit-slice `index.ts:1469` 函数内局部 `shade`（另有窗外款多份同名） | 20-B 归属格代码,不在 N-4 点名面;本窗内收敛留给后续轮「shade 全库归一」时一并做,登记在案 |

## 三、一致性统一化改动说明

- **色板混色单源**（F4）：`shade` 三实现收敛为 kit 单一混色引擎 + 两个量纲适配层,「跨模块照抄参数产出纯黑/纯白」的踩坑面从三套公式压到一套;A 档 qaW7r2 的量纲语义断言 + 本轮等价网格断言双层钉死；
- **身份视觉语言统一**（F1）：大小王徽记沿用 kit 立绘既有的「花=朵朵 / 星=星星」符号系统与暖冷分色,不自创第二套记号；
- B 档横向一致性评审（其报告第三节）出稿后跟进——若点名款在本窗绘制层且当轮可修则修,否则登记（见第五节合并态复验时点的 B 档状态）。

## 四、性能回压前后对比

A 档九款深度走查性能列 **9/9 全 ✅**（星屑 3–4 颗自灭 / 残影硬顶 6+尾流 6 / RING_STARS=8 封顶 / 气泡 ≤6 等预算全部在案）,本轮无粒子高峰点名款。**本轮零粒子/图层改动**,四个修复全部是静态绘制字符串、字号常量与纯函数适配层,绘制调用数不增（F1 徽记 SVG 节点数 6–7 个,与原文字节点同量级;F4 是纯函数换实现）。代码预算口径:粒子池上限与 R1 合并态逐字一致（diff 可查）。

## 五、合并态复验数据

（三人 push 后执行并回填）
