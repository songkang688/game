# r39 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-177**、**N-178**、**N-179**。下一空号 **N-180**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r38 学习 PR 未合主干时，本轮仍从 N-177 起编；若主干已占用 N-177+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r38 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r39-learn-notes.md` §0。尤其 **不要**再开 `.dvs-pick`（N-94）、`.bl-btn`（N-145）、`.frc-open`（N-121）、`.xq-start`（N-95）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#111** / **#107**。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-177 · B · `.dvs-lessonbtn`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/duo-vs-star/index.ts` ≈L231；挂载 ≈L1657 |
| 现状 | 教案 `<button>`，`padding:11px 12px`，无 min-height |
| 不做 | 不改 `.dvs-go`（N-173）/ `.dvs-pick`（N-94）/ `.dvs-over button`（N-157） |
| 验收 | 教案行高 ≥44；点课仍进对应协作关 |

### N-178 · B · `.dvs-mode`

| 项 | 内容 |
| --- | --- |
| 文件 | 同文件 ≈L178；挂载 ≈L1279 |
| 现状 | 大厅模式格 `<button>`，`padding:13px 10px`，无 min-height |
| 不做 | 不并 N-149 `*-open` 胶囊 / `.sks-mode`；不改 `.dvs-go` 钉底 |
| 验收 | 模式卡高 ≥44；点卡仍进对应模式 |

### N-179 · A · `*-lessonbtn` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-lessonbtn` 可点块必须 `min-height`≥44 或 TOUCH 插值 |
| 现状红 | 仅 `.dvs-lessonbtn` |
| 不做 | 不扫 `.dvs-mode`（那是 N-178）；不替代 N-149/173 |

---

## 建议顺序

1. A 先合 **N-179**（或与 B 并行）。
2. B 修 **N-177**，再修 **N-178**（同一文件）。
3. 回归：主干 N-94/101、N-173 go、N-174/175 拔河、N-87 冲刺菜单、N-99/97/100。bowling `.bl-btn` 仍走 N-145。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-177/178 的点击矩形，不能只看 CSS 行。
