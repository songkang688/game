# 1.3 窗口 7 · 视觉升级 backlog（第 3 轮终验收官登记）

> 由第 3 轮 C 档终验修复员成文。收录窗口 7 三轮视觉验收后仍遗留、且经 A 档终验裁决为「合理降级」的一般 / 建议级事项。每条带来源轮次 / 位置 / 期望 / 估算改动面。这些条目**不构成本窗放行的前置**（A 档终验结论「放行不附带条件」），按各自去向在后续专项处理。
>
> 已在第 3 轮清掉、不入本账的：R2 遗留 b（fruit-slice 章节卡 blurb `Math.min(12,…)` 低封顶）——R3 C 档修复 `d673e05` 提 ≥14px 地板 + fitLine 测宽省略号排版，qaW7r3 聚合断言已收紧为全窗清零。
> 已结案、不入本账的：R1 遗留 2（puzzle-tiles blurb 文案，R2 B 档落地 + copyW7r2 钉死）；R2 遗留 d（snake-snack 墨色「孤例」点名，R3 A 档复核纠偏成立——实为物件本色，结案）。

| # | 事项 | 来源轮次 | 位置 | 期望 | 估算改动面 |
| --- | --- | --- | --- | --- | --- |
| 1 | plan 过程文档缺档 ×4（20-A / 20-C / 21-A / 21-C） | R1 遗留 1（R1 C 档登记，R2/R3 维持） | `docs/qa/`（四格 plan 文件缺失） | 由各归属格自行补档或流程上豁免历史缺档 | 纯文档 4 份；C 档不代写他人 plan，需各归属格执行 |
| 2 | HUD / 菜单文案级装饰 emoji（fruit-slice 17 处等） | R1 遗留 3（R1 A 档口径，R2/R3 维持） | fruit-slice `index.ts` 菜单标题 / 章节卡图标 / 地图标记 / HUD 横幅前缀（R3 A 档逐处定性清单） | 现验收口径「装饰性可留」判非缺陷；若日后口径收紧为「全禁 emoji 直出」，需换自绘图标 | 单款约 17 处 fillText 前缀改造 + kit 图标补位；玩法物件层已清零不涉 |
| 3 | destroy 生命周期真机「进出 ×5」验证 | R1 遗留 4（R1 A 档登记，R2/R3 维持） | 本窗九款 `index.ts` destroy 路径 | 真机浏览器环境反复进出各款 ≥5 次，观测计时器 / 监听器 / rAF 零泄漏 | 纯验证不改码；需带浏览器的 CI 或人工真机；三轮静态复审（R1 12 commit + R2 6 commit + R3 1 commit）新增泄漏面均为 0 |
| 4 | 九款样式表 <14px 小字约 86 处（全部 1.1 / 1.2 遗留） | R2 遗留 a（A 档 N-3，R3 宽口径复点 86 处） | 九款 CSS / 内联样式（landlord 31 · fishing-star 30 · poop-hero 16 · memory/puzzle 各 3 · snake 2 · fruit-catch 1，R3 A 档口径） | 「360px 阅读性」专项统一排格：功能小字提 ≥14px，装饰性微字逐处定性 | 约 86 处字号 + 相邻排版联动；跨九款 CSS，须专项统一口径避免逐款漂移 |
| 5 | fruit-slice `shadeColor` / 局部 `shade` 收编 kit 单源 | R2 遗留 c（B 档修订 #1 / A 档 N-4 附项） | `fruit-slice/visual.ts:174`、`index.ts`（局部 shade）；另有窗外款多份同名 | 「shade 全库归一」专项统一收编；±255 通道加法与 kit 比例混合数学不等价，收编=改实际渲染色，需带视觉回归对照 | 两函数替换 + 调用点色值回归验证；F6 量纲守门用例（qaW7r2Fix 2 条）已在跑防漂移 |
| 6 | memory-cards `mcShadow=rgba(60,50,90,.18)` 与全窗 `rgba(90,74,110,α)` 深影双支 | R2 遗留 e（B 档一致性 #2①） | `memory-cards` 视觉常量；被 `visual21.test.ts:135` 规格断言逐字钉死 | 与 21-C 规格归属格协调后一并改值 + 改断言，收敛单家族 | 1 个常量 + 1 条他人规格断言；跨窗协调成本大于改动本身 |
| 7 | kit `GOLD_EDGE` 金描边 token 化 | R2 遗留 f①（B 档第四节自标「下轮可选」） | `src/art/kit/`（新增 token）+ 各款金描边调用点 | 金描边色值收敛具名 token，防各款漂移 | kit +1 token + 多款替换；属新风格资产，终验轮不引入，留后续窗口 |
| 8 | fruit-slice 果渍留底效果 | R2 遗留 f②（B 档自标可选增强） | `fruit-slice` 切果命中反馈层 | 切开后砧板留短寿命果渍印,增强打击感 | 单款新增粒子/贴花通道，需配套粒子预算断言；可选增强非缺陷 |
| 9 | memory-cards 四角角饰 | R2 遗留 f③（B 档一致性 #2② 自标可选） | `memory-cards` 牌背绘制 | 牌背补四角角饰,进一步贴近全窗牌面语言 | 单款牌背绘制函数小改；可选增强非缺陷 |
| 10 | poop-hero 近景前景层 | R2 遗留 f④（B 档自标可选增强） | `poop-hero` 章节背景（现三档视差已达标） | 视差再加一档近景前景剪影,增强纵深 | 单款背景绘制 + 视差常量；2.5D 专项已达标,属锦上添花 |

**统计：共 10 条 —— 验证类 1（#3）· 文档类 1（#1）· 口径类 1（#2）· 专项待办 3（#4 360px 阅读性 / #5 shade 归一 / #6 跨窗规格协调）· 可选增强 4（#7–#10）。缺陷类（阻断 / 严重 / 一般未修）0 条。**
