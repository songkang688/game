# 1.3 窗口 1 · 本窗三轮验收 · 第 3 轮(收官)· 测试员报告

> 角色:窗口 1 第 3 轮视觉测试员(A 档,收官轮)。只写本报告与窗口 1 范围 `*.test.ts` 视觉相关新增用例,
> 不改绘制实现、不改玩法 / 数值 / 关卡代码。
> 范围:基建 `src/art/kit/`、`src/ui/motion.ts`、`src/art/runner/` + 九款
> `orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate` `hero-cards` `weiqi-garden` `flight-chess`。
> 依据:`docs/plan-1.3-visual-bible.md`(视觉宪法)、`docs/qa/1.3-window1-plan.md`(十二格登记)、
> round1 / round2 六份报告(只读)。
> 收官轮重点:**回归第 1、2 轮修复 + 猎新回归**;不重开玩法验收,不把已知跨窗遗留抬成新阻断。

## 0. 扫描计划(开工承诺,commit `c0509a5`)

1. 全量跑:`npm ci` → `npx vitest run`(全仓)→ `npx tsc --noEmit` → `npm run build` → preview 冒烟;
   对照第 2 轮 fixer 交卷水位 **727 文件 / 15579 例**(代码 SHA `8ca41a8`,tip `d062720`),只增不减。
2. 十二格盘点:plan 十二格 SHA 逐一验真;kit / motion / runner `git log --diff-filter=M` 独占纪律抽查。
3. 必读(只读不改):round1 / round2 六份报告 + 视觉宪法。
4. 收官回归:R2-2 独立取证(源码 + 运行时);r1 P1–P10 + clamp 抽查;豁免 G-2/G-3/G-4 维持;
   遗留 R2-1 / L-1 / L-3 / L-4 / L-5 登记不抬级。
5. 回归猎手:第 2 轮只改 hero-cards CSS——确认无新溢出 / 新裸 emoji 主角 / 新商标 / kit 越权 /
   reduced-motion 破坏;360/320 九款复扫。
6. 六大专项(收缩到本窗 12 格)+ 横向一致性(kit 光照 / reduced-motion / destroy×5 / 粒子上限代码抽查)。
7. 产出:本报告 + `src/games/window1-visual-r3.test.ts` 机器化钉子(只增不减)。

---

## 一、全量跑数据与十二格盘点

### 1.1 全量跑(进场基线,代码 = `8ca41a8` 同源的合并态)

| 项 | 结果 | 备注 |
| --- | --- | --- |
| `npm ci` | ✅ | 依赖装净(669 包) |
| `npx vitest run`(全仓,进场) | ✅ **727 文件 / 15579 例全绿** | 与 r2 fixer 交卷水位 727/15579 **逐字持平**,无人动过用例 |
| 本轮交卷复跑 | ✅ **728 文件 / 15591 例全绿**(见第十三节收尾登记) | +1 文件(`window1-visual-r3.test.ts`)/ +12 例,**只增不减** |
| `npx tsc --noEmit` | ✅ 0 错误 | |
| `npm run build`(tsc + vite build + PWA) | ✅ | 主包 `dist/assets/index-CL9tGIe2.js` 110.70 kB(gzip 38.67 kB),precache **188 项 / 5291.55 KiB**,与 r2 fixer 登记持平 |
| `npx vite preview` 冒烟 | ✅ | `GET /` 200;九款 `#/game/<id>` Chrome headless(CDP,Chrome 148)360×640 与 320×640 各一遍共 18 组,逐款实开进对局,主场景(canvas / SVG)全部渲染,**18 组 console error / 页面异常全 0** |

**代码零漂移证明**:`git diff --stat 8ca41a8..HEAD -- src/` 为**空**——r2 fixer 交卷后至本轮进场,
仓库仅有 docs-only 提交(`4143f4b` / `d062720` / `c18864d`),本窗 12 格与全库源码与 r2 fixer
验证过的状态**逐字节相同**。这是本轮回归取证的最强前提:r2 结论的检视对象未被任何人触碰。

### 1.2 十二格 plan ↔ 提交对账

`docs/qa/1.3-window1-plan.md` 登记的 12 格 SHA(`9c345d4` `dbb8bd4` `6dea9e6` `cb2c65a` `310dd1d`
`bb5e090` `58a2055` `3f03658` `a5b446c` `7a07ddb` `0b1dc6b` `8575add`)与三轮验收 SHA
(`040264a` `f278f62` `662e027` `3bc4df1` `d1ec97d` `aaa32ed` `19710a8` `b735db2` `8ca41a8`
`4143f4b` `d062720`)逐一 `git cat-file` 验真,**23 个 SHA 全部存在**。✅

### 1.3 kit / motion / runner 独占纪律

`git log --diff-filter=M origin/game-1.3..HEAD -- src/art/kit/ src/ui/motion.ts src/art/runner/`:
**0 条**;全部历史仍只有三条 owner 创建提交(`9c345d4` / `dbb8bd4` / `6dea9e6`)。
三轮验收下来无任何人修改过基建文件。✅ 另核 `src/art/kit/` 文件清单:palette / chars / props / fx /
testing / index 共 6 源码 + 6 测试,仍无 `decor.ts`(= L-3 现状,见遗留登记)。

---

## 二、收官回归表(本轮重点,逐条独立取证)

证据口径:**代码检视**(本轮重读现源码,标注文件+行)+ **运行时取证**(Chrome headless CDP 实开,
`getComputedStyle` / `getBoundingClientRect` / DOM 查询)+ **契约复跑**(r1/r2 四个钉子文件随全仓全绿)。
不抄 fixer 报告——R2-2 的运行时织纹、44px 四处矩形、40 地格 SVG 覆盖等均为本轮独立实测。

### 2.1 R2-2(第 2 轮唯一修复项)回归

| 取证面 | 结果 | 判定 |
| --- | --- | --- |
| 源码 | `hero-cards/index.ts:123` `.hc-table` `background` 第一层 = `repeating-linear-gradient(45deg,rgba(170,120,80,.04) 0 6px,transparent 6px 12px)`,第二层纸色 `linear-gradient(180deg,#FBEBD8,#F5DCC2)` 未动;**alpha 恰 4% ≤ 4% 红线**;与 `.mj-board`(`mahjong-bloom/index.ts:102`)同 45°/6px/12px 节距 | ✅ |
| 运行时 | CDP 360×640 实开 hero-cards 进对局,`getComputedStyle(.hc-table).backgroundImage` 实测返回 `repeating-linear-gradient(45deg, rgba(170, 120, 80, 0.04) 0px, … 6px, rgba(0,0,0,0) 6px, … 12px), linear-gradient(rgb(251,235,216), rgb(245,220,194))`——**织纹层在产物运行时确认在位且为第一层** | ✅ |
| 改动面复核 | `git diff 19710a8..8ca41a8`:代码 diff 仅 `hero-cards/index.ts`(+3/-1,织纹层与注释)+ 新契约文件;`.hc-card` / `cardart.ts` / 判定零触碰;卡面纸感 `linear-gradient(180deg,#fffdf8,#f6efe2)` 运行时同测在位 | ✅ |
| 契约 | `window1-visual-r2-fix.test.ts` 2 例随全仓复跑仍绿 | ✅ |

### 2.2 r1 P1–P10 + clamp 抽查(12/12 无回退)

因代码自 `3bc4df1`(r1 fixer)→ `8ca41a8`(r2 fixer)→ 本轮**除 hero-cards 织纹 4 行外零改动**
(1.1 节零漂移证明),r2 的 12/12 逐条取证仍完全有效;本轮按任务书点名面再独立抽查:

| 项 | 本轮独立取证 | 判定 |
| --- | --- | --- |
| P1 star-estate SVG 地格 | 运行时:360px 进对局后 `.se-tile` **40 格 / 40 格全部含 `.se-tile-icon svg`**、图标节点内 emoji 码位 **0**、40 格 aria-label 全部携带 emoji(语义保留);源码:`TILE_ICONS`(`art.ts:409`)图形体段 `<text>` 节点 0、`?` 字符 0;`se-tile-emoji` 全文件 0 命中 | ✅ |
| P4 = G-5 触区 44px | 运行时 `getBoundingClientRect` 实测:`.oa-open` 119×44 ×3、`.sr-open` 119×44 ×3、`.sr-skin` 64–117×44 ×10、`.bd-open` 119–148×44 ×3,**四处全部恰 44px 高** | ✅ |
| P5 combo-clash 土丘三阶 | 源码重读 `art.ts:296-315`:base `rgba(th.near,.9)` 半圆 + 底缘 `shade(.2)` 4px 暗带 + 顶部 `tint(.3)` 亮弧(28/40=0.7 倍半径,π→1.72π 偏左上光源)三笔齐 | ✅ |
| P6 snake-royale 远景色岛 | 源码:`ISLAND_CELL=900` / `ISLAND_PARALLAX=0.6` / `DECOR_PARALLAX=0.85` / `DECOR_MIN_SIZE=3.2`(`art.ts:116-124`)全在;视差常量仍恰两档 | ✅ |
| P8 `.mj-board` 织纹(真名) | 运行时:`getComputedStyle(.mj-board).backgroundImage` 实测第一层 `repeating-linear-gradient(45deg, rgba(255,255,255,0.03) …)`,叠于毛毡 radial 之上;**alpha 3% ≤ 4%**。全程使用真名 `.mj-board`(R2-3 口径) | ✅ |
| P9 block-drop 井壁浮雕 | 源码:`RELIEF_H=12`(`art.ts:251`)、`paintWallRelief`(`:258`)四主题在位,件宽收在壁内 | ✅ |
| P3 flight-chess 装饰 | 运行时:`.fc-decor` 在位、`aria-hidden="true"`、`pointer-events:none`,**草簇恰 4 + 云恰 2** | ✅ |
| P2 orb-arena 贴片层 | 源码:`DECOR_CELL=176` / `DECOR_PARALLAX=0.3`(`art.ts:148-150`)、贴片预算 240 件/帧在位 | ✅ |
| P10 weiqi-garden 测试组织 | `art.test.ts` 在位且随全仓全绿;`index.test.ts` 旧 describe 未迁走 | ✅ |
| 自查 clamp | `.se-tile-icon{font-size:clamp(15px,2.6vw,17px)}`(`index.ts:138`)下限 15px ≥ 13px | ✅ |

### 2.3 豁免复核(G-2 / G-3 / G-4,只核依据、不当新阻断)

| 编号 | 本轮运行时复核(360/320 双视口) | 结论 |
| --- | --- | --- |
| G-2 `mj-tile` 30×42(≤480px) | 实测仍 30×42;`mj-cur` 选中态二段确认在位(实测选中牌 class 含 `mj-cur`)、`mj-hand` overflow 兜底在位、零溢出 | 依据成立,维持豁免 |
| G-3 `se-tile` 23×23 / 320px 下 20×20 | 实测同前;`se-deed` 信息行 267×36(320px 下 236×36)可达同一信息;走子仍由骰驱动 | 依据成立,维持豁免 |
| G-4 `fc-token` 19×19 / 320px 下 17×17 | 实测同前;`.fc-pick` 主入口规则在位(`index.ts` `min-width:66px;min-height:44px`) | 依据成立,维持豁免 |

### 2.4 遗留登记(复登记不抬级,本窗不抢共享壳)

| 项 | 本轮实测现状 | 处置 |
| --- | --- | --- |
| R2-1(= L-1 = r1 G-1)`level99.ts` 星级 | 仍在:`level99.ts:512` `.l99-node-stars{font-size:11px}`、`:561` @媒体降 10px;本轮 18 组实测唯一 <14px 命中 = `span.l99-star` 计算样式 10–12px,九款关卡页同源受累 | **复登记即可,本窗不抢共享壳**(任务书明令);修法 r1 learner P7 已留档,交壳层 owner / 统筹轮;严重度维持「一般」 |
| L-3 kit `decor.ts` 供给缺口 | `src/art/kit/` 仍无 `decor.ts`(本轮 ls 复核 6 源码 + 6 测试);本窗九款需求已按「各游戏内自绘」满足,无受害面 | 登记现状,维持 kit owner 待办,不抬级 |
| L-4 `bomb-buddies` `P_EMOJI` | 仍在(`index.ts:102` `["🌸","⭐"]` 座位标,`:423/:562` 引用) | 他窗游戏,本窗零触碰,登记转交,不抬级 |
| L-5 文案层 emoji | 仍在:combo-clash 选人卡 `<em>${c.emoji}</em>`(`index.ts:997`)、star-estate 地契列表行首 `${sel.emoji}`(`index.ts:730`)等 | 维持「文案层可留」口径,登记现状,不抬级 |
| R2-3 `.mj-table` 笔误 | 零代码项;本报告全程使用真名 `.mj-board` | 处置完毕(沿 r2 口径) |

### 2.5 回归猎手结论(第 2 轮只改了 hero-cards CSS)

| 猎查面 | 方法与结果 | 结论 |
| --- | --- | --- |
| 新溢出 | 18 组(九款 × 360/320,关卡页 + 进对局)文档级 `scrollWidth − innerWidth` **全 0** | 0 条 |
| 新裸 emoji 主角 | 12 格非测试源码逐行扫描:`fillText(` 行携带 emoji **0**;各格 emoji 字符数(41/38/47/45/57/166/121/41/49,kit/runner/motion 全 0)与 r2 口径**逐字持平**,无新增 | 0 条 |
| 新商标 | 黑名单 `rg -i`(30 词表,含 4399/任天堂/迪士尼/Tetris/Monopoly/雀魂 等)对 12 格非测试源码 **0 命中**;测试文件命中均为黑名单断言词表自身;织纹为无图案 45° 平行线族 | 0 条 |
| kit 越权 | `--diff-filter=M` 仍 0 条(1.3 节) | 0 条 |
| reduced-motion 破坏 | CDP `prefers-reduced-motion: reduce` 实开 flight-chess / star-estate / combo-clash / mahjong-bloom 进对局:**运行中无限循环动画(`document.getAnimations` iterations=∞)全 0 个**,功能按钮保留 14–54 个/款(对局界面口径);r2 织纹为纯静态 CSS(契约钉子同批复跑绿) | 0 条 |

**收官回归总结论:R2-2 落地无回退,r1 12/12 抽查无回退,豁免 3 项依据均成立,回归缺陷 0 条。**

---

## 三、专项 ①:火柴人 / 草稿圆扫描

**方法**:九款 + kit/runner/motion 全部非测试源码脚本扫描——逐行核 `fillText(` 行是否携带 emoji
字面量;`\p{Extended_Pictographic}` 码位分文件计数;与 r2 同一口径可直接同比。

| 对象 | canvas `fillText` 行数 | 其中 emoji 直出 | 非测试源码 emoji 字符数(r2 同比) | 判定 |
| --- | --- | --- | --- | --- |
| orb-arena | 3 | 0 | 41(持平) | ✅ |
| snake-royale | 4 | 0 | 38(持平) | ✅ |
| block-drop | 1 | 0 | 47(持平) | ✅ |
| combo-clash | 4 | 0 | 45(持平) | ✅ |
| mahjong-bloom | 0 | 0 | 57(持平) | ✅ |
| star-estate | 0 | 0 | 166(持平) | ✅ |
| hero-cards | 0 | 0 | 121(持平) | ✅ |
| weiqi-garden | 1 | 0 | 41(持平) | ✅ |
| flight-chess | 0 | 0 | 49(持平) | ✅ |
| kit / runner / motion | 2 / 0 / 0 | 0 | 0 / 0 / 0(持平) | ✅ |

九款 emoji 字符计数与 r2 **全部逐字持平**——收官轮无任何新增装饰性 emoji。star-estate 的 166 个
仍全部在映射表键 / aria-label / 地契列表文本(L-5 文案层),运行时地格图标 40/40 为 SVG(2.2 节实测)。
「纯圆 + 两点眼」主角 0 处(主体绘制函数自 r1 起零触碰,1.1 节零漂移证明)。

**问题清单:严重 0、阻断 0。**

---

## 四、专项 ②:金币 / 收集物体积

**方法**:1.1 节零漂移证明(r2 已逐名 grep 确认收集物函数自 r1 后零触碰,本轮 `8ca41a8..HEAD` src 零
diff,故 r1 72 格结论仍有效)+ 本轮抽读重点函数复核层次。

| 款 | 收集物(本轮抽读) | 层次证据 | 判定 |
| --- | --- | --- | --- |
| star-estate | `coinSVG`(`art.ts:203-220`,本轮逐行重读) | 3 停径向渐变(tint0.55→base→shade0.2)+ 侧沿厚度(下移 0.9 的 shade0.42 底圆)+ 内圈亮环 + 星形压印 + 左上高光斑(rotate −30°) | ✅ |
| kit | `drawCoin`(`props.ts:62-100`,本轮逐行重读) | 5 层:侧沿暗阶 / 币面 / 内圈环 / 星形浮雕 / 左上高光斑 `(-0.32r,-0.42r)` | ✅ |
| orb-arena / snake-royale / block-drop / combo-clash | `paintStarCandy` / `drawStarBean`+`drawGemDrop` / `drawSentStar` / `drawProjectileOrb` | 函数全部在位(本轮 grep 定位行号复核),实现与 r1/r2 检视对象零漂移 | ✅ |
| hero-cards / mahjong / weiqi / flight-chess | 体力心 / 开花节点 / 提子花瓣 / 塔顶星花环 | 各自既有契约用例随全仓复跑全绿 | ✅ |

**「平涂圆内贴数字/星字符」:0 处。严重 0、阻断 0。**
本轮新增钉子:kit `drawCoin` 高光左上 + `coinSVG` 高光锚 cx38%/cy32%(R3-C 组 2 例)。

---

## 五、专项 ③:双人 / 多角色可区分

**方法**:零漂移前提下复核区分通道资产在位(本轮 grep 逐名定位)+ 18 组实测画面渲染正常。

| 款 | 形状通道(灰度可辨) | 本轮证据 | 判定 |
| --- | --- | --- | --- |
| combo-clash(12 角色) | 12 款头饰 + 姿态差分 + 脚下双色光环 | 头饰段与 `AURA_COLORS` 零漂移;既有 32 例资产契约全绿 | ✅ |
| hero-cards | 14 款帽饰 + 四门花色四剪影 + 红黑双通道 | `hatInner`(`cardart.ts:428`)在位;26 例头像互异契约全绿 | ✅ |
| flight-chess | 四款尾翼(圆/星/双叉/燕尾鳍)+ 四色 | `FIN_NAMES`(`art.ts:23`)在位,契约全绿 | ✅ |
| snake-royale | 头饰(蝴蝶结/棒球帽)+ 4 花纹族 | `accessoryFor`(`art.ts:681`)在位 | ✅ |
| orb-arena | 金星/银月头饰 + 表情 | `crestOf`(`index.ts:212`)在位 | ✅ |
| star-estate | 花/星/云/月四种立牌脑袋 | `tokenHead`(`art.ts:56`)在位 | ✅ |
| weiqi-garden | 黑白明度两极 + 白子暖描边 | 既有契约全绿 | ✅ |

**「只靠颜色区分」:0 款。严重 0、阻断 0。**

---

## 六、专项 ④:跑酷 / 竞速 / 纵深体积(点名层在位无退化)

| 点名对象 | 现状(本轮取证) | 判定 |
| --- | --- | --- |
| orb-arena 视差层 | 圆斑 0.3 + 具象贴片 0.3(同档不同层,预算 240)+ 6% 网格 + 条纹墙,四层结构常量与调用零漂移 | ✅ |
| snake-royale 双档视差 + 色岛 | 0.6(色岛,预算 48)/0.85(贴片,预算 360)恰两档;`DECOR_MIN_SIZE=3.2` | ✅ |
| block-drop 井壁浮雕 | `RELIEF_H=12`、`paintWallRelief` 四主题、离屏缓存在位 | ✅ |
| combo-clash 土丘三阶 | base + 亮弧(0.7r,π→1.72π)+ 暗带(4px)三笔齐(2.2 节逐行重读) | ✅ |
| flight-chess 装饰层 | 运行时实测草簇 4 + 云 2 恰 6 件、aria-hidden、pointer-events:none | ✅ |
| runner 基建 | 本窗九款仍 0 引用(棋牌/竞技精 2D 定级正确),基建 58 例自检随全仓全绿 | ✅(N/A) |
| three.js | package.json 无 three 系依赖;r1 ④组 11 例契约全绿 | ✅ |

**r1/r2 修后退化:0 处。严重 0、阻断 0。**

---

## 七、专项 ⑤:360px 布局实测

**方法**:`npm run build` + `vite preview`,Chrome headless 148(CDP,`Emulation.setDeviceMetricsOverride`)
实开九款,360×640 与 320×640 各一遍(18 组);每款「关卡页初测 → 点『开始冒险 ▶』进对局 → 复测」;
量测:文档级 `scrollWidth − innerWidth`、可见文本 <14px(排除 aria-hidden 与 SVG 图形字)、
交互元素 <40px 触区、console error / 页面异常。

| 款 | 360px 溢出 | 320px 溢出 | 主场景渲染 | <14px 文本 | 小触区 | console error |
| --- | --- | --- | --- | --- | --- | --- |
| orb-arena | 0px | 0px | 3 canvas | 仅共享壳 `l99-star` 10–12px(R2-1) | 无(`.oa-open` 实测 119×44 ×3) | 0 |
| snake-royale | 0px | 0px | 1 canvas | 同上 | 无(`.sr-open` 119×44 ×3、`.sr-skin` 64–117×44 ×10) | 0 |
| block-drop | 0px | 0px | 3 canvas | 同上 | 无(`.bd-open` 119–148×44 ×3) | 0 |
| combo-clash | 0px | 0px | 5 canvas | 同上 | 无 | 0 |
| mahjong-bloom | 0px | 0px | 15 svg | 同上 | `mj-tile` 30×42(G-2 豁免,非新增) | 0 |
| star-estate | 0px | 0px | 115 svg | 同上 | `se-tile` 23×23 / 320px 20×20(G-3 豁免)、`se-deed` 267×36 信息行(G-3 豁免依据自身,1.2 遗留非新增) | 0 |
| hero-cards | 0px | 0px | 19 svg | 同上 | 无 | 0 |
| weiqi-garden | 0px | 0px | 1 canvas | 同上 | 无 | 0 |
| flight-chess | 0px | 0px | 22 svg | 同上 | `fc-token` 19×19 / 320px 17×17(G-4 豁免,非新增) | 0 |

- **文档级溢出 18 组全 0;新溢出 0(阻断 0)**;主场景计数(3/1/3/5/15/115/19/1/22)与 r2 逐字持平。
- <14px 唯一命中仍是共享壳 `l99-star` 10–12px = R2-1,按任务书**不记阻断**,复登记见 2.4 节。
- 小触区命中全部为 G-2/G-3/G-4 既豁免项,尺寸与裁决时逐字一致,无恶化。

---

## 八、专项 ⑥:商标扫描

**方法**:宪法第八节口径,对本窗九款 + kit + runner + motion 的**非测试源码**全量 `rg -i`
(黑名单 30 词:4399 / 任天堂 / 迪士尼 / 马里奥 / 皮卡丘 / 宝可梦 / Hello Kitty / 托马斯 / Bomberman /
Tetris / 俄罗斯方块 / 球球大作战 / 贪吃蛇大作战 / slither / agar / Monopoly / 大富翁 / 三国杀 / 雀魂 /
野狐 / 蛋仔 / 地铁跑酷 / 超级玛丽 / Nintendo / Disney / Mario / Pokemon / Pikachu / Subway Surf 等)。

- 非测试源码 **0 命中**(含注释);测试文件命中全部为各款 `levels.test.ts` 自带的黑名单断言词表
  (「禁止出现」的反例词表本身),非孩子可见文案。
- 造型侧:star-estate `TILE_ICONS` 图形体段(`art.ts:409-647`)`<text>` 节点 0、`?` 字符 0
  ——**无「? 徽章」**;两处桌面织纹均为无图案 45° 平行线族,无字母 / 徽记 / 花型织入;
  r2 以来无任何新画资产(1.1 节零漂移)。

**阻断 0。**

---

## 九、横向一致性抽查

| 项 | 方法与结果 | 判定 |
| --- | --- | --- |
| kit 光照约定(左上光源) | 本轮逐行重读两处锚点:kit `drawCoin` 高光斑 `(-0.32r,-0.42r)` 左上(`props.ts:97`)、star-estate `coinSVG` 径向渐变锚 `cx38%,cy32%`(均 <50% = 左上,`art.ts:208`);combo 土丘亮弧 π→1.72π 偏左上(`art.ts:312`)。已新增 2 例钉子(R3-C) | ✅ |
| prefers-reduced-motion | CDP `prefers-reduced-motion: reduce` 实开 flight-chess / star-estate / combo-clash / mahjong-bloom 进对局:**运行中无限循环动画 0 个**(`document.getAnimations` iterations=∞ 计数),功能按钮保留 14 / 54 / 14 / 25 个(对局界面口径);r2 新增织纹纯静态,契约同批全绿 | ✅ |
| destroy 泄漏 ×5 | CDP 实测六款(orb-arena / combo-clash / star-estate / flight-chess / snake-royale / mahjong-bloom):进→退 ×5 后 `.view canvas` **0**、`.game-screen` **0**、pageerror **0**;再进全部正常挂载 1 屏且主场景渲染(3/5/115/22/1/15 与 18 组实测一致) | ✅ |
| 粒子节流 | **未取到帧率(headless 环境不做帧率声称),只做代码上限抽查**:orb 贴片 240 件/帧、snake 色岛 48 枚 + 贴片 360 件/帧、combo `CONFETTI_COUNT=20` 封顶 + `fxBudget(soft)` 归零(`index.ts:276`)。三预算已新增钉子(R3-B 组 3 例)。CPU 4× 节流下 360px 实开 orb / snake / combo 三款进对局:canvas 均有实绘像素、页面异常 0(观感描述,无帧率数字) | ✅ |

---

## 十、问题清单(全部量化)

**阻断:0 条 严重:0 条 一般:1 条(遗留复登记) 建议:0 条新增**

| 编号 | 位置 | 现状(量化) | 期望 | 严重度 | 1.3 回归? | 去向 |
| --- | --- | --- | --- | --- | --- | --- |
| R3-1(= R2-1 = L-1 = r1 G-1,复登记) | `src/games/level99.ts:512` `.l99-node-stars{font-size:11px}`、`:561` @媒体降 10px | 本轮 18 组实测唯一 <14px 命中(`l99-star` 计算样式 10–12px),九款关卡页同源受累 | 星级改 12×12 内联 SVG 画形(r1 learner P7 方案留档) | 一般 | 否(1.2 逐字遗留) | 共享壳 owner / 统筹轮,**本窗不抢**(任务书明令) |

L-3 / L-4 / L-5 现状登记见 2.4 节(登记不抬级,不占新编号)。G-2 / G-3 / G-4 维持豁免(2.3 节)。
**交 C 档的严重级清单:空。** 本轮无新增可动手项——收官轮的正确产出就是「零新账 + 全部旧账有主」。

---

## 十一、本轮新增机器化回归用例

新增 `src/games/window1-visual-r3.test.ts`(commit `1b77c15`):**12 例全绿**。收官轮不重钉 r1/r2
已固化契约(由既有四个钉子文件继续看守),专钉三类跨款一致性防未来漂移:

| 组 | 用例数 | 内容 |
| --- | --- | --- |
| R3-A 织物语言跨款一致 | 5 | `.mj-board` ↔ `.hc-table` 织纹同 45°/6px/12px 节距逐字一致 / 两处 alpha ≤0.04 / 均为多重背景第一层 / 纯静态 / **6px/12px 桌面织纹全窗恰 2 处**(不许复制进围棋等密度上限最低的款) |
| R3-B 装饰/粒子预算上限 | 3 | orb 贴片 240 / snake 色岛 48 + 贴片 360 / combo `CONFETTI_COUNT` ≤20(防后续轮次悄悄抬预算伤低端机) |
| R3-C kit 光照左上约定 | 2 | kit `drawCoin` 高光 `(-0.32r,-0.42r)` / star-estate `coinSVG` 高光锚 cx·cy <50% |
| R3-D 回归猎手钉子 | 2 | `.hc-table` 木沿 `#EBD2B4`(织纹暖褐派生锚)未被破坏 / `.se-tile-mort` 抵押纹保持状态语义层(4px/8px)不与桌面织纹混同 |

---

## 十二、结果矩阵(12 格 × 六专项)

| 格 | ①火柴人 | ②金币体积 | ③多角色 | ④纵深体积 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| kit(1A) | ✅ | ✅ | ✅ | ✅ | ✅(N/A 布局) | ✅ |
| motion(1B) | ✅(N/A 绘制) | ✅(N/A) | ✅(N/A) | ✅(N/A) | ✅(时序契约) | ✅ |
| runner(1C) | ✅ | ✅(N/A) | ✅(N/A) | ✅(基建自检) | ✅(N/A) | ✅ |
| orb-arena(2A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| snake-royale(2B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| block-drop(2C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| combo-clash(3A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mahjong-bloom(3B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| star-estate(3C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| hero-cards(4A) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| weiqi-garden(4B) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| flight-chess(4C) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

注:⑤ 列 ✅ 指「文档级零溢出、零 console error、无本款 <14px 正文、r1/r2 修复复测通过、豁免项无恶化」;
共享壳 R3-1(= R2-1)单列不改变格判定。**矩阵 72 格全 ✅**——本窗 1.3 视觉升级收官质量达标。

---

## 十三、验收口径核对与收尾登记

- 测试全绿且只增不减:✅ 进场 727 文件 / 15579 例(= r2 fixer 水位逐字持平)→ 交卷
  **728 文件 / 15591 例**(+1 文件 / +12 例);
- `npx tsc --noEmit` 0 错:✅;`npm run build` 全绿:✅(110.70 kB / gzip 38.67 kB,
  precache 188 项 / 5291.55 KiB);
- 阻断 0 残留:✅(商标非测试源码 0 命中、360/320 新溢出 0,当轮保持清零);
- 严重级清单:**0 条**(交 C 档清单为空);一般 1 条 = R3-1 遗留复登记(共享壳,本窗不抢);
- 收官回归:R2-2 源码 + 运行时双取证无回退;r1 12 项抽查无回退;豁免 3 项依据成立;
  遗留 5 项(R2-1/R2-3/L-3/L-4/L-5)全部登记有主;回归缺陷 0;
- 测试员未改 `src/games/**`/`src/art/**`/`src/ui/**` 任何绘制实现:✅
  (diff 仅本报告 + `window1-visual-r3.test.ts`);未派 learner/fixer,未动他人报告与他窗文件。

**收尾登记**:`git fetch origin game-1.3-window1` → rebase(保留他人文件)→ `npx vitest run`
(**728 文件 / 15591 例全绿**,合并态复跑)→ `npx tsc --noEmit`(**0 错**)→ `npm run build`
(**全绿**,precache 188 项 / 5291.55 KiB)→ 普通 push `origin game-1.3-window1`
(无 force、未动 `main` / `game-1.2` / `game-1.2-kk`、未用 `gh` 开 PR)。
**合并态代码 SHA:`1b77c15`**(本轮唯一代码 commit = 新增用例;其后仅本报告 docs-only commit,
全绿结论对推送 tip 同样成立)。

本轮实际使用模型 slug:`claude-fable-5-thinking-xhigh`。
