# 1.3 窗口 1 · 第 3 轮（收官）视觉测试员报告

> 状态：扫描计划已提交，测试执行中。
> 角色：A 档视觉测试员（收官轮）。只验视觉，不改玩法 / 数值 / 关卡 / 绘制实现。
> 工作分支：`game-1.3-window1`。

## 扫描计划

1. 全量跑：`npm ci` → `npx vitest run`（全仓）→ `npx tsc --noEmit` → `npm run build` → preview 冒烟。对照第 2 轮 fixer 水位 727 文件 / 15579 例（代码 SHA `8ca41a8`，tip `d062720`），只增不减。
2. 十二格盘点：对照 `docs/qa/1.3-window1-plan.md` 十二格 SHA；kit / motion / runner `git log --diff-filter=M` 抽查独占纪律。
3. 必读（只读不改）：round1 / round2 六份报告 + `docs/plan-1.3-visual-bible.md`。
4. 收官回归：R2-2 `.hc-table` 斜织纹独立取证；r1 P1–P10 + clamp 抽查；豁免 G-2/G-3/G-4 维持；遗留 R2-1 / L-1 / L-3 / L-4 / L-5 登记不抬级。
5. 回归猎手：第 2 轮只改 hero-cards CSS——确认无新溢出、新裸 emoji 主角、新商标、kit 越权、reduced-motion 破坏；360/320 九款复扫。
6. 六大专项（收缩到本窗 12 格）：①火柴人/草稿圆 ②金币体积 ③多角色可区分 ④纵深体积 ⑤360px ⑥商标。
7. 横向一致性：kit 光照、reduced-motion、destroy×5、粒子上限（代码抽查，不编造帧率）。
8. 产出：本报告 + 可选新增 `src/games/window1-visual-r3.test.ts` 机器化用例（只增不减）。

（下文各节将在测试完成后填充。）
