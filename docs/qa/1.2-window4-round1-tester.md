# 窗口 4 · 第 1 轮 · 测试员

## 档C

范围：`alien-seek` / `box-hamster` / `bubble-aim` / `snake-snack` / `memory-cards` 五款。
基线：`origin/game-1.2-window4` = `3063287`，**175 文件 / 4991 用例**全绿，`npm run build` 绿。

### 一、本轮走查计划（开工前登记）

| # | 剧本 | 覆盖对象 |
| --- | --- | --- |
| 1 | 首页进入：`meta.ts` 的 id / levels / modes 与实现逐条对账 | 五款 |
| 2 | 赢一次 + 输一次（真实胜负，不是看单函数返回值） | 五款 |
| 3 | 战役第 1 / 100 / 188 关可解、可通 | 五款 |
| 4 | 对战 / 无尽 / 双人：按 `meta.modes` 声明的每种模式玩到结算 | 五款 |
| 5 | 360px 窄屏不溢出 | 五款 |

### 二、怎么算「真的玩了一局」

本仓 vitest 跑在 `environment: "node"`，没有 DOM，`index.ts` 挂不起来。
所以本轮不写「假装点了一下 DOM」的空壳，改成**把 `index.ts` 的规则层一比一搬进模拟器**，
再用与游戏完全相同的纯函数结算，逐步跑完一局：

| 游戏 | 玩家怎么模拟的 | 赢的判据 | 输的判据 |
| --- | --- | --- | --- |
| `alien-seek` | 光标按 `travelTime` 走位，逐个目标调 `hitSpot` 真点 | 全部目标点中且剩余时间 > 0 | 半途撒手 / 限时耗尽 |
| `box-hamster` | `solver.solve()` 出解，再逐步喂给 `tryMove` 回放 | `isSolved` 为真 | 箱子推进死角，`deadlockReason` 非空且求解器确认无解 |
| `bubble-aim` | 贪心机器人扫 20°~160°，用 `simulateShot` / `settleShot` / `damageStone` / `pressCeiling` 结算 | `countBubbles === 0` | 只会朝正上方直射的摆烂打法 |
| `snake-snack` | 把 `index.ts` 的 `step()` / `placeSnack()` 一比一搬成模拟器，配一个「先活下来再去吃」的机器人 | 吃满 `target` | 一路撞进围栏 |
| `memory-cards` | 每一次点击真的过 `tapCard → resolve → settle` 状态机 | 全部组配完 | 失误超过额度 |

### 三、走查结果表

| 游戏 | 首页进入 | 赢一次 | 输一次 | 第 1 关 | 第 100 关 | 第 188 关 | 声明模式全部到结算 | 360px |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `alien-seek` | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 战役 / 无尽 15 轮 / 对战 6 局 / 双人两套光标：通过 | 通过 |
| `box-hamster` | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 战役 / 无尽 10 仓：通过 | **不通过（C1-01）** |
| `bubble-aim` | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 战役 / 无尽压 40 行：通过 | 通过 |
| `snake-snack` | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 战役 / 无尽 5 座花园 × 两档节奏：通过 | 通过 |
| `memory-cards` | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 战役 / 无尽 20 轮 / 对战 / 双人轮流：通过 | 通过 |

### 四、问题清单

#### C1-01　推箱小仓鼠：双鼠宽仓在 360px 上被直接切掉右边几列　**【严重】**

- **复现步骤**：360px 宽的手机上进 `box-hamster` → 玩第 6 章「双鼠搭档」任意一关（例如第 133 关）或第 7 章的 `双鼠仓库` / `双鼠溜冰` 变体。
- **期望**：整张棋盘看得见、点得到。
- **实际**：`.bh-grid` 的格子边长是媒体查询写死的（`max-width:420px` → `--cell:34px`，`max-width:340px` → `28px`），
  棋盘宽度 = `列数 × 34 + (列数−1) × 2`。13 列要 **466px**，而 360px 手机上留给棋盘的只有
  `360 − 8(.game-stage 4px 白边 ×2) − 20(.bh-stagebox 10px 内边距 ×2) = 332px`，**溢出 134px**。
  更要命的是 `.game-stage` 是 `overflow:hidden` —— 超出的列不是能滑出来，而是**根本看不见也点不到**。
- **量化**：188 关里 **36 关**溢出（列数分布 11 列 9 关 / 12 列 18 关 / 13 列 9 关），最宽 13 列，最大溢出 **134px（40%）**。
  无尽仓最宽 8 列（274px），不受影响。
- **建议责任文件**：`src/games/box-hamster/index.ts`（`CSS` 与建棋盘处）、`src/games/box-hamster/assist.ts`（放一个可测的算宽函数）。
- **登记方式**：`qaC1.test.ts` 里那条 `【C1-01 严重 · 待修】` 先把现状（36 关 / 466px）钉成断言，
  防止在修复前继续恶化；本轮修复员改成「0 关溢出」。

#### C1-02　记忆翻翻乐：无尽从第 8 轮起完全不再变难　**【一般】**

- **复现步骤**：进 `memory-cards` → 无尽记忆挑战 → 一直玩到第 8 轮以后。
- **期望**：无尽应当一轮比一轮难。
- **实际**：`endlessPairs(round) = min(10, 2 + round)`，第 8 轮就触顶 10 组。
  第 8 轮与第 99 轮的 `pairs / cols / matchSize / decoys / rotateEvery / swapEvery / timeLimit` **完全相同**，
  唯一还在变的只有 `theme`（配色）。也就是说第 8 轮之后是同一关换皮无限重复。
- **量化**：第 8 轮起 20 张牌 / 5 列 / 无任何机关，恒定不变；`simulatePerfectPlay` 给出的失误数与耗时也恒定（2 次 / 37.8 秒）。
- **建议责任文件**：`src/games/memory-cards/logic.ts` 的 `endlessLevel`。
- **建议方向**：牌数封顶是对的（360px 摆不下更多），但可以在封顶之后接着上**不加牌的机关**
  （会挪窝的牌 `swapEvery` → 牌阵旋转 `rotateEvery` → 少量干扰卡），让曲线继续往上走。

#### C1-03　寻找外星朋友：无尽第 20 轮起三个参数同时触顶　**【一般】**

- **实际**：`buildEndlessRound` 的限时 `max(14, 38 − ⌊1.2r⌋)`、藏身点数 `min(16, 7 + ⌊r/2⌋)`、
  目标数 `min(5, 2 + ⌊r/4⌋)` 在第 20 轮左右**同时**到达下限 / 上限，之后每一轮的题目规模完全一样。
- **量化**：第 20 轮 = 14 秒 / 16 个藏身点 / 5 个目标；第 40 轮、第 100 轮一模一样。
- **对比**：不同于 C1-02 的是，这里每轮还会换场景种子（藏身点位置、颜色种类都不同），
  也会每 4 轮插一道推理题，所以**换皮感弱得多**，定级为一般而不是严重。
- **建议责任文件**：`src/games/alien-seek/levels.ts` 的 `buildEndlessRound`。

#### C1-04　寻找外星朋友：无尽曲线在 `logic.ts` 与 `levels.ts` 里写了两份　**【建议】**

- **实际**：`logic.ts` 导出 `endlessSeconds` / `endlessSpotCount` / `endlessTargetCount` 三个函数，
  `levels.ts` 的 `buildEndlessRound` 又把同样的三条公式**手抄了一遍**。
  眼下两边数值一致（已逐轮比对第 1–40 轮），但这是典型的「改一处漏一处」隐患：
  既有单测 `seek12.test.ts` 只盯 `logic.ts` 那一份，`levels.ts` 那一份改坏了不会红。
- **建议责任文件**：`src/games/alien-seek/levels.ts`。

#### C1-05　泡泡瞄准手：自建存档 key 用的是 1.0 老前缀　**【建议 · 本轮不动】**

- **实际**：`bubble-aim/index.ts` 用 `localStorage` 直接读写 `yiduo.bubble-aim.campaign.v2`，
  没走 `save.ts`，前缀也是 `yiduo.` 而不是全库统一的 `yiduo-yixing.`。
- **结论**：**本轮不改**。硬约束是「存档 key 只增不改」，改前缀会让所有老玩家的 188 关进度一夜清零，
  代价远大于收益。登记备案，交主管决定是否在某个大版本做一次带迁移的搬家。

#### C1-06　贪吃毛毛虫：星门园的贪心机器人首个种子跑不满　**【建议 · 非产品缺陷】**

- **实际**：本轮新写的机器人在无尽第 3 座「星门园」用种子 1 跑到步数上限还差几口，换种子就过。
- **判定**：这是**机器人的局限**（星门让「离点心多远」这个启发失准），不是游戏本身的问题——
  该花园的连通性、空地数、目标数都合格。登记下来，避免下一轮误判成产品缺陷。

### 五、分级统计

| 级别 | 条数 | 编号 |
| --- | --- | --- |
| 阻断 | 0 | — |
| 严重 | 1 | C1-01 |
| 一般 | 2 | C1-02、C1-03 |
| 建议 | 3 | C1-04、C1-05、C1-06 |

### 六、本段新增用例

| 文件 | 新增用例 |
| --- | --- |
| `src/games/alien-seek/qaC1.test.ts` | 22 |
| `src/games/box-hamster/qaC1.test.ts` | 25 |
| `src/games/bubble-aim/qaC1.test.ts` | 21 |
| `src/games/snake-snack/qaC1.test.ts` | 23 |
| `src/games/memory-cards/qaC1.test.ts` | 28 |
| **合计** | **119** |

`npm test`：**180 文件 / 5110 用例**全绿（基线 175 / 4991，只增不减）。
`npm run build`：绿。

### 七、权限边界

本段只新增 `*.test.ts` 与本报告，一行玩法代码都没改。C1-01 交本轮修复员，C1-02 ~ C1-04 交本轮学习优化员。

## 档A

### 0. 本档范围与基线

| 项 | 值 |
|---|---|
| 窗口 / 档位 | 窗口 4 · 档 A |
| 独占五款 | `brave-path` 勇者小路 / `brick-break` 碰碰砖块 / `balloon-pop` 气球砰砰 / `fruit-catch` 接住小水果 / `lianliankan` 连连看 |
| 起点分支 | `origin/game-1.2-window4` @ `3063287` |
| 起点全量 | 175 个测试文件 / 4991 个用例全绿；`npm run build` 绿（主 chunk 86.83 kB / gzip 30.39 kB，构建 331ms） |
| 五款起点用例数 | brave-path 219 / brick-break 66 / balloon-pop 52 / fruit-catch 54 / lianliankan 59，合计 **450** |

跳过项按派发单执行：窗口2 点名的 `gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi` 不碰；
窗口1 的 root 门 / 首页筛选 / 2.5D 不碰；第 27–29 步的全局三人组不做。

### 1. 走查方式说明

仓库的 vitest 跑在 **node 环境**（`vite.config.ts` 的 `test.environment: "node"`，无 jsdom），
产品硬约束又不许引外部依赖，所以本档沿用全仓 175 个测试文件一贯的做法：

- **实玩**＝在纯逻辑层跑完整对局（`simulateLevel` / `solveBoard` / `simulateBattle` / 状态机推演），
  一路推到真实的胜负结算，而不是只看关卡表的数字；
- **入口链路**＝`loadGames()` 真的把这一款收进首页 → `await load()` 真的动态加载出 `mount`
  （这一步会真跑一遍 `index.ts` 的模块求值，`index.ts` 写坏了当场红）；
- **模式入口**＝对 `index.ts` 源码做定向巡检，确认 `meta.modes` 声明的每一种模式都有对应的挂载函数；
- **360px**＝对场地常量与 CSS 规则做定向巡检（格子尺寸、热区、媒体查询断点、写死宽度）。

新增走查文件（本轮只增用例、不动玩法代码）：

```
src/games/brave-path/walkthrough.r1.test.ts
src/games/brick-break/walkthrough.r1.test.ts
src/games/balloon-pop/walkthrough.r1.test.ts
src/games/fruit-catch/walkthrough.r1.test.ts
src/games/lianliankan/walkthrough.r1.test.ts
```

### 2. 五款走查结果表

| 游戏 | 首页进入 | 赢一次 | 输一次 | 第 1 关 | 第 100 关 | 第 188 关 | 各模式结算 | 360px | 结论 |
|---|---|---|---|---|---|---|---|---|---|
| 勇者小路 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 闯关 ✅ / 无尽之路 ✅ / 对战星星队伍 ✅ / 同图竞速 ✅ | ✅ | 通过 |
| 碰碰砖块 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 闯关 ✅ / 无尽砖塔 ✅ | ✅ | 通过 |
| 气球砰砰 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 闯关 ✅ / 无尽气球节 ✅ | ✅ | **带 1 条严重问题** |
| 接住小水果 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 闯关 ✅ / 双人抢果 ✅ / 无尽水果雨 ✅ | ✅ | 通过（带 1 条建议） |
| 连连看 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 闯关 ✅ / 无尽连到底 ✅ | ✅ | 通过 |

关键量化数据：

- **勇者小路**：第 1 / 100 / 188 关达标勇者都一口气走通，第 188 关最后一步确实是末章首领「星辉巨像」，
  门口有 `healRatio: 1` 的整装石。无尽之路用第 40 关水平的勇者跑到 **第 18 层**打不动收工（会结束，不是无限赢），
  路上吃到 6 次祝福。擂台三对三接力至少打满 3 小场。同图竞速迷宫第 1 / 5 / 20 / 60 层
  `validateMaze` 全 `ok`，全程路线 21 / 25 / 59 / 99 步；影子从 0 胜到 8 胜每步 420ms → 324ms。
- **碰碰砖块**：第 1 / 100 / 188 关分别 72.1s / 65.9s / 89.5s 打完，全程最平角度 35.9° / 31.1° / 31.2°，
  都在 20° 红线以上。板子按住不动 60 秒会真的掉 4 次球（爱心只有 3 颗，即真输）。
  无尽砖塔：不出手 **29.1 秒**触底收工；每 0.1 秒清一块的话 **10 分钟**都压不到底（清了 1439 排、87352 分）。
- **气球砰砰**：第 1 / 100 / 188 关都拿下；反应调到 5 秒一次会飘走 5 个（超过 `escapes`）判负。
  无尽气球节 900 个出场全戳完不被叫停，最后一个出场在第 **340.7 秒**；放跑 3 个收工。
- **接住小水果**：第 1 / 100 / 188 关 `hazardHits = 0`、`missed = 0`、三星；
  400 颗的无尽出场表 `checkReachable` 全 `ok`（`firstBad = -1`，`hazardRisk = 0`，奖励果 59 颗），
  最后一颗落在第 **189.6 秒**。双人抢果先到 30 颗收工。
- **连连看**：第 1 / 100 / 188 关自动玩家全清；188 关是「只准拐一次弯 + 四方重力」，开局就有得连。
  无尽连到底 **40 盘**一次没卡死，累计 720 对。360px：最宽的 8 列关每格 **34.7px** ≥ 32px（4 列 65.5px / 6 列 45.7px）。

### 3. 本轮问题清单

> 格式：复现步骤 / 期望 / 实际 / 严重度 / 建议责任文件

#### W4A-01 · 严重 · 气球砰砰：不是保护关也偷偷按礼物气球扣星

- **复现**：跑 `simulateLevel(LEVELS[187], { seed: 511 })`（第 188 关，`protect` 为假）。
  或任取第 129–188 关里 `giftChance > 0 && !protect` 的那 28 关。
- **期望**：关卡目标是「戳够数量」时，HUD 从头到尾没提过礼物气球，
  那么礼物飘走就不该影响这一关的成绩；要么算分，要么就在 HUD 上说清楚。
- **实际**：`starsFor(mistakes, escaped, giftLost)` 里 `giftLost` 一个顶 **2 个**「坏事」，
  而 `renderTop()` 只在 `goal === "protect"` 时显示「🎁 护住礼物」，其余分支显示
  「🌤️ 可飘走 N」或「🎯 戳X色」，**从不提礼物**。
  实测：**28 关**会放礼物却不是保护关（第 129–188 关），其中 **13 关**在
  「零戳错、零超额飘走」的完美通关下，仅仅因为礼物飘走就掉星，最狠的从 **3 星掉到 1 星**
  （第 188 关 `giftLost = 4` → `starsFor(0, 0, 4)` 的 `bad = 8` → 1 星）。
- **严重度**：**严重**。孩子打出完美一局却拿 1 星，界面上没有任何线索解释为什么，
  违背「失败只鼓励 / 规则要看得见」的红线。
- **建议责任文件**：`src/games/balloon-pop/logic.ts`（`starsFor` / `GoalState`）
  与 `src/games/balloon-pop/index.ts`（`renderTop`）。
- **建议修法**（两选一，交监督修复员定）：
  1. 非保护关不把 `giftLost` 计入星级（礼物只在保护关算数）；
  2. 或者非保护关也在 HUD 上挂一个礼物计数，让规则看得见。

#### W4A-02 · 一般 · 气球砰砰：连爆时长和「250ms 内连完」的规格对不上

- **复现**：`chainDurationMs(8)` / `chainDurationMs(20)`。
- **期望**：`CHAIN_WINDOW_MS` 的注释写的是「一条链最多在这个时间里连完（毫秒）」＝ **250**。
- **实际**：`chainDelays` 的封顶写成了 `CHAIN_WINDOW_MS * 4` ＝ 1000ms，于是
  链长 3/5/8/12/20 分别是 100 / 200 / **350** / **550** / **950** ms——链长 ≥ 7 就超窗。
- **严重度**：一般（手感与文档不一致，长链会拖出近 1 秒的等待）。
- **建议责任文件**：`src/games/balloon-pop/logic.ts`（`CHAIN_WINDOW_MS` / `chainDelays`）。

#### W4A-03 · 一般 · 气球砰砰：`festPlan` 自己不管礼物气球上限

- **复现**：`festPlan(12345, 400).filter(p => p.kind === "gift")`，按各自的上升速度算存活区间。
- **期望**：`GIFT_MAX_ON_SCREEN = 1`，注释写明「同一时间天上最多只有一个礼物气球」。
- **实际**：出场表里最多会排出 **6 个礼物同时在天上**。真机没出事，全靠
  `index.ts:846` 那一行 `canSpawnGift(...) ? ... : "normal"` 兜底；纯函数层一个断言都没有，
  以后谁再拿 `festPlan` 去别处用就会破功。
- **严重度**：一般（当前不影响玩家，属于纯函数层缺护栏）。
- **建议责任文件**：`src/games/balloon-pop/logic.ts`（`festPlan`）。

#### W4A-04 · 建议 · 接住小水果：模拟器排不出「输」的那条路

- **复现**：`simulateLevel(LEVELS[0], { seed: 3, basketSpeed: 8 })`。
- **期望**：把篮子调得几乎不动，应该会漏水果、会输。
- **实际**：`won: true`、`caught: 8/8`、`missed: 0`。原因是 `simulateLevel` 把同一个
  `basketSpeed` 既喂给 `markReachable`（排可达性）又喂给假玩家，篮子一慢，
  生成器就把几乎所有水果标成「奖励果」，漏了不扣爱心——于是**永远输不了**。
- **严重度**：建议（不是玩法缺陷，是可测性缺陷：188 关的「输」这条路没有可执行的模拟）。
  本轮先在 `walkthrough.r1.test.ts` 里用纯函数手搭了一个「篮子站着不动」的小模拟顶上，
  实测第 1 关会真的漏够 3 颗。
- **建议责任文件**：`src/games/fruit-catch/logic.ts`（`SimOptions` 拆出玩家速度）。

#### 无问题项（逐条核过）

- **商标黑名单 0 命中**：29 个词对五款目录（含 `index.ts` / `logic.ts` / `levels.ts` / `guide.ts` / `meta.ts`）
  全文扫描，生产代码 **0 命中**（只有 `brave-path/copy.test.ts` 这份守门测试自己列了黑名单）。
- **角色只用朵朵 / 星星及原创配角**：勇者小路是朵朵（🌸）对星星（⭐），配角糯糯 / 墩墩 / 闪闪 / 久久 / 云云 / 绿绿豆全原创；其余四款没有具名角色。
- **无血无死亡**：五款可见文案 0 命中「流血 / 死亡 / 阵亡 / 杀死」；勇者小路把生命值叫「星芒」，
  输了写「朵朵有点累啦，坐下来歇口气」。
- **失败只鼓励**：五款失败文案逐条读过，全部只给方法，不出现「笨 / 差 / 不行 / 失败」。
- **无广告 / 内购 / 账号 / 联网**：五款目录 0 命中 `fetch(` / `XMLHttpRequest` / `http://` / `https://`。
- **不引入 three.js / CDN / Socket**：0 命中。
- **音效只用 `api.play(...)`**：五款共 36 处 `api.play(`，0 处 `AudioContext` / `new Audio`。
- **存档 key 只增不改**：五款只新增了 `yiduo-yixing.bravepath` 一个 key（勇者小路的养成存档），
  其余四款一律走平台的 `l99.<id>` / `recordEndlessBest`，没有自造 key。
- **`destroy` 无泄漏**：五款各自的 `Janitor.pending()` 在 `destroy()` 之后归零，既有测试已覆盖；
  本轮抽查 `index.ts` 的 `destroy()` 都做了 `cancelAnimationFrame` + `jan.destroy()` + 子模式 `destroy()`。

### 4. 本轮新增用例数

| 游戏 | 起点 | 本轮新增 | 小计 |
|---|---|---|---|
| brave-path | 219 | **+22** | 241 |
| brick-break | 66 | **+19** | 85 |
| balloon-pop | 52 | **+22** | 74 |
| fruit-catch | 54 | **+23** | 77 |
| lianliankan | 59 | **+25** | 84 |
| 合计 | 450 | **+111** | 561 |

### 5. 权限边界声明

本段只写报告 + 补 `*.test.ts`，**一行玩法代码都没动**。
W4A-01 / 02 / 03 交第 1 轮监督修复员，W4A-04 交第 1 轮学习优化员。
