# 窗口 4 · 第 1 轮 · 测试员

## 档C

> 范围：`alien-seek` / `box-hamster` / `bubble-aim` / `snake-snack` / `memory-cards` 五款。
> 状态：**续跑中**（走查计划已登记，实测结论随后补写在本节）。

### 本轮走查计划

| # | 剧本 | 覆盖对象 |
| --- | --- | --- |
| 1 | 首页进入：`loadGames()` 能取到 meta → `meta.modes` 与实现一致 → `mount` 不抛 | 五款 |
| 2 | 赢一次 + 输一次（真实胜负判定，不是只看函数返回） | 五款 |
| 3 | 战役第 1 / 100 / 188 关可解、可通 | 五款 |
| 4 | 对战 / 无尽 / 双人：每种声明过的模式玩到结算 | 按 `meta.modes` 逐款 |
| 5 | 360px 窄屏不溢出 | 五款 |

### 权限边界

本节只新增 `*.test.ts` 与本报告，不改玩法代码；发现的问题交同轮修复员。
