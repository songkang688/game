# 窗口 8 · 1.3 视觉升级 · 第 1 轮学习优化员报告

- 角色：窗口 8 三人验收组 · B 学习优化员（第 1 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：`red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：本报告 + 12 款纯文案（guide / blurb / 提示语）+ 为文案补的快照测试；绘制代码零行；`src/art/kit/` 已有文件不改；A/C 报告一字不碰。

## 学习计划（动手前提交）

1. 向外看：按本窗 6 个题材（竞速/反应对战、拔河对战、教育问答三连〔时钟/数学/拼音/认字/形状〕、找茬、涂色、音乐节奏、养成）梳理 4399 及同类平台头部作品的**画面密度**（背景层数/装饰物件数/动效通道数）与**角色剪影**（16px 依然可认的那一笔）经验，只记结论文字，不下载不截图不描摹。
2. 向内看：通读本窗 12 款的 `index.ts` / `art.ts` / 场景与 kit 文件（只读），逐款量化密度现状与剪影现状，与题材基准对表。
3. 产出：12 款对照评估表（密度差距/剪影达标/观感短板 TOP1/建议动作，四字段无空行）；给 C 档按「严重度 × 修复成本」排序的建议清单（前 10 条到绘制规格级）；商标风险提醒清单（重点：pinyin-train 火车类像托马斯、kitty-care 猫类像 Hello Kitty）。
4. 文案优化：在唯一授权范围内（guide / blurb / 提示语）把 1.3 新视觉亮点讲进引导，面向约小学六年级、中文粉彩萌系；每处改动登记进本报告并补快照测试。
5. 收尾：fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window8`；不碰玩法/判定/关卡数值/parentAuth/parentGate，禁止 three.js，禁止 force。

（以下各节待本轮完成后填写。）
