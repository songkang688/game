# 1.3 窗口 6 · C 档 · 第 1 轮视觉监督修复员报告

- 角色:窗口 6 视觉验收三人组 第 1 轮 · C 监督修复员(落地 A/B 档问题清单 + 自查修复 + 合并态守门)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款)
- 输入:A 档报告 `docs/qa/1.3-window6-round1-tester.md`(阻断 0 · 严重 5 · 一般 6 · 建议 2)+ B 档报告 `docs/qa/1.3-window6-round1-learner.md`(TOP10 绘制规格建议 + 商标风险清单)。
- 执行说明:首任 fixer 完成 W6R1-01~06 后因外部原因中断(修复已上分支),本任同格补派接续:W6R1-08 核对、B 档 TOP 7–10 评估落地、第四节自查、合并态复验。
- 状态:**第 1 轮修复完毕,合并态复验全绿。**

## 〇、修复计划(动手前提交)

修复顺序铁律:阻断 → 严重 → 一般 → 建议。A 档阻断 0(商标 0 命中、360 新溢出 0),直接从严重项开工。每修一处一个 commit(`fix(visual-r1): <款> <问题>`),每处配套只增不减的视觉用例。

| 序 | 条目 | 位置 | 计划修法 |
| --- | --- | --- | --- |
| 1 | W6R1-03(严重①) | `src/games/brave-path/visual.ts` `ITEM_GLYPH` | 钥匙/门/锁/终点 emoji 直出 → 与勇者/影子徽章同族的内联参数化 SVG(落影 + 1.5px 描边 + 左上高光),只动 visual.ts 绘制层 |
| 2 | W6R1-02(严重①②) | `src/games/adventure-king/visual.ts` `drawArtifactSprite` + `index.ts` 门清单 | 文物 emoji fillText → 三种原创纹石(日/月/星)参数化绘制:≥2 停渐变 + 左上高光 + 1.5–2px 描边;底圆 0 停平涂 → 径向渐变;门上的收集清单小图标同步换绘制。光柱/金描边/自转闪点保留 |
| 3 | W6R1-01(严重①) | `src/games/adventure-king/index.ts` `cellGlyph` | 古堡模式主角 🌸 与箱子 📦 → 参数化 SVG(主角朵朵花徽 + 2.5D 木箱);机关小图标(门/钥匙/锁/开关/传送/贴纸等)按 A 档口径保留并登记,交下一轮 |
| 4 | W6R1-04(严重②) | `src/games/brick-break/index.ts` `drawCapsule` | 胶囊平涂双段 → 纵向 ≥3 停渐变 + 左上高光斑;14px serif emoji 内贴 → 每种道具双色小矢量图标;空心圈「别接」形状通道原样保留(capsuleLook 判定不动) |
| 5 | W6R1-05(严重③) | 新增 `src/art/kit/moleAccents.ts`(kit 只增不改) | 闪光鼠加头顶天线星(剪影级差异)+ 描边星芒;盾牌叠冷灰金属盾面 + 加深描边;moleSvg.ts 一字不动,mole-pop visual.ts 改为组合新 kit |
| 6 | W6R1-06(一般③) | 同上 `moleAccents.ts` | 瞌睡鼠闭眼弧加粗 + 瞌睡泡加描边加深 |
| 7 | W6R1-08(一般⑤) | `src/games/bubble-pop/index.ts` `.bp-cell` | 触区 36→40px(核对 8 列 + gap 在 360px 内不溢出后再落) |
| 8 | 自查(第四节) | 9 款 + kit | 商标 grep / 1.2 时代 emoji 直出点 grep / reduced-motion 抽验 / kit 纪律 `git log` 逐文件核对 |
| 9 | 遗留登记 | 报告 | W6R1-07/09/10/12(存量或登记级,后续轮)与 W6R1-11/13(建议级;badge.ts 为已有 kit 文件,本轮冻结不改)写清理由交下一轮 |

红线自检:只动绘制函数 / CSS / 视觉常量 / kit 新增文件;判定、数值、关卡数据(含 `levels.ts` 的 ARTIFACT_EMOJI 常量本体)、存档 key、玩法测试断言一律不碰;不引入 three.js;不碰 parentAuth / parentGate;别人的报告只读不改。

## 一、修复清单

每条格式:来源 / 位置 / 修法 / 配套用例 / commit SHA。

| # | 严重度 | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 严重 | A·W6R1-03 / B·TOP-1 | `brave-path/visual.ts` `ITEM_GLYPH` | 钥匙/门/锁/终点 emoji 直出换徽章族参数化 SVG(24×24,落影椭圆 + 1.5px 描边 + 左上高光;主色 #F0C25A/#C89B6C/#8FA0D6/#F4859F 按 B 规格);`.bvp-mz-it` 类名与迷雾剪影滤镜一字不动 | `brave-path/visual.fixer.r1.test.ts` | `2352805` |
| 2 | 严重 | A·W6R1-02 / B·TOP-2 | `adventure-king/visual.ts` `drawArtifactSprite` | 文物 emoji fillText 退场,换三种原创纹石(≥2 停渐变 + 左上高光 + 描边,菱形宽高比错开做剪影差异);底圆平涂改渐变;光柱/自转闪点保留原时序 | `adventure-king/visual.fixer.r1.test.ts`(168 行) | `300f501` |
| 3 | 严重 | A·W6R1-01 / B·TOP-5 | `adventure-king/index.ts` `cellGlyph` | 古堡模式主角 🌸 → 参数化花徽 SVG,箱子 📦 → 2.5D 木箱(顶亮/侧暗/板纹);机关小图标按 A 档口径保留并登记(qa1 emoji 上限断言守住) | 同上 | `300f501` |
| 4 | 严重 | A·W6R1-04 / B·TOP-3 | `brick-break/index.ts` 胶囊绘制 | 胶囊四停纵向渐变 + 左上高光斑 + 描边;14px serif emoji 图标换六件双色小矢量;「别接」空心圈形状通道保留 | `brick-break/visual.fixer.r1.test.ts` | `efae2c3` |
| 5 | 严重 | A·W6R1-05 / B·TOP-4 | 新增 kit `src/art/kit/moleAccents.ts`(只增不改)+ `mole-pop/visual.ts` 组合 | 闪光鼠头顶天线星(剪影级差异)+ 描边星芒;盾面叠冷灰钢化层 + 深描边;16px 灰度 diffPct 0% → 5.5%(≥3% 达标,sharp 同尺复测) | `mole-pop/visual.fixer.r1.test.ts`(A 档同款 sharp 灰度管线钉死 ≥3%) | `164866c` |
| 6 | 一般 | A·W6R1-06 / B·TOP-6 | 同上 `moleAccents.ts` | 瞌睡鼠闭眼弧加粗 2.6px + 瞌睡泡加描边,16px 灰度 0.8% → ≥3% | 同上 | `164866c` |
| 7 | 一般(自查升级) | A·W6R1-08 核对 + 自查实测 | `bubble-pop/visual.ts` `bpVisualCss` 追加窄屏 media | **实测发现比「触区小」更重的问题**:360px 下盘面可用宽仅 279px,8×36px 格 + 缝要 316px,1.3 池壁新加的 `overflow:hidden` 把第 8 列裁到只剩 4.2px(320px 下整列不可见;基线是溢出到池外可见 14px)。修法:`@media (max-width:400px)` 里 `.bp-cell{min-width:0}` + 池边距 12→8px + gap 收回 4px → 8 列全可见(360 实测格宽 32.4px、裁切 0px);宽屏基础规则 `min-width:36px` 一个像素不动(既有「热区一个像素不动」断言照常绿)。**40px 触区经核对放不下 → 登记遗留**(见第二节) | `bubble-pop/visual.fixer.r1.test.ts`(media 块 + 宽屏 36px 不动 + 遗留数学钉死);取证脚本 `scripts/qa-window6/fixer-bp-cell.mjs` | `f1bc845` |
| 8 | 一般(登记转修复) | A·W6R1-12 / B·TOP-8 | `balloon-pop/visual.ts` 新增 `kindBadgeSvg` + `index.ts` `paintBalloon` 换用 | 特殊球 emoji 小图标(🎁🛡️👯🧨☁️🌈)换 12px 白底 + 1.2px 主色描边几何徽记:护盾=盾形、双子=双圆相扣、礼物=礼盒缎带、连锁=三连小圆(火药棒造型顺带退场)、乌云=三弧云、彩虹=三色拱弧;挂点沿用 LABEL_TOP_PCT=55 避主高光,far 0.72 缩放 8.6px ≥ 8px 最小可见约定;aria-label/dataset/热区一字不动 | `balloon-pop/visual.fixer.r1.test.ts`(6 条);运行时取证:第 52 关乌云球 `.blp-kbadge` 真挂上、球面 0 emoji | `40e6622` |
| 9 | 密度(B 新提) | B·TOP-7 | `mole-pop/visual.ts` 新增 `nightSceneSvg` + `index.ts` 夜场分支 | 夜场氛围补层:月牙(遮罩抠弯)+ 5 粒星子 + 两棵剪影树,全纯装饰;互斥渲染结构不变(白天关不带夜场节点),火把与洞口暖光一字不动;360 slice 裁切实测后把月牙/星子校准到 HUD 与洞区之间的可见带(栅栏线 y=58 以上) | `mole-pop/visual.fixer.r1.test.ts` 新增 describe(4 条);运行时取证第 172 关截图 | `5419d34` + `1123b86` |
| 10 | 密度(B 新提) | B·TOP-9 | `box-hamster/visual.ts` `BH_THEMES` 加 `mat` 字段 + `index.ts` `applyTheme` 消费 | 三主题舞台底纹:木屋 45° 木纹(±4%,24px 周期)/ 冰窖两粒白光斑(峰值 .08)/ 花园三瓣小花 96px 平铺(.06);全部 tint 收底、峰值透明度 ≤8%,底纹走 background 天然点不到;花园瓣用纯 radial-gradient 拼(初版 data-URI 撞了全库 qaC3fix「禁外链协议字样」硬约束,改写后守卫照常绿) | `box-hamster/visual.fixer.r1.test.ts`(5 条,含 ≤8% 透明度与禁外链钉子) | `4688e2f` + `cbaff1e` |
| 11 | **阻断(自查)** | 自查·商标 grep | `bubble-aim/index.ts` 头注释 | 注释里写着「泡泡龙玩法」——A 档 qa1 词表没有这个词所以漏网;改写为「瞄准发射三消玩法」;并新增 9 款扩展商标扫描钉子(泡泡龙/哈姆太郎/汤姆猫/愤怒的小鸟/arkanoid/吃豆人/kitty 等,与 B 档风险清单对齐,qaAudit.ts 黑名单定义豁免) | `src/games/window6.r1.fixer.scan.test.ts`(9 条) | `f7a08c9` |

统计:**阻断修 1(自查商标注释)· 严重修 5(W6R1-01~05,首任完成、本任核实)· 一般修 3(W6R1-06、W6R1-12、bubble-pop 窄屏裁切)· B 档密度建议落地 2(TOP-7/9)**。每处修复一个 commit,全部带配套用例。

## 二、遗留清单(交第 2 轮)

| 条目 | 理由 | 建议去向 |
| --- | --- | --- |
| W6R1-08 触区 36→40px | **数学上放不下**:40×8 + 7×4 = 348px > 360 屏盘面物理上限约 303px(.screen 4vw×2 + .game-stage 边框 + .l99-stage 10×2 + 池边距全吃掉后仅 279–287px)。已把窄屏从「第 8 列被裁不可见」修成「8 列全可见 32.4px」;再往上抬只能动共享壳层 padding(跨窗口)或减列数(玩法),本窗口无权 | 交统筹:壳层统一瘦身(与 W6R1-09/10 一起)或维持 32px+ 现状 |
| W6R1-07 brave-path 选项卡/背包/商店大号 emoji 图标族 | 中成本(约 40px 大图标 ×20+ 个位点),B 档建议第 2 轮做成 badge 族图标 | 第 2 轮 |
| W6R1-09/10 各款壳层按钮高 26–38px / 字号 10–13px | 存量、跨款壳层统一改;单款零敲碎打会造成 9 款不一致 | 第 2 轮统一抬(触区 ≥40、正文 ≥14) |
| W6R1-11 badge.ts 底圆 2 停渐变(花徽 vs 星徽 16px 灰度 1.6%) | `badge.ts` 是已有 kit 文件,本窗口纪律冻结不改;走「新增 kit 文件叠加」对底圆渐变这种改底色的需求不成立(叠不上去) | 第 2 轮申请解冻或由 kit 归属档执行 |
| W6R1-13 描边宽度分级约定(moleSvg 1.4 / hamsterSvg 1.2–1.3 / alien-seek 2–3.5) | 涉及冻结 kit 文件注释;且 A 档判「大尺寸观感成立」,非缺陷 | 第 2 轮写进 kit 约定 |
| adventure-king 古堡机关小图标(💡🌀🎟️ 等) | A 档口径「可留但登记」;qa1 emoji 上限断言守住不涨 | 第 2 轮视成本换绘制 |
| bubble-aim 画布 HUD ⚠️/♾️/⭐ 字符 | A/B 档均判登记级(提示与结算文案,非主体) | 第 2 轮换 12px 单色 SVG(与 starPath 同族) |
| alien-seek 藏匿点内腔渐变、bubble-pop 池壁双层/光柱 .12 | B 档第二节判「锦上添花,非必改」 | 第 2 轮顺手做 |

## 三、自查修复(第四节底线检查)

1. **商标扫描**(9 款 + kit 非测试源码,含代码注释):A 档词表 grep 0 命中;扩展词表(泡泡龙/puzzle bobble/开心消消乐/哈姆太郎/汤姆猫/愤怒的小鸟/arkanoid/吃豆人/pac-man/kitty)**抓到 1 处**——`bubble-aim/index.ts` 头注释「泡泡龙玩法」,已改写(commit `f7a08c9`)并加扫描钉子;`adventure-king/qaAudit.ts` 黑名单定义自身含词属守卫,豁免。修后全量 0 命中。
2. **裸 emoji 主角残留**:grep `fillText("<emoji>")` 与 `fillText(x.emoji)` / `P_EMOJI` 全款——主角/收集物级 0 残留(W6R1-01/02/03 修复生效);仅剩 bubble-aim 画布 HUD 三枚文案字符(⚠️/♾️/⭐,登记遗留)。balloon-pop 球面 emoji 徽记本轮清零(W6R1-12)。
3. **360/320 溢出**:构建产物 CDP 实测,首页 + 9 款(地图态 + 第 1 关场景态)共 19 采样点 × 2 宽度,**文档级横向溢出全部 0px**(脚本 `scripts/qa-window6/fixer-overflow.mjs`)。元素级命中均为设计内:`l99-tab` 章节页签是 overflow-x:auto 横向滚动器;mole-pop 场景 SVG 内部 `g`(果园草排循环到 x<372)由 viewBox slice 裁切,不外溢。**自查新发现并修复**:bubble-pop 360/320 下第 8 列被池壁 `overflow:hidden` 裁切(修复清单第 7 条,修后裁切 0px)。
4. **reduced-motion**:CDP 模拟 reduce 抽验本轮动过的 4 款——bubble-pop 装饰泡动画停、格宽 32.4px 修复生效;balloon-pop `.blp-cloudpuff` animation=none;mole-pop `.mp-face` animation=none;box-hamster 目标圈呼吸 animation=none 且木纹底纹在(静态件不受 reduce 影响)。本轮新增的夜场件/徽记/底纹全部是静态绘制,不新增动画通道。
5. **kit 纪律(`git log --follow` 逐文件)**:`src/art/kit/` 24 个文件每个都只有它的创建 commit,**无人改过别人的 kit 文件**;本轮唯一新增为首任 fixer 的 `moleAccents.ts`(合规,只被 mole-pop import)。无需恢复、无记名。

## 四、kit 纪律检查

- 结论:**干净**。逐文件 `git log --follow --oneline` 全部恰好 1 条提交(创建即最终态);跨档只有只读 import(palette.ts 被 4 款只读引用,与 A 档盘点一致)。
- 本轮 kit 变动:仅新增 `moleAccents.ts`(W6R1-05/06 载体,首任 fixer 落的);本任未动任何 kit 文件(box-hamster/balloon-pop/mole-pop 的新绘制原语都落在各自游戏目录的 visual.ts 里,不进 kit)。
- `badge.ts` 冻结导致 W6R1-11 无法本轮落地,已登记遗留(见第二节)。

## 五、合并态复验

三份报告(tester / learner / fixer)与全部修复均在分支上;`git fetch` + rebase 后无他人新提交冲突。

| 项 | 结果 |
| --- | --- |
| 复验基点(合并态 SHA) | **`cbaff1e`**(全部修复在位;本报告 commit 为 docs-only 追加,不影响以下数据) |
| `npm ci` | 通过(3.5s) |
| `npm test` | **742 文件 / 15499 用例全绿**,177.4s |
| 用例只增不减 | 基线 15379 → A 档 15440 → B 档 15444 → **合并态 15499**(fixer 净增 55 条:6 个新测试文件 + mole-pop 追加 describe;0 删除,mole-pop「场景互斥」1 条断言随夜场补层同步更新口径,数量不减) |
| `npm run build` | 通过;PWA precache **189 项 / 5232.41 KiB** |
| 构建体积 | dist **5.5 MB**(assets 5.4 MB / 176 文件) |
| `npx vite preview` 冒烟 | `/` 200 · `/sw.js` 200 · `/manifest.webmanifest` 200;首页渲染 76 张卡 0 页面错误;9 款 chunk 全部 200(brave-path 103.5KB · adventure-king 68.4KB · alien-seek 53.9KB · brick-break 44.5KB · mole-pop 41.9KB · box-hamster 39.0KB · balloon-pop 41.3KB · bubble-pop 38.0KB · bubble-aim 69.6KB) |

过程记录:合并态首跑抓到 1 红——box-hamster 花园底纹初版 data-URI 里的 `xmlns` 撞了全库 qaC3fix「禁外链协议字样」硬约束(守卫测试不动),改写成纯 radial-gradient 三瓣(`cbaff1e`)后复跑全绿。

## 六、第 1 轮视觉验收结论

- **阻断:清零。** 商标 grep(含扩展词表与代码注释)0 命中;360/320 文档级新溢出 0;自查抓到的 1 处注释商标词与 bubble-pop 窄屏裁切均当轮修复。
- **严重:清零。** A 档 5 条严重(W6R1-01~05)全部修复并有配套用例钉住(16px 灰度 diffPct、emoji 退场、渐变体积断言)。
- **一般:修 3 条**(W6R1-06、W6R1-12、bubble-pop 裁切),**遗留 4 条**(W6R1-07/08/09/10,存量或跨壳层,理由见第二节)。
- **建议:遗留 2 条**(W6R1-11/13,kit 冻结纪律所致)。
- B 档 TOP10:1–6 落地(首任)、7/8/9 落地(本任)、10 遗留(badge.ts 冻结)。
- `npm test` / `npm run build` / `vite preview` 冒烟全绿,用例只增不减,合并态 SHA `cbaff1e` 已记录。**第 1 轮视觉验收通过,遗留清单移交第 2 轮。**
