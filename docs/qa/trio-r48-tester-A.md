# 三人组 r48 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。`.l99-*` 只给 A。不改 B 游戏文件。不回退首页 `pan-x pan-y`、暂停 `.btn` 58、安全区 / `--vv-h` / CTA 回卷 / 平板 760。N-105 无第四版。

## 915×412 完整闯关（时钟小屋）

路径：首页（分类页签）→ 选关地图 → 进关 → 暂停 / 攻略。

| 面 | 滚动 / CTA | 热区 |
| --- | --- | --- |
| 首页 | `pan-x pan-y` 仍在。915 上分类+玩法/设备都排得下（`scrollWidth=clientWidth`），首卡 top=304 露在首屏 | 页签 / 顶栏 ≥44 |
| 地图 | `.l99-view` room=230 可滚到底，末格不裁。继续钮贴盒顶 top=80=viewTop，**不被裁**。wrap 680（h&lt;600 不上 760） | 继续 44；⏸ / 攻略 44 |
| 关内 | — | `.l99-back` 44 |
| 暂停 | 修前 5×58 把标题和「回首页」裁出弹窗 | 修后钮仍 **58**，标题与五钮都在窗内 |
| 攻略 | body 可滚 | ✕ / 知道啦 44，不被裁 |

## 本拍缺口与修复（N-202）

矮横屏暂停：遮罩 padding 20 + 弹窗 18 + 说明行，把「先歇一会儿」和「回首页」裁掉。`.btn` 保持 58。只在 `max-height:500px` 收 overlay/dialog 边距、藏暂停说明、钮列 gap 改为 6。不改 `dialogs.ts`。

闸：`src/ui/shell.r48.test.ts`。
