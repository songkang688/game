# 1.3 窗口 7 · 第 2 轮视觉验收 · 测试员报告

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 2 轮视觉测试员（A 档）。只写本报告与本窗九款视觉 `*.test.ts`，不改绘制代码、不改玩法/数值/关卡，不碰 A/B/C 第 1 轮三份报告。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 〇、复验计划（开工首 commit）

本轮定位 = 复验 + 深挖，两条主线：

1. **逐条对账**（主责）：把第 1 轮 A 档报告 13 条问题（严重 6 / 一般 5 / 建议 2）+ B 档 1 条（B-10 双虫），对照 C 档修复清单（P1–P12 + B-10，宣称修 12 条 / 遗留 4 项），每条给四选一结论：已修复（附验证证据）/ 修复不彻底 / 修出回归 / 合理遗留。
2. **九款深度走查**：角色特写（2× 与 16px）/ 动效完整链（预告→过程→收尾 + reduced 退化）/ 场景层次（背景/中景/主体/HUD 四层）/ kit 一致性（左上 45° 光源、描边、粉彩板）/ 性能（粒子峰值预算）。重点深挖第 1 轮严重项对应款：`poop-hero`、`puzzle-tiles`、`landlord-cards`、`fruit-slice`。

流程：`npm ci` → `npm test` → `npm run build`（对账第 1 轮合并态 735 文件 / 15384 用例 @ `f8b944a`，只增不减）→ 六大专项两轮达标率对比 → 回归防线三查（判定热区被挪动 / 功能提示被动画吃掉 / 教育玩法数据被改）+ destroy 复查 → 新发现问题量化清单 → 补 round2 守门用例（只写 `*.test.ts`）→ fetch → rebase → 全绿 → 普通 push。

红线：禁止 force、不改 `main`、不动 `game-1.2`、不用 `gh` 开 PR；只验视觉，不重开玩法验收。

（以下各节待本轮执行后回填。）
