# 1.3 窗口 7 · 第 2 轮视觉验收 · 测试员报告

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 2 轮视觉测试员（A 档）。只写本报告与视觉 `*.test.ts`，不改绘制代码、不改玩法/数值/关卡，不碰 A/B/C 第 1 轮三份报告。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。
> 扫描环境：node 22 / vitest 4.1.11 / vite 8.2.2；node 环境无浏览器，动态验证以「可执行用例 + 源码定量扫描 + blame 溯源」落地。

## 〇、复验计划（开工首 commit，`8a60654`）

1. **逐条对账**（主责）：第 1 轮 A 档 13 条问题（严重 6 / 一般 5 / 建议 2 / 登记 2 计入附带）+ B 档 1 条（B-10），对照 C 档修复清单（P1–P12 + B-10），每条四选一：已修复 / 修复不彻底 / 修出回归 / 合理遗留。
2. **九款深度走查**：角色特写（2× 与 16px）/ 动效完整链 / 场景层次 / kit 一致性 / 性能预算；重点深挖 `poop-hero`、`puzzle-tiles`、`landlord-cards`、`fruit-slice`。
3. 回归防线三查 + destroy 复查 + 六大专项两轮达标率对比 + round2 守门用例。

## 一、全量跑数据 + 两轮用例数对比

| 项目 | 结果 |
| --- | --- |
| `npm ci` | ✅ 无报错 |
| `npm test`（开工态，对账基线） | ✅ **735 文件 / 15384 用例全绿**，178.7s —— 与第 1 轮合并态（C 档报告 735/15384 @ `f8b944a`）**逐位一致，Δ=0**（其间只有 docs 提交），只增不减 ✅ |
| `npm run build`（tsc --noEmit + vite build） | ✅ 全绿，708ms；PWA 预缓存 **190 项 / 5249.04 KiB**（与 C 档合并态一致） |
| 本轮新增用例 | **+1 文件 / +13 条**（`src/games/qaW7r2.test.ts`，见第四节末），收尾态 **736 文件 / 15397 用例** |

两轮用例链条：R1 开工基线 698/14996（`origin/game-1.3`）→ R1 A 档 716/15275 → B 档 727/15350 → C 档合并态 **735/15384** → **本轮 736/15397**，全程单调递增 ✅。

第 1 轮机器化扫描用例存活性全查：9 个 `qaW7r1.test.ts` + 8 个 `qaW7r1Fix.test.ts` + `art/kit/pattern.test.ts` + `copyW7r1.test.ts` **19 个文件全部在跑、全绿**（emoji 直出断言 / 灰阶差断言 / 视差系数断言 / 字号全量扫描断言均未被删改）。

## 二、第 1 轮问题逐条对账表（本轮主责）

结论口径：**已修复**（附验证证据）/ **修复不彻底** / **修出回归** / **合理遗留**。全部逐条重验代码现状，不采信报告自述。

| # | 级别 | 款 / 问题 | C 档 commit | 对账结论 | 验证证据（本轮实测） |
| --- | --- | --- | --- | --- | --- |
| A-1 | 严重 | puzzle-tiles 牌面 emoji 直出 | `995ab45` | **已修复** | 五渲染路径（renderSlide 412 / renderRotate 428 / renderFill 455·459·470 / 拖块 722 / 预览 341·347）全走 `skinFor` 内嵌 `patternSliceNestedSvg` 或 `patternSliceSvg`；`tile.emoji` 上屏 0 处（rg 全量），仅存的 `pzv-face` 是缺块洞功能问号「？」；干扰块走 alt 场景；`pattern.test.ts` 9 条 + `qaW7r1Fix` 8 条钉死 |
| A-2 | 严重 | landlord AI 头像裸 emoji | `56179c1` | **已修复** | `BOT_FACES` 存 face 键（`tuantuan`/`yuanyuan`），渲染唯一走 `botFaceSvg`（visual.ts:192-224）：24×24、2 停渐变左上亮、1.5px 描边、左上高光、耳形几何差（长耳 vs 圆耳+眼周深块）；`.ldv-botsvg{pointer-events:none}` 热区不动 |
| A-3 | 严重 | poop-hero 地面垃圾裸 emoji | `7fa3398` | **已修复** | `drawTrashItem` 18 款条目（trashArt.ts:107+）：每款 ≥2 停渐变 + 1.7px 有效描边（小画幅下限 1.2px）+ 左上 gleam；index 1568 地面 + 1716 头顶携带件双接线；不认识 id 有兜底 |
| A-4 | 严重 | poop-hero 香香星裸 ✨ | `eeae31c` | **已修复** | `drawScentStar`（trashArt.ts:56-79）：kit `traceStar` + 3 停径向渐变 + 描边 + 左上小高光；index 1525 接线，原 19px 字号→星半径 9.5 尺寸不变 |
| A-5 | 严重 | poop-hero 分类桶图标裸 emoji | `11c2259` | **已修复** | `drawBinIcon`（trashArt.ts:543+）三枚白系自绘图形互不同形；index 1587 接线 |
| A-6 | 一般 | fruit-slice 果王头顶 emoji 身份牌 | `6b8e9eb` | **已修复** | `drawKingCrown`（visual.ts:383+）三尖齿渐变 + 1.5px 描边 + 珠色分果王（金/粉/蓝身份通道）；index 2102 随本体同角度摆；`fillText(k.spec.emoji)` 清零 |
| A-7 | 建议 | poop-hero 场内装饰 emoji 6 处 | `2f3c182` | **已修复** | 💫🫧⭐🤔🧽🧼🔒 + 粒子文本全自绘（trashArt.ts:632+ `drawParticleGlyph` 等）；`emoji()` 工具函数连根删除（index.ts:327 注释存证）；canvas `fillText` 残留仅功能文本（"香喷喷!"、◀▶ 14px 方位标、开场卡文字） |
| A-8 | 建议 | lianliankan `MASK_FACE` 死常量 | `5c95546` | **已修复** | board.ts 无 `MASK_FACE`、源码 ❓ 清零；判定层 `pickMasked`/`maskKey` 原样（qaW7r1Fix 断言）；本轮复核 `logic.ts` 仍只有 R1 已核准的 240/200 时长差 |
| A-9 | 严重 | poop-hero 双人 16px 灰度不可分 | `d12db80` | **已修复** | 星星披风 `#6690E0`：Rec.601 灰度 ≈140 vs 朵朵 `#F4859F`≈169，**Δ≈29 ≥25**（本轮独立复算）；星星发卡 index 1029-1039：**不走 showDetail 门槛**、`max(2, headR*0.55)` 恒画、伸出头圆改变剪影；边缘方位标 HERO_COLORS 同步 |
| A-10 | 一般 | fishing-star 测深/瞄准/风向 10–13px | `a56e08e` | **已修复** | index 1020/1040/1356/1360 全部 14px（本轮全量再扫 canvas `font=` 无 <14px）；标签小牌随字加宽加高 ≈184px<320 不溢出 |
| A-11 | 一般 | poop-hero 桶签 8–9px + 门帘/箭头 11–12px | `11c2259` | **已修复** | 桶签文字删除（图形化）；门帘计数 `max(14,…)`（1638）、方位箭头 900 14px（1705）；A-11 点名的 1093/1142/1677 三处全清。⚠️ 开场横幅另有 12–13px 两处系 R1 漏扫、非 A-11 范围，立新项 N-2（第四节） |
| A-12 | 一般 | fruit-catch 双人名牌 13px | `78a321f` | **已修复** | index 740 `bold 14px`；qaW7r1Fix 全量扫描断言画布再无 <14px |
| A-13 | 登记 | fruit-slice 菜单说明行 13px（1.2 遗留） | 未修 | **合理遗留** | index 2611 仍 `13px sans-serif`，1.2 遗留非 1.3 产物，维持登记级。⚠️ C 档遗留清单（4 项）漏登记此条，本轮补录进遗留账，交 round2 C 档顺手清或继续登记 |
| A-plan | 一般 | plan 过程文档缺档 ×4（20-A/20-C/21-A/21-C） | 未修 | **合理遗留** | 本轮复查 `docs/qa/` 仍无这四格 plan 文件；C 档遗留清单第 1 项已登记（不代写他人 plan），理由成立 |
| B-10 | B 档建议 | snake-snack 双虫 16px 灰度 Δ12 | `f8b944a` | **已修复** | kit `CatLook.bow` 可选字段（caterpillar.ts:148/338-345，既有默认输出不变）；粉虫深梅 `#9B4E86` 蝴蝶结、绿虫不戴；**不吃 12px 触角门槛**；qaW7r1Fix 量化断言（小头径下粉虫比绿虫多 3 次填充） |
| C-遗留 2 | — | puzzle-tiles blurb 文案未跟上「场景画」 | — | **合理遗留（确认在账）** | meta.ts blurb 仍是「拼块带纸纹齿边像真拼图」，牌面已换场景画但文案未更新——属 B 档职责，C 档遗留清单第 2 项在账未丢，交 round2 B 档 |
| C-遗留 3 | — | HUD/菜单文案级装饰 emoji（数百行） | — | **合理遗留（确认在账）** | 验收口径「装饰性可留」；本轮复扫九款 DOM 装饰 emoji 仍在该口径内 |
| C-遗留 4 | — | destroy 真机「进出 ×5」 | — | **合理遗留（确认在账）** | node 无浏览器；本轮加做静态复查：**12 个 R1 修复 commit 逐个 diff 审计，新增计时器/监听器 0 处**（`995ab45` 的 1 处命中是上下文行非新增），修复全部是纯绘制字符串/纯函数,泄漏面零扩大 |

**对账统计：已修复 13 条（严重 6/6 + 一般 4 + 建议 2 + B 档 1）· 修复不彻底 0 · 修出回归 0 · 合理遗留 5 项（plan 缺档、A-13 补录、C 档遗留 2/3/4）。第 1 轮阻断/严重级修复确认率 6/6 = 100%。**

## 三、九款深度走查表（五项 × 九款）

重点深挖款（R1 严重项对应）：poop-hero · puzzle-tiles · landlord-cards · fruit-slice。走查以源码逐行 + 常量复算 + 既有用例链为证据；无浏览器处以代码预算注明。

| 款 | 角色特写（2× / 16px） | 动效完整链（预告→过程→收尾 + reduced） | 场景层次（背/中/主/HUD） | 一致性（左上光/描边/粉彩） | 性能（粒子预算） |
| --- | --- | --- | --- | --- | --- |
| fruit-catch | ✅ kit 六剪影 3 停渐变（+18%/−14%）+1.5px 描边，全矢量 2× 无损；16px 靠形状通道（弯月/团簇/倒滴） | ✅ 红圈脉动+慢旋预告 → 篮压扁 8%/120ms → 星屑 4 颗 280ms+飘分 0.7s / 落空弹地 240ms；reduced：旋停/压扁 0/星屑 0/**红圈保留**（本轮 qaW7r2 源码级钉死） | ✅ 天空日月→云两档视差 0.1/0.18→树枝草地→玩法→DOM HUD | ✅ 纯函数动效（fcSpinAngle 等）；kit 光源左上 | ✅ 星屑 4×280ms 自灭、篮内裁剪 ≤3 |
| fruit-slice | ✅ 五果专属剪影（桃顶凹 0.12r/瓜 1.25:1/橙凸脐/莓倒滴/柠两尖）外接 ≤1.15r；**新皇冠 2× 细节完好、珠色三分**；16px 剪影互不混 | ✅ 抛果上升弧预告 → 刀光丝带 16 段+切半旋飞 → 液滴 3 颗 300ms+星花+连击字；reduced：TRAIL 5 段/0.06s、FX_FADE ×5、**星花保 1 帧功能反馈** | ✅ 砧板渐变+纱幕薄背景（题材基准即薄）→果/刀光→HUD | ✅ 果身亮心 −0.35r/−0.4r 左上；描边 ≥1.5px | ✅ 稳态峰值 ≈9–12 颗液滴；halves 寿命+出屏双裁剪 |
| snake-snack | ✅ 头身比 0.42（头>身节）+大眼白黑瞳；触角 12px 门槛（qaW7r2 对表）；**粉虫蝴蝶结剪影级豁免**，双虫亮度+几何两通道 | ✅ 食物待机浮动预告 → moveT 插值+张嘴 → 吞咽鼓包波+金闪 1 帧 step；reduced：moveT=1 瞬到位、金闪保留 | ✅ 双色棋盘（明度差 4%）+栅栏→场景件→虫→HUD | ✅ `ball()` 亮心 −0.35r 左上；kit caterpillar 单源 | ✅ 金闪/鼓包为有限帧状态,无粒子池 |
| lianliankan | ✅ kit icons 16 枚一形一色：双色渐变+1.5px 描边+（34,30）左上高光;面具唯一走 `maskFaceSvg()` | ✅ 选中脉冲预告 → 流星光带 240ms 沿判定折线 **1:1 映射**（热区零挪动）→ 星尘 3 颗交错 40ms 收尾；reduced：calm 折线一帧、提示柔光保留 | ✅ 木纹桌面+四角茶点→牌阵→流星覆盖层→HUD | ✅ 渐变 0,0→1,1 左上；粉彩 | ✅ 星尘 3×240ms 自灭 |
| puzzle-tiles（深挖） | ✅ **牌面=场景画**：三层构图（2 停天空+左上光斑圆盘 .35/.35+远近地形带 1.5px 描边）+每格贴纸+边饰圆点；齿径 <40px 降 14%（qaW7r2 对表）；16px 预览格 viewBox 照裁 | ✅ 拾起抬升预告 → 拖动/swap → 放对回弹+接缝白光 / 放错摇头 + 完成装裱收尾；reduced：抬升/摇头/装裱停、**接缝白光保留**（dropFxClasses） | ✅ 画室渐变→齿边皮肤→切片画→纸纹渐变盖层→HUD；记忆关 `pz-hidden` 连切片一并藏 | ✅ pattern 确定性 PRNG 同关同画；皮肤 `pointer-events:none` 热区零像素挪动；alt 干扰块同族不撞图 | ✅ PtFx 计时包统一记账 destroy 一把清；无粒子池 |
| memory-cards | ✅ 牌背双环 0.62/0.78+1.5px 金线+徽章；<48px 角标降级（qaW7r2 对表）；图标 drawIconDeluxe 描边+高光 | ✅ 选中预告 → 3D 翻转 200ms（reduced→短淡入 flipDuration）→ 配对相碰+星屑+星星印章收纳态;波浪翻回 30ms 步进封顶 690ms、**startPlay 解锁时机零改动**；reduced：waveDelay=0 瞬到位 | ✅ 渐变背景→卡阵→FX→HUD；桌面四角素净（B 档可选装饰未做,维持可选不立项） | ⚠️ 本地 `shade` 为第三种量纲（乘法调暗）→ 归入 N-4 建议 | ✅ CSS 动画+timeouts 记账（destroy 689-699 清） |
| landlord-cards（深挖） | ✅ 牌面七道工序;大小王金银框+立绘+角标多通道（qaW7r2 宽窄两档断言）;**AI 头像 16px 耳形几何差可分**;2× 下渐变/高光/腮红完好 | ✅ 轮次呼吸+可出牌抬升预告 → 出牌飞行（anim.ts 单状态机）→ 落桌软影 1 帧 step+炸弹震屏 ±2px/160ms+星屑环 8 颗收尾;reduced：不震屏、星屑环静态保留、飞行→同时序短淡入 | ✅ 毛毡渐变+木沿→座位→桌面→扇形手牌→FX→HUD（LD_LAYERS 钉死）;打牌区简洁化符合题材基准 | ⚠️ `.ldv-kribbon` 9px 缎带是 1.3 新增小字 → 立项 N-1;其余 token 单源 ✅ | ✅ RING_STARS=8 封顶;ghost 渐隐 `both` 自清 |
| fishing-star | ✅ kit 纺锤三停（背深腹浅）+花纹 15px 门槛+深水去饱和;25 鱼全参数化;浮标红白功能剪影 | ✅ **涟漪加密 ×2+浮标点头 160ms/3px 双信号预告**（qaW7r2 功能断言:reduced 点头静态满位移保留、窗口外恒 0）→ fight 拉扯+竿弯线性 0.22 → 水花+金光+图鉴收尾 | ✅ 天空远山→云两档视差→岸→水面波光→深水渐变+光柱→鱼群→HUD（FSH_LAYER_ORDER） | ✅ 左上光;深度变色符合行业规律 | ✅ 气泡 ≤6、波光 2 条（5200/6800ms）、涟漪按 gap 自灭 |
| poop-hero（深挖） | ✅ 双人披风灰度 Δ≈29（复算）+星星发卡 min 2px 恒画改变剪影;2× 十道工序（渐变披风/菱形高光/腮红/4 帧腿摆）;18 款垃圾条目 1.7px 有效描边+左上 gleam | ✅ 豆豆怪 bloom 预备帧预告 → 冲刺清扫+扫帚弧+残影 → 干净带+星花+连击徽章脉冲收尾;reduced（gentle）：残影 0/呼吸停/尾流 0/**扫帚弧保静态 0.5 帧**、腿摆降 2 帧 | ✅ 天空渐变→远景 0.16+云 0.18→中景 0.45→玩法 1.0→HUD;`PH_LAYERS` 与视差系数被 qaW7r1 钉死;近景前景层（0.8–0.9）仍缺=B 档可选建议维持登记 | ✅ 左上光（胸口菱形高光/trashArt gleam）;`shade` 百分比量纲 → 归入 N-4 | ✅ 残影硬顶 6+尾流硬顶 6;粒子字形短寿命自灭 |

九款结论：**深度走查五项 × 九款 = 45 格，43 格 ✅、2 格 ⚠️（landlord 9px 缎带、shade 三量纲）**，均为一般/建议级,无阻断无严重。

## 四、本轮新发现问题清单（量化）

| # | 级别 | 位置 | 现状 | 期望 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| N-1 | 一般 | `src/games/landlord-cards/visual.ts:292`（`.ldv-kribbon`） | 大小王「大/小王」缎带 **font-size:9px**，1.3 本窗 22-A 新增（blame `e48a8a2`）,是九款样式表里唯一一处 1.3 新增 <14px;身份另有金银框+立绘+角标三通道（qaW7r2 已断言）,不至严重 | 提到 ≥14px,或改图形化徽记;窄牌已砍缎带的降级逻辑保留 | `src/games/landlord-cards/visual.ts` |
| N-2 | 一般 | `src/games/poop-hero/index.ts:1756/1760` | 开场横幅关卡提示 `13*max(0.85,scale)`px、卫生小知识 `12*max(0.85,scale)`px——360px（scale≈1）下 12–13px 功能文字;1.1 遗留（blame `d4d855d`）,**第 1 轮 canvas 小字清单漏扫**,不在 A-11 点名范围 | 两处统一 ≥14px（同款门帘计数 `max(14,…)` 的写法照抄即可） | `src/games/poop-hero/index.ts` |
| N-3 | 登记（报告纠偏） | 九款样式表 | 第 1 轮 A 档「样式表字号九款 **0 处 <14px**」结论**不实**:本轮全量重扫,九款样式表 <14px 共 **84 处**（landlord 32 / fishing-star 29 / poop-hero 16 / snake 2 / puzzle 2 / memory 1 / fruit-catch 1,含 320–360px 收紧档）;blame 抽查证实除 N-1 外全部 1.1/1.2 遗留（`f0797d4`/`e314d32`/`9c25e41`/`baecd09`/`8cd8362`/`4e79f1f`/`cb3e9de`）,非 1.3 回归 ⇒ 维持「阻断 0」,但第 1 轮该句口径记入纠偏 | 遗留量登记在案;若后续轮开「360px 阅读性」专项再统一排格,不在本轮视觉验收范围内立即翻修 | （登记项,无单一责任文件） |
| N-4 | 建议 | `src/art/kit/fruit.ts:41` / `src/games/poop-hero/visual.ts:19` / `src/games/memory-cards/visual.ts:53` | 同名 `shade` 三份实现三种量纲:kit=0–1 小数混白黑（|amount|≥1 直接纯白/纯黑）、poop-hero=百分比、memory=小数乘法调暗。现有调用各自正确（pattern/icons 只传小数,qaW7r2 已断言）,但跨模块照抄参数会产出纯黑/纯白色块 | 后续轮归一到 kit 单源（或改名区分）;本轮 qaW7r2 已把三家语义与「kit 传百分比=纯黑」的踩坑面钉进用例,回退/混用会红 | `src/art/kit/fruit.ts` 等三处（归属格协调） |
| N-5 | 登记（对账补录） | `src/games/fruit-slice/index.ts:2611` | R1 问题 A-13（菜单说明行 13px,1.2 遗留）在 C 档遗留清单里漏登记 | 补录进遗留账（本表即为登记）,round2 C 档顺手清或继续登记 | `src/games/fruit-slice/index.ts` |

**本轮新发现统计:阻断 0 · 严重 0 · 一般 2（N-1/N-2）· 建议 1（N-4）· 登记 2（N-3/N-5）。** 阻断级当轮清零 ✅（本就为 0）;N-1/N-2 移交本轮 C 档。

回归防线三查（本轮专项）:

- **判定热区被挪动:0 处。** 12 个修复 commit 全部是 canvas 绘制 / `pointer-events:none` 皮肤层 / SVG 字符串;puzzle 齿边皮肤绝对定位不占盒模型,lianliankan 流星折线对判定拐点 1:1 只读映射,landlord 头像 SVG `pointer-events:none`,fishing 标签小牌是 canvas 内绘制。
- **功能提示被动画吃掉（reduced）:0 处。** fruit-catch 警告红圈 calm 只停脉动圈保留、fishing-star 浮标点头 reduced 静态满位移、puzzle 接缝白光保留、landlord 星屑环静态保留、poop-hero 扫帚弧静态帧保留——前两处本轮以 qaW7r2 新用例钉死,后三处沿用 R1 用例全绿。
- **教育/玩法数据被改:0 处。** 全量 15384 条玩法断言全绿;lianliankan `logic.ts` 复核仍只有 R1 已核准的 240/200 时长差;memory `startPlay` 解锁时机、puzzle `THEME_TILES` 关卡数据、landlord 判定层均一字未动。
- **destroy 泄漏复查:** 12 个修复 commit 新增计时器/监听器 **0 处**;R1 的池级 `clear()` 用例（JuicePool/SparklePool/PhFx/PtFx）全绿;真机 ×5 维持遗留第 4 项。

本轮新增守门用例:`src/games/qaW7r2.test.ts`（4 describe / 13 it,全绿）——① reduced 危险提示保留（红圈/浮标/涟漪）;② shade 三实现量纲钉死+kit 内调用扫描;③ 小尺寸降级门槛对表（48px 角标/12px 触角/40px 齿径/头身比）;④ 大小王身份多通道宽窄两档。

## 五、六大专项两轮达标率对比

| 专项 | 第 1 轮（修复前） | 第 2 轮（本轮复验） | 变化 |
| --- | --- | --- | --- |
| ① 无火柴人/emoji 平涂 | 6/9（puzzle-tiles、landlord、poop-hero ❌ 共 5 条严重） | **9/9** ✅（五条严重全修,守门用例钉死;水果类零 emoji 平涂维持） | +3 款 |
| ② 金币/收集物体积 | 8/9（poop-hero 香香星平涂 ❌） | **9/9** ✅（drawScentStar 3 停渐变+描边;18 款垃圾条目同规格） | +1 款 |
| ③ 双人 16px 可区分 | 2/3（poop-hero 灰度 0 通道 ❌;另 snake 双虫 Δ12 隐患） | **3/3** ✅（披风 Δ≈29+发卡;snake 蝴蝶结加固为亮度+几何双通道） | +1 款+1 加固 |
| ④ 2.5D 纵深（点名 poop-hero） | 1/1 ✅（0.16/0.45/1.0 三档） | **1/1** ✅ 维持（视差系数+图层序用例仍绿;近景前景层缺口维持可选登记） | 持平 |
| ⑤ 360px 布局 | 报「达标」但 canvas 小字 4 款一般（10/11/12/13 号问题） | 4 条全修 ✅;**但本轮深扫翻出 R1 漏扫面**:1.3 新增 9px 一处（N-1）、canvas 12–13px 两处（N-2,1.1 遗留）、样式表遗留 84 处登记（N-3）⇒ 按「1.3 新增无阻断」口径 **8/9**（landlord 待 N-1 清零) | R1 问题清零,口径纠偏后揭出存量 |
| ⑥ 商标/官方角色 | 9/9 ✅ 0 命中 | **9/9** ✅（品牌词全量 grep 仍 0 命中;**R1 修复新画素材逐一造型点检**:pattern 七种几何原语无城堡/角色剪影、botFace 原创耳形、皇冠无标识组合、蝴蝶结深梅色避开「绿身红头」绘本特征、trashArt 18 款皆日用品几何) | 持平（新增素材复检通过） |

分级红线顺带复核:失败文案全鼓励、无血/伤害/死亡、无广告内购 ✅（copyW7r1 黑名单用例全绿）。

## 六、收尾与交接

- 本轮改动面:本报告 + `src/games/qaW7r2.test.ts`（13 条用例）,**绘制代码 0 行、玩法 0 行、他人报告 0 字**。
- 移交本轮 C 档:N-1（landlord 9px 缎带）、N-2（poop-hero 开场卡小字）;N-4 建议归一 shade(可后续轮);N-3/N-5 登记在案。
- 移交本轮 B 档:C 档遗留第 2 项（puzzle-tiles blurb 更新成「场景画」表述）。
- 三方合并态复验由本轮 C 档执行,合并态 SHA 由 C 档写入其报告,本档已把全绿基线（736/15397 + build 全绿）钉在分支上配合复验。
