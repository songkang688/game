# 窗口 8 · 1.3 视觉升级 · 第 1 轮视觉测试员报告

- 角色：窗口 8 三人验收组 · A 测试员（第 1 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围：本窗 12 款（不做全库 39/76 款）——
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只改 `*.test.ts` 与本报告；不改绘制实现代码；kit 文件只 import 不改。

## 扫描计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积。
2. 盘点：`src/games/` 12 款目录、`src/art/kit/` 文件归属（对照窗口 8 派工表独占约定）、`git log --follow` 抽查越界改动；12 款 plan 文档 vs 实际提交对账。
3. 六大专项（范围锁死本窗 12 款）：
   - ① 火柴人 / 草稿圆：grep `fillText(` emoji 直出与纯圆两点眼画法，重点回查 `red-blue-race` 1.2 时代头像圆片。
   - ② 金币体积：12 款全部收集物 / 星星 / 宝石绘制函数需 ≥2 停渐变 + 高光 + 描边/落影之一。
   - ③ 双人可区分：`red-blue-race` / `red-blue-tap` / `red-blue-tug` 16px 灰度对比，剪影/头饰/附件三通道至少两通道可分。
   - ④ 2.5D 纵深：本窗点名款 `red-blue-race` 赛道透视必须 2.5D。
   - ⑤ 360px 布局：12 款 360×640 与 320px 极限实测，记录溢出像素 / 字号 <14px / 触区 <40px。
   - ⑥ 商标扫描：12 款文案 + 代码扫描；注意 `kitty-care` 的 `upgrade12.test.ts` 中「汤姆猫 / hello kitty」是商标扫描测试名单，不是产品文案。
4. 横向一致性：kit 光照约定（左上 45°、描边 1.5–2px）、`prefers-reduced-motion`、destroy 泄漏（进→退→再进）、粒子性能。
5. 新增机器化扫描用例沉淀给第 2/3 轮。
6. 产出本报告：六专项各一节 + 12 款 × 六专项结果矩阵（一格一个 ✅/❌），问题量化到像素 / 帧率 / 文件+函数。

（以下为扫描结果，扫描完成后填写。）
