# 1.3 窗口 2 · 第 3 轮视觉验收（终验） · 学习优化员报告

> 角色：窗口 2 第 3 轮终验学习优化员（B 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货态：`7fbbdbe`（r3 tester 终验放行，合并态 `f0d2ef9`，门禁 746 文件 / 15312 例、build 绿 188 entries / 5215.41 KiB）。
> 范围：只评本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 权限：只写本报告 + `docs/qa/1.3-window2-style-baseline.md` + 本窗 9 款纯文案 + 为文案补的快照测试。绘制代码一行不碰；不改 `src/styles.css`；A 档 `...-tester.md` 与 C 档 `...-fixer.md` 一字不碰。
> 红线遵守：学习只记结论文字，未下载、未截图入库、未描摹任何具体造型。

## 终验计划（动手前登记，`83e476f`→rebase 后 `c1874d6`）

1. **采纳率终账**：r1 TOP10 + r2 修订清单 10 条做最终对账（采纳 / 部分采纳 / 未采纳），未采纳逐条写「同意放弃」或「升级为终验条件」；逐条读码复核，不抄 A 档。
2. **风格基线固化**：产出 `docs/qa/1.3-window2-style-baseline.md`——粉彩色板 token、描边档位、左上光照、动效三段式与 reduced 退化、道具体积、双人可区分、精 2D 可读性；每条注明出处（文件 + 函数名）。不写 2.5D/透视当规范。
3. **商标终审**：题材 → 易踩线对象 → 规避要点，9 款全目录对照，黑名单含 4399 / 马里奥 / 皮卡丘 / 托马斯 / Hello Kitty / Battle City / 官方四色幽灵 / 军徽制式 / 麻将纹样等；给出清零或残留结论。
4. **首页整体感终评**：只评 9 款入口卡片；确认 r2 点名违和款归队；仍违和的点名交 C 档。
5. **文案终查**（唯一代码改动范围）：r1 11 句 + r2 6 句在位性复查（丢了补回）；文案与终态画面一致性；错别字与语气分级；每句 ≤64 字、general 3–6、只增不减；补快照测试 `src/games/__tests__/window2-r3-learner-copy.test.ts`。
6. 收尾：fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push。禁 force。

---

## 一、两轮建议采纳率终账（20/20 对账，全部读码复核）

口径：r1 TOP10 + r2 修订清单 10 条，共 20 条。三档结论：**采纳 / 部分采纳 / 未采纳**；「部分采纳」只用于终态仍有余量未落地的条目（本轮为 0——凡两轮分步补齐的都以终态计「采纳」）。每条的「本轮复核」列为本档第 3 轮独立读码证据，不转抄 A 档。

### r1 TOP10

| # | 建议 | 终账 | 本轮读码复核证据 |
| --- | --- | --- | --- |
| 1 | junqi-camp 双方形状通道 + 灰度差 ≥40 | ✅ 采纳 | `junqi-camp/art.ts:260 sideMarkSVG()`、`view.ts:70–71/113 jq-mark`（左下 11×11px）、`view.ts:125` 星星面 `#C4D8F4→#93B2DE`（r2 依我 #8 把底停压到 Δ42.8 ≥40，达 r1 原始规格）。两轮分步、终态足额 |
| 2 | mine-garden 小地图 hidden 失效 | ✅ 采纳 | `index.ts:388 .mn-mini[hidden]{display:none}`、`:657 if (wide) drawMini()`、`:797 if (!mini.hidden)`——UA 语义 + 宽盘判定 + 重绘守卫三处齐全 |
| 3 | dark-chess 360px 8 列挤爆 | ✅ 采纳 | `view.ts:40 .dc-cell{aspect-ratio:1/1;min-width:0;min-height:0}` + `:46 ::before{inset:min(0px,calc((100% - 44px)/2))}`。r1 只落了一半出 r2-1 回归，r2 按我拆解的另一半（放掉 min-height、扩展区接住 44px）收尾；A 档 r3 浏览器实测 0 互叠、名义热区 44px、32/32 中心命中。终态计采纳 |
| 4 | chess-garden 棋子主体补渐变层 | ✅ 采纳 | `art.ts:110 duoTone()`——描边整形→暗纱 `rgba(56,34,16,.14)`→matrix(.92) 顶部内缩亮层，六个 `*Inner()` 全上、零 defs/id |
| 5 | dark-chess defs 固定 id 撞车 | ✅ 采纳 | `art.ts:29 backSVG()` 三停层叠实心（零 defs）；`art.ts:65` `` gid = `dcIvory-${color}-${kind}-${uid}` ``——两案各用一半，比单案更稳 |
| 6 | dot-maze 幽灵剪影差异化 | ✅ 采纳 | `art.ts:435 tooth = r/1.5`、`:440 for k<3`（三尖裙边）、`:452` 呆毛 quadraticCurve、`:448 lineWidth max(1.5, r*.12)`——与我 r1 规格逐数一致 |
| 7 | HUD 小字统一提级 ≥14px | ✅ 采纳 | 本轮全目录机器复扫：9 款自身源码 `font-size:*px` **0 处 <14px**（r1 六款 6 commit + r2 收 7 条尾巴后的终态）；r3 tester 已钉成每款 `round3-final-verify` 永不回降总闸 |
| 8 | chess-garden 坐标与角标字号 | ✅ 采纳 | `src/styles.css` cg- 追加段 `.cg-wrap .cg-coord{font-size:10.5px}`、`.cg-wrap .cg-piece-tag{font-size:12px}`（2620s）——40px 格内辅助标注口径，A 档核准豁免并登记（r3 备 2） |
| 9 | fruit-stack 双盆沿座位色内衬 | ✅ 采纳 | `index.ts:477 drawRim()`、`:508` seat 0 `#a8306a` / seat 1 `#28568f`——落法（两侧竖条）与我规格（上沿弧段）不同但效果等价、更省，r2 已登记 |
| 10 | merge-2048 盘外星点静饰 | ✅ 采纳 | `index.ts:147–148` 两颗淡星点 radial（`#FFD75E14`/`#D0A9F512`）逐值与规格一致；「radial 总数恰 6」克制契约在跑 |

### r2 修订清单

| # | 建议 | 终账 | 本轮读码复核证据 |
| --- | --- | --- | --- |
| 1 | dark-chess 格互叠收尾（支持 A r2-1） | ✅ 采纳 | 同上表 #3；我提醒的「相邻扩展区 DOM 序仲裁」经 A 档 r3 两档 32/32 全量实测无误吞 |
| 2 | chess-garden 320px h 列（支持 A r2-2） | ✅ 采纳 | `src/styles.css` cg- 段 `.cg-wrap .cg-sq{min-width:0;min-height:0}`；A 档 r3 实测 320px 格宽 36.5px、h 列全宽可见、64/64 中心命中 |
| 3 | 字号尾巴一次收净（A r2-3~r2-7） | ✅ 采纳 | `pool-stars/view.ts:92 fontPx: 14`（删视口分支）；`dark-chess/index.ts:35/43/45` dc-sub/dc-tip/dc-pick 14px；`chess-garden/index.ts:61/66` cg-pick/cg-over-s 14px；`dark-chess/view.ts:106` dc-count span 14px；`junqi-camp/view.ts:120` jq-empty 14px；styles.css `:2653` cg-log-sum/row 14px——七处全中，零豁免登记需要 |
| 4 | pool-stars 壳卡 | ✅ 采纳 | `view.ts:197` `linear-gradient(180deg,#EFF7F0,#E7F1EA)` + 16px 圆角 + `padding:10px 6px`——侧衬 6px 为 C 档按 320px 竖版台面 cssW 封顶 280px 的几何实测收敛，偏离我给的 10px 有据，认可 |
| 5 | fruit-stack 壳卡 | ✅ 采纳 | `index.ts:65` `linear-gradient(180deg,#FFF4F8,#FBF0FF)` + `padding:10px 0`——侧衬 0 为双盆 layout() 按 host.clientWidth 分配的几何约束，有据，认可 |
| 6 | chess-garden 壳卡 | ✅ 采纳 | styles.css `:2664` `linear-gradient(180deg,#f2f7ea,#eaf3e4)`、`padding:10px 6px`、≤420px `padding:10px 0`——按我要求排在 r2-2 修复之后、窄屏内衬归零不动摇格宽实测值 |
| 7 | merge-2048 方块静饰改圆点 | ☑ **未采纳 · 同意放弃** | 本轮亲自读码复核：`index.ts:151–152` `.mg-wrap::before/::after` 带 `clip-path:polygon(50% 0,63% 36%,…)` **十顶点五角星**——C 档「前提不成立」的理由属实，是我 r2 把星形角饰误判成了方块（clip-path 之下 border-radius 无效）。诉求（远离四格方块联想 + 归圆语汇）已由现状满足，**同意放弃**，不升级为终验条件 |
| 8 | junqi-camp 灰度余量压到 ≥40 | ✅ 采纳 | `view.ts:125 #93B2DE`，底停 Δ42.8 ≥40（round2-fix 契约钉死）；顶停 Δ21 维持——形状通道在，足额 |
| 9 | dark-chess textContent 死桩（遗留跟踪） | ✅ 采纳（**本轮闭环**） | r3 fixer `166f926`：`view.ts setCellContent()` 死桩删除、文字口径统一走 `refresh()` 的 aria-label（读屏正文兼测试契约），`art.test.ts`/`smoke.test.ts` 2 处断言换口径不删 it()，`round3-fix.test.ts` 4 例钉修后状态——正是 A/B/C 三档两轮会签的「先换 aria-label 口径再清理」路径。r1 遗留清单就此全部清零 |
| 10 | sudoku-petal 行列完成轻反馈（遗留跟踪） | ✅ 采纳 | r2 fixer `aa8123e`：`index.ts:758 sp-linefx` 纯 fx 子节点 + `:446` reduced 块 `display:none` + JS 早退双保险——按「只动渲染层」重估后落地，判定/存档零触碰 |

**终账统计：20 条 = 采纳 19（其中 2 条经两轮分步补齐、1 条本轮由 r3 fixer 闭环）· 部分采纳 0 · 未采纳 1（r2 #7，前提不成立，同意放弃）。升级为终验条件：0。**
两轮建议 100% 对账完毕；连同 r1 遗留 2 条（textContent 桩、sp 轻反馈）全部闭环，本窗视觉验收无任何悬挂项。

---

## 二、风格基线编写说明与要点摘录

**产出**：`docs/qa/1.3-window2-style-baseline.md`，**28 条规范（B1–B28）**，分七组：粉彩色板 token（B1–B7）/ 描边宽度档位（B8–B10）/ 左上光照约定（B11–B13）/ 动效三段式与 reduced 退化（B14–B17）/ 道具体积规格（B18–B21）/ 双人可区分手法（B22–B24）/ 精 2D 可读性（B25–B28），附「规则组 → 出处 → 机器契约」溯源速查表。每条注明文件 + 函数名（或 CSS 规则名），全部第 3 轮读码核实。

编写口径三条：

1. **只固化已被三轮验收坐实的做法**，不写愿望——每组末尾直接挂上在跑的契约文件名，后人改基线先看会不会踩红测试。
2. **本窗专用**：明文声明不覆盖他窗全库基线；2.5D 地面透视 / 多层视差**不写入**，反而把「零透视关键字一票否决」（B27，`perspective`/`rotate3d`/`matrix3d`/`translateZ`/three.js，9 款 `round3-final-verify` 全目录闸）写成硬条款。
3. **例外全部显式登记**：junqi 朵朵侧暖橙（B2）、两处深色盘面题材豁免（B6）、按钮圆角双方言（B7）、cg-coord/piece-tag/l99-star 字号豁免（B18）——例外不写进基线就会在下个版本被「顺手统一」掉。

要点摘录（每组一句）：主角双色 `#a8306a`/`#28568f` 永远同源（B1）；SVG 棋子描边统一 1.2px、canvas 自适应线 `max(1.5, r*.12)`（B8/B9）；光源左上 45°，SVG 用 duoTone 三层、canvas 高光点落 `(-r*.38,-r*.42)`、CSS 走 135° 对角（B11–B13）；弹出三段式 0%→45–65% 过冲 ≤1.2→100% 落回，翻面一律无 perspective 压扁实现，reduced 双保险（CSS 块 + JS 早退）（B14–B16）；「数量可数」记号 11px 见方够用、四周留 ≥1.5px 静空、先盘点四角占位（B19）；双人「形状 + 颜色」双通道缺一不可、同款道具差异做在沿/边上（B22/B23）；盘面正投影是信息无损的唯一投影、文字通道优先、窄屏收记号留文字（B25/B28）。

---

## 三、商标终审结论

**结论：本窗 9 款商标风险清零。** 黑名单（4399 / 马里奥 / mario / 皮卡丘 / pikachu / 托马斯 / thomas / kitty / battle city / 坦克大战 / blinky·pinky·inky·clyde / pac-man / 吃豆人 / tetris / 俄罗斯方块 / 任天堂 / nintendo / 宝可梦 / pokemon / 超级玛丽 + 麻将 / 万字纹 / 回纹 / 军徽 / 肩章 / threes / 合成大西瓜 / 水果忍者 / staunton）对 9 款**全目录非测试源码** grep：**0 命中**（sudoku-petal 命中项仅为内部类型名 `SudokuBoard` 与 gameId `"sudoku-petal"`，非玩家可见文案且「数独/sudoku」为公有领域题材词；玩家可见层全部用「花田」语汇）。

逐款终审对照（题材 → 易踩线对象 → 终态核验）：

| 款 | 易踩线对象 | 终态核验（本轮读码） |
| --- | --- | --- |
| dot-maze | 街机吃豆角色 + 官方四色幽灵 | 玩家=嘴楔+呆毛+单大眼；幽灵=三尖裙边+呆毛原创组合（`art.ts:435–452`）；四色 `GHOST_COLORS`（`ghosts.ts:32`）全为马卡龙淡彩、每通道 ≥120（`#FF9AB0/#FFC48A/#9FD8F5/#C7B3F2`，第四色取紫不取橙，色相集也与官方分道）；四鬼名「直直/拐拐/绕绕/乱乱」 |
| merge-2048 | 原版 2048 色阶、Threes 拟人块 | 马卡龙自家色表（`tileFaceCSS()`）；数字块无脸；盘外静饰为十顶点五角星（`index.ts:152`）非四格方块语汇；文案无原作者名 |
| fruit-stack | 合成大西瓜果序 / 水果忍者 | 11 级原创果名（籽→团圆瓜）；顶级果无「绿皮红瓤切面」；脸为眯眼弧+腮红自家规格（`art.ts paintFace()`） |
| pool-stars | 台球品牌桌布 logo、白圈黑数制式球 | 台呢零字样零徽标；花印 vs 星印替代号码（`art.ts` 压印）；袋口无品牌金属件 |
| junqi-camp | 真实军衔制式 / 军徽 / Battle City | 军衔条为星/杠/点抽象记号、无肩章轮廓（`rankBadgeSVG()`）；旗为抽象几何旗（三角尖旗+花徽 vs 燕尾旗+星徽，`crestSVG/hqSVG/hoistSVG`）、无穗带无麦穗环绕；全款无坦克造型 |
| chess-garden | 棋子皮肤版权、Hello Kitty 猫形 | 六种头部全是花园居民（蘑菇/木马/树/塔/花/冠花，`art.ts *Inner()`）；无猫脸无蝴蝶结；代数记谱 a–h/1–8 公有领域 |
| dark-chess | 麻将纹样 / 书法字库 | 牌背=「暗」字印章+双线框原创组合（`backSVG()`），无回纹/万字纹；记牌面板=圆环+汉字（`dc-count`）；汉字用系统字重 900，不引字体文件 |
| mine-garden | 桌面扫雷笑脸按钮 + 灰浮雕 | 草地/泥土浮雕 + 文案按钮，无圆黄笑脸；「刺种」叫法与画法维持 |
| sudoku-petal | 数独 App 皮肤 / Sudoku 品牌词 | 界面与攻略全花田语汇；「Sudoku」不出现在任何玩家可见字符串（用例锁定） |

主角只有朵朵/星星（9 款座位/角色终扫无第三主角）；本轮新增文案 0 句，无新增商标面。r3 tester 已把黑名单扫描钉成每款 `round3-final-verify` 契约，本结论受机器闸长期看守。

---

## 四、9 款首页入口卡片整体感终评

口径：首页卡片由 `src/ui/home.ts createGameCard()` 用各款 `meta.ts` 的 `emoji + title + blurb + color` 渲染（emoji 39px + 标题 + 一句 blurb + 星标行，`--card-color` 上色）。只评本窗 9 款。

| 款 | emoji · title · color | 终评 |
| --- | --- | --- |
| merge-2048 | 🔢 星星合成 `#FFF3C8` | ✅ 归队。淡金与盘内木框/星点同族 |
| mine-garden | 🌼 扫雷花园 `#E5F8D8` | ✅ 归队。blurb「刺种」口径与盘内一致 |
| sudoku-petal | 9️⃣ 数独花田 `#E8DDFF` | ✅ 归队。淡紫与宫色系同族 |
| dot-maze | 🟡 豆豆迷宫 `#FFF5B8` | ✅ 归队。卡片淡黄不带深夜色，深盘面豁免只发生在局内 |
| fruit-stack | 🍉 果果合成 `#FFD9D0` | ✅ 归队。粉白与壳卡 `#FFF4F8` 同族 |
| pool-stars | 🎱 朵星台球 `#CDE8D0` | ✅ 归队。淡草绿与新壳卡 `#EFF7F0` 同族——r2 点名的「深色违和」在入口层也被壳卡色收编 |
| junqi-camp | 🎖️ 军旗对决 `#E6F0D8` | ✅ 归队。title 用「军旗」（夺旗玩法）非错别字：blurb「把对方营地里的旗子请回来」与 guide「铁路手册」口径互证，登记为有意用字 |
| chess-garden | ♔ 花园国际象棋 `#F0E6D8` | ✅ 归队。米棕与石板/木篱同族 |
| dark-chess | 🀄️ 翻翻暗棋 `#F6DFC5` | ✅ 归队。emoji 为 Unicode 通用字符、语义（带汉字的牌）与玩法吻合，非麻将玩法暗示，blurb 已写明是棋子 |

**结论：9/9 归队，无仍违和款，本轮无需点名交 C 档。** 复核三点：① 9 色全为马卡龙淡彩（每通道 ≥ 0xC5），并排零跳色；② r2 点名的三张局内壳卡（pool-stars / fruit-stack / chess-garden）已落地（第一节 r2 #4–#6），「首页卡片 → 局内壳卡」的粉彩语汇现在两层贯通 9/9；③ r2 点名的 merge-2048「方块静饰」经本轮读码确认本就是五角星（第一节 r2 #7），入口与局内均无方形方言。9 款 title/blurb 红线与 title 逐字已由本轮快照钉死（见第五节）。

---

## 五、文案终查改动清单

**终查结论：两轮 17 句全部在位，零丢失、零错别字、零口径漂移——本轮玩家可见文案改动 0 处**（快照测试为唯一新增代码）。逐项登记：

1. **在位性**：r1 的 11 句 + r2 的 6 句逐句 grep 复核**17/17 在位**（mine-garden 3 句、dark-chess 3 句、junqi/dot-maze/fruit-stack/chess-garden 各 2 句、merge-2048/sudoku-petal/pool-stars 各 1 句），未被 r2 fixer 14 commit、r3 tester 契约与 r3 fixer 清桩的任何 rebase 冲掉，无需补回。
2. **与终态画面一致性**（抽验到绘制源头）：「将 7 点、兵 1 点」＝`dark-chess/board.ts:20 RANK`（general:7/soldier:1）✅；「三颗星就是司令」＝`junqi-camp/art.ts rankBadgeSVG()` 司令 3 星 ✅；「铜星银星金星→彩虹星」＝`merge-2048/art.ts STAR_TIER_NAMES` ✅；「左下角小记号」＝`.jq-mark{left:2px;bottom:2px}` ✅；「玫红边/深蓝边」＝`drawRim()` seat 色 ✅；「a 到 h、1 到 8 门牌」＝`chess-garden/view.ts:289–290 coordEl()` 底排/左列 ✅；「窄屏藏小圆点」＝`dark-chess/view.ts` ≤400px 收 `g.dcd` ✅（r3 fixer 清的是不渲染的 textContent 桩，SVG 战力点画面与这两句文案无关，复核不受影响）。
3. **错别字与语气**：9 款 guide 全文（72 章 + 52 条 general）通读——错别字 0；语气全部只鼓励（「被假人抢先也没关系」「打歪了不要紧」「翻到『将』也照样算数」），恐吓词 0；「地狱档」为全库既有难度档名（`dot-maze/ghosts.ts:97 hell:"地狱"`，flight-chess/gomoku 等同口径），文案与游戏一致，不属恐吓描写，维持。
4. **结构与红线**：general 条数 9 款全部 3–6（6/6/6/6/5/6/6/6/6）；两轮 17 句每句 ≤64 字复测通过；tips 只增不减（本轮 0 增 0 减）。
5. **双键口径**：WASD+F+G / 方向键+L+K / Esc 讲法在 5 款相关处全部在位——mine-garden general（F 翻开 G 插旗）、chess-garden general（F 选 G 取消 + Esc 两段式）、dot-maze general（G/K 取消 + Esc/⏸）、fruit-stack（A/D+F、方向键+L、G/K 回中）、junqi `TWO_PLAYER_NOTE`（F/G 与 L/K 全套）。玩法说明数值零改动。
6. **快照测试（唯一代码改动）**：新增 `src/games/__tests__/window2-r3-learner-copy.test.ts`（1 文件 / 4 例，`fd6194e`→rebase 后 `c16847a`）：① 两轮 17 句跨轮总账逐字钉死（任何一轮丢句即红，不用再查两份旧快照差集）；② 首页 9 款 title 逐字 + blurb ≤64 字 + 分级/商标双黑名单（入口卡片是第四节终评对象，此前无人钉）；③ 双键口径六处短语 + junqi 双人说明键位逐词保全（防后续润色「解释掉」操作讲法）；④ 终态结构契约（八章 / 每章 ≥2 tips / general 3–6）。与 r1（4 例）r2（4 例）快照互不替代，三份并行看守。

**360px/44px 红线自查**：本轮未用任何文案去解释布局问题——两条存量布局阻断（r2-1/r2-2）均已由 C 档在绘制层真修并经 A 档浏览器实测清零，文案只描述修后的真实画面。

---

## 六、验收自查与收尾登记

- `npm test`：✅ **748 文件 / 15320 例全绿**（185.2s，跑于含 r3 fixer 清桩 `166f926` 与 backlog `e0defe8` 的合并态）。链路：r3 收货 746/15312 → r3 fixer `166f926`（+1 文件 / +4 例：round3-fix 契约，2 处断言换口径不删 it()）→ 本档 +1 文件 / +4 例（`window2-r3-learner-copy.test.ts`）。相对收货 `f0d2ef9` 的 746/15312 **只增不减成立**（+2 文件 / +8 例）。
- `npm run build`：✅ 全绿（`tsc --noEmit` + vite build，727 模块）；PWA precache 188 entries / 5215.34 KiB（较收货 5215.41 KiB 的差异为 fixer 清桩删除死字符串所致，符合预期）。
- 五节齐备：终账 20/20（采纳 19 · 未采纳 1 同意放弃 · 升级终验条件 0）；风格基线 28 条成文可溯源；商标清零；首页 9/9 归队零点名；文案 0 改动 + 快照 4 例。
- 收尾 rebase 保留他人全部文件（`docs/plan-1.3-*.md`、`.cursor/skills/**`、`docs/qa/1.3-window2-plan.md`、tester/fixer 全部报告与 r3 fixer 并行 commit），普通 push 未用 force。
- 提交链：计划 `c1874d6` → 快照 `c16847a` → 基线 `8cb2569` → 本报告收尾 commit。
