# 三人组 r43 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。playbook：PR #116。不回退安全区 / `--vv-h` / CTA / 平板 760 / 既有闸。不改 B 的 `.rbt-vs-btn`（N-189）、`.rte-btn`（N-190）。N-105 无第四版。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-191 | 静态扫可点 `*-vs-btn` 与 `.rte-btn` ≥44。红点 `.rbt-vs-btn` / `.rte-btn` 走 `red-blue-tap` 白名单。对照绿 `.rbe-over-btn` / `.rbv-over-btn`（48）。不扫 `.rbt-vs-back`。不替代 N-139/152/175/189/190。 |
