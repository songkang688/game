# 1.3 窗口 7 · 第 2 轮视觉验收 · 学习优化员报告（B 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 2 轮学习优化员（B 档）。只写本报告 + 本窗九款**纯文案**（guide / blurb / 提示语）+ 文案快照测试；绘制代码一行不碰；A 档 / C 档报告一字不改。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。
> 输入（可读不可改）：第 1 轮三份报告（`round1-tester` / `round1-learner` / `round1-fixer`）+ 本轮 A 档 `round2-tester`（已落地：对账 13 修 / 新发现一般 2）。

## 〇、本轮计划（动手前提交）

1. **回看采纳率**（第一节）：对照 `round1-fixer` 修复清单（P1–P12 + B-10）与 `round2-tester` 对账表，我第 1 轮 TOP10 建议逐条评「采纳 / 部分采纳 / 未采纳」，并做**文字级落地效果评价**（规格是否照抄、变体是否更优、有没有走样）；未采纳的分析是建议不可行还是优先级不够，修订后重排。不截图入库。
2. **补盲区题材**（第二节）：第 1 轮密度基准偏重「往上补密度」，本轮反向补「怎么少而精」——棋牌桌面四款（landlord / memory / lianliankan / puzzle）与钓鱼 / 跑酷对照，把「密度克制为什么反而是对的」这条结论写透。只记文字结论，不下载、不截图、不描摹。
3. **横向一致性评审**（第三节）：九款并排看「像不像一家人」，扫描各款色板 token / 描边宽度 / 光照方向 / 动效时长常量，点名最违和的 ≤5 款（不足如实写），给 C 档绘制规格级统一化建议。
4. **修订后的建议清单**（第四节）：给本轮与下一轮 C 档，按严重度 × 成本重排（吸收 A 档 N-1/N-2/N-4/N-5 与我方未采纳项的重排结论）。
5. **商标风险清单更新**（第五节）：对照第 1 轮修复后的新素材（pattern 场景画 / botFace 头像 / 皇冠 / 蝴蝶结 / trashArt 18 款），把仍有风险的点名，维护「题材 → 易踩线对象 → 规避要点」。
6. **文案优化**（第六节，唯一动代码的范围）：① 接 C 档遗留第 2 项——puzzle-tiles blurb 更新成「场景画」表述；② 把 C 档第 1 轮修复的视觉亮点补进对应款引导（puzzle 场景画切片、landlord 自绘头像、poop-hero 自绘垃圾/香香星/披风发卡、fruit-slice 果王皇冠）；③ 核对第 1 轮 16 处文案没有被 rebase 冲掉，丢了的补回；④ 每处登记入报告并补快照测试（`copyW7r2.test.ts`）。
7. 收尾：fetch → rebase（保留他人文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`、第 1 轮报告与本轮 A 档报告）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window7`。**不 force、不改 `main`、不动 `game-1.2`、不用 `gh` 开 PR。**

学习红线重申：只学**画面密度**与**角色剪影**两件事，只记文字结论；不下载、不截图入库、不描摹任何具体造型；商标与官方角色连「像」都不许像。

（第一至六节随工作推进回填。）
