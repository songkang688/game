# 窗口 2 · 1.2 三轮验收 第 2 轮 · 学习优化员 · 包 A

> 角色：QA 学习优化员（第 2 轮：提手感 / 关卡 / AI 改进，并真正落地一部分）
> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` 共 5 款
> 基线：`origin/game-1.2-window2`（`62c2599`）
> 模型：`claude-opus-5-thinking-high-fast`

## 〇、调研与落地计划（动代码前先立此存照）

### 0.1 边界（红线）

- 只改上面这 5 个游戏目录，以及 `src/styles.css` 里**只属于 chess-garden 的选择器**（`.cg-tool`）。
  别人的 10 款、窗口 1/3/4/5 的款与平台文件（`src/ui/home.ts` `src/ui/rootGate.ts` `src/ui/playModes.ts`）一个字不动。
- **不改 `src/engine/**`**：横向的好点子只落在本包游戏目录内的小函数里，不为「惠及几十款」去动共用引擎。
- 不改测试员报告原文（`docs/qa/1.2-window2-round*-tester*.md`），也不改第 1 轮修复员 / 学习优化员的报告原文。
- **不碰第 2 轮测试员包 B 记的 3 条严重项**（那是包 B 修复员的活）。
- 第 1 轮已清零的三条严重项必须保持：fruit-stack 窄屏 44px、pool-stars Esc 暂停吞输入、junqi-camp 双人各管各的光标；
  第 1 轮学习优化员落地的 L-A-1…L-A-6 一条不回滚。
- 报告只写两处：本文件（独占）与 `docs/qa/1.2-window2-round2-learner.md` 的 `## Pack A` 一节（只追加，不碰 Pack B / C）。
- 只推 `origin/game-1.2-window2`；被拒就 fetch + rebase 再 push，禁止 `--force`，不用 `gh` 开 / 改 / 合 PR。
- 不引入任何外部运行时依赖；不用 three.js / CDN；用例只增不减；角色只用朵朵 / 星星；无血无死亡描写；商标黑名单 0 命中。

### 0.2 本轮的取材口径

第 2 轮测试员包 A 记的是**阻断 0 / 严重 0 / 一般 17**（第 1 轮遗留 12 条 + 本轮新增 5 条）。
本轮从这 17 条里挑「小而稳」的下手——`destroy` 回收注入样式、`Esc` 退出暂停、取消键 `G` / `K`、
360px 热区、键位语义，都是「加一道闸 / 补一个选择器 / 补一个键」的改法，
**不做大翻修、不重写 188 关表、不动难度标定**（凡是要重标定难度或重跑 AI 胜率门槛的，本轮一律只列不落）。

### 0.3 执行顺序

1. 本 commit：只落调研与计划，不动代码。
2. 逐条落地，每条一个独立 commit，自带测试（既有断言只翻面、不删除、不放宽）。
3. 收尾：fetch → rebase `origin/game-1.2-window2` → `npm test` + `npm run build` 全绿 → 普通 push。

### 0.4 本轮列出的改进（先立此存照，落地情况见第一节）

| # | 款 | 一句话 | 打算落地 |
|---|---|---|---|
| L2A-1 | fruit-stack / pool-stars / chess-garden | `destroy` 把注入 `document.head` 的样式一起带走（注一次 + 引用计数） | ✅ |
| L2A-2 | chess-garden | `Esc` 变成开关：再按一次退出暂停，和另外四款口径一致 | ✅ |
| L2A-3 | chess-garden | 接上取消键：朵朵 `G`、星星 `K` 撤掉选中 | ✅ |
| L2A-4 | dot-maze / chess-garden | 360px 热区：`.dmz-btn` 与 `.cg-tool` 补到 44px | ✅ |
| L2A-5 | fruit-stack | 键位语义：`G` / `K` 把落点收回盆中央，`W` / `S` 与上下方向键在攻略里写明不用 | ✅ |
| L2A-6 | chess-garden | 残局连胜里主动认输给一句单独的收场话，不再和被翻盘共用 | ✅ |
| L2A-7 | pool-stars | 瞄准键按座位分（`PA-PS-2`） | ❌ 要重跑 AI 胜率门槛 |
| L2A-8 | junqi-camp | 暂停时按确认不再顺手清掉已选好的落点（`PA-JQ-3`） | ❌ 要动 `createBoard` 的对外通路 |
| L2A-9 | chess-garden | 残局连胜第 10 局起对手封顶、题面池只有 35 个（`R2-PA-4`） | ❌ 属难度 / 内容重标定 |
| L2A-10 | chess-garden | 360px 上 `.cg-sq` 一格实宽 ≈ 42.5px（`R2-PA-3`） | ❌ 8×44 在 360px 上物理排不下，要改版式或立例外 |
| L2A-11 | dot-maze | `meta.blurb` 把豆子叫「小星星」（`PA-DM-2`）、`F` / `G` / `L` / `K` 没接（`PA-DM-3`） | ❌ 上屏文案与键位语义要连攻略一起定 |
| L2A-12 | fruit-stack / chess-garden | `merge.ts` 注释口径（`PA-FS-3`）、`meta.blurb` 的「死角」（`PA-CG-4`） | ❌ 与 L2A-11 同一类，留给修复员统一走一遍口径 |
