# r35 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`17356717`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-165**、**N-166**、**N-167**。下一空号 **N-168**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r34 学习 PR 未合主干时，本轮仍从 N-165 起编；若主干已占用 N-165+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r34）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、N-108 无尽画廊、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r35-learn-notes.md` §0。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / #105 / **#106**（N-162…164）。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-165 · B · `.dr-rules-close`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/duo-rush/index.ts` ≈L421 规则、≈L516 节点 |
| 现状 | `padding: 9px 16px; font-size: 15px`，无 min-height |
| 不做 | 不改 N-87 菜单 CTA；不改赛道 / 半屏圆钮；不改 xq/dua 已绿的关闭钮（可当对照） |
| 验收 | 打开规则层，关闭钮高 ≥44；关掉后赛道可玩 |

### N-166 · B · `.dr-resume`

| 项 | 内容 |
| --- | --- |
| 文件 | 同文件 ≈L417 规则、≈L512 节点 |
| 现状 | `padding: 13px 28px; font-size: 17px`，无 min-height；不吃 `.dr-btns button` 的 46 |
| 不做 | 不改 `.dua-resume`（已有父规则 44）；不并进 N-152 veil |
| 验收 | 暂停罩点「继续比赛」，钮高 ≥44；恢复比赛 |

### N-167 · A · rules-close / resume 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` 内联与独立样式 |
| 规则 | `*-rules-close` / `*-resume` 的 button 必须 `min-height`≥44，或 kit/`MIN_HIT_PX`/`TOUCH_MIN` 插值 |
| 豁免 | 非按钮；父选择器已钉 ≥44 且本规则不压高度 |
| 不做 | 不替代 N-146 / N-164；不扫装饰 span |

---

## 建议顺序

1. A 先合 **N-167**（或与 B 并行，巡检应对现状为红）。
2. B 修 **N-165**，再修 **N-166**（可同一 PR）。
3. 回归：N-87 菜单钉顶、N-99 数独可滚、N-97 农场钉底、N-100 进场锚定、xq/dua 规则关闭仍绿。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-165/166 的点击矩形，不能只看 CSS 行。
