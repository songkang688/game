# 1.3 窗口 4 · 第 2 轮视觉验收 · 测试员报告

> 状态：**复验计划（开工 commit）**，执行中，完成后本文件整体更新为正式报告。
> 分支 `game-1.3-window4`。规格模板 `docs/plan-1.3-step28-A-tester.md`，收窄口径优先：只验本窗 9 款
> duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
> 权限：只改 `*.test.ts` 与本报告；不改绘制代码；问题交本轮 C 档修。

## 复验计划

1. **基线**：`npm ci` → `npm test` → `npm run build`，记录用例总数与第 1 轮合并态（709 文件 / 15247 例）差值（只增不减）。
2. **逐条对账**：round1 tester 问题清单（W4R1-01～07）× round1 fixer 修复清单（`cd187a9` / `f7af94d` / `a91fb0e`）逐条给结论：已修复（附证据）/ 修复不彻底 / 修出回归 / 合理遗留。重点：garden-guard hud12 🌸/💗🤍 残余、DOM 文案 emoji 口径、360/reduced 实测补课。
3. **机器化扫描用例**：`window4-visual-scan.test.ts`（31 例）与 `window4-learner-copy.test.ts`（9 例）必须还在跑、全绿。
4. **深挖（本轮重点，9 款全做）**：
   - 角色特写：2× 与 16px 各一次（换样本：换角色/换皮肤/换帧相位，不复用 round1 同一组）；
   - 动效完整链：预告 → 过程 → 收尾三段 + reduced 退化行为级验证；
   - 场景层次：背景/中景/主体/HUD 四层分明度；
   - 一致性：左上光照、描边规格、色板与库内金币/金星标准对齐；
   - 性能：CPU 节流下帧率记数字（不为测帧率改玩法代码）。
5. **360px 实机**：无头 Chrome 360×640（+ 320 复核）实测溢出/遮挡/触区/字号。
6. **回归防线**：判定热区钉死断言全绿、功能提示在 reduced 下仍在、教育数据快照全绿、destroy 泄漏复验。
7. **收尾**：补第 2 轮机器化用例（只 `*.test.ts`）→ 本报告成文 → fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push。

红线重申：不 force、不改 main、不推 game-1.2 / game-1.2-kk、不用 gh 开 PR；别人的报告（round1 三份、round2 learner/fixer）一字不改。
