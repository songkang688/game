# 1.3 窗口 4 · 第 2 轮视觉验收 · 测试员报告

> 范围：本窗 9 款（非全库 39 款）：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
> 规格模板：`docs/plan-1.3-step28-A-tester.md`（20 款抽样等全局路径按派发收窄为「本窗 9 款各走一遍」）。
> 权限：只改 `*.test.ts` 与本报告；不改绘制代码；问题交本轮 C 档修。
> 本轮定位：复验（round1 问题清单 × round1 fixer 修复清单逐条对账）+ 深挖（换样本 16px 量化、360px 实机、reduced-motion 行为级）。

## 一、全量跑数据与两轮用例数对比

| 项 | 结果 |
| --- | --- |
| `npm ci` | 绿 |
| `npm test`（开工基线复核，SHA `fbe1a71`） | **709 文件 / 15247 例全绿**，与统筹宣称、第 1 轮合并态完全一致 |
| `npm test`（合并态收尾，含本轮 9 例与 r2 learner +3 例） | **710 文件 / 15259 例全绿**（第 1 次合并态跑出现 1 例偶发失败，随后连续复跑全绿，见第八节） |
| `npm run build` | 绿（tsc --noEmit + vite 655ms；PWA precache 188 项 5237.48 KiB） |
| 无头 Chrome 实测 | 全部跑在 `vite preview :4173` 构建产物上（16px 量化经 vite dev :5173 导入真实绘制函数） |

两轮水位：15247 → 15259（+1 文件 / +12 例，只增不减 ✅）。

## 二、逐条对账（round1 tester 7 条 × round1 fixer 3 笔）

### round1 fixer 修复清单（3 笔，全部复验）

| 修复 | 本轮结论 | 验证证据 |
| --- | --- | --- |
| `cd187a9` garden-guard 画布 emoji 13 处手绘化 | **已修复** | index.ts 非注释 emoji 为 0（唯一命中是 1551 行注释）；`drawPetalIcon`/`drawBulbIcon`/`drawShieldIcon`/`drawMapScrollIcon`/`drawThemeBadge` 均在 art.ts（1115–1241）；360px 实测截图：波次预览用 `drawMonsterSprite` 真立绘（软软怪 ×4）、塔价花瓣币为手绘图标；`wave12.ts`/`logic.ts` 的 emoji 数据字段已无画布消费点（`.emoji` 在 index.ts 0 命中）。本轮补回归守护断言（drawWavePreview 接 drawMonsterSprite 且无 `.emoji`） |
| `f7af94d` duo-arena 技能飘字/人机徽章/让分章 | **已修复（本体），但同类漏网 8 处 → 立 W4R2-01** | 技能飘字 527/915 行纯 `spec.name` + 飘字底板 `drawSkillIcon`（1246）；人机徽章 `AI_PIPS` 1–4 颗 `drawMiniStar`（1289–1297）；让分 `drawMicroFlower` + 纯文字 hLabel（1303–1314）。但事件飘字仍有 8 处**字面量** emoji（803/806/816/821/824/829/870/877：🫧×3 💫 🎉 ❄️ ✨ 🌀）经 `fl.text` → `fillText`（1249/1252）每次触发上画布——fixer 的正则 `pushFloat\([^;]*emoji` 只挡属性插值挡不住字面量，round1 的同行扫描也抓不到变量链。r2 learner 独立发现同一条（其 R2-TOP2），交叉验证成立 |
| `a91fb0e` sling-birds 横幅章节 emoji 清零 | **已修复** | `drawBanner`（1628–1652）函数体无 `.emoji`/♾，两端 `drawBannerBadge` 全绘制角标仍在；本轮补回归守护断言 |

**修出回归：0**（三笔修复的配套断言 + 第 1 轮 31 例扫描全部仍绿）。

### round1 tester 问题清单（W4R1-01～07）

| 编号 | 级别 | 本轮结论 | 复验证据（本轮独立取证） |
| --- | --- | --- | --- |
| W4R1-01 garden-guard hud12 段串 🌸/💗🤍 | 一般 | **合理遗留（清单未丢，钉住断言在岗）** | hud12.ts 63/64/66/217 四行非注释 emoji 原样在；钉住断言（`toHaveLength(4)`）仍绿；360px 实测截图 HUD 左段「🌸 13」右段「💗×5」确为系统 emoji 字体上画布。r1 fixer 遗留理由（1.2 冻结契约需联动重设计）仍成立，r2 learner 已给出「渐进换绘」方案（其 R2-TOP8），**交本轮 C 档** |
| W4R1-02 monster-crisis 双英雄 16px 不可分 | **严重** | **遗留待修（本轮 C 档必修项）** | 换管线复测：自然尺寸渲染再降采样 16px，剪影 XOR **0%**、重叠灰度差 **8.5/255**（round1 0% / 11.6，结论一致）；2× 特写两人各 185/182 色、亮度极差 198——渐变层次很好，问题只在轮廓同形（同圆身+同形贝雷帽+同形围裙，帽徽 16px 下消失）。`HERO_SKINS`/`drawHero` 与 round1 完全同版未动。**「第 1 轮严重级修复确认率 100%」的验收挂在这条，收尾时 C 档仍未落地（见第八节）** |
| W4R1-03 duo-rush 双主角 16px 弱可分 | 一般 | 合理遗留 | 换样本复测（runPhase=1.3 奔跑帧）：XOR 10.7%、灰度差 4.7/255——形状通道边界、灰度通道仍弱。缓解因素仍在：实渲 ~49px、默认照片头像 + SEAT_RING、双半屏主题色（360px 截图可见）。learner 顺延 R2-TOP7 |
| W4R1-04 duo-vs-star duoduo/xingxing 16px 边界 | 一般 | 合理遗留 + **换样本挖出更差的一对 → 立 W4R2-03** | 复验 duoduo vs xingxing：XOR 7.3%、灰度差 18.3（round1 6.7%/19.1，边界结论一致）。全花名册 12 角色两两量化（66 对）最难分对是 **xingxing vs jiujiu：XOR 0%、灰度差 7.2**——jiujiu 尖嘴画在身体轮廓内、呆毛是 r*0.22 细弧线，16px 全部消失 |
| W4R1-05 gold-hook drawOre 无渐变 | 一般 | 合理遗留 | 钉住断言（现状无渐变+三重体积线索不许丢）仍绿；2× 特写量化：48 色 / 亮度极差 136（对比库内金币标准 drawKitCoin 121 色）——体积仍全靠高光+描边+落影。learner 顺延 |
| W4R1-06 次要文案字号 9–13px | 一般 | 合理遗留（**量化口径本轮扩大**） | 360×640 DPR2 实测入口+玩法双态：除 round1 的 8 处外新记 duo-rush `.dr-hint` 13.5px×2 / `.k1/.k2` 12px、duo-vs-star 玩法态 `.dvs-back` 13.5px / `.who` 12px / 元气数字 10.5px / `.dvs-padname` 11.5px、duo-arena `.r` 12px、gold-hook `.gdh-tip` 11px，合计约 **18 处**。仍全部为 1.2 存量 DOM 样式、无溢出无遮挡，不升级 |
| W4R1-07 duo-vs-star drawGoldStar 无独立高光 | 建议 | 合理遗留 | 特写量化 89 色 / 亮度极差 103（同门 candy-swing 116、garden-guard 95），顶停近白仍是唯一受光面；跨款一致性建议维持 |

**对账统计：已修复 3（fixer 三笔）/ 修复不彻底 0 / 修出回归 0 / 合理遗留 7（W4R1-01～07 全部仍在清单，其中严重级 W4R1-02 为本轮 C 档必修）。**

### 第 1 轮机器化扫描用例状态

`window4-visual-scan.test.ts` 31 例、`window4-learner-copy.test.ts`（r2 learner 已扩至 12 例）全部还在跑、全绿；本轮新增 `window4-visual-scan-r2.test.ts` 9 例（见第七节）。

## 三、深度走查（9 款 × 五项，换样本）

方法：① 角色特写＝真实绘制函数经 vite dev 导入无头 Chrome，自然尺寸渲染（=2× 特写，量化剪影内独立色数与亮度极差）再降采样 16×16（量化剪影 XOR%/灰度差）；② 动效链＝源码三段核对 + 行为级差分（玩法画布 0.8s 前后逐像素差异占比，normal vs reduced 双档）；③ 场景层次＝360px 玩法截图目检；④ 一致性＝特写色数/光源方向对照库内金币金星标准；⑤ 性能＝CDP CPU 4× 节流 3s rAF 计数。

| 款 | ①角色特写（2× / 16px） | ②动效链（预告→过程→收尾 / reduced 退化） | ③场景层次 | ④一致性 | ⑤性能 4×CPU |
| --- | --- | --- | --- | --- | --- |
| duo-rush | 2×：P1 42 色/极差 153、P2 69 色/150 ✅；16px XOR 10.7%/灰度 4.7 ⚠️（W4R1-03） | 加速带流动箭头→8 帧旋转金币/磁吸→拾取反馈；reduced：箭头静止、金币停正面帧、侧倾归零、sparkle 0（源码 963/988/1006/1053）；行为：差分 13.4%→13.0%（跑动位移为主）、CSS 动画 6→0 ✅ | 天体/远山/近山/近景剪影 4 层 + 雾带 + 路旗，双半屏粉/蓝主题（截图）✅ | 金币线性 2 停+翻面高光 ✅ | **60fps**；泄漏 0 |
| duo-arena | 2×：花 87 色/98、星 64 色/126、KitCoin 121 色/112 ✅；16px XOR 53.6%/灰度 34.4 ✅ | 出手前摇圈→生效波纹→星屑爆点（r1 契约在岗）；行为：差分 1.7%→0.8%、CSS 6→0 ✅ | 座位渐变 + 主题装饰 + 果冻垫，留白式顶视（截图云台广场）✅ | 径向 3 停金币=全库标准 ✅ | **60fps**；泄漏 0 |
| duo-vs-star | 2×：duoduo 98 色/190 ✅；16px duoduo/xingxing 7.3%/18.3 ⚠️、最难对 xingxing/jiujiu **0%/7.2** ❌（W4R2-03） | 平台裂纹 1→3 道预告→坠落→小云重生；裂纹调用（index 725）无 motion 门控，reduced 下功能提示不丢 ✅；差分 0.1%→0（待机） | 天空渐变 + 3 朵云，**无中景带**（截图佐证 learner R2-TOP6），平台层与 HUD 分明 ⚠️ | drawGoldStar 无独立高光（W4R1-07） | **60fps**；泄漏 0 |
| sling-birds | 2×：straight 鸟 107 色/190 ✅；16px 五鸟两两最难对 straight/boomerang 6.8%/12.6 ⚠️边界（其余对更高，体型通道在岗） | 拉弓蓄力表情→飞行速度线→落地捂头 + 材质碎片（木锯齿/石粗粒/冰菱片）；行为：差分 3.6%→0.6% ✅ | 天空+太阳+中景剪影+草丛带 4 层（截图）✅ | 鸟体径向渐变+白高光 ✅ | **60fps**；泄漏 0 |
| candy-swing | 2×：金星 86 色/116 ✅（2 停渐变+中心高光星+深金描边） | 绳张力预告→摆动残影→吃糖收尾/星星逐颗；行为：差分 0.6%→0.6（装饰动效本就克制）、CSS 6→0 ✅ | 主题底+泡泡氛围+绿地，角色是视觉中心（截图）✅ | 与库内金星标准对齐 ✅ | **60fps**；泄漏 0 |
| gold-hook | 2×：crew 16px 19.2%/14.8 ✅；金块特写 48 色/136 ⚠️（W4R1-05 无渐变）；矿石 16px：金vs宝石 33.3%/21.5 ✅、金vs石头 **0%/7.0** ⚠️（W4R2-07 建议） | 摆钩→拉回抖动/2s 扫光→收工结算；行为：差分 1.0%→0.5% ✅ | 矿下三层视差 + 矿壁矿脉 + 草皮边（截图），照明圈叙事 ✅ | 扫光只给金块家族的辨识约定在岗 ✅ | **60fps**；泄漏 0 |
| garden-guard | 塔/小怪家族契约在岗；四类原型 BOSS 16px 两两：**bossArmor/bossSwift/bossSplit 三对 XOR 全 0%**（灰度差 6.9–14.9），仅 bossFly 靠飞行通道 64.3% ❌（W4R2-04，learner TOP10 量化坐实）；金星特写 70 色/95 ✅ | 波次预览真立绘（预告）→塔发射/怪行走→过关星；行为：差分 0.2%→0.6%（布阵倒计时文字重绘噪声，非动效增多）✅ | 棋盘草叶 + 8% 装饰格 + 地平线条带（截图）✅ | 手绘花瓣币按左上光源+径向渐变 ✅ | **60fps**；泄漏 0 |
| sprout-defense | 十二苗/虫家族契约在岗；五虫 16px 最难对 walker/flyer **XOR 0%/9.1** ⚠️（W4R2-06 建议：翅膀不进剪影；实机有悬浮高度+影子分离通道兜底）；金星特写 28 色/140 ✅ | 预警角标（提前 3s，`warnings` 不受 calm 门控 ✅）→子弹拖尾/啃咬帧→过关星；行为：差分 0→0.2%（噪声级）✅ | 泳道相间 + 栅栏 + 夜章萤火虫 + 房子（截图）✅ | drawClearStar 2 停渐变 ✅（星弹平涂是 learner R2-TOP5 顺延项） | **60fps**；泄漏 0 |
| monster-crisis | 2×：P1/P2 各 185/182 色、极差 198（渐变层次全库前列）；16px XOR **0%/8.5** ❌（W4R1-02 严重遗留，复验坐实） | 挥击弧痕（过程）→上色进度彩虹条→告别小云收尾；reduced 弧痕关、功能条不丢；行为：差分 0→0（战场待机连装饰动效都没有——佐证 learner TOP4 天空定格云，不另立项）✅ | 庭院环纹 + 院内装饰 + 天空带（截图；天空素是 learner 顺延项）⚠️ | 元气糖渐变+高光 ✅ | **60fps**；泄漏 0 |

## 四、本轮新发现问题清单

#### W4R2-01　duo-arena：事件飘字 8 处字面量 emoji 经变量链上画布　**【一般】**
- **位置**：`src/games/duo-arena/index.ts` 803/806/816/821/824/829/870/877（🫧×3 💫 🎉 ❄️ ✨ 🌀），经 `pushFloat` → `fl.text` → `drawCourt` 的 `fillText`（1249/1252）。
- **现状**：r1 修复只覆盖 `spec.emoji` 属性插值；字面量前缀是同类残余，事件触发时每 0.8s 生命周期整段上画布。r2 learner 独立发现同条（交叉验证）。
- **期望**：前缀去掉改纯文字，或复用飘字底板的手绘图标位（`drawSkillIcon` 同法，冰/星/泡泡图标 art.ts 都有现成：`drawIceShell`/`drawSparkStar`/`drawTargetBubble`）。
- **量化**：钉住断言现状 8 处（修复后改 0）。
- **建议责任文件**：`duo-arena/index.ts`。

#### W4R2-02　duo-rush：画布 HUD 整串 emoji（round1 登记未编号，本轮正式立项）　**【一般】**
- **位置**：`src/games/duo-rush/index.ts` `hudText()`（1125–1141，字面量 ❤️🤍🪙✋×2🤝 + 变量 `r.emoji`/`POWERUPS[*].emoji`）、`branchTag()`（🌿🌈）、经 `drawHud()`（1143–1165）`fillText` **每帧**上画布。
- **现状**：与 garden-guard hud12（W4R1-01）同族的变量链盲区；round1 专项①表已登记「≈13 种，仅 HUD 条」但未立项，learner 两轮都列第一优先（TOP1 → R2-TOP1）。360px 截图可见 HUD 行「⭐ 星星 0 米 🪙0 ❤️❤️❤️」全为系统 emoji 字体。
- **期望**：learner R2-TOP1 的「文字段+图标段分开画」方案（drawMiniFace/drawHeart 空心分支/drawCoinFrame 正面帧/drawPowerIcon 全都现成），squeeze 挤压逻辑保留。
- **量化**：钉住断言现状 8 枚字面量（hudText 6 + branchTag 2，修复后改 0）+ `r.emoji`/`activeIcons` 变量链入口钉住。
- **建议责任文件**：`duo-rush/index.ts`（HUD 绘制段）、`duo-rush/art.ts`（如需补空心心）。

#### W4R2-03　duo-vs-star：全花名册 16px 最难分对 xingxing vs jiujiu 剪影完全重合　**【一般】**
- **位置**：`src/games/duo-vs-star/art.ts` `drawCharTrait` 的 `xingxing`（320，星呆毛 r*0.34）与 `jiujiu`（382，尖嘴画在轮廓内 + 呆毛为 r*0.22 细弧线）。
- **现状**：12 角色 66 对两两量化，此对 XOR **0%**、重叠灰度差 **7.2/255**（两者同为暖黄系）；双人对战允许双方各选这两位。round1 只测了 duoduo/xingxing 一对，此对更差。
- **期望**：jiujiu 尖嘴移出轮廓（x+r*0.34 → ≥r*1.0 外凸）或呆毛弧改实心羽毛冠并放大到 r*0.4；与 W4R1-04 的 xingxing 呆毛放大一并处理。
- **量化**：现状钉住（两处半径系数），修复后取反。
- **建议责任文件**：`duo-vs-star/art.ts`。

#### W4R2-04　garden-guard：四类原型 BOSS 三对剪影 XOR 0%（learner TOP10 量化坐实）　**【一般】**
- **位置**：`src/games/garden-guard/art.ts` `drawMonsterSprite` boss 分支（617–636）：通用皇冠 + 仅 `boss2` 有钳子。
- **现状**：bossArmor/bossSwift/bossSplit 两两 16px 剪影 XOR 全 **0%**、灰度差 6.9–14.9/255；只有会飞的 bossFly 靠飞行通道分开（64.3%）。孩子在 13 章战役里只能靠颜色记 BOSS。
- **期望**：learner TOP10 的轮廓外配饰方案（铁壳系肩甲/疾风系速度羽/浮云系云座/双生系分裂环，每件 ≤3 条 path）。
- **量化**：钉住断言「drawMonsterSprite 无 bossArmor/bossSwift/bossSplit 专属分支」（修复后取反）。
- **建议责任文件**：`garden-guard/art.ts`。

#### W4R2-05　duo-vs-star：360px 档触控键 38px 宽、返回键 32px 高（<40 底线）　**【一般】**
- **位置**：`src/games/duo-vs-star/index.ts` CSS `@media (max-width:380px)` 段：`.dvs-pad button{min-width:38px;min-height:40px}`（260 附近）；`.dvs-back` padding 7px + 13.5px 字号实测 65×32。
- **现状**：360×640 DPR2 实测玩法态 7 键一行 38×40（◀▲▼▶✋💥🤝）——是 1.2「一个像素都不许溢出」的刻意取舍（源码注释自证），但低于 40px 触区底线 2px；round1 报「触区违规 0」是入口态测量口径所致，玩法态是本轮补课。
- **期望**：两组键上下摞放时把 gap/padding 再收 2px×6 处即可回到 ≥40px；或 4+3 分两行。返回键加 padding-block。
- **量化**：钉住断言现状 `min-width:38px`（修复后 ≥40）。
- **建议责任文件**：`duo-vs-star/index.ts`（CSS 段）。

#### W4R2-06　sprout-defense：walker vs flyer 16px 剪影 XOR 0%　**【建议】**
- **位置**：`src/games/sprout-defense/art.ts` `drawBugBody` 翅膀段（891 附近，flap 振幅 r*0.4*0.5 静态帧）。
- **现状**：16px 下翅膀不进剪影、灰度差 9.1/255。**实机兜底通道在**：飞虫悬浮高度 + 影子分离 + 颜色，故只记建议。
- **期望**：翅膀锚点外移（长在轮廓外），calm 静态帧张角放大一档。
- **建议责任文件**：`sprout-defense/art.ts`。

#### W4R2-07　gold-hook：金块 vs 石头 16px XOR 0%、灰度差 7.0　**【建议】**
- **位置**：`src/games/gold-hook/art.ts` `ORE_SKIN`（金 #FFD264 / 石灰色系明度接近）。
- **现状**：色弱玩家小尺寸下金/石难分；**实机兜底通道在**：实渲半径更大、2s 扫光只给金块家族、重量手感差异。W4R1-05 补渐变时顺手拉开明度即可（金亮停 ≥ 石 fill 明度 +25%）。
- **建议责任文件**：`gold-hook/art.ts`（与 W4R1-05 同笔）。

**本轮新问题统计：阻断 0 / 严重 0 / 一般 5（W4R2-01～05）/ 建议 2（W4R2-06/07）。**
另有 1 条观察不立项：monster-crisis 战场待机画布差分 0%（天空/装饰全静态），佐证 learner 顺延项 R2-TOP4，不重复立项。

## 五、六大专项两轮达标率对比

| 专项 | 第 1 轮（修复后合并态） | 第 2 轮 | 说明 |
| --- | --- | --- | --- |
| ① emoji/火柴人（fillText 同行字面量口径） | 9/9 | **9/9** | 31 例扫描断言仍全绿 |
| ① 同上（本轮变量链深挖口径） | 未测 | **6/9** | garden-guard hud12 残余（W4R1-01）+ duo-arena 飘字（W4R2-01）+ duo-rush HUD（W4R2-02）——达标率「下降」是扫描更深，不是回归；三条全部立项待修 |
| ② 收集物体积 | 8/9 | **8/9** | gold-hook 遗留（W4R1-05），其余 8 款特写 28–121 色渐变证据齐 |
| ③ 双人/家族 16px 可分 | 2/5（适用款） | **2/5** | mc（严重）/duo-rush 未修；本轮新增家族内两两量化（dvs 花名册、gg BOSS、sd 虫、gh 矿石）另立 W4R2-03/04/06/07 |
| ④ 2.5D 纵深 | 9/9 | **9/9** | duo-rush 点名款截图复核透视/雾/视差全在；其余 8 款不退化 |
| ⑤ 360px（溢出/遮挡） | 9/9 | **9/9** | 360+320 双档、入口+玩法双态横向溢出全 0（mc 章节 tab 越界为 `overflow-x:auto` 合法横滑容器） |
| ⑤ 360px（触区 ≥40） | 9/9（入口态） | **8/9（玩法态）** | duo-vs-star 玩法态触控键 38px（W4R2-05），本轮补课口径 |
| ⑥ 商标 | 9/9 | **9/9** | 51 词断言全绿 + 本轮玩法态截图换样本目检（五鸟/双矿工/双画家/12 脸谱）零相似 |

## 六、回归防线

- **判定热区**：各款「判定字段不动」契约测试（RunnerPose 判定口径、BirdPose r 原样传入、HERO_R、gold-hook 钩角数学等）全部在全量里跑、全绿；本轮无绘制改动入库，无热区位移。
- **功能提示被动画吃掉**：源码逐点核对——duo-vs-star 平台裂纹（index 725）无 motion 门控；sprout-defense 预警角标 `warnings` 构建与 calm 无关；garden-guard 波次预览常显；duo-rush reduced 分支只关装饰（旋转/星屑/侧倾）。reduced 实测 9 款全部能进玩法（`reducedAlive` 全 true）、CSS 动画数 6-7→0、画布差分全部 ≤ 正常档（两款 +0.2~0.4pp 为文字重绘噪声）。
- **教育数据**：本窗 9 款无笔顺/音高/题库类数据，不适用（全库教育款快照断言在全量里照跑全绿）。
- **destroy 泄漏**：9/9 款「进 → 看 → 退」×4 后回首页 1 秒 rAF 残留全 **0**（round1 记过的口径全数复验）。

## 七、本轮新增机器化用例

`src/games/__tests__/window4-visual-scan-r2.test.ts`，**9 例**，全绿：

- W4R2-01：duo-arena pushFloat 字面量 emoji 现状 8 处钉住 + r1 修复三件套回归守护（2 例）；
- W4R2-02：duo-rush hudText/branchTag 字面量 8 枚 + 变量链入口钉住（1 例）；
- W4R2-03：duo-vs-star 12 角色 trait 分支齐全守护 + xingxing r*0.34 / jiujiu r*0.22 现状钉住（2 例）;
- W4R2-04：garden-guard drawMonsterSprite 原型 BOSS 无专属配饰现状钉住（1 例）；
- W4R2-05：duo-vs-star ≤380px 档 min-width:38px 现状钉住（1 例）；
- r1 修复回归守护：garden-guard 波次预览真立绘 / sling-birds 横幅（2 例）。

第 1 轮 `window4-visual-scan.test.ts` 31 例一字未动，照跑全绿。

## 八、收尾复跑（合并态）

- 收尾 fetch → rebase `origin/game-1.3-window4`：r2 learner 三笔（计划 `760b0d7`、文案+3 例 `3ca529b`、报告 `c1adbb0`、水位 `40b1d77`）已并入，其报告与文案一字未碰；**r2 fixer 截至本报告提交尚未落地**——W4R1-02（严重）與 W4R2-01～05 全部待其处理，修复确认由其收尾复验 + 第 3 轮对账。
- `npm test` 合并态：**710 文件 / 15259 例全绿**（15247 → +1 文件/+12 例：learner +3、本轮 +9，只增不减）。注：合并后第 1 次全量出现 1 例偶发失败（tail 摘要只余统计行未留名），随后同一工作区连续 2 次全量 **710/15259 全绿**、第 4 次复跑亦绿，判定为偶发（全库既有慢测/时序类波动），未发现与本轮改动相关。
- `npm run build`：绿（precache 188 项 5237.48 KiB）。
- 本轮未触碰：任何绘制代码、判定与数值、`docs/plan-1.3-*.md`、`.cursor/skills/**`、round1 三份与 r2 learner/fixer 的报告。
