# 1.3 窗口 5 · 第 3 轮(终验)监督修复员报告(C 档)

> 角色:窗口 5 第 3 轮(共 3 轮,终验)**监督修复员** —— 清零收官,交付本窗 10 款可放行的终态。
> 工作分支 `game-1.3-window5`;基线 = 远端 tip `80c723d`(round3 tester 定稿,A 档已给「放行——仅本窗 10 款」)。
> 输入(只读):A 档终验 `docs/qa/1.3-window5-round3-tester.md`(10×6 = 60 格 0 不达标)、
> 第 2 轮遗留 `docs/qa/1.3-window5-round2-fixer.md`(R2-b:C-2/C-3 登记交本轮)、
> B 档 R2 `docs/qa/1.3-window5-round2-learner.md`(C-2/C-3 绘制规格在案)。
> B 档 R3 中途仅到计划 commit(`86c7f6e`),定稿报告与 `docs/qa/1.3-window5-style-baseline.md` 截稿未到 ——
> 按口令先按 A 档终验 + R2 遗留动手,收尾按「短等一次 fetch」流程处理(见第五节)。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 红线:只动绘制层(绘制函数 / CSS / 视觉常量);判定、数值、关卡数据、存档 key、玩法断言、parentAuth / parentGate 零触碰;
> `src/art/kit/` 已有文件只 import 不改一字;A / B 档报告一字不碰;用例只增不减、ratchet 只紧不松;禁 three.js。

## 〇、清零计划(动手前立项,commit `6ef2f40`)

修复顺序铁律:A 档终验总表不达标格(本轮 **0 格**,判定不改——不为「显得有活」制造工作)→
第 2 轮遗留交本轮项(C-2 / C-3)→ B 档点名仍违和款(B 档 R3 未到,按 R2 规格)→ 一般 → 建议。
风格基线未到,沿用已落地约定:左上 45° 光照、两停渐变、描边 1.5–2px 深 20%、reduced 静态或淡出;终验轮不引入新风格。

## 一、清零清单(逐项登记)

| # | 来源 / 严重度 | 款 | 位置 | 修法(全部只动绘制层) | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | **A 档终验总表** | — | 10 款 × 6 专项 | 60 格 0 不达标,**无不达标格可修**;判定一格未改 | — | — |
| 1 | **C-2 = R2-b 遗留 / B 档 R2 一致性排名 3**(建议) | bowling-lane | `visual13.ts` 新增 3 个视觉常量;`index.ts` render 暗底段 +2 道覆盖层 | 粉彩夜场,B 档 A/B 二选一**裁决选 A(暗底提暖一档,调参不加料)**:暗底 `#3b3556` 整屏提亮 6%(白覆盖层,逐通道 = `shade("#3b3556",+6)`,覆盖层实现是为保住 R1 修复用例的 `#3b3556` 图层序锚点)再叠 4% 粉紫 tint `#E3A9E0`(粉彩家族暖紫,不用字母不拼品牌色带);两道覆盖层压在暗底之后、邻道剪影之前,不进跟球缩放 —— 主道 / 灯箱 / 邻道剪影 / 立柱一个数不动,「暗场馆」主题与「亮度预算只留给球道与灯箱」的克制保持(实测叠后亮度涨幅 ≤0.08、仍不足主道木色一半) | `visualFixR3.test.ts` **+1 文件 4 例**(提亮 6% + 粉紫 4% 档位合计 ≤0.12 / 白覆盖层与 `shade(+6)` 逐通道数学等价 / tint 粉紫家族且叠后 r/b 比升高、亮度仍暗场 / index.ts 图层序钉死) | `351bbc4` |
| 2 | **C-3 = R2-b 遗留 / B 档 R2 一致性排名 5**(建议) | tank-battle | `visual13.ts` 新增 `drawTurretSheen` 画笔 + 3 常量 + `BOLT_YELLOW`,star/bolt 徽章描边改线;`index.ts` 炮塔段 1 处调用替换 | 工艺代差(轻),克制执行两笔:① 炮塔圆顶左上高光弧从 1px 白细线(`rgba(255,255,255,.6)`)换 `drawTurretSheen` —— `shade(炮塔顶色,+18)` 2px 圆头静态弧,弧段与原细线同位(π×0.9→π×1.45 左上象限),家族光照上主角;② 星徽章手写 `shade(金,-26)`/`max(1,r×0.16)` 描边与闪电徽章手写铁灰描边,统一换 kit `strokeOutline`(深 20% / 1.5px / 圆角接头),B 档「全款 0 处 strokeOutline」清零;**地形块顶侧双面方言一个不动、不新增徽记**(商标提醒对照执行) | `visualFixR3.test.ts` **+1 文件 8 例**(+18/2px/左上象限弧段规格对表 / 落笔时 strokeStyle 与线宽与圆头快照 / r≤0 不画 / index.ts 换笔在场且 1px 白细线 2→≤1 只降不升 / 星与闪电徽章描边 = shade(主色,-20)+1.5px / visual13 内 strokeOutline ≥2 处 / cellBlock 方言源码钉死 / KIND_BADGE 徽记集合不变)。**勘误:commit 说明写「7 例」系笔误,实为 8 例(以本报告与 15411 总数为准)** | `2f192d6` |
| 3 | **R2-a 遗留**(功能文字口径) | prince-princess | DOM 出场卡 `pcp-cue-icons` + HUD chips | **评估后不修,转 backlog(BL-1)**:出场卡 emoji 来自 `teach.ts` 教学口令数据(「先图后字」的图本体),HUD chips 为 DOM textContent;矢量化需重构 DOM 结构并波及教学 / HUD 断言,非低成本,且红线禁止改玩法 HUD 文案语义;画布侧字形账户 R2 已清零(`emoji()`=0),本条为 DOM 层尾巴 | 现有闸(`visualScanR2` / `visualGuardW5R3` 水位表)继续在跑 | — |
| 4 | **L-2(G6–G8)/ L-3(G9)/ L-4 / L-5(Z2/G11)遗留** | 全库 / ss·tb / pfb / PLAN | 各处 | 逐条评估均为窗外统一轮次、非低成本或主管口径 → **backlog(BL-2/BL-3/BL-4/BL-5)**,每条带来源 / 位置 / 期望 / 估算改动面 | — | `2319655`(backlog 成文) |
| 5 | **R3-a(A 档终验新登记,一般·窗外)** | 共享层 | `src/ui/parentAuth.ts` + `dialogs.ts` | **不修(三档禁改区)**→ backlog(BL-6)挂账交全库轮次,A 档复现记录与修复建议随条结转 | — | 同上 |
| 6 | **kit 终态整理** | `src/art/kit/` | 12 个模块 | 复核:三轮 C 档均未新增 kit 文件;12 模块全部有窗外消费者(palette 30 / outline 12 / sparkle 10 / volume 7 / block25d 4 / bubbleSkin·chibi·star 3 / parallax·snow 2 / carnival·mirror 1)——**无死文件可删,他人文件与公共导出零触碰**(本轮新增 import:tank-battle `visual13` 引 kit `outline.strokeOutline`,只 import 不改一字) | — | — |

**清零统计(按来源)**:A 档终验总表不达标 **0 条(无活)**;第 2 轮遗留交本轮 **2 条全修**(C-2 / C-3,即 B 档 R2 一致性点名 5 款中最后 2 款落地——至此 B 档 R2 点名 5/5 全部处理完毕);
遗留评估转 backlog **6 条**(R2-a、L-2、L-3、L-4、L-5、R3-a)。
**按严重度**:阻断 0 / 严重 0 / 一般 0(R3-a 为窗外禁改区,登记非修复项)/ 建议 2(C-2 / C-3)。
共 **2 个修复 commit + 1 个计划 commit + 1 个 backlog commit**;新增配套用例 **2 文件 12 例**,既有用例零删改,
A 档 ratchet(`visualScan13` ×10、`visualGuardW5R3` 水位整表、`visualFix13` / `visualFixR2` 全部)一字未碰、全部在跑全绿。

## 二、降级申请记录

**无。** 终验后六大专项 10 款 × 6 项维持 0 格不达标;交本轮的 C-2 / C-3 全部修复落地,无「确实修不动」项,故无需书面降级申请。

## 三、backlog 条目统计(`docs/qa/1.3-window5-backlog.md`,commit `2319655`)

共 **6 条**(0 阻断 / 0 严重;一般 1 = BL-6 parentAuth 窗外共享层;建议 4 = BL-1 pp DOM 出场卡与 HUD chips、BL-2 全库 1.2 基线三件、BL-3 HUD 芯片行 360px 观感、BL-4 pfb 🍬 计数字;主管口径 1 = BL-5 PLAN 文档点名词)。
每条带「来源轮次 / 位置 / 期望 / 估算改动面」;BL-1/BL-3/BL-4 归并「HUD 统一走查」一揽子轮次,BL-2/BL-6 归全库共享层轮次,BL-5 待主管裁决。

## 四、终态合并数据

1. **合并态复验 SHA:`2319655`**(计划 + C-2 + C-3 + backlog 全部入内;本报告定稿 commit 随后压顶为远端 tip)。
   链:`80c723d`(A 档定稿)→ `86c7f6e`(B 档 R3 计划)→ `6ef2f40`(C 档计划)→ `351bbc4`(C-2)→ `2f192d6`(C-3)→ `2319655`(backlog)。
2. `npm test`:**748 文件 / 15411 例全绿**(179.0s)。三轮曲线只增不减 ✅:
   R1 合并态 740/15365 → R2 合并态 745/15395 → R3 tester 定稿 746/15399 → **R3 fixer 合并态 748/15411**(+2 文件 / +12 例 = C-2 4 例 + C-3 8 例)。
   全库零 skip / todo / fails;`visualGuardW5R3` 四道守卫(文件在位 / 零 skip / emoji / fillText 水位整表)全绿。
3. `npm run build` 全绿(tsc + vite,609ms):dist **5.6M 持平**;PWA precache **194 entries / 5240.12 KiB**
   (对比 R3 tester 起点 195 / 5240.35:**−1 entry / −0.23 KiB** —— 经 80c723d 与 HEAD 双构建 diff 定位,
   消失的是 kit `palette.js` 独立小 chunk:tank-battle `visual13` 新增 import kit `outline`(outline 依赖 palette)后
   Rollup 将 palette 并入 `outline` 共享 chunk,`#C9A8F0` 等 palette 代码在产物内验证在场;良性 chunk 合并,总体积不升反降)。
4. 本窗 10 款 chunk(KiB,括号内 R2 合并态基线):shoot-range 60.1(60.1)、sky-squad 77.9(77.9)、tank-battle 70.5(70.5,C-3 换笔零净增)、
   bomb-buddies 81.7(81.7)、snow-fight 68.9(68.9)、bumper-cars 61.1(61.1)、**bowling-lane 47.8(47.7,+0.1K = C-2 两道覆盖层)**、
   ice-fire-forest 52.2(52.2)、puff-bros 72.9(72.9)、prince-princess 74.5(74.5)。**总涨幅 ≈ +0.002%,远低于 15% 门槛,无需逐项说明仍逐项列出。**
5. `npx vite preview` 冒烟:首页 / `sw.js` / `manifest.webmanifest` + 本窗 10 款产物 chunk = **13/13 全 200** ✅。

## 五、B 档 R3 交付物处理

- B 档 R3 计划 commit(`86c7f6e`)中途到达,已 rebase 到其上保留;定稿报告 `docs/qa/1.3-window5-round3-learner.md` 与
  `docs/qa/1.3-window5-style-baseline.md` 按口令流程处理:先把已修项写进本报告并 push → 短等一次 fetch →
  **(收尾时状态见第七节收官声明附记;两路径均未被本档占用,一字未写)**;
- C-2 / C-3 的修复规格完全取自 B 档 R2 报告原文(排名 3 / 排名 5 的绘制规格级建议),风格约定沿用已落地家规
  (左上 45°、两停渐变、kit 描边、reduced 静态),终验轮未引入任何新风格 —— B 档基线后到也不会与本轮修复冲突。

## 六、纪律自查

- **只动绘制层**:C-2 = 视觉常量 + render 覆盖层两道 fill;C-3 = 画笔函数与描边取色 / 线宽;
  判定、数值、关卡数据、存档 key、玩法断言、`parentAuth` / `parentGate` 零触碰(R3-a 未修,按禁令登记结转);
- **kit 纪律**:`src/art/kit/` 已有文件只 import 未改一字(新增 import 仅 tank-battle `visual13` 引 `outline.strokeOutline`);
  未新增 kit 文件;死文件复核无一可删;禁 three.js 遵守(零依赖新增);
- **测试红线**:用例只增不减(+2 文件 12 例);A 档 / 前两轮 ratchet 一个没放宽(C-3 反而新增「1px 白细线 2→≤1」收紧闸);
  零 skip;本档未动任何他人测试文件;
- **报告纪律**:A / B 档报告与 `docs/plan-1.3-*.md`、`.cursor/skills/**` 一字未碰;本报告写在 `docs/qa/1.3-window5-round3-fixer.md`
  (未占全局 `docs/qa/1.3-round3-fixer.md`);backlog 写在 `docs/qa/1.3-window5-backlog.md`(未占全局 `docs/qa/1.3-backlog.md`);
- **分级红线**:本轮新增绘制为暖紫 tint 与高光弧 / 描边,无血 / 伤害 / 死亡描写,无广告内购,无商标与官方角色
  (C-2 tint 只调色不加图形;C-3 不新增徽记,B 档增量风险提醒逐条对照);
- **分支纪律**:工作基于 `origin/game-1.3-window5`;只普通 push;未动 `main` / `game-1.2` / `game-1.2-kk` / `game-1.3` 主线;
  未 force;未用 `gh` 开 / 改 PR;未派生任何云端子代理。

## 七、窗口 5 · 1.3 视觉升级收官声明

**窗口 5 · 1.3 视觉升级(第 29 步 C 档收窄版)三轮验收至此收官。**

- **终态**:交付物为 `game-1.3-window5` 分支终态,合并态复验 SHA **`2319655`**(本报告定稿 commit 随后压顶为远端 tip;不合并、不动 `game-1.3` 与 `main`);
- **结论**:A 档终验「放行(仅本窗 10 款)」维持有效 —— 六大专项 10 款 × 6 项 0 格不达标,阻断 / 严重级终态 **0**,
  B 档 R2 一致性点名 5 款 5/5 全部落地(N1 / C-1 / N3 于 R2,C-2 / C-3 于本轮),
  `npm test` 748 文件 / 15411 例全绿(三轮用例曲线 15365 → 15395 → 15399 → 15411 只增不减)、
  `npm run build` 全绿(总体积持平)、`vite preview` 冒烟 13/13 全 200;
- **遗留**:backlog **6 条**(0 阻断 0 严重,全部窗外轮次 / 主管口径 / HUD 统一走查项),已成文 `docs/qa/1.3-window5-backlog.md` 作为本窗后续输入。

- 派发指定模型 slug:`claude-fable-5-thinking-xhigh`(实际使用)。
