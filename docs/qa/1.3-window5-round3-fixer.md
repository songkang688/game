# 1.3 窗口 5 · 第 3 轮(终验)监督修复员报告(C 档)

> 角色:窗口 5 第 3 轮(共 3 轮,终验)**监督修复员** —— 清零收官,交付本窗 10 款可放行的终态。
> 工作分支 `game-1.3-window5`;基线 = 远端 tip `80c723d`(round3 tester 定稿,A 档已给「放行——仅本窗 10 款」)。
> 输入(只读):A 档终验 `docs/qa/1.3-window5-round3-tester.md`(10×6 = 60 格 0 不达标)、
> 第 2 轮遗留 `docs/qa/1.3-window5-round2-fixer.md`(R2-b:C-2/C-3 登记交本轮)、
> B 档 R2 `docs/qa/1.3-window5-round2-learner.md`(C-2/C-3 绘制规格在案);
> B 档 R3 报告与 `docs/qa/1.3-window5-style-baseline.md` 开工时未到 —— 先按 A 档终验 + R2 遗留动手,收尾前再 fetch 补齐。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 红线:只动绘制层(绘制函数 / CSS / 视觉常量);判定、数值、关卡数据、存档 key、玩法断言、parentAuth / parentGate 零触碰;
> `src/art/kit/` 已有文件只 import 不改一字;A / B 档报告一字不碰;用例只增不减、ratchet 只紧不松;禁 three.js。

## 〇、清零计划(动手前立项)

修复顺序铁律:A 档终验总表不达标格(本轮 **0 格**,判定不改——不为「显得有活」制造工作)→ 第 2 轮遗留交本轮项 → B 档点名仍违和款(B 档 R3 未到,先按 R2 规格)→ 一般 → 建议。

| # | 来源 | 项 | 处置 |
| --- | --- | --- | --- |
| 1 | A 档终验总表 | 60 格 0 不达标 | **无活可修,不改判定** |
| 2 | R2 遗留 R2-b / B 档 R2 一致性排名 3 | **C-2 bowling-lane 粉彩夜场** | 修——B 档 A/B 二选一,裁决选 **A(暗底提暖一档)**:`#3b3556` 提亮 6%(≈`shade(+6)`)再叠 4% 粉紫 tint,把「灰紫夜场」调成「粉彩夜场」;调参不加料,暗场馆主题与「亮度预算只留给球道与灯箱」保持;配套用例新增 `visualFixR3.test.ts` |
| 3 | R2 遗留 R2-b / B 档 R2 一致性排名 5 | **C-3 tank-battle 炮塔高光弧** | 修——克制执行两笔:① 炮塔圆顶左上高光弧从 1px 白细线换 `shade(炮塔顶色,+18)` 2px 家族语言;② 徽章描边换 kit `strokeOutline` 1.5px 对齐家规;**地形块方言一个不动**;不新增徽记(商标提醒);配套用例新增 `visualFixR3.test.ts` |
| 4 | R2 遗留 R2-a | pp DOM 出场卡 / HUD chips 功能文字 | 评估后 **backlog**:出场卡 emoji 来自 `teach.ts` 教学口令数据(「先图后字」的图本体),HUD chips 为 DOM textContent;矢量化需重构 DOM 结构并波及教学 / HUD 断言,非低成本,且不许改玩法 HUD 文案语义 |
| 5 | R1/R2 遗留 L-2(G6–G8)/ L-3(G9)/ L-4 / L-5(Z2/G11) | 全库基线 / HUD 走查 / 主管口径项 | 逐条评估:窗外或非低成本者进 `docs/qa/1.3-window5-backlog.md`(带来源 / 位置 / 期望 / 估算改动面) |
| 6 | A 档终验新登记 R3-a | parentAuth 跨路由弹窗残留 | **不修**(三档禁改区,窗外共享层)——backlog 挂账交全库轮次 |
| 7 | kit 终态整理 | 三轮里 C 档新增且无人引用的死文件 | 复核后处置(R1/R2 修复员均未新增 kit 文件,预计无活) |

每修一处一个 commit(`fix(visual-r3): <款> <问题>`);配套用例只增不减,不放宽 ratchet;
收尾:fetch → rebase(保留他人文件)→ `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟 →
记录终态 SHA / 用例曲线 / 体积对比 → 收官声明 → 普通 push `origin game-1.3-window5`。
禁 force、不动 `main` / `game-1.2` / `game-1.2-kk` / `game-1.3` 主线、不用 `gh` 开 / 改 PR。

(以下各节随修复推进补齐。)
