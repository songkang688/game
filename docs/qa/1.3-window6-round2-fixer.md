# 1.3 窗口 6 · C 档 · 第 2 轮视觉监督修复员报告

- 角色:窗口 6 视觉验收三人组 第 2 轮 · C 监督修复员(落地 A/B 档问题清单 + 第 1 轮遗留清偿 + 合并态守门)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款)
- 输入:本轮 A 档 `docs/qa/1.3-window6-round2-tester.md`(对账 16 条:已修 10 / 不彻底 0 / 回归 0;新问题仅 W6R2-01 建议级;六大专项 46/46)+ 第 1 轮遗留清单 `docs/qa/1.3-window6-round1-fixer.md` §2 + 本轮 B 档 `docs/qa/1.3-window6-round2-learner.md`(开工时未到,先修 A 档与遗留,B 档到达后 rebase 跟进)
- 状态:**修复计划(动手前提交)。**

## 〇、修复计划(动手前提交)

修复顺序铁律:第 1 轮「不彻底/回归」最优先 → 本轮新阻断 → 严重 → B 档一致性 → 一般 → 建议。本轮 A 档对账:**不彻底 0 · 回归 0 · 新阻断 0 · 新严重 0**,直接进入遗留清偿与建议级,按 A 档 §7 移交清单优先级执行。每修一处一个 commit(`fix(visual-r2): <款> <问题>`),每处配套只增不减的视觉用例。

| 序 | 条目 | 位置 | 计划修法 |
| --- | --- | --- | --- |
| 1 | W6R1-09/10 壳层触区/字号统一抬 | 9 款各自 index.ts 的壳层 CSS(`.bvp-btn` / `.bh-btn` / `.bh-mode` / `.ba-btn` / `ak-open` / `as-open` / `as-back` / `brk-open` / `mp-open` / `blp-open` / `bbp-open` / chips / tips / tags) | 各款自有按钮 min-height 抬到 ≥40px、正文字号抬到 ≥14px;共享壳层(`l99-*`)不属本窗口文件,登记交统筹,不越权改 |
| 2 | W6R1-07 brave-path emoji 图标族 | `src/games/brave-path/index.ts` bvp-opt-em / bvp-ico 位点 + `visual.ts` 新增图标族函数 | 选项卡/背包/商店大号 emoji 换 badge 族参数化 SVG(落影 + 1.5px 描边 + 左上高光,与 mazeItemSvg 同族);类名/热区/dataset 一字不动 |
| 3 | adventure-king 古堡机关小图标 | `src/games/adventure-king/index.ts` `cellGlyph` 机关分支 | 门/钥匙/锁/开关/传送/贴纸 emoji 换参数化 SVG(与 castleHeroSvg/castleBoxSvg 同族规格);qa1 emoji 上限断言只降不升 |
| 4 | bubble-aim HUD 字符 | `src/games/bubble-aim/index.ts` 画布 HUD fillText(⚠️/♾️/⭐) | 换 12px 单色矢量路径(警示三角 2px 描边 / 横 8 字 / 五角星,与 starPath 同族);文案文本不动 |
| 5 | W6R2-01 仓鼠侧/背朝向 16px 灰度 | 新增 `src/art/kit/hamsterAccents.ts`(kit 只增不改)+ `box-hamster/visual.ts` 组合 | 按 moleAccents 先例:B 鼠顶饰呆毛放大加描边、A 鼠小花加描边扩剪影,目标全朝向 diffPct ≥3%;hamsterSvg.ts 一字不动 |
| 6 | B 档遗留·alien-seek 藏匿点内腔 | `src/games/alien-seek/index.ts` 藏匿点绘制 | 内腔 2 停径向渐变(#3E3A66 → -18%)+ "?" 换 2.2px 描边白问号路径 + 落影 |
| 7 | B 档遗留·bubble-pop 池壁/光柱 | `src/games/bubble-pop/visual.ts` `bpVisualCss` | 池壁双层(外 3px 白 .5 + 内 1px #9FD6FF .35);光柱 .08→.12;新增摆动差走 reduced 静止 |
| 8 | W6R1-13 描边分级约定 | 新增 `src/art/kit/conventions.md`(kit 只增不改) | 把「按渲染尺寸分级」的描边宽度约定写成 kit 约定文档,不动冻结 kit 注释 |
| 9 | 遗留登记 | 报告 | W6R1-08(40px 数学放不下)、W6R1-11(badge.ts 冻结、叠加不成立)、l99 共享壳层触区/字号(跨窗口)写清理由交第 3 轮 |

红线自检:只动绘制函数 / CSS / 视觉常量 / kit 新增文件;判定、数值、关卡数据、存档 key、玩法测试断言一律不碰;不引入 three.js;不碰 parentAuth / parentGate;别人的报告只读不改;`hamsterSvg.ts` / `badge.ts` / `moleSvg.ts` 等既有 kit 冻结不动;A 档 9 款实测 59–60fps,不做性能减法。

(以下各节修复完成后回填。)
