# 1.3 窗口 6 · C 档 · 第 2 轮视觉监督修复员报告

- 角色:窗口 6 视觉验收三人组 第 2 轮 · C 监督修复员(落地 A/B 档问题清单 + 第 1 轮遗留清偿 + 合并态守门)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款)
- 输入:本轮 A 档 `docs/qa/1.3-window6-round2-tester.md`(对账 16 条:不彻底 0 / 回归 0;新问题仅 W6R2-01 建议级;六大专项 46/46)+ 第 1 轮遗留清单 `docs/qa/1.3-window6-round1-fixer.md` §2 + 本轮 B 档 `docs/qa/1.3-window6-round2-learner.md`(开工时未到,先修 A 档与遗留;B 档 push 后 rebase 跟进,其修订清单 #1–#6 全部落地、#7–#9 按其建议登记)
- 状态:**本轮完成。修复 9 处(commit 9 条)+ 新增 kit 2 件 + 新增用例 7 文件 / 64 条;合并态 751 文件 / 15580 用例全绿。**

## 〇、修复计划(动手前提交,commit `0dd355e`)

修复顺序铁律:第 1 轮「不彻底/回归」最优先 → 本轮新阻断 → 严重 → B 档一致性 → 一般 → 建议。本轮 A 档对账:**不彻底 0 · 回归 0 · 新阻断 0 · 新严重 0**,直接进入遗留清偿与建议级,按 A 档 §7 移交清单优先级执行。每修一处一个 commit(`fix(visual-r2): <款> <问题>`),每处配套只增不减的视觉用例。计划表原文见 commit `0dd355e`(9 条,与下文修复清单一一对应;第 8 条 conventions.md 按 B 档 §4-9 的更优建议改为「约定写进新 kit 文件头注」,见修复 9)。

红线自检:只动绘制函数 / CSS / 视觉常量 / kit **新增**文件;判定、数值、关卡数据、存档 key、玩法测试断言一律不碰;不引入 three.js;不碰 parentAuth / parentGate;别人的报告只读不改;`hamsterSvg.ts` / `badge.ts` / `moleSvg.ts` 等既有 kit 冻结不动;A 档 9 款实测 59–60fps,不做性能减法。

## 一、修复清单(来源 / 位置 / 修法 / 配套用例 / commit)

| # | 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- |
| 1 | W6R1-09/10(第 1 轮 C 档「第 2 轮统一抬」)| 9 款各自 index.ts / visual.ts 的壳层 CSS | 新增 kit `src/art/kit/uiTouch.ts`(`touchUpliftCss` min-height 40 / min-width 44、`bodyFontUpliftCss` 14px),9 款各自的按钮/模式卡/返回位统一接线;mole-pop 因「纯字面量 CSS」守卫改写成字面量并用 kit 常量钉住;共享壳层 `l99-*` 不属本窗口文件,不越权改(见 §2 遗留) | `src/games/window6.r2.fixer.uplift.test.ts`(12 条:kit 函数规格 + 9 款逐款接线 + mole-pop 字面量对表) | `97d62c5` |
| 2 | W6R1-07(两轮遗留,B 档一致性点名第 1)| `brave-path/visual.ts` 新增 `rowIconSvg` 图标族 + `index.ts` 七个位点 | 选项卡/背包/商店/装备/技能/补给/祝福约 20+ 位点的大号 emoji 换 badge 族参数化 SVG(64 视窗、落影椭圆、1.5px 墨描边、左上高光、粉彩板;道具按 id / 装备按槽 / 技能按型,元素与稀有度 tint 双参),未知键回退原 emoji 兜底;数据文件的 emoji 字段一个不动;`.bvp-opt-em`/`.bvp-ico` 类名热区不动 | `brave-path/visual.fixer.r2.test.ts`(图标族规格 + 族内 16px 灰度两两可分【实测 5.5–33.6%】+ 七位点接线 + 数据不动) | `e548108` |
| 3 | 第 1 轮 TOP-5 未竟部分(B 档一致性点名第 2)| `adventure-king/visual.ts` 新增 `castleGlyphSvg` + `index.ts` `cellGlyph` | 古堡机关 12 态(出口/钥匙/锁/踏板/板门/灯亮/灯灭/彩门/木板/门楔/传送/贴纸)emoji 换与 castleHeroSvg/castleBoxSvg 同族参数化 SVG(64 视窗 + 落影 + 1.5px 墨描边 + 左上高光);灯亮/灯灭按 16px 灰度校准拉开;格子判定与 explore.ts 一字不动;「传送=双弧旋」按 B 档商标规避要点执行(非三角堆叠/字母形) | `adventure-king/visual.fixer.r2.test.ts`(12 态规格 + 两两 16px 灰度 + `cellGlyph` 接线 + emoji 上限只降不升) | `092d3a5` |
| 4 | 第 1 轮登记项(B 档一致性点名第 4)| `bubble-aim/visual.ts` 新增三支 painter + `index.ts` 画布 HUD | fillText 里的 ⚠️/♾️/⭐/☆ 换单色矢量件:`paintWarnTriangle`(圆角三角 2px 描边 + 感叹号)/`paintInfinity`(双环横 8)/`paintStarRow`(金星实心/灰星空心,与 starPath 同族十点星);颜色由调用方给、跟随 HUD 呼吸透明度,不新增色板;文案本体一字不动 | `bubble-aim/visual.fixer.r2.test.ts`(三 painter 作画规格 + fillText 字符清零 + 文案不动) | `f03a081` |
| 5 | **W6R2-01**(本轮 A 档新问题,建议级)| 新增 kit `src/art/kit/hamsterAccents.ts` + `box-hamster/visual.ts` 组合 | 按 moleAccents 先例做叠加层(hamsterSvg.ts 冻结不动):A 鼠花冠放大(瓣 1.9→3.0、轨道 3.4→5.0)+ 每瓣墨描边,侧脸往鼻尖侧偏 1.5;B 鼠呆毛加粗成「墨底 4.2 + 色芯 2.0」双笔道加第三撮,facing3 数值镜像往背拱甩;B 鼠再加仓鼠经典深色背纹(背影整条 5.6px / 侧脸背拱一小段;正脸看不见不画);背影头冠下潜 4.5 贴头。注入 bhh-figure 组内,推箱前倾跟着转 | `box-hamster/visual.fixer.r2.test.ts`(**四朝向 idle 全 ≥3%:实测 7.03/5.08/4.69/5.08;push 侧向 5.86/5.08**;kit 规格 + 组合接线 + 冻结件不含叠加层) | `6b6947c` |
| 6 | W6R1-07 收尾(B 档 §6 登记:「文字与画面说的不是一套语言」)| `brave-path/index.ts` 迷宫 HUD 与介绍卡 6 处字符串 + 过层进度条终点点位 | 🔑🚪🏁🔒 全文清零:HUD 汇报改「钥匙徽章 / 木门 / 终点小旗」;过层进度条终点点位从 🏁 字符改画 `mazeItemSvg("exit")` 棋盘小旗(`.bvp-dot-goal svg` 20px 尺寸规则);游戏文案语言与盘面徽章一致 | `brave-path/visual.fixer.r2.test.ts` 新增 describe(四 emoji 清零 + 六句改口 + 终点徽章接线) | `c124fc2` |
| 7 | B 档第 1 轮建议级(修订清单 #6)| `alien-seek/visual.ts` 新增 `cavityGrad` + `index.ts` `drawSpotShape` / `drawQuestionCloud` | 八种藏身处内腔统一 2 停径向渐变(中心 `#3E3A66` → 边缘 -18%,逐 kind 给开口圆心),掀开后不再平涂;点错问号云 `fillText("?")` 换 2.2px 描边白问号路径(钩 arc+quad、点实心圆)+ 薰衣草落影;命中判定圆心半径与 spotUncover 一个数不动 | `alien-seek/visual.fixer.r2.test.ts`(渐变两停规格 + 8 处逐 kind 接线 + 旧平涂退场 + 问号路径化) | `157e095` |
| 8 | B 档第 1 轮建议级(修订清单 #5)| `bubble-pop/visual.ts` token / `bpVisualCss` | 光柱 `--bp-lightbeam` .08→.12(360px 上可见);两道柱加 5200ms 慢摆(skewX -17°↔-15° = 2° 振幅,B 柱负半周期延迟反相),reduced `animation:none` 静止回基线 -16°;池壁改双层描边(外 3px 白 .5 原样 + 内 1px `#9FD6FF` .35);宽屏 `.bp-cell min-width:36px` 一个像素不动;visual.test.ts 的 token 对表断言随规格同步 .08→.12(用例数不变) | `bubble-pop/visual.fixer.r2.test.ts`(token/摆动/反相/reduced/池壁双层 + 36px 热区复查) | `a7a05fe` |
| 9 | W6R1-13(B 档修订清单 #9:先在新文件立约)| `src/art/kit/hamsterAccents.ts` 头注 | 描边宽度分级约定落进本轮新增 kit 文件头注(按**渲染尺寸**取档:64px 件 1.2–1.5px / 128px 件 2–3.5px / 16px 关键差异件 ≥2px);冻结老文件的注释待解冻决策后补(见 §2) | `box-hamster/visual.fixer.r2.test.ts` 新增 describe(头注三档钉字) | `5824758` |

修复统计:**按来源** —— 第 1 轮遗留清偿 5 处(#1/2/3/4/9)、本轮 A 档新问题 1 处(#5)、B 档建议级 2 处(#7/8)、B 档本轮新登记 1 处(#6);**按严重度** —— 阻断 0 / 严重 0(本轮输入即无)/ 一般 2(#1/#2)/ 登记级 3(#3/#4/#6)/ 建议级 4(#5/#7/#8/#9)。

## 二、第 1 轮遗留清单处理结果(清 6 · 余 3)

第 1 轮 C 档 §2 移交 8 项 + 本轮 A 档新增 1 项(W6R2-01),处理结果:

| 遗留项 | 结果 |
| --- | --- |
| W6R1-07 brave-path emoji 图标族 | **清偿**(修复 #2 + #6,含 B 档本轮新登记的文案改口) |
| W6R1-09/10 壳层触区/字号 | **本窗口范围内清偿**(修复 #1:9 款自有壳层全部 ≥40px/≥14px);**l99-\* 共享壳层残余登记**(见下) |
| adventure-king 古堡机关小图标 | **清偿**(修复 #3) |
| bubble-aim HUD 字符 | **清偿**(修复 #4) |
| alien-seek 内腔 / bubble-pop 池壁光柱(B 档建议级两条) | **清偿**(修复 #7/#8) |
| W6R1-13 描边分级约定 | **部分清偿**(修复 #9:约定已在新 kit 文件立约;老 kit 文件注释待解冻) |
| W6R2-01 双鼠侧/背朝向(本轮新增) | **清偿**(修复 #5,当轮清零) |
| W6R1-08 bubble-pop 360px 下 40px 触区 | **继续遗留**:40px 触区需 8×40+7×4=348px 盘宽,360 屏物理上限约 303px(已扣池壁与页边),**数学上放不下**;窄屏格子等比收窄 + 8 列全可见的现方案是可达最优,宽屏 36px 热区一个像素不动(修复 #8 配套用例持续复查)。交第 3 轮终验裁决 |
| W6R1-11 badge 底圆 2 停渐变(花徽 vs 星徽 16px 灰度 1.6%)| **继续遗留**:`badge.ts` 为冻结 kit,「新增叠加 kit」对**改底色**需求叠不上去(叠加层画在底圆之上会盖掉徽记本体);B 档 §4-8 同判「需统筹解冻或 kit 归属档执行」。颜色 + 图形双通道兜底可用,不阻塞。交第 3 轮 |
| l99-\* 共享壳层触区/字号(W6R1-09/10 残余)| **继续遗留**:`src/ui/` 共享壳层跨全部窗口,本窗口单方面改会造成 76 款不一致,第 1 轮 C 档与本轮 B 档 §4-7 均判「交统筹派单」。本窗口 9 款**自有**壳层已全部达标(修复 #1)。交统筹 |

**结果:9 项处理 6 项清偿 + 1 项部分清偿,余 3 项(W6R1-08 / W6R1-11 / l99 共享壳层)全部写清理由移交第 3 轮或统筹,无一悬空。**

## 三、一致性统一化说明(对照 B 档 §3 点名 5 款)

B 档点名的 5 款全部处理,统一化只动绘制层:

1. **brave-path(点名第 1)**:图标语言「半绘制半 emoji」→ 全 badge 族(修复 #2);「文字与画面两套语言」→ 文案随图标改口(修复 #6)。徽章底圆渐变仍卡 badge.ts 冻结(§2 遗留)。
2. **adventure-king(点名第 2)**:同款内部双标(走廊全绘制 vs 古堡机关 emoji)→ 机关 12 态全换同族 SVG(修复 #3)。B 档建议的共享 kit `gateBadgeSvg.ts` 未采用:两款的门/钥匙/锁各自随本款主角族(mazeItemSvg 族 / castleHeroSvg 族)取色板与落影参数,强行合一支 kit 反而要抹平两族已校准的灰度阈值与规格差,收敛价值低于风险——已按「图标族规格一致、实现各归各家」落地,特此说明。
3. **box-hamster(点名第 3,W6R2-01)**:16px 通道洼地补平(修复 #5),四朝向 4.69–7.03% 全过 3% 家规线;描边分级约定同步立约(修复 #9)。
4. **bubble-aim(点名第 4)**:9 款最后的 canvas 字符图标位点清零(修复 #4),家族「零 emoji 主体」达成。
5. **bubble-pop(点名第 5)**:背景氛围层最弱款补强(修复 #8),水下三层(光柱/水草/装饰泡)全部肉眼可读。

统一化之后:9 款图标语言全 SVG 化(数据字段与兜底除外)、左上光/墨描边/粉彩板/三段反馈链/reduced 降级路径五个维度维持 A 档 46/46 的家族基线。

## 四、性能回压(未回压,说明原因)

A 档本轮深度走查:9 款在 4× CPU 节流下实测 **59–60fps**(最低秒桶),全部远高于 30fps 下限 → 按规则「帧率 <30fps 的款才做减法」,**本轮不做任何粒子/图层削减**。本轮新增的动效只有 bubble-pop 光柱 5200ms 慢摆一处(两个合成层 transform 动画,不触发重排),且 reduced 下静止;其余修复全部是静态 SVG/CSS 替换,无新增每帧成本。

## 五、合并态复验(第 2 轮结论)

三人 push 齐(A `56bd69d` → B `347095f` → C 本批)后 `git fetch` + rebase,合并态含三份 round2 报告与全部修复:

| 项 | 结果 |
| --- | --- |
| 合并态 SHA | **`5824758`**(最后一个修复 commit;本报告 commit 紧随其后,除文档无代码改动) |
| `npm test` | **全绿,751 文件 / 15580 用例**(178.2s) |
| 用例对比 | 第 1 轮合并态 15499 → A 档收尾 15511 → B 档收尾 15516 → **本合并态 15580(+81,只增不减)**;本档新增 7 文件 / 64 条 |
| `npm run build` | 全绿(tsc + vite 724ms);PWA precache **190 项 / 5255.58 KiB** |
| 构建体积对比 | 第 1 轮合并态 189 项 / 5232.41 KiB → **+1 项 / +23.17 KiB(+0.44%)**,远低于 10% 说明线(增量 = 两支新 kit + 各款图标族字符串) |
| `vite preview` 冒烟 | 首页 `/` **200** · `/sw.js` **200** · `/manifest.webmanifest` **200** |

**第 2 轮视觉验收结论(C 档)**:第 1 轮「不彻底/回归」0 条(A 档对账已证,本档无需返工);本轮阻断级 0 残留、严重级 0 残留;A 档唯一新问题 W6R2-01 当轮清零;第 1 轮遗留 9 项清 6 + 部分清 1,余 3 项(W6R1-08 数学上限 / W6R1-11 与老 kit 注释涉 badge.ts 等冻结 / l99 共享壳层跨窗口)全部登记理由移交第 3 轮终验或统筹,无隐藏欠账。**窗口 6 第 2 轮三人组验收通过,可进入第 3 轮终验。**

## 六、红线复述

全库分级红线复验随修复同行:无血、无伤害、无死亡描写;失败只鼓励(alien-seek 点错只出问号云「咦?」);无广告无内购;扩展商标词表扫描持续全绿(`window6.r1.fixer.scan.test.ts`),本轮新绘图标已按 B 档 §5 规避要点自查(钥匙/宝箱不排小圆点链、传送双弧旋非三角堆叠、无天线+电光组合)。
