# 三人组 r40 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。不回退 N-117…N-179、CTA 回卷、消消乐钳高、N-119/123。不改 B 游戏文件。N-105 无第四版。不改 `mapColumns` 断点、四处 `showMap`、N-39 `scrollIntoView`。

## 本拍

抽验选关地图 **1024×768** 与 **768×1024**（时钟小屋 / 识字花园，preview `:4188`）。

修前（wrap 680）：

| 视口 | 页签行/高 | 列 | 格 | wrap | `.l99-view` 余量 |
| --- | --- | --- | --- | --- | --- |
| 1024×768 | 1 / 52px | 7 | 86×86 | 680 | 本章贴合（654=654） |
| 768×1024 | 1 / 52px | 7 | 86×86 | 680 | 本章贴合（910=910） |

N-117 收纳后页签不再堆高。1024 横屏中间卡偏窄、格相对两侧留白偏小。纯 CSS：默认仍 680（915×412 矮横屏不抬格）；`@media (min-width:760px) and (min-height:600px)` 将 `.l99-wrap` 提到 **760px**（`mapColumns(760)=7`）。

## 不改

- `mapColumns`：320→4、420→5、560→6、760→7、否则 8
- `.l99-tabs` 不加 `overflow-x:auto`
- 玩法 / 存档 / 题库 / B 热区
