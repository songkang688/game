# 三人组 r40 测试员 A

> 同分支 `cursor/trio-r21-p0-n117-1cd5`。playbook：PR #113。不回退 N-117…N-179、CTA 回卷、消消乐钳高、N-119/123、平板 wrap 760。不改 B 游戏文件（N-180 `.sn-open`、N-181 `.dr-softbtn`）。N-105 无第四版。不改 `mapColumns` 断点、四处 `showMap`、N-39 `scrollIntoView`。

## 本拍

| # | 做了什么 |
| --- | --- |
| N-182 | 静态扫可点 `*-softbtn` ≥44 或 TOUCH 插值。库内仅 `.dr-softbtn`，走 B 文件白名单。不扫 `.dr-start`。不替代 N-87 sticky / N-181。 |

768×1024 再抽 3 款 l99（消消乐 / 算数小农场 / 拼图）：

| 游戏 | 页签行/高 | 列 | 格 | wrap | CTA top |
| --- | --- | --- | --- | --- | --- |
| match-stars | 1 / 52px（11 章） | 7 | 89×89 | 699 | 162 |
| math-farm | 1 / 52px（10 章） | 7 | 89×89 | 699 | 110 |
| puzzle-tiles | 1 / 52px（10 章） | 7 | 89×89 | 699 | 164 |

页签仍单行，格约 89px，不另改 CSS。

## 平板加宽（上拍，勿回退）

抽验选关地图 **1024×768** 与 **768×1024**（时钟小屋 / 识字花园，preview `:4188`）。

修前（wrap 680）：页签 1 行 / 52px；格 86×86；列 7。

修后 Chrome（hash 刷新 `dist/`）：

| 视口 | 页签行/高 | 列 | 格 | wrap | 划得动 |
| --- | --- | --- | --- | --- | --- |
| 1024×768 第1章 | 1 / 52px | 7 | 98×98 | 760 | 本章贴合；CTA `elementFromPoint` = `.l99-continue` |
| 768×1024 第1章 | 1 / 52px | 7 | 89×89 | 699（舞台内边距） | 本章贴合 |
| 1024×768 第7章 23 格 | 1 / 52px | 7 | 98×98 | 760 | 是（room 44，`scrollTop` 可到 44） |
| 768×1024 第7章 23 格 | 1 / 52px | 7 | 89×89 | 699 | 本章仍贴合 |
| 915×412 对照 | 1 / 52px | 7 | 87×87 | **仍 680** | 是（room 216）；CTA top=66 |
| 390×844 对照 | 2 行（窄屏徽章仍折行） | 5 | 61×61 | 351 | 本章贴合 |

N-117 收纳后平板两档页签不再堆高。1024 横屏中间卡偏窄，把平板档 `.l99-wrap` 提到 **760px**（`mapColumns(760)=7`）。矮横屏 `min-height<600` 保持 680，不抬高格子。`.l99-view` 仍 `overflow-y:auto; touch-action:pan-y`。

## 不改

- `mapColumns`：320→4、420→5、560→6、760→7、否则 8
- `.l99-tabs` 不加 `overflow-x:auto`
- 玩法 / 存档 / 题库 / B 热区 / N-105
