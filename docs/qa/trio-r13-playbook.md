# 三人组第 13 轮 · 测试修复员 A/B 任务清单（playbook）

> 依据：`trio-r13-learn-notes.md`。抽验 SHA `6a013600`；交卷时主干 **`a2372650`**。
> **禁止重做**：N-39 主修、N-43（勿第三套 scrolly）、N-44、N-45 footer、S-4、N-40 赛道内、N-41、壳暂停五钮、L-2/L-3、N-16 `corridorFit` 重写、**N-47 保龄/王子/坦克 min-height 44**、**N-52…N-57 源码**（`340fb768`，只许 915 回归）。
> **编号**：N-48=收藏 overlay；**N-58=暂停+跳关门套娃**；N-59=收藏布局；N-60/61 闯关技能；N-62=2048 四向。**本工位新伤 N-63…N-67。**
> A = 壳+学习；B = 休闲对战。配方 E/K/L 补笔见笔记。

## 纪律

- 不改存档 key / `meta.id` / 题库 / seed / 胜负。
- 验收看 915×412 `getBoundingClientRect`，禁止只 grep sticky / min-height。
- 测试只增不减。禁 force。`casual-duo-fit-r5-b-4683` 仍未合则 rebase 后守门测试只留一份。
- 开/关、root×深关：每档独立 `createBrowserContext()`，只在 `#/game/` 同源页写 storage。

---

## 壳层（A）

### N-63 l99 模式条被 `showMap(true)` 卷出舞台顶 🔧（新；≠ 回退 N-39）

- bowling 舞台 scrollTop **252**，`.bl-open` top **−174**。同构：王子/坦克/sudoku/mine/flight/puff/hue/chess/pool 菜单钮在负 top。
- **改哪**：`level99.ts` 布局——模式条移出 `.l99-map` 滚动盒，或 `sticky;top:0`。**四处 `showMap(true)` 保持。**
- 验收：915 不滚能点保龄「双人对战」；hop-pads `.l99-node-cur` 仍在屏（N-39 回归）。

### N-59 收藏册 915 双栏：页签 36 + 升级切底 🔧（r12 仍开；≠ N-48）

- tab → 44；`.collection-done` 42→44；预览 230 竖卡在 `max-height:500px` 收矮，升级/试穿第一屏可点。关闭 44 勿动。可与 N-48 同 PR。

### N-48 overlay 跨路由 🔧

- `hashchange → close`，对照 S-3。验收：开 🎁 再进 `#/game/*`，`.collection-overlay` 个数 0。

### N-58 暂停 + 跳关确认门套娃 🔧（本轮已复点）

- 关内或地图：⏸ 后再点「⏭️ 跳过」→ `.dialog--pause` 与 `.dialog--gate` 同时存在。
- 已暂停时不要再叠门；或开门前先关暂停。Esc 一次回游戏。`dialogs.ts` 语义 / 冷静期零触碰。

### N-47 残留菜单 40px 🔧（三款主体勿重写）

- shoot-range `.shr-mode` **40**；alien-seek `.as-open` **40**；adventure-king `.ak-open` **40**。只改菜单芯片。

### N-16

- 浏览器六键已在屏。只许顺手把 `.ak-back` 33→44，**禁止重写 corridorFit**。

---

## 闯关学习（A）

### C-6 推理关 121 · 浏览器仍线下 🔧

- 双栏 CSS 已合不够：◀✓▶ **428**、▼ **477**。先消灭该态 `.game-stage` 自滚，或把 pads 钉到视口。验收必须 `isDeduceLevel` 121。判定/seed 不动。

### N-37 残余 · shape-kingdom root×深关 🔧

- 永久 root + 进度 90 再进关：三张选项 top **453**。clock / 识字 172 同交叉已绿，勿扩大打击面。

N-43/N-44 ✅ 禁止重做。

---

## 休闲对战（B）

### N-64 junqi-camp 双人同屏确认行 🔧（新）

- 确认/取消/±/回家 **485**，暂停 **597**。菜单四卡已绿勿动。布阵/胜负零触碰。

### N-65 dark-chess 双人同屏 🔧（可与 N-64 同 PR）

- 取消/暂停 **518**；末行格 377 切底。

### N-66 chess-garden 双人末行 🔧

- 底线格子 379–435，crop 396。≠ N-10。单人/人机零回归。

### N-67 gomoku 自由对战「开始下棋 ▶」 🔧

- 设置页 CTA **431**。进局后工具行本轮在屏，**不要再挤棋盘**。

### N-60 / N-61 竞技场闯关技能 🔧（r12）

- orb/snake 闯关键 top **436**。复用双人底栏。双人零回归。

### N-62 merge-2048 四向 🔧

- ◀▲▼▶ top **392**。规则/seed 不动。

### N-2/3/4 视口返工 🔧

- 先灭 `.game-stage` 自滚。掷骰 525 / 地产 448 / 确定 511。

### 仍开、勿换号

- **N-11** 保龄关内四键 430（菜单高度已是 N-47，别混账）。
- **C-8 hue-hand** 三态抽牌排 412–416。
- **N-12** 台球双人 旋转/暂停 461。
- N-49 数独对战竞速；N-50 七键勿再挤；N-10 围棋等剩余；N-13/14/15/17。
- **N-52…57 只回归**，不要第二份 grid。

---

## 完成定义

1. 新伤 N-63…N-67 或书面降级；N-48/N-58/N-59/C-6 仍开则做或说明。
2. 每条 915 留数字；军旗/暗棋必须菜单+对局两档；N-63 验收含 N-39 回归。
3. 撞车取先合版。报告 SHA。
