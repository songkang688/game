# 1.3 窗口 6 · A 档 · 第 3 轮视觉测试员报告(终验)

- 角色:窗口 6 视觉验收三人组 第 3 轮(终验) · A 测试员(只写报告与补测试,不改绘制代码)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款;step 原文「39 款 / 全库」对本窗口一律等于这 9 款)
- 输入:第 2 轮 A 档报告 `docs/qa/1.3-window6-round2-tester.md`(阻断 0 · 严重 0 · 新问题仅 W6R2-01 建议级)+ C 档报告 `docs/qa/1.3-window6-round2-fixer.md`(修复 9 处 / 遗留清 6 余 3,合并态 SHA `5824758`,报告 `528c1c4`);B 档报告只读参考
- 实测环境:Chrome headless(CDP 驱动 / puppeteer-core `--no-save` 临时装,未入 package.json),设备模拟 360×640 / 320×640 @2x touch,`npx vite preview` 构建产物;sharp 16px 灰度管线与 A 档前两轮同一把尺(白底合成 → 16×16 → 灰度,灰阶差 >24 像素占比)
- 状态:**终验完成。第 2 轮遗留对账清零(已修复 + 合理降级,仍未修 0);六大专项 9 款 × 6 项 54/54 达标;终验新发现阻断 0 · 严重 0 · 一般 0 · 建议 0。终验结论:放行。**

## 〇、终验计划(动手前提交,commit `8a08556`)

本轮定位「终验」,只回答一个问题:这 9 款视觉能不能放行。三件事:① 第 2 轮 A/C 档问题与遗留逐条对账,三种结论(已修复 / 合理降级 / 仍未修→升级终验阻断);② 9 款全部过六大专项,不抽样,逐格二值结论;③ 终验结论三选一。回归防线终验加严:热区 / reduced / 教育数据断言全绿为放行前提,6 款 4× 节流帧率 ≥30fps,`vite preview` 冒烟。权限边界:只动 `*.test.ts` 与本报告。

## 一、全量跑数据 + 三轮用例数曲线(报告要求第一节)

| 项 | 本轮接手时 | 本轮收尾(新增终验钉后) |
| --- | --- | --- |
| `npm ci` | 通过 | — |
| `npm test` | **751 文件 / 15580 用例全绿**(177.1s) | **752 文件 / 15597 用例全绿**(见第六节收尾复跑) |
| `npm run build` | 通过,639ms;PWA precache **190 项 / 5255.58 KiB**(与第 2 轮合并态持平,0 增量) | 通过(收尾复跑) |

三轮用例数曲线(只增不减 ✅):

| 报告点 | 文件 | 用例 |
| --- | --- | --- |
| 第 1 轮基线(接手) | 723 | 15379 |
| 第 1 轮合并态(`cbaff1e`) | 742 | 15499 |
| 第 2 轮合并态(`5824758`,报告 `528c1c4`) | 751 | 15580 |
| **本轮接手(与第 2 轮合并态一致,其后仅 docs 提交)** | **751** | **15580**(差值 0,无人删用例) |
| **本轮 A 档后** | **752(+1)** | **15597(+17)** |

前两轮沉淀的机器化扫描用例**全部在位、全绿、无一 skip**(被删/被 skip 按阻断处理——实测 0 例):

- 单独复跑 15 个机器化扫描文件(9 款 `visual.qa1.test.ts` + `src/art/kit/lighting.qa1.test.ts` + `window6.r1.fixer.scan.test.ts` + `window6.r1.learner.copy.test.ts` + `window6.r2.qa.test.ts` + `window6.r2.fixer.uplift.test.ts` + `window6.r2.learner.copy.test.ts`):**15 文件 / 103 用例全绿**;
- 单独复跑 13 个逐款 fixer 钉子文件(7 个 `visual.fixer.r1.test.ts` + 6 个 `visual.fixer.r2.test.ts`):**13 文件 / 98 用例全绿**;
- 全库 `it/test/describe` 的 `.skip/.only/.todo` grep:**0 命中**。

## 二、第 2 轮问题终验对账表(报告要求第二节,三种结论)

结论口径:**已修复(附证据)/ 合理降级(引 C 档书面理由)/ 仍未修(升级为终验阻断项)**。

### 2.1 第 2 轮 A 档新问题(1 条)

| 编号 | 严重度 | 结论 | 终验证据(本轮独立复测) |
| --- | --- | --- | --- |
| W6R2-01 双鼠侧/背朝向 16px 灰度 <3% | 建议 | **已修复** | 本轮用 A 档同一把 sharp 16px 尺独立复测:idle 四朝向 **7.0 / 5.1 / 4.7 / 5.1%**、push 侧向 **5.9 / 5.1%**,全部 ≥3% 线(与 C 档自报 7.03/5.08/4.69/5.08、5.86/5.08 一致);载体为新增 kit `hamsterAccents.ts` 叠加层(hamsterSvg.ts 冻结未动),`box-hamster/visual.fixer.r2.test.ts` 真实灰度断言在跑;360px 局内截图 A 鼠放大花冠肉眼可辨 |

### 2.2 第 2 轮 C 档修复 9 处复验(全部在位,无回归)

| # | 修复 | 终验证据 |
| --- | --- | --- |
| 1 | 九款壳层触区 ≥40 / 正文 ≥14(kit `uiTouch.ts`) | `window6.r2.fixer.uplift.test.ts` 12 条复跑绿;9 款 360px 截图按钮无一处过小 |
| 2 | brave-path emoji 图标族换 badge 族(`rowIconSvg`) | `brave-path/visual.fixer.r2.test.ts` 复跑绿(族内 16px 两两 5.5–33.6% 断言);金币/心/盾/剑/铃/果冻怪 glyph 代码核实全带高光弧 + 描边 + iconWrap 落影 |
| 3 | adventure-king 古堡机关 12 态换同族 SVG(`castleGlyphSvg`) | `adventure-king/visual.fixer.r2.test.ts` 复跑绿;传送圈双弧旋代码核实(本轮加钉) |
| 4 | bubble-aim 画布 HUD ⚠️/♾️/⭐ 换单色矢量件 | `bubble-aim/visual.fixer.r2.test.ts` 复跑绿(fillText 字符清零断言) |
| 5 | W6R2-01 双鼠叠加层 | 见 2.1,独立复测达标 |
| 6 | brave-path 文案随图标族改口 + 终点点位换棋盘小旗 | 360px 局内截图实拍:过层进度条第 4 点位为棋盘小旗徽章(非 🏁 字符);用例复跑绿 |
| 7 | alien-seek 藏匿点内腔 2 停径向渐变 + 问号云路径化 | `alien-seek/visual.fixer.r2.test.ts` 复跑绿;360px 局内截图藏匿点内腔层次可见 |
| 8 | bubble-pop 光柱 .08→.12 + 2° 反相慢摆(reduced 静止)+ 池壁双层描边 | `bubble-pop/visual.fixer.r2.test.ts` 复跑绿(含宽屏 36px 热区复查);reduced 运行时实测 CSS 动画 0 |
| 9 | 描边宽度分级约定在 `hamsterAccents.ts` 头注立约 | 头注三档钉字断言复跑绿 |

### 2.3 第 2 轮 C 档遗留 3 项终验裁决

| 遗留项 | 终验结论 | 理由 |
| --- | --- | --- |
| W6R1-08 bubble-pop 360px 下 40px 触区 | **合理降级(终验裁决:现方案即最优,毋需再改)** | C 档书面理由成立:40px 触区需 8×40+7×4=348px 盘宽,360 屏扣池壁页边后物理上限约 303px,**数学上放不下**;六大专项⑤的判据是「无横向滚动 / 无 HUD 重叠 / 无文字截断」——本轮实测 360px 下 8 列全可见、文档溢出 0,达标;宽屏 `.bp-cell min-width:36px` 热区「一个像素不动」断言持续在跑 |
| W6R1-11 badge 底圆 2 停渐变(花徽 vs 星徽 16px 1.6%) | **合理降级(移交统筹解冻决策)** | `badge.ts` 为冻结 kit,叠加 kit 方案对「改底色」叠不上去(会盖掉徽记本体);颜色 + 图形双通道兜底可用;该项属 kit 底圆渐变通道弱,不属六大专项任何一格的判据(专项③按整体剪影判,brave-path 徽章族整体依旧可辨) |
| l99-\* 共享壳层触区/字号(W6R1-09/10 残余) | **合理降级(移交统筹跨窗口派单)** | `src/ui/` 共享壳层横跨 76 款,单窗口改会造成跨窗口不一致(第 1 轮 C 档、第 2 轮 B 档 §4-7 同判);本窗口 9 款**自有**壳层已全部达标(修复 #1 + uplift 用例守护) |

**对账统计:第 2 轮 A 档新问题 1 条已修复(独立复测);C 档修复 9 处全部在位无回归;遗留 3 项全部合理降级(引 C 档书面理由,且均不属六大专项判据);仍未修 0 条;升级为终验阻断项 0 条。阻断级 / 严重级终态 = 0。**

## 三、9 款 × 6 专项终验总表(报告要求第三节)

判定方法:① grep 各款绘制文件孤立 `arc(...)`+单色 `fill()` 组合(画布 4 款 arc 位点逐一过上下文,配渐变/描边计数)+ 360px 局内截图肉眼确认;② 代码核验三件套 + sharp 16px 灰度独立复测;③ sharp 16px 灰度(box-hamster);④ CSS/断言 + 截图;⑤ 360/320 双宽 38 采样点文档级溢出审计(复用 r1 取证脚本)+ 9 款局内 + 首页 + 结算屏截图肉眼三查(横滚 / HUD 重叠 / 文字截断);⑥ 独立 grep(qa1 词表 + r1 扩展词表 + 本轮 29 组补充词)+ B 档风险清单残余 3 位点终查。

| 游戏 | ①火柴人/草稿圆 | ②金币/道具体积 | ③双人可区分 | ④2.5D 纵深 | ⑤360px 布局 | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| brave-path | **达标**(绘制文件 0 处 canvas arc;主角/敌人全为徽章族 SVG:落影+色环+1.5px 描边;果冻怪软团+波浪底+豆豆眼) | **达标**(coinGlyph 高光弧+内环暗缘+iconWrap 落影+星形浮雕,非平涂黄圆【本轮加钉】;拾取物族 16px 两两 21.1–30.1% 独立复测) | N/A 达标 | **达标**(点名款:迷宫格 `.bvp-mz`/`.bvp-mz-wall` `inset 0 -4px` 2.5D 地台厚度 + 迷雾层,qa1 断言绿) | **达标**(map/lv1 溢出 0;结算=l99 共享结算,实测两款完好;首页入口截图无截断) | **达标**(0 命中;钥匙同心双环/锁单孔非圆点链【本轮加钉】) |
| adventure-king | **达标**(30 处 arc 全为细节件,配 8 处渐变 + 64 处描边;走廊主角八道工序、古堡主角 `castleHeroSvg` 花徽;hero vs 箱 16px 31.6% 复测) | **达标**(文物纹石三停渐变+墨描边+左上高光弧+三种刻纹;落影由调用方画在地面投影点——代码注释与实现核实) | N/A 达标 | N/A 达标(非点名款;走廊远山 .18/柱影 .4/灌木 .7 三档视差为既有加分项) | **达标**(溢出 0;360px 局内截图主角/平台/文物光柱齐整) | **达标**(0 命中;传送圈=双弧旋,非三角堆叠/字母形【本轮加钉】) |
| alien-seek | **达标**(32 处 arc 配 7 渐变 + 53 描边;六只外星人剪影两两不同 60 条既有用例) | **达标**(信物=十点星:径向渐变左上光+2.2px 描边+夜光柔光圈) | N/A 达标 | N/A 达标(星空+双层丘陵剪影视差为既有加分项) | **达标**(溢出 0;局内截图藏匿点/外星人/HUD 层次分明) | **达标**(0 命中) |
| brick-break | **达标**(15 处 arc 配 4 渐变 + 40 描边;球三停径向左上光) | **达标**(胶囊四停纵向渐变+中缝影线+左上高光椭圆+1.5px 描边;candyBrick 渐变砖) | N/A 达标 | N/A 达标 | **达标**(溢出 0;砖阵/HUD 无重叠) | **达标**(0 命中,arkanoid/breakout 双词表皆扫) |
| mole-pop | **达标**(0 处 canvas arc;moleSvg 三停渐变皮毛+描边+moleAccents 叠加) | **达标**(装备鼠全系 16px 可分 4.7–23%【r2 数据+钉子在跑】;敲击星芒+飘分) | N/A 达标 | N/A 达标 | **达标**(lv1 越界元素逐一甄别:全部在 `svg overflow:hidden` 裁剪内【场景树/云出血属设计】,文档溢出 0) | **达标**(0 命中,whack-a-mole/哈姆太郎皆扫;天线星=三描边星+单杆,无电光【本轮加钉】) |
| box-hamster | **达标**(0 处 canvas arc;hamsterSvg+hamsterAccents 组合) | **达标**(2.5D 木箱顶亮侧暗 `inset 0 -4px`+交叉扎带;礼物盒金环脉冲) | **达标**(双鼠 idle 四朝向 16px 灰度 7.0/5.1/4.7/5.1%、push 5.9/5.1%,全 ≥3%,**本轮独立复测**;灰度剪影靠花冠/呆毛/背纹三通道可分) | N/A 达标 | **达标**(溢出 0;局内截图棋盘/箱/鼠/HUD 无重叠) | **达标**(0 命中,hamtaro 双语扫) |
| balloon-pop | **达标**(0 处 canvas arc;三层皮肤气球+结+贝塞尔线) | **达标**(六种徽记 16px 两两 min 29.3% 独立复测;白底+描边) | N/A 达标 | N/A 达标(双层云视差为既有加分项) | **达标**(溢出 0;**结算屏实拍**:三星/标题/鼓励语/按钮全可见无截断) | **达标**(0 命中) |
| bubble-pop | **达标**(0 处 canvas arc;双层径向渐变+底部反光弧+●▲■ 图案通道) | **达标**(石泡三层灰阶;泡泡体积感 360px 截图肉眼确认) | N/A 达标 | N/A 达标 | **达标**(360px 下 8 列全可见,溢出 0;宽屏 36px 热区断言在跑) | **达标**(0 命中,泡泡龙/puzzle bobble 皆扫) |
| bubble-aim | **达标**(39 处 arc 配 13 渐变 + 63 描边;泡泡五道工序:径向渐变/彩虹薄膜/月牙反光/双高光/色觉标记;炮台六道工序) | **达标**(装填球+预告球同族工序;HUD 星已换 painter 矢量件) | N/A 达标 | N/A 达标 | **达标**(自有选关 `.ba-lv` 入口单独补测:360/320 溢出均 0,画布 299×399 完整含于视口;**结算屏实拍**完好) | **达标**(0 命中) |

**六大专项终验达标率:9 款 × 6 项 = 54/54 = 100%(其中专项③ 8 款 N/A 达标、专项④ 8 款 N/A 达标,按统筹口径记达标)。不达标格:0。**

## 四、终验新发现问题(报告要求第四节)

**0 条(阻断 0 · 严重 0 · 一般 0 · 建议 0)。**本轮全部运行时与静态检查均未发现新问题:

- 360/320 双宽 38 采样点文档级溢出全 0;`worst>0` 的疑似位点逐一甄别——首页 `.tabs` 与战役 `.l99-tabs` 均为 `overflow-x:auto` 设计内横滑页签条(本轮把该结构性根因连同 `html{overflow-x:hidden}` 兜底一起加了只读钉),mole-pop 局内越界 `g` 元素全部在 `svg overflow:hidden` 裁剪之内;
- 结算屏实测:balloon-pop / mole-pop 自动速通真实打赢第 1 关,360px 结算屏(l99 共享结算:头像+三星+标题+鼓励语+下一关/再玩一次/回地图)完好,无截断无重叠;
- 冒烟 5 款(brave-path / alien-seek / box-hamster / bubble-pop / bubble-aim)各持续输入 30 秒:pageerror / console.error 合计 **0**,末帧截图无视觉崩坏;
- B 档两轮风险清单残余 3 位点(brave-path 钥匙宝箱造型 / adventure-king 传送形 / mole-pop 天线星)终查全部安全并已加钉。

「终验新发现问题交本轮 C 档当轮清零」条款:**本轮无需 C 档清零动作**。

## 五、回归防线(终验加严)复核数据

1. **热区钉死断言**:bubble-pop 宽屏 `.bp-cell min-width:36px`(`visual.fixer.r1/r2.test.ts`)、balloon-pop aria/dataset 热区、brave-path `.bvp-opt-em`/`.bvp-ico`/`.bvp-mz-*` 功能类名、alien-seek 命中判定圆心半径——复跑全绿 ✅
2. **reduced 功能提示断言 + 运行时抽查**:`fxClassPlan(true)` 一把闸(飘字原地静置不删除)等断言全绿;CDP 模拟 `prefers-reduced-motion: reduce` 实测 balloon-pop / bubble-pop / mole-pop 局内运行中 CSS 动画数全部为 **0** ✅
3. **教育数据快照断言**:mole-pop 算术黑板「quiz 数据只读,出题一个字不动」(`mole-pop/visual.test.ts` L227)与夜市算式摊节奏断言全绿(本窗口 9 款无笔顺/音高数据,算式即教育数据面)✅
4. **6 款 CPU 4× 节流粒子高峰帧率**(CDP `Emulation.setCPUThrottlingRate(4)` + rAF 秒桶 4 秒 + 持续真实 tap 制造粒子高峰,取最低秒桶):brave-path **60** / brick-break **60** / mole-pop **60** / balloon-pop **60** / bubble-pop **59** / bubble-aim **60** fps——全部远高于 30fps 下限 ✅
5. **`npx vite preview` 冒烟**:首页 `/` **200** · `/sw.js` **200** · `/manifest.webmanifest` **200**;随机 5 款各玩 30 秒无视觉崩坏、零脚本错误(见第四节)✅

本轮新增用例:`src/games/window6.r3.qa.test.ts`(1 文件 / 17 条,commit `d84890d`)——终验补充商标词表 29 组 × 9 款、B 档风险残余 3 位点结构钉、360px 布局静态根因钉(html 兜底 + 双页签条横滑容器)、金币三件套钉。

## 六、终验结论(报告要求第五节,三选一)

### **放行。**

依据:

1. **阻断级 / 严重级问题终态 = 0**:第 2 轮输入即无阻断/严重;本轮对账「仍未修 0、升级阻断 0」;终验新发现 0;
2. **六大专项 54/54 全达标**,判据全部有实测数字或机器断言背书;
3. **三轮问题闭环完整**:第 1 轮 13 条 + 第 2 轮 1 条全部「已修复(有独立复测)或合理降级(有 C 档书面理由,且均不属六大专项判据)」,无一悬空;
4. **回归防线全绿**:用例三轮只增不减(15379 → 15499 → 15580 → 15597),0 删 0 skip;build 体积与第 2 轮合并态持平;帧率 6 款 59–60fps;冒烟零错误;
5. 分级红线复核:失败结算文案只鼓励(结算屏实拍「节奏保持得很稳!」),无血/伤害/死亡描写,无广告内购,商标三层词表(qa1 + r1 扩展 + 本轮补充)全 0 命中。

放行附注(登记项,**不构成放行条件**,均有兜底且不属六大专项):W6R1-11 badge 底圆渐变通道与老 kit 描边注释待统筹解冻决策;l99 共享壳层触区/字号统一抬待统筹跨窗口派单;W6R1-08 已终验裁决为物理上限下的最优解,结案。

三方合并态复验与合并态 SHA 由本轮 C 档执行,本报告数据(752 文件 / 15597 用例、build 0 增量)可直接用作对账基线,本档配合。

## 七、收尾数据

收尾流程:`git fetch origin game-1.3-window6` → rebase → `npm test` / `npm run build` 全绿 → 普通 push(数字见 git log 收尾提交;禁 force、不改 main、不推 game-1.3、不用 gh 开 PR,全部遵守)。
