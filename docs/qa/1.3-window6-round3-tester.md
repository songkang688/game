# 1.3 窗口 6 · A 档 · 第 3 轮视觉测试员报告(终验)

- 角色:窗口 6 视觉验收三人组 第 3 轮(终验) · A 测试员(只写报告与补测试,不改绘制代码)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款;step 原文「39 款 / 全库」对本窗口一律等于这 9 款)
- 输入:第 2 轮 A 档报告 `docs/qa/1.3-window6-round2-tester.md`(阻断 0 · 严重 0 · 新问题仅 W6R2-01 建议级)+ C 档报告 `docs/qa/1.3-window6-round2-fixer.md`(修复 9 处 / 遗留清 6 余 3,合并态 SHA `5824758`,报告 `528c1c4`);B 档报告只读参考
- 状态:**终验计划已立,执行中。**

## 〇、终验计划(动手前提交)

本轮定位「终验」,只回答一个问题:**这 9 款视觉能不能放行**。三件事:

1. **对账清零**:第 2 轮 A 档新问题(W6R2-01)与 C 档修复 9 处、遗留 3 项(W6R1-08 / W6R1-11 / l99 共享壳层)逐条对账,每条给「已修复(附证据)/ 合理降级(引 C 档理由)/ 仍未修(升级为终验阻断项)」三选一;前两轮沉淀的机器化扫描用例全部清点在位、全绿,被删或被 skip 按阻断处理。
2. **本窗口终扫**:9 款全部过六大专项(火柴人/草稿圆 · 金币/道具体积 · 双人可区分(本窗口 box-hamster,余 N/A) · 2.5D 纵深(点名 brave-path) · 360px 布局三屏 · 商标终查),不抽样,每款每项给「达标 / 不达标」二值结论,产出 9 行 × 6 列终验总表。
3. **终验结论**:「放行 / 带条件放行(列条件)/ 不放行(列理由)」三选一。

回归防线(终验加严):热区钉死 / reduced 功能提示 / 教育数据快照断言全绿是放行前提;抽 6 款 CPU 4× 节流复测粒子高峰帧率 ≥30fps;`npx vite preview` 冒烟(首页 / sw.js / manifest 200,随机 5 款各玩 30 秒)。

流程:`npm ci` → `npm test` → `npm run build` 记录基线(第 2 轮合并态约 751 文件 / 15580 用例,只增不减)→ 对账 → 终扫 → 回归防线 → 报告 → 收尾 fetch → rebase → 全绿 → 普通 push `origin game-1.3-window6`。

权限边界:只动 `*.test.ts` 与本报告;不改绘制代码;不碰别人的报告;禁止 force / 改 main / 推 game-1.3 / gh 开 PR。终验新发现问题量化登记,交本轮 C 档当轮清零。

(以下各节执行后回填。)
