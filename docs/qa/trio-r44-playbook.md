# r44 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`f7dbe72e`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-192**、**N-193**、**N-194**。下一空号 **N-195**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r43 学习 PR 未合主干时，本轮仍从 N-192 起编；若主干或先合 PR 已占用 N-192+ / 已修 `.ba-lv` / `.cs-lv`，本号销或顺延，并在笔记勘误。

---

## 红线（抄自 r22–r43 + 主干 B）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、**N-94～N-108**、C-5、N-29 尾款、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r44-learn-notes.md` §0。尤其 **不要**再开 `.fk-ch`（N-144）、`.rbt-vs-btn`（N-189）、`.mmc-open`（N-186）、`.sks-mode`（N-149）、`.bl-btn`（N-145）、`.mp-open`（主干已 44）。
5. 在途勿重做：PR #94 / #96 / #99 / **#104** / **#105**～**#116** / **#107**。
6. 不要用 r21 草稿改写 N-108。不要回退 N-87 sticky。

---

## 本轮工单

### N-192 · B · `.ba-lv`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/bubble-aim/index.ts` ≈L307；挂载 ≈L450 |
| 现状 | 地图关卡格，无 min-height；kit 未覆盖 |
| 不做 | 不改 `.ba-btn` / `.bba-mode`（已 44）/ 关卡表 / 弹道 |
| 验收 | 格子热区 ≥44；点已解锁格仍进关 |

### N-193 · B · `.cs-lv`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/candy-swing/index.ts` ≈L443；挂载 ≈L576 |
| 现状 | 地图关卡格，`padding:7px 2px 5px`，估高贴 44 |
| 不做 | 不改 `.cds-tap` / `.cds-mode`（已 44）/ `.fk-ch`（N-144） |
| 验收 | 格子 ≥44；点已解锁格仍进关 |

### N-194 · A · `*-lv` 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` |
| 规则 | `.-lv` 可点关卡格必须 `min-height`≥44 |
| 现状红 | `.ba-lv`、`.cs-lv`（全库仅此两处） |
| 不做 | 不替代 N-144/192/193 |

---

## 建议顺序

1. A 先合 **N-194**（或与 B 并行）。
2. B 修 **N-192**（bubble-aim）、**N-193**（candy-swing），分 PR。
3. 回归：主干 C-5 地鼠洞、N-29 瞄准工具排、N-144 `.fk-ch`、N-189 vs 结算钮、N-186 翻牌入口。bowling `.bl-btn` 仍走 N-145。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口量 **格子点击矩形**（不是只量内部数字行），不能只看 CSS 行。
