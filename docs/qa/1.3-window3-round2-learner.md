# 1.3 窗口 3 · 第 2 轮视觉验收 · 学习优化员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 2 轮 · B 学习优化员。
> 分支：`game-1.3-window3`（第 1 轮合并态 `ef72248` + round2 tester 之上）。只学画面密度与角色剪影，不入库任何参考截图，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step28-B-learner.md`（思路）、`docs/qa/1.3-window3-round1-learner.md`（第 1 轮本档）、`docs/qa/1.3-window3-round1-fixer.md`（C 档修复清单）、`docs/qa/1.3-window3-round2-tester.md`（只读对照输入）。
> 模型 slug：`claude-fable-5-thinking-xhigh`。

## 〇、学习计划（动手前提交）

1. 通读宪法 / window3 / step28-B / 第 1 轮三份报告 / round2 tester 报告（只读）。
2. **回看采纳率（第一节）**：对照 `1.3-window3-round1-fixer.md` 的 13 笔修复，逐条读代码验证第 1 轮 TOP10 建议的落地效果——采纳 / 部分采纳 / 未采纳 + 截图级文字评价（不入库截图）；未按原规格落地的，判「建议不可行」还是「实现取舍」，修订后重排。
3. **补盲区题材（第二节）**：第 1 轮基准表偏重「铺多少」，本轮把棋牌桌面（gomoku / xiangqi / hue-hand）与跑酷 / 格斗 / 吞鱼的**留白**规律学透——头部作品「少而精」怎么做，只记文字结论，不下载不截图不描摹。
4. **横向一致性评审（第三节）**：本窗 9 款升级后同看，从色板 / 描边粗细 / 光照方向 / 动效节奏四轴挑最违和款（≤5），给 C 档绘制规格级统一化建议。
5. **修订建议清单（第四节）** + **商标风险清单更新（第五节）**。
6. **文案优化（第六节，唯一动代码范围）**：把 C 档第 1 轮修复的视觉亮点（标题避让、画制头像、HUD 去 emoji、云量 / 暗角 / 剪影饰）补进对应款引导；核对第 1 轮 9 处文案与 18 例快照是否被 rebase 冲掉，丢了补回；每处登记 + 快照测试。
7. 收尾：`npm test` / `npm run build` 全绿 → fetch → rebase（保留别人的文件）→ 普通 push `origin game-1.3-window3`。

红线重申：只动本窗 9 款纯文案字符串与配套快照测试；绘制代码一行不碰；A / C 报告一字不碰；不派生子代理；不 force push。

（以下各节随工作推进填写。）
