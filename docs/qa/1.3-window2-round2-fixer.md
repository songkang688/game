# 1.3 窗口 2 · 第 2 轮视觉验收 · 监督修复员报告

> 角色：窗口 2 第 2 轮视觉监督修复员（C 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货态：`7859397`（r2 tester 报告，730 文件 / 15261 例）。
> 范围：只修本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 红线：只动绘制层（绘制函数 / CSS 末尾追加 / 视觉常量 / 各款本地 art.ts）。判定、数值、关卡、存档 key、`parentAuth`、玩法断言不碰（钉修后状态的断言更新可以，不删用例）。不复制窗口 1 的 `src/art/kit/`。用例只增不减。

## 修复计划（动手前登记）

输入：A 档报告 `docs/qa/1.3-window2-round2-tester.md`（SHA `7859397`）已读；B 档第 2 轮报告开工时尚未落地（`docs/qa/` 下无 `1.3-window2-round2-learner.md`），先按 A 档修，B 档落地后 rebase 跟进一致性建议。另读 r1 fixer 遗留清单（`docs/qa/1.3-window2-round1-fixer.md` 第三节 2 条）。

顺序铁律：r1「修复不彻底 / 修出回归」最优先 → 本轮新阻断 → 严重 → B 档一致性点名 → 一般 → 建议。每修一处一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减。

| 序 | 级别·来源 | 款 | 问题 | 预定修法 |
| --- | --- | --- | --- | --- |
| 1 | 修出回归（r2-1，严重） | dark-chess | `.dc-cell{min-height:44px;aspect-ratio:1/1}` 在收缩轨道上被 Chrome 传导成 44×44 固定格：相邻格横向重叠 9.98px@360 / 13.88px@320、末列出框 6.0/9.9px | 按 A 档期望：格尺寸跟随轨道（去掉 `min-height:44px`，高度由 aspect-ratio 从轨道宽出），44px 触控红线改由零视觉扩展点击区保住（`.dc-cell::before{position:absolute;inset:min(0px,calc((100% - 44px)/2))}`，格宽 ≥44 时自动钳回 0 不缩热区）；同步把 smoke/art/round1-fix 三处钉着 `min-height:44px` 的断言更新为钉修后状态（不删用例），新增 round2-fix 契约 |
| 2 | 修复不彻底（r2-3，一般·r1 5-4 尾巴①） | pool-stars | `.ps-tip` CSS 已提 14px 但被 `view.ts` `tableLayout` 的 `fontPx: w<380?13:14` 运行时内联样式改回 13px | `fontPx` 窄屏分支删除，统一 14；新增 320/360 档 `fontPx ≥ 14` 契约 |
| 3 | 修复不彻底（r2-4，一般·r1 5-4 尾巴②） | dark-chess | 局外屏 `.dc-sub` 13px / `.dc-tip` 12px / `.dc-pick` 13.5px（A 5-4 点名「各款 pick 按钮」漏网） | 三类提 ≥14px（同 5-4 口径）；SHELL_CSS 源码契约用例 |
| 4 | 修复不彻底（r2-4，一般·r1 5-4 尾巴②） | chess-garden | `.cg-pick` 13.5px / `.cg-over-s` 13.5px（同上漏网） | 两类提 ≥14px；SHELL_CSS 源码契约用例 |
| 5 | 本轮新阻断（r2-2，存量·r1 漏检） | chess-garden | 320px 视口 `.cg-sq{min-width:40px}` 使 8 列内容 320px > 板箱 292px，`.cg-board{overflow:hidden}` 把 h 列裁到仅 12px 可见、格中心不可点 | `src/styles.css` 末尾追加（cg- 前缀）`.cg-wrap .cg-sq{min-width:0;min-height:0}` 让 1fr 真收缩（320 下格宽 ≈36.4px 全列可见可点；40px 的 min-height 同步解除，防 Chrome 经 aspect-ratio 把 min-height 传导回宽度——r2-1 同款病灶）；360px 及以上格宽 ≥40px 不受影响；实测值登记进报告；新增层叠顺序 + 320 算式契约 |
| 6 | 建议（r2-5） | dark-chess | `.dc-count span` 记牌面板文本层 12px | 提 14px（面板以 SVG 图标为主，数字随行字号一起抬，flex-wrap 已有，360 不裁切）；契约用例 |
| 7 | 建议（r2-6） | junqi-camp | `.jq-empty .jq-face` 空位点位字 13px | 提 14px；契约用例 |
| 8 | 建议（r2-7） | chess-garden | `.cg-log-row` / `.cg-log-sum` 记谱行 13px（1.2 存量） | `src/styles.css` 末尾追加 `.cg-wrap .cg-log-sum,.cg-wrap .cg-log-row{font-size:14px}`（与 cg-tip 同口径，特异性压得住 420px 回降块）；契约用例 |
| 9 | r1 遗留 2 重估（A 档建议本轮按「只动渲染层」重估） | sudoku-petal | 行 / 列刚好种齐缺一条轻反馈（宫已有 `cheerRegion`） | 复核结论：**可以在渲染层做，不必动玩法状态机**——`place()` 里已有纯视觉庆祝钩子（`regionDone`→`cheerRegion`），且落子格此前必未填对，「组现在齐」即「刚刚齐」，无需前后 state diff 之外的任何判定钩子。平行照做：只读检查 `variant.groups[0..2n)`（行、列组），齐了就沿线错峰放一道柔光 `sp-linefx`（纯 fx 子节点，与 `sp-petalfx` 同款生命周期，计时器全部入 `timers` 池随 destroy 清空；弱动效不放，reduced 块同步 `display:none` 兜底）。数字、判定、存档零触碰 |
| 10 | r1 遗留 1 维持 | dark-chess | `setCellContent` 的 `textContent` 赋值总被 `innerHTML` 覆盖（测试桩契约） | 维持遗留：零渲染零性能影响、非视觉问题；清理需把 `art.test.ts` 等既有断言口径换成 aria-label，按 A 档建议交第 3 轮测试员先换口径再动，避免本轮「只修视觉」越权 |
| 11 | A 档备注（酌情） | junqi-camp | 面板灰度差 Δ21–33，未到 B 档建议的 ≥40 | 暂不再拉：形状 + 颜色双通道已被 A 档判达标（12 兵种全量契约在跑），继续压暗星星面板会伤浅蓝粉彩一致性；若 B 档第 2 轮报告落地后点名，rebase 跟进 |

自查（规格三、四节）：kit 纪律（`src/art/` 仍不得出现）、`styles.css` 只末尾追加且带 cg- 前缀、商标黑名单复扫、reduced 接线复核、性能 A 档 9 款均 60fps 无回压（不复测跌破 30 的场景不存在，不做强制回压）。

收尾：三人 push 后 fetch → rebase → `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；合并态 SHA、用例总数（对比 r1 合并态 721/15247、r2 tester 730/15261 只增不减）、构建体积写进本报告。

---

## 一、修复清单（14 commit，全部已落地）

B 档第 2 轮报告（`d651a66`）在本档开工后落地，rebase 跟进：修订建议 #1–#3 与 A 档立案重合（已按 A 档修完，B 档补充规格一致）、#4–#6/#8 全部落地、#7 核实后不采纳（前提不成立，见下表）、#9/#10 为遗留跟踪（#10 本轮已落地）。SHA 为 rebase 后最终值。

| # | 级别·来源 | 款 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **修出回归**（r2-1 严重 / B #1） | dark-chess | `view.ts` `.dc-cell` | 格上最小尺寸全部去掉（`min-width:0;min-height:0`），方格边长由 aspect-ratio 从轨道宽得出——Chrome 把 `min-height:44px` 经 aspect-ratio 传导成 44px 固定宽的病灶根除；44px 触控红线改由 `.dc-cell:not(.dc-empty)::before{inset:min(0px,calc((100% - 44px)/2))}` 零视觉扩展点击区保住（格宽 ≥44 自动钳 0 不缩热区；空格禁用钮不带扩展区防吞点击）。修后 360/320 格宽 = 轨道宽 ≈31.0/27.1px，零互叠零出框，名义热区 44×44 | `round2-fix.test.ts` 3 例（零最小尺寸 / min() 公式数学口径 / 空格无扩展区）；smoke/art/round1-fix 3 处钉修前状态断言更新为修后口径（不删用例） | `a5dd975` |
| 2 | 修复不彻底（r2-3 一般，r1 5-4 尾巴①） | pool-stars | `view.ts:90` `tableLayout` | `fontPx: w<380?13:14` 删分支统一 14——`.ps-tip` 的运行时内联来源与 CSS 从此说同一个数 | `round2-fix.test.ts` 1 例（320–768 八档 fontPx ≥14） | `21c9e03` |
| 3 | 修复不彻底（r2-4 一般，r1 5-4 尾巴②） | dark-chess | `index.ts:35,43,45` | `.dc-sub` 13→14px、`.dc-tip` 12→14px、`.dc-pick` 13.5→14px | `round2-fix.test.ts` 1 例（SHELL_CSS 三类全规则 ≥14） | `d6dbad3` |
| 4 | 修复不彻底（r2-4 一般，r1 5-4 尾巴②） | chess-garden | `index.ts:61,66` | `.cg-pick` 13.5→14px、`.cg-over-s` 13.5→14px | `round2-fix.test.ts` 1 例 | `52eb633` |
| 5 | **本轮新阻断**（r2-2，1.2 存量·r1 漏检 / B #2） | chess-garden | `src/styles.css` 末尾追加（cg- 前缀） | 追加 `.cg-wrap .cg-sq{min-width:0;min-height:0}`（特异性 0,2,0 压住基础 0,1,0）让 `repeat(8,1fr)` 真收缩；`min-height` 一并归零防 aspect-ratio 传导（#1 同款病灶）。**实测口径登记**：320px 格宽 ≈36.5px（(320−2×14−2×10+2×10)/8）八列全部回到板箱内、h 列可见可点；360px ≈41.4px 不受影响；触控为整格 aspect-ratio 正方形面积（44px 物理排不下的例外沿用 1.2 packA 登记） | `round2-fix.test.ts` 2 例（层叠顺序 + 320 算式钉实测值） | `9d89e96` |
| 6 | 建议（r2-5） | dark-chess | `view.ts` `.dc-count span` | 12→14px（记牌面板文本层，随行兵种存量数字一起抬；flex-wrap 已有，360 不裁切） | `round2-fix.test.ts` 1 例 | `b886ba9` |
| 7 | 建议（r2-6） | junqi-camp | `view.ts:120` | `.jq-empty .jq-face` 13→14px（与全款 HUD 同口径，不再登记豁免） | `round2-fix.test.ts` 1 例（`dee392e` 补正则行首锚定） | `7d72731` |
| 8 | 建议（r2-7） | chess-garden | `src/styles.css` 末尾追加 | `.cg-wrap .cg-log-sum,.cg-wrap .cg-log-row{font-size:14px}`，特异性压得住 420px 断点 13px 回降 | `round2-fix.test.ts` 1 例（含层叠顺序） | `000461a` |
| 9 | **r1 遗留 2 落地**（B #10） | sudoku-petal | `index.ts` | 行 / 列种齐轻反馈按「只动渲染层」重估后**可修**：`place()` 里本就有纯视觉庆祝钩子（`regionDone`→`cheerRegion`），落子格此前必未填对 ⇒「组现在齐」即「这一手刚种齐」，零状态机改动。平行照做：只读检查 `groups[0..2n)` 行列组，齐了沿线错峰放杏色柔光 `sp-linefx`（错峰交给 animation-delay、计时器入 timers 池随 destroy 清空、`pointer-events:none`；弱动效 JS 早退 + reduced 块 `display:none` 双保险）。判定/数值/存档零触碰 | `round2-fix.test.ts` 2 例（整行种齐行为契约 + 纯视觉/reduced 契约） | `aa8123e` |
| 10 | B 档 #8（可选微调，采纳） | junqi-camp | `view.ts` `.jq-star .jq-face` | 星星面底停 `#9FBCE4→#93B2DE` 压一档：底停灰度差 Δ32.7→**42.8**（≥40 达 B 档原始建议），顶停 Δ≈21 不动；r1 的 Δ≥20 契约继续在跑 | `round2-fix.test.ts` 1 例（底停 Δ≥40 钉死） | `d832bcd` |
| 11 | B 档一致性①（#4） | pool-stars | `view.ts` `.ps-wrap` | 淡草绿壳卡 `linear-gradient(180deg,#EFF7F0,#E7F1EA)` + 16px 圆角；**侧内衬按 320 实测收敛为 6px**（B 档给 10px；竖版台面 cssW 封顶 280px，恰好放进 320−2×14−2×6，10px 会溢出 8px） | `round2-fix.test.ts` 2 例（卡契约 + 320/360 cssW 几何） | `59d39c5` |
| 12 | B 档一致性②（#5） | fruit-stack | `index.ts` `.fs-wrap` | 粉白壳卡 `linear-gradient(180deg,#FFF4F8,#FBF0FF)` + 16px 圆角；**侧内衬按实测收敛为 0**（B 档给 8px；双盆画布宽由 `layout()` 按 host.clientWidth——卡外容器——分配，任何侧内衬都会让双盆行溢出卡外；上下 10px 留卡即可，360/320 几何零改动） | `round2-fix.test.ts` 2 例（卡契约 + 侧内衬必须为 0） | `df42605` |
| 13 | B 档一致性③（#6） | chess-garden | `src/styles.css` 末尾追加 | 草绿壳卡 `linear-gradient(180deg,#f2f7ea,#eaf3e4)`，按 B 档要求排在 r2-2 修复之后；**≤420px 侧内衬归零**（`padding:10px 0`）——窄屏棋盘靠负外边距全幅出血（R2-PA-3 热区例外），任何侧内衬都会动摇 #5 登记的 36.5/41.4px 实测值；棋盘允许比卡幅宽一线（盘框自带 8px 木边压卡不露白） | `round2-fix.test.ts` 2 例（卡在 r2-2 规则之后 + ≤420 内衬归零） | `c4abb03` |
| 14 | B 档一致性④（#7）·**不采纳（前提不成立）** | merge-2048 | `index.ts:151–155` | B 档称 `.mg-wrap::before/::after` 是「两颗 12px 实心方块」并建议加 `border-radius:50%`。读码核实：两颗伪元素带 `clip-path:polygon(50% 0,63% 36%,…)` **十顶点五角星**，本就是星形角饰（与背景星点同语汇），不存在方形方言；`border-radius` 在 clip-path 之下是无效改动（clip 结果不变）。「远离四格方块联想」的诉求已由现状满足，登记不采纳。badge 改 999px 圆丸为 B 档可选案（自述「不改也不算违和大项」），涉 `overflow-wrap:anywhere` 契约与 320 收窄断点回归面，本轮不动 | 无改动（报告登记） | — |

**统计：r1「修复不彻底 / 修出回归」3 处全部清零（#1–#4）；本轮新阻断 1/1 清零（#5）；建议级 3/3 落地（#6–#8）；B 档一致性 4 条采纳 3 条 + 1 条有据不采纳；r1 遗留 2 条中 1 条落地（#9）、1 条维持（见第二节）。** 新增契约用例 **6 文件 / 20 例**，另 3 处既有断言按钉修后状态更新（dark-chess smoke/art/round1-fix 的 `min-height:44px` 口径），用例只增不减。

## 二、r1 遗留清单处理

| # | 遗留项 | 本轮结论 |
| --- | --- | --- |
| 1 | dark-chess `setCellContent` 的 `textContent` 赋值总被 `innerHTML` 覆盖 | **维持遗留（交第 3 轮）**。按红线评估：零渲染零性能影响、非视觉问题，本轮「只修视觉」不越权动它；清理需把 `art.test.ts:73` 等既有断言口径换成 aria-label，按 A/B 档一致建议由第 3 轮测试员先换口径再删，避免单方面动既有契约 |
| 2 | sudoku-petal 行 / 列种齐缺轻反馈 | **已落地（`aa8123e`）**。r1「需动玩法状态机」的判断经 A 档 r2 勘察口径重估后不成立：落子格此前必未填对，「组现在齐」即「刚刚齐」，纯只读检查 + 纯 fx 子节点即可，详见第一节 #9 |

## 三、一致性改动摘要（B 档跟进）

- **壳卡三张**（pool-stars 淡草绿 / fruit-stack 粉白 / chess-garden 草绿）：家族「粉彩壳卡」语汇从 6/9 补到 **9/9**（dot-maze 深盘面 + 奶油壳卡、pool-stars 深台面 + 浅卡均按 B 档豁免口径成立）。三张卡的内衬全部按几何实测收敛（6px / 0 / ≤420px 归零），**320/360 盘面几何一像素未动**，各有契约钉死。
- **junqi 灰度余量**：底停 Δ42.8 ≥ 40，颜色通道与形状角标双通道都留足余量。
- **merge-2048「方块方言」**：核实为五角星角饰（clip-path），不存在方言，登记不采纳；按钮圆角「双方言」与朵朵侧主题色两派按 B 档口径维持并登记。
- B 档 #1/#2 的执行提醒均已吸收：#1 的热区仲裁（DOM 顺序取后者）已在 view.ts 注释与契约中写明，「32 格中心命中自身」的浏览器级全量复测移交第 3 轮 A 档（node 桩无 elementFromPoint）；#2 的验收口径按「h 列格中心可点」执行，不追求像素零裁。

## 四、性能

A 档本轮 9 款实测 CPU 4× 节流下静置/高峰全部 60fps（红线 ≥30），无强制回压场景。本轮改动为 CSS 规则与轻量 fx（sp-linefx 单次最多 n=9 个 span、420ms 自清；::before 扩展区零绘制成本；壳卡为静态渐变），无新增每帧逻辑，不触发复测条件。

## 五、合并态复验（规格第五节）

三人报告均已在分支上（tester `7859397`、learner `d651a66`、本报告随收尾 commit 落地）。fetch → rebase（保留他人全部文件，含 learner 文案 commit `cc23927` 与监督登记）→ 复验：

- **合并态 SHA：`c4abb03`**（全量验证跑于此态；本报告收尾 commit 在其上）
- `npm ci`：绿
- `npm test`：✅ **737 文件 / 15285 例全绿**（179.5s）。链路对账：r1 合并态 721/15247 → r2 tester 730/15261 → r2 learner 731/15265 → 本档收尾 **737/15285（+6 文件 / +20 例，全部为本档契约）**，只增不减成立；r1/r2 全部机器化用例在跑
- `npm run build`：✅ 全绿（tsc --noEmit + vite build）；PWA precache **188 entries / 5215.41 KiB**（较 learner 态 5212.87 KiB 微增 2.5 KiB，为本轮 CSS/fx 体积，符合预期）
- `npx vite preview` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**
- kit 纪律：`src/art/` 仍不存在；`src/styles.css` 相对基线 `origin/game-1.3` 仅一段末尾追加（零删改行），全部带 cg- 前缀；商标黑名单 9 款非测试代码 grep **0 命中**；reduced 接线：本轮唯一新增动效 sp-linefx 带 JS 早退 + reduced 块双保险

## 六、第 2 轮视觉修复结论

- r1「修复不彻底 / 修出回归」清零：dark-chess 格互叠（回归）、pool-stars 内联字号、dc/cg pick 字号（不彻底尾巴）全部修复并有契约钉住；
- 本轮新阻断清零：chess-garden 320px h 列（r2-2）修复，实测口径写入契约；
- 建议级 3/3 落地（r2-5/6/7 无一登记豁免）；
- B 档一致性：4 条采纳 3 条（壳卡 ×3 按实测收敛内衬）、1 条有据不采纳（mg 星形角饰），#8 可选微调采纳（Δ≥40）；
- 遗留清单交第 3 轮 **1 条**：dark-chess textContent 测试桩（建议级、零玩家影响，待第 3 轮换 aria-label 断言口径后清理）；另移交 1 条浏览器级复测：dark-chess 32 格「中心 elementFromPoint 命中自身」全量验证（B 档 #1 提醒，node 桩无法覆盖）。
