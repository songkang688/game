# 1.3 窗口 2 · 第 2 轮视觉验收 · 监督修复员报告

> 角色：窗口 2 第 2 轮视觉监督修复员（C 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。收货态：`7859397`（r2 tester 报告，730 文件 / 15261 例）。
> 范围：只修本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 红线：只动绘制层（绘制函数 / CSS 末尾追加 / 视觉常量 / 各款本地 art.ts）。判定、数值、关卡、存档 key、`parentAuth`、玩法断言不碰（钉修后状态的断言更新可以，不删用例）。不复制窗口 1 的 `src/art/kit/`。用例只增不减。

## 修复计划（动手前登记）

输入：A 档报告 `docs/qa/1.3-window2-round2-tester.md`（SHA `7859397`）已读；B 档第 2 轮报告开工时尚未落地（`docs/qa/` 下无 `1.3-window2-round2-learner.md`），先按 A 档修，B 档落地后 rebase 跟进一致性建议。另读 r1 fixer 遗留清单（`docs/qa/1.3-window2-round1-fixer.md` 第三节 2 条）。

顺序铁律：r1「修复不彻底 / 修出回归」最优先 → 本轮新阻断 → 严重 → B 档一致性点名 → 一般 → 建议。每修一处一个 commit（`fix(visual-r2): <款> <问题>`），配套用例只增不减。

| 序 | 级别·来源 | 款 | 问题 | 预定修法 |
| --- | --- | --- | --- | --- |
| 1 | 修出回归（r2-1，严重） | dark-chess | `.dc-cell{min-height:44px;aspect-ratio:1/1}` 在收缩轨道上被 Chrome 传导成 44×44 固定格：相邻格横向重叠 9.98px@360 / 13.88px@320、末列出框 6.0/9.9px | 按 A 档期望：格尺寸跟随轨道（去掉 `min-height:44px`，高度由 aspect-ratio 从轨道宽出），44px 触控红线改由零视觉扩展点击区保住（`.dc-cell::before{position:absolute;inset:min(0px,calc((100% - 44px)/2))}`，格宽 ≥44 时自动钳回 0 不缩热区）；同步把 smoke/art/round1-fix 三处钉着 `min-height:44px` 的断言更新为钉修后状态（不删用例），新增 round2-fix 契约 |
| 2 | 修复不彻底（r2-3，一般·r1 5-4 尾巴①） | pool-stars | `.ps-tip` CSS 已提 14px 但被 `view.ts` `tableLayout` 的 `fontPx: w<380?13:14` 运行时内联样式改回 13px | `fontPx` 窄屏分支删除，统一 14；新增 320/360 档 `fontPx ≥ 14` 契约 |
| 3 | 修复不彻底（r2-4，一般·r1 5-4 尾巴②） | dark-chess | 局外屏 `.dc-sub` 13px / `.dc-tip` 12px / `.dc-pick` 13.5px（A 5-4 点名「各款 pick 按钮」漏网） | 三类提 ≥14px（同 5-4 口径）；SHELL_CSS 源码契约用例 |
| 4 | 修复不彻底（r2-4，一般·r1 5-4 尾巴②） | chess-garden | `.cg-pick` 13.5px / `.cg-over-s` 13.5px（同上漏网） | 两类提 ≥14px；SHELL_CSS 源码契约用例 |
| 5 | 本轮新阻断（r2-2，存量·r1 漏检） | chess-garden | 320px 视口 `.cg-sq{min-width:40px}` 使 8 列内容 320px > 板箱 292px，`.cg-board{overflow:hidden}` 把 h 列裁到仅 12px 可见、格中心不可点 | `src/styles.css` 末尾追加（cg- 前缀）`.cg-wrap .cg-sq{min-width:0;min-height:0}` 让 1fr 真收缩（320 下格宽 ≈36.4px 全列可见可点；40px 的 min-height 同步解除，防 Chrome 经 aspect-ratio 把 min-height 传导回宽度——r2-1 同款病灶）；360px 及以上格宽 ≥40px 不受影响；实测值登记进报告；新增层叠顺序 + 320 算式契约 |
| 6 | 建议（r2-5） | dark-chess | `.dc-count span` 记牌面板文本层 12px | 提 14px（面板以 SVG 图标为主，数字随行字号一起抬，flex-wrap 已有，360 不裁切）；契约用例 |
| 7 | 建议（r2-6） | junqi-camp | `.jq-empty .jq-face` 空位点位字 13px | 提 14px；契约用例 |
| 8 | 建议（r2-7） | chess-garden | `.cg-log-row` / `.cg-log-sum` 记谱行 13px（1.2 存量） | `src/styles.css` 末尾追加 `.cg-wrap .cg-log-sum,.cg-wrap .cg-log-row{font-size:14px}`（与 cg-tip 同口径，特异性压得住 420px 回降块）；契约用例 |
| 9 | r1 遗留 2 重估（A 档建议本轮按「只动渲染层」重估） | sudoku-petal | 行 / 列刚好种齐缺一条轻反馈（宫已有 `cheerRegion`） | 复核结论：**可以在渲染层做，不必动玩法状态机**——`place()` 里已有纯视觉庆祝钩子（`regionDone`→`cheerRegion`），且落子格此前必未填对，「组现在齐」即「刚刚齐」，无需前后 state diff 之外的任何判定钩子。平行照做：只读检查 `variant.groups[0..2n)`（行、列组），齐了就沿线错峰放一道柔光 `sp-linefx`（纯 fx 子节点，与 `sp-petalfx` 同款生命周期，计时器全部入 `timers` 池随 destroy 清空；弱动效不放，reduced 块同步 `display:none` 兜底）。数字、判定、存档零触碰 |
| 10 | r1 遗留 1 维持 | dark-chess | `setCellContent` 的 `textContent` 赋值总被 `innerHTML` 覆盖（测试桩契约） | 维持遗留：零渲染零性能影响、非视觉问题；清理需把 `art.test.ts` 等既有断言口径换成 aria-label，按 A 档建议交第 3 轮测试员先换口径再动，避免本轮「只修视觉」越权 |
| 11 | A 档备注（酌情） | junqi-camp | 面板灰度差 Δ21–33，未到 B 档建议的 ≥40 | 暂不再拉：形状 + 颜色双通道已被 A 档判达标（12 兵种全量契约在跑），继续压暗星星面板会伤浅蓝粉彩一致性；若 B 档第 2 轮报告落地后点名，rebase 跟进 |

自查（规格三、四节）：kit 纪律（`src/art/` 仍不得出现）、`styles.css` 只末尾追加且带 cg- 前缀、商标黑名单复扫、reduced 接线复核、性能 A 档 9 款均 60fps 无回压（不复测跌破 30 的场景不存在，不做强制回压）。

收尾：三人 push 后 fetch → rebase → `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；合并态 SHA、用例总数（对比 r1 合并态 721/15247、r2 tester 730/15261 只增不减）、构建体积写进本报告。
