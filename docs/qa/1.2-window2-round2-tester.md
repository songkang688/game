# 窗口 2 · 1.2 三轮验收 第 2 轮 · 测试员汇总

> 本文件只做汇总索引，各包详版在 `docs/qa/1.2-window2-round2-tester-<pack>.md`。
> 每个包自己追加一节，不代写别人的包。

## Pack A

- 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
- 详版：[`docs/qa/1.2-window2-round2-tester-packA.md`](./1.2-window2-round2-tester-packA.md)
- 只读走查脚本：`docs/qa/1.2-window2-round2-packA-scan.mjs`（`node` 直接跑，不进 `npm test`）
- 基线：`origin/game-1.2-window2`

### 计数

| 级别 | 条数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **17** = 第 1 轮遗留仍在 12 条 + 本轮新增 5 条 |

### 第 1 轮严重项回归（三条都没回潮）

| 编号 | 口径 | 结论 |
|---|---|---|
| PA-FS-1 | fruit-stack 360px 按钮 `min-height` 仍 ≥ 44 | ✅ 未回潮（`.fs-btn` / `.fs-back` / `.fs-open` / `.fs-pick` 全 44，`.fs-key` 宽窄两段都 44） |
| PA-PS-1 | pool-stars 暂停时键盘瞄准 / 蓄力 / 出杆与按钮全部失效 | ✅ 未回潮（暂停期间乱敲，恢复后 `angle` 仍 0、总出杆 1；暂停 / 恢复 20 次也没卡死） |
| PA-JQ-1 / PA-JQ-2 | junqi-camp 双人同屏两套键各管各的光标 | ✅ 未回潮（朵朵回合星星拨不动光标；换手后 `L` 选中 / `K` 取消都生效；工具条键名跟着座位走） |

### 第 1 轮遗留一般项（12 条，**全部「仍在」**）

| 编号 | 款 | 状态 |
|---|---|---|
| PA-DM-1 | dot-maze | 仍在（`.dmz-btn` 32.8px） |
| PA-DM-2 | dot-maze | 仍在（blurb 把豆叫「小星星」） |
| PA-DM-3 | dot-maze | 仍在（F / G / L / K 按与不按结果逐字相同） |
| PA-FS-2 | fruit-stack | 仍在（`KEY_CODES` 只有 6 个，W / S / G / K 没接） |
| PA-FS-3 | fruit-stack | 仍在（`merge.ts:36` 的「回血」注释） |
| PA-PS-2 | pool-stars | 仍在（瞄准键不分座位，两人互相够得着） |
| PA-PS-3 | pool-stars | 仍在（`destroy` 不回收 `ps-shell-style`） |
| PA-JQ-3 | junqi-camp | 仍在（暂停时按确认把落点清掉） |
| PA-CG-1 | chess-garden | 仍在（`Esc` 只进不出） |
| PA-CG-2 | chess-garden | 仍在（G / K 取消没接） |
| PA-CG-3 | chess-garden | 仍在（`destroy` 不回收 `cg-shell-style`） |
| PA-CG-4 | chess-garden | 仍在（blurb「请进死角」） |

### 本轮新增（5 条，都是一般）

| 编号 | 款 | 现象 |
|---|---|---|
| R2-PA-1 | fruit-stack | `destroy` 之后 `fs-style` 留在 `document.head`——与 PA-PS-3 / PA-CG-3 同一个写法，第 1 轮漏登记了这一款 |
| R2-PA-2 | chess-garden | `.cg-tool`（提示 / 翻转 / 认输）`min-height:40px`，360px 上不到 44 |
| R2-PA-3 | chess-garden | `.cg-sq` `min-height:40px`，360px 下一格实宽 ≈ 42.5，这一款最主要的热区低于 44 |
| R2-PA-4 | chess-garden | 残局连胜第 10 局起对手封顶，题面池 35 个、第 36 局起循环，后段不再变难也不再变新 |
| R2-PA-5 | chess-garden | 无尽里主动认输，收场话说的是「这一局被对方翻过来了」，和真被翻盘共用一句 |

### 七条铁则

| 铁则 | dot-maze | fruit-stack | pool-stars | junqi-camp | chess-garden |
|---|---|---|---|---|---|
| 1 首页 / 深链 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 2 真赢 + 真输 | ✅ | ✅ | ✅ | ✅ | ✅（战役只扣星不判负，输由对战 / 无尽取证） |
| 3 战役 1 / 84 / 188 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 4 对战 / 无尽 / 双人到结算 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 5 360px 热区 ≥ 44 | ⚠️ PA-DM-1 | ✅ | ✅ | ✅ | ⚠️ R2-PA-2 / R2-PA-3 |
| 6 `destroy` 无泄漏 | ✅ | ⚠️ R2-PA-1 | ⚠️ PA-PS-3 | ✅ | ⚠️ PA-CG-3 |
| 7 商标黑名单 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 |

### 一句话建议给第 2 轮修复员

三处 `document.head` 样式残留（fruit-stack / pool-stars / chess-garden）是同一个写法，
`PA-DM-3` / `PA-FS-2` / `PA-CG-2` 又都是「规格里的动作键没接」，
这两组各并成一条改，17 条一般项能一口气砍掉一半。
