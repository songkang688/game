# 窗口 2 · 1.2 三轮验收 第 2 轮 · 测试员汇总

> 本文件只做汇总索引，各包详版在 `docs/qa/1.2-window2-round2-tester-<pack>.md`。
> 每个包自己追加一节，不代写别人的包。

## Pack A

- 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
- 详版：[`docs/qa/1.2-window2-round2-tester-packA.md`](./1.2-window2-round2-tester-packA.md)
- 只读走查脚本：`docs/qa/1.2-window2-round2-packA-scan.mjs`（`node` 直接跑，不进 `npm test`）
- 基线：`origin/game-1.2-window2`

### 计数

| 级别 | 条数 |
|---|---|
| 阻断 | **0** |
| 严重 | **0** |
| 一般 | **17** = 第 1 轮遗留仍在 12 条 + 本轮新增 5 条 |

### 第 1 轮严重项回归（三条都没回潮）

| 编号 | 口径 | 结论 |
|---|---|---|
| PA-FS-1 | fruit-stack 360px 按钮 `min-height` 仍 ≥ 44 | ✅ 未回潮（`.fs-btn` / `.fs-back` / `.fs-open` / `.fs-pick` 全 44，`.fs-key` 宽窄两段都 44） |
| PA-PS-1 | pool-stars 暂停时键盘瞄准 / 蓄力 / 出杆与按钮全部失效 | ✅ 未回潮（暂停期间乱敲，恢复后 `angle` 仍 0、总出杆 1；暂停 / 恢复 20 次也没卡死） |
| PA-JQ-1 / PA-JQ-2 | junqi-camp 双人同屏两套键各管各的光标 | ✅ 未回潮（朵朵回合星星拨不动光标；换手后 `L` 选中 / `K` 取消都生效；工具条键名跟着座位走） |

### 第 1 轮遗留一般项（12 条，**全部「仍在」**）

| 编号 | 款 | 状态 |
|---|---|---|
| PA-DM-1 | dot-maze | 仍在（`.dmz-btn` 32.8px） |
| PA-DM-2 | dot-maze | 仍在（blurb 把豆叫「小星星」） |
| PA-DM-3 | dot-maze | 仍在（F / G / L / K 按与不按结果逐字相同） |
| PA-FS-2 | fruit-stack | 仍在（`KEY_CODES` 只有 6 个，W / S / G / K 没接） |
| PA-FS-3 | fruit-stack | 仍在（`merge.ts:36` 的「回血」注释） |
| PA-PS-2 | pool-stars | 仍在（瞄准键不分座位，两人互相够得着） |
| PA-PS-3 | pool-stars | 仍在（`destroy` 不回收 `ps-shell-style`） |
| PA-JQ-3 | junqi-camp | 仍在（暂停时按确认把落点清掉） |
| PA-CG-1 | chess-garden | 仍在（`Esc` 只进不出） |
| PA-CG-2 | chess-garden | 仍在（G / K 取消没接） |
| PA-CG-3 | chess-garden | 仍在（`destroy` 不回收 `cg-shell-style`） |
| PA-CG-4 | chess-garden | 仍在（blurb「请进死角」） |

### 本轮新增（5 条，都是一般）

| 编号 | 款 | 现象 |
|---|---|---|
| R2-PA-1 | fruit-stack | `destroy` 之后 `fs-style` 留在 `document.head`——与 PA-PS-3 / PA-CG-3 同一个写法，第 1 轮漏登记了这一款 |
| R2-PA-2 | chess-garden | `.cg-tool`（提示 / 翻转 / 认输）`min-height:40px`，360px 上不到 44 |
| R2-PA-3 | chess-garden | `.cg-sq` `min-height:40px`，360px 下一格实宽 ≈ 42.5，这一款最主要的热区低于 44 |
| R2-PA-4 | chess-garden | 残局连胜第 10 局起对手封顶，题面池 35 个、第 36 局起循环，后段不再变难也不再变新 |
| R2-PA-5 | chess-garden | 无尽里主动认输，收场话说的是「这一局被对方翻过来了」，和真被翻盘共用一句 |

### 七条铁则

| 铁则 | dot-maze | fruit-stack | pool-stars | junqi-camp | chess-garden |
|---|---|---|---|---|---|
| 1 首页 / 深链 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 2 真赢 + 真输 | ✅ | ✅ | ✅ | ✅ | ✅（战役只扣星不判负，输由对战 / 无尽取证） |
| 3 战役 1 / 84 / 188 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 4 对战 / 无尽 / 双人到结算 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 5 360px 热区 ≥ 44 | ⚠️ PA-DM-1 | ✅ | ✅ | ✅ | ⚠️ R2-PA-2 / R2-PA-3 |
| 6 `destroy` 无泄漏 | ✅ | ⚠️ R2-PA-1 | ⚠️ PA-PS-3 | ✅ | ⚠️ PA-CG-3 |
| 7 商标黑名单 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 |

### 收尾水位

- 本包五款自己的测试：36 文件 / 727 例全绿。
- `npm run build`：✅ 通过。
- 全仓 `npm test`：✅ 251 文件 / 6400 例全绿。
  （过程中在上游 `f27c16c` 上曾有 2 例红，都在 `src/games/gomoku/tiers.test.ts`，
  是包 C 的 gomoku 档位续作「先红后绿」的中间态，与本包无关，已由包 C 的 `44fdabf` 转绿。）

### 一句话建议给第 2 轮修复员

三处 `document.head` 样式残留（fruit-stack / pool-stars / chess-garden）是同一个写法，
`PA-DM-3` / `PA-FS-2` / `PA-CG-2` 又都是「规格里的动作键没接」，
这两组各并成一条改，17 条一般项能一口气砍掉一半。

---

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 明细报告：`docs/qa/1.2-window2-round2-tester-packB.md`
- 计数：**阻断 0 · 严重 3 · 一般 8**
- 第 1 轮已修严重项复验：**B-2（`tap-tiles` 打平判平局）、B-6（`hop-pads` 按指号分台）均无回潮**

### 新记严重 3 条

| 编号 | 游戏 | 一句话 |
| --- | --- | --- |
| R2B-1 | `dark-chess` | 双人同屏里 `w/a/s/d` 与 `f`/`g` 无条件接（`view.ts:156`），轮到星星时朵朵按 `D`/`S` 光标照样动、按 `F` 直接替星星落子；反向不成立。整块棋盘还共用一个 `cursor`。与包 A 在 `junqi-camp` 修掉的 `PA-JQ-1`/`PA-JQ-2` 同形 |
| R2B-2 | `hop-pads` | 幽灵对战两边的「16」不是同一个 16：幽灵是**起跳 16 次**，玩家是**站满 16 座**就冻结，弹簧台白送的台数只算给幽灵。同一条台序上满分玩家比地狱幽灵少 40–100 分，必输。且 `expert`/`hell` 噪声都太小，两档成绩逐局完全相同 |
| R2B-3 | `fight-king` | 格斗塔第 125 关往后，默认出战的 🌸 朵朵 lv4 的手 0/8；同关换 🍡 糯糯 / 🐼 墩墩 / ⭐ 星星 立刻 1–7/8。第 1 轮的 `B-10`（后段是墙）与 `B-11`（角色强弱）是同一个根 —— 墙不在关卡表，在默认主角 |

### 一般 8 条

`R2B-4` `tap-tiles` 对战前几局普通/高手/地狱三档成绩完全一样、满分只能打平 ·
`R2B-5` `hop-pads` `B-7` 仍在（WASD / 方向键无语义，`G` 收力已接） ·
`R2B-6` `dark-chess`/`hop-pads` `B-3` 仍在（档名「菜鸟/地狱」偏冲） ·
`R2B-7` `tap-tiles` `B-9` 已消失（`F` 已认，文案与实现逐字吻合），仅记跨款键位口径差异 ·
`R2B-8` `fight-king` `B-12` 仍在（训练场进不了 `meta.modes`，**平台 `src/engine` 枚举问题，记给主管，未自行改**） ·
`R2B-9` `fight-king` 无尽第 12 场上下见顶 ·
`R2B-10` `hue-hand` 战役每关只有生成时验过的那副牌稳，换牌约五成 ·
`R2B-11` `fight-king` 塔第 1 关与第 92 关对手撞成同一位

### 第 1 轮 12 条遗留逐条结论

| 已消失 | 仍在 | 部分 |
| --- | --- | --- |
| B-1 B-2 B-4 B-5 B-6 B-8 B-9（7 条） | B-3 B-7 B-11 B-12（4 条） | B-10（关卡表不是墙，默认角色是墙） |

### 本轮怎么验的

- 每款：真赢一次真输一次；战役第 1 / 换过的中间关 / 第 188 关三关都真打到结算；对战 / 无尽 / 双人各模式各打到结算；
  360px 量热区；`destroy` 进→玩→退出→再进 20 轮数监听、定时器、子节点；商标逐条扫。
- 中间关按要求换掉第 1 轮的第 100 关：`dark-chess` 84 · `hue-hand` 88 · `hop-pads` 92 · `tap-tiles` 96 · `fight-king` 92。
- `fight-king` 的对局画在 `canvas` 上读不出局面，改用**逐帧复刻**：在界面旁边跑一份同参数、同 AI 种子、
  同步进的对局，用它算出该按什么再真的 `keydown`/`keyup` 出去；复刻件与界面的回合比分、结算时刻逐帧吻合，
  这也顺带成了双人同屏串台的判据。
- 商标：五个目录去掉各自的黑名单守卫测试后 54 个文件，24 条商标词逐条扫，**0 命中**。
- `src/games/**` 一个字节没改；临时走查脚本跑完即删，未入库。

## Pack C

- 范围：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 明细报告：[`docs/qa/1.2-window2-round2-tester-packC.md`](./1.2-window2-round2-tester-packC.md)
- 基线：`origin/game-1.2-window2`，历史对照 `origin/game-1.1`
- 计数：**阻断 0 · 严重 0 · 一般 8**
- 第 1 轮 `gomoku` 阻断复验：**换一组独立 seed（4200/8600/15400）重跑相邻档对局，没有回潮**

### 点名五项各一句

| 项 | 结论 |
| --- | --- |
| ① `gomoku` | 抽 10 关（7/22/45/67/88/101/120/145/170/188）按 `solutionLine()` 主变实摆到底、终局 `findWinLine()` 验出黑五连；188 关 `isForcedWin` **无解 0 条**、虚标 0、前 99 题相对 `origin/game-1.1` **漂移 0**；换 seed 独立复跑六档，**五对相邻档全部单调**（`smart→normal` 从第 1 轮倒挂的 30% 变成 58.3%，`hell→master` 从打平变成 83.3%），第 1 轮阻断**未回潮** |
| ② `match-stars` | 换第 88 关 8×8 真盘走完整条时间线：段序 `swap→boom→fall→land→settle` 共 48 帧，**有方块飘在半空的帧数 = 20**（第 1 关 19、第 188 关 61），三段可见；`planGravity` 幂等且错峰 20ms、`planRefill` 的 `fromRow` 全为负（新块从盘外掉进来）；`prefers-reduced-motion` 走的是**同一条状态机**只是压到 7 帧，没有另开瞬变分支 —— **瞬变阻断不成立** |
| ③ `rainbow-run` | `view3d.ts` / `controls.ts`（连带 `logic.ts` / `sim.ts`）与 `origin/game-1.1` **逐字节相同**（blob 哈希一致）；`endless.ts` +376/−19 是**严格超集**（1.1 的 42 个导出一个不缺，新增 14 个，`ENDLESS_TIERS` 逐字符未动），差的是路段语法与「可跳可滑」的必过口径，摄像机/控制仍是 1.1 的；全仓无 three.js；`makeCamera` 改成**运行期计数**实测 —— mount 后 2 次、再跑 300 帧仍是 2 次、尺寸真变一次变 3 次、之后 200 帧不再涨 |
| ④ `ocean-munch` | 无尽真跑到系统判 `nibbled` 失败，**新深度 591 米**（第 1 轮是 767）；`recordEndlessBest("ocean-munch", 591)` 真触发、平台底账写入 591；`destroy` 后重新 `mount` 读回 591 且首屏显示「最好 591 米」；`LEVELS` 逐条 JSON 比对，**前 99 关漂移 0、全 188 关漂移 0**（`logic.ts` 只往 `DEX` 加了 3 条深海鱼） |
| ⑤ `xiangqi` | 全仓 65 个目录里只有 `src/games/xiangqi/` 一套中国象棋，且 1.1 的 5 个文件全在原地；搜索层数 0–5、时间预算 20–400ms、思考延时 320–760ms **三条线严格递增**，假时钟实测一档都没超预算；188 课**无解 0、虚标 0**，换一组 10 课（7/33/55/71/90/111/133/155/170/181）按主线实走**终局状态全对**；长将三种边界全部正确 —— 单方面连将第 9 手判 `perpetual` 且 `loser` 就是连将方（黑方镜像同样成立）、中间断一手就重新数不判、双方走回原位判 `repetition` 且 `loser=null` |

### 一般 8 条

`R2C-G1` `gomoku` 360px 下 `.gmk-btns button` 实高 36.8–39.6px ·
`R2C-G2` `gomoku` `.gmk-seg button` 40.8–43.6px、`.gmk-mode` 36.0–39.0px ·
`R2C-G3` `gomoku` `smart` 对 `normal` 和棋率 36%，两档区分度偏弱（不影响单调） ·
`R2C-R1` `rainbow-run` `btnEndless` 42px、`btnCollection` 38×34、`btnBack` 62×30 / 62×28 ·
`R2C-R2` `rainbow-run` 红线词 2 处（`guide.ts` 「掉血」、关名《广告墙走廊》），**均继承自 1.1，不是 1.2 引入** ·
`R2C-O1` `ocean-munch` 6 个导航类按钮 < 44px（`btnDex` / `btnBack`，主流程按钮都达标） ·
`R2C-X1` `xiangqi` `.xq-btns button` 38.8–41.6px、`.xq-mode` 与 `.xq-over-btn` 临界 ·
`R2C-X2` `xiangqi` `easy` 与 `normal` 在残局考卷上同分 36/36（尺子分辨率不足，非强弱倒挂）

> 8 条里有 5 条（`R2C-G1` `R2C-G2` `R2C-R1` `R2C-O1` `R2C-X1`）是同一类：
> **360px 下导航类/次要按钮不足 44px**，主流程按钮基本都达标。建议统一按 44px 兜底，不必一款一款改。

### 本轮怎么验的

- 换样本：战役统一抽第 **1 / 88 / 188** 关（避开第 1 轮的第 100 关）；
  `gomoku` 解局换成 7/22/45/67/88/101/120/145/170/188；
  `xiangqi` 残局换成 7/33/55/71/90/111/133/155/170/181（将死 + 困毙都覆盖）；
  AI 对局换 seed 4200/8600/15400；`match-stars` 换第 88 关真盘；
  `ocean-munch` 换深度 591 米；无尽路段抽检换 `mulberry32(20260827)`。
- 每款：首页/深链定位 → 真赢一次 + 真输一次玩到结算 → 各模式各打到结算 →
  360px 量热区 → `destroy` 进→玩→退出→再进 20 轮数监听/定时器/子节点 → 竞态狂点 → 商标红线逐条扫。
- 三款纯画布（`rainbow-run` / `ocean-munch` 全画布，`xiangqi` 半画布）读不出 DOM，
  自写了一份**会录 `fillText` 的画布桩**，按文案反查它画在哪儿再去点它，实现黑盒读屏。
- `rainbow-run` 的 `makeCamera` 用 `vi.mock` 包一层计数器，把「每帧重建」这件事从 grep 升级成**运行期实测**。
- `xiangqi` 双人同屏没有 AI 随机源，镜像一块棋盘就能完全对齐，
  红方用最强档、黑方故意挑最差的一手，**第 9 手真下出将死**——这也是本款「真赢一次」的取证之一。
- `src/games/**` `src/engine/**` `src/ui/**` `src/styles.css` 一个字节没改；
  临时走查脚本放在 `src/__r2packc__/`，取证后**整目录删除**，未入库。
