# 1.3 窗口 5 · 视觉验收遗留 backlog(第 3 轮终验收官时结转)

> 由窗口 5 第 3 轮(终验)监督修复员成文,作为本窗后续输入。
> 收录口径:终验后仍在案的一般 / 建议级遗留 —— 全部为窗外轮次、主管口径或非低成本项;
> 本窗六大专项(10 款 × 6)不达标格为 0,以下没有任何一条是阻断 / 严重级。
> 每条带「来源轮次 / 位置 / 期望 / 估算改动面」。

| # | 项 | 来源轮次 | 位置 | 期望 | 估算改动面 |
| --- | --- | --- | --- | --- | --- |
| BL-1 | prince-princess DOM 出场卡 / HUD chips emoji(R2-a) | R1 L-1 的 DOM 半项,R2 fixer 登记,R3 评估维持挂账 | `src/games/prince-princess/index.ts`(`pcp-cue-icons` 出场卡、HUD chips);icons 数据在 `teach.ts` 教学口令(「先图后字」的图本体) | 全库 HUD 统一走查轮把 DOM emoji 换矢量小图(内联 SVG / 小画布),沿用 visual13 已有徽章几何;不得改教学口令与 HUD 文案语义 | 中:pp 单款 2–4 文件(DOM 结构 + cue 消费方式),需与 `teach.test.ts` / 快照断言联动;画布侧几何已备齐(R2 徽章画笔可复用) |
| BL-2 | 全库 level99 星标 / 按钮触区 / tank-battle 窄屏小字(L-2 = G6–G8) | R1 tester 登记,三轮维持「窗外」 | `src/games/level99` 共享层与各款按钮 / HUD CSS | 1.2 基线项,留全库统一轮次一次做齐(涉及全库 39+ 款一致性,不宜单窗先动) | 大:共享层 + 全库回归;单窗动会造成款际不一致 |
| BL-3 | sky-squad / tank-battle HUD 芯片行 360px 横滑观感(L-3 = G9) | R1 tester 登记;R3 复测横向溢出 0px、信息可达 | 两款 HUD 芯片行容器 CSS | 纯观感增强:芯片行末尾加渐隐「还有更多」提示(B 档 R2 建议) | 中(B 档自评):两款 HUD CSS(mask / 伪元素渐隐)+ 360px 实机回归;信息可达已达标,收益低于风险,归 HUD 统一走查轮 |
| BL-4 | puff-bros HUD 🍬 DOM 计数字 1 处(L-4) | R1 tester 登记;现有用例钉 ≤2 / ≤1 不拦 | `src/games/puff-bros/index.ts` HUD chip(`rightTxt.textContent = "🍬 …"`) | 随 HUD 统一走查换矢量小图;糖果矢量几何 `drawCandy` 已在 visual13 备齐 | 小:单处 DOM chip 结构改造 + `visualFix13` 闸联动;归 BL-1/BL-3 同一走查轮顺手做 |
| BL-5 | PLAN 文档对标点名词口径(L-5 = Z2/G11) | R1 learner 提出,三轮维持「主管口径」 | `docs/PLAN-1.2/1.3-15A.md` 等计划文档(玩家可见内容 0 命中,测试禁词表为负向断言防线) | 主管裁决是否统一改写历史 PLAN 文档的对标叙述;三档均不私改 PLAN | 极小(纯文档),但需主管拍板,不属任何执行档权限 |
| BL-6 | parentAuth 跨路由弹窗残留(R3-a) | R3 tester 终验新登记(1.2 基线行为,非视觉) | `src/ui/parentAuth.ts` + `src/ui/dialogs.ts`(窗外共享层,三档禁改区) | 全库轮次修复:路由切换时对活跃 dialog 统一 `close()`,或 parentAuth 轮询里 `location.hash` 变化即 `finish(false)`(A 档 R3 报告已附复现 5/5 与建议) | 小而敏感:共享层 2 文件 + 路由钩子;涉及家长门安全语义,须专轮带回归验证,禁止顺手改 |

**统计:共 6 条(一般 1 = BL-6;建议 4 = BL-1/BL-3/BL-4/BL-2;主管口径 1 = BL-5)。**
其中 BL-1 / BL-3 / BL-4 同属「HUD 统一走查」一揽子轮次;BL-2 / BL-6 属全库共享层轮次;BL-5 待主管裁决。
本窗画布层字形账户已清零封顶(全部剩余 emoji 为 DOM 功能文字口径,水位闸 `visualGuardW5R3` 整表锁死只降不升)。
