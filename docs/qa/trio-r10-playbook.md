# 三人组第 10 轮 · 测试修复员 A/B 任务清单（playbook）

> 依据：`trio-r10-learn-notes.md`（基线 `game-1.3 = 3c9902cb`；**主干 src 与 r9 基线 `6a9f42d0` 逐字节一致**,r9 全部行号/数字/干净名单原样有效,本清单不重抄 r9 原文）。
> **对账要点（先读这条再动手）**：r9 之后主干零 src 合入——**r9 playbook 全部 🔧 法律上仍开账**;但 **5 条在途分支已并行动手且互相撞车**（N-30 三路、N-33/36/37/38+收藏册 双路、N-1 双路、⑤ 与主干 8 文件真实冲突）。**本轮第一要务是合流,不是开新工**——先把在途的收进主干,新伤只有一条 N-40 且骑 N-33 的公共修复。
> 分工建议：**A = 合流①②③ + 壳层弹窗组**,**B = 合流④⑤ + 无人认领的休闲账**。
> 修补配方 A/B/C 见 r4 learn-notes,D/E/F 见 r5,G/H 见 r6,I/J 见 r8,K 见 r9,**本轮新增 L（共享容器按最长内容实例验收）见 r10 learn-notes 第四节**。

## 通用纪律（与 r4–r9 一字不差的红线）

- **不改存档 key**：`yiduo-yixing.l99.<id>` / `yiduo-yixing.l99skip.<id>` / `yiduo-yixing.root.v1` / 平台钱包 key、`meta.id` 一个字符不动。
- **不动题库/判定/关卡生成**：`levels.ts` 数据、seed 链路、win/lose 判定零触碰。
- **测试只增不减**：进场 `npm test` 记水位（`3c9902cb` = **1095 文件 / 19288 用例**全绿,本轮实测复核）;每条修复配 1 份小测试;交卷水位 ≥ 进场。**合流后全库只留一份 modebar 守门测试**。
- **kit 冻结**：`src/art/kit/` 已有文件只 import 不改（stickers.ts 扩容例外）。
- **宽屏零回归**：钳高改动在 412×915 与 1280×800 复测「修前修后一致」。
- 收尾：fetch → rebase → `npm test` / `npm run build` 全绿 → 普通 push,禁 force。

## 测试步骤备忘

- 本地起服：`npm run build && npx vite preview --port 4173`;无头验证 puppeteer-core + `/usr/local/bin/google-chrome`。
- 视口五档：360×640 / 390×844 / 412×915 / **915×412（重灾主档）** / 1024×768;宽屏对照 1280×800。（平板竖屏 768×1024 本轮探档干净,不必扩进常规矩阵。）
- **管理员直达**：家长面板 → 管理员权限 → 密码 `kangkang`;脚本可直接种 `yiduo-yixing.root.v1 = {"expiresAt":9999999999999,"mode":"permanent"}`。
- 工装红线三条：①「开/关对照」每档独立 incognito context 或先 `localStorage.clear()`（r9）;②**弹窗量数等 pop-in 动画（0.28s scale0.7→1）静止后再取 rect,否则高度是 0.7 倍假数**（r10 新增）;③家长面板验收先预种跳关记录（`yiduo-yixing.l99skip.math-farm=[3,7,12]`）,列表非空才是真实高度。
- 量法口径沿用：stage 裁切（r4）→ 自滚容器（r5）→ 逐模式（r6）→ 开关打开再量（r7）→ 弹窗内滚与够不着的钮（r8-I）→ 深关关型矩阵（r8-J）→ 调用路径 × 默认参数（r9-K）→ **共享容器 × 最长内容实例（r10-L）**。

---

## 第〇节 · 合流（A/B 共同,最高优先,做完这节大半旧账自动销）

**在途 5 条**（详表见 r10 learn-notes 第二节）：① `trio-r9-tester-a-7779`（N-38/33/37/36/34/35/30+S-4 扩容+收藏册）;② `trio-r9-tester-a-c14c`（N-33/38/37/36+收藏册+quiz99,**与①同职能**）;③ `tester-a-r7-fixes-def4`（N-30/N-32/N-27/L-2）;④ `trio-r9-tester-b-c14c`（N-25/N-31/N-1）;⑤ `casual-duo-fit-r5-b-4683`（11 笔,r4/r5 老账大扫除,**基点停在 r5 时代 `3c7fb691`**）。

**建议顺序 ③ → ① → ④ → ⑤**（每步：rebase 最新 `game-1.3` → 全量测试全绿 → 合入 → 下一条再 rebase）：

1. **③ 先合**（4 笔,基点新,账目独立度最高：N-30/N-32/N-27/L-2,带 4 份测试）。
2. **① 随后**（A 的 r9 主体;rebase 时 `adventure-king/index.ts` 撞③的 N-30 双栏——**取③版（先合版）**,①的 castleShell.r9.test 断言若与③方案不符,改断言对齐行为而不是改回①方案）。**② 不整支合**：与①同职能全面撞车（level99/tracing/styles/collection 四文件）,①合入后对 ② diff 逐文件核,只把独有增量（如 quiz99 侧改动）摘成小提交带进来,其余弃。
3. **④ 再合**（fight-king/fruit-catch 与①③不相交;towerFit/stageFit 两份 .r9.test 一起进）。
4. **⑤ 最后且必须先大 rebase**：与主干真实冲突 8 文件（adventure-king / box-hamster / ice-fire-forest / orb-arena / prince-princess / snake-royale / sudoku-petal / tank-battle 的 index.ts）——[hidden] 行取主干 A 版,snake-royale/orb-arena 撞 A 的 fitPane 钳高,双方案并存时以「先合版行为不劣化」为准;`fruit-catch/index.ts` 撞④（先合版=④）,古堡盘宽撞③①(先合版);**守门测试二合一**：`src/games/__tests__/modebar-hidden-guard.test.ts` 并进主干 `modebarHidden.guard.test.ts`,保主干口径、把 ⑤ 新增提名并进去,库里只留一份。
5. **销账纪律**：每合一支,按分支报告把对应编号在 r6–r9 playbook 标 ✅ 并记量化数字;⑤ 的 `e0efd778`（shoot-range/balloon-pop/ice-fire-forest/puzzle-tiles/hue-hand 五款）**以 diff 对照 r4/r5 playbook 原文逐条核销**,疑似顺手覆盖 N-8/N-9/N-20/N-21/N-22,别只看提交题目。

---

## 壳层（给 A）

### N-33 + N-40（新）壳层弹窗必点钮线下,同一 PR 🔧（走配方 I + 本轮模式 L）

- **N-33 原文见 r8/r9 playbook**（结算弹窗 915×412 两钮线下;92dvh 口径;正面样板=攻略抽屉 sheet 态）。在途①②都做了 sticky——**合流后先复测,已覆盖就只补 N-40 验收,别重复修**。
- **N-40 新账**：家长面板 `dialog--parent`（`parentGate.ts:19`）功能钮线下,**五档中四档中招**——915×412 内滚 411,「导入进度 463 / 清空跳关 678 / 关闭 742」线下;360×640「清空跳关 759 / 关闭 823」线下,**点开导入后「确认导入」667 线下**;1024×768 桌面档「关闭」789 线下;390×844「关闭」只露 7px;412×915 唯一干净。遮罩可关（dismissible）是逃生门,**重伤在「清空全部跳关记录」「确认导入」两个功能钮**。
- **改哪**：与 N-33 同处——`src/styles.css` `.dialog-buttons` sticky（公共类一处改,结算/暂停/家长门/家长面板全家族受益）;家长面板内容侧只允许矮档收「家长说明」列表字号,**别发明面板专属方案**;`parentGate.ts` 的备份/清空语义零触碰。
- **验收（模式 L 口径）**：矩阵 = `.dialog` 家族按内容高取 top2 实例（家长面板 + 结算弹窗）× 五档;915×412 与 360×640 不滚可点「清空跳关」「关闭」;**展开导入区后 textarea 与「确认导入」同屏**断言一条;跳关记录预种非空;遮罩关闭行为不变;412×915 / 1280×800 零回归。

### N-37 / N-38 / S-4 扩容 / 收藏册热区 🔧（合流销账为主）

- 在途①②均已动手（详见 r9 playbook r10 标注）。**合流①后逐条复测 r9 playbook 验收口径**（N-37 验收矩阵含深关限时档 135、每档隔离 context;N-38 永久文案;`.qz-jump-input` 与收藏册 44px 取反断言）——通过即销账,不足才补,②的 quiz99 独有增量按第〇节第 2 条摘取。

---

## 闯关学习（给 A）

### N-39 l99 蓝本地图不聚焦当前关 🔧（**仍无人认领**,一行级 × 4 调用点,本轮继续置顶）

- r9 playbook 原文照做（`level99.ts:782` 默认 false;`:1085/:978/:1009/:1038` 四处补 `showMap(true)`;`:851` 切章保持 false 别误改）。本轮复证 hop-pads 426–502 与 r9 逐位一致,验收样本仍用 hop-pads。**注意**：合流①②后 `level99.ts` 行号会漂,按符号（`showMap` 调用点）定位,别按行号。

### N-36 描红关 / L-2 题面钟 / L-3 贴纸补章

- **N-36**：在途①②都改了 `tracing.ts`（+59 vs +115,方案可能不同）——先合版进主干后按 r8 验收口径复测,另一支只挑独有断言。
- **L-2**：在途③已做（`d44e0129`,faceLift 正则钉死自闭合写法+真机复验）——③合入即销账。
- **L-3 仍无人认领 🔧**：r7 原文照做（`boardArt.ts` 贴纸补章）。

---

## 休闲对战（给 B）

### 合流④⑤（第〇节顺序,B 主责)

- **N-25 + N-31 + N-1**：④合入即销账（塔折叠/训练场 sticky/接果钳高,量化数字按④报告记档）;N-1 撞⑤取④版（先合版）。
- **⑤ 大 rebase**：8 文件冲突清单与裁决规则见第〇节第 4 条。合入后 N-2…N-7 / N-11…N-19 / C-2…C-9 按 diff 对照 r4/r5 playbook 逐条核销。

### 无人认领组 🔧（合流后 B 的新工位）

- **N-26 duo-vs-star 闯关七键**（r6 原文）/ **N-29 bubble-aim 关内发射台 + N-23 补充版**（三款自建地图 focusCurrent,与 N-39 姊妹账,修法口径统一 `scrollIntoView({block:"center"})`）——照 r6/r7 playbook。
- **N-10 棋类三款横屏**（连续多轮无人认领,`66601b46` 当年不够,r5 口径仍开账）。
- 护栏（勿扩大化）：r9 干净名单（攻略抽屉 sheet 态/搜索态/组字 root/限时基线四档/gold-hook）+ 本轮新增 **red-blue-tap 双人态两档、平板竖屏 768×1024 三面**——都别当回归重查,合流后只跑 915×412 主档防回归即可。

---

## 完成定义（两人共用）

1. **第〇节合流完成**：5 条在途分支全部收进主干或书面弃案（弃案写明「同职能被先合版覆盖」+ 独有增量去向）;全库只留一份 modebar 守门测试。
2. 全部 🔧 关账或书面降级（降级写数学/物理理由,照 BL-W6-03 格式）。
3. `npm test` / `npm run build` 全绿,水位只增不减（进场 = **1095 文件 / 19288 用例**）;每条修复有配套测试或取反断言。
4. N-40 与 N-33 在五档复测数字留档,弹窗验收矩阵按模式 L 含最长内容实例;「展开导入区同屏」断言在库。
5. 报告按既有命名续档,对账本 playbook 与 r4–r9 playbook 六方编号;撞车裁决全部按「先合版」纪律记录明细。
