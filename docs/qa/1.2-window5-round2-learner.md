# 窗口5 · 第2轮学习优化员

> 本文件由三名学习优化员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏；平台文件（`src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`）不在本轮范围内。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 2 轮** · 档A 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`bdd649a`（对齐 `origin/game-1.2-window5`，第 2 轮档B 测试员的走查计划 commit）
**模型 slug**：`claude-opus-5-thinking-high-fast`

**读过的输入**

| 文件 | 拿来当输入的那一节 |
| --- | --- |
| `docs/qa/1.2-window5-round2-tester.md` **档A 全文** | 阻断 1（W5R2-A-01）/ 严重 2（W5R2-A-02、W5R2-A-03）/ 一般 2（W5R2-A-04、W5R2-A-05）/ 建议 5（W5R2-A-06…10） |
| `docs/qa/1.2-window5-round1-tester.md` 档A | W5-A-01…05（一般 ×5，第 1 轮已清）、W5-A-06 / 07（交窗口1） |
| `docs/qa/1.2-window5-round1-learner.md` 档A | 落地 5 条（W5-L-01…05）；留第 2 轮 W5-L-08 / 09 / 10；交窗口1 W5-L-06 / 07 |
| `docs/qa/1.2-window5-round1-fixer.md` 档A | 补修 W5-F-01…04；收尾发现阻断 W5-F-07（舞台硬裁）；留第 2 轮 W5-F-05 / 06 |
| `docs/qa/1.2-window5-round1-fixer.md` 档B（只读，借方法） | `fitIntoStage()` 这套「CSS 的 `max-height:100%` 空转、必须用运行期像素钳位」的结论，本轮在档A 复用同一套做法，代码各写各的、不跨档 import |
| `docs/plan-1.2-step28-B-learner.md` | 报告结构与「来源思路 / 现状 / 落地方式 / 量化 / 受益 id / 风险与回滚」的六段式（路径按窗口5 覆盖） |

**权限边界**：只改 `src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**` 与本文件的**档A 节**。
不改测试员报告与第 1 轮三份报告，不改 `src/ui/**` / `src/engine/**` / `src/styles.css` / `level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，不改本档以外的任何游戏，不引入任何外部运行时依赖，不动存档 key，CSS 前缀沿用各款已有前缀（`ld-` / `ldc-` / `rbr-` / `rbe-` / `rbv-` / `clf-`），**用例只增不减**，失败只鼓励，前 99 关摘要锁不放水。

**这一轮列 18 条、真正改代码落地 10 条**（提示词要求「至少 8 条 / 至少落地 3 条，且 W5R2-A-03 必须在内」）。
落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例（先红后绿）、失败可以靠 `git revert` 单条回滚。

**优先级**：先做测试员点名「必须落地」的 W5R2-A-03（严重·教育正确性），再做三条舞台裁切的**本档那一半**（W5R2-A-01 / A-02 / A-05），然后是玩法与难度曲线的修正（W5R2-A-04 / A-07 / A-08 / A-09 / A-10），最后补第 1 轮留下来的覆盖率缺口（W5-L-09）。
平台那一半（`.game-stage{overflow:hidden}` / `.l99-stage-wrap` / 壳顶栏 `inert`）按纪律**一行都不碰，交窗口1**。

**跑测环境**：`google-chrome` headless + CDP，`Emulation.setDeviceMetricsOverride` 打视口、`setTouchEmulationEnabled` 开触屏，
点得着与否一律用 `document.elementFromPoint(键心 x, 键心 y)` 回读，不认 `el.click()`——那玩意儿在裁切线外也会「成功」。
量尺脚本写在 `tools-tmp/` 下，跑完即删，一个字节都没进提交。

### A1 · 十八条改进逐条

编号沿用第 1 轮 learner 的 `W5-L-nn` 序列往下接（第 1 轮档A 用到 10，档B 用到 21，档C 用到 37），本轮档A 从 `W5R2-L-01` 起另开一段，避免与并行的档B / 档C 撞号。
每条六段：**来源思路 / 现状 / 落地方式 / 量化 / 受益 id / 风险与回滚**。

---

#### W5R2-L-01 · `clock-house` 12↔24 时制改用课本的七段时段词 —— ✅ 本轮落地（测试员 W5R2-A-03，**严重·点名必须落地**）

**来源思路**：测试员把 188 关的 `h12`（60 道）与 `h24`（62 道）按小学《24 时计时法》口径逐题核，**78 道对不上、落在 46 关**，其中 9 道是硬错（「上午 12:30」「下午 12:10」——中文里这两句根本不存在）。

**现状**：`levels.ts` 只有 `PERIODS = ["上午", "下午"]`，`to12Hour()` 走「0–11 → 上午 / 12–23 → 下午」的纯 AM/PM 二分。这套二分是英文 am/pm 的直译，跟中文口语和课本都对不上：0 点被说成「上午 12 点」，12 点被说成「下午 12 点」，孩子照着念在课堂上会被判错。

**落地方式**：
- `logic.ts` 把 `DayPeriod` 从二分改成课本的**七段**：夜里(0) / 凌晨(1–5) / 早上(6–8) / 上午(9–11) / 中午(12) / 下午(13–17) / 晚上(18–23)，抽出 `DAY_PERIODS`、`PERIOD_BANDS`、`dayPeriodOf()`、`hoursOfPeriod()`，`to12Hour()` / `to24Hour()` 一起改口径。
- `levels.ts` 的 `qH12` / `qH24` 跟着改：**干扰项一律由 `to12Hour()` 生成**，保证每一个都是真说得出口的说法，只是配错了时刻——不再出现「上午 12:40」这种语言上就不成立的选项。
- `hints.ts` / `guide.ts` 的方法提示与攻略同步改口径，并把「上午 12 点」「下午 12 点」这两个常见误区明写出来。
- 改动**全在第 100 关之后**，前 99 关摘要锁一个字没碰。

**量化**（独立复算题面 + 正确答案，不含干扰项）：

| | 改前 | 改后 |
| --- | --- | --- |
| 不合课本口径的说法 | **140 处 / 77 关** | **0** |
| 其中硬错（「上午 12:xx」「下午 12:xx」） | **58 处 / 45 关** | **0** |
| 与测试员手点的 9 道硬错逐个对上 | ✅ | — |

测试员手点到的 9 处硬错全在 `h12`+`h24` 里；本轮另外 49 处硬错在测试员没量过的**跨中午题（`spanNoon`）** 里，一并收掉。

**受益 id**：`clock-house`（第 100–188 关的 `h12` / `h24` / `spanNoon` 三种题型）。

**风险与回滚**：口径变了，`logic.test.ts` / `levels.test.ts` 里原来按二分写的断言同步改成七段（**只改断言不删用例**）。新增 `dayPeriod.test.ts` 9 条守门，含「任何一道题的题面与选项里都不许出现『上午 12 点』『下午 12 点』」这条全量扫描。单条 `git revert` 可回滚。

---

#### W5R2-L-02 · `color-fun` 这一屏钳进舞台看得见的那一段 —— ✅ 本轮落地（测试员 W5R2-A-01 **阻断**的本档那一半）

**来源思路**：测试员实测 390×844 起，调色板整排 + 调锅三原色 + 撤销/重做（8–11 颗）用真实坐标点不着，舞台还不滚——选不了色 = 涂不了 = 过不了关。

**现状**：舞台看得见 730px，这一屏能长到 1093px，多出来的 363px 被平台 `.game-stage{overflow:hidden}` 直接裁掉。CSS 侧写不了：舞台比视口矮一大截，按 `vh` 判会判成「屏幕够高，不用收」。

**落地方式**（三步，都在本款自己的 `ui.ts` / `index.ts` 里）：运行期量出祖先裁切线算真实可视高 → 挂 `clf-tight` 收留白与字号（44px 热区一个不动） → 收画布框（下限 180px） → 还高就让这一屏自己滚（写**像素** `max-height`，不写在这条壳层链上空转的 `100%`）。另补 `.clf-wrap>*{flex:0 0 auto}`——不加这一行，钳出天花板后调色板整排会从 81px 被压成 6px、画布框直接归零，「点不着」会从「在屏幕外」变成「压没了」，比原来还糟。

**量化**（CDP 真机，3 档视口 × 第 40/90/140/170 关 = 12 组）：点不着的按钮 `10/7/8/10 · 10/8/8/10 · 11/8/9/11` → **全部 0**；舞台残留裁切 194–598px → 14–15px，且裁掉的那一段里没有任何可点元素；真手指上滑两次 `scrollTop` 0 → 最大值。

**受益 id**：`color-fun`（关卡 + 🎨自由画室）。

**风险与回滚**：慢慢涂的游戏，挂滚动条不伤手感（这一点和跑步游戏刚好相反）。新增 `stageFit.test.ts` 20 条。单条 `git revert` 可回滚。

---

#### W5R2-L-03 · `red-blue-race` 舞台矮到装不下就逐档收紧 —— ✅ 本轮落地（测试员 W5R2-A-02 **严重**的本档那一半）

**来源思路**：测试员实测 360×720 起「🦘 跳」点不着，360×640 上左脚 / 右脚 / 跳**三颗全挂**——触屏玩家在这两档视口上根本跳不了。

**现状**（本轮 CDP 复量，和测试员对上）：

| 视口 | 舞台看得见 | 裁切线 | 这一屏 | 底沿 | 超出 | 点不着 |
| --- | --- | --- | --- | --- | --- | --- |
| 390×844 | 730px | y=830 | 535px | y=863 | 33px | 0 |
| 360×720 | 610px | y=706 | 565px | y=889 | **183px** | **1**（跳） |
| 360×640 | 530px | y=626 | 565px | y=889 | **263px** | **3**（左脚/右脚/跳） |

拆开看：平台壳层在 360×640 上吃掉 538px 舞台里的 **236px**（壳顶栏 88 + `l99` 抬头 110 + 关卡 HUD 116），留给本款的只有 **302px**，而这一屏要 565px。**平台那一半交窗口1，本档这一半就是这 565px。**

**落地方式**：新增 `red-blue-race/fit.ts`——实测祖先裁切线算出可视高，装不下就逐档挂 `rbr-tight` / `rbr-tighter`（三个模式：关卡场 / 对战场 / 跑不完的跑道全接上）。
- 第一档：收抬头条、赛道条、留白与字号，**并把跳键从「独占一整行」改成三颗并排**——这一改直接省掉一整行 86px，是这条里最大的那一笔。
- 第二档：再收抬头条的头像徽章（名字在赛道条上还留着一份）与赛道高度。
- **不给它挂滚动条**：连点玩法里能滚就会「想按却滑走了」，跑动键必须落在哪儿就是哪儿。
- **热区一分不动**：最狠一档跑动键仍有 52px、让分开关仍是 44px；三颗并排后每颗宽 100px 出头。

**量化**（CDP，`elementFromPoint` 真实坐标）：

| 视口 | 这一屏高度 | 点不着 |
| --- | --- | --- |
| 390×844 | 565 → **364px** | 0 → 0 |
| 360×720 | 565 → **364px** | **1 → 0** |
| 360×640 | 565 → **292px** | **3 → 0** |

**受益 id**：`red-blue-race`（188 关战役 + ⚔️对战场 + ♾️跑不完的跑道）。

**风险与回滚**：三颗并排是这条里唯一动到布局的地方，只在装不下时才挂——宽屏上一个像素不变；且左右拇指各守一颗、跳在正中间，对跑步游戏反而更顺手。新增 `fit.test.ts` 26 条（含「这个文件不许往 style 上写 overflow / maxHeight」这条防跑偏）。单条 `git revert` 可回滚。

---

#### W5R2-L-04 · `landlord-cards` 舞台矮到装不下就逐档收紧 —— ✅ 本轮落地（测试员 W5R2-A-05 的本档那一半）

**来源思路**：测试员实测 360×640 上「⏸ 暂停」点不着——孩子想暂停只能退出重来。

**现状**（本轮 CDP 复量）：360×640 上舞台裁在 y=626，这一桌从 y=270 起、467px 高，底沿掉到 y=737，**超出 111px**，暂停键心 y=669 落在裁切线以下 **43px**。往下到 320×568 更狠：叫地主那一排「不叫 / 1 分 / 2 分 / 3 分」四颗连同暂停一起挂掉，**连开局都开不了**。

**落地方式**：新增 `landlord-cards/fit.ts`，同一套「实测裁切线 → 逐档收紧」，类名沿用本款 1.2 的 `ldc-` 前缀锁（`ldc-tight` / `ldc-tighter`）。第一档收留白、字号与键盘那一行；第二档再把对家面板上的**装饰小牌背**收起来——「还剩几张」那行字说的是同一件事。出牌那一排 48px、底下那一排 44px、手牌本身**一分不动**：为了「装得下」把热区收到 44 以下，等于换一种点不着。换档才回头重摆手牌扇，不换档不白重画。

**量化**：

| 视口 | 这一桌高度 | 点不着 |
| --- | --- | --- |
| 390×844 | 475 → 475px（地方够，一档都不挂） | 0 → 0 |
| 360×720 | 467 → **380px** | 0 → 0 |
| 360×640 | 467 → **340px** | **1 → 0** |
| 320×568 | 554 → **339px** | 5 → 5 **（压不掉，见 W5R2-L-14）** |

**受益 id**：`landlord-cards`（188 关战役 + ♾️无尽连胜 + ⚔️双人对战）。

**风险与回滚**：新增 `fit.test.ts` 23 条。单条 `git revert` 可回滚。

---

#### W5R2-L-05 · `landlord-cards` 无尽连胜输了重开不再永远发同一副牌 —— ✅ 本轮落地（测试员 W5R2-A-04）

**来源思路**：测试员走「输了之后重开」这条路时发现，每次回到第 1 局都是同一副牌。

**现状**：`showOver()` 的「🔁 从第 1 局再来」把 `streak` 和 `bump` 一起清零，`startRound()` 于是回到恒定种子 `buildEndlessRound(1).seed + 0`——连开三次首局，手牌签名逐字相同。孩子在第 1 局卡住就会一直卡在这副牌上。对照 ⚔️双人对战的「🔁 再来一局」是 `round++`，每局本来就换牌，所以没这毛病。

**落地方式**：`bump` 改成**只增不减**，并把「第几局 + 第几次重开 → 种子」抽成纯函数 `endlessDealSeed(round, attempt)`。

**量化**：连开 8 次第 1 局，改前得到 **1 副**牌、改后得到 **8 副互不相同**的牌。

**受益 id**：`landlord-cards` ♾️无尽连胜。

**风险与回滚**：新增 `endless.test.ts` 8 条，含一条「老口径只发 1 副」的反例防空转。单条 `git revert` 可回滚。

---

#### W5R2-L-06 · `landlord-cards` 连输两次之后帮着挑一副能赢的牌 —— ✅ 本轮落地（测试员 W5R2-A-08，建议）

**来源思路**：测试员让「完全照着 💡 教练提示打」的玩家把 188 关各打一局，输了换一副再打——187 关都在 2 次以内翻到能赢的牌，**只有第 188 关要连输 5 次**，第 6 次才过第一次。

**现状**（本轮用同一套牌桌 `sim.runTable` 的 `coach` 座位 + 同档小牌灵复算，比测试员多扫出几关）：

```
最坏连输次数 6 次;要连输 ≥3 次的关共 7 关:
  第 81 关=5 次  第 188 关=6 次
  第 150 / 153 / 167 / 170 / 177 关各 3 次
```

**落地方式**：`levels.ts` 抽出 `levelDealSeed(lv, n)`（原来散在 `index.ts` 里的 `lv.seed + bump*104729`），新增 `coachCanWin(lv, n)` 与 `mercyRedeal(lv, n)`。头两次原样换牌——**摔两跤是塔的一部分**；从第三次起往后最多试 10 副，端第一副「照着教练提示打能赢」的上来，试不到就用原来那一副，绝不让孩子干等。

**为什么不是抬 `boost`**：`boost` 抬上去只是换一手更强的牌，而「牌强」和「这一局赢不赢」不是一回事。实测四种抬法都试过——

| 兜底口径 | 最坏连输 | 要连输 ≥3 次的关 |
| --- | --- | --- |
| 现状（不兜底） | 6 | 第 81 / 150 / 153 / 167 / 170 / 177 / 188 关 |
| 连输 2 次 → boost 1 | 6 | 变成第 81 / 131 / 138 / 150 / 153 / 158 / 165 / 177 / 178 关 |
| 连输 2 次 → boost 2 | 6 | 变成第 81 / 138 / 148 / 150 / 165 / 166 / 178 / 188 关 |
| 连输 3 次 → boost 2 | 5 | 变成第 81 / 150 / 153 / 167 / 170 / 177 / 188 关 |
| **按「能不能赢」挑** | **2** | **0 关** |

抬 `boost` 尾巴只是换了个地方长；直接按「能不能赢」挑才是对着问题本身下手。

**量化**（188 关全塔复算）：

| | 改前 | 改后 |
| --- | --- | --- |
| 最坏连输次数 | **6** | **2** |
| 要连输 ≥3 次的关 | **7 关** | **0 关** |
| 一把过的关数 | 134/188 | **134/188（没动）** |
| 兜底一次的开销 | — | 最多 10 次牌桌模拟，实测 <3ms，只发生在按下重开的那一刻 |

「一把过 134/188 一个数没变」是这条最要紧的一行：**没有任何一关因为这条兜底变得更容易一把过**，变的只是「卡住之后要卡多久」。

**受益 id**：`landlord-cards` 188 关战役（其中第 81 / 150 / 153 / 167 / 170 / 177 / 188 关直接受益）。

**⚠️ 需要签字的一点**：这条兜底是**全塔生效**的，包括第 81 关（≤99）。选择这么做的理由是——关卡表 `LEVELS` 一个数没改（`boost` / `seed` / `aiLevel` 全原样）、前 99 关摘要锁一个字没动、任何一关的**首次难度完全不变**（一把过 134/188 不动），改的只是「已经输了两次之后」的运行期行为，方向上属于「失败只鼓励」。若审阅认为前 99 关连这个也不该碰，`mercyRedeal()` 里加一句 `if (lv.index < 99) return n;` 即可收窄，是一行的事。

**风险与回滚**：新增 `mercy.test.ts` 16 条，含「188 关一关不落：谁都不用连输超过 2 次」的全量断言。单条 `git revert` 可回滚。

---

#### W5R2-L-07 · `red-blue-race` 节拍风廊首关那一级 4.25 秒的台阶削平 —— ✅ 本轮落地（测试员 W5R2-A-07，建议）

**来源思路**：测试员逐关跑熟练档，**第 145 关（节拍风廊首关）只剩 0.98 秒余量**，是全 188 关最小值，比上一关（5.23 秒）一口气掉了 4.25 秒。

**现状**：本轮用仓库自带的 `simulateRace` 复算，测试员的数字逐个对上（145：5.23 → 0.98，掉 4.25；167：5.17 → 2.28，掉 2.89）。根因是这一关同时做了三件事——电脑速度从 8.93 只降到 8.60、步长从 1.6 掉到 1.5、外加**鼓点机制第一次出场**（不踩准就没有连击加成）。孩子第一次撞上新机制就被压到 1 秒以内。

**落地方式**：**不调水这一章**——章末（第 166 关）的电脑速度 9.65 与步长 1.5 一个数没动。
- 电脑速度 `8.6 + t*0.05` → `8.3 + t*1.35/21`：开头放缓，章末落回同一个数。
- 头三关给一小段步长缓坡 `1.6 → 1.57 → 1.53`，第 148 关起仍是本章原来的 1.5。

**量化**（熟练档 = 每秒 6 下、会跳、会踩点、会换气）：

| | 改前 | 改后 |
| --- | --- | --- |
| 第 145 关余量 | **0.98 秒** | **2.20 秒** |
| 本章最小余量 | 0.98 秒 | **1.40 秒** |
| 章内单关最大掉幅 | 0.32 秒 | 0.33 秒（**没推出新台阶**） |
| 乱点档整章战绩 | 0 / 22 | **0 / 22**（还是一关都赢不了） |

鼓点间隔、连击上限、机关表一个字没改——**难点仍旧是「踩不踩得准」**，只是不再要求第一次就踩得准。

**受益 id**：`red-blue-race` 第 145–166 关（节拍风廊整章，收益集中在头三关）。

**风险与回滚**：新增 `step145.test.ts` 12 条，含「它不再是全 188 关余量最小的那一关」「乱点档整章还是一关都赢不了」两条对拉的断言，防止一头调过火。单条 `git revert` 可回滚。

---

#### W5R2-L-08 · `red-blue-race` 跑不完的跑道补一个「跑完全程」的出口 —— ✅ 本轮落地（测试员 W5R2-A-09，建议）

**来源思路**：测试员的机器人「见坑就跳」跑满 **91 秒 / 6950 步 / 511 次起跳**、密度到「每百米 6.3 个机关」仍未收工。

**现状**：规则只有「撞 3 次」这一个出口，不撞就永远不结束。而**不结束就不结算**——`finish()` 才写纪录、才发小星星，所以跑得再远也白跑，这才是这条真正扎人的地方。

**落地方式**：`logic.ts` 新增 `ENDLESS_GOAL_M = 1200` 与 `endlessGoalReached()`，`endlessRunOver(hits, meters)` 多认一个出口（第二个参数默认 0，老调用点不受影响）。跑满全程走**庆祝版**结算（🎉「跑完全程 N 米!」+ `win` 音效），和摔三跤那一版分开；摔三跤那条老路一个字没改。

**1200 米不是拍脑袋**：陪跑星星的速度在 **1020 米**封顶（`endlessChaserSpeed`）、机关密度在 **810 米**封顶（`endlessGapMeters` 撞到 16 米最小安全间距）。过了 1020 米，这条跑道上每一个旋钮都已经拧到头，再跑就是同一段路原样重播。收在 1200 米，两条曲线都完整跑过一遍了，谁也没被腰斩。

**量化**：「一跤没摔」的打法 改前 **永不收工 / 永不结算**（91 秒 6950 步实测仍在跑）→ 改后 **1200 米收工并正常写纪录、发小星星**。摔三跤那条路的行为 0 变化。

**受益 id**：`red-blue-race` ♾️跑不完的跑道。

**风险与回滚**：新增 `endless.test.ts` 11 条，含两条「收线必须摆在两条曲线封顶之后」的算式断言——以后谁调速度 / 密度曲线，收线跟不上就会红。单条 `git revert` 可回滚。

---

#### W5R2-L-09 · `color-fun` 中绿改由「浅绿 + 深绿」兑出 —— ✅ 本轮落地（测试员 W5R2-A-10 ①，建议）

**来源思路**：测试员说「浅绿 + 绿色 = 中绿」这条的亮度落在两个原料之间、方向不如别的配方直白。

**现状**：本轮按 CIE L\* 逐条复核，**比测试员说的更糟**——

```
浅绿 L=94.6    绿色 L=85.1    中绿 L=74.4    深绿 L=57.5
```

中绿比两支原料**都深**。这条配方实际在教「两支淡的兑出一支更深的」，是真的教错，不只是「方向不直白」。

**落地方式**：改成「浅绿 + 深绿 = 中绿」（94.6 与 57.5，中绿 74.4 正好夹在中间），`why` 改成「浅绿加深绿，兑出中间的中绿」——名字也就说得通了。配方总数不变，前 99 关在用的基础色 hex 一个字没动。

**量化**：出来的颜色 比两支原料都深（差 10.7 / 20.2 个 L\*）→ **严格夹在两者中间**。188 关的 `needMix` 目标与四种模式的规则全量跑过，0 失败。

**受益 id**：`color-fun`（22 条配方里的这一条，以及所有把中绿当目标色的关）。

**风险与回滚**：新增 `mixSaying.test.ts` 13 条，其中「凡是『兑出中间那一档』的配方，出来的都必须真的夹在两支原料之间」是一条**通用规则**，以后再加这类配方会自动被管住。单条 `git revert` 可回滚。

---

#### W5R2-L-10 · `clock-house` 错题回顾按题型逐种补覆盖率 —— ✅ 本轮落地（第 1 轮 W5-L-09 遗留，测试员点名交给本轮 learner）

**来源思路**：第 1 轮 learner 把这条列成「留第 2 轮」；第 2 轮测试员实机走通了这条链（第 90 关故意答错「1 点 3 刻」→ 回顾轮出「1 点 1 刻」，同题型换数字），结论写的是「**行为正确，覆盖率补测的活留给本轮 learner**」。

**现状**：已有的 `levels.test.ts` 是抽样查（每 7 关一关 + 四个锚点关）。抽样能证明「这几关没问题」，证明不了「每一种题型都没问题」——一种只在某几关出现的题型，抽样很容易整个漏过去。

**落地方式**：换个查法——先把 188 关里**真出现过的题型全列出来**（实测 ≥15 种），再要求每一种都被真的验过：同类、换数字、三个唯一选项、答得出一句人话、连错两轮不重复；另加 188 关全量（不抽样）三条。以后加新题型忘了管回顾轮，会当场被拦住。

**量化**：抽样 5 个锚点关 + 每 7 关一关 → **每一种题型 × 首次出现关 逐个验 + 188 关全量三条**。跑下来全绿，**一行产品代码没改**，与测试员「行为正确」的结论一致。

**受益 id**：`clock-house` 📒错题回顾链（全 188 关）。

**风险与回滚**：纯补用例，无产品代码风险。这条**没有「先红」**——因为行为本来就是对的，红的话反而说明测试员的结论错了；它买的是「以后不许悄悄退化」。

---

#### W5R2-L-11 · `color-fun` 画室焦点圈盖不住壳顶栏 —— ❌ 本轮不落地，**交窗口1**

**来源思路**：测试员 W5R2-A-06（建议），并已在报告里写明「→ 交窗口1」。

**为什么不落地**：焦点陷阱要盖住的是**壳顶栏**，那是 `src/ui/**` 的地盘，本档禁改。本款自己这一侧第 1 轮已经做过（W5-L-04：把背后的选关地图关进 `inert` 并接管焦点），能做的都做完了；剩下这一段必须由持有壳层的人来收。

**交接**：`color-fun` 打开 🎨自由画室时会 `inert` 掉选关地图，但壳顶栏（返回 / 音量 / 家长锁）不在它能触及的 DOM 子树里，Tab 仍会跑出去。窗口1 侧的做法应是壳层自己在「子游戏开了模态」时把顶栏一起 `inert`。

---

#### W5R2-L-12 · 平台舞台硬裁本身（`.game-stage{overflow:hidden}` / `.l99-stage-wrap`） —— ❌ 本轮不落地，**交窗口1**

**来源思路**：W5R2-A-01 / A-02 / A-05 三条的**平台那一半**，测试员已写明拆分。

**为什么不落地**：`src/styles.css` 与 `src/ui/**` 是禁改文件，提示词写明「一行都不要动」。

**交接（把数字交出去，别只交结论）**——360×640 上，538px 的舞台被平台壳层吃掉 **236px**：

| 层 | 高度 | 归属 |
| --- | --- | --- |
| `.game-topbar` | 88px | 平台 |
| `l99` 抬头（`.l99-stage-wrap` 顶 → `.l99-stage` 顶） | 110px + 16px | 平台 |
| 留给子游戏的 | **302px** | 本档 |

本档已经把三款分别压到 **292 / 340 / 364px**（原 565 / 467 / 565），三档视口点不着全部归 0。**再往下压就要动 44px 热区了**。窗口1 若能让 `.game-stage` 在装不下时可滚，或把 `l99` 抬头在矮屏上折起来，320×568 那一档（见 W5R2-L-14）也能一并解决。

---

#### W5R2-L-13 · `color-fun`「同色 + 同色 = 更深」的口径 —— ❌ 本轮不落地（测试员 W5R2-A-10 ②），改成**锁住说法**

**来源思路**：测试员说「红+红=深红」「黄+黄=金黄」是游戏化近似，物理上同色相混不会变深。

**为什么不落地**：要看这句话说的是哪件事。如果说的是「两束光 / 两种色相相加」，那确实不会变深；但这一条的 `why` 写的是「**再加一勺颜料，颜料更浓**」——一杯水里多化一勺同样的颜料，画出来就是更沉，这在水彩台上真会发生，说法站得住。而且这三条是 **1.0 的老配方、前 99 关靠它**，动不得。

**改成做什么**：既然不改，就守住**说法不许漂**——`mixSaying.test.ts` 里钉了三条：这一族的理由必须出现「加一勺」和「浓」、**不许**出现「调出 / 混出」（那是色相合成的说法）、并且「既然说更浓，出来的就必须真的更沉」（按 L\* 验）。同时把「调出」这个词锁死只给三原色两两相加那三条 + 白加黑。

---

#### W5R2-L-14 · `landlord-cards` 320×568 上还余 107px 装不下 —— ❌ 本轮压不掉，**数字交 fixer / 窗口1**

**来源思路**：本轮 CDP 自查扫出来的（测试员只列到 360×640）。

**现状**：320×568 上舞台只看得见 458px、裁在 y=554，这一桌从 y=322 起——**留给本档的只有 232px**。已经挂到最狠一档，这一桌 554 → **339px**，仍差 107px。叫地主那一排四颗 + 暂停共 5 颗点不着。

**为什么压不掉**：出牌那一排 54px + 底下那一排 44px + 手牌扇 82px = **180px 就是热区本身**，再加对家面板与桌面最少也要 60px 上下。**232px 装不下 180px 热区 + 必要信息**，除非把热区压到 44 以下——那是换一种点不着，不是修。

**交接**：这一档要么靠 W5R2-L-12（平台让舞台可滚 / 矮屏折起 `l99` 抬头，只要还回来 ~110px 就够），要么由 fixer 决定在这一档改横排布局。同视口下 `red-blue-race` 已经压到 292px 并且**点不着 0**，说明本档侧该压的都压了。

---

#### W5R2-L-15 · 三款各写了一份「舞台收紧器」，可以抽成一个公共件 —— ❌ 本轮不落地，**留第 3 轮 / 交窗口1**

**来源思路**：本轮实现 W5R2-L-02 / L-03 / L-04 时，`color-fun/ui.ts`、`red-blue-race/fit.ts`、`landlord-cards/fit.ts` 里各有一份「往上找裁切祖先 → 算可视高 → 逐档收紧」，`visibleRoomPx()` 三份实现逐字相同。

**为什么不落地**：公共件该放的地方是 `src/ui/**` 或 `src/engine/**`——本档禁改。跨游戏 import（比如 `red-blue-race` 去 import `color-fun/fit`）在权限上做得到，但那是更糟的耦合：两款的收紧策略本来就不一样（涂色滚得起、跑步滚不起），硬合成一个会逼出一堆开关。

**交接**：真要抽，建议抽的只有 `visibleRoomPx(selfTop, clipperBottoms)` 与「往上找裁切祖先」这两段**纯几何**，逐档收紧的策略仍旧各游戏自己写。第 1 轮档B 的 `fitIntoStage()` 是同一个东西的第四份实现——四份都在了，值得由窗口1 收口。

---

#### W5R2-L-16 · `red-blue-race` 战役对手的局内动态难度 —— ❌ 本轮不落地，**不建议做**（第 1 轮 W5-L-08 遗留）

**来源思路**：第 1 轮 learner 留给第 2 轮；第 2 轮测试员给了口径：读招电脑 `aiAdapt` 已在 167–188 关逐关抬（0.277 → 0.349），让分开关封顶 8% 且默认关；量化下来「熟练档全 188 关不输、乱点档后期全输、章节余量单调下探」。

**为什么不落地**：测试员的结论是「这条**不是必须**；真要动，先抬的应该是第 145 关那 0.98 秒余量」——那正是本轮已落地的 **W5R2-L-07**。曲线本身健康的情况下再叠一层局内动态难度，只会让「为什么这局特别难」变得不可解释，对孩子和家长都不友好。**这条建议结案，不必留到第 3 轮。**

---

#### W5R2-L-17 · `landlord-cards` 提示「不泄底」口径 / 13 道指令语带答案字 / 摘要锁边界 —— ❌ 无需动代码（第 1 轮 W5-L-10、W5-F-05、W5-F-06 遗留）

**来源思路**：第 1 轮留的三条，第 2 轮测试员逐条复算完毕。

**为什么不落地**：三条测试员都已给出闭环结论——W5-L-10 三档提示按新口径 **0 命中**；W5-F-05 的 13 道指令语按新口径复算 **13 → 0**，不用动摘要锁；W5-F-06 摘要锁边界「量清楚了，不是缺陷」。本轮独立复读了这三处的代码与用例，与测试员一致，**没有可落地的动作**。

---

#### W5R2-L-18 · `word-garden` 本轮无改动 —— ❌ 无可落地项

**来源思路**：本档五款里 `word-garden` 是唯一一款两轮测试都零发现的。

**现状**：第 2 轮测试员对它的记录是——1181 道题 **0 错、0 重复、0 不唯一**；两种非选择题形态都真走了一遍（笔顺描红台第 105 关「月」四笔一笔不差，笔画名与标准笔顺逐笔对上；组字工坊第 188 关正常）；描歪了只说「描错顺序也没关系，我们再来一次～」，不判失败。第 1 轮的 W5-L-02（题干不许写着要填的那个字）已落地并有守门用例。

**为什么不落地**：**没找到值得改的东西**。本轮读了它的题库生成与描红判定，没有发现新的教育正确性问题或热区问题；它也不在测试员列出的任何一条缺陷里。硬凑一条改动只会平白引入风险。

---

### A2 · 交卷

**窗口号**：5
**角色**：本窗三轮验收 · **第 2 轮 · 档A 学习优化员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（未推 main，未 `--force`，未动 `game-1.2`，未用 `gh` 开/改/合 PR）
**基线**：`bdd649a` → 开工前分支已推进到 `509fc84`（档C 监督修复员的计划 commit），本节所有改动压在它之上

**列了几条 / 落地几条**：**列 18 条 / 真正改代码落地 10 条**（要求：≥8 条 / ≥3 条且含 W5R2-A-03 —— W5R2-A-03 是落地的第一条 W5R2-L-01）

| | 编号 | 对应测试员条目 | 游戏 |
| --- | --- | --- | --- |
| ✅ 落地 | W5R2-L-01 | **W5R2-A-03 严重·点名必须落地** | `clock-house` |
| ✅ 落地 | W5R2-L-02 | W5R2-A-01 阻断（本档侧） | `color-fun` |
| ✅ 落地 | W5R2-L-03 | W5R2-A-02 严重（本档侧） | `red-blue-race` |
| ✅ 落地 | W5R2-L-04 | W5R2-A-05 一般（本档侧） | `landlord-cards` |
| ✅ 落地 | W5R2-L-05 | W5R2-A-04 一般 | `landlord-cards` |
| ✅ 落地 | W5R2-L-06 | W5R2-A-08 建议 | `landlord-cards` |
| ✅ 落地 | W5R2-L-07 | W5R2-A-07 建议 | `red-blue-race` |
| ✅ 落地 | W5R2-L-08 | W5R2-A-09 建议 | `red-blue-race` |
| ✅ 落地 | W5R2-L-09 | W5R2-A-10 ① 建议 | `color-fun` |
| ✅ 落地 | W5R2-L-10 | 第 1 轮 W5-L-09 遗留 | `clock-house` |
| ❌ 交窗口1 | W5R2-L-11 | W5R2-A-06 | `color-fun` |
| ❌ 交窗口1 | W5R2-L-12 | W5R2-A-01 / 02 / 05 平台侧 | 平台 |
| ❌ 锁说法不改码 | W5R2-L-13 | W5R2-A-10 ② | `color-fun` |
| ❌ 压不掉·交 fixer | W5R2-L-14 | 本轮自查新增 | `landlord-cards` |
| ❌ 留第 3 轮 | W5R2-L-15 | 本轮自查新增 | 三款共性 |
| ❌ 不建议做·结案 | W5R2-L-16 | 第 1 轮 W5-L-08 遗留 | `red-blue-race` |
| ❌ 无需动码 | W5R2-L-17 | 第 1 轮 W5-L-10 / F-05 / F-06 遗留 | `landlord-cards` |
| ❌ 无可落地项 | W5R2-L-18 | —— | `word-garden` |

**新增用例数**：**+149 条 / +10 个文件**（净增，与全量水位差逐条对得上：5667 − 5518 = 149）

| 新增守门文件 | 用例 | 守的是 |
| --- | --- | --- |
| `src/games/clock-house/dayPeriod.test.ts` | 9 | W5R2-L-01 |
| `src/games/clock-house/reviewCoverage.test.ts` | 11 | W5R2-L-10 |
| `src/games/color-fun/stageFit.test.ts` | 20 | W5R2-L-02 |
| `src/games/color-fun/mixSaying.test.ts` | 13 | W5R2-L-09 / L-13 |
| `src/games/red-blue-race/fit.test.ts` | 26 | W5R2-L-03 |
| `src/games/red-blue-race/step145.test.ts` | 12 | W5R2-L-07 |
| `src/games/red-blue-race/endless.test.ts` | 11 | W5R2-L-08 |
| `src/games/landlord-cards/fit.test.ts` | 23 | W5R2-L-04 |
| `src/games/landlord-cards/endless.test.ts` | 8 | W5R2-L-05 |
| `src/games/landlord-cards/mercy.test.ts` | 16 | W5R2-L-06 |
| **合计** | **149** | |

**用例只增不减**（逐个复核改过的三个老用例文件，只改断言口径、一条没删）：

| 老用例文件 | 改前 | 改后 |
| --- | --- | --- |
| `src/games/clock-house/levels.test.ts` | 44 | 44 |
| `src/games/clock-house/logic.test.ts` | 22 | 22 |
| `src/games/color-fun/upgrade12.test.ts` | 43 | 43 |

**npm test**

| | 文件 | 用例 |
| --- | --- | --- |
| 基线 `bdd649a`（独立 worktree 复跑，不照抄他人数字） | 212 passed | 5518 passed |
| **本节交卷** | **222 passed (222)** | **5667 passed (5667)** |

```
 Test Files  222 passed (222)
      Tests  5667 passed (5667)
   Duration  36.15s
```

**npm run build**：✅ 通过（`✓ built in 348ms`；PWA `generateSW` precache 139 entries / 3311.72 KiB；仅有仓库既有的 `parentAuth.ts` 动态导入提示，与本节无关）

**SHA**

| | |
| --- | --- |
| 跑测水位（`npm test` / `npm run build` 都在这条上跑的） | **`e56c937`** |
| 本节最后一条 commit | 本条报告 commit（只动本文件档A 节的 markdown，不改任何代码，不影响上面的水位） |

本节 11 条 commit（每条一件事，可单条 `git revert`）：

```
2b5d2b5 docs  第2轮档A学习优化员 · 调研与落地计划（A0）
b463d0a fix   clock-house 七段时段词            W5R2-A-03  → W5R2-L-01
8baf5b5 fix   landlord-cards 无尽重开换牌        W5R2-A-04  → W5R2-L-05
7602916 fix   color-fun 钳进可视段              W5R2-A-01  → W5R2-L-02
4f7093a fix   red-blue-race 舞台逐档收紧         W5R2-A-02  → W5R2-L-03
f0797d4 fix   landlord-cards 舞台逐档收紧        W5R2-A-05  → W5R2-L-04
96e5b7e feat  red-blue-race 跑完全程出口         W5R2-A-09  → W5R2-L-08
1ea86e9 fix   red-blue-race 第145关台阶削平       W5R2-A-07  → W5R2-L-07
ddd70a1 fix   landlord-cards 连输两次后兜底       W5R2-A-08  → W5R2-L-06
0829893 fix   color-fun 中绿改由浅绿+深绿兑出      W5R2-A-10① → W5R2-L-09
e56c937 test  clock-house 错题回顾逐题型覆盖      W5-L-09遗留 → W5R2-L-10
```

**改到的文件（18 个，全部落在本档五款 + 本文件档A 节）**

| 游戏 | 产品代码 | 用例 |
| --- | --- | --- |
| `clock-house` | `logic.ts` `levels.ts` `hints.ts` `guide.ts` | `dayPeriod.test.ts`✨ `reviewCoverage.test.ts`✨ `logic.test.ts` `levels.test.ts` |
| `color-fun` | `ui.ts` `index.ts` `mix.ts` | `stageFit.test.ts`✨ `mixSaying.test.ts`✨ `upgrade12.test.ts` |
| `red-blue-race` | `fit.ts`✨ `index.ts` `levels.ts` `logic.ts` | `fit.test.ts`✨ `step145.test.ts`✨ `endless.test.ts`✨ |
| `landlord-cards` | `fit.ts`✨ `index.ts` `levels.ts` | `fit.test.ts`✨ `endless.test.ts`✨ `mercy.test.ts`✨ |
| `word-garden` | （无改动） | （无改动） |

（✨ = 本节新建）

**纪律自查**

| 项 | 结果 |
| --- | --- |
| 禁改文件（`src/ui/**` / `src/engine/**` / `src/styles.css` / `level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`） | ✅ 一行未动（`git diff --name-only 509fc84 HEAD` 逐条核过） |
| 平台 `.game-stage{overflow:hidden}` / `.l99-stage-wrap` | ✅ 一行未动，数字交窗口1（W5R2-L-12） |
| 本档以外的游戏 | ✅ 一个目录没碰 |
| 别人的报告（测试员 / 第 1 轮三份 / 本文件档B 档C 节） | ✅ 一个字节没动 |
| 前 99 关摘要锁 | ✅ 未放水（`clock-house` 改动全在第 100 关之后；`landlord-cards` 关卡表 `seed` / `boost` / `aiLevel` 一个数没改） |
| 用例只增不减 | ✅ +149 / −0 |
| 失败只鼓励 | ✅ 新增文案无责备词；`red-blue-race` 跑完全程走庆祝版结算 |
| 商标 | ✅ 本节新增的每一行（含注释）按 28 词黑名单扫过 **0 命中**；五款生产文件 0 命中（各款 `shell.test.ts` / `upgrade12.test.ts` 里出现的词是扫描器自己的词表，不是文案） |
| 是否派生子代理 / 开第 3 轮 / 写 fixer 报告 / 改档B 档C | ✅ 都没有 |
| 临时量尺脚本 | ✅ `tools-tmp/` 跑完即删，0 字节进提交 |

---

## 档B · 五款（待填写）

## 档C · 五款（待填写）
