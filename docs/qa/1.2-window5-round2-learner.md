# 窗口5 · 第2轮学习优化员

> 本文件由三名学习优化员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏；平台文件（`src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`）不在本轮范围内。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 2 轮** · 档A 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`bdd649a`（对齐 `origin/game-1.2-window5`，第 2 轮档B 测试员的走查计划 commit）
**模型 slug**：`claude-opus-5-thinking-high-fast`

**读过的输入**

| 文件 | 拿来当输入的那一节 |
| --- | --- |
| `docs/qa/1.2-window5-round2-tester.md` **档A 全文** | 阻断 1（W5R2-A-01）/ 严重 2（W5R2-A-02、W5R2-A-03）/ 一般 2（W5R2-A-04、W5R2-A-05）/ 建议 5（W5R2-A-06…10） |
| `docs/qa/1.2-window5-round1-tester.md` 档A | W5-A-01…05（一般 ×5，第 1 轮已清）、W5-A-06 / 07（交窗口1） |
| `docs/qa/1.2-window5-round1-learner.md` 档A | 落地 5 条（W5-L-01…05）；留第 2 轮 W5-L-08 / 09 / 10；交窗口1 W5-L-06 / 07 |
| `docs/qa/1.2-window5-round1-fixer.md` 档A | 补修 W5-F-01…04；收尾发现阻断 W5-F-07（舞台硬裁）；留第 2 轮 W5-F-05 / 06 |
| `docs/qa/1.2-window5-round1-fixer.md` 档B（只读，借方法） | `fitIntoStage()` 这套「CSS 的 `max-height:100%` 空转、必须用运行期像素钳位」的结论，本轮在档A 复用同一套做法，代码各写各的、不跨档 import |
| `docs/plan-1.2-step28-B-learner.md` | 报告结构与「来源思路 / 现状 / 落地方式 / 量化 / 受益 id / 风险与回滚」的六段式（路径按窗口5 覆盖） |

**权限边界**：只改 `src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**` 与本文件的**档A 节**。
不改测试员报告与第 1 轮三份报告，不改 `src/ui/**` / `src/engine/**` / `src/styles.css` / `level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，不改本档以外的任何游戏，不引入任何外部运行时依赖，不动存档 key，CSS 前缀沿用各款已有前缀（`ld-` / `ldc-` / `rbr-` / `rbe-` / `rbv-` / `clf-`），**用例只增不减**，失败只鼓励，前 99 关摘要锁不放水。

**这一轮列 11 条、真正改代码落地 6 条**（提示词要求「至少 8 条 / 至少落地 3 条，且 W5R2-A-03 必须在内」）。
落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例（先红后绿）、失败可以靠 `git revert` 单条回滚。

**优先级**：先做测试员点名「必须落地」的 W5R2-A-03（严重·教育正确性），再做两条舞台裁切的**本档那一半**（W5R2-A-01 / A-02 / A-05），然后是两条一般 / 建议级的玩法修正（W5R2-A-04 / A-08），最后补第 1 轮留下来的覆盖率缺口（W5-L-09）。
平台那一半（`.game-stage{overflow:hidden}` / `.l99-stage-wrap` / 壳顶栏 `inert`）按纪律**一行都不碰，交窗口1**。

（下面 A1 起是逐条记录，动完代码回填。）

## 档B · 五款（待填写）

## 档C · 五款（待填写）
