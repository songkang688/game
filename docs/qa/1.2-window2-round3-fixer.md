# 窗口 2 · 1.2 三轮验收 第 3 轮 · 监督修复员汇总

> 本文件按包追加。各包只写自己那一节，不改别人的。

## Pack A + Pack C

> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
> \+ `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` 共 10 款
> 详版：`docs/qa/1.2-window2-round3-fixer-packAC.md`
> 模型：`claude-opus-5-thinking-high-fast`

### 结论

| | 数 |
|---|---|
| 阻断 | **0 条**（本轮无输入） |
| 严重 | **2 条全部修掉** |
| 一般 | **3 条全部修掉**，另 2 条给出「仍在但可发布」的终态 |
| 交给主管 | **0 条** |

**就这 10 款，能否随本窗口一起发布：能。**

### 输入盘点（两名测试员定级不同，按「就高不就低」处理）

| 来源 | 阻断 | 严重 | 一般 |
|---|---|---|---|
| 包 A 测试员（第 1 名） | 0 | 0 | 1（`R3-PA-1`） |
| 包 A 测试员（**同岗第 2 名 · 独立复走**） | 0 | **2** | **3** |
| 包 C 测试员 | 0 | 0 | 0 |

第 1 名的 `R3-PA-1` 与第 2 名的 `R3-PA-FS-1` **是同一个根因**：第 1 名定「一般 · 可发布」，
前提是「刻意挑最差落点才逼得出来」；第 2 名的取证不支持这个前提（随机落点下第 188 关 **20 盆全中**，
人机对战里电脑还会把自己冻住 283 秒）。**修复员采纳严重定级，并且真的修掉了。**

### 已修：2 条严重

| 编号 | 款 | 症状 | 修法 |
|---|---|---|---|
| `R3-PA-FS-1` / `-FS-2` | fruit-stack | 果盆进极限环，`allSettled` 永远回不了真 —— 两条判负通路与 `stepAi` 全挂在它上面：第 132 关空跑 100 秒不出结算，人机对战电脑空转 283 秒 | `physics.ts` 三段递进兜底：① 相对速度低于 `BOUNCE_MIN_SPEED` 就不再回弹，只做位置分离；② 慢速果子每帧乘 `CREEP_KEEP` 蠕动阻尼；③ 看门狗累计 `stallMs`，超 `STALL_MS` 上强阻尼、超 `FREEZE_MS` 直接 `forceSettle`。①② 让绝大多数情形自然停稳，③ 只在前两段没拽住时出手，**正常玩法手感 / 落点 / 合成判定一点没变** |
| `R3-PA-FS-3` | fruit-stack | 双盆同帧都达标永远判朵朵赢（投放数 81:81 却是 3:0），「这一局打平」是够不着的死文案 | 判定抽成纯函数 `decideRound(bowls)`：**先把两盆收场状态都读出来再一起裁** —— 同帧双赢 / 同帧双负都判平，单盆达标按实际那一盆报赢家与原因 |

### 已修：3 条一般

| 编号 | 款 | 症状 | 修法 |
|---|---|---|---|
| `R3-PA-PS-1` | pool-stars | 结算浮层上「这一杆差一点点，换个角度再来。」**连着出现两遍**（`loseLine` 硬接鼓励语，而最常见的 reason 自己就是这一句） | 抽 `ENCOURAGE` 常量；`loseLine` 先查 reason 里有没有这句，有就只补标点 |
| `R3-PA-DM-1` | dot-maze | 无尽第一次玩就是历史最好，收场话却在说「再来一次就能刷新它」，也没有一句「新纪录」 | 抽纯函数 `endlessLine(score, before, best)`，破纪录 / 没破 / 打平三态各说各的 |
| `R3C-FIX-1` | ocean-munch | `logic.ts:225` 注释「血越少冲刺越快」—— 第 2 轮刚把 `rainbow-run` 的「掉血」统一成卸护甲口径，这一处漏网 | 改成「精神条越少冲刺越快」（**只是注释，行为零改动**）。这一条是修复员自己扫出来的，不在任何测试员单子上 |

每一条都带回归网：新增 `fruit-stack/stall.test.ts`（极限环）与 `fruit-stack/round.test.ts`（双盆裁决）
两个文件，另在 `pool-stars/levels.test.ts` `dot-maze/index.test.ts` 里加断言，**共 +29 例**。

### 未修的 2 条 · 最终结论

| 编号 | 款 | 终态 | 为什么可以发 |
|---|---|---|---|
| `R2-PA-3` | chess-garden | **仍在，一般，记录在案的例外** | 棋盘格 `.cg-sq` 360px 下 41.4px，差 2.6px。`8×44 = 352 > 331.2`，差额只能靠动**窗口 1** 的 `.screen` 内边距补（跨包），硬压会裁列或重叠。键盘通路完整、实测能不点格子走完一步杀；其余六类按钮全 ≥ 44；例外带自动过期提醒 |
| `R2C-G3` | gomoku | **仍在，一般，不阻断发布，转 1.3** | 三组 seed 共 72 局里 `smart` 只输 3 局、赢 39 局，**强弱关系没有倒挂**；和棋率 25%–47% 是 15 路满盘的样本噪声。压它必须动 `ai.ts` 评分权重 —— 第 1 轮修复员刚修好阻断的现场，红线明令不许碰。本轮学习优化员的 `P3C-3` 已做掉「不动 AI 前提下能做的那一半」 |

观察项 1 条 `R3C-G4`（gomoku 强行开禁手时 6 关无解）：解局战役把 `forbidden:false` 写死，
**玩家打不开这个开关，战役不可达**，不构成缺陷，不修。

**没有任何一条要交给主管。**

### 商标扫描：0 命中

```
扫了 167 个产品文件（本窗口 15 款，排除 *.test.*）
商标命中 0     ← 29 条商标黑名单逐条扫
红线命中 19    ← 逐条看过,全部是「自述红线」的句子(PLAN.md 与注释里写着的规矩本身、
                  以及 hop-pads 的反义句「掉下去不是死亡」),一条真违规都没有
```

唯一一处不是自述句的（`ocean-munch/logic.ts`「血越少冲刺越快」）**已改掉**。

### `destroy` 无泄漏：10 款各 20 轮全部归零

进 → 玩 → 退出 → 再进 20 轮，`window` 监听 / 根子节点 / `head` 注入样式 / 待跑定时器
一律 **0**，累计 `cancelAnimationFrame` **每轮正好 +1**（不多不少）。
另验 `destroy` 连调两次不抛、`destroy` 之后再推事件 / 再跑帧不抛，平台 `onWin` / `onLose` 保持 0。

### 改了哪些文件

```
src/games/fruit-stack/physics.ts     +49      三段兜底
src/games/fruit-stack/index.ts       +54/-16  decideRound 抽成纯函数
src/games/fruit-stack/stall.test.ts  +144     新文件
src/games/fruit-stack/round.test.ts  +72      新文件
src/games/pool-stars/levels.ts       +7/-2
src/games/pool-stars/levels.test.ts  +30
src/games/dot-maze/index.ts          +17/-4   endlessLine 抽成纯函数
src/games/dot-maze/index.test.ts     +35
src/games/ocean-munch/logic.ts       +1/-1    注释口径
```

包 B 的 5 款、窗口 1 平台文件、`src/engine/**`、`src/ui/**`、`src/styles.css`、`package.json`
**一个字节都没碰**。

### 水位

| 项 | 结果 |
|---|---|
| `npm test` | ✅ **269 文件 / 6840 例全绿**（写稿时 268 / 6831，rebase 到包 B 修复员之后的最终水位） |
| `npm run build` | ✅ 全绿（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条） |
| 修复带来的新用例 | **+29 例**（既有断言一条没删、没放宽） |
