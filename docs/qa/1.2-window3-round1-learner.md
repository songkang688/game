# 1.2 窗口 3 · 本窗三轮验收 · 第 1 轮 · 学习优化员

- 角色：窗口 3 学习优化员（第 1 轮 / 共 3 轮）
- 分支：`game-1.2-window3`（不改 `main`，不合 `game-1.2`，不 force，不开 PR）
- 模型 slug：`claude-opus-5-thinking-high-fast`
- 范围：**仅本窗口 19 款**
  `duo-rush` `duo-arena` `duo-vs-star` `sling-birds` `candy-swing` `gold-hook`
  `garden-guard` `sprout-defense` `monster-crisis` `shoot-range` `sky-squad` `tank-battle`
  `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`
- 跳过：点名五项（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`，全在窗口 2）、窗口 1 平台文件、全局第 27–29 步
- 不碰：`docs/qa/1.2-window3-round1-tester.md`、`docs/qa/1.2-window3-round1-fixer.md`、`src/ui/**`、`src/engine/**`
- 让给监督修复员：`bumper-cars/guide.ts` 的 `general` 条数上限

## 〇、开工基线

| 项 | 数 |
| --- | --- |
| `npm test` 测试文件 | 234 |
| `npm test` 用例 | 6293 |
| 结果 | 全绿 |

本轮的硬约束：**用例只增不减，既有断言不删、不改名、不调弱。**
下面每一条「不落地」的理由里，凡是写着「会调弱既有断言」的，都是真的去翻了那条断言之后才放弃的。

---

## 一、本轮清单（12 条，落地 3 条）

排序按「孩子当场能感觉到」的程度，不按实现难度。

### 1. `prince-princess` 跳跃没有土狼时间，也没有跳跃缓冲 —— **本轮落地**

- **来源思路**：平台跳跃通行的两条输入宽容（coyote time / jump buffer）。本仓库里 `puff-bros/feel.ts`
  与 `ice-fire-forest/feel.ts` 已经各自实现过一版，注释里也写清了为什么要有。
- **我们的现状**：`prince-princess/logic.ts` 的 `applyActions` 里只有一条规则 ——
  `if (input.up && !h.prevUp) { if (h.onGround) 跳 else if (h.airJumps > 0) 二段跳 }`。
  于是两种按键**全被吞掉**：
  1. 走出台沿 / 断口边缘之后的那一两帧才按跳 —— 王子 `airJumps` 恒为 0，直接掉坑；
  2. 落地前一两帧按的跳 —— 落地那一刻按键沿已经过去了，人贴着地不跳。
  这一款是本窗口唯一「有断口、有浮台、有一段跳角色」却没有输入宽容的游戏。
- **落地方式**：新建 `src/games/prince-princess/jumpFeel.ts`（纯函数 + 常量），
  在 `HeroState` 上加 `coyote` / `jumpBuf` 两个计时器，`applyVertical` 负责补充，
  `applyActions` 改成「取缓冲里的那一下跳」。公主的二段跳规则一个字不动。
- **是否落地**：是。
- **收益量化**：王子的宽容窗口从 0ms → 土狼 90ms / 缓冲 120ms；
  按 `MOVE_SPEED` 折算，走出台沿后还有 `≈ 90ms × 速度` 的距离算「还站着」。
- **受益游戏**：`prince-princess`。
- **风险与回滚**：机器人托管（`botInput`）也会吃到这层宽容，等于变强一点点，
  但 `autoPlay` 的既有断言只要求「能通关」与「两次跑步数相同」，不会因此变红；
  回滚只需把 `takeJump` 换回原来的 `input.up && !h.prevUp` 两行。

### 2. `bowling-lane` 三档电脑差的只是手，不是脑子 —— **本轮落地**

- **来源思路**：「会失误的 AI 比完美 AI 更好玩」，但**失误要失误在决策上**，
  光加高斯噪声只会让所有档位看起来像同一个人手抖程度不同。
- **我们的现状**：`aiShot()` 三档共用同一套计划 —— 满架瞄 `POCKET_AIM`、
  补中一律用 `spareAimX()`（这是专门为分瓶设计的**专家级**启发式：先找离重心最近的那一瓶，
  保证球实打实砸在瓶身上而不是穿过两瓶之间的空气）。三档唯一的差别是 `AI_WOBBLE` 的标准差。
  实测 60 格（首球 + 补中）总倒瓶数：**新手 545 / 熟练 572 / 冠军 596** ——
  单调是单调，可是一档到三档只差 9.4%，孩子选「新手球童」和「冠军球手」几乎没有体感差别。
- **落地方式**：新建 `src/games/bowling-lane/aiPlan.ts`：
  1. **补中目标按档位退化**：新手用「还站着那几瓶的重心」（就是会瞄空气的那个天真做法，
     分瓶时正好穿缝而过），熟练取重心与专家解的中点，冠军仍用 `spareAimX()`；
  2. **力度按剩瓶数**：满架要有速度才连锁得开，单瓶补中要的是控制。
     冠军按剩瓶数调力度，新手不会这一手（永远那一个力度）。
  `logic.ts` 的 `aiShot` 改成调用这份计划表，`spareAimX` / `POCKET_AIM` / `AI_WOBBLE` 全部保留原义。
- **是否落地**：是。
- **收益量化**：见第三节的实测表。最能说明问题的一项 ——
  **经典分瓶「至少碰到一瓶」的比例：83% / 85% / 100% → 37% / 56% / 99%**。
- **受益游戏**：`bowling-lane`。
- **风险与回滚**：`upgrade12.test.ts` 有一条「满架时三档都往口袋投」，所以**满架的瞄准目标一个字没动**，
  只动补中与力度；回滚就是把 `aiShot` 里的两处调用换回 `spareAimX` 与常数 0.7。

### 3. `tank-battle` 三档脾气在闯关里根本没生效 —— **本轮落地**

- **来源思路**：难度分档必须「一处定义、处处生效」，否则数据表就是装饰。
- **我们的现状**：`ai12.ts` 的 `TIER_SPECS` 明明白白写了三档的
  `think`（0.55 / 0.3 / 0.26 秒）与 `fireRange`（5 / 9 / 10 格），
  可是闯关的 `stepEnemies()` 里写死了 `t.aiTimer = 0.3` 和 `lineOfFire(w, t, 9)`，
  `enemyIntent()` 里也是写死的 `lineOfFire(w, t, 9)`。结果：
  - 「乱转」档的思考间隔和「追人」档一模一样（0.3 秒），说明里的「撞墙才换方向」名不副实；
  - 「乱转」档隔着 9 格就开火，比自己规格里的 5 格远了 80%，第一章就在远距离点射；
  - 「绕后卡位」档反而被砍到 9 格，比规格的 10 格短。
  这两个字段目前只在**对战陪练** `aiPlayerInput()` 里被读过，闯关里一次都没读。
- **落地方式**：`logic.ts` 新增 `enemyFireRange(tier)`，把 `enemyIntent()` 与 `stepEnemies()`
  里的两处 `lineOfFire(w, t, 9)` 换成按档位取值。不新增随机数调用 ——
  世界的 `rng()` 消耗顺序**一次都不变**，所以既有的确定性仿真断言不会被这条改动扰动。
- **是否落地**：**开火距离落地了；重想间隔试过之后放弃**（下面写清楚为什么）。
- **收益量化**：乱转档的开火距离 9 → 5 格（**-44%**），绕后档 9 → 10 格（+11%）。
  实测：一条空走廊上隔着 7 格对峙 30 帧，乱转档一炮不放，绕后档立刻开火。
- **`think` 那一半为什么没落地（实测留痕）**：
  把 `t.aiTimer = 0.3` 换成 `TIER_SPECS[tier].think` 之后，`sim.test.ts` 的三条
  「188 关都打得通」立刻变红 —— 第 **51 / 90 / 98** 关无头机器人打不过去（堡垒被砸 / 超时）；
  给绕后档压一条 0.3 秒的下限（wander 0.55 / chase 0.3 / flank 0.3）仍然红在同样几关；
  再把乱转档封到 0.4 秒，**第 164 关**照样守不住。
  结论：这套 188 关是围着 0.3 秒这个**整体节奏**配出来的，动它要连波次一起重配 ——
  那是关卡设计的活，不是手感调参的活。这条结论已经写进 `ENEMY_RETHINK` 的注释与
  `aiTier12.test.ts` 的一组断言里留痕，交给主管排期。
- **受益游戏**：`tank-battle`。
- **风险与回滚**：前几章会变容易一点（那正是「乱转」该有的样子），后几章的绕后档略凶；
  188 关可通过性实测未受影响（`sim.test.ts` 13 条全绿）。回滚就是把 `enemyFireRange` 换回常数 9。

### 4. `bumper-cars` 虚拟摇杆的死区没有重映射 —— 本轮不落地

- **现状**：`stickVector(dx, dy, r, dead = 0.18)` 在 `len < 0.18` 时给 0，
  过了门槛直接返回原始长度，于是「刚推出死区」这一下输出从 0 跳到 0.18，
  而且 0..0.18 这段行程被整段吃掉，慢速微调开不出来。
- **为什么不落地**：`logic.test.ts:628` 那条「虚拟摇杆有死区，推到底也只算满舵」明确断言
  `stickVector(20, 0, 40).dx ≈ 0.5`。重映射之后应当是 `(0.5-0.18)/(1-0.18) ≈ 0.39`，
  直接改这条就是**调弱既有断言**，本轮的规矩不允许。
- **建议轮次**：第 2 轮。做法是新增 `stickPush()`（死区外线性重映射到 0..1），
  `index.ts` 的摇杆改调新函数，`stickVector` 原样保留并保留原断言，新函数配自己的用例。

### 5. `gold-hook` 收绳期间的按键被静默吞掉 —— 本轮不落地（且**建议不要**做成预输入）

- **现状**：`index.ts` 里 `if (paused || phase !== "swing") return;`，
  收绳/放绳期间按下钩没有任何反馈（按钮 `disabled` 了，所以至少不是骗人的）。
- **为什么不落地**：常规的「输入缓冲」在这一款是**负优化** —— 钩子回到顶点那一瞬的摆角是任意的，
  把缓冲里的那一下在回摆瞬间兑现，等于替玩家随机扔了一钩，比吞掉更糟。
- **建议轮次**：第 2 轮改成另一件事 —— 收绳期间按键给出**看得见的「等一下」反馈**
  （按钮闪一下 + 一句提示），把「没反应」变成「有反应但现在不行」。

### 6. 连着输很多次之后没有任何「换个档位试试」的出口 —— 本轮不落地

- **现状**：本窗口有人机档位的六款（`duo-rush` `duo-arena` `duo-vs-star` `bumper-cars`
  `tank-battle` `snow-fight`）都把档位选择放在开局前，输了之后的结算文案只鼓励，不指路。
  孩子如果一开始挑了「高手」，会连着输很多把还想不到该去改档位。
- **为什么不落地**：这条要碰六款的结算 UI，本轮已有三条落地，摊子铺太开容易和
  测试员 / 监督修复员撞车。而且**绝不做偷偷改数值的动态难度** —— 那违反「不骗孩子」的口径。
- **建议轮次**：第 2 轮先在 `duo-arena` 与 `bumper-cars` 各落一处
  「连输 3 次 → 结算里显式建议降一档」的纯函数 + 文案，跑通了第 3 轮再谈铺开。

### 7. `candy-swing` 割绳判定带对手指来说偏窄 —— 本轮不落地

- **现状**：`CUT_HALF_WIDTH = 10`（逻辑像素），`index.ts` 与 `swing12.ts` 各写一份同名常量。
  手指接触面直径通常按 44px 算，鼠标是 1px；同一个 10px 判定带对两种输入并不公平。
- **为什么不落地**：`swing12.ts` 已经把 `halfWidth` 做成了参数（默认 10），
  接线只要在 `index.ts` 按 `pointerType` 传值即可 —— 但**判定带变宽会改变可通关性仿真**
  `sim.ts` 的口径，`upgrade12.test.ts` 有大量按 10px 算出来的期望值，
  必须先确认「触屏加宽」只走渲染层的那条路径。这需要一轮专门的核对。
- **建议轮次**：第 2 轮。

### 8. `sky-squad` 的「这一波躲得开吗」自检没有接到关卡表上 —— 本轮不落地

- **现状**：`bullets.ts` 里有 `aimedDodgeable(spec, playerSpeed, playerR)`，
  是一条很好的「关卡数据 + 校验器」范式，但它只在用例里被点名调用，
  关卡表 `levels.ts` 生成的每一波并没有整表跑一遍这个校验器。
- **为什么不落地**：这属于**测试基建**而不是玩法改进，第 1 轮的名额先给孩子能直接摸到的三条。
- **建议轮次**：第 3 轮收口时补一条「全 188 关每一波都过 `aimedDodgeable`」的整表断言。

### 9. `garden-guard` / `sprout-defense` 输了之后没有「你是被哪一种虫子突破的」复盘 —— 本轮不落地

- **现状**：两款都有很完整的**波次预告**（`wavePreview` / `buildWarnings`），
  但结算只给分数与鼓励，没有回头看的那一句。塔防最值钱的学习点恰恰在事后复盘。
- **为什么不落地**：要动结算数据结构，改动面比本轮预算大。
- **建议轮次**：第 2 轮（先在 `garden-guard` 试一款）。

### 10. `monster-crisis` 无敌帧只有一条视觉通道 —— 本轮不落地

- **现状**：`SPIN_INVULN = 1.35` 秒的无敌期靠闪烁表达。闪烁对「减少动态效果」用户与
  部分孩子不友好，缺一条不依赖动画的第二通道（例如一圈固定的护罩描边）。
- **为什么不落地**：纯渲染层改动，收益不如前三条直接；且要和监督修复员的 `destroy` 审计错开。
- **建议轮次**：第 3 轮。

### 11. `ice-fire-forest` 与 `puff-bros` 的手感常量各写一套，测试口径不统一 —— 本轮不落地

- **现状**：两款都有 `feel.ts`，土狼时间都是 90ms 上下，跳跃缓冲都是 120ms，
  但一个叫 `FEEL.COYOTE_MS`、一个叫 `COYOTE_TIME`（秒），用例的写法也各走各的。
  加上本轮给 `prince-princess` 新增的一套，本窗口就有三份同义常量。
- **为什么不落地**：真要统一得放进 `src/engine/**`，而**公共文件本窗口一律不碰**。
- **建议轮次**：不在本窗口做。写进第 3 轮的「交给主管」清单：
  等窗口 1 的平台层稳定后，由主管决定要不要抽一个 `src/engine/feel.ts`。

### 12. 本窗口 19 款里只有 4 款在纯逻辑层做了「帧率无关」的显式断言 —— 本轮不落地

- **现状**：`ice-fire-forest`（定步长累加器）、`puff-bros`（`substeps`）、
  `garden-guard` / `sprout-defense`（`planSteps` / `accumulateSteps`）四款把
  「30fps 与 60fps 走同样的墙上时间」写成了用例。其余各款靠 `dt` 直接积分，
  慢机器上的手感没有断言兜底。
- **为什么不落地**：逐款补属于测试员/第 3 轮收口的活，且不是「改进玩法」。
- **建议轮次**：第 3 轮挑 1–2 款补，其余列进交接清单。

---

## 二、本轮真正改的文件

| 文件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/games/prince-princess/jumpFeel.ts` | **新建**。土狼时间 90ms + 跳跃缓冲 120ms 的纯状态机；`freshJumpFeel` / `tickJumpFeel` / `noteJumpPress` / `peekJump` / `takeJump` / `consumeJump` / `coyoteSlack` | 把「什么时候算数」从 `logic.ts` 的物理里拆出来，好让单测直接盯窗口长度 |
| `src/games/prince-princess/jumpFeel.test.ts` | **新建**，20 条 | 一半验纯状态机（窗口长度、帧率无关、不重复触发），一半接进真世界验「走出台沿后按跳真的跳得起来」 |
| `src/games/prince-princess/logic.ts` | `HeroState` 加 `jump: JumpFeel`；`makeHero` 初始化；`applyActions` 的跳跃分支改成「按下沿进缓冲 → 这一帧再兑现」 | 起跳初速、公主二段跳次数、蹲跳穿浮台三条规则一个字没改，只放宽「什么时候算数」 |
| `src/games/bowling-lane/logic.ts` | 新增 `PLAN_SKILL` / `centroidAimX` / `planSpareX` / `planPower` / `spareMissBy` 与三个 `PACE_*` 常量；`aiShot` 改成走这套按档位分层的打算 | 让三档差在「脑子」上而不只是手抖幅度上。**满架仍然瞄 `POCKET_AIM`**，教学主线没动 |
| `src/games/bowling-lane/aiPlan.test.ts` | **新建**，16 条 | 验分层本身（新手=重心 / 熟练=中点 / 冠军=专家解）、力度分层、教学主线没被改掉，再真的滚瓶验档位差 |
| `src/games/tank-battle/logic.ts` | 新增 `ENEMY_RETHINK` 常量与 `enemyFireRange(tier)`；`enemyIntent()` 与 `stepEnemies()` 里两处 `lineOfFire(w, t, 9)` 改成按档位取值 | `TIER_SPECS.fireRange` 以前只在对战陪练里被读过，闯关里从来没生效 |
| `src/games/tank-battle/aiTier12.test.ts` | **新建**，9 条 | 量出三档的开火距离；顺便把「重想间隔为什么不按档位走」这条实测结论钉成断言 |
| `docs/qa/1.2-window3-round1-learner.md` | 本报告 | —— |

一个字都没碰：`src/ui/**`、`src/engine/**`、`src/styles.css`、
`bumper-cars/guide.ts`、测试员与监督修复员的报告、其他窗口的游戏目录。
本轮**没有新增任何依赖**（`package.json` 未改）。

## 三、实测数据

### 3.1 `prince-princess` 输入宽容

| 项 | 改前 | 改后 |
| --- | --- | --- |
| 走出台沿后仍算「踩着地」的时间 | 0ms | **90ms** |
| 落地前提前按的跳被记住的时间 | 0ms | **120ms** |
| 折成距离（`MOVE_SPEED = 240`） | 0px | **21.6px**（远小于关卡最窄断口，难度没被抹平） |
| 起跳初速 / 公主二段跳次数 | `PRINCE_JUMP_V 660` / 1 次 | **完全不变** |

行为对照（用例里跑的就是这两条）：

- 王子站在断口边缘 `x = 296`，先只按右走 0.05 秒（人已在半空），这时才按跳 ——
  改前 `vy > 0`（继续掉），改后 `vy < -594`（真的跳起来了）。
- 离地超过 90ms 之后再按跳，**仍然跳不起来**：宽容只有一瞬，不是凌空跳。

### 3.2 `bowling-lane` 三档档位差

同一套种子、同一条球道（`oil = 0.4`），三档各自跑：

| 指标 | 新手球童 | 熟练球手 | 冠军球手 | 一档→三档 |
| --- | --- | --- | --- | --- |
| 改前 · 十格总分（40 局均值） | 139.5 | 153.2 | 218.7 | +56.8% |
| **改后** · 十格总分（40 局均值） | **136.4** | **155.3** | **225.4** | **+65.2%** |
| 改前 · 经典分瓶碰到一瓶的比例 | 83% | 85% | 100% | +17pt |
| **改后** · 经典分瓶碰到一瓶的比例 | **37%** | **56%** | **99%** | **+62pt** |
| 改前 · 60 格总倒瓶数 | 545 | 572 | 596 | +9.4% |
| 改后 · 60 格总倒瓶数 | 542 | 562 | 595 | +9.8% |

读法：**总倒瓶数这个口径本来就分不出档位**（满架首球谁都能倒八九瓶），
所以它改前改后都只有 9% 出头 —— 这正说明为什么原来的三档「玩起来一个样」。
真正的分水岭是**分瓶**：改前三档都用专家级的 `spareAimX`，谁都碰得到；
改后新手真的会整颗球从两瓶中间的缝里穿过去（37%），冠军仍然稳稳砸在瓶身上（99%）。
分瓶用的经典组合：3-7 / 4-6 / 2-7 / 7-10 / 6-7-10，每组 20 个种子，共 100 球。

### 3.3 `tank-battle` 三档开火距离

| 档 | 规格 `fireRange` | 改前实际 | 改后实际 |
| --- | --- | --- | --- |
| 乱转 | 5 | 9（远了 **80%**） | **5** |
| 追人 | 9 | 9 | 9 |
| 绕后卡位 | 10 | 9（短了 10%） | **10** |

一条 13 格空走廊上正面对峙 30 帧的实测：

| 间距 | 乱转 | 追人 | 绕后卡位 |
| --- | --- | --- | --- |
| 4 格 | 开火 | 开火 | 开火 |
| 7 格 | **不开火**（改前开火） | 开火 | 开火 |
| 10.2 格（折成射程 ≈ 9.35） | 不开火 | 不开火 | **开火**（改前不开火） |

188 关可通过性：`sim.test.ts` 13 条**全绿**，`rng()` 消耗顺序一次都没变。

### 3.4 性能

本轮三条改动都是**决策层**的，没有动渲染、没有新增每帧分配：

- `jumpFeel` 每位主角各持有一个常驻对象（两个 `number` 字段），
  `tickJumpFeel` 原地改值不新建对象，每帧新增分配 **0 次**；
- `planSpareX` / `planPower` 只在电脑投球那一刻各跑一次（一局最多 21 次），
  循环上界是常数 10；
- `enemyFireRange` 是一次 record 取值，替换掉的是同一处的字面量 9。

`npm run build` 产物与改动前同为 **140 个预缓存条目 / 3238.78 KiB**，主 chunk 无变化 ——
这三条改动**不该**也确实没有影响体积。（本轮没有性能类改进，所以不列 fps 数据；
第 12 条「帧率无关断言」留到第 3 轮。）

## 四、验收

| 项 | 值 |
| --- | --- |
| `npm test`（本人三条落地全部接上之后） | **241 文件 / 6359 用例 全绿** |
| `npm test`（与测试员 / 监督修复员的**三方合并态**复验，SHA `6730650`） | **241 文件 / 6386 用例 全绿** |
| `npm run build` | 通过（`tsc --noEmit` + vite 均无错） |
| 本轮新增用例 | **45**（`prince-princess` 20 + `bowling-lane` 16 + `tank-battle` 9） |
| 删除 / 改名 / 调弱的既有断言 | **0** |
| 新增运行时依赖 | **0**（`package.json` 未改） |
| 存档 key | 未新增、未修改 |
| 商标黑名单扫描 | 0 命中（新增的三份文件与两处 diff 逐词过了一遍） |
| 分级红线 | 无血 / 无死亡 / 无伤害语义；新增文案只有代码注释与用例名，全部面向「怎么玩得更顺」 |

### 起止水位

| 时点 | 文件 | 用例 | 其中本人新增 |
| --- | --- | --- | --- |
| 拿到分支时（`1fbf175`） | 234 | 6293 | —— |
| 落地 1 之后（`prince-princess`） | 239 | 6330 | +20 |
| 落地 2 之后（`bowling-lane`） | 240 | 6346 | +16 |
| 落地 3 之后（`tank-battle`） | 241 | 6359 | +9 |

> 同一条分支上测试员与监督修复员在并行推进，每次 rebase 都会把他们的用例一起带进来，
> 所以上表相邻两行的差**不全是**本人的。用例只增不减这一条按**本人的净增量 +45** 核：
> `prince-princess` 20 + `bowling-lane` 16 + `tank-battle` 9，删除 / 调弱 0 条。

### 交给下一轮 / 交给主管

- 第 2 轮学习优化员接手：清单第 4、5、6、7、9 条（`bumper-cars` 摇杆重映射、
  `gold-hook` 收绳反馈、连败降档建议、`candy-swing` 触屏判定带、`garden-guard` 复盘提示）。
- 第 3 轮收口：清单第 8、10、12 条。
- **交给主管**（本窗口做不了）：
  1. 第 11 条 —— 三份同义的手感常量要不要抽进 `src/engine/feel.ts`，公共文件不归本窗口；
  2. `tank-battle` 的 188 关只在 0.3 秒这个整体 AI 节奏下成立，
     想让三档的 `think` 在闯关里也生效，得连波次一起重配。
