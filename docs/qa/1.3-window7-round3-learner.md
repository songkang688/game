# 1.3 窗口 7 · 第 3 轮（终验）视觉验收 · 学习优化员报告（B 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 3 轮终验学习优化员（B 档）。只写本报告 + `docs/qa/1.3-window7-style-baseline.md` + 本窗九款**纯文案**（guide / blurb / 提示语）+ 文案快照测试；绘制代码一行不碰；A 档 / C 档报告一字不改。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。
> 输入（可读不可改）：前两轮六份报告 + 本轮 A 档 `round3-tester`（已落地：放行，54/54 达标，新问题 0）。

## 〇、终验计划（动手前提交）

1. **采纳率终账**（第一节）：把第 1 轮 TOP10 + 三条可选建议 + 第 2 轮修订清单 8 条做最终对账——采纳 / 部分采纳 / 未采纳各几条；未采纳的逐条写「同意放弃」或「升级为终验条件」（后者交 A 档并入结论）。对照 A 档本轮对账表（已修 5 / 降级 8 / 升级阻断 0），不采信报告自述，逐条重读代码现状。
2. **风格基线固化**（第二节 + 独立文件）：三轮验收沉淀的视觉规范整理成 `docs/qa/1.3-window7-style-baseline.md`——粉彩色板 token 表、描边宽度档位、左上 45° 光照约定、动效三段式与 reduced 退化规则、金币三件套规格、双人剪影差异化手法、2.5D 纵深做法（本窗以 poop-hero 视差为主）。每条规范注明出自哪一款的哪个函数（文件 + 函数名）可溯源；只覆盖本窗九款，不用全库 `1.3-style-baseline.md` 文件名避免抢文件。
3. **商标终审**（第三节）：两轮维护的「题材 → 易踩线对象 → 规避要点」清单做最后一次本窗对照（含 R2 持续盯防项逐条源码复核），给出「商标风险清零」或逐条残留说明。
4. **本窗整体感终评**（第四节）：九款入口卡片（meta 名称 / blurb / 主题色）+ 场内视觉语汇放一起看最后一遍，确认第 2 轮点名的最违和款统一化后归队；仍违和的点名交 C 档当轮处理。不评外窗 39 款首页。
5. **文案终查**（第五节，唯一动代码的范围）：① 两轮 24 处文案改动有没有被 rebase 冲掉（copyW7r1/copyW7r2 快照复跑 + 逐句 grep 复核，丢了补回）；② 文案里提到的视觉亮点与终态画面是否一致（对照互证用例 + 终态源码）；③ 九款 guide / blurb 错别字与语气分级（只鼓励、无恐吓词汇）过一遍；每处改动登记进报告并补快照测试。
6. 收尾：fetch → rebase（保留他人文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`、前两轮与本轮 A 档报告）→ `npm test` / `npm run build` 全绿 → 普通 push `origin game-1.3-window7`。**不 force、不改 `main`、不动 `game-1.2`、不用 `gh` 开 PR。**

学习红线（三轮一致，最后重申）：只学**画面密度**与**角色剪影**两件事，只记文字结论；不下载、不截图入库、不描摹任何具体造型；商标与官方角色连「像」都不许像。

## 一、两轮建议采纳率终账

口径：两轮全部**不重复**建议 = R1 TOP10（10 条）+ R1 可选 3 条 + R2 新增 5 条 = **18 条独立建议**（R2 修订清单第 5–7 条是 R1 可选三条的顺延重排，不重复计）。全部逐条重读代码终态与 commit diff 定案，不采信报告自述。

### 1.1 R1 TOP10（终态复核）

R2 已对账 10/10 采纳（逐字 4 · 变体 2 · 超范围增强 4）；本轮终验复核**全部仍在位**：10 条对应的实现（`drawScentStar` / `HERO_VIS` 双通道 / `drawBinIcon` / `drawTrashItem` 18 款 / `botFaceSvg` / `drawKingCrown`+珠色 / kit `pattern.ts` 五渲染路径 / emoji 六处清场+`emoji()` 连根删 / 三款 canvas 小字 / `CatLook.bow` 蝴蝶结）逐一核到源码，对应守门用例 25 个文件全部在跑（A 档 R3 `qaW7r3` 已把存活性钉成断言）。**终账：采纳 10 / 部分采纳 0 / 未采纳 0，零回退。**

### 1.2 R1 可选 3 条 + R2 新增 5 条（终态定案）

| # | 建议 | 终态 | 采纳结论 | 未采纳处置（同意放弃 / 升级终验条件） |
| --- | --- | --- | --- | --- |
| R2-③ | F1/F2/F3 维持 C 档计划原样（缎带图形化 / 开场卡 ≥14px / 菜单 13px） | 三条全落地（`3a73660` / `9d4ccff` / `519bcb3`），R3 A 档终验判「已修复」 | **采纳** | — |
| R2-① | F4 扩容：fruit-slice 两份 ±255 加法 `shade` 收编 kit 单源，至少钉量纲守门 | C 档 F6（`7fc6da2`）按我方最低要求落地量纲守门 2 条（`src/games/qaW7r2Fix.test.ts`）；实现收编因数学不等价（加法平移 vs 比例混合，收编=改实际渲染色）顺延「shade 全库归一」专项 | **部分采纳** | 未落地部分**同意放弃**（本窗内）——backlog #5 已登记去向，且 B/C/A 三方书面理由一致 |
| R2-② | 墨色三支收敛（snake 场景件棕描边 / memory `mcShadow` / landlord `46,26,60`） | landlord 支 F5 落地（`c7f91e7`）；snake 支被 C 档纠偏、R3 A 档复核成立——点名两色实为刺猬五官 / 苹果梗 / 饼干豆的**物件本色**而非场景收边，我方本轮重读 `visual13.ts:473-550` 后**认可纠偏**（判据有误，记入方法论）；memory 支被 `visual21.test.ts` 规格断言逐字钉死，跨窗协调成本大于改动，登记 backlog #6 | **部分采纳**（三支中 1 落地 / 1 点名有误结案 / 1 登记） | snake 支**同意放弃（点名有误，结案）**；memory 支**同意放弃**（本窗内）——backlog #6 交跨窗规格协调 |
| R2-④ | kit `GOLD_EDGE` 金描边 token（自标下轮可选） | 未立项；backlog #7 登记「新画金物统一 `#C7861F`、存量不回改」；基线文档 1-7 条已把该约定写进规范 | **未采纳** | **同意放弃**（本窗内）——终验轮不引入新风格资产是对的；规范先行进基线，token 化留后续窗口 |
| R2-⑧ | N-3 样式表 <14px 84 处（宽口径 86）跨窗专项 | 全部 1.1/1.2 遗留（R3 A 档复点量级一致、landlord −1 即 F1 可交叉验证）；backlog #4 交「360px 阅读性」专项 | **未采纳**（跨窗不立项，与建议本意一致） | **同意放弃**（本窗内）——qaW7r3 已钉「遗留面不许扩大」（1.3 新增小字 0、低封顶 R3 后全窗清零 `d673e05`） |
| R1-可选① | fruit-slice 果渍留底残留通道 | 三轮未立项；backlog #8 | **未采纳** | **同意放弃**——可选增强非缺陷；规格（事件残留、自灭、≤6 硬顶、reduced 不生成）已沉淀进基线 8-2 条口径 |
| R1-可选② | memory-cards 四角角饰 | 三轮未立项；backlog #9 | **未采纳** | **同意放弃**——落地时须守基线 8-1 条对比度上限（压在卡背之下） |
| R1-可选③ | poop-hero 近景前景层（0.8–0.9 档） | 三轮未立项；backlog #10；2.5D 专项现三档已达标（A 档 54/54） | **未采纳** | **同意放弃**——锦上添花项，落地须配「玩法层可读性不降」用例（基线 7-7 条） |

**终账统计：18 条 = 采纳 11（TOP10 10 + R2-③）· 部分采纳 2（R2-① / R2-②）· 未采纳 5（R2-④ / R2-⑧ / R1-可选①②③）。未采纳与部分未落地共 7 项处置全部为「同意放弃（本窗内）」，各有 backlog（`docs/qa/1.3-window7-backlog.md` #4–#10）书面去向；「升级为终验条件」0 条——与 A 档「放行不附带条件」的终验结论一致，无需 A 档并入。**

方法论终结（三轮学习成果收口）：① 给修复方的规格**钉「量纲 + 验收线」最有效**（Δ≥25 / 不吃门槛 / ≥14px——三轮全部按线落地），**钉具体色值与挂载对象最脆**（两处被更优变体推翻、一处点名判据与代码用途不符）；② 建议分「治理项 / 加分项」两档排序，治理项（token 收敛、字号地板）三轮全清，加分项（果渍 / 角饰 / 前景层）三轮零落地——**验收窗口里加分项就不该占 C 档带宽**，直接入 backlog 是正确路径。

## 二、风格基线固化（正文单独成文：`docs/qa/1.3-window7-style-baseline.md`）

编写说明：基线由三轮验收的**已验证事实**沉淀——只收「代码里真的这么写、且有用例钉着」的规范，不收愿望；每条注明文件 + 函数 / 常量名，共 **8 节 / 37 条**，只覆盖本窗九款与本窗归属 kit 文件（不用全库 `1.3-style-baseline.md` 文件名，避免与全局步骤抢文件）。要点摘录：

1. **粉彩色板 token 表**（8 条）：主粉 `#F4859F` 七处消费点、主金 `#F0C25A` 六处同值（kit `JOKER_GOLD` = landlord `--ld-gold` = memory `mcBackGold` = fishing `fshRare` = poop-hero `phBelt` = fruit-slice `gold`）、深影紫灰家族 `rgba(90,74,110,α)` 三档 α；**登记的例外**（fruit kit 暖棕落影 / memory `mcShadow` / 金物收边四家 / fruit-slice 加法量纲 shade）全部写明白,防后来者当成可模仿样例。
2. **描边宽度档位**（5 条）：1.5px 基准（`FRUIT_OUTLINE_PX` / `ICON_STROKE_PX` / `PATTERN_STROKE`）、随径放大不缩小（`max(1.5, r*k)`）、道具加厚 1.7px、场景件 3px+1px 双层、描边取色两法（同色加深 / 紫灰家族）**不发明第三种**。
3. **左上 45° 光照约定**（5 条）：径向亮心 −0.3r~−0.35r、线性 (0,0)→(1,1)、高光白 50–55% 左上一粒、落影偏右下、鱼类「背深腹浅」同源表达。
4. **动效三段式与 reduced 退化**（8 + 3 条）：三段式 = 预备 → 爆发 → 回落,反馈 120–300ms 自灭 / 氛围通道慢一个数量级（1.6–6.8s）,九款逐款给了三段实例与时长常量;reduced 三规则 = 纯装饰不生成、**功能反馈保留静态满位移**（浮标 3px / 接缝白光 / 星屑环静态）、状态切换一帧到位。
5. **金币三件套规格**（5 条）：左上高光 +18%、侧缘暗面 −14%（描边 −34%）、落地影 0.8–1.05×0.2–0.3 倍**贴地才画**;金物专档三停 `#FFF6C4→#F0C25A 族→#C7861F 族`;细节降级门槛五款五值（18 / 15 / 48 / 48 / 40px）只砍细节不砍三件套。
6. **双人剪影差异化**（6 条）：四通道优先级形状 > 附件 > 亮度 Δ≥25 > 色相（16px 验收色相记 0）;附件画进轮廓本体、不吃降级门槛（星星发卡 / 蝴蝶结两例）;验收断言钉量纲不钉色值。
7. **2.5D 纵深**（7 条）：poop-hero 三档视差 0.16 / 0.45 / 1.0 + 层间 α 0.4 / 0.8 / 1.0 双线索 + `PH_LAYERS` 图层序钉死;视点不动的款不加层,垂钓走 `depthFade` 深度变色;可选第四档入 backlog。
8. **附三条横向口径**：牌桌「少而精」数值线、事件残留通道、画布字号 ≥14px 地板与 emoji 口径。

## 三、商标终审结论

> **商标风险清零（本窗九款），残留 0 条。**

依据（全部本轮重验,不采信历史报告）：

1. **机器面**：A 档 R3 `qaW7r3.test.ts` 商标黑名单**全源码**扫描（九款 + kit,扫描面大于 copyW7r1/r2 的新增文案面）0 命中且在跑;本轮文案两处修正过 `BRAND_WORDS` / `BABY_TALK_WORDS` 黑名单断言（`copyW7r3.test.ts`）。
2. **造型面（R2 持续盯防项逐条源码复核,全部在位）**：① pattern 十主题仍是七原语（`motifSvg` 五种贴纸 + 光斑盘 + 三型地形带）,零新形状、无多尖塔剪影;② `botFaceSvg` 团团**有嘴**（`M11 17.7q1 1 2 0`,规避米菲 X 嘴布局）、圆圆眼周深块**带白瞳孔高光且有嘴**（`r=.8` 白圆 ×2,不做无嘴呆脸）;③ 星星发卡纯几何五角星（`#F5C542`）,无缎带穗子无变身光效;④ 果王皇冠珠 r = 冠宽 0.07 维持,远离「三圆组合」剪影;⑤ 蝴蝶结深梅 `#9B4E86` 未改红（红结+白脸是 Kitty 强特征、绿身红头是绘本强特征,双双避开）;⑥ trashArt 18 款无品牌贴纸、无 logo 字样。
3. **清单面**：R1 九行「题材 → 易踩线对象 → 规避要点」清单（Fruit Ninja / 超人系 / 欢乐斗地主 / Kitty·轻松熊·Line / 贪吃蛇大作战·好饿的毛毛虫 / 捕鱼达人·动森 / 迪士尼·皮克斯 / Chiquita / 小新·小丸子）维持有效,本窗三轮新画素材（pattern 场景画 / botFace / 皇冠 / 发卡 / 蝴蝶结 / trashArt / 徽记）**全部能回答「这是我们自己的造型」**。

后续维护提醒（非残留）：给 pattern 加新主题必须过「七原语之外零新形状」底线（`pattern.test` 可钉）;botFace 调表情保住「有嘴、有瞳孔高光」;蝴蝶结永不改正红。

## 四、本窗整体感终评

九款入口卡片并排终看（`meta.ts` 的 title / emoji / color / blurb）+ 场内视觉语汇四通道复核:

1. **入口卡**：九款底色全部低饱和粉彩（奶油黄 / 杏黄 / 淡绿 / 杏粉 / 淡紫蓝 / 淡蓝 ×2 / 淡粉 / 奶咖）,色相互不撞、饱和度同档;blurb 九条全部「玩法事实 + 1.3 视觉卖点」双段式;**本轮把 puzzle-tiles blurb 的全角标点半角化后,九卡标点终于全齐**（此前它是唯一全角逗号 / 叹号款,详见第五节）。
2. **场内语汇**：形状语汇 kit 单源（fruit / caterpillar / icons / pattern / jigsaw / fishArt / cardArt 七件）、描边 1.5px 档位、左上 45° 光照、反馈动效 120–300ms 带——四通道九款全对齐（基线文档第一至四节逐条溯源）。
3. **R2 点名 5 款的归队确认**：① snake-snack「墨色孤例」——点名纠偏结案（物件本色,非违和）;② memory-cards「界面感 > 世界感」——`mcShadow` 与四角角饰入 backlog #6/#9,但卡阵单位精致（双环 + 金线 + 徽章 + 印章收纳）达标、A 档六专项全过,**已归队**,剩余是打磨不是违和;③ fruit-slice shade 双份——F6 守门钉死,渲染色终态无漂移;④ landlord 缎带 + 深影——F1/F5 已修,`LDV_CSS` 小字清零、深影单家族;⑤ fishing-star 金边散——四家灰度接近,九款并排金物质感已齐,token 化入 backlog #7。
4. **终评结论**：**九款整体感达标,仍违和款 0,无需点名交本轮 C 档**（C 档本轮亦无新增点名输入,与 A 档「新问题 0」互证）。不评外窗 39 款首页。

## 五、文案终查改动清单（B 档唯一动代码的范围）

终查三件事的结果:

1. **两轮文案完整性**：R1 16 处 + R2 8 处共 24 处文案**零丢失**——`copyW7r1.test.ts` + `copyW7r2.test.ts` 合计 49 条用例本轮复跑全绿,另逐句 grep 复核无被 rebase 冲掉。
2. **文案与终态画面一致性**：24 处文案提到的视觉亮点逐条对照终态源码——乌云/辣椒剪影（`drawNaughtyCloud`/`drawFcChili`）、六果剪影与切面、双虫绿粉镜像（`SS_WORM_GREEN/PINK`）、图标形色双记、接缝白光（`dropFxClasses` reduced 保留）、场景画光斑坡带与干扰块换色（`THEMES`/`altSpec`）、星星印章（`.mmc-gone::after`）、金银王框与暖冷花色（`--ld-gold/--ld-silver`）、团团圆圆耳形（`botFaceSvg`）、浮标点头+涟漪加密（`bobberNodMs/rippleDense`）、披风认人+星星发卡+香香星柔光（`HERO_VIS`/`drawScentStar`）、三套街景、果王珠色三分（`kingBeadColor`）——**全部一致,零吹牛**（互证用例在跑）。
3. **错别字与语气分级**：九款 guide 全文 + 九条 blurb 通读——语气分级全过（失败文案全鼓励:「撞到了也只是这一局结束」「一竿失手不要紧」「投错不扣分,换个桶再投一次就好」;零恐吓词汇);错别字与标点查出 **2 处**,均已修:

| # | 文件 | 改动 | 理由 | commit |
| --- | --- | --- | --- | --- |
| 1 | `fishing-star/meta.ts` | blurb「水下咬钩**收杆**」→「水下咬钩**收竿**」 | 同一句前半「抛竿」用竿、后半用杆,字形不一致;且本款 guide 写的是「完美收竿」——用户可见文案统一成「竿」(钓竿的规范字)。`index.ts` 注释与 `PLAN-1.2.md` 里的「收杆」是非用户可见文本,不在 B 档权限内,不动 | `34c059f`(rebase 后 `66be861`) |
| 2 | `puzzle-tiles/meta.ts` | blurb 全角「，！」→ 半角「,!」 | 九款入口卡里唯一一处全角逗号/叹号(1.2 遗留),首页九卡并排标点不齐;R1/R2 钉住的两个短语不含标点,改动零冲突 | 同上 |

配套新增用例:`src/games/copyW7r3.test.ts`(3 describe / 6 it):收竿字形钉死(含「收杆」清零与 guide「完美收竿」保留)、puzzle blurb 全角标点清零 + 钉住短语保留、**九款 blurb 标点半角统一聚合断言**(防后续新文案再混进全角逗号/叹号)、修后文案过商标/低幼黑名单双保险。guide 文件本轮 0 改动(终查未发现问题,不为改而改)。

## 六、自验结果

| 项目 | 结果 |
| --- | --- |
| `npm test`(含本轮新增 copyW7r3,合并态含 A 档 qaW7r3 + C 档 R3 修复) | ✅ **744 文件 / 15470 用例全绿**,178.5s。用例只增不减:R3 A 档收尾 **742/15443** → 本档 +1 文件(copyW7r3,6 it) → C 档 R3 +1 文件(+21 it,d673e05 配套) → **744/15470** |
| `npm run build`(tsc --noEmit + vite build) | ✅ 全绿,649ms;PWA 预缓存 **190 项 / 5250.55 KiB**(对 R2 合并态 5250.39 KiB 仅 +0.16 KiB,本档 0 行绘制改动) |
| 分支纪律 | 计划先行提交(`f7616a4`);文案两处 + copyW7r3 一个 commit、报告一至五节 + 风格基线一个 commit;期间三次 fetch → rebase 配合 C 档并行推送(保留他人全部文件,尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`、前两轮六份与本轮 A/C 档报告);全程普通 push `origin game-1.3-window7`,未 force、未动 `main`/`game-1.2`、未开 PR |
| 改动面 | 仅 `docs/qa/1.3-window7-round3-learner.md` + `docs/qa/1.3-window7-style-baseline.md` + 两处纯文案(fishing-star / puzzle-tiles 各 1 处 blurb)+ `src/games/copyW7r3.test.ts`;**绘制代码 0 行、玩法 0 行,A 档 / C 档报告 0 字** |
| 两轮文案完整性 | 24 处零丢失(copyW7r1 + copyW7r2 共 49 条用例合并态全绿) |
| 学习红线 | 三轮全部学习结论只以文字记录;未下载、未截图入库、未描摹任何具体造型 |
| 合并态复验 | 三方合并态复验与合并态 SHA 由本轮 C 档收尾执行并写入其报告;本档已完成收尾 rebase 配合,并在含 A 档 qaW7r3、C 档 d673e05 与本档全部提交的合并态上自跑 test/build 全绿(上表) |
