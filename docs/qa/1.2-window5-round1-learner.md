# 窗口5 · 第1轮学习优化员

> 本文件由三名学习优化员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏；平台文件（`src/ui/**`、`src/engine/**`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`）与 76 款全集口径都不在本轮范围内。
> 原计划第 27–29 步的全局三人组已取消，本文只按窗口 5 的口径写。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · 第 1 轮 · 档A 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`ba90919`（对齐 `origin/game-1.2-window5`），`npm test` 194 文件 / 5379 用例全绿，`npm run build` 全绿。
**输入**：`docs/qa/1.2-window5-round1-tester.md` 的档A 节（阻断 0 / 严重 0 / 一般 5 / 建议 2），以及 `docs/plan-1.2-step27-B-learner.md` 的方法论（数字与范围按本窗口提示词覆盖）。

**权限边界**：只改 `src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**` 与本文件。不改测试员报告，不改平台文件，不改本档以外的游戏，不引入任何外部运行时依赖，CSS 前缀沿用各款已有前缀，用例总数只增不减。

**这一轮列 10 条、落地 5 条**。落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例、失败可以靠 `git revert` 单条回滚。

### A1 · 十条改进逐条

---

#### W5-L-01 · `clock-house` 题面钟面的读屏标签不许把答案念出来 —— ✅ 本轮落地

- **来源思路**：无障碍工程里一条通行的规矩——**装饰性图形给空标签，信息性图形给等价描述，但「题目本身」不能在标签里把答案说了**。同一件事在题库类开源项目里通常靠一条「题面渲染出来的任何文本表示都不得包含 `answer`」的守门用例钉住。测试员的 W5-A-01 就是这条规矩的一个反例。
- **现状**：`levels.ts` 的老钟面 `clockSVG()` 把 `aria-label` 写死成 `formatClock(hour, quarter)`，`clockface.ts` 的新钟面 `faceSVG()` 默认 `label = formatClockMinute(time)`。于是「钟面上是几点？」这道题，题面那张 SVG 的标签就是「4 点」，而三个选项正是「6 点 / 4 点 / 2 点」。作者其实清楚这个风险——`qSetMin()` 给**选项**钟面显式传了 `label: "钟面"`——只是**题面**漏了。
- **落地方式**：把两个渲染器的默认标签一起改成 fail-closed：新导出常量 `FACE_LABEL = "钟面，自己读一读"`，`clockSVG()` 多一个 `label` 形参（默认 `FACE_LABEL`），`faceSVG()` 的 `opts.label` 默认值从时刻改成 `FACE_LABEL`；选钟面题（`qSet`）的三张选项统一显式给「钟面」；顺手给老 `clockSVG()` 补了缺的 `role="img"`。**拨一拨那个练手钟面照旧实时念读数**——它由 `dial.ts` 在拖动时改写标签，念的是「孩子现在把针拨到了哪」，不是答案，属于必要反馈。
- **量化**：`read` 413 道 + `readMin` 33 道 = **446 道**题的题面标签不再泄答案（这个数是我在本轮遍历 188 关重新点出来的，和测试员的 446 对得上）；选钟面题另有 96×3 张选项钟面一并收口。`clock-house` chunk 29.13 → **29.18 kB**（+0.05 kB）。
- **受益游戏 id**：`clock-house`。
- **守门用例**：新增 `src/games/clock-house/a11yLabels.test.ts`（4 条）——188 关题面与选项的每一个 `aria-label`①不许含数字、②不许包含本题答案；两个渲染器的默认标签与显式传参行为；`role="img"` 在位。另有一条自检「扫到的标签数 > 400」，防止将来标签被整体删光后这条用例空转变绿。
- **既有断言被迫修改（单独解释）**：`levels.test.ts` 有一条 `LEGACY_DIGEST` 摘要锁，钉「前 99 关一个字节都没被 1.2 碰过」。改了钟面的读屏属性以后它会红。**我没有换摘要值放水**，改的是比对方式：先把标签按钟面自己的 `data-h` / `data-q` 还原成 1.1 的写法、去掉新加的 `role`，还原完的字节流仍然必须等于**原来那串** `5a314f…296cf`。也就是说这条锁现在钉的是「除了那个读屏属性，前 99 关没有第二处改动」，比原来更明确。同时另补一条「前 99 关的钟面确实换上了不含时刻的标签（扫到 >300 个）」，两条一正一反。
- **风险与回滚**：风险是读屏用户失去了「不读钟也知道现在几点」的捷径——但这正是这道题要考的东西，且题面另有 `ask`（「钟面上是几点？」）与三个可读选项，题仍然做得下去。回滚：`git revert 3bc5fcf`，单条独立。

---

#### W5-L-02 · `word-garden` 题干不许写着要填的那个字 —— ✅ 本轮落地

- **来源思路**：题库工程里的「自指检查」——**题面（含释义、义项、提示词）不得包含 `answer` 的任何字面**。现有的「答案唯一」类用例只管三个选项之间的关系，管不到题干，是个常见盲区。测试员的 W5-A-02 与 A5 节点名的守门缺口就是这条。
- **现状**：两处自指。① 三张成语卡的 `meaning` 里带着被挖掉的那个字——「兴高采烈」挖「高」而释义写「**高**兴得神气十足」、「一心一意」挖「心」而释义写「**心**思全在一件事上」、「春暖花开」挖「暖」而释义写「天气转**暖**花都开了」；② 偏旁题反过来问「哪个字和「树木」有关？」时，答案可能正好就是义项词里那个「树」。
- **落地方式**：
  - 成语卡换三条不含答案字的说法：「只认准一件事去做」/「情绪很好、很神气」/「天气回温，花都开了」（都在既有用例要求的 ≤10 字以内，且互相不撞）；`IdiomCard.meaning` 的注释里把这条规矩写死。
  - 偏旁题不改文案改生成器：`logic.ts` 新增纯函数 `radicalTargets(card)`，**把义项词里出现过的字从候选答案里剔掉**，剔干净了一个都不剩就退回全表（数据写坏时宁可弱一点也要出得了题）。`levels.ts` 的 `qRadical()` 从 `pick(rand, card.chars)` 改成 `pick(rand, radicalTargets(card))`——`pick` 无论数组多长都只消耗一次随机数，**题库的确定性生成序列不受影响**。
- **量化**：成语这一路修掉 **21 道**白送题（8 + 8 + 5，与测试员的统计逐条对得上）。偏旁这一路测试员点名了 2 张卡（`木/树木` → 树、`讠/说话` → 说），本轮实际堵上的是 **4 张**——顺带扫出 `艹/花草植物` 里的「花」「草」和 `目/眼睛` 里的「眼」「睛」，同样是义项词自己写着答案。四张卡剔完各自还剩 4–5 个字可问，出题面不变窄。`word-garden` chunk 67.28 → **67.38 kB**（+0.10 kB）。
- **受益游戏 id**：`word-garden`。
- **守门用例**：新增 `src/games/word-garden/noSpoiler.test.ts`（3 条）——成语卡释义不含挖空字；偏旁卡剔完仍剩 ≥3 个字且候选一个都不在义项词里（带「确实有 ≥2 张卡被剔过」的自检，防空转）；188 关每一道成语题与偏旁题的 `ask` 都不含正确答案（覆盖 195 道，断言 `checked > 150`）。近反义 / 多音字 / 看字选意思那几类题干点名的是**被问的那个词**、不是答案，属玩法本身，明确不在这条线里。
- **风险与回滚**：唯一的内容风险是新释义与相邻成语的释义撞车（「一心一意」和「专心致志」本来就近），我挑词时避开了原来的「一门心思」句式；`bank.test.ts` 既有的「成语题答案唯一」与 `levels188.test.ts` 的「答案能从字词库反查」两条全绿，说明没撞。回滚：`git revert e738590`。

---

#### W5-L-03 · `landlord-cards` / `red-blue-race` 的模式入口与让分开关补到 44px 热区 —— ✅ 本轮落地

- **来源思路**：移动端触摸目标 ≥44×44 是通行口径。更值钱的不是改这几个数，而是**把口径写成用例**——同窗另三款（`clock-house` / `word-garden` / `color-fun`）在 360px 下一个小于 44px 的热区都没有，说明这是本库做得到的水位，那就该有东西钉着不许掉下来。
- **现状**（测试员 360px 真机实测）：`.ld-open` 的「♾️ 无尽连胜」191×**38**、「⚔️ 双人对战」115×**38**；`.rbe-open` 的「🤝 对战场」210×**40**、「♾️ 跑不完的跑道」229×**40**；`.rbr-chip-btn` 的「🤝 让分」158×**30**——最后这颗还是攻略点名推荐给大人用的，偏偏最容易点歪。
- **落地方式**：`.ld-open` 与 `.rbe-open` 各加 `min-height:44px` 并改成 `inline-flex` + 上下左右居中（撑高之后不居中会上下留白不对称）；`.rbr-chip-btn` 的 `min-height` 从 30 提到 44。**只动尺寸，配色、圆角、阴影、按下位移一个都没碰**，CSS 前缀沿用各款已有的 `ld-` / `rbr-` / `rbe-`，没有新造类名（`landlord-cards` 有一条「1.2 新增块里只许用 `ldc-` 前缀」的用例，我改的是那一块之外的既有规则，不受影响）。
- **量化**：本档五款在 360px 下的 <44px 热区从 **5 个降到 0 个**。`landlord-cards` chunk 54.47 → **54.71 kB**（+0.24 kB），`red-blue-race` 45.84 → **46.16 kB**（+0.32 kB）。
- **受益游戏 id**：`landlord-cards`、`red-blue-race`。
- **守门用例**：`landlord-cards/shell.test.ts` 既有的「热区」describe 里补一条（模式入口 `min-height ≥ 44` 且文字居中）；新增 `src/games/red-blue-race/touch.test.ts`（3 条）——让分开关 ≥44、模式入口 ≥44 且居中，外加一条回归「返回键与结算按钮这次没有被顺手改小」。
- **风险与回滚**：视觉上这几颗胶囊高了 4–14px，`.ld-bar` / `.rbe-bar` / `.rbr-gear` 三条都是 `flex-wrap: wrap` 的自适应行，不会顶出布局；360px 下测试员原本就量到零横向溢出，加高不改宽。回滚：`git revert d380045`。

---

#### W5-L-04 · `color-fun` 自由画室要把背后的选关地图关进 `inert` 并接管焦点 —— ✅ 本轮落地

- **来源思路**：模态浮层的标准三件套——**背景 `inert` + `aria-hidden`、打开时焦点进浮层、关闭时焦点回到触发它的那颗按钮**。仓库自己的平台层其实已经有这套（`src/ui/dialogs.ts` 的焦点陷阱、`a11y.test.ts` 的「Esc 关闭、关闭后焦点归位」），画室是关卡外自建的一层纸，没吃到。
- **现状**：画室是 `position:absolute; inset:0; z-index:9` 的 `.clf-sheet`，视觉上盖满了 host，可底下的模式条与选关地图 `display:block`、没有 `inert`、没有 `aria-hidden`，**11 个看不见的控件仍然可聚焦**。测试员实测 Tab 要先走 20 站才轮到「✖ 关上画室」，中途还可能误触「开始冒险」直接跳出画室。
- **落地方式**：`sandboxUi.ts` 新增纯函数 `muteBehind(targets)`——给每个节点挂 `inert` + `aria-hidden="true"`，并**按原值**返回一个还原函数（本来就写着这两个属性的节点不会被顺手擦掉）。`openSandbox()` 里把 `host` 除画室以外的孩子交给它；开画室前记下 `doc.activeElement`，开完把焦点直接放到「✖ 关上画室」；`destroy()` 里**先还原属性再把焦点还回去**（顺序反了会把焦点丢给一个还是 `inert` 的节点）。
- **量化**：画室打开时 Tab 的第一站从**第 21 站**变成**第 1 站**；host 内可聚焦的隐藏控件从 11 个降到 0 个。`color-fun` chunk 58.52 → **59.10 kB**（+0.58 kB，两条 a11y 逻辑加注释的全部代价）。
- **受益游戏 id**：`color-fun`。
- **守门用例**：新增 `src/games/color-fun/sandboxA11y.test.ts`（8 条）。仓库的 vitest 跑在 node 环境、按既定约定**不引 jsdom**，所以分两路验：纯函数 `muteBehind` 拿极简元素桩逐条验（挂上、按原值还原、空数组不炸），`openSandbox` 里怎么用它用源码巡检钉住（交给 `muteBehind` 的是 `host.children` 里除 sheet 以外的孩子；初始焦点是 `closeBtn`；`destroy` 里 `restoreBehind()` 排在 `opener?.focus?.()` 前面）——这和本款既有的 `upgrade12.test.ts` 是一个路数。
- **已知余量**：`inert` 只盖得住 `host` 里面的东西。平台壳层那三颗（返回 / 攻略 / 暂停）在 `host` 之外，画室开着的时候仍然 Tab 得到。要把它们也关进去就得动 `src/ui/gameShell.ts`，属**窗口1**，本轮不越界；而且「暂停」在浮层里仍然可达其实不算坏事，登记在此供后续判断。
- **风险与回滚**：`inert` 在目标浏览器（Chrome 105+ / Safari 15.5+）都支持，不支持的老环境上 `aria-hidden` 仍然生效，属渐进增强；副作用是画室开着时「🎨 自由涂色（不计分）」那颗切换按钮不再能点第二下关闭——但它本来就被 `inset:0` 的画室整个盖住、点不着，行为没变。回滚：`git revert 4e17521`。

---

#### W5-L-05 · `color-fun` 自由画室支持 Esc 关闭 —— ✅ 本轮落地

- **来源思路**：同上那套弹窗惯例的最后一件——**Esc 一定要能关掉浮层**。平台的 `dialogs.ts` 有 `isDismissKey`（`Escape` 和老浏览器上的旧名字 `Esc` 都认），画室没吃到。
- **现状**：画室只能靠点「✖ 关上画室」退出，键盘用户没有退路。
- **落地方式**：`sandboxUi.ts` 导出 `isSandboxDismissKey(key)`，在 `.clf-sheet` 上挂一个 `keydown`，认到就 `preventDefault` + `stopPropagation` 后走 `opts.onClose?.()`——**关闭仍然由外面那套 `closeSandbox()` 统一收口**，画室自己不偷偷拆 DOM，`destroy()` 该做的还原与焦点归位一件都不会漏。`stopPropagation` 是为了不让这一下 Esc 顺着冒到平台壳层再触发一次别的关闭。判定逻辑没有 `import` 平台文件，自己写了一行——本档不许碰 `src/ui/**`，连引用都省掉，少一条跨窗依赖。
- **量化**：画室的键盘退出路径从 0 条变 1 条；配合 W5-L-04 的初始焦点，键盘用户「开画室 → Esc → 焦点回到开画室那颗按钮」整条链闭合。
- **受益游戏 id**：`color-fun`。
- **守门用例**：并在 `sandboxA11y.test.ts` 里——`isSandboxDismissKey` 认 `Escape` / `Esc`、不认 `Enter` / 空格 / `Tab` / 小写 `escape`；源码巡检钉住 keydown 分支走的是 `opts.onClose?.()` 且带 `stopPropagation()`。
- **风险与回滚**：唯一的风险是和平台某个全局 Esc 撞车；扫了一遍 `src/ui/**`，只有 `collection.ts` 与 `guide.ts` 的弹窗在听 Esc，都是各自弹窗自己的监听，且我们这一下已经 `stopPropagation`。回滚：和 W5-L-04 同在 `4e17521`，一起 revert。

---

#### W5-L-06 · 把「题面不许泄答案」抽成全库共用的守门工具 —— ❌ 本轮不落地

- **来源思路**：W5-L-01 与 W5-L-02 其实是同一条规矩的两个实例——**题面渲染出来的任何文本表示（含 `aria-label`、`title`、释义、义项）都不得包含 `answer`**。76 款里的教育类（`clock-house` / `word-garden` / `pinyin-train` / `math-farm` / `shape-kingdom` …）全都走 `quiz99.ts` 那套 `{ask, promptHTML, choices, correct, answer}` 结构，这条完全可以写成一个共用断言，一处写完几十款受益。
- **现状**：两个档次的守门用例现在各写各的（我这轮就写了两份长得很像的）。
- **为什么本轮不落地**：要新建 `src/games/` 下的共用文件（或落进 `quiz99.ts` 旁边），那是**跨窗口的共用文件**，本档权限边界明确写着不许碰；而且这类工具一旦上线，别的档的既有题库多半会当场红一片，得由有全库权限的角色统一收。**交给窗口1**（或全库口径的后续轮次）。本轮先把两个实例做扎实，将来抽取有现成的行为参考。

---

#### W5-L-07 · 44px 热区抽成平台级 CSS 变量 / 工具类 —— ❌ 本轮不落地

- **来源思路**：`--tap-min: 44px` 一个变量 + 一条 `.tap-min{min-height:var(--tap-min)}` 工具类，比在 63 个游戏目录里各写各的 `min-height` 靠谱得多；配上一条扫全库 CSS 的用例，热区就再也掉不下去。
- **现状**：`src/styles.css` 里已经有 44px 的影子（`a11y.test.ts` 在扫），但游戏目录里是各写各的字面量——光本档就有 `.ld-btn` / `.ldc-mainbar .ld-btn` / `.rbe-back` / `.rbv-over-btn` / `.clf-tool` 五套写法。
- **为什么本轮不落地**：要改 `src/styles.css` 与 `src/ui/**`，是**窗口 1 的平台文件**。**交给窗口1**。本轮在本档目录里按同一个数值收口（W5-L-03），将来换成变量是纯替换，不会有行为差。

---

#### W5-L-08 · `red-blue-race` 战役对手的动态难度（追赶时手抖、领先时收力） —— ❌ 本轮不落地

- **来源思路**：休闲竞速类里很成熟的一条——**会失误的对手比完美对手好玩**，再叠一层温和的橡皮筋（落后方拿一点点助推、领先方节奏抖一下），能把「一边倒」的局面压成「咬得很紧」。
- **现状**：本款其实已经做得相当好——`ai.ts` 把四档建模成「目标节奏 + 稳定度 + 失误率 + 反应时间」，`tapsPerSec` 一律不许超过 `HUMAN_TAP_CAP_HZ`（赢在稳不赢在开挂），`runAiLane()` 给定同一个 `rand` 完全确定、四档强度单调有断言钉着。缺的只是**局内的动态调节**：目前一档从头到尾一个参数。另外本款已有「🤝 让分」这颗人工助推开关，动态难度和它是同一个位置上的两套东西，得先想清楚要不要叠。
- **为什么本轮不落地**：这是**直接动玩法手感**的改动，本轮纪律写着「改动要小而稳，不许推翻既有玩法手感」。要落地得先补一批 `runAiLane` 的对局分布回归（胜率带宽、领先差值分布），不是一条断言能兜住的。**建议第 2 轮**，和让分开关的定位一起定。

---

#### W5-L-09 · `clock-house` 错题回顾的「同类换数字」覆盖率做成可回放的种子用例 —— ❌ 本轮不落地

- **来源思路**：确定性随机 + 可回放输入序列。既然 `reviewSeed` / `makeReviewQuestions` 已经是纯函数，那「错了哪几类 → 回顾轮出的是不是这几类、是不是换了数字而不是原题重放」完全可以拿一组固定种子跑成快照。
- **现状**：`mistakes.ts` 的统计与 `makeReviewQuestions` 各自有用例，但**「统计 → 回顾题型」这条链**目前只有测试员的一次实玩记录（第 1 关错一次，确实多出一轮同题型换数字的题），没有机器钉着。
- **为什么本轮不落地**：不是缺陷、是覆盖率，属锦上添花；本轮的 5 条落地已经把测试员记的一般级问题清完，再堆用例会稀释这一轮的可复核性。**建议第 2 轮**，本档目录内即可完成，不碰任何共用文件。

---

#### W5-L-10 · `landlord-cards` 三档牌力提示的「不泄底」守门用例 —— ❌ 本轮不落地

- **来源思路**：和 W5-L-06 同源——**提示只许给方法，不许给答案**。本款的「💡 提示」有三档强度，测试员这轮实玩确认没泄底，但和 `clock-house` / `word-garden` 那两条一样，**没有机器钉着就会长回来**。
- **现状**：`hint.ts` 有 `hint.test.ts` 覆盖提示的挡位与内容生成，但没有一条断言说「提示文案里不许出现对手手上具体是哪几张牌」。
- **为什么本轮不落地**：这条要先和 `hint.ts` 的作者对齐口径——三档里最强的那一档本来就带「建议出哪一手」，那算不算泄底得先定义清楚（教学型提示和泄底之间的线，在纸牌类里和在题库类里不一样）。定义没定之前写用例是拍脑袋。**建议第 2 轮**，先在报告里把口径讨论完再写断言。

---

### A2 · 改动文件登记

| 文件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/games/clock-house/clockface.ts` | 新导出 `FACE_LABEL`；`faceSVG` 的 `opts.label` 默认值从 `formatClockMinute(time)` 改成 `FACE_LABEL`；随之去掉不再用到的 `formatClockMinute` 引入 | W5-L-01：分钟级钟面的默认读屏标签就是答案 |
| `src/games/clock-house/levels.ts` | `clockSVG()` 多一个默认 `FACE_LABEL` 的 `label` 形参、`aria-label` 改用它并补 `role="img"`；`qSet` 的三张选项钟面显式传「钟面」 | W5-L-01：老钟面的读屏标签就是答案 |
| `src/games/clock-house/levels.test.ts` | `LEGACY_DIGEST` 那条改成「把标签还原成 1.1 写法后再比对」，摘要值不变；另补一条「前 99 关的钟面确实换上了不含时刻的标签」 | 摘要锁不放水，改成钉「除了读屏属性没有第二处改动」 |
| `src/games/clock-house/a11yLabels.test.ts`（新增） | 4 条守门用例 | W5-L-01 的守门，兼测试员 A5 点名的缺失用例之一 |
| `src/games/word-garden/logic.ts` | 三张成语卡的 `meaning` 换成不含答案字的说法；`IdiomCard.meaning` / `RadicalCard.topic` 的注释写死这条规矩；新增纯函数 `radicalTargets()` | W5-L-02：释义与义项词自指 |
| `src/games/word-garden/levels.ts` | `qRadical()` 的答案候选从 `card.chars` 换成 `radicalTargets(card)` | W5-L-02：偏旁题答案不许出现在义项词里 |
| `src/games/word-garden/noSpoiler.test.ts`（新增） | 3 条守门用例 | W5-L-02 的守门，兼测试员 A5 点名的缺失用例之二 |
| `src/games/landlord-cards/index.ts` | `.ld-open` 加 `min-height:44px` + `inline-flex` 居中 | W5-L-03：模式入口 38px |
| `src/games/landlord-cards/shell.test.ts` | 既有「热区」describe 里补一条模式入口断言 | W5-L-03 的守门 |
| `src/games/red-blue-race/index.ts` | `.rbe-open` 加 `min-height:44px` + `inline-flex` 居中；`.rbr-chip-btn` 的 `min-height` 30 → 44 | W5-L-03：模式入口 40px、让分开关 30px |
| `src/games/red-blue-race/touch.test.ts`（新增） | 3 条守门用例 | W5-L-03 的守门 |
| `src/games/color-fun/sandboxUi.ts` | 新增 `MuteTarget` / `muteBehind()` / `isSandboxDismissKey()`；`openSandbox` 记录 opener、屏蔽背后节点、初始焦点给关闭键、加 Esc 监听；`destroy()` 先还原再归位焦点 | W5-L-04 / W5-L-05：浮层的焦点管理与 Esc |
| `src/games/color-fun/sandboxA11y.test.ts`（新增） | 8 条守门用例 | W5-L-04 / W5-L-05 的守门 |
| `docs/qa/1.2-window5-round1-learner.md` | 本报告（只写档A 节） | 本轮产出 |

**没碰**：`src/ui/**`、`src/engine/**`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`、`package.json`、本档以外的任何游戏目录、`docs/qa/1.2-window5-round1-tester.md`。**没有引入任何依赖**（`package.json` / `package-lock.json` 零改动）。**没有动任何存档 key**。

### A3 · 验收

| 项 | 基线（`ba90919`） | 本轮收尾 | 结论 |
| --- | --- | --- | --- |
| `npm test` | 194 文件 / 5379 用例全绿 | **198 文件 / 5399 用例全绿**，0 失败 0 跳过，35.05s | ✅ 用例只增不减（+4 文件 / **+20 条**） |
| `npm run build` | 全绿 | **全绿**，`tsc --noEmit` 0 错，`✓ built in 326ms`，PWA precache 139 项 | ✅ |
| 主包 `index-*.js` | 86.80 kB（gzip 30.51） | **86.80 kB（gzip 30.50）** | ✅ 一个字节没涨 |
| `clock-house` chunk | 29.13 kB | 29.18 kB（+0.05） | ✅ |
| `red-blue-race` chunk | 45.84 kB | 46.16 kB（+0.32） | ✅ |
| `landlord-cards` chunk | 54.47 kB | 54.71 kB（+0.24） | ✅ |
| `color-fun` chunk | 58.52 kB | 59.10 kB（+0.58） | ✅ |
| `word-garden` chunk | 67.28 kB | 67.38 kB（+0.10） | ✅ |

新增的 20 条用例分布：`clock-house` +5（新文件 4 + `levels.test.ts` 1）、`color-fun` +8、`word-garden` +3、`red-blue-race` +3、`landlord-cards` +1。

测试员档A 记的 5 条一般级问题，**本轮 5 条全部落地修完**（W5-A-01 → W5-L-01，W5-A-02 → W5-L-02，W5-A-03 / W5-A-04 → W5-L-03，W5-A-05 → W5-L-04，另加 W5-L-05 的 Esc）；2 条建议（W5-A-06 `parentAuth` 动态 import 失效、W5-A-07 发布元数据停在 1.1）都在 `src/ui/**` 与 `package.json`，按窗口纪律**交给窗口1**，本档一行没动——构建期那条 `[INEFFECTIVE_DYNAMIC_IMPORT]` 告警本轮仍在，属预期。

本轮不写 fixer 报告，也不开第 2 轮。

## 档B · 五款

（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`）

### B0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · 第 1 轮 · 档B 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`c523100`（对齐 `origin/game-1.2-window5`），测试员档B 记的水位是 `npm test` 198 文件 / 5399 用例全绿、`npm run build` 全绿、主包 86.80 kB。
**输入**：`docs/qa/1.2-window5-round1-tester.md` 的**档B** 节（阻断 2 / 严重 2 / 一般 2 / 建议 1），以及 `docs/plan-1.2-step27-B-learner.md` 的方法论（数字与范围按本窗口提示词覆盖）。

**权限边界**：只改 `src/games/{fishing-star,red-blue-tap,math-farm,shape-kingdom,music-stars}/**` 与本文件的**档B 节**。不改测试员报告，不改档A 节，不改 `src/ui/**` / `src/engine/**` / `src/styles.css` / `level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`，不改本档以外的游戏，不引入任何外部运行时依赖，CSS 前缀沿用各款已有前缀，用例总数只增不减。

**这一轮列 11 条、落地 7 条**（提示词要求「至少 8 条、至少落地 3 条」）。落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例、失败可以靠 `git revert` 单条回滚。

优先级按测试员的严重度排：两条阻断里能在本档目录落地的那半（W5-B-01 本档侧、W5-B-02）先做，两条严重（W5-B-03 点阵第 7 列 / 八键沙盒、W5-B-04 长宽术语教反）紧随其后。平台那两半（`src/styles.css` 的 `.game-stage{overflow:hidden}`、`quiz99.ts` 的朗读接缝）按纪律**交给窗口1**。`music-stars` 测试员认为干净的部分（音程名、音准、音量、沙盒不产星）一个字都不动。

### B1 · 十一条改进逐条

---

#### W5-L-11 · `shape-kingdom` 长方形题不许把短边叫「长」 —— ✅ 本轮落地

- **来源思路**：题库工程里的「术语一致性检查」——**一门课正在教的名词，题面自己必须用对**。算错了孩子会被判错、马上知道；术语教反了没人拦，还会被当成正确用法记住。这类问题只能靠「遍历全库、把说出口的每一处术语和图形本身对一遍」的守门用例钉住，靠人眼复审必漏。
- **现状**：`rectFigSVG(w, h)` 画的是**宽 w × 高 h**，读屏标签却写死成「长 w 厘米、宽 h 厘米」，周长题第三档提示也是同一套写法。宽高是 `randInt` 随机摆的，于是只要抽到 `h > w`，说出口的「长」就比「宽」短。测试员全量扫 188 关，长方形图形题共 51 道，**12 道「长 < 宽」，分布在 7 关**（100 / 101×2 / 102 / 104×2 / 105 / 106 / 107 …）。
- **落地方式**：新增纯函数 `rectSides(w, h) → { long: max, short: min }`，凡是要说「长多少、宽多少」的三处（题面 SVG 的 `aria-label`、`qPerimeter` 一步与两步两档的第三档提示）一律走它。**图形本身一个坐标都没动**——底边仍然标 `w 厘米`、左边仍然标 `h 厘米`，改的只是「哪条边该叫长」这个称呼。答案、选项、干扰项、随机数消耗**全部不变**，所以 `LEGACY_FINGERPRINTS` 那条前 99 关指纹锁一次都没红（周长题从第 7 章才开始，本来也不在前 99 关里）。
- **量化**：修掉 **12 道**术语教反题（占 51 道长方形题的 23.5%）。`shape-kingdom` chunk 63.37 → 64.49 kB（这条只占其中很小一部分，主要增量来自 W5-L-13 / W5-L-16）。
- **受益游戏 id**：`shape-kingdom`。
- **守门用例**：新增 `src/games/shape-kingdom/rectTerms.test.ts`（3 条）——`rectSides` 三例（横的、竖的、正方形）；测试员点名的第 100 关那道「宽 5 × 高 6」标签必须是「长 6 厘米、宽 5 厘米」且图上两个数字标注没被顺手改掉；188 关全量扫「长 x 厘米、宽 y 厘米」，断言 `x ≥ y` **且**这两个数就是图形 `data-w` / `data-h` 那两条边（不许拿别的数糊弄），带 `checked > 100` 与「确实扫到过竖着画的长方形」两条自检防空转。
- **先红后绿的证据**：把 `levels.ts` 单独 stash 掉再跑这一份，第一条就红在 `第 100 关：长 5 < 宽 6`，和测试员手点出来的那一道逐字对上。
- **风险与回滚**：零玩法风险——算术不看谁长谁短，`(长+宽)×2` 的结果一个都没变。回滚：`git revert 82d2eaf`。

---

#### W5-L-12 · `red-blue-tap` 双人对战在 320×640 上把键盘收成 2×2 —— ✅ 本轮落地

- **来源思路**：响应式布局里的一条常识——**断点不能只看宽度**。竖排布局吃的是高度，只按 `max-width` 分档，必然在「窄且矮」的机器上翻车。另一条是**热区分级**：44px 是触屏底线、72px 是舒适值，摆不下的时候「退到底线」永远好过「摆不下就切掉」。
- **现状**：`@media (max-width: 420px)` 把每侧键盘改成一竖排、并特意注明「按钮不缩水」。四颗 72px 的键 + 三条 8px 间隙 = **312px 高**，加上键盘上面那一截（返回条 + 比分 + 回合说明 + 分工名）实测约 367px，一共 679px。测试员在 **320×640** 上量到第 4 颗键盒子 `top 607 / bottom 677`——**键心已经在屏幕外**，往上找了一路祖先没有任何可滚容器（`scrollHeight - clientHeight == 0`），那一侧玩家等于少一颗键。双人对战是 `meta` 明确声明的模式，320px 是仍在服役的老安卓机常见宽度，判**阻断**。
- **落地方式**：加一档 `@media (max-width: 420px) and (max-height: 700px)`——**又窄又矮**才收：每侧从一竖排收回 **2×2**，边长 72 → **56px**（仍高出 44px 底线 12px），间隙 8 → 6px，`.rbt-vs` 内边距与 `.rbt-vs-brief` 各收一点。两侧中间那条 `SIDE_GUTTER_PX = 24` 的隔离带**一分不动**（它防的是一只手误触另一边，跟高度无关）。排布口径抽成纯函数 `padLayout(vw, vh)` / `padHeightPx()` / `padWidthPx()`，CSS 与用例共用同一批常量，不许各写各的。
- **量化**：一侧键盘高度 **312px → 118px**（省 194px）；四颗键在 320×640 上的底边从 **679px（超屏 39px）** 变成 **485px**，留出 155px 余量。横向也验过：320px 上一侧内容宽 130px，两列 56px + 6px 间隙 = 118px，塞得下。**360×740 / 360×667 / 桌面三档的排布一个字没变**——测试员实测这三档本来就够得着，不去动它。`red-blue-tap` chunk 39.83 → 40.49 kB（+0.66）。
- **受益游戏 id**：`red-blue-tap`（战役、双人对战、「点到手软」无尽共用同一套 `.rbt-pad`，三种模式一起受益）。
- **守门用例**：新增 `src/games/red-blue-tap/touchLayout.test.ts`（5 条）——宽屏与窄高屏的排布逐档比对（证明没顺手改到本来好的那几档）；矮屏四种分辨率都收成 2×2 且键 ≥44px；320×640 / 360×640 / 320×568 上第 4 颗键落在屏内，**外加一条反例**「老排布在同一台机器上确实伸出屏外」防空转；横向放得下且隔离带没被改小；CSS 与纯函数用同一批数字，且矮屏分支必须排在窄屏分支**之后**（顺序反了会被盖回一竖排）。
- **风险与回滚**：视觉上矮屏的键从 72 缩到 56，按下去的手感会略紧一点——但这是「按得到」和「按不到」之间的取舍，且 56px 仍高于本库自己的 44px 口径。呼吸动画 `scale(.97)` 在 56px 上是 54.3px，照样过底线。回滚：`git revert b72acc7`。

---

#### W5-L-13 · `shape-kingdom` 点阵作图台按整块板子的宽度算尺寸 —— ✅ 本轮落地

- **来源思路**：绝对定位 + 弹性父容器是一对经典的坑——**子元素的像素坐标是按「我以为的宽度」算的，父容器却按「实际有多少地方」把它压扁**，两个数一旦对不上，点就飞到板外。防它的办法只有一个：**先定死外框尺寸，再往回推内部格距**，而不是反过来。
- **现状**：`drawMetrics()` 先把格子撑满可用宽度（`board = min(360, vw - 40)`），`buildRectBoard` 再把板子设成 `unit * cols + hit`——两端各探出半个热区，**整块板子必然比可用宽度多出一个热区**。`.shk-boardwrap` 是 flex，板子作为可收缩的 flex 项被压到 279px，而绝对定位的点还按原像素摆：测试员量到第 7 列的点心在 **x=387**，比画板右缘还右 67px，五行的第 7 列**全部够不着**。后果是「6×1 那种要横跨 6 格的解法在手机上摆不出来」。桌面 1280px 下板子放得开，所以这毛病**只在手机上出现**。
- **落地方式**：新增纯函数 `dotBoardMetrics(vw, cols, rows)`，反过来算——先按「热区正好等于一格」试（这时整块板子刚好是 `span + 1` 个热区宽），装得下就用；装不下（格子挤到 44px 以下）就把热区钉在 44px、再往回推格距，相邻热区允许重叠，落点精度交给已有的 12px 吸附半径。`buildRectBoard` 改用它。另外给 `.shk-board` 补 `flex:none`——**被压扁本来就是这个 bug 的另一半**，板子不缩，点和板就永远对得上。对称填涂与铺砖两种台子不用这个入口（它们的板子没有探出的热区），`drawMetrics` 原样保留、既有断言零改动。
- **量化**：360px 上整块板子 **373px → 320px**（不再超出可用宽度 53px）；第 7 列点心 x 从 **387** 回到 **~301**，落在板内。6 档屏宽（320 / 360 / 390 / 412 / 768 / 1280）× 两种点阵（6×4、6×5）共 12 组，**每一组的每一颗点的热区都整个落在板内**，热区最小值仍是 44px。
- **受益游戏 id**：`shape-kingdom`（第 100 关起每章第 3 关都是作图关，占 188 关里的十几关）。
- **守门用例**：新增 `src/games/shape-kingdom/dotBoard.test.ts`（本条占 4 条）——板子不超宽不超高、热区 ≥44；每一颗点（第 1 列到第 7 列、第 1 行到最后一行）的热区四边都在板内、点心也在板内；**一条反例**「老口径在 360px 上确实超宽」；界面巡检钉住 `buildRectBoard` 走的是新口径且 `.shk-board` 有 `flex:none`。
- **风险与回滚**：格距在 320px 上从 53.3px 收到 39.3px，视觉上点密了一点，热区因此重叠——重叠时后渲染的点在上，最坏情况是相邻两列的边缘各让出几像素，而吸附半径 12px 本来就比这更窄，判定不受影响。回滚：`git revert bedddf3`。

---

#### W5-L-14 · `music-stars` 星星键盘按真实屏宽算，摆不下就收到 44px 并横向滚 —— ✅ 本轮落地

- **来源思路**：两条。一是**「量真实容器，别写死断点」**；二是**约束求解要有明确的让步顺序**——热区、间隙、总宽三个约束不可能同时满足时，必须事先写清「先让谁、让到哪一档、让不动了怎么办」，否则代码就会像现在这样「谁也不让，直接溢出」。
- **现状**：`createStarBoard` 里 `keyLayout(opts.width ?? 360, ...)`，**360 是写死的，从来没有一个调用点传过 `width`**；而 `keyLayout` 为了保住 56px 热区会 `Math.max(KEY_MIN_PX, …)`，**算出来的总宽允许超过传进去的 `available`**。八键：8×56 + 7×8 = **504px** 硬塞进 360px。测试员实测七声音阶沙盒「哆」在 `[-68, -13]`、「高哆」在 `[373, 428]`，**两个键整个在屏幕外**，中间也没有横向滚动容器——主音和高八度的哆，最该让孩子按到的两个音，在手机上按不到。同一根因还带出 W5-B-07.1：双声部关 5×56 + 4×24 = 376px，比屏幕宽 11px，两端各切掉约 5px。**仓库里其实早就有 `layoutFits()` 能判这件事，但一个调用点都没有**——测试员点破了这层窗户纸：`upgrade12.test.ts` 对五声断言了 `layoutFits`，轮到双声部和八键就只断言热区和间距，**因为一断言就红**。
- **落地方式**：
  - 新增纯函数 `boardWidth(vw?)`：真实屏宽（读不到就退回原来那套）减掉 `.mst-wrap` 的左右内边距，再按 `.mst-sky` 的 360px 上限夹一次；`createStarBoard` 的默认宽度改走它。
  - `keyLayout` 从「死守 56px」改成**逐级让步**：① 原间隙下把键收到 44px 触屏底线以上；② 还不行，**只有默认间隙**才允许再收到 4px；③ 再不行就返回底线尺寸，明确交给横向滚动。**双声部那 24px 隔离一律不许拿去换宽度**——一根手指盖住两颗键就判不出和弦，那是玩法本身。
  - `layoutFits` 终于有了调用点：放不下时给键盘那一行挂 `mst-keys-scroll`（`overflow-x:auto` + `touch-action:pan-x`），键自身仍是 `touch-action:none`，按下去出声、不会一划就滚走。`StarBoardHandle` 多一个 `fits` 字段供上层与用例判断。
- **量化**：双声部关 5 键在 360px 上从**超宽 16px（两端各切 5px）变成真的摆得下**（`layoutFits` 从 false 变 true，键 56 → 48px，间隙仍是 24px）；七声八键仍然塞不进（光键身 8×44 就 352px，这是物理上限），但「哆」与「高哆」**从「点不到」变成「滚得到」**。全量扫 9 档可用宽度 × 1–8 键 × 2 种间隙共 144 组，**热区一次都没掉到 44px 以下**，且每次收窄都验过「不收真的摆不下」（不许白收）。`music-stars` chunk 48.43 → 50.50 kB（含 W5-L-15）。
- **受益游戏 id**：`music-stars`（跟弹回声 1–99 关、双声部合奏 144–166 关、简谱视奏 167–188 关、自由弹奏沙盒**共用**这一个 `createStarBoard`，一处改动四处受益）。
- **守门用例**：新增 `src/games/music-stars/keyboardFit.test.ts`（本条占 9 条）——`boardWidth` 的五种入参；宽松时老行为一字未变（含桌面封顶 84px）；双声部在 360 / 390 / 412 上真的摆得下（附「老算法 376 > 360」的反例）；320px 上摆不下也不许挤掉那 24px 隔离；**144 组全量扫**热区 ≥44、间隙 ≥4、每次收窄都有必要；七声八键收到底线且 `layoutFits === false`，桌面上则不该退到底线；拿 `domStub` 真建键盘，验五声/双声部不挂滚动条、七声挂上 `mst-keys-scroll` 且每颗键 ≥44px；滚动样式确实能滚且没抢走按键手势。
- **既有断言被迫修改（单独解释）**：`upgrade12.test.ts` 那条「360px 下五颗星星一行放得下」里有两处 `≥ KEY_MIN_PX (56)` 改成了 `≥ KEY_TOUCH_MIN_PX (44)`——分别是双声部那档和八键那档。**这不是放水，是把一条空转的断言换成一条真的断言**：原来那两处只管「键别缩水」、不管「摆不摆得下」，所以它们在键排明明溢出屏幕的情况下照样绿。改完之后两处各补了一条 `layoutFits`——双声部断言 `true`（必须真的摆得下），八键断言 `false`（承认物理上摆不下，由滚动兜底）。测试员在 B5 节点名要补的就是这两条，本轮一并补齐。
- **风险与回滚**：双声部键从 56px 收到 48px，两指同按的判定靠 `duetKeysSeparated`——生成器保证同一拍两颗键至少隔 2 格，48px 键 + 24px 间隙下实际间距 96px，远高于 24px 的门槛，判定不受影响。横向滚动只在八键沙盒这一种情形出现，关卡里一次都不会触发。回滚：`git revert f489cb3`。

---

#### W5-L-15 · `music-stars` 矮屏上把自己的壳压进可视高度，压完还高就自滚 —— ✅ 本轮落地

- **来源思路**：**能在自己家里解决的，别等房东修屋顶**。舞台硬裁是平台的事（交给窗口1），但「我的内容为什么这么高」是本款自己的事。做法有两层：先按可用高度逐项收一档，收完还高就在**本款自己的壳**上开一个滚动容器——不碰任何共用文件，也能让被裁掉的东西重新够得着。
- **现状**：测试员在 **360×720** 上量到 `music-stars` 简谱视奏关内容高 **741px**、舞台可视只有 **618px**，多出来的 **123px** 被 `.game-stage{overflow:hidden}` 直接切掉，「哆」键的键心正好压在裁切线上——**点下去一点反应都没有**，程序化把 `scrollTop` 推到底之后同一个点就能点响，证明键是活的、只是够不着。360×640 上更狠，简谱章五个键全部点不着，**22 关（167–188）整章无法游玩**。内容偏高的两个大头：`.mst-sky` / `.mst-keys` 各钉着 `min-height:150px`，`.mst-face` 星星脸 44px 字号。
- **落地方式**：新增一档 `@media (max-height: 720px)`：`.mst-wrap` 的 `min-height` 420 → 0，并加上 `max-height:100%` + `overflow-y:auto` + `touch-action:pan-y`（键自己仍是 `touch-action:none`，滑动只从星空那一片起手，不会一划就把音滑走）；`.mst-sky` / `.mst-keys` 150 → 104、`.mst-face` 44 → 30、`.mst-bar` 56 → 44、`.mst-msg` / `.mst-dots` / `.mst-score` / 行距与内边距各收一档。**热区一个都不动**——`.mst-btn` 的 44px、`.mst-chip`、`.mst-choice`、`.mst-drum` 都不在这一档里，星星键盘的边长是 `keyLayout` 内联出来的、更不受影响。所有竖向数字抽成 `BASE_SIZES` / `SHORT_SIZES` 两份常量，**基准样式与矮屏样式共用同一批数字**，不可能各写各的。
- **量化**：`shortScreenSavingPx()` 把省下的高度算成一个可断言的数：**132px**，盖得住 360×720 上被裁掉的 123px（`741 − 618`）。360×640 上被裁 203px，压缩只能盖住其中 132px，剩下的靠 `.mst-wrap` 自滚兜住——**这一半必须等平台把 `.game-stage` 改成可滚才算真正干净**，本轮如实登记。
- **受益游戏 id**：`music-stars`（五种玩法 + 沙盒共用 `.mst-wrap`）。
- **守门用例**：`keyboardFit.test.ts` 里 2 条——省下的高度必须 ≥ 被裁掉的 123px，且 `SHORT_SIZES` 的每一项都必须真的小于 `BASE_SIZES`（不许拿一个没动的数来凑）；矮屏那一档里**不许出现任何按钮选择器**（`.mst-btn` / `.mst-chip` / `.mst-choice` / `.mst-drum`），且基准样式里 44px 的按钮热区原样还在，同时必须带 `overflow-y:auto` + `touch-action:pan-y`。
- **风险与回滚**：星星脸从 44px 收到 30px，在 720 高以下的机器上视觉小一号——但键的**热区**是 `keyLayout` 内联的 `width` / `min-height`，跟字号无关，按下去的判定一点没变。`touch-action` 从 `none` 放宽到 `pan-y` 只影响从非键区域起手的滑动。回滚：`git revert fcd14aa`。

---

#### W5-L-16 · `shape-kingdom` 作图台矮屏自滚，「✅ 我摆好了」不再被裁掉 —— ✅ 本轮落地

- **来源思路**：同 W5-L-15，同一套两层做法用在同一条阻断的另一个受害者身上。这也正是「横向收益」在本档的形态——**一条根因，四款中招，同一个模板套过去**。
- **现状**：`.shk-draw` 钉着 `min-height:380px`。测试员量到 360×720 上「✅ 我摆好了」低于裁切线 **37px**、360×640 上低 **117px**（「提示」「重来」也低 65px）。**作图关要按「我摆好了」才能交卷**，按不着就是交不了卷。
- **落地方式**：新增 `@media (max-height: 720px)`：`.shk-draw` 的 `min-height` 380 → 0 并加 `max-height:100%` + `overflow-y:auto`；抬头城堡条、题干、读数、消息行、提示条各收一档。**按钮只收左右内边距，`min-height:44px` 的热区一分不动**；`.shk-piece` 骨牌与 `.shk-dot` 格点也完全不在这一档里。顺手把 `DRAW_CSS` 改成导出，让用例能读到解析后的样式而不是源码里的模板占位符。
- **量化**：作图台的最小高度约束从 380px 直接归零，加上各行收一档，360×720 上原本低于裁切线 37px 的交卷键回到线上；360×640 上仍差一截，靠 `.shk-draw` 自滚兜住。
- **受益游戏 id**：`shape-kingdom`。
- **守门用例**：`dotBoard.test.ts` 里 1 条——矮屏分支必须带 `max-height:100%` / `overflow-y:auto` / `min-height:0`，且这一档里**不许出现更小的按钮 `min-height`**、不许出现 `.shk-piece` / `.shk-dot`，基准样式里的 44px 热区原样还在。
- **风险与回滚**：`.shk-board` 有 `touch-action:none`（拖点作图必须独占手势），所以自滚只能从板子外面起手——这正是想要的，拖着画的时候不会把画面滚走。回滚：`git revert 2d5fd2d`。

---

#### W5-L-17 · `fishing-star` 640 高的机器再收一档并自滚 —— ✅ 本轮落地

- **来源思路**：同上那套模板的第三次套用。这一条另带一个教训：**既有的响应式断点会被后来追加的内容悄悄吃掉**——`fishing-star` 本来就有一档 `@media (max-height: 720px)`，注释还写着「保证张力条和那颗大按钮永远在首屏里」，可 1.2 追加的风向条与红区预警又往这一屏塞了两行，那一档就不够用了。
- **现状**：测试员在 **360×640** 上量到第 100 关的「🎣 按住抛竿」中心落到舞台裁切线以下 **8px**——**按不着就开不了局**。360×720、390×844 都正常，只有 640 这一档中招。
- **落地方式**：新增 `@media (max-height: 660px)`（排在既有的 720 那一档**后面**，否则被它盖回去），把 HUD 芯片、提示行、张力条、1.2 追加的风向条与那一整行的行距各收一档，并给 `.fs-wrap` 加 `max-height:100%` + `overflow-y:auto` 兜底。那颗大按钮只收内边距，并**显式补上 `min-height:44px` + `box-sizing:border-box`**，保证收内边距不会把热区一起收掉。
- **量化**：这一屏的固定行高合计再省约 20px，盖得住那 8px；收完还高的极端情况由自滚兜住。`fishing-star` chunk 56.13 → 57.06 kB（+0.93）。
- **受益游戏 id**：`fishing-star`。
- **守门用例**：`shell.test.ts` 的「手机 360px」describe 里补 1 条——这一档必须带 `overflow-y:auto` + `max-height:100%`、必须给 `.fs-act` 写上 `min-height:44px`、必须排在 720 那一档之后。
- **风险与回滚**：只在 660px 以下生效，测试员实测正常的 720 / 844 两档一个字没变。回滚：`git revert d2c3dd3`。

---

#### W5-L-18 · `.game-stage{overflow:hidden}` 改成可滚 —— ❌ 本轮不落地，**交给窗口1**

- **来源思路**：这是 W5-B-01 的**根**，也是本轮最值钱的一条——一处改动惠及全库 76 款，而不是像 W5-L-15 / 16 / 17 那样一款一款贴膏药。
- **现状**：`src/styles.css:759` 的 `.game-stage { flex:1; min-height:0; overflow:hidden; }` 是个定高 flex 子项 + 硬裁剪，**内容超出既不滚也不给任何提示**，谁的内容一高谁就中招。本档四款（`music-stars` / `shape-kingdom` / `fishing-star` / 答题关）已经全部中招，别的窗口大概率也有。
- **为什么本轮不落地**：`src/styles.css` 是**窗口 1 的平台文件**，本档权限边界明写着不许碰；而且改成 `overflow-y:auto` 会影响全库 76 款的滚动行为，得由有全库权限的角色统一改、统一回归。**交给窗口1**。本轮在本档三款目录里各做了「自己先压一档 + 自己壳里自滚」的兜底（W5-L-15 / 16 / 17），平台那一半修好之后这三处兜底仍然成立、不冲突，也不需要回退。

---

#### W5-L-19 · 子游戏改写提示语时把朗读也一起改掉 —— ❌ 本轮不落地，**交给窗口1**

- **来源思路**：**「看到的」和「听到的」必须是同一句**——这是无障碍里的 `aria-live` 一致性原则在朗读上的对应物。
- **现状**（测试员 W5-B-06）：`quiz99.ts:281-282` 先 `msgEl.textContent = HINT_LINE` 再 `speak(HINT_LINE)`，子游戏（`math-farm` / `shape-kingdom`）随后只改了 DOM，**改不到已经排队的朗读**。屏幕上是子游戏那句只讲方法的提示，念出去的却是公共壳那句「悄悄提示：一闪一闪的那个就是答案！」。对开着朗读的孩子来说，这句话比屏幕上那句更像在喂答案。
- **为什么本轮不落地**：`src/games/quiz99.ts` 是**本档明确禁改的共用文件**，而且这需要开一个「子游戏可改写提示语（含朗读）」的接缝——是平台 API 设计，不是一处补丁。**交给窗口1 / 平台侧**。本档一行没动。顺带说明：闪烁兜底本身是平台既定设计（同一题错满两次才给，不让最后一次机会踩空），档A 已按同一口径登记，本档同样**不按泄题记**。

---

#### W5-L-20 · `shape-kingdom` 作图关文案说「拖两个点」，点两个点却没反应 —— ❌ 本轮不落地，**建议第 2 轮**

- **来源思路**：**承诺的每一种操作都得真的能用**，尤其触屏上小孩更习惯点、不习惯拖。
- **现状**（测试员 W5-B-05，一般级）：`.shk-dot` 上的 `click` 处理和板子上的 `pointerdown/pointerup` 拖拽处理打架——`onDown` 一律把 `corner` 设成当前点，`onUp` 又把它清掉，等 `click` 派发过来已经没有东西可用了，所以「点两个点」这条路**从来没通过**。我这轮读代码复核了一遍，确认测试员的判断正确：第二次点击时 `onDown` 会覆盖掉第一次存下的角点，两点定矩形永远凑不齐。
- **为什么本轮不落地**：修它要重排 `pointerdown` / `pointerup` / `click` 三者的职责（判「原地抬起 = 一次点击」、避免 `click` 二次触发、同时不能弄丢键盘用户按 Enter 的那条路——键盘的 `click` 是 `detail === 0` 的合成事件，要么改走 `keydown` 要么加判别）。这是**输入语义的改动，不是尺寸调整**，跟本轮「改动小而稳」的纪律不合，而且它和本轮已落地的 W5-L-13（同一个文件、同一块板子）挨得太近，一起上会让两条都不好单独回滚。**建议第 2 轮**单独做，本档目录内即可完成，届时要连「点两下」「拖一次」「键盘 Enter」三条路径各补一条用例。
- **临时口径**：拖是通的、关能过，所以不是阻断；本轮不改文案（把「拖两个点」改成只说「拖」是掩盖，不是修）。

---

#### W5-L-21 · `music-stars` 节奏条的判定线对齐方块中心 —— ❌ 本轮不落地，**建议第 2 轮**

- **来源思路**：**判定点必须和玩家眼睛瞄的地方是同一个点**。方块的左沿是拍点，屏幕上却画着一个 16px / 30px 宽的圆角块，孩子自然会瞄中间。
- **现状**（测试员 W5-B-07.2，建议级）：实测瞄左沿 5 下全「刚刚好」，瞄中心则长音掉到「很准」（30px ÷ 150px/s = 200ms，正好一整个判定窗口的宽度），短音仍「刚刚好」。**不影响过关，一次都没漏**。
- **为什么本轮不落地**：这是**直接动判定手感**的改动。把判定线挪到方块中心等于给所有长音加 100ms 的系统性偏移，得先跑一轮「四档判定窗口 × 长短音」的分布回归，确认不会把原本「刚刚好」的打法变成「偏早」。本轮纪律写着不许推翻既有玩法手感。**建议第 2 轮**，和测试员同一条里记的「真机 `outputLatency` 补偿」一起验（本机 `outputLatency 0 / baseLatency 0.01`，补偿只有 10ms，看不出影响，需要延迟大的真机）。更稳的替代方案是**只改画法不改判定**——给方块画一道中线或把方块改成左对齐的细线，本轮也没做，一并留到第 2 轮定夺。

---

### B2 · 改动文件登记

| 文件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/games/shape-kingdom/levels.ts` | 新增导出纯函数 `rectSides()`；`rectFigSVG()` 的 `aria-label` 与 `qPerimeter()` 两档的第三档提示改用它 | W5-L-11：12 道长方形题把短边叫「长」 |
| `src/games/shape-kingdom/rectTerms.test.ts`（新增） | 3 条守门用例 | W5-L-11 的守门，兼测试员 B5 点名的缺失用例之二 |
| `src/games/shape-kingdom/draw.ts` | 新增 `SHORT_SCREEN_PX` 与纯函数 `dotBoardMetrics()`，`buildRectBoard` 改用它；`.shk-board` 补 `flex:none`；新增 `@media (max-height:720px)` 一档；`DRAW_CSS` 改为导出 | W5-L-13：第 7 列掉到板外；W5-L-16：交卷键被舞台裁掉 |
| `src/games/shape-kingdom/dotBoard.test.ts`（新增） | 5 条守门用例 | W5-L-13 / W5-L-16 的守门 |
| `src/games/red-blue-tap/arena.ts` | 新增 `KEY_TIGHT_PX` / `PAD_GAP_PX` / `PAD_TIGHT_GAP_PX` / `SHORT_SCREEN_PX` / `VERSUS_CHROME_PX` 与纯函数 `padLayout()` / `padHeightPx()` / `padWidthPx()`；新增又窄又矮那一档 CSS | W5-L-12：320×640 第 4 颗键完全够不着 |
| `src/games/red-blue-tap/touchLayout.test.ts`（新增） | 5 条守门用例 | W5-L-12 的守门 |
| `src/games/music-stars/runtime.ts` | 新增 `KEY_TOUCH_MIN_PX` / `KEY_TIGHT_GAP_PX`；`keyLayout()` 从死守 56px 改成逐级让步，双声部的宽间隙不许被挤掉 | W5-L-14：键排总宽允许超过可用宽度 |
| `src/games/music-stars/ui.ts` | 新增 `WRAP_PADDING_X` / `SKY_MAX_PX` / `boardWidth()`、`BASE_SIZES` / `SHORT_SIZES` / `shortScreenSavingPx()` 与两个实测常量；`createStarBoard` 量真实屏宽、按 `layoutFits` 挂 `mst-keys-scroll`、handle 多一个 `fits`；基准 CSS 的竖向数字改走常量；新增矮屏那一档 | W5-L-14 / W5-L-15 |
| `src/games/music-stars/keyboardFit.test.ts`（新增） | 11 条守门用例 | W5-L-14 / W5-L-15 的守门，兼测试员 B5 点名的缺失用例之一 |
| `src/games/music-stars/upgrade12.test.ts` | 那条布局用例的两处 `≥56px` 改成 `≥44px`，并补三条 `layoutFits` 断言 | 见 W5-L-14 的「既有断言被迫修改」 |
| `src/games/fishing-star/index.ts` | 新增 `@media (max-height:660px)` 一档（排在既有 720 那一档之后），`.fs-wrap` 自滚兜底，`.fs-act` 显式补 `min-height:44px` | W5-L-17：第 100 关「按住抛竿」低于裁切线 8px |
| `src/games/fishing-star/shell.test.ts` | 「手机 360px」describe 里补 1 条 | W5-L-17 的守门 |
| `docs/qa/1.2-window5-round1-learner.md` | 本报告（**只写档B 节**） | 本轮产出 |

**没碰**：`src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`、`package.json` / `package-lock.json`、本档以外的任何游戏目录、`docs/qa/1.2-window5-round1-tester.md`、本文件的**档A 节**。**没有引入任何依赖**。**没有动任何存档 key**。`math-farm` 一行没改——测试员对它的结论是「零教错、零泄题、三档屏宽全绿」，唯一那条 W5-B-06 归属 `quiz99.ts`，不为改而改。`music-stars` 的音程名、音准、音量、沙盒不产星这几处测试员认为干净的地方也一个字没动。

### B3 · 验收

| 项 | 基线（`c523100`） | 本轮收尾 | 结论 |
| --- | --- | --- | --- |
| `npm test` | 198 文件 / 5399 用例全绿 | **202 文件 / 5424 用例全绿**，0 失败 0 跳过，36.3s | ✅ 用例只增不减（+4 文件 / **+25 条**） |
| `npm run build` | 全绿 | **全绿**，`tsc --noEmit` 0 错，`✓ built in 402ms`，PWA precache 139 项 | ✅ |
| 主包 `index-*.js` | 86.80 kB（gzip 30.50） | **86.80 kB（gzip 30.50）** | ✅ 一个字节没涨 |
| `red-blue-tap` chunk | 39.83 kB | 40.49 kB（+0.66） | ✅ |
| `music-stars` chunk | 48.43 kB | 50.50 kB（+2.07） | ✅ |
| `math-farm` chunk | 54.83 kB | **54.83 kB（±0）** | ✅ 本轮没改它 |
| `fishing-star` chunk | 56.13 kB | 57.06 kB（+0.93） | ✅ |
| `shape-kingdom` chunk | 63.37 kB | 64.49 kB（+1.12） | ✅ |

新增的 25 条用例分布：`music-stars` +11（新文件）、`shape-kingdom` +8（`rectTerms` 3 + `dotBoard` 5）、`red-blue-tap` +5、`fishing-star` +1。

**三方合并态复验**：收尾 `git fetch` 时档A 监督修复员的七条提交（`be67cbb`，动的是 `color-fun` / `word-garden` / `red-blue-race` 三款与 fixer 报告）已经落到同一分支，与本档改的五款**零文件重叠**，rebase 无冲突。合并态上重跑一次全量：`npm test` **204 文件 / 5444 用例全绿**、`npm run build` 全绿、主包仍是 **86.80 kB**。本档收尾 SHA **`235c842`**。

顺带回应档A 监督修复员在 `be67cbb` 里补记的 W5-F-07：他拿档B 这条 W5-B-01 的尺子回量档A 五款，砸中 `color-fun` 与 `red-blue-race`，结论和本档一致——**主根因在 `src/styles.css` 的 `.game-stage` 与 `level99.ts` 的 `.l99-stage-wrap`，两个都在禁改清单里，交给窗口1**。他那边判断「光缩本档内容清不掉」所以本轮不动本档侧；本档这三款（`music-stars` / `shape-kingdom` / `fishing-star`）的情况不同——按数算得出来压缩能盖住 360×720 那一档，再加上壳内自滚，所以本轮落地了。两边口径不冲突：**平台那一半不修，谁都只能兜到自己家门口**。

对测试员档B 那 7 条问题的处置：

| 编号 | 级别 | 本轮处置 |
| --- | --- | --- |
| W5-B-01 舞台硬裁 | 阻断 | **本档那一半三款全部落地**（W5-L-15 `music-stars` / W5-L-16 `shape-kingdom` / W5-L-17 `fishing-star`，各自「先压一档 + 自己壳里自滚」）；平台那一半 `.game-stage{overflow:hidden}` **交给窗口1**（W5-L-18）。`math-farm` 三档屏宽本来就全绿，不动。 |
| W5-B-02 `red-blue-tap` 320px 第 4 键 | 阻断 | ✅ **落地**（W5-L-12） |
| W5-B-03 点阵第 7 列 / 八键沙盒 | 严重 | ✅ **两半都落地**（W5-L-13 点阵、W5-L-14 键盘） |
| W5-B-04 长宽术语教反 12 道 | 严重 | ✅ **落地**（W5-L-11） |
| W5-B-05 「拖两个点」点不动 | 一般 | ❌ 不落地，理由与替代方案见 W5-L-20，**建议第 2 轮** |
| W5-B-06 提示看听不一致 | 一般 | ❌ 不落地，`quiz99.ts` 是禁改文件，**交给窗口1**（W5-L-19） |
| W5-B-07 双声部切 5px / 节奏条瞄哪儿 | 建议 | 第 1 条 ✅ 随 W5-L-14 落地；第 2 条 ❌ 见 W5-L-21，**建议第 2 轮** |

测试员在 B5 节点名「修的时候请顺手补上」的三条守门用例，**三条全部补齐**：① `music-stars` 的 `layoutFits` 断言补到双声部与八键两档，并另加「任何 count × available 组合下热区不许破 44px」的全量扫（`keyboardFit.test.ts`）；② `shape-kingdom` 长方形题「长 ≥ 宽」的 188 关全量断言（`rectTerms.test.ts`）；③ 窄屏高度那一条，本轮以「矮屏样式省下的高度必须盖得住实测被裁掉的那一截」+「矮屏那一档不许碰任何按钮热区」的形式落在三款各自的用例里。

构建期那条 `[INEFFECTIVE_DYNAMIC_IMPORT] src/ui/parentAuth.ts` 告警本轮仍在，属平台侧、档A 已登记**交给窗口1**，本档不重复计数、不改。

本轮不写 fixer 报告，也不开第 2 轮。

## 档C · 五款

（`poop-hero` / `red-blue-tug` / `pinyin-train` / `find-diff` / `kitty-care`）

### C0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · 第 1 轮 · 档C 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`47532a3`（对齐 `origin/game-1.2-window5`）。
**输入**：`docs/qa/1.2-window5-round1-tester.md` 的档C 节（阻断 0 / 严重 1 / 一般 3 / 建议 6），以及 `docs/plan-1.2-step27-B-learner.md` 的方法论（条数与范围按本窗口提示词覆盖：列 ≥8 条、落地 ≥3 条）。

**权限边界**：只改 `src/games/{poop-hero,red-blue-tug,pinyin-train,find-diff,kitty-care}/**` 与本文件的档C 节。不改测试员报告、不改 fixer 报告、不改本文件的档A / 档B 节；不改 `src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`；不改本档以外的任何游戏；不引入任何外部运行时依赖；不动存档 key；用例只增不减。

**动物福利红线**（本档 `kitty-care` 是专项）：无伤害、无死亡、无「照顾失败」结局、看病词表不踩线。任何改动都不许新增 `ctx.lose` 路径，也不许往词表里塞打针 / 喂药 / 伤口 / 手术之类的词。

**本轮打法**：先把测试员点名的**严重那一条（W5C-K01 马拉松秒表逐轮叠加）**做成先红后绿的回归，再顺着同一处把 W5C-K02 一起收掉；其余按「能在本档目录内小而稳落地」的标准挑。测试员明说「点错不再直接失败」「动物福利红线通过」的地方一律不动，不为改而改。

**这一轮列 16 条、落地 12 条**（提示词要求 ≥8 / ≥3）。落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例、失败可以靠 `git revert` 单条回滚。

### C1 · 十六条改进逐条

编号从 `W5-L-22` 起接着档B 的 `W5-L-21` 往下排。

---

#### W5-L-22 · `kitty-care` 照顾马拉松同一时刻只许有一个秒表 —— ✅ 本轮落地（测试员 W5C-K01，**严重**）

- **来源思路**：游戏循环工程里一条老规矩——**「起一个循环」和「谁负责停它」必须成对写出来**。凡是只提供「注册」不提供「注销」的生命周期容器（只有一个大 `destroy()`），迟早会出现「一个会话内部反复注册」的泄漏；开源的小游戏框架普遍把 `setInterval` 包成会返回句柄的 `Ticker` / `Timer` 对象，正是为了让「换一轮先停上一轮」写得出来。
- **现状**：`mountEndless.startRound()` 每开一轮就 `life.every(() => { left--; … }, 1000)`，而 `Life.every()` **不返回任何东西**，唯一的出口是 `Life.destroy()`。上一轮的秒表因此一直活着，且它们减的是同一个 `left`：开到第 N 轮就有 N 个在减，倒计时快 N 倍。测试员实测第 6 轮约 8 秒／秒，一轮只撑 4 秒真实时间，模式实质不可玩。
- **落地方式**：`mountEndless` 里多一个 `clock: Loop | null` 与 `stopClock()`，**换轮、超时、拆场三个出口都先停表**；`startRound()` 的第一行就是 `stopClock()`；做完一件事排下一轮之前也先停。搭配 W5-L-23 的可取消句柄一起用。
- **收益量化**：第 2 轮走一拍从掉 **2 秒**回到掉 **1 秒**；第 9 轮之前 `setInterval` 的活口数从**逐轮 +1**（第 3 轮就 3 个）压到**恒 ≤2**（倒计时 + 哄睡拍子灯）。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：改动只在 `mountEndless` 内部，闯关一条路不经过它。回滚 = `git revert` 这一条。
- **先红后绿**：`marathonClock.test.ts`「连开 8 轮，每一轮都还是 1 秒／秒」在旧代码上报 `第 2 轮走一拍只该掉 1 秒: expected 32 to be 33`；「秒表数量不随轮次涨」报 `第 3 轮开局挂了太多循环: expected 3 to be less than or equal to 2`。

---

#### W5-L-23 · `Life.every()` 交回一个能单独停掉的把手 —— ✅ 本轮落地

- **来源思路**：同上那条规矩的「工具层」一半。生命周期容器该同时支持**批量收**（`destroy`）与**单点收**（句柄），而且句柄要 fail-safe：容器已经死了也照样交一个「死把手」回去，调用方不必到处判空。
- **现状**：`Life.every()` 返回 `void`，`loops` 是私有 `Set`，外面没有任何单独取消的入口。
- **落地方式**：新增 `interface Loop { stop(): void; readonly live: boolean }`；`every()` 返回一个闭在 `loops`/`host` 上的把手，`stop()` 幂等（`Set.delete` 返回 false 就直接收手），`live` 直接问 `Set`。`destroy()` 之后返回常量 `DEAD_LOOP`。**签名从 `void` 改成 `Loop` 是纯追加**，既有那两处不接返回值的调用一个字都不用改。
- **收益量化**：`kitty-care` 目录内 2 处 `life.every` 全部接上了把手（马拉松倒计时 + 哄睡拍子灯），原先能单独停掉的是 **0 处**。
- **受益游戏 id**：`kitty-care`（本档唯一用 `Life` 的一款；`runtime.ts` 是它自己的目录文件，不是平台文件）。
- **风险与回滚**：纯追加，既有 `Life` 用例（`upgrade12.test.ts` 那条 `life.every(() => fired++, 100)`）零改动仍绿。
- **想推广但本轮不做**：见 W5-L-37。

---

#### W5-L-24 · 倒计时徽章 fail-closed，哨兵值一律画成「这一轮先跳过」 —— ✅ 本轮落地（测试员 W5C-K02，一般）

- **来源思路**：**「屏幕是最后一道防线」**——任何要渲染给用户的数值，渲染函数自己就该把 `Infinity` / `NaN` / 负数挡下来，而不是指望上游永远不传错。这一条在数据可视化与仪表盘类开源项目里几乎是标配（格式化函数先 `Number.isFinite` 再动手）。
- **现状**：超时分支用 `left = Number.POSITIVE_INFINITY` 防重入，`paint()` 直接 `⏳ ${Math.max(0, left)} 秒`，于是徽章上真的画出 `⏳ Infinity 秒`，一直挂到 600ms 后新一轮重置。
- **落地方式**：两层一起改。① 语义层：超时那一下改成 `left = 0` + `stopClock()`，不再靠哨兵值防重入（表都停了，自然不会重入）；② 渲染层：新增纯函数 `endlessClockText(left)`，只画得出有限的正整数秒，其余一律返回常量 `ENDLESS_SKIP_BADGE = "⏳ 这一轮先跳过"`。
- **收益量化**：一整轮采样（≈34 个采样点）里 `Infinity` / `NaN` / `undefined` 出现次数从 **1 次**降到 **0 次**；超时后再走两拍，徽章不会往负数跑。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：`ENDLESS_SKIP_BADGE` 的措辞过了红线扫（不含「输 / 失败 / 来不及」），与 `endlessTimeout()` 那句「这一件慢了一点点，没关系」口径一致。
- **先红后绿**：把 `paint` 换回原来的模板字符串，用例立刻报 `倒计时徽章上出现了哨兵值: expected '⏳ Infinity 秒' not to match /Infinity|NaN|undefined/`。

---

#### W5-L-25 · 舞台自己收掉哄睡的拍子灯 —— ✅ 本轮落地（**本轮读代码另外找到的**）

- **来源思路**：和 W5-L-22 同源。既然 `Life` 是**整局共用**的，那么「一件事」起的循环就该跟着这件事收，而不是留给整局结束时的大扫除。
- **现状**：`renderSleep()` 里 `this.life.every(…, 80)` 每次渲染都新挂一个，`Arena.destroy()` 只是 `dead = true` + 摘节点，循环仍挂在共用的 `Life` 上空转。马拉松里每跑一遍哄睡（每 7 轮一次）就多留一个；躲纸箱回来重画哄睡也会再多一个，而且旧的那个还在 toggle 已经被丢弃的节点。
- **落地方式**：`Arena` 加一个 `loops: Loop[]` 与 `stopLoops()`，`renderTask()` 开头与 `destroy()` 各调一次；`renderSleep` 改成 `this.loops.push(this.life.every(…))`。另加一个 `get liveLoops()` 供用例问。
- **收益量化**：连开 9 轮马拉松，`setInterval` 活口从最多 **3 个**（倒计时 + 两个僵尸拍子灯）压到 **≤2 个**；退出后仍是 0。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：`upgrade12.test.ts` 那条「destroy 之后 timer / rAF 一个不剩」原样仍绿（它本来就靠 `life.destroy()` 兜底，现在只是提前收）。

---

#### W5-L-26 · 看病做岔那句针对性的话不许被通用提示盖掉 —— ✅ 本轮落地（测试员 W5C-K03，一般）

- **来源思路**：交互工程里的**「一次操作只写一次反馈」**。同一个 tick 里对同一个节点写两次文本，浏览器只渲染最后一次；要么把两句合成一句，要么让重绘函数知道「刚刚发生了什么」。开源教育类项目常见做法是把「上一步的结果」当参数传进重绘函数，而不是让重绘函数无脑回落到默认文案。
- **现状**：`renderCure` 的 `draw()` 每次都无条件 `this.say(cureHint(state))`；点错之后 `settle()` 先写了「先看一看再动手，顺序反了它会紧张～」，紧接着 `draw()` 又把那句谁都一样的通用提示写上去，**第一句根本没机会渲染**。做对时的「第 N 步做好了，接着来～」同样被盖掉。看病是本款唯一有「顺序」概念的任务，恰恰最需要即时反馈。
- **落地方式**：新增纯函数 `cureMessage(note, hint, miss)`，三条口径：做岔了只留针对性的那句；做对了把「第 N 步做好了」和下一步的类型提示接起来；没有新话要说才给通用提示。`draw(note?, miss?)` 把刚刚那一下带上，三个调用点（选工具、退一步、首次绘制）分别传。
- **收益量化**：做岔一次之后 `.ktc-msg` 的最终内容从「通用提示」变成「针对性指导」，孩子能看到的**有效反馈从 0 句变成 1 句**；做对一步从只剩通用提示变成「结果 + 下一步类型」两段都在。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：不改任何判定，只改这一行字怎么拼。`cureMessage` 有 6 条口径用例单独钉。
- **先红后绿**：三条用例在旧代码上分别报 `expected '先别急着动手——挑一样「看一看」…' to be '先看一看再动手，顺序反了它会紧张～'`、`expected '看清楚啦…' to contain '第 1 步做好了，接着来～'`、`expected '先别急着动手…' to contain '退回第 1 步'`。

---

#### W5-L-27 · 看病途中心情掉光时，别把安抚按钮又画回护理台 —— ✅ 本轮落地（**本轮读代码另外找到的**）

- **来源思路**：同一处的姊妹问题。`settle()` 在心情归零时会把舞台整块换成「📦 钻进纸箱 + 🤲 轻轻摸摸它」，调用方紧接着的重绘必须知道「舞台已经被别人接管了」。
- **现状**：`settle(res, row)` 之后无条件 `draw()`，于是在暖心诊所 + 心情条那几关（第 12 关起 `moodStart/moodMax` 都在），做岔到心情归零的那一下，纸箱画面会被护理台立刻盖回去——安抚按钮消失，猫还在 `hiding` 状态。
- **落地方式**：一行守卫 `if (!res.done && !this.cats[this.targetCat()].hiding) draw(…)`。
- **收益量化**：受影响的关卡是**第 8 章暖心诊所里带心情条的 11 关**（`t >= 12`）加上第 10 章末段带心情条的搭配关；改完这些关里心情归零走的路和其余任务完全一致（纸箱 → 摸三下 → 接着做）。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：单行守卫，回滚即恢复。
- **本轮没做完的那一半**：见 W5-L-36。

---

#### W5-L-28 · 洗澡的无指针兜底认「还没搓过的格子」，不认下标 —— ✅ 本轮落地（测试员 W5C-K04，一般）

- **来源思路**：可达性工程里的一条：**兜底路径必须自己能走到终点**，不能只是「看起来动了一下」。判断依据要用**状态**（这一格搓过没有）而不是**游标**（我数到第几个了），游标一旦被夹住就会原地打转。
- **现状**：兜底是 `auto += 2` 再 `Math.min(state.cells.length - 1, auto)` 夹住。步进 2 只碰得到一半格子，`auto` 越过末格之后每一下都在重复搓最后那一格 —— 覆盖率封顶 **53%**，走这条路永远洗不完（测试员实测点 40 下还是 53%）。
- **落地方式**：新增纯函数 `nextWashCell(state, from)`：从 `from` 起往后找第一个 `cells[i] === false`，到末尾绕回开头，全搓完返回 −1。兜底改成「问它要一格 → 搓它 → 游标挪到下一个」。真手指那条路（`pointerdown` / `pointermove` 走坐标）一个字没改。
- **收益量化**：无指针环境下的覆盖率上限从 **53%** 提到 **≥90%（`WASH_TARGET`）**，第 18 关 6×5 的搓澡区在 **40 下以内**必定完成；期间覆盖率单调不退。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：`scrub()` 的判定、半径、完成阈值全没动，真手指手感不变。
- **先红后绿**：用例在旧代码上报 `点了 40 下还是搓不到九成: expected 53 to be greater than or equal to 90` —— 和测试员手测的 53% 一模一样。

---

#### W5-L-29 · 通关掉了收藏就当场刷新模式条上的相册计数 —— ✅ 本轮落地（测试员 W5C-K05，建议）

- **来源思路**：**「一份数据只留一个真相，改了就通知」**。相册计数在两处显示（模式条按钮、相册页里），只有后者是每次重绘都读，前者只在 `closeMode()` 时刷。
- **现状**：结算里写着「（1/24）」，回地图模式条仍是「📷 小屋相册 0/24」，得进一次模式再退出才对得上。
- **落地方式**：把「掉一件 + 拼结算文案 + 通知」三件事收进 `album.ts` 的 `claimDrop(store, level, onChange?)`；`makePlayLevel(api, onAlbumChange)` 多收一个回调，`mount()` 传自己的 `refreshBar`。掉不到东西（收齐了）就不喊。
- **收益量化**：模式条计数的滞后从「要多进出一次模式」变成 **0 帧滞后**；`claimDrop` 收齐 24 件的全过程只喊 24 次，收齐之后一次都不喊。
- **受益游戏 id**：`kitty-care`。
- **风险与回滚**：`onChange` 有默认空实现，不给回调也照旧能用（有用例钉）。

---

#### W5-L-30 · `poop-hero` HUD 那排小药丸按钮把最小热区钉进 CSS —— ✅ 本轮落地（测试员 W5C-P01，建议）

- **来源思路**：触摸目标下限该是**声明出来的常量**，不是「padding 算下来正好够」的巧合。这一档里摇杆那几个大键早就有 `padMetrics` + `MIN_HOT = 44` 守着，HUD 这排小的却是漏网的。
- **现状**：`.ph-btn` 只有 `padding: 5px 12px` + `font-size: 13px`，窄屏那一档还把 padding 收成 `5px 9px`。测试员在 360×720 量到「⏸ 暂停」**30×32**，宽度差 2px 就低于 32 的下限。
- **落地方式**：`runtime.ts` 新增 `HUD_BTN_MIN_W = MIN_HOT`（44）与 `HUD_BTN_MIN_H = 32`，`.ph-btn` 加 `min-width` / `min-height` + `inline-flex` 居中（不加居中的话撑大之后字会贴左上角）。窄屏那一档只收 padding，不碰 min-*。CSS 常量从 JS 插值进去，用例直接量得到。
- **收益量化**：360px 上「⏸ 暂停」从 **30×32** 变成 **≥44×32**；宽度从**差 2px 不达标**变成**富余 12px**。同屏其余按钮本来就达标，没被撑变形（`min-*` 只抬下限）。
- **受益游戏 id**：`poop-hero`。
- **风险与回滚**：`.ph-btn` 只用在 HUD 与结算面板；结算那几个本来就更大，不受影响。主包 0 字节变化，`poop-hero` chunk +111 B。

---

#### W5-L-31 · `red-blue-tug`「🔥 拼一把」开关的最小高度钉进 CSS —— ✅ 本轮落地（测试员 W5C-T02，建议）

- **来源思路**：同上。这一款的两侧大拉绳按钮有 `SIDE_BTN_MIN = 72` 守着，齿轮条上这颗小的没人管。
- **现状**：`.rbg-toggle` 只靠 `padding: 5px 13px`，测试员在 360px 量到 **107×26**，高度差 6px。
- **落地方式**：`tuning.ts` 的「手机 360px 排版红线」那一节新增 `TOGGLE_MIN_H = 32`，`.rbg-toggle` 加 `min-height` + `inline-flex` 居中。
- **收益量化**：高度从 **26px** 抬到 **≥32px**，齿轮条整体只长 6px（`.rbg-gear` 原本 `min-height: 22px`，本来就有富余）。
- **受益游戏 id**：`red-blue-tug`。
- **风险与回滚**：只抬下限不改宽度，绳场高度（`.rbg-field` 124px）与两侧大按钮一个像素没动。`red-blue-tug` chunk +106 B。

---

#### W5-L-32 · `red-blue-tug` 星星队的键位对齐平台双人约定 —— ✅ 本轮落地（测试员 W5C-T01，建议）

- **来源思路**：**跨游戏的肌肉记忆本身就是一种可用性**。平台约定是「朵朵 `WASD` + `F` + `G`，星星 `↑←↓→` + `L` + `K`」，一款游戏私自换个字母，代价是全库一致性。
- **现状**：`BLUE_KEYS = ["KeyJ", "KeyL"]`，`L` 在约定内，另一个取了 `J` 而不是 `K`；屏幕上三处提示（对战入口说明、双人模式卡片、拉绳按钮副标题）都写「星星按住 J」。
- **落地方式**：不做**替换**做**并集**——`BLUE_KEY_MAIN = "KeyK"`，`BLUE_KEYS = [KeyK, KeyL, KeyJ]`。屏幕上一律改写 `K`（对齐约定），老的 `J` 留在表里继续接管（已经玩熟 `J` 的孩子不会突然按不动）。单人打小电脑时蓝队照旧一个键都不接管。
- **收益量化**：星星那半边与平台约定的重合度从 **1/2**（只有 L）变成 **2/2**；屏幕提示里的 `J` 从 **3 处**降到 **0 处**；接管的键从 2 个变 3 个，`boundKeys(true)` 无重复、`destroy` 一个不漏。
- **受益游戏 id**：`red-blue-tug`。
- **风险与回滚**：既有那条键位用例（`keySideOf("KeyJ", true) === "blue"`、`keySideOf("KeyJ", false) === null`、`boundKeys(false)` 不含 `KeyJ`）**一个字都没改就仍然绿**，这正是选并集而不是替换的理由。

---

#### W5-L-33 · `find-diff` 马拉松清干净一轮之后先停秒表再排下一轮 —— ✅ 本轮落地（**本轮读代码另外找到的**）

- **来源思路**：还是 W5-L-22 那条规矩，换一款游戏再走一遍。凡是「异步换场 + 独立倒计时」的结构，换场的**开头**停表还不够，触发换场的那一刻就该停。
- **现状**：`onCleared` 里 `round++` 之后排一个 450ms 的 `nextId` 就走了，`timerId` 没停。清干净的那一刻要是只剩最后 1 秒，这 450ms 空档里秒表会踩到 0 → `showOver()` 闪一屏「⏰ 这一轮没找完」，明明刚刚才找齐；450ms 后新一轮把它盖掉。不影响成绩（`recordEndlessBest` 已经先记过了），但画面会跳。
- **落地方式**：`onCleared` 里把 `if (nextId) clearTimeout(nextId)` 换成 `stopTimer()`（它同时收倒计时与换轮延时），再排下一轮。一行。
- **收益量化**：马拉松的「换轮空档」从**有 450ms 的窗口能闪失败屏**变成 **0**；全款开倒计时的地方仍然只有 1 处，超时 / 换轮 / 拆场三条路全部经过 `stopTimer()`。
- **受益游戏 id**：`find-diff`。
- **风险与回滚**：`find-diff` chunk **−15 B**（少了一行 `clearTimeout`）。
- **守门强度要说清楚**：这块逻辑长在 `mountEndless` 里、外面拿不到句柄，本款目录也没有 DOM 桩，所以 `endlessClock.test.ts` 守的是**源码结构**（清干净那条路上 `stopTimer()` 必须排在 `nextId = setTimeout` 之前、`stopTimer` 两种句柄都收、全款只有一处开倒计时），不是行为。这一点不遮掩，真机行为**建议第 2 轮测试员复验**。

---

#### W5-L-34 · `find-diff` 第二级提示直接点名答案格 —— ❌ 本轮不落地（测试员 W5C-F01，建议）

- **测试员的判断**：这是**写进攻略的有意设计**（「第一下只圈范围，实在找不到再点第二下」），第二级不消耗提示预算、用完也照样能过关，对观察类玩法不构成教学错误，只登记口径。
- **我的判断**：同意，**不改**。而且它和 `hintStageOf` / `hintsUsed` 两个纯函数是配套的（奇数次圈区域、偶数次才精确指、一次「圈+指」算一次额度），既有用例已经把这套经济钉死了；改它等于推翻一套已经自洽的设计，不符合「小而稳」。
- **建议轮次**：不建议改。若产品侧要改口径，属于设计决策而非缺陷，应从攻略与提示预算一起重定，留给窗口 1 / 产品。

---

#### W5-L-35 · `pinyin-train` 同一题连错两次会闪正确答案 —— ❌ 本轮不落地（测试员 W5C-Y01，建议）

- **现状**：`quiz99.ts` 的 `shouldHint(wrongHere >= 2 || wrongTotal >= maxWrong)` 给正确按钮加 `qz-hint` 闪烁并播报「悄悄提示：一闪一闪的那个就是答案！」。
- **不落地的理由**：`src/games/quiz99.ts` 是**平台共用文件，在本档禁改清单里**。而且它是有意的兜底（源码注释写明「不让最后一次机会踩空」），只在已经错过两次之后才出现。档B 的 W5-L-19 遇到同一个文件时也是同样的处置。
- **交给谁**：**窗口 1**。本档不重复计数、不改。
- **本档 `pinyin-train` 一行没改**：测试员把 188 关 1184 道题整个生成出来逐题复核（788 道独立重推、491 条词表题人工过），零读音错误、零越界、零重复选项，攻略与题干都不泄题。唯一那条问题归 `quiz99.ts`。**不为改而改。**

---

#### W5-L-36 · `kitty-care` 看病途中躲进纸箱，出来之后护理进度清零 —— ❌ 本轮不落地，**建议第 2 轮**

- **现状**：猫从纸箱出来后走 `renderTask()`，而 `renderTask()` 是从头 `renderCure(spec)`——`state = cureStart(round)`，已经做对的步骤全没了。喂饭 / 打扮这类单步任务看不出差别（测试员在第 135 关验的正是打扮，所以现象没露出来），但看病是多步的，4 步护理做到第 3 步躲一次纸箱就得从头来。
- **不落地的理由**：修它要给 `Arena` 加「任务内部状态跨重绘保留」的机制，七种任务都要过一遍——这是**重构**，不是小补丁，本轮的规矩是不做大重构。W5-L-27 先把更急的那一半（纸箱画面被盖掉）收了。
- **建议轮次**：**第 2 轮**，做法建议是让 `renderTask()` 支持「恢复」而不是「重来」（把每种任务的 state 提到 `Arena` 上，重绘只读不重建），并且先给七种任务各补一条「躲纸箱回来进度还在」的用例再动手。
- **不影响红线**：进度清零只是重做一遍，仍然通得过、仍然没有失败结局。

---

#### W5-L-37 · 把「同一时刻只许一个循环」的 `Loop` 句柄推广到平台层 —— ❌ 本轮不落地，**交给窗口1**

- **来源思路**：本轮在**两款**游戏（`kitty-care` 的马拉松、`find-diff` 的马拉松）身上遇到了同一个形状的问题——「异步换轮 + 独立倒计时」。76 款里带无尽 / 马拉松模式的远不止这两款，这正是「一处改动惠及几十款」的那类改进。
- **落地方式（建议）**：在 `src/engine/**` 提供一个 `TimerSlot`（同一时刻只放得下一个定时器，`set` 新的自动收旧的）或让平台的生命周期容器统一返回句柄，再配一条「无尽模式换轮时活着的 interval 数必须恒为常数」的共用用例模板。
- **不落地的理由**：`src/engine/**` 在本档禁改清单里。本轮只在本档目录内把两款各自的门关上。
- **交给谁**：**窗口 1**。本档已经把两处的实现摆出来当参考（`kitty-care/runtime.ts` 的 `Loop`、`find-diff/index.ts` 的 `stopTimer`）。

---

### C2 · 改动文件登记

| 文件 | 改了什么 | 为什么 |
| --- | --- | --- |
| `src/games/kitty-care/runtime.ts` | 新增 `interface Loop` 与 `DEAD_LOOP`；`every()` 返回值从 `void` 改成 `Loop`（纯追加，既有调用点零改动） | W5-L-23 |
| `src/games/kitty-care/index.ts` | `mountEndless` 导出并多一个可选 `timers: TimerHost`（假时钟接得进来）；新增 `clock`/`stopClock()`，换轮 / 做完一件 / 超时 / 拆场四处都先停表；超时改 `left = 0` 不再用哨兵；徽章改走 `endlessClockText`；`makePlayLevel(api, onAlbumChange)` 与 `claimDrop` 接线 | W5-L-22 / 24 / 29 |
| `src/games/kitty-care/endless.ts` | 新增 `ENDLESS_SKIP_BADGE` 与纯函数 `endlessClockText()` | W5-L-24 |
| `src/games/kitty-care/arena.ts` | 新增 `loops` / `stopLoops()` / `liveLoops`，`renderTask()` 与 `destroy()` 各收一次，哄睡拍子灯登记进去；`renderCure` 的 `draw(note?, miss?)` 改走 `cureMessage`，三个调用点分别传；心情归零时不再画回护理台；洗澡兜底改用 `nextWashCell` | W5-L-25 / 26 / 27 / 28 |
| `src/games/kitty-care/tasks.ts` | 新增纯函数 `cureMessage()` 与 `nextWashCell()` | W5-L-26 / 28 |
| `src/games/kitty-care/album.ts` | 新增 `claimDrop(store, level, onChange?)`，把「掉落 + 文案 + 通知」收在一处 | W5-L-29 |
| `src/games/kitty-care/marathonClock.test.ts`（新增） | 7 条：开局 1 秒/秒、连开 8 轮都 1 秒/秒、循环数恒 ≤2、超时不画哨兵值、拆场归零、`endlessClockText` 口径、`Life.every` 把手 | W5-L-22 / 23 / 24 / 25 的守门 |
| `src/games/kitty-care/careFeedback.test.ts`（新增） | 11 条：看病做岔 / 做对 / 退一步三条屏幕文案 + `cureMessage` 六口径、洗澡兜底点到 ≥90% + `nextWashCell` 三条、`claimDrop` 两条、动物福利红线两条 | W5-L-26 / 27 / 28 / 29 的守门 |
| `src/games/poop-hero/runtime.ts` | 新增 `HUD_BTN_MIN_W`（= `MIN_HOT` 44）与 `HUD_BTN_MIN_H`（32） | W5-L-30 |
| `src/games/poop-hero/index.ts` | `const CSS` → `export const PH_CSS`（用例要量得到）；`.ph-btn` 补 `min-width` / `min-height` / `inline-flex` 居中，数值从常量插值 | W5-L-30 |
| `src/games/poop-hero/touchTargets.test.ts`（新增） | 3 条：常量守 32 下限、`.ph-btn` 真的写了 min-*、窄屏那几档不许把 min-* 改小 | W5-L-30 的守门 |
| `src/games/red-blue-tug/tuning.ts` | 「手机 360px 排版红线」一节新增 `TOGGLE_MIN_H = 32` | W5-L-31 |
| `src/games/red-blue-tug/runtime.ts` | 新增 `BLUE_KEY_MAIN = "KeyK"`，`BLUE_KEYS` 改成 `[KeyK, KeyL, KeyJ]`（并集，不是替换） | W5-L-32 |
| `src/games/red-blue-tug/index.ts` | `const CSS` → `export const RBG_CSS`；`.rbg-toggle` 补 `min-height` + 居中；三处屏幕提示 `星星按住 J` → `星星按住 K` | W5-L-31 / 32 |
| `src/games/red-blue-tug/touchTargets.test.ts`（新增） | 4 条：`TOGGLE_MIN_H` 与 CSS 对得上、K 与 L 都接管得到、老 J 继续算数且单人不接管、源码里再没有「星星按住 J」 | W5-L-31 / 32 的守门 |
| `src/games/find-diff/index.ts` | `onCleared` 里 `clearTimeout(nextId)` 换成 `stopTimer()`（一行） | W5-L-33 |
| `src/games/find-diff/endlessClock.test.ts`（新增） | 3 条源码结构守门 | W5-L-33 的守门 |
| `docs/qa/1.2-window5-round1-learner.md` | 本报告（**只写档C 节**） | 本轮产出 |

**没碰**：`src/ui/**`、`src/engine/**`、`src/styles.css`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`、`package.json` / `package-lock.json`、本档以外的任何游戏目录、`docs/qa/1.2-window5-round1-tester.md`、`docs/qa/1.2-window5-round1-fixer.md`、本文件的**档A 节与档B 节**。**没有引入任何依赖**（`npm install` 只是把已有的 `package-lock.json` 装回来，锁文件零改动）。**没有动任何存档 key**。`pinyin-train` **一行没改**，理由见 W5-L-35。`find-diff` 只动了一行，且不是测试员点名的那条（W5C-F01 明确不改）。

### C3 · 验收

| 项 | 基线（`47532a3`） | 本轮收尾 | 结论 |
| --- | --- | --- | --- |
| `npm test` | 204 文件 / 5444 用例全绿 | **209 文件 / 5472 用例全绿**，0 失败 0 跳过，34.9s | ✅ 用例只增不减（**+5 文件 / +28 条**） |
| `npm run build` | 全绿 | **全绿**，`tsc --noEmit` 0 错，`✓ built in 336ms`，PWA precache 139 项 / 3293.78 KiB | ✅ |
| `vite preview` 冒烟 | — | `/` `/sw.js` `/manifest.webmanifest` 全 **200** | ✅ |
| 主包 `index-*.js` | 86801 B | **86801 B** | ✅ 一个字节没涨 |
| `kitty-care` chunk | 66248 B | 67154 B（**+906 B**） | ✅ 四条修复 + 两个新纯函数的代价 |
| `poop-hero` chunk | 59934 B | 60045 B（+111 B） | ✅ |
| `red-blue-tug` chunk | 34223 B | 34329 B（+106 B） | ✅ |
| `find-diff` chunk | 29036 B | **29021 B（−15 B）** | ✅ 少了一行 `clearTimeout` |
| `pinyin-train` chunk | 54052 B | **54052 B（±0）** | ✅ 本轮没改它 |

新增的 28 条用例分布：`kitty-care` +18（`marathonClock` 7 + `careFeedback` 11）、`red-blue-tug` +4、`find-diff` +3、`poop-hero` +3。

**既有断言被迫修改：0 处。** 五款的既有用例一条都没删、一条都没改——这是本轮选实现方案时的硬约束，最典型的是 W5-L-32 选「并集」而不是「替换」，就是为了让 `keySideOf("KeyJ", true) === "blue"` 那三条老断言原样仍绿；`Life.every()` 的返回值从 `void` 改成 `Loop` 也是纯追加，`upgrade12.test.ts` 里那两处不接返回值的调用零改动。

**先红后绿的证据**（每条落地都在旧代码上跑过一遍）：

| 用例 | 在旧代码上的失败信息 |
| --- | --- |
| 连开 8 轮都 1 秒／秒（W5-L-22） | `第 2 轮走一拍只该掉 1 秒: expected 32 to be 33` |
| 循环数恒 ≤2（W5-L-22 / 25） | `第 3 轮开局挂了太多循环: expected 3 to be less than or equal to 2` |
| 超时不画哨兵值（W5-L-24） | `倒计时徽章上出现了哨兵值: expected '⏳ Infinity 秒' not to match /Infinity\|NaN\|undefined/` |
| 看病做岔留针对性的话（W5-L-26） | `针对性的指导被通用提示盖掉了: expected '先别急着动手——挑一样「看一看」…' to be '先看一看再动手，顺序反了它会紧张～'` |
| 看病做对接上下一步提示（W5-L-26） | `expected '看清楚啦，挑一件温柔的日常照顾。' to contain '第 1 步做好了，接着来～'` |
| 退一步留「退回第 N 步」（W5-L-26） | `expected '先别急着动手…' to contain '退回第 1 步'` |
| 洗澡兜底搓得完（W5-L-28） | `点了 40 下还是搓不到九成: expected 53 to be greater than or equal to 90` —— 和测试员手测的 53% 一模一样 |

### C4 · 对测试员档C 那 10 条问题的处置

| 编号 | 级别 | 本轮处置 |
| --- | --- | --- |
| W5C-K01 马拉松秒表逐轮叠加 | **严重** | ✅ **落地**（W5-L-22，配套 W5-L-23 的可取消句柄），先红后绿 |
| W5C-K02 闪「⏳ Infinity 秒」 | 一般 | ✅ **落地**（W5-L-24，语义层 + 渲染层两道） |
| W5C-K03 看病做岔提示被盖掉 | 一般 | ✅ **落地**（W5-L-26），另顺手收了同一处的 W5-L-27 |
| W5C-K04 洗澡兜底封顶 53% | 一般 | ✅ **落地**（W5-L-28），真手指那条路一个字没改 |
| W5C-K05 相册计数不刷新 | 建议 | ✅ **落地**（W5-L-29） |
| W5C-P01 暂停键 30×32 | 建议 | ✅ **落地**（W5-L-30） |
| W5C-T01 星星队第二键取了 J | 建议 | ✅ **落地**（W5-L-32，并集不替换） |
| W5C-T02「拼一把」107×26 | 建议 | ✅ **落地**（W5-L-31） |
| W5C-F01 第二级提示点名答案格 | 建议 | ❌ **不落地**，测试员判为有意设计、我同意，理由见 W5-L-34 |
| W5C-Y01 连错两次闪答案 | 建议 | ❌ **不落地**，`quiz99.ts` 是禁改文件，**交给窗口1**（W5-L-35） |

**严重 1 条已清，一般 3 条已清，建议 6 条清 4 条**（剩下 2 条一条是有意设计、一条归平台）。另外本轮读代码自己找出并落地了 3 条测试员没记的（W5-L-25 舞台循环累积、W5-L-27 纸箱画面被盖、W5-L-33 `find-diff` 换轮闪失败屏）。

### C5 · 动物福利红线复核（本档专项）

改的四处（马拉松秒表、看病反馈、洗澡兜底、相册计数）**没有一处碰判定，只碰反馈与生命周期**，所以红线原样成立，并且补了两条回归钉住：

- **无失败结局**：`kitty-care` 全目录仍然**一处 `ctx.lose` 都没有、一处 `onLose(` 都没有**（新增用例逐文件扫）。马拉松超时照旧只是「这一件慢了一点点，没关系——小猫已经跑去下一件事啦！」，星级仍保底 1 星。
- **无伤害 / 无死亡**：本轮新写的每一句话（`ENDLESS_SKIP_BADGE`、三种 `cureMessage` 组合）都过了 `打针|喂药|伤口|手术|流血|受伤|死|失败|输了|笨|快点` 的词扫。
- **看病词表**：护理柜 12 件、五种轻症、`CURE_SAFETY_LINE` 一个字没动，既有那条「症状与处置全表只有温和的日常照料」的用例仍绿。W5-L-26 只改了**这一行字怎么拼**，没有往词表里加任何词。
- **心情归零仍走纸箱**：W5-L-27 让它在看病任务里也真的走得到（原先纸箱画面会被护理台盖回去），是**把红线兜得更严**，不是放松。
- **商标扫**：本轮新增的标识符与文案（`Loop` / `endlessClockText` / `cureMessage` / `nextWashCell` / `claimDrop` / 「这一轮先跳过」）都过了本款自带的商标与低幼措辞黑名单。

### C6 · 明确跳过的部分

- **窗口 2 点名五项**（`gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi`）：不在本档范围，没碰。
- **平台侧三条**：`quiz99.ts` 的闪答案兜底（W5-L-35）、`Loop` 句柄推广到 `src/engine/**`（W5-L-37）、构建期那条 `[INEFFECTIVE_DYNAMIC_IMPORT] src/ui/parentAuth.ts` 告警（档A 已登记交窗口1，本档不重复计数、不改）。
- **大重构**：W5-L-36「任务内部状态跨重绘保留」建议第 2 轮，本轮只收了更急的那一半。
- **`find-diff` 的行为级验证**：W5-L-33 本轮只有源码结构守门，本款目录没有 DOM 桩、马拉松也没有可注入的句柄，建议第 2 轮测试员在真机上复验换轮不闪失败屏。
- **fixer 报告与第 2 轮**：按要求没有写、没有开始。

### C7 · 收尾复验（合并态）

收尾 `git fetch origin game-1.2-window5` 时远端与本地同 SHA，**没有新的他人提交要 rebase**（档A / 档B 的三位同事本轮已收尾，最后一笔是 `47532a3`）。本档的改动与档A 五款（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）、档B 五款（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`）**零文件重叠**；报告文件只在末尾追加档C 一节，档A / 档B 的段落一个字节都没动。

合并态即当前分支态：`npm test` **209 文件 / 5472 用例全绿**、`npm run build` 全绿、`vite preview` 三项全 200、主包 **86801 B** 未变。

本轮不写 fixer 报告，也不开第 2 轮。
