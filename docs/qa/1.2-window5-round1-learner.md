# 窗口5 · 第1轮学习优化员

> 本文件由三名学习优化员分档共写。**只改自己那一节**，别人的节一个字都不要动。
> 范围仅限窗口 5 独占的 15 款游戏；平台文件（`src/ui/**`、`src/engine/**`、`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts`）与 76 款全集口径都不在本轮范围内。
> 原计划第 27–29 步的全局三人组已取消，本文只按窗口 5 的口径写。

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

### A0 · 调研与落地计划（动代码前登记）

**角色**：窗口 5 · 本窗三轮验收 · 第 1 轮 · 档A 学习优化员
**分支**：`game-1.2-window5`（在已有分支上干活，不另开分支）
**基线**：`ba90919`（对齐 `origin/game-1.2-window5`），`npm test` 194 文件 / 5379 用例全绿，`npm run build` 全绿。
**输入**：`docs/qa/1.2-window5-round1-tester.md` 的档A 节（阻断 0 / 严重 0 / 一般 5 / 建议 2），以及 `docs/plan-1.2-step27-B-learner.md` 的方法论（数字与范围按本窗口提示词覆盖）。

**权限边界**：只改 `src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**` 与本文件。不改测试员报告，不改平台文件，不改本档以外的游戏，不引入任何外部运行时依赖，CSS 前缀沿用各款已有前缀，用例总数只增不减。

**这一轮打算列 10 条、落地 5 条**，逐条见 A1。落地那几条的共同标准：改动小、不推翻既有玩法手感、每条带守门用例、失败可以靠 `git revert` 单条回滚。

### A1 · 十条可落地的改进（详表待回填）

_（本节在计划提交时只列题目，落地后回填「来源思路 / 现状 / 落地方式 / 是否本轮落地 / 受益游戏 id / 风险与回滚」。）_

| 编号 | 一句话 | 本轮落地 |
| --- | --- | --- |
| W5-L-01 | `clock-house` 题面钟面的 `aria-label` 不许把答案念出来 | 计划落地 |
| W5-L-02 | `word-garden` 成语释义与偏旁义项词不许含被挖掉的答案字 | 计划落地 |
| W5-L-03 | `landlord-cards` / `red-blue-race` 的模式入口与让分开关补到 44px 热区 | 计划落地 |
| W5-L-04 | `color-fun` 自由画室是浮层，要把底下的选关地图关进 `inert` 并接管焦点 | 计划落地 |
| W5-L-05 | `color-fun` 自由画室支持 Esc 关闭（对齐平台弹窗的关掉习惯） | 计划落地 |
| W5-L-06 | 「题面不许泄答案」抽成全库共用的守门工具 | 不落地（共用文件，交给窗口1） |
| W5-L-07 | 44px 热区抽成平台级 CSS 变量 / 工具类 | 不落地（共用文件，交给窗口1） |
| W5-L-08 | `red-blue-race` 战役对手的动态难度（追赶时手抖、领先时收力） | 不落地（动手感，留第 2 轮） |
| W5-L-09 | `clock-house` 错题回顾的「同类换数字」覆盖率做成可回放的种子用例 | 不落地（留第 2 轮） |
| W5-L-10 | `landlord-cards` 三档牌力提示的「不泄底」守门用例 | 不落地（留第 2 轮） |

### A2 · 改动文件登记

_（待回填。）_

### A3 · 验收

_（待回填。）_

## 档B · 五款（待填写）

## 档C · 五款（待填写）
