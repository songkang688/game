# 窗口 2 · 1.2 三轮验收 第 2 轮 · 学习优化员 · 汇总

> 各包只写自己那一节，别的节留给对应的包自己填。

## Pack A

> 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
> 详版：[`1.2-window2-round2-learner-packA.md`](./1.2-window2-round2-learner-packA.md)
> 模型：`claude-opus-5-thinking-high-fast`

**列出 12 条改进，落地 9 条**（计划里说好落 6 条，做下来 `L2A-7` `L2A-8` `L2A-11a` 也够得着，一并落了）。

| # | 款 | 一句话 | 编号 | 落地 |
|---|---|---|---|---|
| L2A-1 | fruit-stack / pool-stars / chess-garden | `destroy` 把注进 `document.head` 的样式一起带走（注一次 + 引用计数） | `PA-PS-3` `PA-CG-3` | ✅ |
| L2A-2 | chess-garden | `Esc` 变成开关，再按一次退出暂停，恢复后电脑接着想 | `PA-CG-1` | ✅ |
| L2A-3 | chess-garden | 取消键 —— 朵朵 `G`、星星 `K` 把选中的子放回去，按座位认人 | `PA-CG-2` | ✅ |
| L2A-4 | dot-maze / chess-garden | 360px 热区：`.dmz-btn` 与 `.cg-tool` 补到 44px，字仍竖直居中 | `PA-DM-1` `R2-PA-1` | ✅ |
| L2A-5 | fruit-stack | `G` / `K` 把落点收回盆正中央；`W` / `S` 写明不用；**暂停期间投放键不再偷偷落果** | `PA-FS-2` | ✅ |
| L2A-6 | chess-garden | 残局连胜里主动认输给一句单独的收场话，不再和被翻盘共用 | — | ✅ |
| L2A-7 | pool-stars | 瞄准键也按座位分，两人互相拨不动对方那一杆；单人局两套都归真人 | `PA-PS-2` | ✅ |
| L2A-8 | junqi-camp | 暂停期间点格子 / 挪光标 / 确认 / 取消一概不接，选好的落点不再被丢掉 | `PA-JQ-3` | ✅ |
| L2A-11a | dot-maze | 取消键撤回还没到路口的转向（新增 `clearTurn`）；`F` / `L` 攻略里写明不用 | `PA-DM-3` | ✅ |
| L2A-9 | chess-garden | 残局连胜第 10 局起对手封顶、题面池只有 35 个 | `R2-PA-4` | ❌ 属难度 / 内容重标定 |
| L2A-10 | chess-garden | 360px 上 `.cg-sq` 一格实宽 ≈ 42.5px | `R2-PA-3` | ❌ 8×44 在 360px 上物理排不下 |
| L2A-11b | dot-maze | `meta.blurb` 把豆子叫「小星星」 | `PA-DM-2` | ❌ 上屏文案要连攻略一起统一 |
| L2A-12 | fruit-stack / chess-garden | `merge.ts` 注释口径、`meta.blurb` 的「死角」 | `PA-FS-3` `PA-CG-4` | ❌ 同属文案口径 |

**改了 22 个文件**：五款游戏目录内的 `index.ts` / `view.ts` / `logic.ts` / `guide.ts` / `domStub.ts` / 测试，
外加 `src/styles.css` 里只属于 chess-garden 的 `.cg-tool` 一条规则。

**顺手抓到的新漏子**：fruit-stack 暂停期间按 `F` / `L` 照样能把果子投下去，遮罩盖着的时候果子在背后偷偷落
（不是第 1 轮已清零严重项的回滚，是同一款上另一条线），已随 L2A-5 一起加闸。

**用例只增不减**：本包 5 款从 **717 例增到 747 例（+30）**，另有 10 条【已知问题】用例就地翻面
（断言从「记录当前的坏行为」改成「校验修好后的行为」），既有断言一条没删、没放宽。

**第 1 轮已清零严重项复查**：fruit-stack 窄屏 44px、pool-stars `Esc` 暂停吞输入、junqi-camp 双人各管各的光标，
三条全部保持；第 1 轮学习优化员的 `L-A-1`…`L-A-6` 一条没回滚。

`npm test` **251 文件 / 6431 例全绿**；`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）。
没碰 `src/engine/**`、窗口 1 平台文件、别人的 10 款，也没碰第 2 轮包 B 的 3 条严重项；
没引外部依赖、没有 three.js / CDN；商标黑名单 0 命中，角色只有朵朵 / 星星，无血无死亡描写。
## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 完整明细：[`docs/qa/1.2-window2-round2-learner-packB.md`](./1.2-window2-round2-learner-packB.md)
- 基线：`origin/game-1.2-window2`
- 结论：**列了 10 条，本轮落地 6 条，记录 4 条（其中 2 条记给主管）。**
- 测试员本轮记的 3 条严重（R2B-1 / R2B-2 / R2B-3）留给第 2 轮修复员，**一个字节都没碰**；
  第 1 轮已清零的 B-2（`tap-tiles` 打平判平局）、B-6（`hop-pads` 按指号分台）与第 1 轮落地的 8 条改进全部仍生效。
- 新增 2 个测试文件 / 31 条用例，既有断言一条没删、没放宽。

### 改进清单

| # | 款 | 改进 | 本轮落地 | 收益（实测） |
|---|---|---|---|---|
| P2B-1 | dark-chess · hop-pads · tap-tiles · hue-hand | 档名 `菜鸟 / 地狱` → `新手 / 大师`，`Tier` id 一个字母不动 | ✅ | 两个偏冲的词换掉，存档零影响 |
| P2B-2 | tap-tiles | 同谱对战给中间两档加确定性的「良好」失手，最高档仍一下不差 | ✅ | 前八局原有六局至少两档同分，现在八局全部严格递增 |
| P2B-3 | hue-hand | 无尽重开换一批牌；发牌前先校验玩家开局接得上 | ✅ | 对战 36 组合原有 3 组开局一张接不上，现在 216 个样本 0 个 |
| P2B-4 | hop-pads | 蓄满力换色 + 闪一句提示 + 画布挂 `data-full` | ✅ | 不用再靠「飞过头」才知道按久了；判定一行没动 |
| P2B-5 | dark-chess | 顶栏补一枚「再 N 手不吃不翻就算和」倒数 | ✅ | 不再「突然就结束了」；规则与结算文案未改 |
| P2B-6 | fight-king | 360px 最小字号 11px → 12px（帧数读数、触屏钮名） | ✅ | 纯 CSS，不影响任何一局胜负 |
| P2B-7 | fight-king | `foeIdOf` 跨章撞回同一位对手（第 1 关与第 92 关都是星星） | ❌ | 会换掉一大批关的对手，修复员正拿胜率表量 R2B-3 |
| P2B-8 | fight-king | 无尽连胜天花板落在第 12 场上下，中间六七场既无梯度也见不到头 | ❌ | 同上；星奖梯度牵涉全平台刷分口径 |
| P2B-9 | hop-pads | 方向键 / `WASD` 在单键蓄力玩法里没有语义 | ❌ | 口径题，要主管定 |
| P2B-10 | fight-king | 训练场进不了 `meta.modes`，首页玩法芯片筛不到 | ❌ | 平台侧枚举，按纪律不碰 `src/engine/**`，记给主管 |

### 改了哪些文件

`dark-chess/{ai,guide,levels,index,view}.ts` · `hue-hand/{ai,guide,rules,levels,sim,index}.ts` ·
`hop-pads/{ai,index}.ts` · `tap-tiles/ai.ts` · `fight-king/index.ts`，外加这 5 款各自的 `*.test.ts`
（新增 `tap-tiles/tiers.test.ts` 与 `dark-chess/hud.test.ts`）与两份 `PLAN.md`。
`src/engine/**`、窗口 1 的平台文件、包 A / 包 C 的 10 款一个字没碰。

### 红线

- 商标黑名单 0 命中（5 款 54 个文件拿 42 条商标词逐条扫过，只有 `runOne` 含 `uno` 这类子串误报）。
- 角色只有朵朵 / 星星，本轮没有新增角色名；无血、无死亡，失败只鼓励。
- `npm test` **253 文件 / 6512 例全绿**（基线 `9e44f88` 为 251 / 6481，本包净增 +2 文件 / +31 例），
  本包 5 款单独跑 **38 文件 / 727 例全绿**。
- `npm run build` 全绿，`package.json` / `package-lock.json` 未改。

### 记给主管

- **`菜鸟 / 地狱` 这套档名全仓有 24 款在用**，本包只改得了自己的 4 款，剩下 20 款分属包 A / 包 C 与其他窗口。
  要么由主管把 `新手 / 普通 / 高手 / 大师` 定成全仓口径让各包跟上，要么承认各款自己起名。
- **P2B-10**：`src/engine/types.ts` 的 `GAME_MODES` 里没有 `training`，`fight-king` 训练场因此筛不到。
