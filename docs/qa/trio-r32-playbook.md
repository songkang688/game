# 三人组第 32 轮 · 测试修复员 A/B 任务清单（playbook）

> 依据：`trio-r32-learn-notes.md`。基线 `origin/game-1.3` @ `17356717`。
> 前文：PR #102 r31 … #90 r22；**PR #94** A、**PR #96** B。
> **新伤 N-156…N-158**。下一空号 **N-159**。
> 撞车取主干先合版。学习员零改 `src/**`。

## 〇、进场

1. rebase 最新 `game-1.3`。
2. N-105 禁第四份。N-108 = 拼图无尽画廊。N-99 族只回归。
3. **PR #94 / #96** 合入则对应号只回归。
4. `--k` 族走 N-153–155，勿写成 N-156。仓鼠 `.bh-btn` 走 N-47。C-8 禁改 `SKY_H`。

## 红线

不改存档 / 题库 / seed / 胜负 / 物理。热区 ≥44。三视口。root 走 UI 门。测试只增不减。禁 force。

## 测试步骤

`npm run build && npx vite preview --port 4173`。仓鼠 915 量方向键；钓鱼矮屏量收线；duo-vs 打完一局量结算钮。

---

## A / B 独占

| 工位 | 本轮新号 |
| --- | --- |
| **A** | **N-158** 键格 `grid-auto-rows` 巡检 |
| **B** | **N-156** 仓鼠垫 40；**N-157** `.fs-act` / `.dvs-over button` |

---

## A 面

### P0 · N-158

静态：pad 网格行高 ≥44。`--k` 仍 N-155。

### 其余

N-155/152/149/146/143/138；PR #94。N-99 族回归。

---

## B 面

### P0 · N-156

仓鼠 `@media (max-height:500px)` 的 `grid-auto-rows` 40→≥44，或 `.bh-key` 钉 44。N-80 钉底勿回退。

### P0 · N-157

`.fs-act` 全媒体 `min-height:${TOUCH_MIN_PX}`；`.dvs-over button` 补 44。N-148 芯片原号。

### 仍开不换号

N-153/154/150/151/147/148/144/145；N-135 三款；N-125/126/129；N-108；N-121。

### 只回归

N-12/10/3/55/81/C-8/N-90/N-87/88；N-99/97/100/109。

---

## 完成定义

1. 三视口数字 + 小测试只增不减。
2. vitest 全绿。
3. 交卷 `docs/qa/trio-r32-tester-A.md` / `trio-r32-tester-B.md`。
4. 新伤从 **N-159** 起。
