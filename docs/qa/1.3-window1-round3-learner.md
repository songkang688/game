# 1.3 窗口 1 · 本窗三轮验收 · 第 3 轮（收官）· 学习优化员报告

> 角色：窗口 1 第 3 轮学习优化员（B 档，收官轮）。本轮是收官重评：更新九题材基准表的「本库现状」列，
> 确认 r2 织纹落地后 4A 是否达标，把测试员 R3-1 与跨窗遗留写清去向。
> 依据（只读）：`docs/qa/1.3-window1-round3-tester.md`（进场水位 727 文件 / 15579 例、交卷 728 / 15591；
> 阻断 0 / 严重 0 / 一般 R3-1 = R2-1；72 格全 ✅；R2-2 无回退）、round1 / round2 六份报告、
> `docs/plan-1.3-visual-bible.md`、`docs/qa/1.3-window1-plan.md`。
> 红线：只记结论文字，不下载、不截图入库、不描摹任何具体造型；绘制代码与 `src/art/kit/` 一行不碰；
> 别人的报告一字不碰；唯一可动代码范围 = 本窗九款纯文案 + 为文案补的契约测试（只增不减）。
> 收官纪律：第 2 轮后 12 格里本窗可改的短板已基本清完，**禁止为凑条数而编造必须改的绘制短板**。

## 0. 学习计划（动手前立项，commit `dcc8e3b`）

1. 只读七份必读文档（r3 tester + round1/round2 六份）+ 视觉宪法 + 窗口计划，锚定「72 格全 ✅、
   阻断 0 严重 0、R2-2 无回退」的收官进场事实；
2. 外部学习只学两件事（画面密度 / 角色剪影），九题材基准表的外部基准列沿用 r1/r2，
   「本库现状」列按 r2 织纹落地 + r3 零漂移的实况**收官重写**——重点确认 4A 唯一余量是否清零；
3. 对照评估本窗 12 格，四字段逐格；已达标的格子观感短板写「无」，不编造必改项；
4. 给 C 档的清单按实情出条：R3-1（= R2-1）写明跨窗去向不抢 `level99.ts`；L-3 不动 kit；
   L-4 不动 `bomb-buddies`；L-5 维持观察；若本窗权限内 0 条可修项，明写「收官：本窗绘制无待办」；
5. 文案只补「r2 `.hc-table` 织纹」这类**还没讲进引导且对玩家有用**的亮点；对照 r2 学习员立下的
   「纯背景层次不带玩家信息不入攻略」原则逐一裁决，没有就零改动并写明抽查过哪些文件；
6. 收尾 fetch → rebase → `npx vitest run` / `npx tsc --noEmit` / `npm run build` 全绿后
   普通 push `origin game-1.3-window1`（禁 force、不改 `main`、不推 `game-1.2` / `game-1.2-kk`、
   不用 `gh` 开 PR）；进场水位 728 文件 / 15591 例，只增不减。

---

## 一、九题材密度基准表与剪影经验（收官版「本库现状」）

**学习方法与红线**：调研对象仍为成熟小游戏平台同题材头部作品与公开休闲游戏美术方法论；
本轮是收官对表——外部基准列与三条通用结论（**80/20 密度律** / **休闲档视差规范** 2–3 层甜点区 /
**16px 剪影三律**）自 r1 立下、r2 复核后本轮再核仍然成立，不重抄全文。全程只记文字结论，
无下载、无截图入库、无造型描摹。收官前提：r3 测试员已证明 r2 fixer 交卷（代码 SHA `8ca41a8`）后
本窗 12 格源码**零漂移**（`git diff --stat 8ca41a8..HEAD -- src/` 为空，r3 仅新增测试文件），
故本表「本库现状」= r2 织纹落地后的最终收官态。

### 1.1 密度基准表（外部基准列沿用 r1/r2，「本库现状」按收官实况重写）

| 题材（格） | 外部基准（背景层 / 装饰件 / 动效通道） | 本库现状（收官实况，点名实现） | 差距 |
| --- | --- | --- | --- |
| 吞噬 io（2A） | 3–4 层 / 6–12 件具象 / 4–6 通道 | 4 层 + 独立贴片层（渐变 / 圆斑×7 视差 0.3 / 具象贴片三式 40% 格 × 176px 格边 / 6% 网格 / 条纹墙），贴片预算 240 件/帧（r3 新钉 R3-B）；动效 5+ | **0**（r1 P2 落地，r2/r3 两轮复证无回退） |
| 蛇 io（2B） | 3 层 / 8–14 件贴片 / 4–6 通道 | 4 层（渐变 / 8% 网格 / 远景色岛 0.6 档预算 48 枚 / 贴片 0.85 档预算 360 件），`DECOR_MIN_SIZE=3.2`，视差恰两档（r3 新钉 R3-B） | **0**（r1 P6 落地，两轮复证无回退） |
| 方块消除（2C） | 2–3 层 / 2–4 件（克制） / 4–5 通道 | 3 层（井内渐变 / 井壁四主题浮雕每 6 格一件 `RELIEF_H=12` 离屏缓存 / 网格+井壁五件套），堆高过半仍有装饰 | **0**（r1 P9 落地，两轮复证无回退） |
| 休闲格斗（3A） | 4–5 层 / 8–16 件 / 6–8 通道 | 5 层双视差 0.25/0.6，土丘三阶（base + tint0.3 亮弧 + shade0.2 暗带，r3 逐行重读三笔齐）；`CONFETTI_COUNT=20` 封顶；动效 8+ | **0**（r1 P5 落地，两轮复证无回退） |
| 麻将（3B） | 2–3 层 / 3–6 件 / 3–4 通道，桌布须有 <5% 暗纹 | 3 层 + 3% 斜织纹（`.mj-board`，`mahjong-bloom/index.ts:102`，45°/6px/12px 节距，r3 运行时实测在位） | **0**（r1 P8 落地；真名 `.mj-board`，R2-3 口径） |
| 掷骰地产（3C） | 20–40 件地格图标，与建筑同套画法 | 33 款 20×20 SVG 全覆盖 40 格（`TILE_ICONS/tileIconSVG`），双色阶、与 `houseSVG/coinSVG` 同族；r3 运行时实测 40/40 全 SVG、emoji 仅 aria-label | **0**（r1 P1 落地，两轮复证无回退） |
| 卡牌对战（4A） | 2 层 / 4–8 件 / 3–5 通道，卡面纸感 > 桌面装饰 | 2 层齐备：卡面米白纸感（r1 #6）+ **`.hc-table` 暖褐斜织纹已落地**（r2 R2-2，`hero-cards/index.ts:123`，`rgba(170,120,80,.04)` alpha 恰 4% 顶格、45°/6px/12px 与 `.mj-board` 同套织物语言；r3 源码 + 运行时双取证在位且为多重背景第一层） | **0**（r2 唯一余量本轮确认清零，**4A 达标**） |
| 围棋（4B） | 2–3 层 / 2–4 件 / 2–3 通道（密度上限最低） | 4 层（木渐变 / 5–8% 木纹 / 深木框 / 四角花藤），克制正确；r3 新钉 R3-A「6px/12px 桌面织纹全窗恰 2 处」防织纹被复制进本格 | **0** |
| 飞行棋（4C） | 2–3 层 / 6–12 件 / 4–5 通道，四象限每角 1–2 件低饱和静态件 | 3 层（渐变 / 白格 / 装饰层草簇×4 + 云×2，r3 运行时实测恰 6 件、aria-hidden、pointer-events:none） | **0**（r1 P3 落地，两轮复证无回退） |

**收官结论：九题材密度差距全部为 0**——r1 清掉 4 格短板、r2 清掉最后一项 4A 余量、r3 证明零回退。
本窗 12 格的密度与层次已全部进入外部基准带内，且无一格越过「密度加层次感不是塞满」的上限
（2B/4B/4C 的克制口径由 r2 明立、r3 用预算钉子固化）。

### 1.2 剪影经验（收官复核 = r3 专项③ 0 失分 + 三轮角色资产零触碰）

r2 报告 1.2 节的九行剪影结论本轮逐行复核**全部仍然成立**（r3 测试员专项③「只靠颜色区分 0 款」+
1.1 节零漂移证明 = 检视对象未变），不重抄全表，只列收官要点：

- 形状通道全窗无一缺席：orb 金星/银月头饰、snake 蝴蝶结/棒球帽 + 4 花纹族、block 七形轮廓 + 色弱角标、
  combo 12 款头饰 + 双色光环、mahjong 饼圆/条竹节/万方块字、star-estate 花/星/云/月四立牌、
  hero-cards 14 款帽饰 + 红黑四花色（本窗剪影最强格）、weiqi 黑白明度两极 + 白子暖描边、
  flight-chess 四款尾翼。
- r2 新增的 `.hc-table` 织纹与剪影通道零冲突：alpha 4% 低于牌堆/头像/体力标一阶（眯眼测试口径），
  不构成新的「类角色」形状；r3 的 R3-A 钉子把「织纹永远是多重背景第一层且纯静态」固化为契约。

---

## 二、本窗 12 格对照评估表（收官重评）

口径：密度差距以 1.1 节基准表为准（0=达标）；剪影按 16px 涂黑测试口径；短板 TOP1 已无则写「无」
（收官轮证据 = r3 测试员 72 格全 ✅ + 零漂移证明 + 本轮抽读源码复核）；建议动作只写
「不改 / 跨窗不抢」或可执行规格，不编造必改项。

| 格 | 密度差距 | 剪影达标 | 观感短板 TOP1 | 建议动作 |
| --- | --- | --- | --- | --- |
| 1A kit | N/A（按供给能力评）：中景装饰件供给缺口仍在（`src/art/kit/` 无 `decor.ts`，r3 复核 6 源码 + 6 测试），但本窗九款需求已全部由各游戏自绘满足，受害面 0 | ✅ 朵朵/星星头饰涂黑可辨，三轮零触碰；kit `drawCoin` 高光左上已被 r3 R3-C 钉住 | 无（供给缺口 = L-3 登记项，非本窗画面短板） | **跨窗不抢**：维持 L-3 去向，kit 1A owner 待办；本窗 C 档不动 kit（独占约定） |
| 1B motion | N/A（纯时序契约，入场卡 600ms / 星级 250ms 步进 / 回弹 1.08 均在头部作品节奏区间） | N/A | 无（无绘制面） | 不改 |
| 1C runner | N/A（本窗九款仍 0 引用，棋牌/竞技精 2D 定级正确，基建 58 例自检随全仓全绿） | N/A | 无 | 不改 |
| 2A orb-arena | 0（5 层结构 + 贴片预算 240 件/帧已钉） | ✅ 果冻球头饰+嘴型、金星/银月双人通道 | 无（r1 P2 落地，r2/r3 两轮取证无回退） | 不改 |
| 2B snake-royale | 0（两档视差 0.6/0.85 恰在甜点区，色岛 48 + 贴片 360 预算已钉） | ✅ 蝴蝶结/棒球帽 + 4 花纹族 | 无（r1 P6 落地，两轮取证无回退） | 不改（宪法口径：不加第三档视差） |
| 2C block-drop | 0（浮雕挂壁后中后期一屏不再零装饰） | ✅ 七形块轮廓+亮度阶+色弱角标 | 无（r1 P9 落地，两轮取证无回退） | 不改（浮雕换装亮点已由 r2 讲进 guide 并钉住） |
| 3A combo-clash | 0（5 层双视差，土丘三阶 r3 逐行重读三笔齐） | ✅ 12 款头饰+姿态差分+红蓝光环三通道 | 无（r1 P5 落地，两轮取证无回退） | 不改 |
| 3B mahjong-bloom | 0（毛毡 3% 织纹低于 4% 红线，r3 运行时实测在位） | ✅ 饼圆/条竹节/万方块字类别可辨 | 无（r1 P8 落地；R2-3 真名口径本报告全程沿用 `.mj-board`） | 不改 |
| 3C star-estate | 0（33 款 SVG 图标全覆盖 40 格，r3 实测 40/40 全 SVG） | ✅ 花/星/云/月四立牌头形互异；图标线宽/留白达 r1 规格 | 无（r1 P1 落地，两轮取证无回退；地契列表行首 emoji 属 L-5 文案层观察项，不抬级） | 不改（认图标记路已由 r2 讲进 guide 第一章并钉住） |
| 4A hero-cards | **0（收官确认达标）**：r2 织纹落地后卡面纸感 + 桌面暗纹两项齐备，r3 源码 + 运行时 + 契约三面取证无回退；上一轮的「−1 项」余量清零 | ✅ 14 款帽饰 + 四门花色红黑双通道，本窗最强格 | 无（R2-2 = L-2 已由 r2 fixer `8ca41a8` 落地，本轮为收官确认） | 不改；织纹为纯背景层次不入攻略（第五节裁决） |
| 4B weiqi-garden | 0（4 层克制正确，围棋密度上限最低；r3 R3-A 钉子明令织纹不许复制进本格） | ✅ 黑白明度两极 + 白子暖描边 | 无（r1 P10 落地，测试组织已对齐） | 不改 |
| 4C flight-chess | 0（草簇×4 + 云×2 恰 6 件，落在「每角 1–2 件」基准带内） | ✅ 四款尾翼 + 四色双通道 | 无（r1 P3 落地，两轮取证无回退） | 不改（密度克制：不再往象限加第二批件） |

**整体结论：12 格 12 达标，0 格待改**。r1 十二项修复 + r2 唯一余量 R2-2 经 r3 测试员逐条独立取证
全部无回退；本窗权限内已无任何真实绘制差距，**不为收官轮编造必改短板**。唯一场外项 = 共享壳 R3-1
（`level99.ts` 星级 10–12px），属跨窗文件不计入格判定，去向见第三节。

---

## 三、给 C 档的优先级清单（收官：本窗绘制无待办）

**收官裁决：本窗权限内可修项 0 条。** r3 测试员交 C 档的严重级清单为空、无新增可动手项；
本轮 12 格重评亦未发现真实差距。若统筹仍派本窗 r3 C 档，其正确产出 = 确认零待办 + 合并态复验，
不要为「显得有活干」往达标格加装饰（2B/4B/4C 密度余量为 0，多加即过载）。
以下为跨窗转交与维持项（有几条写几条，共 5 条，均**非**本窗动手项）：

### 转交 1 · R3-1（= R2-1 = L-1 = r1 G-1）共享壳星级 10–12px（一般级 × 跨窗，本窗不抢）

- **位置**：`src/games/level99.ts:512` `.l99-node-stars{font-size:11px}`、`:561` @media 降 10px、
  `:472` `span.l99-star` ★ 字符（本轮复核行号仍准）。
- **现状**：r3 测试员 18 组实测唯一 <14px 命中，九款关卡页同源受累；1.2 逐字遗留非 1.3 回归。
- **去向**：共享关卡壳属壳层 owner / 统筹轮，**本窗任何档位都不要动这个文件**（任务书三轮明令）。
  修法 r1 learner P7 已留档：★ 字符改 12×12 内联 SVG 星形，双态 fill `#ffb937`/`#e3ddef`，
  连带 `.l99-beststars` 与 `.l99-ov-stars` 同族字符星一次改净。严重度维持「一般」。

### 转交 2 · L-3 kit `decor.ts` 供给缺口（kit 1A owner 待办）

`src/art/kit/` 仍无 `decor.ts`（软云 / 花草簇 / 星簇三件套，Canvas + SVG 双出口）。
本窗九款需求已按「各游戏内自绘」满足（r1 P2/P3/P6/P9），无受害面；后续窗口若出现同类中景需求，
建议 kit owner 先补供给再开工，避免各窗继续重复自绘。**本窗 C 档不动 kit（L-3 1A owner 独占）。**

### 转交 3 · L-4 `bomb-buddies` `P_EMOJI` 座位标（他窗 fixer）

仍在（`index.ts:102` `["🌸","⭐"]`，`:423/:562` 引用）。他窗游戏本窗零触碰，维持转交对应窗口 fixer；
r2 fixer 已留全库 `P_EMOJI` 登记清单（bomb-buddies / pool-stars / bumper-cars / bowling-lane /
monster-crisis / fruit-stack，均他窗）可直接取用。

### 维持 4 · L-5 文案层 emoji（观察项，不抬级）

combo-clash 选人卡 `<em>${c.emoji}</em>`（`index.ts:997`）、star-estate 地契列表行首 `${sel.emoji}`
（`index.ts:730`）等维持「文案层可留」口径；若未来全局轮（27–29）要求文案层也画形，
工作量估算已在 r1 fixer L-5 留档（12 角色徽记 + 列表图标复用 `tileIconSVG`）。**维持观察，本窗不动。**

### 已结 5 · R2-3 织纹落点真名（零代码，处置完毕）

r1 fixer 报告笔误 `.mj-table`，真名 **`.mj-board`**（`mahjong-bloom/index.ts:102`）。r2/r3 两轮报告
与本报告全程用真名；后续轮次文档引用照此即可，无任何代码动作。

---

## 四、商标风险提醒清单（收官收窄）

r1 学习员的题材全表（九行「易踩线对象 → 规避要点」）仍全部有效，见
`docs/qa/1.3-window1-round1-learner.md` 第四节，本轮不重抄。收官轮本窗无任何新绘制动作，
风险面收窄为「转交项未来落地时」的三条前瞻提醒：

| 未来工作面（承接方） | 易踩线对象 | 规避要点 |
| --- | --- | --- |
| 共享壳星级 SVG（转交 1，壳层 owner 做时） | 商业平台评星徽章样式（带描边光晕的定制星形、五星连排徽章版式、缎带/桂冠底盘） | 用最朴素五角星几何形（`pathStar` 同族画法），双态只换 fill（`#ffb937`/`#e3ddef`），不加任何徽章化元素 |
| kit `decor.ts` 三件套（转交 2，kit owner 做时） | 商业休闲游戏的标志性云形/草簇画法与配色组合 | 从 `KIT_PALETTE` 派生色出发画抽象几何件；参照本窗已验收的 `grassSVG` / `cloudSVG` 体量与构图即可，不引入新风格源 |
| `bomb-buddies` 座位标画形（转交 3，他窗 fixer 做时） | Bomberman 系角色造型（黑名单词已含） | 座位标改花/星几何徽记（可复用 kit `drawStar`/花瓣画法），不做「圆头白脸炸弹人」式构图 |
| 本轮 B 档已完成面（零代码） | 黑名单词（30 词表，r3 测试员口径） | 本轮零文案改动、零新增字符串，非测试源码 0 命中的收官结论（r3 专项⑥）保持不变 |

沿用总原则：对标质量等级，不对标具体形象；黑名单词零容忍（含注释）；
新装饰一律从 `KIT_PALETTE` 或本款既有色派生。本窗两处桌面织纹均为无图案 45° 平行线族，
无字母/徽记/花型织入，r3 已钉「全窗恰 2 处」防扩散。

---

## 五、本轮文案改动清单：零改动（附抽查清单与裁决理由）

**裁决**：r2 学习员文案巡检（commit `19710a8`）之后，本窗代码只发生过一次绘制改动——
r2 fixer 的 `.hc-table` 暖褐斜织纹（`8ca41a8`），r3 测试员只新增了测试文件。该织纹为**纯背景层次**，
不携带任何玩家可用的玩法信息（不提示决策、不标识角色、不预告事件），按 r2 第五节已立并经契约固化的
原则（「orb 贴片 / snake 色岛 / combo 土丘明暗 / fc 草云 / mj 织纹 / hc 纸感为纯背景层次，不进攻略」，
见 `window1-copy-r2.test.ts` 文件头注释），同类的 hc 桌面织纹**不入引导**——写进攻略即是「为改而改」。
除此之外无任何「还没讲进引导且对玩家有用」的亮点，故本轮**文案零改动、零新增测试**，
水位保持 728 文件 / 15591 例。

**抽查过的文件清单**（逐一核过，全部零改动）：

| 抽查面 | 文件 | 结论 |
| --- | --- | --- |
| 九款攻略 | `orb-arena` / `snake-royale` / `block-drop` / `combo-clash` / `mahjong-bloom` / `star-estate` / `hero-cards` / `weiqi-garden` / `flight-chess` 九款 `guide.ts` | r1 那批「视觉信号 → 玩法含义」讲解（缩圈光点 / 围栏泛红 / 彩虹消行+主题换装 / 红蓝光环 / 圈风罗盘 / 金骰子 / 红黑双门+头像帽 / 涟漪+花瓣 / 四款尾翼）与 r2 补的两处（block-drop 井壁浮雕换装、star-estate 认图标记路）**全部在位**；hero-cards 帽饰认人与红黑卡角已讲透，桌面织纹无需也不应入文；无一处与收官画面冲突 |
| 九款简介 | 同九款 `meta.ts` `blurb` | 全部只讲玩法与情绪，与 r2 织纹等新视觉零冲突，零改动 |
| 文案契约 | `src/games/window1-copy-r1.test.ts`（10 例）、`window1-copy-r2.test.ts`（2 例） | 随全仓复跑全绿，r1/r2 文案钉子无一失守；本轮零改动无需新钉 |

**语言口径自查**：本轮无新增孩子可见文案；既有文案的商标与低幼措辞由 `copy.test.ts` 全量巡检看守
（随全仓全绿）。

---

## 六、收尾数据与验收口径

- 分级红线自查：本轮改动仅本报告一份 docs 文件，代码零触碰——绘制 / 判定 / 数值 / 关卡 /
  `parentAuth.ts` / `parentGate.ts` / `src/art/kit/` 全部 0 diff；无 three.js；未派 fixer、
  未帮他窗改文件、未做全局 27–29。
- 权限自查：他人报告（round1/round2 六份 + round3-tester）、`docs/plan-1.3-*.md`、
  `.cursor/skills/**` 一字未动；`docs/qa/1.3-window1-plan.md` 未动（统筹文件）。
- `npx vitest run`（全仓，合并态）：**728 文件 / 15591 例全绿**——与 r3 测试员交卷水位逐字持平，
  **只增不减成立（本轮 +0，零文案改动无需新钉）**。
- `npx tsc --noEmit`：**0 错误**。
- `npm run build`（tsc + vite build + PWA）：**全绿**，precache 188 项。
- **收尾登记**：`git fetch origin game-1.3-window1` → rebase（origin 无新提交，rebase no-op，
  他人文件零改动）→ 全量三验（上列数字即合并态复跑结果）→ 普通 push `origin game-1.3-window1`
  （无 force、未动 `main` / `game-1.2` / `game-1.2-kk`、未用 `gh` 开 PR）。
- **本轮 commit**：`dcc8e3b`（学习计划）→ 本报告 docs-only commit（SHA 见 git log 顶端）。
  本轮零代码改动，全绿结论对推送 tip 成立；**合并态代码 SHA 维持 `1b77c15`**（r3 测试员用例 commit，
  即当前源码态的最后一个代码 commit）。

**收官总结**：12 格 12 达标、本窗绘制无待办；场外遗留 5 项（R3-1 / L-3 / L-4 / L-5 / R2-3）
全部有主有去向；文案零改动（抽查清单齐备）；水位 728 / 15591 全绿只增不减。
窗口 1 的 1.3 视觉升级三轮验收至此收官。

本轮实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
