# 1.3 窗口 3 · 第 1 轮视觉验收 · 测试员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 1 轮 · A 测试员。
> 分支：`game-1.3-window3`（基线 `origin/game-1.3` @ `b7cd80d`）。只验视觉，不重开玩法验收，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`、`docs/plan-1.3-step27-A-tester.md`（思路）。
> 实测环境：Chromium 151 无头（Playwright chromium-headless-shell），移动仿真 360×640 / 320×568、DPR 2、`prefers-reduced-motion` 双态、CDP CPU 4× 节流。

## 〇、扫描计划（动手前已提交，commit `1faf865`）

1. 全量跑：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；对照「只增不减」。
2. 盘点：9 款目录 × `src/art/kit/` import 关系；plan 文档 vs 实际提交逐款对账。
3. 六大专项（仅 9 款）：火柴人/emoji、金币体积、双人可区分、2.5D 纵深、360px、商标。
4. 横向一致性：光照约定、reduced-motion、destroy 泄漏（进出 ×5）、CPU 4× 帧率。
5. 新增机器化扫描用例（仅 9 款 `*.test.ts`）沉淀给第 2、3 轮。

---

## 一、全量跑数据与盘点结论

### 1.1 全量跑

| 项 | 结果 |
| --- | --- |
| `npm ci` | ✅（Node 22.14.0） |
| `npm test`（全库） | ✅ **705 文件 / 15251 例全绿**（基线 `b7cd80d` 时为 14969 例；只增不减） |
| `npm run build`（tsc --noEmit + vite build） | ✅ 全绿；PWA precache **188 项 / 5215.48 KiB**；`dist/` 共 5.5 MB / 175 个资产 |
| `npx vite preview` 冒烟 | ✅ 首页 200；9 款 chunk 全部 200 |

9 款测试用例数（本分支 vs 基线 `origin/game-1.3`，同机复测）：

| 游戏 | 基线例数 | 现例数 | Δ | 交付时宣称 |
| --- | --- | --- | --- | --- |
| hue-hand | 134 | 158 | **+24** | 24 例 ✅ |
| hop-pads | 137 | 151 | **+14** | 契约测试 ✅ |
| tap-tiles | 147 | 162 | **+15** | 15 例 ✅ |
| gomoku | 277 | 312 | **+35** | 35 例 ✅ |
| match-stars | 147 | 168 | **+21** | 21 例 ✅ |
| rainbow-run | 260 | 285 | **+25** | ✅ |
| ocean-munch | 178 | 214 | **+36** | 36 例 ✅ |
| xiangqi | 263 | 312 | **+49** | 49 例 ✅ |
| fight-king | 290 | 326 | **+36** | 36 例 ✅ |
| **合计** | **1833** | **2088** | **+255** | 无一款减少 |

（上表不含本轮测试员新增的 27 例 `visual-scan.test.ts`，见第八节。）

9 款 chunk 体积（gzip 前）：hue-hand 58.4 kB、hop-pads 43.4 kB、tap-tiles 39.0 kB、gomoku 80.4 kB、match-stars 62.2 kB、rainbow-run 111.6 kB、ocean-munch 110.4 kB、xiangqi 70.6 kB、fight-king 88.7 kB。无 three.js、无二进制素材入库。

### 1.2 kit 归属与越界盘点

- 本分支 **没有 `src/art/`**（窗口 1 的 step1 kit 未合入），按窗口约定 9 款各自带独占 `src/games/<id>/art.ts`——**9 款全部具备**，全库 `rg "from .*art/kit"` 0 命中，无人引用不存在的 kit，也就无人改别人的 kit 文件。
- 越界检查：`git diff --name-only b7cd80d..HEAD` 中 9 款目录与 `docs/qa/` 之外只有统筹的 `docs/plan-1.3-window*.md`、`docs/plan-1.3-{index,roles,windows}.md` 与 `scripts/gen-1.3-window-prompts.py`（提示词工程），**没有任何其他游戏的 `src/games/<id>/` 被动**。

### 1.3 plan 文档 ↔ 实际提交对账（9/9 齐）

| 格 | id | 交付提交（feat/test） | plan 核心项落地 |
| --- | --- | --- | --- |
| 8-A | hue-hand | `c6324cb` `a256568` `57dbb5b` | 卡面四层/功能牌 SVG 图标/花背牌堆/四色符号 ✅ |
| 8-B | hop-pads | `d35066b` `bce7279` `21ff21f` `8c9b99d` `43b8336` `d00fad6` | 角色呆毛裙边披风/台种造型/三层视差+深度雾/完美粒子 ✅ |
| 8-C | tap-tiles | `c4a89e6` `cee8324` `a0ca570` `b648be8` | 星光琴键 sprite/判定爆点/舞台光束/连击上屏 ✅ |
| 9-A | gomoku | `efab747` `575b3c8` `e8509f7` `1733b36` | 精装木盘/落定波纹/漆印/结算仪式/金花 ✅ |
| 9-B | match-stars | `57f7e90` `74aa309` | 星星家族 SVG（六色六形）/绘制机关/彩虹星/三星仪式 ✅ |
| 9-C | rainbow-run | `610c` `4ed1` `885b` `6ca8` `7a96` `9923` `9817` | 星币 8 帧/跑者重画/障碍立面/12 世界剪影/道具去 emoji ✅ |
| 10-A | ocean-munch | `71365cc` `d62b18e` `1856d4d` `f2dbc36` | 三层景深光柱/鱼身渐变鳍鳞/吞吃演出/BOSS 进退场 ✅ |
| 10-B | xiangqi | `8fca2c3` `0f2595d` `690407a` | 侧壁刻字 sprite/走子三段/吃子花瓣金环/将军演出/吃子槽 ✅ |
| 10-C | fight-king | `a205241` `db85501` `bc09e0f` | 八套行头/五款表情/火花三档/地面纵深带/选人头像去 emoji ✅ |

**漏做、做了没推：无。**

---

## 二、专项① 火柴人 / emoji 直出扫描

方法：9 款非测试源码 grep emoji 码位（含代理对）→ 逐处判定「画布 `fillText` 直出核心资产」vs「DOM/画布 HUD 装饰图标」；实机进游戏目检主角与核心道具；重点回查 fight-king 选人/触屏板头像。

| 游戏 | 核心资产 emoji 直出 | 「纯圆+两点眼」残留 | 装饰 emoji 登记（源码出现处 / 画布 fillText 行） |
| --- | --- | --- | --- |
| hue-hand | 0（卡面/牌背全 SVG） | 无角色 | 66 处 / 0 行（对手头像 🐰🐼🦊 为 DOM，见 P-06） |
| hop-pads | 0（台面图案全绘制，`KIND_ICONS` 字符已移除） | 已升级：呆毛+裙边/披风+表情四态（实测截图） | 60 处 / 1 行（画布暂停字「⏸ 已暂停」） |
| tap-tiles | 0（音符 sprite、粒子贴图，无 `fillText("♪")`） | 无角色 | 56 处 / 1 行 |
| gomoku | 0（胜利星改矢量 `paintStar`） | 无角色 | 65 处 / 0 行 |
| match-stars | 0（六色棋子/特殊块/机关/彩虹星全 SVG，替掉 `skin.emoji` 双字符叠加） | 无角色 | 78 处 / 0 行 |
| rainbow-run | 0（星币 8 帧 sprite、道具 🧲🚀🛹⚡ 换绘制图标、跑者重画） | 已升级：发带披风双臂姿态脸（实测截图） | 88 处 / 9 行（战役地图 🔒🏁⏱/章节图标/画布 HUD 💗🤍🍬⭐，见 P-07） |
| ocean-munch | 0（鱼/NPC/BOSS/收集物全绘制） | 已升级：渐变鱼身+鳍+冠（实测截图） | 89 处 / 14 行（画布主页/地图/对手名牌，见 P-07） |
| xiangqi | 0（14 种棋子 sprite） | 无角色 | 86 处 / 0 行（AI 头像 🐘 为 DOM 兜底，见 P-05） |
| fight-king | 0（**选人/触屏板/HUD 头像已画制**，实测截图核验） | 已升级：四肢骨架+行头+五款表情 | 55 处 / 0 行（菜单模式卡 🥊🤖🏯🔥🎓 为 DOM） |

**结论：核心资产 emoji 直出 0 处，「严重」0 条。** 装饰 emoji 全部为 HUD/菜单层小图标（按规格可留、已登记）。机器化断言已沉淀（第八节）。

---

## 三、专项② 金币 / 收集物体积

方法：逐款抽收集物绘制函数，核「≥2 停渐变 + 高光 + 描边/落影之一」；代码检视 + 实测截图双证。

| 游戏 | 收集物 | 代码证据 | 判定 |
| --- | --- | --- | --- |
| rainbow-run | 星币 | `art.ts` `drawCoinFrame`：3 停径向渐变（L71-74）+ 8 帧旋转侧棱 + 1.5px 深金描边 + `drawCoinSweep` 扫光；星星带光晕（L186 径向 2 停） | ✅ |
| match-stars | 星星棋子 | `starTokenSVG`：linearGradient 顶亮 +15%（L131）+ 深 25% 描边 + 左上 gloss 椭圆（L125） | ✅ |
| ocean-munch | 收集星/护盾 | `art.ts` L666-696：「厚边（暗金错位）+ 亮金星面 + 内圈小星 + 高光点」注释即规格，实现对应 | ✅ |
| tap-tiles | 音符（分值物） | `art.ts` L157-160 纵向 3 停渐变 + 顶面高光条 + 底部厚边；判定爆点 `burstSprite` 径向光 | ✅ |
| gomoku | 棋子/金花 | 径向渐变（L59 左上偏移）+ 投影 + 高光；谜题金花五瓣 + 花心 | ✅ |
| xiangqi | 棋子/花瓣金环 | 径向渐变面（L184 左上偏移）+ 3px 月牙侧壁 + 阴刻字；被吃大子金环 | ✅ |
| hue-hand | 卡面（核心资产） | 对角渐变卡底（`lighten` 左上提亮）+ 白内框 + 斜切椭圆 + SVG 图标 | ✅ |
| hop-pads | 完美星屑/皇冠 | `drawStar`/`drawCrown` 矢量 + 台顶发光 + 金色扩散环（截图证：完美落点金环+星屑） | ✅ |
| fight-king | 无收集物（火花/彩带） | `hitSpark` 三档 + `drawStarShape` 高光描边 1.5px | ✅（不适用项按同类资产核） |

**「平涂圆内贴数字/星字符」0 处，「严重」0 条。**

---

## 四、专项③ 双人 / 对阵角色可区分（16px 灰度量化）

方法：起 vite dev server，无头 Chromium 直接调用各款 `art.ts` 绘制函数，64px 渲染 → 16×16 降采样 → 灰度：`gray`=非透明像素平均灰度差（0-255）、`sil`=剪影差异像素占比、`top`=顶部 1/4（头饰区）灰度差。判定线：剪影/头饰/附件三通道至少两通道可分。

| 款 | 对 | gray | sil | top(头饰区) | 判定 |
| --- | --- | --- | --- | --- | --- |
| fight-king | 8 位行头两两 28 对 | 11.8–33.7 | 0.008–0.043 | **23.7–138.2** | ✅ 头饰+附件（背件/裙摆/围巾）两通道全过；战斗中另有 P1/P2 脚环+头顶标记双通道（截图证） |
| fight-king | 最弱对 duoduo\|xingxing | 11.8 | 0.008 | 26.8 | ✅（花苞头饰 vs 星发卡 + 裙摆 vs 披风，灰度模式截图仍可辨） |
| hop-pads | P1 朵朵 \| P2 星星 | 20.6 | 0.012 | 38.2 | ✅ 呆毛（花苞/星）+ 裙边/披风 |
| ocean-munch | P1 金冠 \| P2 银星带 | 7.2 | 0.016 | **79.6** | ✅ 头饰形状+金/银明度 |
| xiangqi | 红帅 \| 黑将 | **41.4** | 0（圆子同形，棋类天然） | 31.2 | ✅ 字形（帅/将）+ 色墨明度双通道（plan 认可的天然双通道） |
| match-stars | 六色棋子两两 15 对 | 30.2–64.1 | 0.012–0.102 | 0–138.2 | ✅ 六形（圆角星/尖角星/花形/心形/八角/胖星）+ 表情 |
| gomoku | 黑 \| 白子 | 明度差 **176/255**（中间调 69 vs 245） | — | — | ✅ 灰度/色弱天然安全 |
| hue-hand | 四色卡 | 色 + 角标形状符号（圆/方/三角/星，实测截图色条含方形符号） | — | — | ✅ |
| tap-tiles | 四列 | 列色 + 列首形状符号 ● ◆ ▲ ★（实测截图） | — | — | ✅ |

**「只靠颜色区分」0 款，「严重」0 条。**

---

## 五、专项④ 跑酷 / 竞速 2.5D 纵深（本窗点名 4 款）

| 款 | 要求 | 证据 | 判定 |
| --- | --- | --- | --- |
| hop-pads | 2.5D 跳台厚度/投影 | `project()` 投影保留（回归测试钉住）；台子顶面椭圆+侧壁双面色+内圈纹（截图证）；深度雾 `fogAlpha`（FOG_MAX 0.35, 140→560）；三层视差背景（天空渐变/云与远山/浮岛）；影子随高缩放 | ✅ |
| rainbow-run | 伪 3D 跑酷，1.1 的 2.5D 不许退化 | `view3d.ts` 全导出回归测试通过（相机/雾/视差/画质三档一字未动）；实测截图：透视路面收敛、彩虹路肩、12 世界视差剪影、障碍立面（水晶簇/糖果栏架）、星币 8 帧近大远小、接地影统一 | ✅ 无退化，纵深增强 |
| ocean-munch | 尽量 2.5D 水下层/光柱景深 | `art.ts`：`drawLightShafts`（斜光柱摆动 ±3°）、`drawFarLayer`/`drawForeLayer` 三层视差、`drawDepthTint` 按 y 深度着色、气泡池 ≤24 走画质开关；实测截图：光柱/远景剪影鱼/深度渐变可见 | ✅ |
| fight-king | 尽量 2.5D 地面纵深带/视差 | `art.ts`：`PARALLAX` 四层（sky 0.08/far 0.22/near 0.52/petal 1.15）+ `drawGroundBands` 地面三阶渐变带；实测截图：樱花山道主题远山/近树/花瓣/地带清晰 | ✅ |

其余 5 款按宪法五节分级为精 2D（棋牌/消除/音游/卡牌），不硬上透视——符合规范。**纵深缺失/退化「严重」0 条。**

---

## 六、专项⑤ 360px 布局实测

方法：Chromium 移动仿真 360×640 与 320×568（DPR 2，触屏），逐款进入游戏页与实战画面；程序量测横向溢出（`scrollWidth`−视口）、超出视口右缘的元素、字号 <14px 的可见文本、<40px 的可点目标；截图目检画布内布局。

| 游戏 | 360 溢出 | 320 溢出 | 字号 <14px | 触区 <40px | 备注 |
| --- | --- | --- | --- | --- | --- |
| hue-hand | 0px | 0px | 共享外壳 `.l99-star` 10px（装饰星形字符） | 0 | |
| hop-pads | 0px | 0px | 同上 | 0 | 实战画布内 HUD/蓄力条无遮挡 |
| tap-tiles | 0px | 0px | 同上 | 0 | 四列触区=整列 |
| gomoku | 0px | 0px | 同上 | 0 | 15 路盘自适应完好 |
| match-stars | 0px | 0px | 同上 | 0 | 8×8 格 SVG 在 ~38px 格内形状可辨（截图证） |
| rainbow-run | 0px | 0px | 0 | 0 | 画布内：首页标题末字与 🎁 按钮重叠 ≈18px（P-02，1.2 遗留） |
| ocean-munch | 0px | 0px | 0 | 0 | 画布内：首页标题被图鉴徽章遮挡 ≈34px（P-01，1.2 遗留） |
| xiangqi | 0px | 0px | 共享外壳同上 | 0 | 棋子 ≥30px 刻字清晰（截图证） |
| fight-king | 0px | 0px | `.fk-sub` 13px、`.fk-mode-d` 12.5px（P-03，1.2 遗留） | 0（触屏板四键 ≥44px，截图证） | |

说明：扫描器报的 `button.l99-tab.l99-tab-lock right>视口` 为**误报**——`.l99-tabs` 是 `overflow-x:auto` 的横向滚动页签容器（`level99.ts` L494），页签超出右缘属于可滚动内容，非溢出。

**视觉升级导致的新溢出 0 处，「阻断」0 条。** 上表三处画布遮挡/小字均逐一与基线 `b7cd80d` 比对确认为 1.2 遗留（非本窗升级引入），列入问题清单交 C 档。

---

## 七、专项⑥ 商标扫描

方法：`rg -i` 黑名单（1.2 主管第八节全量 + 1.3 视觉侧追加：任天堂/迪士尼/4399/Hello Kitty/托马斯/Bomberman/UNO/Candy Crush/开心消消乐/钢琴块/Cytus/跳一跳/大鱼吃小鱼/Subway Surf 等）扫 9 款全部源码与注释；实测截图逐款目检高相似造型。

- 测试文件（`copy.test.ts` 等反断言黑名单本身）之外，9 款非测试源码命中 **0**。
- 两处字面撞词均为误报：`hue-hand/art.ts:119` 渐变色值 `#5b4399` 撞「4399」（十六进制色值）；`fight-king/index.ts:1379` 注释「一跳一跳的」撞「跳一跳」（描述镜头，非产品名）。
- 造型目检：hue-hand 四色为黄/绿/粉/紫（非 UNO 红黄绿蓝），牌背为原创四色花瓣；fight-king 八位角色 Q 版原创行头，无街霸/拳皇形象；xiangqi/gomoku 为传统公有制式，无棋院商标；rainbow-run 跑者为原创彩虹发带团子，无地铁跑酷角色/涂装。
- README 命中「泡泡炸弹人」为其他窗口游戏（bomb-buddies）自有原创名，不在本窗范围，仅登记不处置。

**「阻断」0 条。** 商标静态断言已沉淀为每款 `visual-scan.test.ts` 用例（第八节）。

---

## 八、横向一致性抽查

1. **光照约定**（左上 45°、高光偏移、描边 1.5–2px），9 款逐一对照：gomoku 径向高光 `(-0.38r,-0.42r)` ✅；xiangqi `(-0.35r,-0.4r)` ✅；rainbow-run 金币 `(-0.35rx,-0.4r)`、描边 1.5px ✅；match-stars gloss 椭圆 (11.6,10.2)@32px 左上象限 ✅；tap-tiles 顶亮渐变+顶面高光条 ✅；hue-hand 「卡底左上提亮」`lighten` ✅；ocean-munch 「气泡左上高光弧」✅；hop-pads 眼高光 `(-0.24er,-0.3er)` ✅；fight-king lineWidth 1.5/2 描边 ✅。**无背离。**（注：本分支无共享 kit，约定按各款 art.ts 落实。）
2. **prefers-reduced-motion**：浏览器 reduce 双态 9 款载入 0 报错；fight-king reduce 态实战截图证——视差呈**静态一帧保留**（修复了 1.2「921 行整层不画」），角色行头/脚环仍在；单测层面 9 款 reduce 分支共约 40+ 例（各 art/view 测试）覆盖粒子归零/光束静止/波纹关闭/演出直落。**通过。**
3. **destroy 泄漏**：9 款「进 → 看 → 退」×5 后回首页，instrument 计数：**rAF 每秒 0 次、残留 interval 0 个**（9/9），页面 0 报错。**无泄漏。**
4. **性能（CPU 4× 节流，rAF 计帧 3s）**：fight-king 实战（火花/视差/花瓣）**60fps**；ocean-munch 无尽下潜（气泡/光柱/鱼群）**47fps**；rainbow-run 无尽跑（金币/障碍/视差）**60fps**；match-stars 关卡内（消除后棋盘）**60fps**。全部 ≥30fps 门槛。**通过。**

---

## 九、问题清单（全部量化）

| 编号 | 位置（文件+函数） | 现状（截图/量测） | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| P-01 | `src/games/ocean-munch/index.ts` `drawHome()`（标题 fillText w/2,28 与 `dexFace {x:w-120,w:112}`） | 360 视口（画布 w≈336）主页标题「海底大胃王」右侧 ≈34px 被图鉴徽章盖住（标题横跨 [86,250]，徽章 [216,328]），截图可见「胃王」被压；**基线 b7cd80d 同现，1.2 遗留** | 标题左对齐或缩字号避让徽章 | 一般 | ocean-munch/index.ts（C 档） |
| P-02 | `src/games/rainbow-run/index.ts` 战役主页（标题 fillText 与 `face {x:w-46,w:38}`） | 360 视口标题「十二大世界」末字与 🎁 收藏册按钮重叠 ≈18px（标题右缘 ≈308 vs 按钮左缘 290）；**1.2 遗留** | 标题按钮避让（缩字号或右移） | 一般 | rainbow-run/index.ts（C 档） |
| P-03 | `src/games/fight-king/index.ts` 内嵌 CSS `.fk-sub{font-size:13px}` `.fk-mode-d{font-size:12.5px}` | 菜单副标题 13px、模式卡描述 12.5px，低于宪法 14px 门槛（实测量得 13/12.5px）；**基线同值，1.2 遗留** | ≥14px | 一般 | fight-king/index.ts（C 档） |
| P-04 | `src/games/xiangqi/index.ts` `avatarHTML()` L112 | AI 对手「棋灵象」座位头像走 🐘 emoji 兜底（无 robot 头像资产），30px 圆片；**1.2 遗留**；将/帅小棋子图标（1.3 交付）正常 | 画制小象头像或复用棋子 sprite | 一般 | xiangqi/index.ts 或 art.ts（C 档） |
| P-05 | 共享外壳 `src/games/level99.ts` L513 `.l99-star` | 选关页签内 10px「★」装饰字符（非正文），全部 188 关游戏共用 | 共享外壳统一处理，非本窗职权，移交统筹登记 | 建议 | level99.ts（统筹/主管） |
| P-06 | `src/games/hue-hand/index.ts` L100-102 `BOT_FACES` | 对手头像 🐰🐼🦊 DOM emoji（plan 8-A 曾评「对手区可爱、保留」，1.2 遗留）；截图中与升级后的卡面质感有代差 | 后续轮次可换画制头像（与 fight-king 对齐） | 建议 | hue-hand/index.ts（C 档酌情） |
| P-07 | `rainbow-run/index.ts`（9 行）、`ocean-munch/index.ts`（14 行） | 画布绘制的战役地图/主页/HUD 仍用 emoji 小图标（🔒🏁⏱👑⚔💗🤍🍬⭐ 及章节表情），属规格允许的装饰层，已登记数量 | 后续轮次逐步换绘制小图标（对齐 98174b7 的做法） | 建议 | 各自 index.ts（C 档酌情） |

**统计：阻断 0 · 严重 0 · 一般 4（P-01~P-04，全部为 1.2 遗留，非本窗升级引入）· 建议 3（P-05~P-07）。**

---

## 十、新增机器化扫描用例（沉淀给第 2、3 轮）

每款新增 `src/games/<id>/visual-scan.test.ts`（9 文件 × 3 例 = **27 例**，全绿）：

1. `art.ts` 的 `fillText` 行不直出 emoji（排除注释行）；
2. 体积/纵深哨兵：7 款断言渐变调用仍在；hop-pads 断言 `mixColor`+`fogAlpha`；fight-king 断言 `gearDark`+`drawGroundBands`——防后续改动把三阶光影/2.5D 退化回平涂；
3. 本目录全部正式源码（含注释）零商标黑名单词（23 个无误报词条）。

加上本轮新增后全库 `npm test`：**705+9=714 文件 / 15251+27=15278 例全绿**（见收尾复跑）。

---

## 十一、结果矩阵（9 款 × 六专项）

| 游戏 | ①火柴人/emoji | ②金币体积 | ③双人可区分 | ④2.5D 纵深 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| hue-hand | ✅ | ✅ | ✅ | ✅（精 2D 合规） | ✅ | ✅ |
| hop-pads | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| tap-tiles | ✅ | ✅ | ✅ | ✅（精 2D 合规） | ✅ | ✅ |
| gomoku | ✅ | ✅ | ✅ | ✅（精 2D 合规） | ✅ | ✅ |
| match-stars | ✅ | ✅ | ✅ | ✅（精 2D 合规） | ✅ | ✅ |
| rainbow-run | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| ocean-munch | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| xiangqi | ✅ | ✅ | ✅ | ✅（精 2D 合规） | ✅ | ✅ |
| fight-king | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

54/54 全 ✅（一般级遗留项见第九节清单，不构成专项 ❌）。

---

## 十二、分级红线抽查（顺带）

- 失败文案只鼓励：tap-tiles 实测失败页「就差一点点！有几个音符溜走了，慢一点、稳一点，再试一遍准能过。」✅；rainbow-run BOSS 战失败文案「还剩一点力气就溜走了…再来一次多铲几个箱子!」✅。
- 无血/无死亡描写：fight-king 命中=星形火花、条子写「元气」；ocean-munch 吞吃=旋入+气泡、BOSS 战败=翻肚缓沉 ✅。
- 无广告无内购、离线可玩（PWA precache 全量）✅。

## 十三、合并态复验（配合 C 档）

本报告基于 `game-1.3-window3` @（见 push 后 SHA，收尾章）。三方（tester/learner/fixer）合并态复验与合并态 SHA 由 C 档执行后补记于 fixer 报告，本报告数据可复现：`npm ci && npm test && npm run build`，浏览器实测脚本方法见第一节「实测环境」。
