# 1.3 窗口 3 · 第 1 轮视觉验收 · 测试员报告

> 角色：窗口 3（第 8–10 步，9 款）三轮视觉验收第 1 轮 · A 测试员。
> 分支：`game-1.3-window3`。只验视觉，不重开玩法验收，不改绘制代码。
> 范围（仅 9 款）：`hue-hand` `hop-pads` `tap-tiles` `gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi` `fight-king`。
> 依据：`docs/plan-1.3-visual-bible.md`（宪法）、`docs/plan-1.3-window3.md`、`docs/plan-1.3-step{8,9,10}-{A,B,C}-*.md`、`docs/plan-1.3-step27-A-tester.md`（思路）。

## 〇、扫描计划（动手前提交）

1. 全量跑：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积，对照「只增不减」。
2. 盘点：9 款目录 × `src/art/kit/` import 关系；plan 文档 vs 实际提交（`git log`）逐款对账。
3. 六大专项（仅 9 款）：
   - ① 火柴人 / emoji 直出扫描（grep `fillText(` emoji + 纯圆角色画法；重点 fight-king 选人/触屏板头像）；
   - ② 金币 / 收集物体积（≥2 停渐变 + 高光 + 描边/落影之一；重点 match-stars、ocean-munch、tap-tiles）；
   - ③ 双人角色可区分（gomoku / xiangqi / fight-king 双方 16px 灰度三通道对比）；
   - ④ 2.5D 纵深（hop-pads 跳台厚度投影 / rainbow-run 伪 3D 不退化 / ocean-munch 水下景深 / fight-king 地面纵深带）；
   - ⑤ 360px 布局（360×640 与 320px 极限：溢出 / 遮挡 / 字号 <14px / 触区 <40px，量化到像素）；
   - ⑥ 商标扫描（`rg -i` 黑名单 + 逐款目检近似造型）。
4. 横向一致性：kit 光照约定（左上 45°、描边 1.5–2px）、`prefers-reduced-motion` 降级、destroy 泄漏（进出 ×5）、CPU 4× 节流帧率（fight-king 火花 / ocean-munch 气泡 / rainbow-run / match-stars）。
5. 新增机器化扫描用例（仅 9 款 `*.test.ts`）沉淀给第 2、3 轮。
6. 报告矩阵：9 款 × 六专项 ✅/❌。

（以下各节由扫描完成后填写。）
