# 1.3 窗口 6 · A 档 · 第 1 轮视觉测试员报告

状态：扫描计划已提交，执行中。

## 扫描计划

- 范围：本窗口 9 款 —— `brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`。
- 基线：`origin/game-1.3`；工作分支 `game-1.3-window6`。
- 只验视觉，不重开玩法验收；不碰 parentAuth / parentGate；不改绘制代码。
- 可写文件：9 款 `*.test.ts` 视觉新增用例 + 本报告。

### 步骤

1. 全量跑：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数、构建体积。
2. 盘点：9 款目录与 `src/art/kit/` 文件归属（对照 1.3 各步独占约定），`git log --follow` 抽查越权改动。
3. 对照 1.3 第 17–19 步 9 款 plan 文档与实际提交。
4. 六大专项（逐款给证据、量化）：
   - ① 火柴人 / 草稿圆 / emoji 直出扫描（重点 mole-pop、box-hamster、气球/泡泡平涂圆）；
   - ② 金币 / 星星 / 收集物 / 气球 / 泡泡 / 砖块体积：≥2 停渐变 + 高光 + 描边或落影之一；
   - ③ 多角色 16px 灰度可分辨（alien-seek 六只、mole-pop 多种地鼠）；本窗口无双人竞速款 → 双人专项 N/A；
   - ④ 2.5D 纵深：点名款仅 `brave-path`（格子厚度）；
   - ⑤ 360×640 + 320px 极限布局：溢出 / 遮挡 / 字号 <14px / 触区 <40px，写清像素数；
   - ⑥ 商标扫描：素材与文案 grep + 目检。
5. 横向一致性：kit 光照约定（左上 45°、高光偏移、描边 1.5–2px）、`prefers-reduced-motion`、destroy 泄漏（进→退 ×5）、粒子性能（brick-break / balloon-pop / bubble-pop / bubble-aim，CPU 4× 节流 ≥30fps）。
6. 沉淀机器化扫描用例到 9 款 `*.test.ts`。
7. 报告 + 9 款 × 六专项结果矩阵；收尾 rebase → `npm test` / `npm run build` 全绿 → 普通 push。

（以下各节在扫描完成后填写。）
