# 1.2 窗口 1 · 第 1 轮 · 测试员报告

> 分支：`game-1.2-window1`。范围**仅限窗口 1**：第 1 步平台能力 + 12 款新游戏。
> 窗口 2 点名的 `gomoku` / `match-stars` / `rainbow-run` / `ocean-munch` / `xiangqi` 一律跳过。
> 本轮只记问题、不改玩法代码；发现的阻断/严重交给本轮修复员。

## 〇、走查计划（动文件前先立此计划）

### 范围清单

平台（窗口 1 独占）：

| 编号 | 项 | 硬要求 |
| --- | --- | --- |
| P1 | root 管理员门 | 密码 `kangkang`；弹窗「要打开请联系管理员 18438037080」；直达第 N 关 1–188；1 小时过期（注入假时钟，不真等）；可手动关；连错 3 次锁 120 秒；只写 `yiduo-yixing.root.v1` = `{ expiresAt }`；密码绝不落盘 |
| P2 | 家长算术门 | 原样保留，未被 root 门改坏 |
| P3 | 首页手游/端游筛选 | `GameMeta.platform` 缺省 `both`；芯片「全部 / 手游 / 端游」可与分类、玩法、搜索任意组合 |
| P4 | `playModes` | 闯关/对战/无尽口径统一 |
| P5 | `view25d` | 2.5D 基建可用，且**禁 three.js** |

12 款新游戏：`orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate` `hero-cards` `weiqi-garden` `flight-chess` `merge-2048` `mine-garden` `sudoku-petal`

### 每款的走查链路

1. 首页能不能看见卡片、点进去（走 `import.meta.glob` 自动发现，不许手工注册）。
2. 真实胜负：**赢一次、输一次**都要真跑出来。
3. 退出再进：`destroy` 之后监听 / timer / rAF / AudioContext 不留残。
4. 战役：第 **1 / 100 / 188** 关各进一次，看关号、章节、难度是不是对得上。
5. 有对战 / 无尽 / 双人同屏的，每种模式各玩到**结算**。
6. **360px** 宽走一遍：文字不溢出、`documentElement.scrollWidth - clientWidth <= 1`。
7. 双人键位：朵朵 `WASD + F + G`，星星 `方向键 + L + K`。

### 取证手段

- 单元测试全量：`npm test`，记录文件数 / 用例数。
- 类型 + 构建：`npm run build`。
- 真浏览器：`puppeteer-core` + 本机 `google-chrome`，视口 `360×640`，跑 `scripts/smoke-1.2-window1-r1-tester.mjs`（本轮新写的测试员专用脚本，只读不改玩法）与仓库已有的 `scripts/smoke-1.2-step*.mjs`。
- 假时钟：root 门的 1 小时过期用改写 `Date.now` / 直接写入过期 `expiresAt` 的方式验证，**不真等 1 小时**。

### 严重度口径

| 级别 | 定义 |
| --- | --- |
| 阻断 | 进不去 / 玩不到结算 / 白屏报错 / 数据写坏 / 安全红线（密码落盘等） |
| 严重 | 模式缺失或走不到结算、360px 溢出、`destroy` 泄漏、关号对不上、键位失灵 |
| 一般 | 手感、文案、边角视觉、可优化项 |

### 独占文件

本轮只许新建 / 修改：

- `docs/qa/1.2-window1-round1-tester.md`
- `scripts/smoke-1.2-window1-r1-tester.mjs`（可选）

---

## 一、清点：12 款 + 平台是否在首页出现

在 `360×640` 的真浏览器里打开首页，靠 `import.meta.glob("../games/*/meta.ts")` 自动发现，**12 张卡片一张不少**，首页一共列出 **67** 张卡（1.1 的 55 款 + 已合进本分支的新款）。

| # | id | 卡片标题 | 首页可见 | 点卡片进得去 | `platform` | `modes` | `levels` |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `orb-arena` | 圆圆大作战 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 2 | `snake-royale` | 长蛇争霸 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 3 | `block-drop` | 方块叠叠乐 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 4 | `combo-clash` | 连招对决 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 5 | `mahjong-bloom` | 花开麻将 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 6 | `star-estate` | 朵星地产 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 7 | `hero-cards` | 英杰令 | ✅ | ✅ | `both` | campaign / versus / endless | 188 |
| 8 | `weiqi-garden` | 围子花园 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 9 | `flight-chess` | 飞行棋乐园 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 10 | `merge-2048` | 星星合成 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 11 | `mine-garden` | 扫雷花园 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |
| 12 | `sudoku-petal` | 数独花田 | ✅ | ✅ | `both` | campaign / versus / endless / twoPlayer | 188 |

平台能力在首页的落点也都在：

- 分类页签 `.cat-tabs`、玩法芯片 `.mode-chips`、**平台芯片 `.platform-chips`（🌈全部 / 📱手游 / 💻端游）**、搜索框 `.home-search-input` 四排控件齐全；
- 管理员入口 `.icon-btn--admin`（🔑）在首页右上角，点开就是 root 密码门；
- 首页 360px 下 `scrollWidth - clientWidth = 0`，不横向溢出。

`meta.ts` 纪律抽查（12 款全过）：`meta.ts` 里 `import` 数为 **0**（纯数据，没有拖玩法代码进首页 chunk）；`index.ts` 第 1–2 行就是 `import { meta } from "./meta"; export { meta };`。

---

## 二、每款证据

下面每一格都是 `360×640` 真浏览器里点真按钮、按真键盘量出来的。战役第 1 / 100 / 188 关的「章节名 · 关号」是从 `.l99-stagetitle` 上读回来的原文。

### 通用结论（12 款一致）

- **战役第 1 / 100 / 188 关**：12 款 × 3 关 = **36 关全部进得去、盘面画得出、360px 下 `doc+0` 不溢出**，关号与章节标题都对得上。
- **`destroy` 不泄漏**：12 款进 → 玩 → 退回首页后，`window` 监听 / `setInterval` / 排队中的 `rAF` 全部回到基线 `{listeners:4, intervals:1, frames:0}`，一个不多。
- **退出再进**：12 款都能从首页再点一次卡片重新挂起来。
- **无报错**：12 款全程没有 `pageerror`，没有 `console.error`。
- **模式入口**：12 款的模式条 `.<前缀>-modebar` 都按 `meta.modes` 列齐（`hero-cards` 按设计不做同屏双人，只有对战 + 无尽）。

### 逐款明细

| id | 战役 1/100/188 | 赢一次 | 输一次 | 对战 | 无尽 | 双人同屏 | 双人键位 | 360px | destroy |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `orb-arena` | 🟢彩豆平原 / 🏅排行赛场 / 🏆圆圆杯总决赛 ✅ | ❌ 未取到 | ✅ 就差一点点！ | ✅ 结算「这一局到此为止」 | ✅ 结算「圆圆先去休息啦」 | ✅ 结算「这一局结束啦」 | ✅ 朵朵 / 星星都有反应 | ✅ doc+0 | ✅ |
| `snake-royale` | 🌱星光草地 / 🔄绕圈成环 / 🏆长蛇杯 ✅ | ❌ 未取到 | ✅ 就差一点点！ | ✅ 结算「这一局到此为止」 | ✅ 结算「长蛇打了个盹」 | ✅ 结算「这一局结束啦」 | ✅ | ✅ doc+0 | ✅ |
| `block-drop` | 🧱落块入门 / 🌟满四行 / 🏆叠叠杯 ✅ | ❌ 未取到 | ✅ 就差一点点！（70 次操作） | ✅ 结算「这一局到此为止」 | ✅ 结算「叠得好高呀」 | ✅ 结算「这一局结束啦」 | ✅ | ✅ doc+0 | ✅ |
| `combo-clash` | 🌸轻击学堂 / 💫超必剧场 / 🏆连招杯 ✅ | ❌ 未取到 | ✅ 第 188 关 就差一点点！ | ⚠️ 开得起来，结算未取到 | ⚠️ 同上 | ⚠️ 同上（训练场也一样） | ✅ | ✅ doc+0 | ✅ |
| `mahjong-bloom` | 🌊顺子小溪 / ✨七对星空 / 👑国标华章 ✅ | ❌ 未取到 | ✅ 就差一点点！（9 次操作） | ⚠️ 一桌 119 个节点画得出，55s 内没和牌 | ⚠️ 同上 | ⚠️ 同上（154 个节点） | ✅ | ✅ doc+0 | ✅ |
| `star-estate` | 🏷️买地入门 / 🪑小黑屋 / 🏆破产终局 ✅ | ✅ 第 1 关过关！ | ❌ 未取到 | ⚠️ 186 个节点画得出，55s 内没分胜负 | ⚠️ 同上 | ⚠️ 同上 | ✅ | ✅ doc+0 | ✅ |
| `hero-cards` | 🌸击挡愈 / ✨技能初绽 / 🏆英杰杯 ✅ | ✅ 见 `smoke-1.2-step4-a`（8 章各一关打到过关，65/65 全过） | ⚠️ 本轮通用假人未取到 | ⚠️ 身份场 61 个节点画得出，结算未取到 | ✅ 结算「连胜到这里啦」 | — 按 meta 无同屏双人 | — | ✅ doc+0 | ✅ |
| `weiqi-garden` | 🌱气的花园 / 🧮数子练习 / ✨十九路星空 ✅ | ✅ 见 `smoke-1.2-step4-b`（8 章各一关真解到过关） | ✅ 就差一点点！（4 次操作） | ⚠️ 21 个节点画得出，55s / 365 手内没分先 | ⚠️ 同上 | ⚠️ 同上 | ✅ | ✅ doc+0 | ✅ |
| `flight-chess` | 🛫起飞跑道 / 🏰叠机堡垒 / 🏆四人决赛 ✅ | ✅ 第 1 关过关！ | ✅ 见 `smoke-1.2-step4-c`（1280×800 闯关第 1 关走到「就差一点点！」） | ✅ 四人对战开得起来，`step4-c` 里三档视口都在推进（已掷 74–79 次） | ⚠️ 连胜无尽结算未取到 | ⚠️ 结算未取到；`step4-c` 里 F 键掷得动骰子、四色同场 | ✅ | ✅ doc+0 | ✅ |
| `merge-2048` | 🌱小数入门 / 🌺障碍花 / 🏆合成杯 ✅ | ✅ 第 1 关过关！（另见 `smoke-1.2-step5-a` 8 章各一关） | ⚠️ 本轮未取到 | ✅ 结算「你先合到啦！」 | ✅ 结算「这一盘到此为止」 | ⚠️ 结算未取到 | ✅ | ✅ doc+0 | ✅ |
| `mine-garden` | 🌱小苗床 / 🌫️迷雾园 / 🏆园丁杯 ✅ | ✅ 见 `smoke-1.2-step5-b`（8 章各一关真扫到过关） | ✅ 第 188 关 就差一点点！ | ⚠️ 193 个节点画得出，55s 内没扫完 | ⚠️ 同上 | ⚠️ 同上 | ✅ 分边取证通过 | ✅ doc+0 | ✅ |
| `sudoku-petal` | 🌱四宫萌芽 / 🌸成对花 / 🏆花田杯 ✅ | ✅ 见 `smoke-1.2-step5-c`（8 章各一关真解到过关） | ✅ 就差一点点！（16 次操作） | ⚠️ 699 个节点画得出，55s 内没解完 | ⚠️ 同上 | ⚠️ 同上 | ✅ | ✅ doc+0 | ✅ |

**⚠️ 的读法**：这些格子里模式**入口开得起来、盘面真的画出来、360px 不溢出**都验过了，只是本轮的**通用假人**（按配方乱按键 + 乱点可点元素）在 55 秒预算里没能把一局麻将 / 一盘围棋 / 一局地产打到分胜负。这是**取证手段的上限，不是已证实的缺陷**，处理办法见问题清单 W1-12。

**❌ 的读法**：`orb-arena` / `snake-royale` / `block-drop` / `combo-clash` / `mahjong-bloom` 这 5 款既没有专用冒烟脚本，通用假人又打不赢（补跑 6 局 × 90 秒仍 0 胜），所以本轮**没有拿到过关证据**。同为窗口 1 产物的第 4、5 步七款都配了 `scripts/smoke-1.2-step4-*.mjs` / `step5-*.mjs` 靠自家 AI 真解到过关，第 2、3 步的六款缺这一套（见 W1-12）。

### 双人键位取证

`orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate` `weiqi-garden` `flight-chess` `merge-2048` `sudoku-petal` 十款的同屏双人里：先连按 `W A S D F G`，画面指纹变化；再连按 `↑ ← ↓ → L K`，画面指纹再次变化 —— 朵朵、星星两套键位都真的接得住。

`mine-garden` 单拎出来说：整屏指纹这招在它身上不管用 —— 双人同屏的两块盘用的是**同一个种子**，朵朵先走一遍、星星再走同样的相对路线，两边长得一模一样，整屏指纹分辨不出第二个人到底有没有动。所以给它换了**分边读 DOM** 的取证（`PART=R`）：数每一半里 `.mg-cursor` 落在第几格、翻开了几格、插了几面旗。

| 按谁的键 | 左半（🌸 朵朵） | 右半（⭐ 星星） |
| --- | --- | --- |
| 起手 | `{cursor:40, opened:0}` | `{cursor:40, opened:0}` |
| 朵朵按 `D D S F A G` | `{cursor:50, opened:31}` 变了 ✅ | `{cursor:40, opened:0}` 没动 ✅ |
| 星星按 `→ → ↓ L ← K` | `{cursor:50, opened:31}` 没动 ✅ | `{cursor:50, opened:31}` 变了 ✅ |

两套键位各管各的一半，一点都不串台。代码侧的键位表 `keyAction(key, "p1"|"p2")` 也逐键核对无误（`p2` 认方向键 + `L` + `K`，不认 `WASD` / `F` / `G`）。**前两遍走查报的「星星键位失灵」是量法不对，不是缺陷**，此处更正。

`hero-cards` 按 meta 就没有同屏双人（身份场两个人挤一屏会互相看光手牌），跳过。

---

## 三、平台专项证据

浏览器专项一共 **40 项，通过 39 项**（`PART=B node scripts/smoke-1.2-window1-r1-tester.mjs`），唯一失败是拼音搜索（W1-02）。收尾 rebase 之后连首页清点一起重跑，**55 项全过**，详见第六节。

### P1 · root 管理员门

| 硬要求 | 结果 | 证据 |
| --- | --- | --- |
| 首页有入口 | ✅ | `.icon-btn--admin`（🔑，`aria-label="管理员权限"`） |
| 弹窗那句话原样出现 | ✅ | 弹窗正文含 `要打开请联系管理员 18438037080` |
| 密码框看不见字 | ✅ | `input.type === "password"`，`autocomplete="off"` |
| 密码 `kangkang` 能开 | ✅ | 在弹窗里真敲一遍 + 回车，门开 |
| 连错 3 次锁 120 秒 | ✅ | 第 3 次错后提示「密码连着输错了,先歇 120 秒再试一次。」，输入框与「打开」双双 `disabled` |
| 锁定期内密码对了也不给开 | ✅ | `submitRootPassword("kangkang")` 返回 `{ok:false, locked:true, open:false}` |
| 锁定 120 秒后自动解锁 | ✅ | **假时钟**推到 `now + 121s` 再交同一个密码 → `{ok:true, open:true}`，不真等 |
| 只写 `yiduo-yixing.root.v1` | ✅ | `localStorage` 里只多这一个 key |
| 值里只有 `expiresAt` | ✅ | `{"expiresAt":1787817545854}`，`Object.keys().length === 1` |
| 1 小时过期 | ✅ | 写入的 `expiresAt - now = 60 分钟`；**假时钟**推到 `+59min` 仍开、`+60min+1s` 关，且过期会话连存档一起清掉（`getItem` 返回 `null`） |
| 可手动关 | ✅ | 门开着时弹窗多一颗「关闭管理员权限」，按下去 `localStorage` 里的 key 当场消失 |
| 密码绝不落盘 | ✅ | 开门后把 `localStorage` + `sessionStorage` + `document.cookie` + `location.href` 全部拼起来搜 `kangkang`，**0 命中**；弹窗关掉时 `input.value` 也被抹成空串 |
| 360px 不溢出 | ✅ | 弹窗打开时 `doc+0` |

### P1b · 直达第 N 关

| 硬要求 | 结果 | 证据 |
| --- | --- | --- |
| 门关着连控件都不生成 | ✅ | 选关页 `.l99-jump` 数量为 **0**（不是 `hidden`，是根本没建 DOM） |
| 门开着才出现 | ✅ | 开门后 `.l99-jump` 出现 1 个，旁边注明「管理员权限还剩 60 分钟」 |
| 直达 1 / 100 / 188 | ✅ | 以 `sudoku-petal` 取证：`🌱 四宫萌芽 · 第 1 关`（16 格）、`🌸 成对花 · 第 100 关`（81 格）、`🏆 花田杯 · 第 188 关`（162 格），盘面都真画出来了 |
| 直达不发星 | ✅ | 直达第 188 关之后 `yiduo-yixing.l99.sudoku-petal[187]` 仍是 `0` |
| 越界 / 乱输自动夹 | ✅ | `999→188`、`0→1`、`-5→1`、`1e9→188`、`99.6→100`、`abc→null`、`"   "→null` |
| 跳过按钮切管理员版 | ✅ | 门关着是「⏭️ 跳过 第1关」，门开着是「⏭️ 跳过 第1关（管理员）」，不必再做算术题 |

### P2 · 家长算术门原样保留

`src/ui/parentAuth.ts` 一个字都没被 root 门改动：还是出算术题（`makeQuestion("basic"|"high")`），答对放行、答错拦下；参数全部维持 1.1 口径 —— `AUTH_TTL_MS = 5 分钟`、`MAX_WRONG = 2`、`LOCK_MS = 90 秒`、`HIGH_NEED_CORRECT = 2`。门关着时选关页的跳过按钮仍旧走这道算术门。

### P3 · 首页手游 / 端游筛选

| 硬要求 | 结果 | 证据 |
| --- | --- | --- |
| 三颗芯片都在、都能筛 | ✅ | 🌈全部 67 / 📱手游 67 / 💻端游 67 |
| 缺省当 `both` | ✅ | `matchesPlatformChip` 对 `{}`、`{platform:"both"}`、`{platform:"什么鬼"}`、`{platform:undefined}` 一律手游端游双命中 |
| 只写一边就只落一边 | ✅ | `{platform:"mobile"}` → 手游 ✔ 端游 ✘；`{platform:"desktop"}` → 手游 ✘ 端游 ✔ |
| 与分类叠加 | ✅ | 休闲 × 端游 × 双人 = 6 款 |
| 与玩法叠加 | ✅ | 对战 × 手游 = 32 款 |
| 与搜索叠加 | ✅ | 搜「数独」× 手游 → `sudoku-petal`；聚会 × 对战 × 端游 × 「麻将」 → `mahjong-bloom` |
| 首页真点四条件 | ✅ | 67 → 32（聚会 + 对战 + 手游）→ 1（再搜「麻将」＝花开麻将），全程 `doc+0` |

> 目前 67 款的 `platform` 全是 `both`，所以三颗芯片筛出来的张数一样。这是**当下数据的样子**，不是筛选坏了 —— 纯函数层已经证明只写一边时会正确收窄。等别的窗口按 1-B 文档给自己的 meta 填 `mobile` / `desktop`，芯片才会真正分流。

### P4 · `playModes` 口径

`compatFromMeta` / `assertModeMenu` / `pickInitialMode` / `availableModes` / `describeModes` 五个纯函数齐备，23 个单测全绿，`twoPlayer` 与 `coop` 都并进 `versus` 大类再用 `versusKind` 细分，口径一致。**但出货代码里没有任何一处 import 它**（详见 W1-03）。

### P5 · 2.5D 基建（禁 three.js）

| 硬要求 | 结果 | 证据 |
| --- | --- | --- |
| 没有 three.js | ✅ | `package.json` / `package-lock.json` 里没有 `three`；`src/` 里没有 `from "three"`；运行时 `window.THREE` 不存在、没有 three 的 `<script>`、页面上 0 个 WebGL 画布 |
| 自写透视管用 | ✅ | `project()` 在 `z=1` 时 `scale=0.857`，`z=40` 时 `scale=0.130`，越远越小 |
| flat 档降级正交 | ✅ | `scale` 恒为 1；相机背后的点 `visible=false` |
| 地平线在画面里 | ✅ | 640 高的画布上 `horizonY = 192` |
| 极端输入不炸 | ✅ | `z=NaN`、`z=-999`、`fov=0`、视口 `0×0`、全 `NaN` 五种脏输入都给有限数，不抛异常、不出 `NaN` |

### 商标黑名单扫描（窗口 1 范围）

29 个词逐个扫 12 款游戏目录 + 6 个平台文件的**出货代码**（排除 `*.test.ts`）与 `dist/assets/` 打包产物：**0 命中**。所有命中都落在各款自带的黑名单自查测试里（那是断言用的词表，本来就该出现）。

---

## 四、问题清单

| 编号 | 严重度 | 标题 | 收尾复核（rebase 到 `origin/game-1.2-window1` 之后） |
| --- | --- | --- | --- |
| W1-01 | **阻断** | 5 款战役里按一次 Esc 之后游戏再也回不来 | ✅ **已修复**，测试员真浏览器复核通过（见第六节） |
| W1-02 | **严重** | 12 款新游戏一款都没补拼音首字母，首页拼音搜索全废 | ✅ **已修复**，12 串首字母逐条搜得到（见第六节） |
| W1-03 | 一般 | `src/engine/playModes.ts` 出货代码 0 引用，也没从 `engine/index.ts` 导出 | ⬜ 仍在 |
| W1-04 | 一般 | `src/engine/view25d.ts` 同样 0 引用，1.1 的两份透视副本没有并过来 | 🟡 部分：四款开始引它，但只用到「减弱动效」，透视数学仍是三份 |
| W1-05 | 一般 | `merge-2048` 与 `mine-garden` 抢同一个 `mg-` 类名前缀，8 个类撞车 | ⬜ 仍在 |
| W1-06 | 一般 | 「← 回闯关」返回键与模式入口共用 `.<前缀>-open` 类 | ⬜ 仍在 |
| W1-07 | 一般 | 冒烟脚本分裂成 puppeteer / playwright 两套，仓库里两种都没有 | ⬜ 仍在 |
| W1-08 | 一般 | `snake-royale` 新增的皮肤存档 key 不在硬约束的 key 清单里 | ⬜ 仍在 |
| W1-09 | 一般 | 7 份 `PLAN.md` 混在 `src/games/<id>/` 出货源码目录里 | ⬜ 仍在（7 份原地不动） |
| W1-10 | 一般 | 复跑 `smoke-1.2-step4-b` 时「地狱档自由对战连下三手都有回应」失败 | ⬜ 留给第 2 轮定 seed 复现 |
| W1-11 | 一般 | 复跑 `smoke-1.2-step4-c` 时「375×667 四人对战四色都飞上了环线」失败 | ⬜ 留给第 2 轮定 seed 复现 |
| W1-12 | 一般 | 第 2、3 步六款没有专用冒烟脚本，本轮拿不到过关证据 | ⬜ 仍在 |

> W1-01 / W1-02 是本轮**测试员先报、修复员与学习优化员在同一分支上后修**的：报告正文保留发现当时的原始取证（复现步骤、根因、对照组），第六节补上修完之后测试员自己重新量的结果。曾经报过的「`mine-garden` 双人星星键位失灵」经分边取证证明是量法问题，**已撤回，不进本清单**。

---

### W1-01 · 阻断 · 5 款战役里按一次 Esc 之后游戏再也回不来

> **状态：已在本分支修掉**（`5a16463 fix(esc): 五款自带暂停的游戏拦住 Esc，不再和游戏壳各弹一次暂停`）。下面是发现当时的原始取证，复核结果见第六节。

**受影响**：`orb-arena` `snake-royale` `block-drop` `combo-clash` `merge-2048`（战役与模式页都一样）。

**复现步骤**

1. 首页点「圆圆大作战」→ 进第 1 关，等画面动起来。
2. 按一次 `Esc`。游戏自己停住，同时壳层的「先歇一会儿」暂停面板弹了出来（两层暂停叠在一起）。
3. 按第二次 `Esc`（或者点面板上的「继续玩」按钮）。面板收掉了，但**游戏没有恢复**。
4. 之后怎么按方向键、点画面都没反应，只能退出这一关重来。

**取证**（真浏览器，DOM 结构 + canvas 像素双重指纹判断「画面还在不在动」）

| 款 | 开局在动 | 按 1 次 Esc 后 | 按 2 次 Esc 后 | 改点「继续玩」按钮 |
| --- | --- | --- | --- | --- |
| `orb-arena` | ✅ | ❌ 停 | ❌ **仍停** | ❌ 仍停 |
| `snake-royale` | ✅ | ❌ 停 | ❌ **仍停** | ❌ 仍停 |
| `block-drop` | ✅ | ❌ 停 | ❌ **仍停** | ❌ 仍停 |
| `combo-clash` | ✅ | ❌ 停 | ❌ **仍停** | ❌ 仍停 |
| `merge-2048` | （回合制，静止） | 盘面不动 | **盘面仍不动**：暂停前后盘面指纹完全一致，方向键全部无效 |

`merge-2048` 另有一条肉眼可见的痕迹：`.mg-msg` 一直停在「⏸️ 暂停中,再按一次 Esc 继续。」，永远不会变成「继续!」。

对照组（同样按两次 Esc 就能接着玩）：`weiqi-garden` `sudoku-petal` `mahjong-bloom` `star-estate` `flight-chess` —— 这几款没事。

**根因**（三件事凑在一起）

1. 这 5 款的 `Escape` 处理里没有 `e.preventDefault()`；
2. `src/ui/gameShell.ts` 的 `onGlobalKeyDown` 是延后一拍再看 `e.defaultPrevented` 才决定要不要弹壳层暂停面板，于是面板被弹了出来：

```383:391:src/ui/gameShell.ts
  function onGlobalKeyDown(e: KeyboardEvent): void {
    if (disposed || !isDismissKey(e.key)) return;
    if (dialog || pauseDialog) return;
    window.setTimeout(() => {
      if (disposed || dialog || pauseDialog || e.defaultPrevented) return;
      openPause();
    }, 0);
  }
```

3. 面板一开，`src/ui/dialogs.ts` 就在 `document` 上挂了一个**捕获阶段**的 `Escape` 处理器，它 `stopPropagation()` 之后自己把弹窗关掉 —— 第二次 `Esc` 根本传不到游戏那个 `window` 监听：

```217:225:src/ui/dialogs.ts
  function onKeyDown(e: KeyboardEvent): void {
    if (closed) return;
    if (isDismissKey(e.key)) {
      if (!escapable) return;
      e.preventDefault();
      e.stopPropagation();
      dismiss();
      return;
    }
```

而壳层关面板时调的 `tellGame("resume")` 对这 5 款也是空转 —— 它们的 `mount()` 只返回 `{ destroy }`，没有 `resume` 方法。于是游戏内部那个 `paused = true` 再也没人翻回来。

**建议给修复员的方向**（任选其一，别两头都改）：这 5 款的 `Escape` 分支补 `e.preventDefault()`（和 `mahjong-bloom` / `star-estate` / `flight-chess` / `sudoku-petal` 现在的做法对齐）；或者让它们的 `mount()` 一并导出 `pause` / `resume`，交给壳层面板统一管。

---

### W1-02 · 严重 · 12 款新游戏一款都没补拼音首字母，首页拼音搜索全废

> **状态：已在本分支修掉**（`50565c8` / `c5693e0` 补 12 款新游戏用字，`b7f135c` 顺带把 1.1 遗留的 16 个标题也补齐）。下面是发现当时的原始取证，复核结果见第六节。

首页搜索框写着「找游戏:打名字或拼音首字母」，1.1 起就支持拼音首字母。`src/ui/homeFilters.ts` 里的 `PINYIN_INITIALS` 是一张手工核对的字表，文件里自己也留了话：

```85:91:src/ui/homeFilters.ts
// ---------------------------------------------------------------------------
// 拼音首字母(只覆盖已上架游戏标题里出现过的字,新游戏请顺手补进来)
// ---------------------------------------------------------------------------
```

**这 12 款一款都没补**。逐款算出来的实际首字母串：

| id | 标题 | 应得 | 实际 `pinyinInitials()` | 缺的字 |
| --- | --- | --- | --- | --- |
| `orb-arena` | 圆圆大作战 | `yydzz` | `dz` | 圆 圆 作 |
| `snake-royale` | 长蛇争霸 | `cszb` | **（空）** | 长 蛇 争 霸 |
| `block-drop` | 方块叠叠乐 | `fkddl` | `kl` | 方 叠 叠 |
| `combo-clash` | 连招对决 | `lzdj` | `l` | 招 对 决 |
| `mahjong-bloom` | 花开麻将 | `hkmj` | `h` | 开 麻 将 |
| `star-estate` | 朵星地产 | `dxdc` | `dxd` | 产 |
| `hero-cards` | 英杰令 | `yjl` | **（空）** | 英 杰 令 |
| `weiqi-garden` | 围子花园 | `wzhy` | `zhy` | 围 |
| `flight-chess` | 飞行棋乐园 | `fxqly` | `qly` | 飞 行 |
| `merge-2048` | 星星合成 | `xxhc` | `xx` | 合 成 |
| `mine-garden` | 扫雷花园 | `slhy` | `hy` | 扫 雷 |
| `sudoku-petal` | 数独花田 | `sdht` | `sh` | 独 田 |

**复现步骤**

1. 打开首页，在搜索框里打 `cszb`（长蛇争霸的首字母）。
2. 一个结果都没有，出现空态文案「没找到这个名字的游戏,换个词试试吧!」。
3. 换 `hkmj`（花开麻将）、`sdht`（数独花田）、`yjl`（英杰令），同样搜不到。

`snake-royale` 与 `hero-cards` 最惨：`searchKeys()` 会把空串滤掉，它们只剩下 id 一条路（`snakeroyale` / `herocards`），中文用户不可能打出来。

**影响**：不是崩溃，但首页一个已上线的检索入口对整批新游戏彻底失效，且随着 1.2 上新只会越来越糟。修法很轻：往 `PINYIN_INITIALS` 里补 28 个字（圆 作 长 蛇 争 霸 方 叠 招 对 决 开 麻 将 产 英 杰 令 围 飞 行 合 成 扫 雷 独 田），再补一条「12 款新游戏首字母串长度等于标题汉字数」的回归单测。

---

### W1-03 · 一般 · `playModes.ts` 出货代码 0 引用

`src/engine/playModes.ts`（137 行、23 个单测）在整个 `src/` 里除了自己的测试文件之外**没有任何 import**，也没有从 `src/engine/index.ts` 的统一出口导出。文件注释说它是给「攻略侧栏 / 暂停菜单」用的一句中文，但没有任何一处真的调用 `describeModes()`。

窗口 1 自己那 12 款是各写各的模式条（`.<前缀>-modebar`），一个都没走这套口径。作为「给后面窗口用的共享模块」可以接受，但至少应当从 `engine/index.ts` 导出，否则别的窗口按「从 `../../engine` 导入」的约定根本拿不到，多半会各造一套 —— 那正是这个模块想避免的事。

---

### W1-04 · 一般 · `view25d.ts` 同样 0 引用，1.1 的两份透视副本没有并过来

第 1 步 C 档要求「`view25d.ts` 抽 1.1 跑酷 / 冲冲冲透视」。现在 `src/engine/view25d.ts`（198 行、28 个单测）写好了，数学也对，但：

- 出货代码 0 引用；
- 1.1 的两份原件还在原地各过各的：`src/games/duo-rush/view25d.ts`（184 行，`duo-rush/index.ts` 从 `./view25d` 引它）、`src/games/rainbow-run/view3d.ts`（247 行）。

于是仓库里现在有**三份**同口径的透视数学。`rainbow-run` 是窗口 2 的点名项、`duo-rush` 也不在窗口 1 的清单里，本窗口不该越界去改 —— 但「抽取」目前只做成了「又抄了一份」，需要主管排一次跨窗口的收敛，或者在报告里明确这是有意为之的过渡态。

**收尾复核**：学习优化员的 `ae705fc refactor(view25d): 减弱动效的判定收进共享模块` 之后，`orb-arena` / `block-drop` / `merge-2048` / `flight-chess` / `duo-rush` 五个文件确实 `import ... from "../../engine/view25d"` 了 —— 但引进去的只有 `prefersReducedMotion` 这一个无障碍开关，`project()` / 地平线那套**透视数学仍然一次都没被复用**：`src/games/duo-rush/view25d.ts` 和 `src/games/rainbow-run/view3d.ts` 两份原件原地未动，共享模块也还是没从 `src/engine/index.ts` 导出。所以这条降为「部分解决」，透视三份并存的账留着。

---

### W1-05 · 一般 · `merge-2048` 与 `mine-garden` 抢同一个 `mg-` 前缀

两款窗口 1 的游戏都用 `mg-` 开头命名 CSS 类，撞车的有 8 个：`.mg-wrap` `.mg-modebar` `.mg-open` `.mg-btn` `.mg-msg` `.mg-over` `.mg-over-t` `.mg-over-s`。

眼下**不会真的串味**：两款的 `<style>` 标签都塞在自己的根节点里，游戏一卸载就跟着走，而且同一时刻只会挂一款。但这属于埋着的雷 —— 以后只要出现「首页同时预览两款」「双人拼屏跨游戏」这类需求，或者谁把样式提到 `styles.css` 里，两款的盘面就会互相改样子。

更别扭的是 `mg-open` 在两款里含义完全相反：`merge-2048` 里是「模式入口按钮」，`mine-garden` 里是「已经翻开的格子」（`src/games/mine-garden/index.ts:581`）。建议给其中一款换前缀（例如 `mine-garden` 改 `mn-`）。

---

### W1-06 · 一般 · 「← 回闯关」返回键与模式入口共用 `.<前缀>-open`

各款 `mount()` 里进模式后补的那颗返回键也顶着 `<前缀>-open`：

```1312:1320:src/games/mine-garden/index.ts
      const backBtn = document.createElement("button");
      backBtn.type = "button";
      backBtn.className = "mg-open mg-ghost";
      backBtn.textContent = "← 回闯关";
```

于是「模式入口按钮」和「退出模式按钮」是同一个类，靠 `mg-ghost` 才分得开。这一条本身不影响玩家，但会绊倒任何按类名找入口的自动化 —— 本轮走查脚本第一版就被它带着一路退回选关页，`mine-garden` 三个模式因此都只量到 20 个节点、双人键位误报失败。建议返回键换一个独立类名。

---

### W1-07 · 一般 · 冒烟脚本分裂成两套驱动，仓库里两种都没装

`scripts/` 下 1.2 的六份冒烟脚本里，五份用 `puppeteer-core`，只有 `smoke-1.2-step4-c.mjs`（飞行棋）用 `playwright`；两者都不在 `package.json` 里（脚本头注释也写明是临时工具）。结果就是照着注释 `npm i --no-save puppeteer-core` 装完，跑到第 4 步 C 档直接 `ERR_MODULE_NOT_FOUND: Cannot find package 'playwright'`。

本轮是临时给 `playwright` 搭了个指向 `playwright-core` + 系统 Chrome 的垫片才跑起来的（垫片只在 `node_modules/` 里，没有进 git）。建议统一到 `puppeteer-core` 一种，或者在 `docs/` 里写清两套的装法。

---

### W1-08 · 一般 · `snake-royale` 的皮肤存档 key 不在清单里

硬约束列的存档 key 是 `save.v1`、`l99.<id>`、`l99skip.<id>`、`collection.v1`、`fav.v1`、`recent.v1`、`root.v1`。`snake-royale` 另开了一个：

```77:77:src/games/snake-royale/skins.ts
export const SKIN_KEY = "yiduo-yixing.snake-royale.skin.v1";
```

它是**新增**、不是改动老 key，读写也都包了 `try`，坏档会退回默认皮肤，实际风险很低。但既然约束写的是「只增不改」，新增的这一条应当回写进约束清单，免得后面清缓存 / 导出存档时漏掉它。12 款里只有这一处直接碰 `localStorage`，其余都走 `save.ts` / `level99.ts`。

---

### W1-09 · 一般 · 7 份 `PLAN.md` 混在出货源码目录里

`src/games/{combo-clash,flight-chess,hero-cards,merge-2048,mine-garden,sudoku-petal,weiqi-garden}/PLAN.md`。它们没有被打进 `dist/`（Vite 不收 `.md`），所以不影响体积和离线包，但把设计稿放进 `src/games/<id>/` 与「目录里只放 `meta.ts` / `index.ts` 及玩法模块」的约定不合，其余 5 款也没有这个文件，前后不一致。建议挪到 `docs/`。

---

### W1-10 · 一般 · 复跑 `smoke-1.2-step4-b` 时围棋自由对战少回一手

复跑仓库里现成的 `scripts/smoke-1.2-step4-b.mjs`（围子花园），22 项里 21 项通过，失败的一项是：

```
 FAIL  地狱档自由对战连下三手都有回应 — 已走 2 手（开局 0）
```

同一次跑里「AI 单手用时 ≤ 1s」是过的（最慢 356ms），所以更像是第三手的落点被判为不合法（自填眼 / 打劫禁着）导致这一手没落下去，而不是 AI 卡死。需要在第 2 轮换固定 seed 复现，确认是脚本挑点不够讲究，还是 AI 在某些局面下真的会拒绝回应。

---

### W1-11 · 一般 · 复跑 `smoke-1.2-step4-c` 时 375×667 四色没飞齐

`scripts/smoke-1.2-step4-c.mjs`（飞行棋乐园）78 项里 77 项通过，唯一失败：

```
 FAIL  375×667 四人对战四色都飞上了环线 — 3/16 架在路上
```

同一份脚本在 360×640 与 1280×800 两档都过（1280 档 8/16 架在路上），且这一档「一直在推进 — 已掷 0 → 74 次」是过的。看起来是掷骰随机性造成的偶发不达标（要掷出 6 才能起飞），需要第 2 轮用固定 seed 复跑判定是真缺陷还是脚本阈值定得太死。

---

### W1-12 · 一般 · 第 2、3 步六款没有专用冒烟脚本，本轮拿不到过关证据

第 4、5 步的七款每款都配了一份靠自家 `ai.ts` / `solver.ts` 真解到过关的脚本（`smoke-1.2-step4-a/b/c`、`step5-a/b/c`），所以 `hero-cards` `weiqi-garden` `flight-chess` `merge-2048` `mine-garden` `sudoku-petal` 的「赢一次」都有硬证据。

第 2 步（`orb-arena` `snake-royale` `block-drop`）与第 3 步（`combo-clash` `mahjong-bloom` `star-estate`）**一份都没有**。本轮只能拿通用假人（按配方乱按键 + 乱点可点元素）去撞，结果：

- `star-estate` 撞出了「第 1 关过关！」；
- 另外 5 款补跑 **6 局 × 90 秒**仍然 0 胜。

这不能反推成「这 5 款打不通」—— 乱按键本来就赢不了技巧类游戏。但它确实意味着**本窗口有 5 款的过关路径至今没有任何自动化证据**，同样地，麻将一桌 / 围棋一局 / 地产一盘这些长回合模式的「玩到结算」也取不到。建议第 2 轮补齐这六款的专用冒烟脚本（照 `step5-c` 的写法：页面里 import 自家 AI 挑下一手，再用真鼠标 / 真键盘落子）。

---

## 五、全量跑

### 单元测试

走查当时（`04592b1`）跑到 **232 文件 / 6378 用例**；收尾 rebase 到 `origin/game-1.2-window1`（含修复员、学习优化员这一轮的提交）之后重跑：

```
npm test
→ Test Files  233 passed (233)
        Tests  6445 passed (6445)
     Duration  56.03s
```

**233 个测试文件 / 6445 个用例，全绿。** 相对窗口 1 基线（158 文件 / 4456 用例）多出 **75 个文件 / 1989 个用例**，只增不减。

窗口 1 范围内的用例分布（收尾这一版逐目录数出来的）：

| 模块 | 测试文件 | 用例 | 是否达标（新游戏 ≥ 15） |
| --- | --- | --- | --- |
| `orb-arena` | 3 | 61 | ✅ |
| `snake-royale` | 4 | 137 | ✅ |
| `block-drop` | 6 | 161 | ✅ |
| `combo-clash` | 6 | 161 | ✅ |
| `mahjong-bloom` | 8 | 300 | ✅ |
| `star-estate` | 7 | 109 | ✅ |
| `hero-cards` | 7 | 205 | ✅ |
| `weiqi-garden` | 7 | 130 | ✅ |
| `flight-chess` | 6 | 95 | ✅ |
| `merge-2048` | 4 | 154 | ✅ |
| `mine-garden` | 6 | 155 | ✅ |
| `sudoku-petal` | 5 | 120 | ✅ |
| **12 款小计** | **69** | **1788** | |
| `rootGate` + `root12Contract` | 2 | 49 | |
| `homeFilters` | 1 | 71 | |
| `playModes` | 1 | 23 | |
| `view25d` | 1 | 34 | |
| `mobileText`（窄屏字号，本轮新增） | 1 | 43 | |
| `window1-esc-contract`（W1-01 的回归，本轮新增） | 1 | 13 | |
| `parentAuth`（1.1 原样） | 1 | 44 | |

### 构建

```
npm run build   （tsc --noEmit && vite build）
→ ✓ built in 397ms
→ PWA v1.3.0 · precache 164 entries (3535.55 KiB)
```

**通过。** 类型检查 0 错。唯一一条输出是 Rollup 的既有提示 `INEFFECTIVE_DYNAMIC_IMPORT: src/ui/parentAuth.ts is dynamically imported by src/ui/gameShell.ts but also statically imported by src/ui/parentGate.ts` —— 这是 1.1 就在的分块提示，不是本窗口引入的，也不影响产物。

### 浏览器走查

跑法（`puppeteer-core` 与 `playwright` 都不在 `package.json`，是临时工具）：

```bash
npm i --no-save puppeteer-core
npx vite --port 5185
PART=AB node scripts/smoke-1.2-window1-r1-tester.mjs   # 首页清点 + 平台专项
PART=C  node scripts/smoke-1.2-window1-r1-tester.mjs   # 12 款逐款
PART=W  node scripts/smoke-1.2-window1-r1-tester.mjs   # 补过关证据专场
PART=R  node scripts/smoke-1.2-window1-r1-tester.mjs   # 收尾复核(Esc / 拼音 / 双人分边)
```

| 脚本 | 项数 | 通过 | 说明 |
| --- | --- | --- | --- |
| `smoke-1.2-window1-r1-tester.mjs` `PART=A`（首页清点） | 15 | 15 | 全过 |
| `smoke-1.2-window1-r1-tester.mjs` `PART=B`（平台专项） | 40 | 39 | 走查当时唯一失败＝W1-02 拼音搜索 |
| `smoke-1.2-window1-r1-tester.mjs` `PART=AB`（收尾在 rebase 后重跑） | 55 | **55** | 拼音那条已转绿，平台全项通过 |
| `smoke-1.2-window1-r1-tester.mjs` `PART=C`（12 款逐款） | 226 | 189 | 失败集中在「赢一次」与长回合模式的「玩到结算」，见 W1-12 |
| `smoke-1.2-window1-r1-tester.mjs` `PART=R`（收尾复核） | 21 | **21** | W1-01 五款 + W1-02 十二串 + `mine-garden` 双人分边 |
| `smoke-1.2-window1-r1-tester.mjs` `PART=W`（补过关） | 5 | 0 | 见 W1-12 |
| `smoke-1.2-step4-a.mjs`（英杰令） | 65 | 65 | 全过 |
| `smoke-1.2-step4-b.mjs`（围子花园） | 22 | 21 | 见 W1-10 |
| `smoke-1.2-step4-c.mjs`（飞行棋乐园） | 78 | 77 | 见 W1-11；需 playwright 垫片才跑得起来，见 W1-07 |
| `smoke-1.2-step5-a.mjs`（星星合成） | 22 | 21 | 唯一失败＝「再按一次 Esc 就接着玩」，即 W1-01 |
| `smoke-1.2-step5-b.mjs`（扫雷花园） | 19 | 19 | 全过 |
| `smoke-1.2-step5-c.mjs`（数独花田） | 25 | 25 | 全过 |

---

## 六、收尾复核（rebase 到 `origin/game-1.2-window1` 之后）

收工前按纪律 `git fetch` → rebase，上游这时已经多出 15 个提交：修复员与学习优化员在同一分支上把本轮报上去的两条大问题（W1-01、W1-02）以及若干窄屏字号问题修掉了。**测试员不认别人的自证**，在 rebase 后的代码上把这几件事自己重新量了一遍（`PART=R`，21 项全过）。

### 复核 1 · W1-01 五款的 Esc 暂停 / 恢复

做法：进第 1 关 → 先确认画面在动（连拍两次 DOM + canvas 指纹；回合制的款改用「按方向键盘面会不会变」）→ 按一次 `Esc` → 看有没有暂停提示 → 再按一次 `Esc` → 再确认画面又动起来了。

| 款 | 开局在动 | Esc 一次有暂停提示 | Esc 两次后又动起来 |
| --- | --- | --- | --- |
| `orb-arena` 圆圆大作战 | ✅ | ✅ | ✅ |
| `snake-royale` 长蛇争霸 | ✅ | ✅ | ✅ |
| `block-drop` 方块叠叠乐 | ✅ | ✅ | ✅ |
| `combo-clash` 连招对决 | ✅ | ✅ | ✅ |
| `merge-2048` 星星合成 | ✅ | ✅ | ✅ |

五款全部恢复正常，**这条阻断解除**。修法与本报告的建议一致（游戏侧拦住 `Escape`），另外还留下了 `src/games/__tests__/window1-esc-contract.test.ts` 这份 13 个用例的回归。

### 复核 2 · W1-02 拼音首字母搜索

在首页搜索框里真敲首字母，看卡片列表里还剩谁：

| 首字母 | 期望 | 结果 |
| --- | --- | --- |
| `yydzz` / `cszb` / `fkddl` / `lzdj` | 圆圆大作战 / 长蛇争霸 / 方块叠叠乐 / 连招对决 | ✅ 四次都收窄到 1 张，就是它 |
| `hkmj` / `dxdc` / `yjl` / `wzhy` | 花开麻将 / 朵星地产 / 英杰令 / 围子花园 | ✅ 同上 |
| `fxqly` / `xxhc` / `slhy` / `sdht` | 飞行棋乐园 / 星星合成 / 扫雷花园 / 数独花田 | ✅ 同上 |

**12 串全中，这条严重问题解除。**

### 复核 3 · `mine-garden` 双人键位（撤回误报）

见第二节「双人键位取证」的分边表：朵朵那套只动左半、星星那套只动右半，互不串台。原先两遍走查报失败是因为整屏指纹在「同种子镜像盘」上分辨不出，属于**取证手段的问题**，此条撤回。

### 复核 4 · 平台五项与全量跑

- `PART=AB` 在 rebase 后的代码上重跑：**55 项全过**（走查当时是 55 项过 54 项，差的就是拼音那条）。root 门的密码 / 电话 / 锁 120 秒 / 只写 `{expiresAt}` / 一小时过期 / 密码不落盘、直达第 N 关的越界夹取与不发星、家长算术门的四个参数、平台芯片四条件叠加、`view25d` 禁 three.js —— 逐条都是在新代码上重新量的。
- `npm test` **233 文件 / 6445 用例全绿**；`npm run build` 类型检查 0 错、构建通过。

### 复核 5 · 剩下的十条一般项现在是什么状态

| 编号 | 现状 |
| --- | --- |
| W1-03 `playModes` 0 引用 | 仍在：出货代码里依旧没有任何 import，也没从 `src/engine/index.ts` 导出 |
| W1-04 `view25d` | 部分：五个文件开始引它，但只引了 `prefersReducedMotion`；透视数学仍是 `engine` / `duo-rush` / `rainbow-run` 三份 |
| W1-05 `mg-` 前缀撞车 | 仍在：`merge-2048` 与 `mine-garden` 还是同一个前缀 |
| W1-06 返回键与模式入口同类名 | 仍在 |
| W1-07 冒烟脚本两套驱动 | 仍在：`step4-c` 还是 `playwright`，两个包都不在 `package.json` |
| W1-08 `snake-royale` 皮肤 key | 仍在：`yiduo-yixing.snake-royale.skin.v1` 还没回写进约束清单 |
| W1-09 7 份 `PLAN.md` | 仍在：7 份原地不动 |
| W1-10 / W1-11 两条冒烟偶发 | 留给第 2 轮定 seed 复现 |
| W1-12 六款缺专用冒烟脚本 | 仍在：第 2、3 步那六款还是只能靠通用假人 |

---

## 七、本轮结论

- **平台五项（root 门 / 直达第 N 关 / 家长算术门 / 手游端游筛选 / 2.5D 禁 three.js）逐条取证通过**；走查当时唯一挂掉的拼音搜索，收尾复核已转绿。
- **12 款全部上得了首页、36 个战役关卡全部进得去画得出、`destroy` 全部不泄漏、360px 全程不溢出、双人两套键位都接得住、商标扫描 0 命中。**
- 本轮报出 **1 条阻断 + 1 条严重 + 10 条一般**；阻断（W1-01）与严重（W1-02）已在同一分支被修复并经测试员复核解除，W1-04 降为部分解决，其余 9 条留给第 2 轮。
- **本轮取证的最大缺口**：第 2、3 步那六款（`orb-arena` `snake-royale` `block-drop` `combo-clash` `mahjong-bloom` `star-estate`）没有专用冒烟脚本，通用假人在预算内打不出过关，也打不完一桌麻将 / 一盘围棋 / 一局地产。这不等于它们有缺陷，但意味着**「赢一次」和长回合模式的「玩到结算」至今没有自动化硬证据**，第 2 轮应当优先补齐（W1-12）。
- 全量：`npm test` **233 文件 / 6445 用例全绿**，`npm run build` 通过。
