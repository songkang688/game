# 窗口5 · 第3轮监督修复员（本窗收官）

> 范围仅限窗口 5 独占的 15 款游戏。不改窗口 1 的平台文件，不碰别的窗口的游戏目录。
> **实际模型 slug**：`claude-opus-5-thinking-high-fast`
> **分支**：`game-1.2-window5`（不推 `main`、不推 `game-1.2`、禁止 `--force`）
> **输入**：本轮 `1.2-window5-round3-tester.md` 的 `# 收官 · 全 15 款终检`、
> `1.2-window5-round3-learner.md` 的 L1 / L2，以及第 1 / 2 轮六份报告的遗留表。

## F0 · 这一轮我要签的字

收官轮监督修复员只签三件事，签不了就写清楚为什么签不了：

1. **本轮测试员记的阻断 / 严重，全部清零**（不是「有对策」，是**真机上再量一遍已经不复现**）。
2. **商标扫描 0 命中，`destroy` 无泄漏**。
3. **前两轮 + 本轮的每一条遗留都有最终结论**——已修 / 仍在＋原因＋交谁 / 不修＋理由，**三选一，不许空着**。

## F1 · 阻断与严重的清零账

| # | 款 | 严重度 | 改动 | 真机复验（同一台探针、同一把尺子重打） |
| --- | --- | --- | --- | --- |
| `W5R3-A-02` | `word-garden` 组字工坊 | **阻断** | 窄屏四选一收进一行（`min-width` 70→54、`gap` 10→6、`padding` 14→10）＋ 补接 `fitQuizHost` 兜底 ＋ 钳住时松开 `min-height` | 320×568 L188：四颗选项 **4/4 `elementFromPoint` 命中自己**（修前 3/4，第四颗滚不动）；`.bc-wrap` 下沿 633→**554**（正好压在舞台裁切线上）；360×640 与 390×844 复验 4/4，390 档不挂滚动条（高屏一个像素没变） |
| `W5R3-C-02` | `kitty-care` | **阻断** | 粘住的提示行改 `pointer-events:none` ＋ 挂滚动条时 `showPlayRow()` 把动手层送进眼里（减掉粘住那一行的高度再算） | **L1 / 63 / 117 / 188 × 320×568 / 360×640 八格**，落地即 `elementFromPoint` 逐颗自证：食物 / 饭碗 / 吸附圈 / 按钮 **全部 100% 命中**（修前 360×640 L188 落地 0/5、L117 落地 0/4） |
| `W5R3-A-01` | `landlord-cards` | 严重 | 提示与按键统一到 `PASS_WORD` / `PASS_BUTTON_LABEL` | 源码巡检 + 用例守门：`hint.ts` / `index.ts` 里不许再出现散落的「不要」 |
| `W5R3-A-03` | `word-garden` 描红台 | 严重 | 第三条入口补接 `fitQuizHost` | 320×568 / 360×640 L117 重扫：**hard-clip 归 0**（修前 `.wgd-msg` 0/45px、田字格 241/259） |
| `W5R3-B-01` | `shape-kingdom` | 严重 | 常驻控件摞按量收薄（`needsTightDock` + `TIGHT_DOCK_CSS`） | 320×568 L117：`.shk-dock` **174→94px**，可滚距离 177→97；滚到 75% 起 **35/35 颗点同时够得着**（修前任何滚动位置最多 28/35）。360×640 / 390×844 不触发收薄，读数与修前一致 |
| `W5R3-B-03` | `shape-kingdom` | 一般 | 钳完顺手 `showBoard()` | L117 × 320×568 / 360×640 / 390×844 三档：落地 `scrollTop` 分别 85 / 102 / 0，**38/38 个可点元素全部命中**（修前落地 21/35） |
| `W5R3-C-01` | `poop-hero` | 严重 | 画布收到底仍装不下时壳层自己挂滚动条（`wrapRoomPx`，下限 240px） | 320×568 / 360×640 / 390×844：`overflowY=auto` 与 `maxHeight` 正确写入，滚到底之后垃圾桶名与提示行全部可见 |
| `W5R3-B-02` | `red-blue-tug` | 严重 | 在「扣场地 → 减空隙」后加两档：收字号（`rbg-tighter`）→ 逐档扣按钮（`--rbg-pull-h`，底线 56px） | 320×568：**L1 提示行 31/36 → 33/33 全可见；L117 1/36 → 33/33 全可见；L188 0/36 → 14/33**（第一行完整）。两颗大按钮 L188 由 51/76 变成 **56/56 完整**，且 56 ≥ 44 热区下限。**残留见 F3** |

**清零结论：本轮阻断 2 条、严重 5 条、一般 1 条，代码全部落地并在真机上复验。**

## F2 · 硬约束逐条自查

| 约束 | 做法 | 结果 |
| --- | --- | --- |
| **商标黑名单** | 29 个词（`愤怒的小鸟` `植物大战僵尸` … `王者荣耀`）× 本窗口 15 款目录**全部文件含注释** | **真命中 0** |
| **`destroy` 无泄漏** | 15 款逐款进关 → `destroy()` → 读残留句柄 | window 监听 **0**、document 监听 **0**、rAF **0**、interval **0**、timeout **0**（`AudioContext` 计数 1 是平台 `api.play` 的共享实例，非各款自建） |
| **44px 热区** | 每一处收紧都单独有一条用例盯下限 | `shape-kingdom` 收薄档 `min-height:44px` 原样重申；`word-garden` 窄屏档只动 `min-width`（54 ≥ 44）不动 `min-height`；`red-blue-tug` `MIN_PULL_H=56`，`.rbg-toggle` / `.rbg-back` 一格没碰 |
| **无广告 / 无内购 / 无联网 / 无 three.js / 无 CDN 外链** | 本轮 8 个 commit 未新增任何 `import`、未新增任何依赖 | 通过 |
| **存档 key 只增不改** | 本轮改动全在布局与文案层，一个 storage 读写都没动 | 通过 |
| **公共文件不越界** | 本轮改动清单见 F4 | **全部落在窗口 5 独占的 6 个游戏目录内**；`src/styles.css` / `src/ui/**` / `src/engine/**` / `level99.ts` / `quiz99.ts` **一个字节未改** |
| **用例只增不减** | 见 learner L2 | 新增 82 个 `it(` 断言块，一条都没删 |
| **失败只鼓励** | 本轮打出的所有败局文案 | 「就差一点点！」「这几个字挺会藏的，喝口水我们再拼一次！」——只鼓励，无评价，无血无死亡 |

## F3 · 遗留最终结论（一条不许空着）

| # | 事 | 归属 | 最终结论 |
| --- | --- | --- | --- |
| `W5R3-B-02` 残留 | `red-blue-tug` 320×568 **L188** 提示行仍被裁 19/33px（第一行完整可读） | 窗口 5 | **仍在，交主管，不在收官轮再改**。本款**故意不挂滚动条**（拔河是按住不放的连点玩法，挂了滚动条手指一滑就松力，比看不见半行更糟）。四档收紧已到底：场地 76px 底线、按钮 56px 底线、字号已收一档；再压就得动 44px 热区，**不换**。根因是 `.rbg-gear` 的机关胶囊数随章节**无上限增长**（L1 44px → L188 104px），根治要给胶囊排「按重要度收起」的规则，属玩法改动 |
| `W5R3-D-01` | `red-blue-race` 320×568 L188 `.rbr-msg` 可见 23/30px | 窗口 5 | **仍在，不修**。本款已有两档收紧且 L188 已在第二档、提示行已收到 11px；第一行完整、热区完好、玩法不受影响。第三档换 7px 不值一次全屏布局抖动 |
| `W5R3-D-02` | `find-diff` 26×26 格子被判「小 + 出屏」 | 窗口 5 | **不修，结案（探针噪声）**。格子视觉 26px，代码里有 **22px 命中半径**兜底，实际热区 44px；「出屏」是 `.fdf-viewport` 内部横向滚动的正常表现，探针自己标了 `rescuable:true`；360 档量到 `reachable:true` |
| `W5R3-D-03` | `landlord-cards` 每关 11–12 个 `.ld-c-big` 被判「被裁」 | 窗口 5 | **不修，结案（探针噪声）**。手牌是扇形叠放，后一张压住前一张的右半边本来就是设计；逐个量下来差的是 1–3px 横向重叠，最上面那张永远完整 |
| 平台 · `quiz99.ts` 连错闪答案 | 连错 2 次时给正确选项挂 `qz-hint` 一闪一闪，并朗读「一闪一闪的那个就是答案」。本轮 25 格触发 **57 次** | **窗口 1 / 主管** | **仍在，我一行不动**。与「学习三连提示不直接给答案」的红线直接冲突，但 `src/games/quiz99.ts` 是跨窗口共用文件，不在窗口 5 所有权范围。**建议**：改成「划掉一个错项」或再给一句方向，保留兜底、不给答案。**本窗口五款自己的三级提示（`hints.ts` / `safeHints`）另有 `noSpoiler` 用例守门，不给答案，这条只针对平台侧那一闪** |
| 平台 · `.game-stage{overflow:hidden}` 定高硬裁 | 本轮 8 条缺陷里 6 条的根因 | **窗口 1** | **仍在，`src/styles.css` 一行未动**。本窗口一律在自己的壳里「量一次舞台下沿 → 自己钳 → 自己滚 → 自己把该看的送进眼里」，五款各存一份实现（见 F5）。等平台侧愿意收的时候一次合并 |
| 第 1 / 2 轮遗留 | `W5-A-01…07` / `W5-B-01…11` / `W5-L-*` / `W5R2-*` / `FBS-01…04` / `FB-01…04` / `FC-*` | 窗口 5 | **本轮 90 格重扫未见任何一条回潮**。前两轮记在教育正确性头上的 5 处「分歧」，本轮追下去**全部是判据自己的 bug**（上午/下午 12 点归属、「N 点半」写法、声调标在哪个韵母上），判据修好后分歧归零，**这几条对游戏的指控收官轮撤销** |

## F4 · 本轮改动登记（8 个 commit，全部落在窗口 5 独占目录）

| commit | 款 | 文件 |
| --- | --- | --- |
| `bf1f07a` | `landlord-cards` | `hint.ts` `index.ts` `hint.test.ts` `shell.test.ts` |
| `673b064` | `poop-hero` | `runtime.ts` `index.ts` `wrapScroll.test.ts`（新） |
| `7ae8a01` | `shape-kingdom` | `dockTight.ts`（新） `draw.ts` `dockTight.test.ts`（新） |
| `0acf5b4` | `word-garden` | `buildChar.ts` `fit.ts` `buildCharFit.test.ts`（新） `stageFit.test.ts` |
| `bca25bd` | `kitty-care` | `runtime.ts` `styles.ts` `playRow.test.ts`（新） |
| `f0635ba` | `red-blue-tug` | `fit.ts` `index.ts` `tighterFit.test.ts`（新） `tightFit.test.ts` |
| `d5cfaa9` | `shape-kingdom` | `dockTight.ts` `draw.ts` `dockTight.test.ts` |
| `d5fdc8d` | `word-garden` | `tracing.ts` `buildCharFit.test.ts` |

**未改动**：`src/styles.css`、`src/ui/**`、`src/engine/**`、`src/games/level99.ts`、`src/games/quiz99.ts`、
`src/games/speech.ts`、`parentAuth.ts`、`save.ts`、`collection.ts`，以及**其他窗口的任何游戏目录**。

## F5 · 一个刻意的重复，写在这里免得以后被当成疏漏

本窗口现在有 **5 份**「量舞台下沿 → 自己钳 → 自己滚」的近似实现：

| 文件 | 这一份多做了什么 |
| --- | --- |
| `poop-hero/runtime.ts` `wrapRoomPx` | 画布收到底仍装不下时，壳层自己挂滚动条 |
| `shape-kingdom/dockTight.ts` | 按量收薄常驻控件摞；钳完把图形整张送进眼里 |
| `word-garden/fit.ts` `fitQuizHost` | 钳位 + 把选项排送进眼里 + 松开 `min-height`；答题屏 / 组字工坊 / 描红台三条入口共用 |
| `kitty-care/runtime.ts` `fitIntoStage` / `showPlayRow` | 先逐档收猫、收不下才滚；滚了就把动手层送进眼里（要减掉粘住那一行的高） |
| `red-blue-tug/fit.ts` | **不挂滚动条**，改成四档收紧（扣场地 → 减空隙 → 收字号 → 扣按钮） |

**这是故意的，不是复制粘贴的疏忽。** 抽成公共件得放 `src/games/` 根或 `src/ui/**`，
那是跨窗口共用目录，本窗口无权改。每份文件的头注释里都写明了这一点。
真要合并，得等平台侧（窗口 1）把 `.game-stage` 的定高硬裁一起收掉——
到那天这五份大半可以直接删掉，而不是搬家。

## F6 · 收尾水位

| 项 | 结果 |
| --- | --- |
| `npm test` | **704 个测试文件 / 15076 条用例，全绿**（240s） |
| `npm run build` | **通过**（PWA precache 188 entries / 5107.79 KiB） |
| 本窗口 15 款用例合计 | **2487 条**（`vitest` 实计），每款都远高于「新游戏 ≥ 15 条」的下限 |
| 商标 | 29 词 × 15 款目录，**真命中 0** |
| `destroy` 泄漏 | 15 款全 0 |
| 分支 | `game-1.2-window5`（已 rebase 到主管回写的 `origin/game-1.2-window5` 之上，无 `--force`） |

## F7 · 发布结论

**窗口 5 这一批 15 款可以合入 `game-1.2`。**

本轮阻断 2 条、严重 5 条、一般 1 条全部已修并真机复验；
教育正确性 99 道能独立复算的题**分歧 0**，前两轮的教育疑点收官轮撤销；
泄漏 0、商标 0、热区下限一处未破、用例只增不减、构建通过。

四条遗留全部有最终结论，其中两条属平台侧（`overflow:hidden` 硬裁、`quiz99` 连错闪答案），
**只登记不代改，交窗口 1 与主管**；它们不阻塞本批合入——
前者本窗口已在各款壳里自行兜住，后者是兜底提示的强度问题、不影响题目正确性。

---

## 档C · 五款

（`poop-hero` / `red-blue-tug` / `pinyin-train` / `find-diff` / `kitty-care`）

> 本节由**档C 监督修复员**独立填写，**新增**在 F0–F7 之后。
> 上面 F0–F7 是并行执行者写的「收官监督修复员」正文，**一个字节都没动、没重排、没删**。
> 档A / 档B 两节若还不存在，也不由我代写。
> tester / learner 报告与本文件文末的 `R3-*` 一律**只读**。

### C0 · 清零与终审计划（动 `src/` 之前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 3 轮（收官）** · **档C 监督修复员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，**不另开分支**；不推 `main` / 不推 `game-1.2` / 禁止 `--force` / 不用 `gh` 开改 PR）
**基线**：`f826748`（`git fetch origin game-1.2-window5` 后一格不差，即档C 学习优化员 C1–C6 那一条）
**没派任何云端子代理**——提示词里那句「请通过 Task 派生」是写给派发方看的，我就是执行者，活自己干完。**不开第 4 轮。**

#### 我要签的字（签不了就写清为什么）

1. learner 宣称落地的 **5 条**，我**自己拿探针从头量一遍**，量化数字自己复算，逐条判 **真修 / 假绿 / 空转**——
   照抄别人报告里的数字等于没验。
2. **本档能修的阻断 / 严重清零**：复算里发现假绿或新的阻断 / 严重，就在本档五款目录内补修，**先红后绿**，失败原文留在本节。
3. **终审（本档五款，不是全库 76）**：商标 0 真命中、`destroy` 各进出 10 次句柄不涨、动物福利复扫、
   教育两条守门（`pinyin-train` 声调 / `find-diff` 二级提示只登记）、脏存档不白屏、合并态自跑 `npm test` + `npm run build`。
4. learner **不落地的 7 条**逐条表态；交窗口 1 的三条**维持只量不改，绝不写成已解决**。

#### 我的尺子（和别人报的数字对不上时，以我这把为准）

| 项 | 口径 |
| --- | --- |
| 「点得着」 | 只认 `document.elementFromPoint(元素中心点)` 拿回它自己或它的后代。**全程禁止 `el.click()`** |
| 「滚得着」 | 真手指 `touchStart → touchMove ×N（分段） → 停 220ms → touchEnd`，**不 poke `scrollTop`**；纵向来回各一趟，每趟后重新 `elementFromPoint` |
| 裁切线 | **padding box**：`rect.top + borderTopWidth + clientHeight`，`.game-stage` 那 4px 白边不算进可视段 |
| 「被裁的字」 | 扫 `.game-stage` 下**所有有文字的叶子节点**，可见高 ≤ 0 且**没有可滚祖先**的记为「裁死」 |
| 视口 | 横屏必测 **640×360** / **844×390**，外加测试员用过的矮横屏；竖屏对照 **320×568** / **360×640** / **390×844** |
| 反例 | 同一支探针在**补丁前的 commit**（`562790f^` = `64adef7`、`4ab45e8^` = `562790f`）上跑一遍，证明旧侧红、新侧绿。没有反例的「绿」一律记为**空转** |
| 真机 | `npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖** |

#### 权限边界

- **只动本档五款自己的目录**：`src/games/{poop-hero,red-blue-tug,pinyin-train,find-diff,kitty-care}/**`。
- **一个字不改**：`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css`。
- **动物福利红线**：`kitty-care` 生产代码保持**零 `ctx.lose` / 零 `onLose`**，失败只鼓励；改完独立复扫。
- 报告**只写本文件的 `## 档C · 五款` 这一节**；F0–F7 与 `R3-*` 一个字节不动，不替档A / 档B 写节。
- 不验 76 款全集、不改 README / 窗口 1 平台、**不把本窗分支合进 `game-1.2`**。
- **收官轮不许大重构**：补修一律小而准，改一处就把这一处的回归测试补上。
- 探针落 `/tmp/w5r3cf/`，**跑完即删、0 字节进提交**；不引入依赖、不动存档 key。

#### 要独立复算的 5 条（learner 宣称全落）

| # | 级 | 款 | learner 的说法 | 我怎么复算 |
| --- | --- | --- | --- | --- |
| `W5R3-C-03` | **严重** | `poop-hero` | `WRAP_MIN_ROOM` 240→56 当下限改底线、`wrapRoomPx()` 不超可视段、`showPad()`、6/6 落地即可点 | 三档横屏 + 两档竖屏逐颗 `elementFromPoint`；双人 12 柄一并打；反例跑 `64adef7` |
| `W5R3-C-04` | **严重** | `red-blue-tug` / `find-diff` / `kitty-care` | 拔河第五档滚动兜底、找不同顶栏折行 rAF 重量 + 工具条、相册 `LIST_MIN_ROOM` 160→44（24/24 滚动累计） | 三款分开量；相册那 24 颗用**真手指**逐档翻，不 poke `scrollTop`；反例分别跑 `64adef7` / `562790f` |
| `W5R3-C-05` | 一般 | `red-blue-tug` | `showPull()` 连 `.rbg-msg` 一起送进来，三档全见 | 量 `.rbg-msg` 的可见高（不是「存在」），三档都要；反例跑 `562790f` |
| `W5R3-C-06` | 一般 | `kitty-care` | `showPlayRow()` 退一层挑 `.ktc-tray`，4/4 落地可拖 | L117 双猫关四档；连 `.ktc-drag small` 名字行的可见高一起量 |
| `W5-L-36` | 一般 | `kitty-care` | 护理进度提到 `Arena` 实例，重画不再清零 | 真机触发一次重画（点猫 / 躲纸箱）看进度条；外加读代码确认「只从一个入口写」 |

**不开下一轮**：本节写完即收官，不写 tester / learner，不派任何人。

---

### C1 · 独立复算表（learner 宣称落地的 5 条，逐条判真修 / 假绿 / 空转）

**取证怎么做的**：`npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带
`WebSocket` 直连 CDP，**零新增依赖**。点得着只认 `document.elementFromPoint(中心点)` 拿回它自己或
它的**后代**（拿回祖先不算——那说明它自己被压住了；**全程一次 `el.click()` 都没有**）。
滚得着一律真手指 `touchStart → 18 段 touchMove → 停 220ms → touchEnd`，**一次都没 poke `scrollTop`**。
裁切线按 **padding box** 算。**反例**：同一支探针在补丁前的 commit 上重跑
（`562790f^` = `64adef7`、`4ab45e8^` = `562790f`，各自 `vite build` + 独立端口 preview）。
视口：横屏 **640×360 / 844×390**，外加**我自己加的一档矮横屏 568×320**（把 320×568 那台机器转 90°），
竖屏对照 **320×568 / 360×640 / 390×844**。探针落 `/tmp/w5r3cf/`，**跑完即删、0 字节进提交**。

| # | 级 | 我的判定 | 我自己量出来的数（新侧） | 反例（旧侧同一支探针） |
| --- | --- | --- | --- | --- |
| `W5R3-C-03` | **严重** | ✅ **真修** | 单人 `.ph-key` **6/6 落地即可点**：640×360（`.ph-wrap` max 177、落地 `scrollTop` 118）/ 844×390（147、88）/ 568×320（271、184）/ 320×568（62、3）/ 390×844（无可滚祖先）。**双人 12 柄 12/12**：640×360 max 203、落地 144；844×390 max 173、落地 114。`.ph-tip` 落地看不见但真手指一趟就到（累计 1/1）。五档裁死的有字叶子 **0** | `64adef7`：640×360 **0/6，vis 全 0**，`.ph-wrap` 写着 `max-height:240px` > 可视段 190px——**滚动口自己有一截在屏外**，真手指慢拖 8 趟 `scrollTop` 一格没动（轨迹 `[0,0,0,0,0,0,0,0]`），累计仍 0/6；844×390 0/6（前两颗只露 16px）；双人 640×360 **0/12**，慢拖 8 趟累计 0/12 |
| `W5R3-C-04` | **严重** | ⚠️ **真修，但没修干净**（见 C2） | **(a) 拔河**：`.rbg-pull` **2/2 落地即可点** × 640×360 / 844×390 / 568×320，`rbg-wrap rbg-tight rbg-tighter rbg-scroll`、`overscroll-behavior:contain`、`.rbg-pull` `touch-action:none` 三样都在。**(b) 找不同**：`.fdf-tools` 两颗真控件 **2/2 落地即可点** × 五档。**(c) 相册**：640×360 逐档滚动累计 **24/24**（`.ktc-grid` max 1595）、844×390 **24/24**（1565）、320×568 **24/24**（2345）、390×844 **24/24**（2071）——**与 learner 报的数一个像素不差** | `64adef7`：拔河 640×360 **0/2 vis 0**、慢拖 0/2、裁死的有字叶子 7 个；找不同 640×360 **0/2**（只露 9px）；相册 640×360 / 844×390 **0/24，无可滚祖先，慢拖累计 0/24**，裁死的有字叶子 **90 / 84 个** |
| `W5R3-C-05` | 一般 | ✅ **真修** | `.rbg-msg` **1/1 落地即可见，vis 16/16 满格** × 320×568（落地 `scrollTop` 22 / max 28）/ 640×360（115 / 121）/ 844×390（62 / 68）/ 568×320（209 / 215）。文案原文读得出来：「看到 🟢 才按住拉，🔴 时松手歇着攒体力!」 | `562790f`（`showPull()` 还只保按钮那一版）：四档**全部 `vis 0/16`**，落地 `scrollTop` 分别 3 / 96 / 43 / 190——**都比新侧少滚十几像素，少的正是提示行那一条**；320×568 那格裁死的有字叶子里明明白白就是 `.rbg-msg` |
| `W5R3-C-06` | 一般 | ✅ **真修** | `.ktc-drag` **4/4 落地即可拖**，且**名字行 `.ktc-drag small` 也 4/4、vis 15/15 满格** × 640×360（max 285）/ 844×390（255）/ 320×568（242）/ 360×640（102）/ 568×320（379）/ 390×844（无可滚祖先，一个像素没变）。六档裁死的有字叶子 **0** | `64adef7`：640×360 打扮件只露上半截（**vis 34/58**）、**名字行 0/4 且 vis 全 0**——正是 learner 描述的那一幕；568×320 打扮件 **0/4**，慢拖 8 趟 `scrollTop` 卡死在 217（轨迹全平），累计 0/4 |
| `W5-L-36` | 一般 | ✅ **真修** | 真机第 157 关（暖心诊所带心情条那一档）走完整条路：做对第 1 步 →「1.🐾 看看小爪子 → 2.❓ 动手照顾」→ 一路点错把心情耗光 → 猫钻纸箱 → 摸三次它探出头（**这一下就是重画**）→ 计划行仍是「1.🐾 看看小爪子 → 2.🤏 轻轻拔小刺 → 3.❓ 动手照顾」，**一步没丢**；护理台摆的是**下一步**的工具。全程失败屏 0、伤害词 0 | `64adef7`：同一条路、同一关、同样先做对两步，探出头之后计划行**退回「1.❓ 先看一看 → 2.· · · → 3.· · ·」**，工具也摆回第 1 步那一批 |

**两处我复算不出来的，据实写在这里，不替它圆场**：

| 事 | learner 的说法 | 我量到的 | 我的判定 |
| --- | --- | --- | --- |
| `W5R3-C-04` 里 `find-diff` 「顶栏折行量早」那一小段（`4ab45e8` 的 `requestAnimationFrame` 重量） | 「320×568 上实测 `.fdf-wrap` 382px、可视段 330px，兜底一次都没触发」 | 我在 320×568 上量到的是 `.fdf-wrap` `scrollHeight` **330 = clientHeight 330**——**它本来就装得下**，工具条补丁前后都是 **2/2 落地即可点**。把 `562790f`（只有第一版兜底、没有 rAF）单独 build 出来对比：640×360 与 568×320 **两档的读数与新侧一模一样** | **空转**（无害）。`find-diff` 兜底本身是**真修**，功劳全在 `562790f`；rAF 那一小段在我这五档视口上**一处差异都量不出来**。它是防顶栏折行竞态的保险，`destroy()` 里有 `liveFit=false` 兜着，**不 revert**，只如实登记 |
| tester `C13` 记的「无可滚祖先」 | `W5R3-C-03` 现象写作「全部落在裁切线以下且**无可滚祖先**」 | 补丁前 `.ph-wrap` **是有** `overflow-y:auto` 的（`W5R3-C-01` 那一修加的，max 141），只是**推不动**——真手指 8 趟 `scrollTop` 恒为 0 | 措辞出入，**结论不变**：够不着 0/6 是真的。差别只在「没有滚动条」还是「有滚动条但滚了也没用」——后者更准，因为病灶正是**滚动口自己有一截在屏外** |

### C2 · 补修（本档能修的严重清零）：`W5R3-CF-01`

**我发现的**：`LIST_MIN_ROOM` 从 160 改判成 44，只是把那道悬崖从 160 **挪到了 44**，没有把它填掉。
把手机横过来拿得再矮一点——**568×320，也就是把 320×568 那台机器转 90°**——舞台看得见 170px，
而卡片格上面还压着「◀ 回选关」+ 四个摆放位置 + 一行说明，`.ktc-grid` 自己的可视段只剩 40 来 px，
`< 44` 于是那条早退**照旧生效**：

```
补修前 · 相册兑换钮 568×320（新侧 f826748，不是旧侧）
  .ktc-grid  max-height:none / overflow:visible，scrollHeight 1724 = clientHeight 1724
  兑换钮 .ktc-grid .ktc-mini: 落地 0/24，可见 0/24，**一个可滚祖先都没有**
  真手指慢拖 30 趟：scrollTop 恒 0 → 逐档累计 **0/24**
  .game-stage 下裁死的有字叶子：**93 个**（24 张卡片的名字 / 说明 / 兑换钮全在里面）
```

星星兑换是相册唯一的主动玩法，一颗都点不着＝这一屏白摆，与 `W5R2-C-03` 当初记成阻断的**是同一件事**，
只是换了一档视口。本档能修，**按严重补修**。

**改法**（小而准，收官轮不动玩法）：卡片格自己挤不出一颗兑换钮的中心点（44px）时**往外退一层**，
把整块 `.ktc-album` 钳进可视段并挂滚动条。退路（「◀ 回选关」）跟着一起滚了——这是代价；
可「翻得到全部」比「退路永远钉在眼前、卡片一张都够不着」值钱。判定抽成纯函数
`listOrShellRoom(listRoom, shellRoom, minRoom)`，**卡片格自己有地方时返回 `who:"list"`，行为一个字节不变**。

**先红**（`src/games/kitty-care/albumShellScroll.test.ts`，改代码之前真跑了一遍，16 条里 **9 红 7 绿**）：

```
× listOrShellRoom 这把尺子 > 真机 568×320 那一幕：卡片格只剩 40px，整块相册板还有 162px —— 退一层
  TypeError: listOrShellRoom is not a function
× scrollIntoStage 接上这一层之后 > 568×320 那一幕：卡片格一格没钳，改钳整块相册板并挂上滚动条
  AssertionError: 整块相册板得钳进可视段，不然 24 颗兑换钮一颗都点不着: expected '' to be '162px'
× scrollIntoStage 接上这一层之后 > 重排先还原两层再量，不然转一次屏就越量越小
  AssertionError: 反复 relayout 得稳在同一个数: expected '' to be '162px'
× 接线与样式 > 相册那一处真的把整块板子传进去了，不然这一档兜底永远不会触发
  AssertionError: scrollIntoStage 还是只传了卡片格: expected 'import { meta } …' to contain 'scrollIntoStage(grid, LIST_MIN_ROOM, wrap)'
× 接线与样式 > 相册板一旦变成滚动口，手指竖划得归它，翻到底也不许把外面那层带着走
  AssertionError: expected 'padding:8px;' to contain 'overscroll-behavior:contain'
```

**后绿**（同一支探针、同一把尺子重打）：

```
相册兑换钮 568×320 矮横屏：可滚祖先=[{"cls":"ktc-album","max":1697}]
  .ktc-album  max-height:170px / overflow-y:auto / overscroll-behavior:contain / touch-action:pan-y
  真手指慢拖轨迹 95 → 190 → … → 1615 → 1697（推得动），逐档累计 **24/24**
  .game-stage 下裁死的有字叶子：**93 → 0**
不许连坐的三档（回归）：
  640×360  仍旧只钳卡片格（.ktc-grid max-height 129px，.ktc-album max-height:none），累计 24/24
  390×844  仍旧只钳卡片格（521px），累计 24/24
  568×320 第 117 关打扮件 4/4、名字行 4/4（这一修没碰 fitIntoStage 那条路）
```

**改动**：`kitty-care/runtime.ts`（新增 `listOrShellRoom`，`scrollIntoStage` 加第三个可选参数 `shell`，
不传＝老行为一个字节不差）、`kitty-care/index.ts`（相册那一处把 `wrap` 传进去）、
`kitty-care/styles.ts`（`.ktc-album` 补 `overscroll-behavior:contain; touch-action:pan-y`）、
`albumShellScroll.test.ts`（**新增 16 条**）。**热区一分没缩**（`.ktc-mini` 仍 `min-height:44px`），
**动物福利红线一格没踩**（这一修连判负路径的边都没沾，另有一条用例钉着「生产代码零 `ctx.lose` / 零 `onLose`」）。

### C3 · learner 不落地的 7 条，逐条表态

| # | learner 的处置 | 我的表态 |
| --- | --- | --- |
| `W5R3-C-07`（选关地图 320×568 整张点不着 + 章节页签只剩 2–3 颗） | 不落地，交窗口1 | **同意，维持只量不改**。病灶在 `level99.ts`，本档禁改，我一行没动。**仍在，交窗口 1**——不许写成已解决 |
| `quiz99` 连错闪答案 | 不落地，交窗口1 | **同意，维持只量不改**。病灶在 `src/games/quiz99.ts`，跨窗口共用禁改文件，我一行没动。**仍在，交窗口 1**。本档五款自己的提示另有守门用例，不给答案 |
| `.game-stage{overflow:hidden}` 定高硬裁 | 一个字没动 | **同意**。`src/styles.css` 我一个字节没改。这是本节四处横屏缺陷的共同上游，**仍在，交窗口 1**；本档一律在自己的壳里兜 |
| `find-diff` 26×26 图格 | 维持原判，不动 | **同意**。我自己复量：竖屏四档 + 横屏两档，工具条与可点图格在真手指之后都够得着；撑到 44px 就得让两张图不能同屏对比，那是玩法改动，**收官轮不动玩法** |
| `find-diff` 二级提示直给答案 | 不落地（第 1 轮判有意设计 `W5-L-34`） | **同意，只登记**。源码巡检确认分级还在（奇数次圈 3×3 区域、偶数次才精确指），是**有意的提示经济**，不是漏判。改它属于教育口径重新定调，不在收官轮 |
| `W5R2-C-11` 难度倒挂 | 不落地（tester C14 判关闭） | **同意关闭**。我复跑 `src/games/w5r3Curve.test.ts` 那 20 条量化守门**全绿**，判据不支持「倒挂」 |
| `pinyin-train` 本轮没动一行 | 不落地 | **同意**。我横竖六档复量：`pinyin-train` 五款里唯一**没有任何横屏严重项**，没有账就不改 |

### C4 · 终审（本档五款，不是全库 76）

| 项 | 怎么审的 | 结论 |
| --- | --- | --- |
| **商标 / 官方角色名** | 85 个词 × 五款目录全部 116 个文件（含 `.md`）。**先把 `//` 与 `/* */` 注释整段抹成等长空白再扫一遍**，用来证明「注释里也没有」 | **真命中 0**。全文 65 处里，58 处是 `upgrade12.test.ts` **守门用例自己的黑名单表**、7 处是本档报告在讨论黑名单本身；**生产代码 0 处、只出现在注释里的 0 处** |
| **`destroy`** | 五款各**进出 10 次**（`location.hash` 真切换，不是重新加载）。页面脚本跑起来之前先把 `add/removeEventListener`、`set/clearTimeout`、`set/clearInterval`、`request/cancelAnimationFrame`、`AudioContext` 全包一层记账 | **五款全部 0 涨**。第 1 圈与第 10 圈都是 window 监听 1 / document 监听 2 / 未清 timeout 0 / 未清 interval 0 / 未收 rAF 0；`AudioContext` 恒 1（平台 `api.play` 的共享实例，非各款自建） |
| **动物福利** | ① 静态复扫 `src/games/kitty-care/**` 生产代码（去掉 `*.test.ts`）；② 真机三场景 × 120 拍乱按（第 117 关 / 第 157 关 / 照顾马拉松，专挑错的按把心情耗光、逼猫钻纸箱） | **`ctx.lose` / `onLose` 命中 0**；360 拍下来**失败屏 0 次、伤害词 0 处**（扫的是「失败 / 输了 / game over / 受伤 / 流血 / 生病加重 / 死 / 淘汰 / 惩罚」）。心情掉光是躲纸箱等安抚，不判负、不重来 |
| **教育 · `pinyin-train` 声调** | 复跑守门：`toneIndependent.test.ts`（判据从 Unicode 组合附加符号现场拼、不 import 被测代码算答案的任何工具）+ 上一棒的 `toneFullRecheck.test.ts` | **全绿**。探针自打的账：`tone` 题 126 道 **0 错**、`tonemark` 48 道（问字母 23 / 问写法 25）**0 错**、带调号的选项落位全题型通扫合规 |
| **教育 · `find-diff` 二级提示** | 源码巡检提示分级 + 复跑该款全部用例 | **只登记，一行没改**。分级仍在（奇数次圈区域、偶数次精确指），维持 `W5-L-34` 有意设计的原判 |
| **脏存档不白屏** | 六个本档相关 key（`l99.<id>` / `l99skip.<id>` / `save.v1` / `collection.v1` / `kitty-care.album.v1` / `pinyin-train.wrong.v1`）**同时**灌 7 种垃圾（空串 / 半截 JSON / 不是数组 / 越界数字 / 400 长数组 / 夹了对象 / 纯垃圾），五款 × 7 = **35 格** | **白屏 0 格、`error` / `unhandledrejection` 0 条**。每格 `.game-stage` 都在、节点 67–167、文字 328–489 字，标题与「📖 攻略」「⏸」都读得出来 |
| **禁改公共文件** | `git diff --name-only f826748..HEAD` 过滤禁改清单 | **命中 0**。`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css` / `README.md` **一个字节没动** |
| **用例只增不减** | `git diff f826748..HEAD -- 'src/**'` 里删除的 `it(` 行数 | **0**。既有断言一条没改判、一条没删 |
| **合并态自跑** | 见 C6 | `npm test` **711 文件 / 15173 用例全绿**；`npm run build` **通过** |

### C5 · 遗留：交窗口 1 vs 本档

| # | 事 | 归属 | 最终结论 |
| --- | --- | --- | --- |
| `W5-F-19` / `.game-stage{overflow:hidden}` 定高硬裁 | `src/styles.css` | **窗口 1** | **仍在，一行没动**。本节四处横屏缺陷的共同上游。本档一律在自己的壳里「量舞台下沿 → 自己钳 → 自己滚 → 把该动手的送进眼里」，五款各存一份 |
| `W5R3-C-07` 选关地图 320×568 整张点不着 | `level99.ts` | **窗口 1** | **仍在，一行没动，不写成已解决**。逃生口（`▶ 继续` / `🎯 跳到当前关` / `⏭️ 跳过`）在，所以不阻塞本档 |
| `quiz99` 连错闪答案 | `src/games/quiz99.ts` | **窗口 1** | **仍在，一行没动，不写成已解决**。与「学习提示不直接给答案」的红线冲突，但不影响题目正确性 |
| `W5-L-37` `Loop` 句柄推广到 `src/engine/**` | `src/engine/**` | **窗口 1** | **仍在**。本档五款自己那一份已到位（C4 `destroy` 五款全 0 涨） |
| `find-diff` 26×26 图格 | 本档 | 本档 | **不修，结案**。真手指之后够得着，撑到 44px 是玩法改动 |
| `find-diff` 二级提示直给答案 | 本档 | 本档 | **不修，只登记**。有意设计（`W5-L-34`） |
| `W5R3-C-04` 里 rAF 那一小段 | 本档 | 本档 | **空转，不 revert**。五档视口上量不出差异，但它是防竞态的保险，拆掉只会让下一棒重踩 |
| `W5R2-C-11` 难度倒挂 | 本档 | 本档 | **关闭**。20 条量化守门全绿，判据不支持 |

**本轮档C 计数（我签的字）：阻断 0 / 严重 0 / 一般 0 —— 全部清零。**
learner 落地的 5 条**独立复算全部成立**（4 条真修 + 1 条真修但含一小段空转），
我自己揪出并补修的 1 条严重（`W5R3-CF-01`）已先红后绿、真机复验。

### C6 · 改动登记与水位

| 我的 commit | 改了什么 |
| --- | --- |
| `ec2e335` | `docs/qa/1.2-window5-round3-fixer.md`（**只新增 `## 档C · 五款` 的 C0**，F0–F7 一个字节没动） |
| `d889374` | `kitty-care/runtime.ts` `index.ts` `styles.ts` + **新增** `albumShellScroll.test.ts`（16 条） |
| 本节 | 同一个文件的 C1–C6 |

| 项 | 结果 |
| --- | --- |
| `npm test` | ✅ **711 个测试文件 / 15173 条用例全绿**（179.8s） |
| `npm run build` | ✅ 通过（`tsc --noEmit` 无报错，PWA precache **188 entries / 5111.90 KiB**） |
| 本档五款 | **57 个测试文件 / 760 条用例全绿**（开工时 56 / 744，`+1 文件 / +16 条`，全是我这一条补修带的） |
| 删除的 `it(` | **0** |
| 禁改公共文件 | **命中 0** |
| 新增依赖 | **0**（`package.json` / `package-lock.json` 没动） |
| 存档 key | **一个没动**（`yiduo-yixing.l99.<id>` / `l99skip.<id>` / `save.v1` / `collection.v1` / `kitty-care.album.v1` / `pinyin-train.wrong.v1`） |
| 探针 | 落 `/tmp/w5r3cf/`，另开两个 `git worktree`（`64adef7` / `562790f`）跑反例，**全部跑完即删、0 字节进提交** |
| 分支 | `game-1.2-window5`（`fetch` → `rebase`：**别人的节 theirs、档C 节 ours**；**没推 `main` / 没推 `game-1.2` / 一次 `--force` 都没用 / 没用 `gh` 开改 PR**） |
| 派了几个云端子代理 | **0**。不开第 4 轮，不写 tester / learner |

### C7 · 本档五款能否随窗口 5 一起提交进 `game-1.2`

**能。**

1. **learner 宣称落地的 5 条，我拿自己的尺子从头量了一遍，条条站得住**——四条真修、
   一条真修但夹了一小段空转（`find-diff` 的 rAF 重量，无害、不 revert）。
   每一条都带**补丁前 commit 上的反例**：旧侧红得清清楚楚（0/6、0/2、0/24、0/4、进度退回第 1 步），
   新侧同一支探针同一把尺子全绿。**没有一条是照抄别人报告里的数字。**
2. **本档能修的阻断 / 严重清零**：我自己揪出 1 条严重（`W5R3-CF-01`，相册在 568×320 矮横屏上
   24 颗兑换钮仍旧 0/24、裁死 93 个有字叶子），已在本档目录内先红后绿补掉，
   真机复量 **0/24 → 逐档累计 24/24、裁死 93 → 0**，另外三档一个像素没变。
3. **终审九项全过**：商标真命中 0（注释也扫了）、`destroy` 五款各 10 圈 0 涨、
   动物福利 360 拍 0 失败屏 0 伤害词、`pinyin-train` 声调守门全绿、
   `find-diff` 二级提示只登记、脏存档 35 格 0 白屏、禁改文件命中 0、
   删除的 `it(` 0、合并态 `npm test` + `npm run build` 全绿。
4. **遗留分得清清楚楚**：交窗口 1 的四条（`overflow:hidden` 硬裁、选关地图、`quiz99` 闪答案、
   `Loop` 句柄推广）**我一行没动，也没写成已解决**；本档自己的四条各有明确结论。

**这一批五款可以随窗口 5 一起提交进 `game-1.2`。**
（本档只签这五款，**不签整个 1.2**；`game-1.2` 的合并由主管定，我没把本窗分支合进去。）

---

## 档B · 五款

（`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars`）

> 本节由**档B 监督修复员**独立填写，**新增**在 F0–F7 与 `## 档C · 五款` 之后、`## 档A · 五款` 之前。
> 上面 F0–F7（并行执行者的收官正文）与 C0–C7（档C 监督修复员）**一个字节都没动、没删、没重排**；
> 下面 `## 档A · 五款` 也一个字节没动（并行档A 监督修复员还在往下写）。
> 我把本节插在档A 之前，是为了让并行的档A 继续往文件末尾追加时不跟我抢同一段上下文——**没有拆开任何人的节**。
> tester / learner 报告与 tester 文末的 `R3-0…R3-6` 一律**只读**。

### B0 · 清零与终审计划（动 `src/` 之前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 3 轮（收官）** · **档B 监督修复员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，**不另开分支**；不推 `main` / 不推 `game-1.2` / 禁止 `--force` / 不用 `gh` 开改 PR）
**基线**：`4ecd0c1`（`git fetch origin game-1.2-window5` 后一格不差，即档B 学习优化员 B1–B6 收官那一条）
**代码态**：`217ab6c`（`fishing-star`）+ `2902dc5`（`red-blue-tap` 提示行）+ `6505e98`（`shape-kingdom` 动词）
+ `c59b60b`（`music-stars` 键排手势）+ `53ebb13`（`red-blue-tap` 横屏侧模式键排）
**没派任何云端子代理**——提示词里那句「请通过 Task 派生」是写给派发方看的，我就是执行者，活自己干完。**不开第 4 轮。**

#### 我要签的字（签不了就写清为什么）

1. learner 宣称落地的 **6 条**（`W5R3-BT-01…04`、`LB-13` 后半、`W5R3-BL-01`），我**自己拿探针从头量一遍**，
   量化数字自己复算，逐条判 **真修 / 假绿 / 空转**——照抄别人报告里的数字等于没验。
2. **`BT-02` 那个「`show` 相位 100ms 内瞬时 2.6px 残留」我自己量。** learner 没把它写成已解决，
   我既不当成假绿（它确实修掉了静止那 29.6px），也不当成漏修（残留是真的）——**按我量到的数写**。
3. **`BL-01` 的 `568×320` 那条余量我自己量。** learner 交了平台、没写成已解决；
   我要自己确认「顶栏 138px + 面板收到头 274px > 舞台 170px」这笔账**是不是真的物理装不下**，
   还是本档自己还能再让一点。
4. **`BT-01` 那个「进关后横屏 `scrollTop` 是 128/98 不是 0」不许误判成回潮。**
   这是本轮**有意送键**，不是 `W5-B-09` / `LB-03` / `FBS-03` 的陈旧位移。
   我扫「进关带位移」时按「进关那一刻是不是先归 0 了、这个值是不是本帧重新算出来的」来区分。
5. **本档能修的阻断 / 严重清零**：复算里发现假绿或新的阻断 / 严重，就在本档五款目录内补修，
   **先红后绿**，失败原文留在本节。
6. **终审（本档五款，不是全库 76）**：商标 0 真命中（含注释，注释抹白再扫一遍）、
   `destroy` 五款各进出 10 次句柄不涨、教育两条（`math-farm` / `shape-kingdom` 独立判据仍绿，
   **判据不 import 被测代码算答案**）、脏存档不白屏、合并态自跑 `npm test` + `npm run build`。

#### 我的尺子（和别人报的数字对不上时，以我这把为准）

| 项 | 口径 |
| --- | --- |
| 「点得着」 | 只认 `document.elementFromPoint(元素中心点)` 拿回它自己或它的**后代**。拿回祖先 / 兄弟一律不算。**全程禁止 `el.click()`** |
| 「滚得着」 | 真手指 `Input.dispatchTouchEvent`：`touchStart → 分段 touchMove ×N → 停 220ms → touchEnd`，**不 poke `scrollTop`**；每趟后重新 `elementFromPoint` |
| 裁切线 | **padding box**：`rect.top + clientTop + clientHeight`，`.game-stage` 那 4px 白边不算进可视段 |
| 「被裁的字」 | 扫 `.game-stage` 下**所有有文字的叶子节点**，可见高 ≤ 0 且**没有可滚祖先**的记为「裁死」 |
| 视口 | **横屏必测 640×360 / 740×360 / 844×390**，外加矮横屏 **568×320**（把 320×568 那台机器转 90°）；竖屏对照 320×568 / 360×640 / 390×844 |
| 反例 | 同一支探针在**补丁前的 commit** 上重跑：`fishing-star` 跑 `217ab6c^` = **`6c23b35`**；其余四条（`BT-03` / `BT-04` / `LB-13` / `BL-01`）**共用** `2902dc5^` = **`1d9099c`**（它排在这四条补丁之前、之后的四条一条都还没落）。没有反例的「绿」一律记为**空转** |
| 真机 | `npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖** |
| 余量 | learner 写成「残留 / 已知余量」的两处（`BT-02` 瞬时 2.6px、`BL-01` 的 `568×320`）**我自己重量**：既不当成已修，也不误判成假绿 |

#### 权限边界

- **只动本档五款自己的目录**：`src/games/{fishing-star,red-blue-tap,math-farm,shape-kingdom,music-stars}/**`。
- **一个字不改**：`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` /
  `src/ui/**` / `src/engine/**` / `src/styles.css`。
- 报告**只写本文件的 `## 档B · 五款` 这一节**；F0–F7、`## 档C · 五款`、`## 档A · 五款` 与
  tester 文末 `R3-0…R3-6` 一个字节不动，**不替档A 写节**，`docs/qa/1.2-window5-round2-fixer.md` 也不动。
- 不验 76 款全集、不改 README / 窗口 1 平台、**不把本窗分支合进 `game-1.2`**。
- **收官轮不许大重构**：补修一律小而准，改一处就把这一处的回归测试补上；**不 revert 已落地补丁**。
- learner 不落地的平台项（`.game-stage{overflow:hidden}`、选关地图末节点、`quiz99` 闪答案）**只量不改，绝不写成已解决**。
- `W5-B-08`（上鱼后再抛竿）测试员与 learner 都判不复现——**我自己不复现就不去动抛竿主路径**。
- 编号：本节自记的条目走 `W5R3-BF-*`，**避开** 修复员收官节的 `W5R3-B-01…03`、测试员的 `W5R3-BT-01…04`
  与 learner 的 `W5R3-BL-01`。
- 探针落 `/tmp/w5r3bf/`，**跑完即删、0 字节进提交**；不引入依赖、不动存档 key。

#### 要独立复算的 6 条（learner 宣称全落）

| # | 级 | 款 | learner 的说法 | 我怎么复算 |
| --- | --- | --- | --- | --- |
| `W5R3-BT-01` | **严重** | `fishing-star` | 钳完 `showAct()` 把唯一那颗操作键送进可视段，**18/18 落地即可按**；横屏落地 `scrollTop` 128 / 98 | 六档 × L12/63/117 逐颗 `elementFromPoint`；**外加 L1/100/155/181/188** 把测试员点名的八关补齐；反例跑 `6c23b35`。同时按 B0 第 4 条区分「有意送键」与「陈旧位移」 |
| `W5R3-BT-02` | 一般 | `fishing-star` | 320×568 `show` 相位静止 `cut −0.41px`（改前 −29.6），命中 `.fs-act`；**残 2.6px 瞬时** | 连打十几竿真上鱼，逐帧记 `show` 相位整段的 `cut` 与命中；**瞬时那 2.6px 我自己量最大值与持续帧数**；反例跑 `6c23b35` |
| `W5R3-BT-03` | 一般 | `red-blue-tap` | 横屏矮档「本关新玩法」被切 10/25 → **0/25**，竞技场反而长回 100/130 | 五档 × L100/117/155/181/188 量 `.rbt-msg` 的**可见高**（不是「存在」）；顺带量竞技场与点阵热区不许连坐；反例跑 `1d9099c` |
| `W5R3-BT-04` | 建议 | `shape-kingdom` | `.shk-ask` 「拖两个点」→「点两个点（或者按住拖）」，`askCut` 0/5 | 真机读屏抓 `.shk-ask` 原文 + 遍历 188 关题面扫「拖两个点」；反例跑 `1d9099c` |
| `LB-13` 后半 | 建议 | `music-stars` | 矮屏那一档补 `.mst-keys-scroll{touch-action:pan-x pan-y}`，L155 三档 `0 → 51/51/123` | **真手指从键身正中起手竖划**，读壳 `scrollTop`；横向不许丢（`scrollLeft` 也要动）；读祖先链 `touch-action` 交集；反例跑 `1d9099c` |
| `W5R3-BL-01` | **严重** | `red-blue-tap` | 横屏三档 × 两个侧模式，8 颗键 4 坏 → **8/8 够得着**；**`568×320` 仍 8/8 够不着，交平台** | 六格逐颗 `elementFromPoint`；**`568×320` 那条余量我自己算**：顶栏到底吃掉多少、面板收到头到底要多少、差多少；反例跑 `1d9099c` |

#### 终审要过的项（本档五款）

| 项 | 怎么审 |
| --- | --- |
| 商标 / 官方角色名 | 黑名单 × 五款目录全部文件（含 `.md`）；**先把 `//` 与 `/* */` 注释整段抹成等长空白再扫一遍**，用来证明「注释里也没有」 |
| `destroy` | 五款各**进出 10 次**（`location.hash` 真切换，不重新加载）；页面脚本跑起来之前先把 `add/removeEventListener`、`set/clearTimeout`、`set/clearInterval`、`request/cancelAnimationFrame`、`AudioContext` 全包一层记账 |
| 教育 · `math-farm` | 独立判据现场算答案（**不 import 被测代码算答案的任何工具**），换一批题复算 |
| 教育 · `shape-kingdom` | 独立判据复算周长 / 面积 / 长宽术语；复跑 `rectTerms.test.ts` 那一族守门 |
| 脏存档不白屏 | 本档相关 key 同时灌多种垃圾，五款逐个进关 |
| 禁改公共文件 | `git diff --name-only 4ecd0c1..HEAD` 过滤禁改清单 |
| 用例只增不减 | `git diff 4ecd0c1..HEAD -- 'src/**'` 里删除的 `it(` 行数必须为 0 |
| 合并态 | 自跑 `npm test` + `npm run build` |

**不开下一轮**：本节写完即收官，不写 tester / learner，不派任何人。

### B1 · 独立复算表（learner 宣称落地的 6 条，逐条判真修 / 假绿 / 空转）

六条我一条都没照抄，全部自己拿探针从头量。热区只认 `elementFromPoint(元素中心)`，
全程没有一次 `el.click()`；滚得着一律真手指 `Input.dispatchTouchEvent`；裁切线一律 padding box。
每条都带反例：`fishing-star` 那两条跑补丁前的 `6c23b35`，其余四条跑 `1d9099c`。

| # | 级 | 款 | 我量到的（反例 → 现在） | 判定 |
| --- | --- | --- | --- | --- |
| `W5R3-BT-01` | **严重** | `fishing-star` | 7 视口 × 8 关 = **56 格**。`6c23b35`：横屏四档 **0/32** 点得着，抛竿键整颗在裁切线下 —— 640×360 / 740×360 `cut −128.3`、844×390 `cut −98.3`、568×320 `cut −222.3`，`elementFromPoint` 一律回 `game-stage`；竖屏三档 24/24 本来就好。现在：**56/56 点得着**，最坏 `cut −0.3`（就是 `.game-stage` 那 4px 白边的亚像素），命中一律 `BUTTON.fs-act.fss-act`，裁死的字 0 | **真修** |
| `W5R3-BT-02` | 一般 | `fishing-star` | 320×568 连打 4 关 × 每关 4 竿，`show` 相位逐帧（rAF）取样 **165–196 帧/关**。`6c23b35`：**每一帧** `cut −29.59`，命中 `game-stage`。现在：整段最坏 **+0.41 / −0.3px**，`show` 相位一帧都没超过 0.3px | **真修**（见 B4 第 1 条：learner 记的「2.6px 瞬时残留」我这支探针量不到） |
| `W5R3-BT-03` | 一般 | `red-blue-tap` | 5 视口 × 5 关 = 25 格量 `.rbt-msg` 的**可见高**。`1d9099c`：**10/25 被切**（640×360 可见高 0–15px、740×360 15px），裁死的字 1。现在：**0/25**，可见高一律 18px；竞技场没被连坐，反而从 80 → **100**（640/740×360）、103 → **130**（844×390），点阵热区 0 颗点不着 | **真修** |
| `W5R3-BT-04` | 建议 | `shape-kingdom` | 7 视口真机读屏 L117 作图关：「拖两个点」**0 命中**，现文案「点两个点（或者按住拖），画一个面积是 6 平方厘米的长方形」7/7 在屏，动作区那句也同步改成「点两个点（或者按住拖），拉出一个长方形」 | **真修** |
| `LB-13` 后半 | 建议 | `music-stars` | L155 键身正中起手，真手指竖划。`1d9099c`：`.mst-keys` 的 `touch-action` 只有 `pan-x`，竖划两趟 `mst-wrap` 一动不动 **0/51**。现在：`pan-x pan-y`，同一趟竖划 `mst-wrap` **51/51**（滚到头）；横划没被连坐，`mst-keys` 仍旧 64/64（320×640）、32–33/33（360×640） | **真修** |
| `W5R3-BL-01` | **严重** | `red-blue-tap` | 6 格（3 横屏 × 2 侧模式）+ 矮横屏 2 格。`1d9099c`：三档横屏键排摊成 **2 行**，对战 **4/8**、点到手软 2/4（844×390 是 4/4），裁死的字 9–17；`568×320` **0/8 与 0/4**。learner 补丁后：三档横屏收成 1 行、**8/8 与 4/4**，`568×320` **仍旧 0/8 与 0/4** —— 这条 learner 自己写明了没解决，所以三档横屏那半段是**真修**，`568×320` 那半段是**真残留**，不是假绿 | **真修 + 残留**（残留我在 B2 补掉） |

**一条都不是空转**：六条的反例侧全部复现了原症状，数字与方向都对得上。

### B2 · 补修：`W5R3-BF-01`（`BL-01` 的 `568×320` 那笔余量）

#### 我先自己算这笔账，不接受「物理装不下」

learner 把 `568×320` 交了平台。我把三个数自己量了一遍：

| 量 | 真机 CDP 读数（`568×320`，对战侧模式） |
| --- | --- |
| 壳层顶栏吃掉 | `.game-stage` 的 padding box 从 **138** 起 —— 顶栏整整 138px |
| 舞台看得见的一段 | 138…308，只有 **170px** |
| 面板摊成一排之后 | **274.3px** |
| 键排实际位置 | 303.6…359.5，整排落在裁切线 308 **以下 51.5px** |
| 真手指救援 | 3 趟竖划，`own` 仍旧 0/8（`.game-stage` 是 `overflow:hidden`，压根没有滚动口） |

差的是 **52px**。这 52px 我一项项翻，全都躺在**这一款自己的**留白和字号里，不用碰平台：
面板上下内衬、三条行缝、比分头像、回合说明的字号行高，
外加**键位名那一行**—— A S D F 本来就印在每一颗键上（`.rbt-key-cap`），
谁是哪一边看比分行的头像，这一行在超矮档纯属冗余。

#### 先红

新用例 `src/games/red-blue-tap/versusUltraShort.test.ts`（6 个 `it`），先跑一遍全红：

```
AssertionError: expected undefined to be a number   （ULTRA_STAGE_ROOM_PX / ULTRA_AVA_PX 还不存在）
AssertionError: 键位名那一行收起来，A S D F 印在键上: expected '' to contain '.rbt-vs-name { display: none; }'
```

#### 后绿

`src/games/red-blue-tap/arena.ts` 加一档 `@media (max-height: 340px)`（`ULTRA_SHORT_PX`），
外加一组和 CSS 读同一份的常量与预算函数 `ultraAboveKeysPx()` / `ultraKeyBottomPx()`，
用例与样式因此不会走散。**热区一分不缩**：`.rbt-key` 仍旧 `KEY_TIGHT_PX`、
头上那两颗按钮仍旧 `TOUCH_MIN_PX`（44px），隔离带一个像素没动。

真机复量（同一支 `p-rbt.mjs`，同一台 headless）：

| 视口 / 模式 | 补丁前 | 补丁后 |
| --- | --- | --- |
| `568×320` ⚔️ 双人对战 | own **0/8**，面板 274.3px，裁死的字 17 | own **8/8**，面板 **210.6px**，裁死的字 **1** |
| `568×320` ♾️ 点到手软 | own **0/4**，面板 274.3px，裁死的字 9 | own **4/4**，面板 **184.6px**，裁死的字 **0** |
| 640×360 / 740×360 / 844×390 两个模式 | 8/8 与 4/4 | **8/8 与 4/4 一格没动**，面板仍旧 274.3 / 240.3px，键盒仍旧 55.4×55.4 | 

键盒 `55.4×55.4` ≥ 44px 触控底线，一颗都没缩到线下。三档横屏的读数补丁前后逐字一致，**没有连坐**。

#### 这条没修干净的部分（不写成已解决）

`568×320` 对战侧模式，面板底沿 348.6 vs 舞台裁切线 308 —— **还有 40.6px 在线下**，
脚注那一句「两边的题目是镜像的：同样的颜色、同样的号……」仍旧被裁死，
因为 `.game-stage{overflow:hidden}` 不给滚动口（平台文件，本档只读）。
**键与热区已经全部够得着**，被裁的只剩这一句说明文字。这条写进 B6 遗留，**不当已解决**。

### B3 · learner 不落地的平台项与 `W5-B-08`：只量不改

| 项 | 我的处置 |
| --- | --- |
| `.game-stage{overflow:hidden}`（平台） | **只量不改**。`568×320` 上它就是「键排掉到线下捞不回来」的直接原因（真手指 3 趟 `own` 仍 0/8）；我从本档一侧把面板压回去 52px 绕开了它，**平台本身一个字节没动**，也不写成已解决 |
| 选关地图末节点 | 本档没碰，`level99.ts` 只读。**不写成已解决** |
| `quiz99` 闪答案 | 本档没碰，`quiz99.ts` 只读。**不写成已解决** |
| `W5-B-08`（上鱼后再抛竿） | 测试员与 learner 都判不复现，**我自己也没复现**（BT-02 那 16 竿全程没撞上）。按 B0 的规矩，**不去动抛竿主路径** |

### B4 · 两处我自己重量的数（既不当已修，也不误判成假绿）

1. **`BT-02` 那个「`show` 相位 100ms 内瞬时 2.6px 残留」我量不到。**
   4 关 × 4 竿、`show` 相位逐帧取样 165–196 帧，整段最坏只有 **0.3px**（`+0.41 … −0.3`）。
   我不拿这个去反证 learner 记错——取样窗口和机器都不一样；
   **按我量到的写：我这台上没有 2.6px，最坏 0.3px。** 这 0.3px 是 `.game-stage` 4px 白边的亚像素，不是裁切。
   （新侧确实还有几十帧 `own=false`，但命中的是 `l99-ov-btn l99-ov-ghost` —— 过关浮层盖上来那一段，是平台浮层，不是壳裁。）

2. **`BT-01` 的「进关后横屏 `scrollTop` 是 128 / 98」我按 B0 第 4 条做了甄别，判定是有意送键，不是回潮。**
   同一关做 A/B：进关读到 128 → 真手指把它划回 0 → 退出选关 → 再进同一关，
   读到的是 **128 又重新算了出来**，抛竿键 `own=true`、`cut −0.3`；844×390 同理 98 → 0 → 98。
   **本帧重算、而且送的正是那颗唯一的操作键**，与 `W5-B-09` / `LB-03` / `FBS-03` 那种「陈旧位移」不是一回事。
   顺带记一笔：`shape-kingdom` 进关也带位移（`shk-fit-scroll` 98 / 102），同样是它自己的 fit-scroll，也不是回潮。

3. **一条我追出来又自己排除掉的假警报**（写在这里免得以后被当成漏报）：
   探针一开始报 `shape-kingdom` L117 的 `.shk-ask` 在 `568×320` 上「可见高 0/22.5，真手指 3 趟捞不回来」。
   查祖先链才发现是**我起手点错了**——那一档 `.shk-fit-scroll` 的滚动口只有 96px 高（212…308），
   我三次起手都落在滚动口外面。把起手点移进滚动口，**一趟就回来了**（22.5/22.5，`scrollTop` 98 → 69 → 40 → 11），
   而且补丁前（`1d9099c`）与补丁后读数**逐字一致**。**不是问题，也不是本轮引入的。**

### B5 · 终审（本档五款，不是全库 76）

| 项 | 判据 | 结果 |
| --- | --- | --- |
| 商标 / 官方角色名 | 85 词黑名单 × 五款 **124 个文件**（含 `.md`）；`//` 与 `/* */` 整段抹成等长空白后重扫 | 原始命中 60，其中 **58 条落在两支 `shell.test.ts` 里（那就是黑名单自己）**；抹白注释后生产文案命中 2，逐条看都是 `一跳一跳`（冰纹湖「张力像心跳一样一跳一跳」与攻略同一句）里**套出来的子串**，不是那款游戏。**真命中 0**；注释里也 0 |
| `destroy` 不漏句柄 | 五款各 `location.hash` 真进出 **10 次**（不重新加载）；页面脚本起来之前先把 `add/removeEventListener`、`set/clearTimeout`、`set/clearInterval`、`request/cancelAnimationFrame`、`AudioContext` 全包一层记账 | 五款第 1 / 5 / 10 圈读数**逐格相同**（window 1、document 2、timeout 0、interval 0、rAF 0、AudioContext 0），`grew=false`，控制台报错 0 |
| 教育 · `math-farm` | 判据**自己写一套四则求值 + 分数 / 小数 / 百分比 / 比 / 方程 / 数列的答案**，只吃 `spec` 字段，**不 import `answerOf` / `evalExpr`**；顺带查「选项里对的只有一颗」「`correct` 下标指得准」 | 独立复算 **2118 道，分歧 0**。判据自检：往 `answerOf` 的 `mul` 分支注一个 `+1`，判据立刻报 **135 处分歧** —— 有牙 |
| 教育 · `shape-kingdom` | 判据**只从题面 SVG 的 `data-*` 反推题目条件**，答案自己算：周长 / 面积从格子一条条数边、对称轴从多边形顶点自己找轴、展开图自己滚一遍立方体、坐标方位自己走、分类自己筛。**不 import `rectPerimeter` / `exactAxisCount` / `cubeNets` 里任何一个** | 188 关正题 **1224 道 / 22 种题型一种不漏**，错题回顾加练 **2088 道**，合计 **3312 道，分歧 0**；顺带查「三个选项不许有两个一样」「`answer` 与正解对得上」「正方体题里不许混进也说得通的『6 个长方形』」。判据自检：把 `lShapePerimeter` 改成 `+2`、把五边形对称轴改成 4，两处都被逮住 |
| `rectTerms` 那一族守门 | 复跑 `rectTerms.test.ts` / `geometry.test.ts` / `nets.test.ts` / `levels.test.ts` / `upgrade12.test.ts` | 全绿（含在下面 15330 里） |
| 脏存档不白屏 | 先让每款自己写一遍存档、收齐它写下的 key，再拿 **8 种脏法**（空串 / 截断 JSON / `null` / 类型全错 / 数组里塞垃圾 / 深嵌套超长 / 控制字符与乱码 / 超大数与负数）同时灌满，选关页与关内各查一次：有字、有可见盒子、没有未捕获异常 | 五款 × 8 种 × 2 处 = **80 次全部存活，白屏 0**。判据自检：拿一张真空白页跑同一套判据，立刻判死 |
| 禁改公共文件 | `git diff --name-only edd96a5..HEAD` | 只有 `src/games/red-blue-tap/arena.ts`、`src/games/red-blue-tap/versusUltraShort.test.ts` 与本报告。`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css` **一个字节没动** |
| 用例只增不减 | 同区间 `git diff -- 'src/**'` | 删除的 `it(` **0** 行，新增 **6** 行 |
| 合并态 | 自跑 | `npm test` **723 个文件 / 15330 个用例全绿**；`npm run build` 通过（PWA 预缓存 188 项） |

### B6 · 遗留（一条不许空着）

| # | 款 | 遗留 | 归属 | 我为什么不在本档修 |
| --- | --- | --- | --- | --- |
| 1 | `red-blue-tap` | `568×320` 对战侧模式，面板底沿仍在裁切线下 **40.6px**，脚注「两边的题目是镜像的……」被裁死；**键与热区已 8/8 全部够得着** | 交窗口 1（`.game-stage{overflow:hidden}` 不给滚动口） | 键排那 52px 我已经从本款自己的留白里让回来了；剩下这 40.6px 要么再砍说明文字、要么平台给滚动口。收官轮不砍教学文案，也不动平台文件 |
| 2 | 五款共通 | `.game-stage{overflow:hidden}`：矮横屏上凡是掉到裁切线下的东西，真手指一律捞不回来 | 交窗口 1 | 平台文件只读 |
| 3 | `fishing-star` | `show` 相位仍有几十帧 `elementFromPoint` 回 `l99-ov-btn l99-ov-ghost`（过关浮层的幽灵按钮盖住抛竿键） | 交窗口 1（`level99` 浮层） | `level99.ts` 只读；不是壳裁，不影响那一竿的操作时序 |
| 4 | `fishing-star` | learner 记的「`show` 相位 100ms 内瞬时 2.6px」我这台量不到（最坏 0.3px） | 本档 | 量不到就不改；**既不写成已解决，也不判 learner 假绿** |
| 5 | 平台三条 | 选关地图末节点、`quiz99` 闪答案、`overflow:hidden` | 交窗口 1 | learner 就没落地，我**只读不改、不写成已解决** |

### B7 · 改动登记与水位

| commit | 动了什么 | 范围 |
| --- | --- | --- |
| `4337bc9` | 本节 B0 计划（动 `src/` 之前登记） | 只有 `docs/qa/1.2-window5-round3-fixer.md` |
| `56af2f0` | `W5R3-BF-01`：`red-blue-tap` 超矮横屏（`max-height: 340px`）一档，把键排从裁切线下 51.5px 收回屏内 | `src/games/red-blue-tap/arena.ts` + 新用例 `versusUltraShort.test.ts` |
| 本条 | 本节 B1–B8（复算表 / 补修 / 终审 / 遗留 / 结论） | 只有 `docs/qa/1.2-window5-round3-fixer.md`，**只加 `## 档B · 五款` 自己这一节** |

- **本档只碰了 `red-blue-tap` 一个目录**，另外四款一个字节没动（复算全绿，没有该补的）。
- 新增 **6 个用例**，删除 **0 个**。
- 探针全部落在 `/tmp/w5r3bf/`（`cdp.mjs` / `lib.mjs` / `p-bt01` / `p-bt02` / `p-rbt` / `p-shk` / `p-mst2` / `p-entryscroll` / `p-dirty` / `audit-tm` / `audit-destroy` 与两支临时 `__w5r3bf_probe.test.ts`），**跑完即删，0 字节进提交**；没引入任何依赖，没动存档 key。
- 水位：`npm test` 723 / 15330 全绿，`npm run build` 通过。
- 没派任何云端子代理；没推 `main`、没推 `game-1.2`、没有 `--force`、没用 `gh` 开改 PR。**不开第 4 轮。**

### B8 · 本档五款能否随窗口 5 一起提交进 `game-1.2`

**能。**

- learner 宣称落地的 6 条，我独立复算下来 **6 条真修、0 条假绿、0 条空转**，条条带反例。
- 本档唯一那条真残留（`W5R3-BL-01` 的 `568×320`）**我自己补掉了**：8 颗键从 0/8 变 8/8，
  三档横屏一格没被连坐，热区一分没缩。**本档能修的阻断 / 严重已清零。**
- 终审九项全过：商标真命中 0、`destroy` 五款十圈句柄不涨、
  两款教育独立判据（2118 + 3312 道）分歧 0 且判据经过注错自检、脏存档 80 次不白屏、
  禁改清单一个字节没动、用例只增不减、合并态 `npm test` + `npm run build` 全绿。
- 还没修干净的只剩一条**文字被裁**（`568×320` 对战脚注 40.6px），键与热区都够得着，
  根因在平台 `overflow:hidden`，已写进 B6 交窗口 1，**没有写成已解决**。

**结论：`fishing-star` / `red-blue-tap` / `math-farm` / `shape-kingdom` / `music-stars` 五款，可以随窗口 5 一起提交进 `game-1.2`。**

---

## 档A · 五款

（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` / `color-fun`）

> 本节由**档A 监督修复员**独立填写，**新增**在 F0–F7 与 `## 档C · 五款` 之后。
> 上面 F0–F7（并行执行者的收官正文）与 C0–C7（档C 监督修复员）**一个字节都没动、没删、没重排**。
> 档B 节不由我代写（并行档B 学习优化员还在干活）。
> tester / learner 报告与 tester 文末的 `R3-0…R3-6` 一律**只读**。

### A0 · 清零与终审计划（动 `src/` 之前登记）

**角色**：窗口 5 · 本窗三轮验收 · **第 3 轮（收官）** · **档A 监督修复员**
**实际模型 slug**：`claude-opus-5-thinking-high-fast`
**分支**：`game-1.2-window5`（在已有分支上干活，**不另开分支**；不推 `main` / 不推 `game-1.2` / 禁止 `--force` / 不用 `gh` 开改 PR）
**基线**：`5a00ec4`（`git fetch origin game-1.2-window5` 后一格不差，即档A 学习优化员 A1–A6 收官那一条）
**代码态**：`71790eb`（五款主改）+ `6c23b35`（守门用例）+ `aca1965`（`.ld-fanbox` 第二刀）
**没派任何云端子代理**——提示词里那句「请通过 Task 派生」是写给派发方看的，我就是执行者，活自己干完。**不开第 4 轮。**

#### 我要签的字（签不了就写清为什么）

1. learner 宣称落地的 **5 条**（`W5R3-TA-01…05`），我**自己拿探针从头量一遍**，量化数字自己复算，
   逐条判 **真修 / 假绿 / 空转**——照抄别人报告里的数字等于没验。
2. **`TA-01` 必须真机 CDP，不许只信用例绿。** learner 自己写过：钳位数值和 `vis` 都对、
   单元测试全绿，可 `elementFromPoint` 打回来的是 `.ld-card`——**只跑用例会误判已修**。
   这一条我一定在浏览器里逐颗自证。
3. **本档能修的阻断 / 严重清零**：复算里发现假绿或新的阻断 / 严重，就在本档五款目录内补修，
   **先红后绿**，失败原文留在本节。
4. **终审（本档五款，不是全库 76）**：商标 0 真命中（含注释，注释抹白再扫一遍）、
   `destroy` 五款各进出 10 次句柄不涨、教育两条（`clock-house` 时段词 / `word-garden` 独立判据，
   **判据不 import 被测代码算答案**）、脏存档不白屏、合并态自跑 `npm test` + `npm run build`。
5. learner **不落地的 11 条**逐条表态；交窗口 1 的平台项（`W5R2-A-06` Tab 穿顶栏、
   `overflow:hidden`、4px 边框、选关地图、`quiz99`）**维持只量不改，绝不写成已解决**。

#### 我的尺子（和别人报的数字对不上时，以我这把为准）

| 项 | 口径 |
| --- | --- |
| 「点得着」 | 只认 `document.elementFromPoint(元素中心点)` 拿回它自己或它的**后代**。拿回祖先 / 兄弟一律不算。**全程禁止 `el.click()`** |
| 「滚得着」 | 真手指 `touchStart → 分段 touchMove ×N → 停 220ms → touchEnd`，**不 poke `scrollTop`**；每趟后重新 `elementFromPoint` |
| 裁切线 | **padding box**：`rect.top + clientTop + clientHeight`，`.game-stage` 那 4px 白边不算进可视段 |
| 「被裁的字」 | 扫 `.game-stage` 下**所有有文字的叶子节点**，可见高 ≤ 0 且**没有可滚祖先**的记为「裁死」 |
| 视口 | **横屏必测 640×360 / 844×390**，外加我自己加的一档矮横屏 568×320；竖屏对照 320×568 / 360×640 / 390×844 |
| 反例 | 同一支探针在**补丁前的 commit**（`71790eb^` = `1ab90a9`；`.ld-fanbox` 那一刀另跑 `aca1965^` = `217ab6c`）上重跑。没有反例的「绿」一律记为**空转** |
| 真机 | `npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带 `WebSocket` 直连 CDP，**零新增依赖** |
| 余量 | learner 写成「已知余量」的（`color-fun` 横屏 142px 钉不住）**我自己重量**：既不当成已修，也不误判成假绿 |

#### 权限边界

- **只动本档五款自己的目录**：`src/games/{landlord-cards,red-blue-race,clock-house,word-garden,color-fun}/**`。
- **一个字不改**：`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` /
  `src/ui/**` / `src/engine/**` / `src/styles.css`。
- 报告**只写本文件的 `## 档A · 五款` 这一节**；F0–F7、`## 档C · 五款` 与 tester 文末 `R3-*` 一个字节不动，
  **不替档B 写节**。
- 不验 76 款全集、不改 README / 窗口 1 平台、**不把本窗分支合进 `game-1.2`**。
- **收官轮不许大重构**：补修一律小而准，改一处就把这一处的回归测试补上；**不 revert 已落地补丁**。
- 探针落 `/tmp/w5r3af/`，**跑完即删、0 字节进提交**；不引入依赖、不动存档 key。

#### 要独立复算的 5 条（learner 宣称全落）

| # | 级 | 款 | learner 的说法 | 我怎么复算 |
| --- | --- | --- | --- | --- |
| `W5R3-TA-01` | **严重** | `landlord-cards` / `red-blue-race` | 第三档壳层钳 + 挂滚 + `showCtrl`/`showPads` 送进眼里；第二刀 `.ld-fanbox{flex:0 0 auto}` 防手牌盖键；**24/24 落地即可点** | **必须真机 CDP**：横屏三档 × 两款逐颗 `elementFromPoint`；`.ld-fanbox` 那一刀单独跑 `217ab6c` 反例证明「钳位对了但仍 0/5」；竖屏三档回归不许连坐 |
| `W5R3-TA-02` | **严重** | `color-fun` | `clf-tighter` 让竖屏选色→涂色回到同一屏；**横屏 142px 仍钉不住（未写成已解决）** | 竖屏真滚到调色板落地再量画布可见高；**横屏那条余量我自己量**——是真的物理装不下（180 > 142），还是可以再让 |
| `W5R3-TA-03` | 一般 | `word-garden` | 描红台 `.wgd-msg` vis 0 → 26/45，粘住下沿 | 五档量 `.wgd-msg` 的**可见高**（不是「存在」）与 `elementFromPoint`；反例跑 `1ab90a9` |
| `W5R3-TA-04` | 一般 | `landlord-cards` | `index.ts` 三处「不要」→ `PASS_WORD`，守门扩到 `index.ts` | 源码扫 + **真机读屏**：进关把渲染出来的文字全抓下来找「不要」；反例跑 `1ab90a9` |
| `W5R3-TA-05` | 建议 | 四份 `fit.ts` | 裁切线改 `clientHeight` 口径，`overshoot` +4 → 0 | 真机量 border box 口 vs padding box 口的差，和钳出来的下沿；反例跑 `1ab90a9` |

**不开下一轮**：本节写完即收官，不写 tester / learner，不派任何人。

---

### A1 · 独立复算表（learner 宣称落地的 5 条，逐条判真修 / 假绿 / 空转）

**取证怎么做的**：`npm run build` 产物 → `vite preview` → Chrome `--headless=new` + Node 22 自带
`WebSocket` 直连 CDP，**零新增依赖**。「点得着」只认 `document.elementFromPoint(元素几何中心点)`
拿回它自己或它的**后代**（**全程一次 `el.click()` 都没有**）；露出来一小条、几何中心落在裁切线
以下的，一律算够不着。「滚得着」一律真手指 `touchStart → 10~18 段 touchMove → 停 220ms → touchEnd`，
**一次都没 poke `scrollTop`**，而且推到**推不动为止**（连着 3 趟 `scrollTop` 一格没动才收手）。
裁切线按 **padding box** 算。**反例**：同一支探针在补丁前的 commit 上重跑
（`71790eb^` = `1ab90a9`；`.ld-fanbox` 那一刀另跑 `aca1965^` = `217ab6c`；两份各开 `git worktree`
独立 `vite build` + 独立端口 preview）。视口：横屏 **640×360 / 844×390**，外加**我自己加的一档
矮横屏 568×320**（把 320×568 那台机器转 90°），竖屏对照 **320×568 / 360×640 / 390×844**。
探针落 `/tmp/w5r3af/`，**跑完即删、0 字节进提交**。

| # | 级 | 我的判定 | 我自己量出来的数（新侧 `5a00ec4`） | 反例（旧侧同一支探针） |
| --- | --- | --- | --- | --- |
| `W5R3-TA-01` | **严重** | ✅ **真修**（两刀都真，见下方专段） | 第 118 关横屏三档逐颗自证：`landlord-cards` `.ldc-mainbar .ld-btn` **4/4** ＋ `.ldc-subbar .ld-btn` **1/1**，`red-blue-race` `.rbr-pads button` **3/3** —— 三档合计 **24/24 落地即可点**，`elementFromPoint` 拿回来的是键自己（`button.ld-btn` / `button.rbr-step` / `button.rbr-jump-btn`）。壳层可滚 `ldc-scroll` max 195 / 165 / 289（落地 `scrollTop` 189 / 159 / 283）、`rbr-scroll` max 87 / 57 / 181（80 / 50 / 174）。**六档视口「裁死的有字叶子」全部 0**。竖屏三档 8/8，`390×844` 上 `maxHeight` 仍是空、没有凭空多出滚动容器 | `1ab90a9`：横屏三档 `landlord-cards` **0/5、vis 全 0**，`red-blue-race` **0/3**；`.ld-wrap` 恒 422px、`.rbr-wrap` 恒 277px，**一个可滚祖先都没有**，真手指慢拖 8 趟 `scrollTop` 轨迹全平 `[0,0,0,0,0,0,0,0,0]`，**累计仍 0/24**；裁死的有字叶子 **47 / 26 / 64**（`landlord-cards`）与 **4 / 1 / 10**（`red-blue-race`） |
| `W5R3-TA-02` | **严重** | ✅ **真修（竖屏那一格），横屏那条余量属实、learner 没写成已解决** | 第 181 关 320×568：内容 **701 → 631**，收出 `clf-tighter`，后面最高那一排 **105 → 91**，`canPin` **false → true**。真手指推到调色锅整排落地（`scrollTop` 272）时**画布整块 180px 还在眼里**，`transform: translateY(225px)` —— 选完色抬眼就看得见自己在涂哪一块。`.clf-primary` 5/5、`.clf-palette button` 3/3 | `1ab90a9`：同一台机器同一关同一支探针，调色锅整排落地时（`scrollTop` 280）**画布 `visH` = 0** —— 画早滑没影了，这正是「一块颜色两趟」的现场 |
| `W5R3-TA-03` | 一般 | ✅ **真修** | 描红台第 117 关 `.wgd-msg`「顺着虚线慢慢描…」**六档全部 1/1，vis 26 / 26 / 45 / 26 / 26 / 26**，钳住那五档 `position` 实测就是 `sticky`。落地 `scrollTop` 89（`showFocus` 真的动了手）。田字格 `.wgd-pad` 可见高 **241→244**（320×568）、**101→190**（640×360）、**131→220**（844×390）、**7→96**（568×320），真手指累计 1/1。六档裁死的有字叶子 **0** | `1ab90a9`：`.wgd-msg` 在 **320×568 / 360×640 / 640×360 / 844×390 / 568×320 五档全部 0/1、vis 0**，`position` 是 `static`；落地 `scrollTop` 恒 0 |
| `W5R3-TA-04` | 一般 | ✅ **真修** | 不光扫源码，**真机读屏**：390×844 第 118 关连打 60 轮（叫分 → 出牌 → 过牌，牌桌流水 / 对家气泡 / 键位提示全跑出来），每一轮把 `.game-stage` 的 `innerText` 与所有 `aria-label` / `title` / `alt` 抓下来找「不要」——**60 轮命中 0 处** | `1ab90a9`：同一条路同样 60 轮，**命中 55 处**（`:399` 键位提示「G 不要」、`:622` 对家气泡「不要～」、`:663` 牌桌流水「不要～」逐轮复现） |
| `W5R3-TA-05` | 建议 | ✅ **真修**（但**漏了本档第五份收紧器**，见 A2） | `clock-house` / `word-garden` 第 42 关 × 六档，宿主 `.clk-quizhost` / `.wgd-quizhost` 的 `overshoot`（钳出来的下沿 − 真裁切线）**全部为 0**；`.game-stage` 的 border box 下沿与 padding box 下沿实测差恒定 **4px**（554/550、626/622、352/348、382/378、312/308）。`.qz-choice` 六档 3/3 | `1ab90a9`：同样十格，**每一格 `overshoot` 都是 +4**（`maxHeight` 334 / 406 / 194 / 224 / 100 对新侧 330 / 402 / 190 / 220 / 96）。`390×844` 那一档裁切祖先换成没有边框的 `.l99-stage-wrap`，两侧都不钳、差 0 —— 高屏行为一字未变 |

#### `W5R3-TA-01` 的第二刀单独立账（这一条正是「只跑用例会误判已修」的活标本）

learner 自己写过：钳位数值和 `vis` 都对，真机 `elementFromPoint` 才暴露手牌盖住按钮。
我在 `aca1965^` = `217ab6c` 上单独跑了一遍，**一字不差地复现了那一幕**：

```
217ab6c（壳层钳位 + 挂滚 + showCtrl 都已落地，只差 .ld-fanbox{flex:0 0 auto}）
  landlord-cards L118 · 640×360 / 844×390 / 568×320
  .ld-wrap  ldc-tight ldc-tighter ldc-scroll，max-height 190/220/96，overflow-y:auto  ← 钳位全对
  五颗键 vis 全是 48,48,48,48 / 44                                                  ← 可见高全对
  .game-stage 下裁死的有字叶子：0                                                    ← 连这一项都是绿的
  可 elementFromPoint(键心) 打回来的是 span.ld-c-r / div.ld-card.ldc-card-move …
  落地 0/4 + 0/1，真手指慢拖累计仍 0/5
  .ld-fanbox：height 0（scrollHeight 124，被弹性布局压成 0 高，牌照着 JS 写死的偏移画出去）
```

**钳位数值、`vis`、裁死叶子这三项全绿，落地却是 0/5**——只跑单元测试确实会判成已修。
`aca1965` 补上那一刀之后同样三档 **5/5**、`.ld-fanbox` 高 137px。这一条我判 **真修**，
而且是**两刀缺一不可**：少了第一刀 0/5（没得滚），少了第二刀还是 0/5（滚了也被牌盖住）。

#### 两处我和 learner 报的数对不上，据实写在这里，不替谁圆场

| 事 | learner 的说法 | 我量到的 | 我的判定 |
| --- | --- | --- | --- |
| `red-blue-race` 844×390 改前 | 「改前落地 **0/3**」 | 按**元素几何中心**这把尺子确实是 **0/3**（三颗键 `vis` 21/52，中心点在裁切线以下）；但换成宽松口径「露出来那一截的中心能不能按到」，旧侧这一格是 **3/3** | **结论不变，措辞更准**：这一格旧侧不是「整颗在屏外」，是「只露了个头、中心点够不着」。另两档（640×360 / 568×320）旧侧是彻底的 `vis 0`。**够不着 0/24 这个总数成立** |
| 「五档视口裁死的有字叶子均为 0」 | 新侧 0 | 我第一版探针把 `<style>` 的文本、以及 `display:none` 的模式入口（`.ld-open` / `.rbe-open`）也算了进去，得出 4–6。**剔掉「压根没渲染出来」的之后新侧确实全 0** | **learner 的数对**，是我第一版尺子松了。修正后的口径两侧同源：新侧 0，旧侧 47/26/64 与 4/1/10 |

### A2 · 补修（本档能修的清零）：`W5R3-AF-01`

**我发现的**：`TA-05` 写的是「本档**四份** `fit.ts` 的裁切线改 `clientHeight` 口径」，
可这一档五款里的**收紧器有五份**——第五份是 `color-fun/ui.ts` 的 `fitColoringStage`，
它**没跟上**，照旧读父级的 `getBoundingClientRect().bottom`（border box 下沿）。
同一支探针、同一把尺子，第 181 关六档全中：

```
补修前（新侧 5a00ec4，不是旧侧）· .clf-wrap 钳出来的下沿 vs 真裁切线
  320×568  maxH 282  wrapBottom 554  真裁切线 550  overshoot +4
  360×640  maxH 354  wrapBottom 626  真裁切线 622  overshoot +4
  390×844  maxH 556  wrapBottom 830  真裁切线 826  overshoot +4
  640×360  maxH 142  wrapBottom 352  真裁切线 348  overshoot +4
  844×390  maxH 172  wrapBottom 382  真裁切线 378  overshoot +4
  568×320  maxH  48  wrapBottom 312  真裁切线 308  overshoot +4   ← 可视段总共才 44px，白多算近一成
```

跟 `TA-05` 是同一件事、同一个根因，只是漏在了第五个文件上。**本档能修，按同一把尺子补齐。**

**改法**（小而准，收官轮不动玩法）：给 `color-fun/ui.ts` 补一个与四份 `fit.ts` **逐字同源**的
`clipBottomPx()`，`fitColoringStage` 收集裁切祖先时改走它。**样式一个像素没动**，
那圈 `border:4px` 仍在 `src/styles.css`（禁改），原样交窗口1。

**先红**（`src/games/color-fun/clipBox.test.ts`，**改代码之前真跑了一遍**，8 条里 **6 红 2 绿**）：

```
× 量得出 clientHeight 就用它（横向滚动条也一并算掉）
    TypeError: clipBottomPx is not a function
× 四档真机实测：border box 那把尺子恒定多算 4px
    TypeError: clipBottomPx is not a function
× 量不出 clientHeight（桩节点 / SSR）就退回减掉下边框宽度
    TypeError: clipBottomPx is not a function
× 连边框宽度都读不到就照原样返回，绝不算成 NaN
    TypeError: clipBottomPx is not a function
× 没有边框的那一层（`.l99-stage-wrap`）一分不减，高屏上的行为一个字节没变
    TypeError: clipBottomPx is not a function
× `fitColoringStage` 量裁切祖先时真的按 padding box 走
    AssertionError: expected '/**\n * 涂色小屋 · 界面公共件（1.2 新增）。…' to contain 'clipBottomPx(p.getBoundingClientRect(…'
```

**后绿**（同一支探针、同一把尺子重打，六档 `overshoot` 全部归 0）：

```
补修后 · 第 181 关
  320×568  maxH 278  overshoot 0   调色锅整排落地@272  画布还看得见 180  ← TA-02 那一修没被顶回去
  360×640  maxH 350  overshoot 0   调色锅整排落地@270  画布还看得见 180
  390×844  maxH 552  overshoot 0   调色锅整排落地@  0  画布还看得见 180
  640×360  maxH 138  overshoot 0   调色锅整排落地@312  画布还看得见   0（横屏那条余量，见 A4）
  844×390  maxH 168  overshoot 0   调色锅整排落地@280  画布还看得见   0
  568×320  maxH  44  overshoot 0   调色锅整排落地@391  画布还看得见   0
```

**这一修最要紧的是不许把 `TA-02` 顶回去**：320×568 上滚动视口从 282 收准成 278，
`canPinCanvas(278, 180, 91)` = `98 ≥ 91` 仍旧成立，真机复量画布照旧整块 180px 钉住。
两条用例专门钉着这件事（「不许把 TA-02 那一修顶回去」「不许把已知余量粉饰成已解决」）。

**改动**：`color-fun/ui.ts`（新增导出 `clipBottomPx`，`fitColoringStage` 换尺子）、
`color-fun/clipBox.test.ts`（**新增 8 条**）。**热区一分没缩、玩法一行没动、存档 key 一个没碰。**

### A3 · learner 不落地的 11 条，逐条表态（交窗口 1 的一律只量不改）

| # | learner 的处置 | 我的表态 |
| --- | --- | --- |
| `W5R2-A-06` `color-fun` 画室开着还能 Tab 到壳顶栏 | 不落地，交窗口1 | **同意，维持只量不改，绝不写成已解决。** 我自己复量：390×844 打开画室，连按 **70 下 Tab** 走完一整圈，焦点**确实跑出去 5 次**——`a.skip-link`「跳到主要内容」、`button.btn--back`「🏠返回」、`button.guide-btn`「📖 攻略」、`button.icon-btn--pause`「⏸」。画室自己那 45 颗是关得住的（`inert` 挂在 `.clf-tools` 与它的兄弟上），漏的正是**壳顶栏**那一段，病灶在 `src/ui/**`，本档禁改。**仍在，交窗口 1** |
| `.game-stage{overflow:hidden}` 定高硬裁 | 一个字没动 | **同意。** 现场读数：`overflow:hidden / overflow-y:hidden`、`height:738px`（定高）、`box-sizing:border-box`。这是本节全部横屏兜底的共同上游。`src/styles.css` 我**一个字节没改**。**仍在，交窗口 1** |
| `.game-stage` 的 `border:4px` | 不落地，只把自己的尺子改准 | **同意，而且我把它改得更彻底**：`TA-05` 漏掉的第五份收紧器由我补齐（A2）。边框本身仍是 `4px`（实测 `borderWidth: 4px`），一行没动。**仍在，交窗口 1** |
| 选关地图节点 / 章节页签够不着 | 不落地，交窗口1 | **同意，维持只量不改。** 病灶在 `level99.ts`，禁改文件，我一行没动。**仍在，交窗口 1** |
| `quiz99` 连错闪答案 | 不落地，交窗口1 | **同意，维持只量不改。** 病灶在 `src/games/quiz99.ts`，跨窗口共用禁改文件，我一行没动。**仍在，交窗口 1**。本档两款学习游戏自己的提示不给答案，另有 `noSpoiler` 守门用例 |
| `color-fun` 横屏 142 / 172px 上画布仍钉不住 | 登记为**已知余量**，不写成已解决 | **同意，而且我自己重量过，既不当成已修也不误判成假绿**（详见 A4）。`CANVAS_MIN_PX = 180 > 142`，物理上塞不进去，不是没使劲 |
| `W5R2-A-07` / `A-08` 塔顶难度 | 不落地（测试员判设计意图并结案） | **同意关闭。** 收官轮为一关的手感动难度曲线，风险大于收益。我没另开判据重算，据实登记 |
| `W5R2-A-09` 无尽跑道无收工条件 | 不落地（玩法定义） | **同意关闭。** 规则是撞 3 次收工，不撞就不结束 |
| `W5R2-A-10` 两处混色近似 | 不落地（不影响过关） | **同意关闭。** 改配方表会把跑通的 188 关配色全部推倒，**收官轮不动玩法** |
| `W5R3-D-01` / `D-03` | 维持测试员原判，不修 | **同意维持。** `.rbr-msg` 第一行完整、手牌扇形叠放是设计 |
| `W5R2-L-15` 三款各写一份收紧器可抽公共件 | 不落地（收官轮不许大重构） | **同意。** 而且我这一棒**又坐实了它的代价**——正因为五份各写各的，`TA-05` 才会漏掉第五份（A2）。但抽取要新建跨款共用文件，收官轮不动。**留给后续轮次 / 窗口 1** |

### A4 · 「已知余量」我自己重量了一遍（不当已修，也不误判假绿）

learner 写的是「横屏 142px 仍钉不住，没写成已解决」。这句站不站得住，我按两问分开量：

**问一：横屏上够不够得着？** —— 够得着，两侧都够。真手指推到推不动为止（**不 poke `scrollTop`**）：

| 视口 | 补修后累计 | `1ab90a9` 累计 | 备注 |
| --- | --- | --- | --- |
| 640×360 | 调色锅 **5/5** · 调色板 **3/3** · 工具 3/3 · 缩放 1/1 | 5/5 · 3/3 · 3/3 · 1/1 | 两侧都够得着；新侧内容 614→556、可滚距离 472→414，**少滚 58px** |
| 844×390 | **5/5 · 3/3 · 3/3 · 1/1** | 5/5 · 3/3 · 3/3 · 1/1 | 同上，可滚距离 442→384 |
| 568×320 | **5/5 · 3/3 · 3/3 · 1/1** | 5/5 · 3/3 · 3/3 · 1/1 | 我加的这一档同样够得着，可滚距离 566→508 |

（我第一版探针在这三档上一度报出 0/5，追下去是**我自己的步长问题**：一趟推得太狠越过了那一格，
以及趟数按位移预算算、可触摸滑动起手要吃掉十几像素的 slop。改成「细步长 + 推到推不动为止」
之后两侧读数都干净了。**据实写在这里，免得后人以为横屏出过回归。**）

**问二：在不在同一屏？** —— **不在，余量属实**。三档横屏调色锅整排落地那一刻，
画布可见高**新旧两侧都是 0**。根因是算术，不是没使劲：

```
canPinCanvas(port, canvas, tail) = port - canvas >= tail
  640×360   138 - 180 = -42  <  91   ← 画布底线 180px 压根塞不进 138px 的可视段
  844×390   168 - 180 = -12  <  91
  568×320    44 - 180 = -136 <  91
  320×568   278 - 180 =  98  >= 91   ← 竖屏这一格才是 TA-02 真正救回来的那一格
```

再往下收线稿里的小块就点不准，那是拿一个毛病换另一个毛病。上游是 `.game-stage` 只给横屏
138 / 168 / 44px（平台定高硬裁，`src/styles.css`，**交窗口1**）。
**我的结论与 learner 一致：这一条不算已解决，登记为余量交窗口 1。**

### A5 · 终审（本档五款，不是全库 76）

| 项 | 怎么审的 | 结论 |
| --- | --- | --- |
| **商标 / 官方角色名** | **86 个词** × 五款目录全部 **119 个文件**。**先把 `//` 与 `/* */` 注释整段抹成等长空白再扫一遍**，用来证明「注释里也没有」 | **真命中 0**。全文 85 处**全部**是守门用例自己的黑名单表（`landlord-cards` / `red-blue-race` / `clock-house` / `word-garden` 的 `shell.test.ts` 与 `color-fun/upgrade12.test.ts`，5 个文件 × 17 词）；**生产代码 0 处、只出现在注释里的 0 处** |
| **`destroy` 无泄漏** | 五款各**进出 10 次**（`location.hash` 真切换，不是重新加载）。页面脚本跑起来之前先把 `add/removeEventListener`、`set/clearTimeout`、`set/clearInterval`、`request/cancelAnimationFrame`、`AudioContext` 全包一层记账 | **五款全部 0 涨**。第 1 圈与第 10 圈**逐项相同**：window 监听 1 / document 监听 2 / 未清 timeout 0 / 未清 interval 0 / 未收 rAF 0 / `AudioContext` 0。10 圈中间也没有任何一圈鼓包 |
| **教育 · `clock-house` 时段词** | **自己另写一份判据**：课本七段词表（夜里 0 / 凌晨 1–5 / 早上 6–8 / 上午 9–11 / 中午 12 / 下午 13–17 / 晚上 18–23）现场抄，**不 import `logic.ts` 的任何函数去算答案**；扫 188 关每道题的题面 / 引导语 / 选项 / 夸奖语，连 SVG 的 `aria-label` 一起 | **1224 道题 / 404 条说法，硬错 0**。判据**自证会咬**：喂「上午 12 点」「下午 12 点」「中午 3 点」「凌晨 9 点」逐条报错，喂「下午 3 点」放行 |
| **教育 · `word-garden` 独立判据** | 同样**不 import 它算答案的任何工具**，只看题目数据自洽：答案非空、选项 ≥2 且不重复、`correct` 下标不越界且指向答案、干扰项不许跟答案撞车，外加**「问的和判的必须是同一个字」**（引导语点名「哪个字是「X」」的，判对那一项就得是 X） | **1181 道题，硬错 0**。判据自证会咬：捏一道「问「山」判「水」」的假题，它立刻报错。（中途我这把尺子误伤过 `radical` 题型——「哪个字和「水」有关？」答案是「湖 / 河 / 流」本来就对，**是我判据的 bug，不是游戏的错**，已据实修正并写在这里） |
| **脏存档不白屏** | 六个 key（`l99.<id>` / `l99skip.<id>` / `save.v1` / `collection.v1` / `fav.v1` / `recent.v1`）**同时**灌 8 种垃圾（空串 / 半截 JSON / 不完整数组 / `null` / 裸数字 / 不是数组的对象 / 400 长数组 / 纯垃圾字符），五款 × 8 = **40 格** | **白屏 0 格、`console.error` 与未捕获异常 0 条**。每格 `.game-stage` 都在，节点 **65–84**、文字 **272–317 字**，标题、「🚩 关数」「⭐ 星数」「开始冒险 ▶」「📖 攻略」全读得出来 |
| **禁改公共文件** | `git diff --name-only 5a00ec4..HEAD` 过滤禁改清单 | **命中 0**。`level99.ts` / `quiz99.ts` / `speech.ts` / `parentAuth.ts` / `save.ts` / `collection.ts` / `src/ui/**` / `src/engine/**` / `src/styles.css` / `README.md` **一个字节没动** |
| **用例只增不减** | 我的提交里删除的 `it(` 行数 | **0**。既有断言一条没改判、一条没删 |
| **热区下限** | 补修那一笔只换了一个量法函数，没碰任何尺寸 | `.clf-tool` / `.clf-swatch-dot` / `.clf-primary` / `.clf-zoom` 的 44px **一分没缩** |
| **合并态自跑** | 见 A7 | `npm test` **722 文件 / 15324 用例全绿**；`npm run build` **通过** |

### A6 · 遗留：交窗口 1 vs 本档

| # | 事 | 归属 | 最终结论 |
| --- | --- | --- | --- |
| `W5-F-19` / `.game-stage{overflow:hidden}` 定高硬裁 | `src/styles.css` | **窗口 1** | **仍在，一行没动。** 现场读数 `overflow:hidden`、`height:738px`、`box-sizing:border-box`。本节全部横屏兜底的共同上游 |
| `.game-stage` 的 `border:4px` | `src/styles.css` | **窗口 1** | **仍在，一行没动。** 本档**五份**收紧器现在都按 padding box 量（第五份由我补齐），但那圈边框还在，仍旧吃掉 4px 可视段 |
| `W5R2-A-06` 画室开着能 Tab 到壳顶栏 | `src/ui/**` | **窗口 1** | **仍在，一行没动，不写成已解决。** 我复量到 Tab 一圈跑出去 5 次（skip-link ＋ 🏠返回 ＋ 📖攻略 ＋ ⏸）。画室内部那 45 颗关得住，漏的是壳顶栏 |
| `color-fun` 横屏画布钉不住 | 平台给的可视段 | **窗口 1** | **仍在，登记为余量，不写成已解决。** `180 > 138 / 168 / 44`，物理装不下。按钮本身横屏三档真手指全够得着（A4） |
| 选关地图节点 / 章节页签够不着 | `level99.ts` | **窗口 1** | **仍在，一行没动，不写成已解决** |
| `quiz99` 连错闪答案 | `src/games/quiz99.ts` | **窗口 1** | **仍在，一行没动，不写成已解决。** 与「学习提示不直接给答案」的红线冲突，但不影响题目正确性 |
| `W5R2-L-15` 五份收紧器可抽公共件 | 跨款 | 后续轮次 | **不动，但代价已坐实**：正因为各写各的，`TA-05` 才漏掉第五份。抽取要新建跨款共用文件，**收官轮不许大重构** |
| `W5R2-A-07` / `A-08` / `A-09` / `A-10`、`W5R3-D-01` / `D-03` | 本档 | 本档 | **关闭，维持原判**（塔顶该紧是设计意图 / 玩法定义 / 游戏化近似 / 探针噪声） |

**本轮档A 计数（我签的字）：阻断 0 / 严重 0 / 一般 0 —— 全部清零。**
learner 落地的 5 条**独立复算全部成立，5 条真修、0 条假绿、0 条空转**；
我自己揪出并补掉 1 条（`W5R3-AF-01`，`TA-05` 漏掉的第五份收紧器），已先红后绿、真机复验。

### A7 · 改动登记与水位

| 我的 commit | 改了什么 |
| --- | --- |
| `1d9099c` | `docs/qa/1.2-window5-round3-fixer.md`（**只新增 `## 档A · 五款` 的 A0**；F0–F7 与 `## 档C · 五款` 一个字节没动） |
| `3f7d797` | `color-fun/ui.ts` + **新增** `color-fun/clipBox.test.ts`（8 条） |
| 本节 | 同一个文件的 A1–A7 |

| 项 | 结果 |
| --- | --- |
| `npm test` | ✅ **722 个测试文件 / 15324 条用例全绿**（0 失败 0 跳过，175.1s） |
| `npm run build` | ✅ 通过（`tsc --noEmit` 无报错，PWA precache **188 entries / 5125.17 KiB**） |
| 本档五款 | **61 个测试文件 / 1035 条用例全绿**（开工时 60 / 1027，`+1 文件 / +8 条`，全是我这一条补修带的） |
| 先红后绿 | `clipBox.test.ts` 在改 `ui.ts` 之前真跑了一遍：**8 条里 6 红 2 绿**，失败原文见 A2；改后 8 条全绿 |
| 删除的 `it(` | **0** |
| 禁改公共文件 | **命中 0** |
| 新增依赖 | **0**（`package.json` / `package-lock.json` 没动） |
| 存档 key | **一个没动** |
| `color-fun` chunk | 65.60 → **65.83 kB**（+0.23，就是那个 `clipBottomPx` 加它的注释） |
| 热区口径 | 一律 `document.elementFromPoint(元素几何中心点)`，**`el.click()` 一次没用**；滚动一律真手指、**一次没 poke `scrollTop`**；裁切线按 padding box |
| 真机视口 | `320×568` / `360×640` / `390×844` / `640×360` / `844×390` ＋ 我自己加的 `568×320` 矮横屏 |
| 探针 | 落 `/tmp/w5r3af/`，另开两个 `git worktree`（`1ab90a9` / `217ab6c`）跑反例，**全部跑完即删、0 字节进提交** |
| 分支 | `game-1.2-window5`（`fetch` → `rebase`：**别人的节 theirs、档A 节 ours**；**没推 `main` / 没推 `game-1.2` / 一次 `--force` 都没用 / 没用 `gh` 开改 PR**） |
| 派了几个云端子代理 | **0**。不开第 4 轮，不写 tester / learner |

### A8 · 本档五款能否随窗口 5 一起提交进 `game-1.2`

**能。**

1. **learner 宣称落地的 5 条，我拿自己的尺子从头量了一遍，条条站得住**——**5 条全是真修，
   没有一条假绿、没有一条空转**。每一条都带**补丁前 commit 上的反例**：旧侧红得清清楚楚
   （横屏 0/24 且慢拖 8 趟纹丝不动、调色锅落地时画布 `visH` 0、`.wgd-msg` 五档 vis 0、
   屏上「不要」60 轮 55 处、`overshoot` 十格全 +4），新侧同一支探针同一把尺子全绿。
   **没有一个数字是照抄别人报告里的。**
2. **`TA-01` 我按要求真机 CDP 复测，没只信用例绿**——而且专门在 `aca1965^` 上复现了
   「钳位对、`vis` 对、裁死叶子 0，`elementFromPoint` 却仍是 0/5」那一幕。
   横屏 **24/24 落地即可点**，六档裁死的有字叶子全 0。
3. **本档能修的清零**：我自己揪出 1 条（`W5R3-AF-01`，`TA-05` 漏掉的第五份收紧器，
   六档 `overshoot` 全 +4、矮横屏上白多算近一成可视段），已在本档目录内先红后绿补掉，
   真机复量 **+4 → 0**，而且用两条用例钉住「不许把 `TA-02` 那一修顶回去」。
   **阻断 0 / 严重 0 / 一般 0。**
4. **终审九项全过**：商标真命中 0（注释抹白再扫）、`destroy` 五款各 10 圈逐项 0 涨、
   两款学习游戏的教育正确性用**我自己写的、能自证会咬的独立判据**复算（1224 题 404 条说法硬错 0；
   1181 题硬错 0），脏存档 40 格 0 白屏、禁改文件命中 0、删除的 `it(` 0、
   合并态 `npm test` + `npm run build` 全绿。
5. **遗留分得清清楚楚**：交窗口 1 的六条（`overflow:hidden` 硬裁、4px 边框、画室 Tab 穿顶栏、
   横屏画布钉不住、选关地图、`quiz99` 闪答案）**我一行没动，也没有一条写成已解决**；
   本档自己的六条各有明确结论。

**这一批五款可以随窗口 5 一起提交进 `game-1.2`。**
（本档只签这五款，**不签整个 1.2**；`game-1.2` 的合并由主管定，我没把本窗分支合进去，也不开第 4 轮。）
