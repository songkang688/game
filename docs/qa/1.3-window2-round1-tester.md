# 1.3 窗口 2 · 第 1 轮视觉验收 · 测试员报告

> 角色：窗口 2 第 1 轮视觉测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库 39 款、不碰其他窗口。
> 权限：只写本报告 + 本窗 9 款目录下新增 `round1-visual-scan.test.ts`。不改绘制代码、不改玩法数值、不改 `src/art/kit/`、不改 `src/styles.css`（chess-garden 末尾追加段仅检视）。
> 实测环境：node 22.14.0 / npm 10.9.7 / headless Chrome（`/usr/local/bin/google-chrome`，puppeteer-core `--no-save` 临时驱动，未入库）。

## 扫描计划（动手前登记）

1. **全量跑**：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积。
2. **盘点**：9 款目录与 `src/art/kit/` 是否合入；plan 文档（step5-A/B/C、step6-A/B/C、step7-A/B/C）与实际提交对照；`git log` 抽查是否有人改动他窗目录。
3. **六大专项**（只扫本窗 9 款）：① 火柴人/草稿圆 ② 金币体积 ③ 双人可区分 ④ 精 2D ⑤ 360px ⑥ 商标。
4. **横向一致性**：光照约定、`prefers-reduced-motion`、destroy 进退 ×5、粒子性能。
5. **产出**：本报告 + 问题清单 + 9×6 矩阵 + 新增视觉契约用例。

---

## 一、全量跑数据与盘点结论（对应规格第三节）

### 1.1 全量跑

| 项 | 结果 |
| --- | --- |
| `npm ci` | ✅ 通过（依赖 5.4s 装完） |
| `npm test`（收货基线） | ✅ **703 文件 / 15178 例全绿**，178.57s——与包工头门禁宣称一致，只增不减基线成立 |
| `npm test`（本轮收尾，含新增用例） | ✅ **712 文件 / 15216 例全绿**（+9 文件 / +38 例，见第八节） |
| `npm run build` | ✅ 全绿；PWA precache **188 entries / 5208.50 KiB** |
| `npx vite preview` 冒烟 | ✅ HTTP 200；9 款深链 `#/game/<id>` 全部可进、进到第 1 关盘面、360/320 两档视口 **0 条 pageerror** |

本窗 9 款构建体积（`dist/assets/*.js`）：

| 款 | 体积 | gzip |
| --- | --- | --- |
| dark-chess | 34.15 kB | 13.14 kB |
| dot-maze | 38.29 kB | 14.48 kB |
| merge-2048 | 41.90 kB | 16.03 kB |
| fruit-stack | 49.79 kB | 18.31 kB |
| junqi-camp | 50.89 kB | 17.80 kB |
| mine-garden | 56.16 kB | 20.23 kB |
| pool-stars | 60.36 kB | 22.28 kB |
| chess-garden | 60.96 kB | 23.31 kB |
| sudoku-petal | 90.95 kB | 33.56 kB |

### 1.2 目录与 kit 盘点

- `src/art/` 目录不存在：**窗口 1 kit 未入本分支，各款本地 `art.ts`**（9/9 都有本地 art.ts，符合各款 plan 的「kit 未合入前本款自绘」口径）。
- `git log origin/game-1.3..HEAD`（41 个提交）改动只落在：本窗 9 款目录、`docs/qa/`、`src/styles.css`。`styles.css` 的改动是 chess-garden 第 7 步 B 的**末尾追加段（+473 行，`28b69be`）**，选择器全部带 `.cg-wrap`/`cg-` 前缀，不覆盖上方既有规则——属已授权范围，本轮仅检视。**没有人碰他窗目录。**

### 1.3 plan 文档对照（9/9 已交，无漏做、无做了没推）

| 步/档 | 款 | plan 文档 | 实际提交（本分支可见） |
| --- | --- | --- | --- |
| 5A | merge-2048 | `docs/plan-1.3-step5-A-merge-2048.md` | ✅ `35d1a52` feat + `1893149` test |
| 5B | mine-garden | `docs/plan-1.3-step5-B-mine-garden.md` | ✅ `3bebdc3`/`9dcc96c` feat + `a060017` test |
| 5C | sudoku-petal | `docs/plan-1.3-step5-C-sudoku-petal.md` | ✅ `fd424dd`/`7cbed62` feat + `c9278b9` test |
| 6A | dot-maze | `docs/plan-1.3-step6-A-dot-maze.md` | ✅ `dd4c1ff`…`dc188b8` feat ×5 + `44e3e71` test |
| 6B | fruit-stack | `docs/plan-1.3-step6-B-fruit-stack.md` | ✅ `b618681` feat + `8e8ac82` test |
| 6C | pool-stars | `docs/plan-1.3-step6-C-pool-stars.md` | ✅ `828dda8` feat + `673ada9` test |
| 7A | junqi-camp | `docs/plan-1.3-step7-A-junqi-camp.md` | ✅ `c987076`/`b3b27a5` feat + `fc2474f` test |
| 7B | chess-garden | `docs/plan-1.3-step7-B-chess-garden.md` | ✅ `a2613a5`/`28b69be`/`82a21a7` feat + `f7d8a50` test |
| 7C | dark-chess | `docs/plan-1.3-step7-C-dark-chess.md` | ✅ `815ae9a`/`f995fcd` feat + `7571c5f` test |

---

## 二、专项① 火柴人 / 草稿圆扫描

**方法**：`rg fillText\(` 9 款全量；unicode emoji 码点（U+1F300–1FAFF / 2600–27BF / 2B00–2BFF）grep 非测试代码逐处核对用途；核心主角/道具的绘制函数逐一读码 + 360px 盘面截图目检。

**结果**：`fillText` 在 9 款里共 5 处，全部是**文字**（fruit-stack 警戒线标签 `index.ts:568`、合并飘字 `art.ts:753`；dot-maze 连击飘字 `index.ts:426`、结算百分比 `index.ts:459`；另 1 处在 art.test 断言里）。**核心主角/道具 0 处 emoji 直出、0 处无渐变纯圆**：

| 款 | 核心视觉主体 | 画法 | 判定 |
| --- | --- | --- | --- |
| merge-2048 | 数字块/星角标/障碍花 | CSS 三停渐变面 + SVG 角标/花 | ✅ |
| mine-garden | 花/旗/四叶草/木牌 | `art.ts` 纯函数 SVG（emoji 只活在 `<title>` 无障碍口径） | ✅ |
| sudoku-petal | 花瓣/花苞/叶片 | 三层实心 SVG | ✅ |
| dot-maze | 玩家/幽灵/豆/果子 | canvas 贴图：渐变身体+呆毛+单眼；幽灵四尖裙边三态 | ✅ |
| fruit-stack | 11 级果卡 | `paintFruit` 径向渐变+纹理+脸，`CHAIN[].emoji` 字段不进画布 | ✅ |
| pool-stars | 球体/球杆/台呢 | sprite 预渲染（渐变+压印） | ✅ |
| junqi-camp | 棋子/牌背/地形 | 军衔条 SVG + CSS 渐变面板 | ✅ |
| chess-garden | 12 张棋子 | `pieceSVG` 内联 SVG | ✅ |
| dark-chess | 牌背/棋面/迷你棋 | `backSVG`/`pieceFaceSVG` | ✅ |

**装饰性 emoji 登记**（非测试代码出现次数，集中在 guide.ts 教程文案、HUD chips、模式按钮文案）：merge-2048 28 / mine-garden 42 / sudoku-petal 24 / dot-maze 32 / fruit-stack 46 / pool-stars 48 / junqi-camp 28 / chess-garden 46 / dark-chess 19，合计 313。

**问题**：

| # | 位置 | 现状 | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| 1-1 | `src/games/dark-chess/view.ts:353,357`（renderCell） | `b.textContent = "🌸"` / `= labelOf(...)` 赋值后紧跟 `b.innerHTML = ...SVG` 整体覆盖，emoji 是死代码，不渲染 | 删掉冗余 textContent 赋值（或注明是읽屏兜底再改成 aria-label） | 建议 | `src/games/dark-chess/view.ts` |

**结论：严重 0。**

---

## 三、专项② 金币体积（收集物 / 果子 / 球 / 棋子）

**方法**：逐款读绘制函数，统计渐变（canvas `addColorStop` / SVG `<stop>` / CSS `linear-gradient`）、高光、描边/落影三件套；360px 盘面截图目检双证。

| 款 | 抽查对象 | 渐变 | 高光 | 描边/落影 | 判定 |
| --- | --- | --- | --- | --- | --- |
| merge-2048 | 数字块 `tileFaceCSS` | 3 停 135° | 左上提亮停 | `.mg-tile` box-shadow + 星角标 stroke + 白高光点 | ✅ |
| mine-garden | 盛开花 `flowerSVG(2)` | 层叠光影（外瓣+内高光瓣 5+5） | 花芯高光点 `#FFF3C9` | 花瓣/花芯描边 + 落影椭圆 | ✅（层叠等效） |
| sudoku-petal | 花瓣 `petalSVG` | 3 层实心（暗部/主色/高光，灰度实测递增） | 第 3 层 opacity .8 | 暗部衬底当落影；**注释明示不用 defs/id 防撞车** | ✅（层叠等效） |
| dot-maze | 豆/能量豆/果子/玩家 | 2–3 停径向 ×10 处 | 光晕 halo + 顶部高光 | 果子描边、幽灵星标描边 | ✅ |
| fruit-stack | 果卡 `paintFruit` | 3 停径向（hi→主色→edge） | 白高光 | 描边 + 底部内侧环境影 | ✅ |
| pool-stars | 球 `paintBallBase` | 3 停径向 + spot 高光渐变 | ✓ | 阵营压印 + 厚度 | ✅ |
| junqi-camp | 棋面 `.jq-face` | CSS 2 停 | 渐变上亮 | `box-shadow 0 2px 0` 落影 + inset 描边；军衔条金星 stroke | ✅ |
| chess-garden | 棋子 `pieceSVG` | **0 处渐变** | 左上高光椭圆 `t.hi` | 描边 1.2 + 底座投影 rgba(70,45,20,.20) | ⚠ 一般 |
| dark-chess | 棋面 `pieceFaceSVG` | 2 停象牙径向 | 渐变左上亮心 cx.38 cy.3 | 厚度椭圆 + 阵营环 + 汉字双钩 | ✅ |

**问题**：

| # | 位置 | 现状 | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| 2-1 | `src/games/chess-garden/art.ts` 全部 `*Inner()`（106–187 行） | 12 张棋子主体是平涂 fill + 描边 + 高光椭圆 + 底座投影，**渐变 0 处**。非「平涂圆贴字」（那才记严重），三层体积俱全，但不满足「≥2 停渐变」的字面口径 | body 补一层 2 停线性/径向渐变（或 kit 合入后统一）；注意 SVG 多份内联需避 defs/id（可参照 sudoku-petal 层叠法再加一层提亮 path） | 一般 | `src/games/chess-garden/art.ts` |
| 2-2 | `src/games/dark-chess/art.ts:35,73`（backSVG/pieceFaceSVG） | `<defs>` 用固定 id `dcWood`/`dcIvory`，32 格内联后同文档重复 id（无效 HTML）。当前各份内容相同、浏览器取首个 id 恰好同款，视觉无碍；但与 sudoku-petal 明确规避的口径相悖，后续改版有串色隐患 | 渐变改层叠实心或 id 加格位后缀 | 一般 | `src/games/dark-chess/art.ts` |

**结论：严重 0（「平涂圆内贴数字/星字符」0 处）；一般 2。**

---

## 四、专项③ 双人 / 对战座位可区分（16px 灰度、形状+颜色双通道）

**方法**：只查本窗双人/对战位。取双方主色算灰度（0.2126R+0.7152G+0.0722B），读绘制代码找形状通道；截图目检。

| 款 | 双方 | 颜色通道（灰度差/255） | 形状通道 | 判定 |
| --- | --- | --- | --- | --- |
| fruit-stack 双盆 | 朵朵/星星座位条 | 字色 #a8306a vs #28568f | 座位条 🌸 vs ⭐ 字形 + 名字；盆体同款（靠盆头标签区分，登记备注） | ✅ |
| pool-stars | 暖组/冷组球 | 主色灰度 Δ≈3.6（153.0 vs 156.6，几乎同档） | **花印 vs 星印**；极小尺寸退化仍是圆点 vs 菱形点 | ✅（新用例锁定） |
| junqi-camp | 朵朵/星星棋面 | 面板底 #FFE7D5→#FBD3B6 vs #DFEAFA→#C4D8F4：灰度 **Δ≈2.0 / Δ≈3.6**；字色 Δ≈22 | **无**——军衔条双方同为金色，`faceHTML` 不含侧别图形；`crestSVG`/`hqSVG` 去掉旗色后逐字节相同（新用例把该证据钉死） | ❌ 严重 |
| chess-garden | 白/黑 | 主体 #FBF2E2 vs #57412F：灰度 **Δ≈174** | 棋子同形属国象制式，明暗+描边深浅已过 16px 灰度测试 | ✅ |
| dark-chess | 红/蓝 | 环色 #c03a2b vs #2f66b8 | **14 个汉字两两互异**（帅≠将、俥≠車…）+ 金钩 vs 银钩 | ✅ |
| merge-2048 双人同屏 | 朵朵/星星 | 座位标签色 | 左右双盘 + 名字标签 + 位置通道（盘面同款，登记备注） | ✅ |

（mine-garden / sudoku-petal / dot-maze 的双人模式为同屏双盘/轮流制，不在本专项点名清单内——登记不展开。）

**问题**：

| # | 位置 | 现状 | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| 3-1 | `src/games/junqi-camp/view.ts:112–115`（.jq-duo/.jq-star 面板）+ `src/games/junqi-camp/art.ts:207–218`（crestSVG） | 双方棋面只差底色/字色：16px 灰度下面板亮度差仅 ≈2–3.6/255，形状通道缺位（军衔条、字体、边框全同款） | 给一方加形状第二通道：如棋面角落加小旗/星徽剪影、或双方边框形状（圆角 vs 切角）区分；灰度差拉到 ≥40 更稳 | **严重** | `src/games/junqi-camp/view.ts`（+`art.ts`） |

**结论：严重 1（junqi-camp），交 C 档 fixer。**

---

## 五、专项④ 精 2D 可读性（本窗替代跑酷 2.5D 专项）

**方法**：9 款 grep `three|THREE|perspective|rotate3d|rotateX|rotateY|matrix3d`；`package.json` 依赖核对；盘面截图目检。

- **three.js：0**（`package.json` 无依赖；代码 `three` 命中全是变量名 `threeFoulLoss`/星级 `three`）。
- **perspective：0 处**。`rotateX/rotateY` 共 4 处（mine-garden `mnflip` 翻格、sudoku-petal 数字键用完翻面、junqi `jq-flip` 翻子、dark-chess `dcFlipIn` 翻子），全部是**未加 perspective 的纯 2D 压扁翻转动画**，瞬时演出、不改变盘面读法。
- 9 款棋牌/数字盘面全部正视精 2D，无透视破坏可读性。
- 专项④点名的跑酷 2.5D 款（sky-squad 等）：**本窗范围外，跳过**。

**结论：阻断 0。**

---

## 六、专项⑤ 360px 布局实测

**方法**：headless Chrome 实测 **360×640 与 320×568（dpr2）**，逐款走「深链 → 选模式 → 进第 1 关盘面 → 点一次盘面」，量化采集：`scrollWidth` 溢出、越界元素矩形（剔除 `overflow:auto` 滚动容器内元素）、可见文本字号 <14px、交互触区 <40px；截图存证；可疑项定点复测。

**总体**：9 款 ×2 视口 `document.scrollWidth == innerWidth`，页面级 0 溢出、0 JS 错误。触区抽测：chess-garden 64 格恰 40×40、junqi-camp 57×57、merge-2048 76×76、sudoku-petal 56×56，均 ≥40px。选关页 `.l99-tabs` 章节页签条是 `overflow-x:auto` 滚动容器（初测「越界」为误报，已剔除）。

**问题**：

| # | 位置 | 现状（量化） | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| 5-1 | `src/games/mine-garden/index.ts:385`（`.mn-mini{display:block}`）+ `:653`（`mini.hidden = !wide`）+ `:659`（`drawMini` 的 `floor(300/w)` 基准） | **小地图 `hidden` 失效**：`.mn-mini` 的 `display:block` 覆盖了 `[hidden]` 的 UA `display:none`（实测 `hidden=true` 但 computed display=block）。后果 a) 盘面下方**永远**渲染一块 302×152 的空底板（截图：5×5 第 1 关下方半屏空绿框）；b) 320px 视口下该 canvas L32→R334，**越界 14px**（canvas 属性宽 300+2px 边框 > 容器 256，auto 边距归零）。小地图是 1.3 新增（`9dcc96c`）→ 视觉升级导致的新占位/溢出 | `.mn-mini[hidden]{display:none}`（一行 CSS）或改用 style.display 切换；`drawMini` 的 300px 基准按 `min(容器宽-4, 300)` 收 | **阻断（1.3 新增）** | `src/games/mine-garden/index.ts` |
| 5-2 | `src/games/dark-chess/view.ts:34,37`（`.dc-board` 8×1fr + `.dc-cell{min-height:44px;aspect-ratio:1/1}`） | 8 列格子最小内容宽 44px：一行 8×44+7×3 gap≈373px，盘容器只有 283.2px。末列右缘实测 **418.4px**，裁切线在 `.l99-stage-wrap{overflow:hidden}` 的 **341.6px**——360px 下**约 1.7 列不可见且不可点**（无横向滚动可达）；320px 下约 2.5 列（截图为证）。基线 `origin/game-1.3` 已如此（`min-height:44px` 非 1.3 引入）→ 存量、非视觉升级新增，但属 360px 硬破坏 | 触区与列宽解耦：`.dc-cell{min-height:0;min-width:0}` 让 1fr 收缩（8 列 ×35px 仍 ≥32px 可点面积，或格子 40px 上限 + 棋盘换 `aspect-ratio` 自适应两行四列布局） | **阻断（存量）** | `src/games/dark-chess/view.ts` |
| 5-3 | `src/games/chess-garden/view.ts`（`.cg-coord` 9px、`.cg-piece-tag` 11px） | 坐标字号实测 **9px** ×16 处、棋子中文角标 **11px** ×32 处（<14px 红线）。均为辅助标注非正文，40px 格内空间受限 | 坐标提到 ≥10.5px 加粗、角标 ≥12px，或 420px 以上视口放大 | 一般 | `src/games/chess-garden/view.ts` |
| 5-4 | 各款 HUD chips / 模式按钮 | <14px 清单（实测值 × 处数）：`l99-star`★10–12px×3（选关星标，装饰字符）；`dmz-chip`13px×5；`fs-chip`12px×3、`fs-bowlname`12.5px、`fs-tip`12.5px、`fs-btn`13px；`ps-chip`13px×2、`ps-power-*`13px×2、`ps-tip`13px；`jq-chip`13px×4、图例 span12px×5、`jq-note`13px；`dc-chip`13px×4、`dc-note`13px；`cg-tool`/`cg-seat`13px；各款 pick 按钮 13–13.5px | HUD 状态 chips 统一提至 ≥14px（或至少正文类 tip/note 提级）；装饰性 ★ 可豁免但登记 | 一般 | 各款 index/view 的内联 CSS（C 档统一权衡） |

（dark-chess 曾测得一枚 16×41 的 `dc-cell`——复测动画停稳后 32 格全部 44×44，系翻子过渡中间态，不立案。）

**结论：阻断 2（1 处 1.3 新增、1 处存量）→ 清单交 C 档 fixer；一般 2。**

---

## 七、专项⑥ 商标扫描

**方法**：9 款全部文件 grep 黑名单（4399/任天堂/nintendo/迪士尼/disney/马里奥/mario/皮卡丘/pikachu/宝可梦/pokemon/米老鼠/mickey/吃豆人/pac-man/pacman/blinky/pinky/inky/clyde/tetris/乐高/lego/hello kitty/奥特曼/ultraman/喜羊羊/熊出没/小猪佩奇/peppa/supercell）+ 造型目检。

- **非测试代码 0 命中。** 测试代码的全部命中都是各款**负面清单断言自身**（dot-maze `copy.test.ts` 一款就锁了官方角色名与四只官方幽灵名，junqi/pool/chess/dark 的 smoke.test 同款负词表）——这是防线不是违规。
- 误报排除 1 处：`pool-stars/lose-line.test.ts:8` 注释中的提交 SHA `d324399` 含子串「4399」，为十六进制 git SHA，非商标使用。
- **造型相似度目检**：dot-maze 幽灵为「圆顶+四尖裙边+白眼深瞳」，剪影与街机通用幽灵相近；但配色是马卡龙淡彩（#FF9AB0/#FFC48A/#9FD8F5/#C7B3F2，每通道 ≥138，实测与官方饱和红/粉/青/橙全部不同色），角色名「智/怪/绕/乱」原创，玩家是「渐变身体+呆毛+单大眼」的原创豆豆勇士（非纯黄圆缺）。新用例把「四色两两不同且全为淡彩」锁死。
- HUD/文案里的 emoji（🌸⭐🍬 等）为系统字体字符，不构成商标。

| # | 位置 | 现状 | 期望 | 严重度 | 建议责任文件 |
| --- | --- | --- | --- | --- | --- |
| 6-1 | `src/games/dot-maze/art.ts:404`（drawGhostFigure） | 幽灵剪影（圆顶+四尖波浪裙边）与街机原作同构；已有淡彩配色/原创名/三态画法差异缓解 | 第 2 轮跟踪造型差异化（如改三尖裙边、加小耳朵/呆毛），拉开剪影距离 | 建议 | `src/games/dot-maze/art.ts` |

**结论：阻断 0；建议 1。**

---

## 八、横向一致性抽查（只在本窗 9 款）

- **光照约定（左上 45°）**：抽查一致——dot-maze 玩家渐变心 (-0.24r,-0.3r)、fruit-stack (-0.35r,-0.4r)、pool-stars (-0.34r,-0.38r)、dark-chess 象牙渐变心 (cx .38, cy .3)、chess-garden 高光椭圆全部左上象限、merge-2048 135° 左上提亮、junqi/sudoku/mine-garden 顶亮底暗。9/9 同向。
- **`prefers-reduced-motion`**：9 款仿真开启后全部正常进盘、0 JS 错误（media=true 确认生效）。代码接线处数：merge-2048 1 / mine-garden 12 / sudoku-petal 14 / dot-maze 7 / fruit-stack 23 / pool-stars 13 / junqi-camp 6 / chess-garden 11 / dark-chess 4。登记备注：merge-2048 只有 1 处接线（粒子分级入口），其弱动效降级已有既有用例覆盖，本轮不立案。
- **destroy 泄漏（进→看→退 ×5）**：9 款 DOM 节点数与 JS 堆全程平稳（DOM 逐款恒定，堆 1.1–2.0MB 无爬升）。dot-maze / pool-stars 首测监听器数抖动，**强制 GC 后 8 周期复测恒定 181/182 个监听器、堆 1.1–1.3MB**——判 0 泄漏。
- **粒子性能（360×640 dpr2 headless）**：merge-2048 静置 60.2fps / 方向键连发合并中 60.2fps；fruit-stack 静置 60.0 / 连续投果 10 颗中 60.1；pool-stars 静置 60.0 / 出杆滚动中 60.1。全部满帧。

---

## 九、新增视觉契约用例（本轮测试员唯一代码产出）

每款一个 `round1-visual-scan.test.ts`，共 **9 文件 / 38 例**，全部只锁「已达标性质」防回退（发现的问题写在本报告交 C 档，不写成红灯用例）：

| 文件 | 例数 | 锁什么 |
| --- | --- | --- |
| `merge-2048/round1-visual-scan.test.ts` | 5 | 块面 3 停渐变、星角标 5 档互异+描边+高光、彩虹档 ≥4 停、花/杯零 emoji |
| `mine-garden/round1-visual-scan.test.ts` | 4 | emoji 只在 `<title>`、盛开花四层体积、三档花与八图标互异且 ≥3 图元 |
| `sudoku-petal/round1-visual-scan.test.ts` | 4 | 花瓣三层灰度递增、花苞 6+ 图元、defs/id/url(# 零出现、shade/mix 单调 |
| `dot-maze/round1-visual-scan.test.ts` | 5 | 幽灵四色互异全淡彩（每通道 ≥120，不撞街机饱和色）、玩家 ≥3 停渐变+呆毛+高光眼、闪白帧、幽灵三态互异、顶部高光+瞳孔随向 |
| `fruit-stack/round1-visual-scan.test.ts` | 4 | 11 级果身 ≥3 停且三色分离、绘制序列零 emoji fillText、双脸互异、11 hi 色互异 |
| `pool-stars/round1-visual-scan.test.ts` | 4 | 暖冷灰度同档（Δ<12）故形状通道必须在、花印/星印序列不同、极小尺寸圆点 vs 菱形、四球三停分阶 |
| `junqi-camp/round1-visual-scan.test.ts` | 3 | 牌背无参逐字节一致且不含阵营色、军衔条 12 互异零 emoji、**双方图形去色后逐字节相同（专项③严重项的量化证据钉死给 fixer）** |
| `chess-garden/round1-visual-scan.test.ts` | 3 | 白黑灰度差 ≥80、12 张全有投影+描边+高光（缺渐变已另立案）、提示双形状 |
| `dark-chess/round1-visual-scan.test.ts` | 5 | 棋面四层体积、14 字互异、战力点=RANK、炮弧唯一、记牌 dim 双通道 |

---

## 十、问题总表与 9×6 结果矩阵

**统计：阻断 2（5-1 新增 / 5-2 存量）· 严重 1（3-1）· 一般 4（2-1、2-2、5-3、5-4）· 建议 2（1-1、6-1）。**
按验收标准：「阻断 0 残留」目前**不达标**——两条阻断连同严重清单移交本轮 C 档 fixer。

矩阵口径：❌ = 该格存在阻断或严重级问题；✅ = 通过（可能带一般/建议级登记，见各节）。

| 款 \ 专项 | ①火柴人 | ②金币体积 | ③双人区分 | ④精2D | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| merge-2048 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| mine-garden | ✅ | ✅ | ✅ | ✅ | **❌**(5-1 阻断) | ✅ |
| sudoku-petal | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| dot-maze | ✅ | ✅ | ✅ | ✅ | ✅ | ✅(6-1 建议) |
| fruit-stack | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| pool-stars | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| junqi-camp | ✅ | ✅ | **❌**(3-1 严重) | ✅ | ✅ | ✅ |
| chess-garden | ✅ | ✅(2-1 一般) | ✅ | ✅ | ✅(5-3 一般) | ✅ |
| dark-chess | ✅(1-1 建议) | ✅(2-2 一般) | ✅ | ✅ | **❌**(5-2 阻断·存量) | ✅ |

---

## 附：分级红线复核

9 款盘面与文案抽查：无血、无伤害、无死亡描写（dot-maze 被抓是「委屈脸 0.4s 重来」、junqi 对撞是「卡通烟云+小星星」、dark-chess 吃子是「花瓣退场」）；失败文案只鼓励；无广告无内购入口；商标见专项⑥。
