# 1.3 窗口 2 · 第 1 轮视觉验收 · 测试员报告

> 角色：窗口 2 第 1 轮视觉测试员（A 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。
> 范围：只验本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。不扫全库 39 款，不碰其他窗口。
> 权限：只写本报告 + 本窗 9 款目录下新增 `*.test.ts` 视觉扫描用例。不改绘制代码、不改玩法数值、不改 `src/art/kit/`、不改 `src/styles.css`。

## 扫描计划（动手前登记）

1. **全量跑**：`npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟；记录用例总数与构建体积。
2. **盘点**：9 款目录与 `src/art/kit/` 是否合入；plan 文档（step5-A/B/C、step6-A/B/C、step7-A/B/C）与实际提交对照；`git log` 抽查是否有人改动他窗目录。
3. **六大专项**（只扫本窗 9 款）：
   - ① 火柴人 / 草稿圆：grep `fillText(` emoji 直出、纯圆两点眼画法；
   - ② 金币体积：收集物 / 果子 / 球 / 棋子绘制须 ≥2 停渐变 + 高光 + 描边或落影之一；
   - ③ 双人可区分：fruit-stack 双盆、pool-stars 阵营、junqi/chess/dark-chess 双方，16px 灰度下形状+颜色双通道；
   - ④ 精 2D：禁止透视破坏可读性、禁止 three.js；外窗跑酷点名款标「范围外跳过」；
   - ⑤ 360px：360×640 与 320px 极限，溢出 / 遮挡 / 字号 <14px / 触区 <40px；
   - ⑥ 商标：4399 / 任天堂 / 迪士尼等商标、官方角色名、高相似造型。
4. **横向一致性**：光照约定（左上 45°）、`prefers-reduced-motion`、destroy 进退 ×5 泄漏、粒子性能（merge-2048 / fruit-stack / pool-stars）。
5. **产出**：本报告六专项各一节 + 问题清单（位置 / 现状 / 期望 / 严重度 / 建议责任文件）+ 「9 款 × 六专项」矩阵；新增视觉契约用例。

（以下各节将在扫描完成后填写。）
