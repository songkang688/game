# 1.3 窗口 5 · 第 2 轮视觉验收 · 测试员报告

> 角色:窗口 5 第 2 轮**视觉测试员**(方法对齐 1.3 第 28 步 A 档,范围收窄为本窗 10 款;不去验 76 款/20 款窗外抽样)。
> 工作分支 `game-1.3-window5`;输入(只读):`docs/qa/1.3-window5-round1-tester.md`、`round1-learner.md`、`round1-fixer.md`、`docs/plan-1.3-visual-bible.md`。
> 本窗 10 款:`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight` `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`。
> 测试员只写报告与补测试用例,不改绘制代码、不改判定 / 数值 / parentAuth / parentGate;发现的问题交本轮 C 档修。
> learner / fixer 报告另立文件,本文件一字不碰他人产出。

## 〇、复验计划(动手前立项)

1. **全量跑**:`npm ci` → `npm test` → `npm run build`,记录用例总数与第 1 轮合并态(fixer 宣称 `1638cde` / 740 文件 15365 例,报告定稿 `bc62fd9`)的差值,只增不减。
2. **逐条对账(本轮主责)**:round1-tester 的问题清单(阻断 Z1 / 严重 S1–S7 / 一般 G1–G11 / 建议 B1–B2)对 round1-fixer 修复清单(12 项 15 commit)逐条给结论:**已修复(附验证证据)/ 修复不彻底 / 修出回归 / 合理遗留**;遗留 L-1–L-5 确认还在遗留清单没丢。
3. **机器化用例复核**:第 1 轮 `visualScan13.test.ts` ×10 与 fixer `visualFix13.test.ts` ×9 全部还在跑、全绿,断言未被调低。
4. **深挖:10 款深度走查**(全部本窗 10 款,含点名双人 iff / puff-bros / pp 与点名 2.5D sky-squad / tank-battle / bumper-cars / bowling-lane):
   角色特写(2× 与 16px)/ 动效完整链(预告→过程→收尾 + reduced 退化)/ 场景层次(背景/中景/主体/HUD 四层)/ 一致性(kit 左上 45° 光照、描边规格、粉彩色板)/ 性能(CPU 4× 粒子高峰 ≥30fps,记数字)。
5. **回归防线**:判定热区被挪动 / 功能提示被动画吃掉 / 教育数据被改,三类逐款排查;`destroy` 泄漏 10 款复验。
6. **六大专项两轮达标率对比**:火柴人草稿圆 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px / 商标。
7. **产出**:本报告(六节)+ 本轮新增机器化复验用例(仅限本窗 10 款 `*.test.ts`);收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.3-window5`,禁 force、不动主线、不开 PR。

(以下各节按计划执行后填写。)
