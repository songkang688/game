# 1.3 窗口 6 · A 档 · 第 2 轮视觉测试员报告

- 角色:窗口 6 视觉验收三人组 第 2 轮 · A 测试员(只写报告与补测试,不改绘制代码)
- 范围:`brave-path` · `adventure-king` · `alien-seek` · `brick-break` · `mole-pop` · `box-hamster` · `balloon-pop` · `bubble-pop` · `bubble-aim`(共 9 款;step 原文「39 款 / 全库 / 20 款深度走查」对本窗口一律等于这 9 款)
- 输入:第 1 轮 A 档报告 `docs/qa/1.3-window6-round1-tester.md`(阻断 0 · 严重 5 · 一般 6 · 建议 2)+ C 档报告 `docs/qa/1.3-window6-round1-fixer.md`(修复 11 条 / 遗留 8 条,合并态 SHA `cbaff1e`)
- 状态:**复验计划已立项,执行中。**

## 〇、复验计划(动手前提交)

本轮定位:**复验 + 深挖**。两条主线:

1. **逐条对账**(第二节主责):第 1 轮 A 档 13 条问题(W6R1-01~13)+ C 档修复清单 11 条 + 遗留清单 8 条,逐条给结论——已修复(附验证证据)/ 修复不彻底 / 修出回归 / 合理遗留;第 1 轮沉淀的机器化扫描用例(qa1 emoji 上限断言、商标钉子、灰度 diffPct、fixer.r1 用例)确认全部还在跑、全绿。
2. **深度走查**(第三节):本窗口 **9 款全部**做五项深度走查(抽样规则=本窗口全覆盖,`balloon-pop` / `brave-path` 也在 step 原文必抽名单内,一并覆盖):
   - 角色特写:主角 2× 放大与 16px 缩小各看一次(渐变层次 / 描边一致性 / 剪影可辨);
   - 动效完整链:核心反馈「预告 → 过程 → 收尾」三段完整性;reduced 下退化是否合约定;
   - 场景层次:背景 / 中景 / 主体 / HUD 四层分明;装饰层饱和度与动效频率不喧宾夺主;
   - 一致性:kit 左上 45° 光照 / 描边规格 / 粉彩色板;菜单-局内-结算风格统一;
   - 性能:CPU 4× 节流下粒子高峰帧率 ≥30fps,记录数字。

回归防线(第五节):判定热区钉死断言全绿;危险提示 / 信号灯在 reduced 下仍在;笔顺 / 音高 / 题目文本快照断言全绿;第 1 轮记录过 destroy 数据的款全部复验(9 款全做)。

执行顺序:`npm ci` → `npm test` → `npm run build` 记全量数据(对照第 1 轮合并态 742 文件 / 15499 用例,只增不减)→ 逐条对账 → 构建产物 CDP 实测深挖 → 回归防线 → 补测试用例 → 报告 → fetch/rebase → 全绿 → push。

权限边界自检:本角色只写 `docs/qa/1.3-window6-round2-tester.md` 与 `*.test.ts`(只增不减),不改绘制代码,不动别人的报告与 kit 已有文件;新发现问题全部立项交本轮 C 档。

(以下各节随执行填充。)
