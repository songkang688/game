# 窗口 8 · 1.3 视觉升级 · 第 1 轮监督修复员报告

- 角色：窗口 8 三人验收组 · C 监督修复员（第 1 轮 / 共 3 轮）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围：本窗 12 款——
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` **新增独占文件**）；
  判定、数值、关卡数据、存档 key、玩法测试断言、`parentAuth.ts` / `parentGate.ts` 一律不碰；
  kit 已有 15 件只 import 不改；别人的报告只读不改。

## 修复计划（动手前提交）

输入：A 档报告 `docs/qa/1.3-window8-round1-tester.md`（阻断 0 / 严重 5 / 一般 3 / 建议 2）。
开工时 B 档报告 `1.3-window8-round1-learner.md` 尚未产出——先按任务书第四节做自查修复，B 档出了再合并跟进。

按铁律排序（阻断→严重→一般→建议），逐条一个 commit：

| 序 | 问题 | 修法 | 责任文件 |
| --- | --- | --- | --- |
| 1 | W8R1-05 严重：双跑者 16px 灰度可分辨像素 0%（目标 ≥15%） | `runnerSvg.ts` 是冻结 kit，不改；新增独占 kit 件给红蓝跑者叠「剪影级」装饰通道（红方双丸子头蝴蝶结 + 白条纹背心亮部，蓝方长帽舌 + 深色下摆暗部），race `index.ts` 帧装配处套用；用 sharp 复现测试员 16×16 灰度量化并写成用例钉死 ≥15% | 新增 `src/art/kit/runnerDuoTrim.ts`；`red-blue-race/index.ts` |
| 2 | W8R1-01 严重：math-farm 数一数题核心计数物裸 emoji | 题目数据一字不动；新增独占 kit 贴纸图集（自绘 SVG，工序沿 `crops.ts`：双色分面 + 2px 深描边 + 左上高光），`illustrate.ts` 渲染层识别「重复同一图物的数一数题面」→ 在既有 `.mtf-illus` 插图卡里摆同数量自绘贴纸并 CSS 隐藏 emoji 行（隐藏是纯视觉动作，`.qz-prompt` childList 不动，两个 MutationObserver 都不受扰） | 新增 `src/art/kit/stickers.ts`；`math-farm/illustrate.ts` `farmLayer.ts` `farmScene.ts` |
| 3 | W8R1-02 严重：word-garden 看图认字 / 认字选图 / 数一数配图裸 emoji | 字库数据一字不动；新增本款渲染层 `picArt.ts`：题面 emoji → 贴纸卡（挂 `.qz-prompt` 之后 + CSS 隐藏原 emoji 行，childList 不碰），选项按钮内 emoji span → 原位换贴纸（观察 `.qz-choices` childList，按钮几何与热区零改动） | 新增 `word-garden/picArt.ts`；`word-garden/runner.ts` |
| 4 | W8R1-03 严重：kitty-care 喂饭/逗猫核心道具裸 emoji（食物 / 饭碗 / 气泡想吃物 / 逗猫棒） | 沿 `cureScene.toolIconSvg` 工序新增本款独占图标模块：9 种食物 + 饭碗 + 逗猫羽毛棒自绘 SVG；`arena.ts` 渲染层替换（文案与热区零改动，判定读的是 `food.name` 不受影响） | 新增 `kitty-care/foodArt.ts`；`kitty-care/arena.ts` |
| 5 | W8R1-06 一般：word-garden 奖励花花瓣单色平涂 | `flower.ts` 是冻结 kit 不改；在本款消费端后处理：给花瓣叠 2 停径向渐变（瓣尖亮 / 瓣根深） | word-garden 花朵消费端 |
| 6 | W8R1-10 建议：red-blue-tug 12px 键盘提示副标签 | `.rbg-sub` 提至 14px（纯 CSS，热区不动） | `red-blue-tug/index.ts` |

挂账预案（修不动 / 超权限 / 非本窗，详见收尾的遗留清单）：

- W8R1-04（find-diff 盘面 emoji）：盘面 92 种 emoji、题目数据 SHA-256 被 `visual25.test.ts` 钉死；绘制层整套换图集属「自绘图集 + 数据版本化」专项量级，本轮挂账并写清路径。
- W8R1-07（clock-house 旧题面钟 9px 刻度）：`clockSVG` 产物进了 `promptHTML` 题目数据，挂账。
- W8R1-08（find-diff 26px 盘面格触区）：玩法热区，超视觉权限，挂账。
- W8R1-09（壳层 12px 装饰星）：壳层统一件非本窗 12 款，移交全局。

自查底线（第四节）：复核商标 grep / fillText grep / kit 纪律 `git log`；已由 A 档全量跑过的 360px 溢出与 reduced-motion 结论直接引用，收尾合并态复验时抽查确认。

每处修复配套用例只增不减；收尾做合并态复验（fetch → rebase → `npm ci` → `npm test` → `npm run build` → `vite preview` 冒烟）并把合并态 SHA、用例总数、构建体积写进本报告。

---

## 修复清单（每处一个 commit）

| 来源 | 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- |
| A 档 W8R1-05（严重）+ B 档 TOP1 | `red-blue-race/index.ts` 帧装配处 | `runnerSvg.ts` 冻结不改；新增独占 kit 件 `src/art/kit/runnerDuoTrim.ts` 给双跑者叠剪影级配件：红方双丸子头（亮部套装）＋明度带＋袜口环，蓝方反戴帽舌（暗部套装）；四姿态（跑 A/B、跳、滑）全套用。16px 灰度可分辨像素 0% → **17.6%～22.7%**（sharp 复现 A 档量化口径，阈值 24/255） | `red-blue-race/duoTrim.test.ts`（9 例：sharp 量化钉 ≥15%、装饰层结构通道、姿态偏移不漂移） | `1475a8b` |
| W8R1-01/02/03 公共底座 | 新增独占 kit 件 `src/art/kit/stickers.ts` | 106 张自绘 SVG 教具贴纸，工序沿 `crops.ts`（双色分面 + 2px 深描边 + 左上高光 + 软投影，无 defs/id 同页多铺不撞）；覆盖 math-farm 数一数 18 种、word-garden 字卡配图与数一数小物 101 种、kitty-care 喂饭逗猫道具 12 种；VS16 归一 | `src/art/kit/stickers.test.ts`（8 例：三款对账覆盖率、工序、纯函数、`data-sticker` 放中文名不放 emoji 字符） | `1aa240e` |
| A 档 W8R1-01（严重）+ B 档 TOP2 | `math-farm/illustrate.ts` `farmLayer.ts` `farmScene.ts` | 题目数据一字不动：绘制层 `countPlan()` 只读题面解析「同一 emoji 重复 n 次」，贴纸行顶上当题卡（白底木牌描边与换肤题卡同装），原 emoji 行收进 sr-only（读屏念的原样；只改 class 属性，两个 childList 观察器无感）；窄屏贴纸 34→26px | `math-farm/countArt.test.ts`（9 例：解析、1–34 关全量扫无漏网且 n=答案、渲染 0 裸 emoji、端到端挂/摘、destroy 归零） | `a3277f5` |
| A 档 W8R1-02（严重）+ B 档 TOP3 | 新增 `word-garden/picArt.ts`；`word-garden/runner.ts` 两轮接入 | 字库与题目数据零改动：题卡收 sr-only + 镜像贴纸卡（题卡的兄弟节点，childList 不碰）；选项按钮换「sr-only 原文 + aria-hidden 贴纸」里子（监听在按钮自身，热区零改动）；看图认字 56→58px、数一数行 26→30px、混排 44px、选项 34→38px；拼音/句子等无图题面原样放行；镜像换装后喊 `fit.relayout` | `word-garden/picArt.test.ts`（10 例：解析分档、**188 关题面+选项全量扫无漏网**、渲染 0 可见裸 emoji、端到端、wgd- 前缀纪律） | `c090511` |
| A 档 W8R1-03（严重）+ B 档 TOP4 | `kitty-care/arena.ts` `styles.ts` | 沿看病 `toolIconSvg` 工序（空标签按钮 + 图标 span + 小字 + `order:-1`）：九种食物、饭碗、逗猫棒、爪印全换贴纸；想吃气泡原 emoji 收 sr-only（朗读一字不差）；贴纸一律纯装饰 aria-hidden，不给装饰件挂标签和真按钮撞车；文案/热区/判定零改动 | `kitty-care/propArt.test.ts`（4 例：propIcon 两态、喂饭端到端「点想吃的→点碗照样完成」、逗猫端到端、道具图集对账） | `4467ad4` |
| A 档 W8R1-06（一般）+ B 档 TOP6 | 新增 `word-garden/flowerShade.ts`；`tracing.ts` 两个调用点 | `flower.ts` 冻结不改：消费端给 `<g>` 片段塞 2 停径向渐变（userSpaceOnUse 花心系，瓣根 −16 深 → 瓣尖 +14 亮），五瓣平涂 fill 全换渐变引用；花心/三点蕊/路径几何/帧序字节级不动 | `word-garden/flowerShade.test.ts`（5 例：2 停方向、字节级不动、三色×五帧全组合、调用点扫描） | `b70dad7` |
| A 档 W8R1-10（建议）+ B 档 TOP8 | `red-blue-tug/index.ts` | `.rbg-sub` 基础 13→14px、`.rbg-pull .rbg-sub` 键帽 12→14px；按钮盒子/热区/三处「按住 K」文本零改动 | `red-blue-tug/subLabel.test.ts`（2 例：≥14px、尺寸规则与文本原样） | `e6e36f1` |

修复统计：**阻断 0 条（A 档本就 0）；严重修 4 条（W8R1-01/02/03/05）；一般修 1 条（W8R1-06）；建议修 1 条（W8R1-10）**；另有公共底座 1 件（stickers 图集）。

## 遗留清单（修不动 / 超权限 / 移交）

| 条目 | 级别 | 理由与去向 |
| --- | --- | --- |
| W8R1-04 find-diff 盘面 emoji 格 | 严重 | 盘面 92 种 emoji，题目数据 SHA-256 被 `visual25.test.ts` 钉死，绘制层整套换图需「自绘图集 + 数据版本化」专项。采纳 B 档 TOP5 裁决：第 2 轮 C 档出设计稿（映射表放渲染层、题库不动、SHA 用例改钉映射表），第 3 轮实施 + 全量回归。本轮 stickers.ts 底座已备好工序参照 |
| W8R1-07 clock-house 前 99 关素钟面 | 一般 | `clockSVG` 产物进了 `promptHTML` 题目数据；渲染层替换需与题面 MutationObserver 协调，成本中。移交第 2 轮（B 档 TOP7 已给规格） |
| W8R1-08 find-diff 360px 盘面格触区 26px | 一般 | 玩法热区改动超视觉权限（热区回归用例钉死），挂账请裁决 |
| W8R1-09 壳层 12px 装饰星 / 顶栏 | 建议 | 壳层统一件非本窗 12 款，移交全局 |
| B 档 TOP9 red-blue-tap 闯关场地氛围底、TOP10 pinyin-train 车票边条 | 建议（B 档新发现） | 建议级且非 A 档条目，第 1 轮预算用在严重项清零上；移交第 2 轮（B 档已给规格） |

## 合并态复验（第六节）

- `git fetch origin game-1.3-window8` → 远端头 `1aa240e`（无他人新提交）→ 本地直接快进推送，无需 rebase；三份报告（tester / learner / fixer）与全部修复确认在位。
- `npm ci` → `npm test`：**736 个测试文件 / 15507 条用例全绿**（A 档过检水位 728/15432 → B 档合并态 730/15464 → 本档 736/15507，**用例只增不减**，其中本档修复期净增 6 文件 / 43 例）。
- `npm run build` 全绿：PWA precache **192 项 / 5385.52 KiB**（dist 共 5.7M）。
- `npx vite preview` 冒烟：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**。
- 合并态复验基于 SHA **`e6e36f1`**（最后一个修复 commit；本报告 commit 落其上后普通 push `origin game-1.3-window8`，未 force）。

## 自查与 kit 纪律（第四节）

- **商标扫描**：`window8-round1-visual-scan.test.ts` 专项⑥（20 个品牌词 × 12 款实现代码）持续全绿；本档新增文件（stickers / runnerDuoTrim / picArt / flowerShade 等）全部入扫描范围，0 命中。
- **裸 emoji fillText 直出**：专项① 12 款 0 处，持续全绿；本轮再消掉四款 DOM 层裸 emoji 核心道具（见修复清单）。
- **360px / reduced-motion**：本轮全部改动为绘制层替换与字号提升，无新几何、无新动画；A 档 360/320 全量结论与 reduced 抽查结论未被触动（贴纸为静态 SVG；数一数/配图窄屏另配 26–34px 缩档）。
- **kit 纪律（git log 复核）**：15 件只读 kit（`palette` `jellyBtn` `sparkle` `tugTeam` `runnerSvg` `crops` `woodSign` `train` `flower` `brush` `frame` `gem` `paintBlob` `kittySvg` `glowStar`）自创建后**零后续改动**，无人越权；本档新增独占件 2 个（`runnerDuoTrim.ts` `stickers.ts`），`stickers.ts` 的两次 commit 均为本档自己（创建 + `data-sticker` 属性从 emoji 字符改中文名）；`music-stars/starTheme.ts` 本轮无人动。结论：**无冲突改动，无需恢复，无记名**。

## 第 1 轮视觉验收结论

- **阻断：0 残留**（A 档本就 0，本轮商标/溢出扫描持续全绿）。
- **严重：5 条修 4 条**，残留 1 条（W8R1-04 find-diff 盘面，属「图集 + 数据版本化」专项，已按 B 档裁决排入第 2/3 轮路径）。
- 一般 3 条修 1 条（W8R1-06），挂账 2 条（W8R1-07 移交第 2 轮、W8R1-08 超权限请裁决）；建议 2 条修 1 条（W8R1-10），移交 1 条（W8R1-09 壳层）＋ B 档新发现 2 条（TOP9/10 移交第 2 轮）。
- 合并态 `npm test`（736 文件 / 15507 例）、`npm run build`、`vite preview` 冒烟全绿；用例只增不减。
