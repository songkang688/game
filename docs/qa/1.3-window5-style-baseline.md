# 1.3 窗口 5 视觉风格基线(三轮验收沉淀 · 传家宝)

> 用途:窗口 5 十款(`shoot-range` `sky-squad` `tank-battle` `bomb-buddies` `snow-fight`
> `bumper-cars` `bowling-lane` `ice-fire-forest` `puff-bros` `prince-princess`)后续维护的视觉规范。
> 来源:1.3 视觉升级三轮验收(A 档扫描量化 / B 档学习基准 / C 档修复落地)沉淀的结论,
> **每条规范注明出处(文件 + 函数或常量名),全部可在本窗 10 款(含本窗各步创建的 `src/art/kit/` 模块)溯源**;
> 窗外款不在本文口径内。编写:窗口 5 第 3 轮学习优化员(B 档);终态基准 = round3 合并态。
> 纪律重申:`src/art/kit/` 已有文件只 import 不改;判定 / 数值 / parentAuth / parentGate 永不因视觉改动;
> ratchet 水位只降不升;不提交位图。

## 一、粉彩色板 token 表

| # | 规范 | 出处 |
| --- | --- | --- |
| 1-1 | **全库粉彩六色起点 token**:pink `#F4859F` / blue `#7FB2F0` / mint `#7FE7C4` / lemon `#FFE8A3` / lilac `#C9A8F0` / peach `#FFB36B`;还没有自己配色板的画笔从这六色取 | `src/art/kit/palette.ts` `PASTELS`(14B 步创建) |
| 1-2 | **取色唯一入口**:一切加深 / 减淡走 `shade(color, ±n)`(正朝白负朝黑,±100 夹紧),透明度走 `withAlpha`;「各游戏不许自己手写加深减淡的十六进制」是写在源码注释里的家规 | `src/art/kit/palette.ts` `shade` / `withAlpha` |
| 1-3 | **各款自有色板必须 token 化**(常量表 + 测试对表,不许散落魔法色值):范例 bowling-lane `BL_COLORS`(含 `blNeonPink #FF9FBE` / `blNeonBlue #9FD0FF`)、snow-fight `SNOWFOE_BODY_STOPS ["#FFFFFF","#F6FAFF","#DFE8F2"]` / `SEAT_STAR_GOLD #FFD66E` / `WARM_FLAME_STOPS`、bomb-buddies `BB_CHIBI_OUTFIT ["#F79BB2","#5E97DE"]` | `bowling-lane/visual13.ts` `BL_COLORS`;`snow-fight/visual13.ts:200` 与 `paint13.ts`;`bomb-buddies/visual13.ts:668` |
| 1-4 | **暗场景也要姓「粉」**:大面积低明度底色要往粉彩家族拉色温——夜场馆暗底 `#3b3556` 上压 6% 白提亮层 + 4% 粉紫 tint `#E3A9E0`,「灰紫」调成「粉紫」,亮度预算仍留给主体(球道与灯箱) | `bowling-lane/visual13.ts` `BL_HALL_TINT` / `BL_HALL_LIFT_ALPHA` / `BL_HALL_TINT_ALPHA`(R3 C-2);调用序在 `index.ts` `render` 开头 |
| 1-5 | **半场色温分区用低浓度 tint**:冷 `rgba(159,216,245,.10)` / 暖 `rgba(245,130,78,.08)`,一成透明度以内就能讲清「这半边是谁的地盘」,不动任何底下图层 | `ice-fire-forest/visual13.ts:55` `IFF_TINT_COLD` / `IFF_TINT_WARM` |
| 1-6 | **首页卡片同带**:本窗 10 款 `meta.color` 全部落在 `#DBE—#FFE` 浅粉彩带(如 sr `#ffe0ec`、tb `#dfeed4`、pp `#FFE3EF`),新款卡片色不出这条带 | 各款 `meta.ts` `color` 字段 |
| 1-7 | **危险语义色独立于粉彩**:预警用泛红呼吸与深红条纹底座,不挪用粉彩六色,保证「要小心的东西」永远第一眼可辨 | `bomb-buddies/visual13.ts:147` `dangerGlowAlpha`;`prince-princess/index.ts:417` `drawHazardMark`(粉红小三角恒画最顶层) |

## 二、描边宽度档位

| # | 规范 | 出处 |
| --- | --- | --- |
| 2-1 | **档位:1.5–2px,取色 = 主色加深 20%**。「细过 1.5 看不见,粗过 2 抢戏」;`strokeOutline` 自动把线宽夹在 `OUTLINE_MIN 1.5`–`OUTLINE_MAX 2`、圆角连接 | `src/art/kit/outline.ts` `strokeOutline` / `OUTLINE_MIN` / `OUTLINE_MAX` / `OUTLINE_DARKEN` |
| 2-2 | **普通实体 1.5px,重规格件顶格 2px**:sky-squad 拾取徽章 `strokeOutline` 2px;prince-princess BOSS 描边 2px 深 26%(BOSS 级允许比家规再深一档) | `sky-squad/index.ts` shots 层拾取绘制;`prince-princess/visual13.ts` `drawBossFigure` |
| 2-3 | **不直接 import 的款要走等价实现,深度对齐 `OUTLINE_DARKEN`**:bumper-cars / bowling-lane 自绘处 `shade(color, OUTLINE_DARKEN)`;tank-battle 徽章 R3 起也归队 kit `strokeOutline`(全款 0 处的工艺代差清零) | `bumper-cars/visual13.ts:17`、`bowling-lane/visual13.ts:18` import `OUTLINE_DARKEN`;`tank-battle/visual13.ts:322/373`(星 / 闪电徽章,R3 C-3) |

## 三、左上 45° 光照约定

| # | 规范 | 出处 |
| --- | --- | --- |
| 3-1 | **径向三停标准件**:高光圆心偏 `(-0.35r, -0.35r)`,停点 顶光 +25% → 0.55 主体 → 背光 −15%(`BALL_STOPS`);「让纯色圆一秒变球」 | `src/art/kit/volume.ts` `ballGradient` / `BALL_STOPS` |
| 3-2 | **高光偏移带宽 −0.3r 至 −0.42r,方向永远左上**:bomb-buddies 泡泡王 `(-0.38r, -0.42r)`、puff-bros 糖果高光 `-0.3r`、snow-fight 雪怪 `-0.35r` 都在带内;右下高光 = 反例,三轮零出现 | `bomb-buddies/visual13.ts:595` `drawBossKing`;`puff-bros/visual13.ts:722` `candyGloss`;`snow-fight/paint13.ts` `paintSnowFoe` |
| 3-3 | **线性渐变必须拉斜,禁纵向**:起点 `(cx − 0.18w, top)`、终点 `(cx + 0.18w, bottom)`,停点幅度 +16/−10 属家族带宽;pp 全款「零纵向线性渐变」有正则防线用例钉死(R2 C-1 教训:纵向 = 款内两套光照语言) | `prince-princess/visual13.ts:413` `enemyBodyGrad`(±0.18w 同规覆盖盾面 / 王冠 / 徽章金渐变);防线在 `prince-princess/visualFixR2.test.ts` |
| 3-4 | **2.5D 侧面固定深 −22**:顶面 = 主色、右与下侧面 = `shade(顶面, -22)`,厚度 `SIDE_RATIO 0.18 × 块宽`;tank-battle / bomb-buddies / bumper-cars 三款顶视块同源 | `src/art/kit/block25d.ts:17/19` `SIDE_RATIO` / `SIDE_SHADE` |
| 3-5 | **静态高光弧也走家族语言**:圆顶受光面用 `shade(顶色, +18)` 2px 圆头弧画在左上弧段(π0.9–π1.45),不用裸白细线 | `tank-battle/visual13.ts:105` `drawTurretSheen`(R3 C-3,弧段与 1px 白线时代同位) |

## 四、动效三段式与 reduced 退化规则

### 4.1 三段式(预告 → 过程 → 收尾),每段各有画笔

| # | 范例链 | 出处 |
| --- | --- | --- |
| 4-1 | bomb-buddies:脚下泛红呼吸(预告)→ 爆风十字(过程)→ 两阶裂纹 + 砖屑(收尾) | `bomb-buddies/visual13.ts:147` `dangerGlowAlpha` → `index.ts` 爆风层 → `visual13.ts:161` `crackStage` |
| 4-2 | snow-fight:蓄力读数(预告,恒走)→ 弧线 + 落点圈(过程)→ 8 粒溅雪(收尾) | `snow-fight/visual13.ts:123` `chargeReadout` → `index.ts` 弹道层 → `paint13.ts` `paintFeedbackPuff` |
| 4-3 | tank-battle:前摇管口预亮(预告)→ 弹道(过程)→ 炮口十字闪 + 爆星(收尾,功能反馈 reduced 留 1 帧) | `tank-battle/index.ts:583` windup 分支 → `drawMuzzleFlash` 调用(`index.ts:592`) |

### 4.2 节奏两档(快慢各自成组,跨款一致)

| # | 规范 | 出处 |
| --- | --- | --- |
| 4-4 | **两帧摆动类 200–300ms**:履带 200 / 围巾与旗 240 / 蝙蝠翼 300 | `tank-battle/visual13.ts:60` `TRACK_STEP_MS`;`snow-fight/visual13.ts:70/78` `SCARF_SWING_MS` / `FLAG_WAVE_MS`;`prince-princess/visual13.ts` bat 翼两帧(`drawEnemy`) |
| 4-5 | **呼吸 / 旋转类 900–2400ms**:灯串 900 / 集合旗 900 / 呼吸圈 1200 / 重生环 1200 / 水波 1600 / 宝石闪点 1800 / 霓虹 2400 / 法珠公转 2400 | `bumper-cars/visual13.ts:60` `LAMP_MS`;`ice-fire-forest/visual13.ts:96` `FLAG_WAVE_MS`;`shoot-range/paint13.ts:762` 呼吸外圈;`tank-battle/visual13.ts:66/62` `REBUILD_RING_MS` / `WATER_WAVE_MS`;`ice-fire-forest/visual13.ts:91` `GEM_SPIN_MS`;`bowling-lane/visual13.ts:62` `NEON_MS`;`prince-princess/visual13.ts:389` `ORB_SPIN_MS` |
| 4-6 | **出场仪式 400ms**:BOSS 缩放弹入 0.7→1 带一点回弹;星尘轨迹同档 400ms | `prince-princess/visual13.ts:579/583` `BOSS_INTRO_MS` / `bossIntroScale`;`visual13.ts:87` `STARDUST_MS` |

### 4.3 reduced(`prefers-reduced-motion`)退化四规则

| # | 规则 | 出处 |
| --- | --- | --- |
| 4-7 | **功能提示不退化**:离场闪烁 reduced 保留、危险三角恒画最顶层、蓄力读数恒走——「信息不许被无障碍设置吃掉」 | `shoot-range/visual13.ts:131` `LEAVE_FLASH_HZ`(paint13.ts:676 消费);`prince-princess/index.ts:417` `drawHazardMark`;`snow-fight/visual13.ts:123` `chargeReadout` |
| 4-8 | **位移动画 → 定格 / 停浮,反馈不消失**:溅雪 reduced 单帧原地淡出(粒子圆心不动,有用例钉死);眩晕双星 reduced 相位恒 0;水波 reduced 冻结第 0 帧;拾取浮动 reduced 停浮 | `snow-fight/paint13.ts` `paintFeedbackPuff`(reduced 分支);`puff-bros/visual13.ts:848` `dizzyPhase`;`tank-battle/visual13.ts:108` 水波;`sky-squad/index.ts` 拾取层 |
| 4-9 | **呼吸 / 闪烁 → 常亮**:霓虹 reduced 返回 1、灯串 reduced 全亮、全中灯箱 reduced 一次长亮 | `bowling-lane/visual13.ts:136` `neonAlpha`;`bumper-cars/visual13.ts:101` 灯串;`bowling-lane/visual13.ts` 灯箱闪(`NEON_MS` 同文件时序表) |
| 4-10 | **出场 / 彩纸 → 淡入 / 不生成**:BOSS reduced 直接淡入(scale 恒 1,淡入交调用侧 alpha);全中彩纸 reduced 不生成;宝石闪点 reduced 不生成、静止高光另画;星尘 reduced 不生成 | `prince-princess/visual13.ts:583` `bossIntroScale`;`bowling-lane/index.ts` 彩纸层;`ice-fire-forest/visual13.ts:282` 闪点;`prince-princess/visual13.ts:85` 星尘注释 |

### 4.4 粒子与通道预算(停手线)

| # | 规范 | 出处 |
| --- | --- | --- |
| 4-11 | **单次反馈 6–10 粒、两停配色、单一运动通道**(实证取 8) | `snow-fight/paint13.ts:864` `SNF_PUFF_DOTS = 8` / `SNF_PUFF_COLORS` 两停 |
| 4-12 | **全局粒子池 40–42 封顶,超了 shift 最旧**(fps@4× 全 10 款 ≥52.5 反证水位安全) | `puff-bros/index.ts` `particles.length > 40` 分支;`prince-princess/index.ts:1021` `> 42` 分支 |
| 4-13 | **动效通道 ≤7 / 款;再加视觉只许加零动效静态层**(奖品架 / 邻道剪影 / 平台亮边三件静态装饰 = 「加密度不加通道」范本) | `shoot-range/paint13.ts:148` `drawPrizeRack`;`bowling-lane/index.ts` `drawNeighborLanes` 调用;`puff-bros/index.ts` 平台顶条亮边(`shade(deck,+18)`) |

## 五、金币 / 收集物三件套规格

| # | 规范 | 出处 |
| --- | --- | --- |
| 5-1 | **三件套定义:≥2 停渐变(体积)+ 高光(受光)+ 描边或落影(轮廓)**——A 档专项②的判定口径,缺一即「平涂」;平涂圆内贴字符是负面清单(S7 教训) | 判定口径见 `docs/qa/1.3-window5-round1-tester.md` 第三节;下列全部为达标范本 |
| 5-2 | 金星:三停渐变 + 描边,「自绘金星,不再用字形 ★」 | `shoot-range/paint13.ts:831` 发射台金星(经 `carnivalBallGradient`,kit `carnival`) |
| 5-3 | 球体:kit 三停 + 三指孔 + 镜面倒影(暗场地面的高光语言) | `bowling-lane/index.ts` 球绘制(kit `volume.ballGradient` + kit `mirror.mirrorEllipse`) |
| 5-4 | 宝石:切面亮 / 暗三角 + 白点高光 + 描边 + 旋转闪点(1800ms);pp 变体加 2000ms 呼吸光圈 | `ice-fire-forest/index.ts:1223` 宝石段;`prince-princess/index.ts:511` `drawRewardGem` |
| 5-5 | 雪球:三停渐变 + 底部冷影 + 两道滚纹(「白色物体也要有三阶」) | `snow-fight/paint13.ts:534` `paintSnowball` |
| 5-6 | 糖果四型:统一左上 `-0.3r` 白高光 + 1.5px 深 20% 描边;**防混淆件**(纸角 / 木棍 / 竹签)把收集物剪影与泡泡(正圆)拉开 | `puff-bros/visual13.ts:735/722` `drawCandy` / `candyGloss` |
| 5-7 | **弹雨里的拾取物加「专属色圈」信息通道**:七型七色圈互不重复 + 矢量符号,「先认色圈、再认图案」双通道;信息性颜色不算装饰预算 | `sky-squad/art.ts:442/468` `PICKUP_BADGE` / `pickupArt` |

## 六、双人剪影差异化手法

| # | 规范 | 出处 |
| --- | --- | --- |
| 6-1 | **三层判定法:剪影(alpha 掩码)→ 头饰(顶部 6 行)→ 灰度(交集平均灰差),至少两通道明显可分**(掩码差 ≥5% 或灰差 ≥15/255)——A 档三轮量化口径,后续改双人造型必须复测 | 口径见 `docs/qa/1.3-window5-round1-tester.md` 第四节;各款读数记录在 round1/2 tester 报告 |
| 6-2 | **首选手法:相反头形 / 身形母形**(全场最佳 13.8%/31.3%/45.4):水滴身形 + 雪晶发饰 vs 火苗身形(尖端右偏 0.10)+ 火簇 | `ice-fire-forest/visual13.ts:323` `drawHeroFigure` |
| 6-3 | **帽形定人**(头饰通道可到 100%):圆绒帽 vs 尖睡帽;主色亮度接近时用 `HAT_BODY_SHADE [-6, -18]` 把帽体灰度差拉到 ≥15/255(色弱可辨底线,有用例常绿) | `snow-fight/paint13.ts:183` `paintFighterBody` + `paintHat`(`visual13.ts:191` `HAT_BODY_SHADE`) |
| 6-4 | **chibi 骨架 + 双 spec**:裙装 + 花发卡 vs 裤装 + 星呆毛;服装色 `BB_CHIBI_OUTFIT` 粉亮 / 蓝深灰度差近翻倍是 R1 B1 的修复经验——**配件相同骨架的双人款,灰度就是第三保险** | `bomb-buddies/index.ts:140` `CHIBI_SPECS`(kit `chibi.drawChibi` 消费);`visual13.ts:668` `BB_CHIBI_OUTFIT` |
| 6-5 | **耳形 / 头顶件分兄弟**:呆毛 vs 圆帽 + 围兜,双熊同骨架靠顶部件分 | `puff-bros/visual13.ts:300/522` `broBody` / `paintBro` |
| 6-6 | **冠齿数 + 裙形 / 裤形**:三齿大皇冠 + 双裤腿凹口 vs 0.45 倍小冠 + 钟形裙;识别件几何沉为可复用路径(指路徽章 / 头像直接复用,不新造小人——也是商标安全手法) | `prince-princess/index.ts:1049` `drawHero`;`visual13.ts:202/239` `crownPath` / `bowShape`(`visual13.ts:1031` `drawRoyalBadge` 复用) |

## 七、2.5D 纵深三法

| # | 做法 | 规格 | 出处 |
| --- | --- | --- | --- |
| 7-1 | **地面透视(会聚投影)** | 一套投影函数管全场:近宽远窄、近大远小(`k = widthScaleAt(t)`),木板缝 / 邻道剪影 / 瓶球缩放全部走同一投影;布景压在跟球运镜之前**不进缩放**(调用序有用例钉死) | `bowling-lane/logic.ts:726/718` `laneProject` / `widthScaleAt`;调用序防线 `bowling-lane/visualFix13.test.ts` |
| 7-2 | **多层视差滚动** | 倍率带宽 0.15–0.9:远景 ≤0.2、中景 0.3–0.5、近景 0.8–0.9;层数 2–3 层足够,通用滚动器走 kit | kit `src/art/kit/parallax.ts:21` `makeParallax`;`sky-squad/art.ts:60` `CLOUD_PARALLAX {0.2/0.5/0.9}` 三档;`puff-bros/visual13.ts:82` `CLOUD_FACTORS [0.15, 0.3]`;`snow-fight/paint13.ts:58` `paintPineRow`(远山 + 松树两层);`prince-princess/index.ts:1445` 城堡塔楼两层 + 灌木 |
| 7-3 | **落影缩放(影子讲高度)** | 柔和椭圆落影 `softShadow(…, scale)`,「悬空物越低影子越大」;纵版飞行按 y 算影子比例 `shadowScaleAt(y)`;横版全场统一落影椭圆(宽 0.75× 角色宽、高 0.2× 宽) | kit `src/art/kit/volume.ts:36` `softShadow`;`sky-squad/art.ts:115` `shadowScaleAt`(`index.ts:1958` 消费);`prince-princess/visual13.ts:48-55` `ppShadow` 落影规格 |
| 7-4 | (附注)**顶视棋盘方言:顶侧双面块** | 顶面主色 + 右下侧面 `shade(-22)`、厚度 0.18 块宽——顶视款(tank / bb / bc)的 2.5D 不走透视走双面块,属方言不必消灭,但主体要带家族光照(见 3-5) | kit `src/art/kit/block25d.ts` `block25d`(`SIDE_RATIO` / `SIDE_SHADE`) |

## 八、维护守则(机器化防线资产清单)

| # | 守则 | 出处 |
| --- | --- | --- |
| 8-1 | **ratchet 只降不升**:各款绘制文件 emoji 码点与画布 fillText 水位有整表终态闸(sr52/ss43/tb49/bb64/snf53/bc43/bl47/iff28/pb72/pp38;fillText sr5/bb3/pp3/iff2/snf1/其余 0),动绘制前先看闸 | `src/games/visualGuardW5R3.test.ts`(整表);各款 `visualScan13.test.ts` |
| 8-2 | **防线文件在位钉死,谁删谁红**:`visualScan13` ×10、`visualFix13` ×9、`visualFixR2` ×3、`visualScanR2`、`copy13`、`copyW5R2` + R3 新增 `visualFixR3` ×2(bl/tb) | `src/games/visualGuardW5R3.test.ts` 第①例;R3 新增见 `bowling-lane/visualFixR3.test.ts`、`tank-battle/visualFixR3.test.ts` |
| 8-3 | **谁建画笔谁带单测;kit 已有文件只 import 不改**;单款消费的小画笔留在各款 `visual13.ts` / `paint13.ts`,不到「多款重复轮子」不上收 kit | 三轮 fixer 纪律(round1/2/3-fixer 报告 kit 纪律节);kit 归属表见 `docs/qa/1.3-window5-round1-tester.md` 1.2 节 |
| 8-4 | **功能字与贴纸字的分界**:「删掉它孩子会不会失去信息」——泡泡倒数 / 层数圆牌数字 / 风旗 / 记分是功能字(保留),装饰性字形一律矢量化(三轮已清零) | 范例:`bomb-buddies` 层数圆牌数字(`drawBossKing` 圆牌)、`snow-fight` 风旗(全款唯一 fillText) |

> 条目合计:**49 条**(一 7 / 二 3 / 三 5 / 四 13 / 五 7 / 六 6 / 七 4 / 八 4)。
> 本文只收本窗 10 款可溯源条目;全库推广(如 reduced「反馈不消失」家规、粒子池封顶)由主管在全库轮次裁决。
