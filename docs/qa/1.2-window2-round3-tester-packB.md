# 1.2 窗口 2 · 第 3 轮验收 · 测试员 包 B（本窗口收官）

- 窗口：2
- 角色：QA 第 3 轮 · 测试员 · 包 B
- 模型 slug：`claude-opus-5-thinking-high-fast`
- 范围（只验这 5 款，别人的游戏一个字节都不碰）：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 基线：`origin/game-1.2-window2` @ `d4decfc`
- 输入（本轮开工前逐份读完）：
  - `docs/qa/1.2-window2-round2-tester-packB.md`（阻断 0 · 严重 3 · 一般 8）
  - `docs/qa/1.2-window2-round2-learner-packB.md`（落地 P2B-1 ~ P2B-6，记录 4 条）
  - `docs/qa/1.2-window2-round2-fixer-packB.md`（R2B-1 / R2B-2 / R2B-3 声称清零，`f3a351a` 把格斗塔默认出战改成星星）
- 本轮定位：**收官轮**。只写报告与临时走查脚本，`src/games/**` 一个字节都不改，走查脚本跑完即删。

---

## 一、走查计划（动笔前先立此计划）

### 1. 换样本：本轮抽第 1 / 第 76 / 第 188 关

第 1 轮抽的是「第 1 / 100 / 188 关」，第 2 轮换成 84 / 88 / 92 / 96 那一档。
本轮**这些号一个都不许再用**，统一抽 **第 1 关 / 第 76 关 / 第 188 关**：

| 游戏 | 第 1 轮中间关 | 第 2 轮中间关 | 本轮中间关 |
| --- | --- | --- | --- |
| `dark-chess` | 100 | 84 | **76** |
| `hue-hand` | 100 | 88 | **76** |
| `hop-pads` | 100 | 92 | **76** |
| `tap-tiles` | 100 | 96 | **76** |
| `fight-king` | 100（塔） | 92（塔） | **76（塔）** |

`fight-king` 的战役就是格斗塔，关号口径与另外四款一致（`meta.levels = 188`），
所以不需要另换号，76 本身已经与第 2 轮的 92 不同。第 76 关落在第 5 章前后，
与前两轮抽过的第 5/6 章尾巴（84 / 88 / 92 / 96）和第 7 章（100）都不是同一段。

三关都要真打到结算，不许只读关表。

### 2. 每款必做的 7 条

| 序号 | 铁则 | 取证方式 |
| --- | --- | --- |
| 1 | 首页 / 深链进得去 | 真走 `src/main.ts` 的路由：`#/g/<id>` 深链 + 首页卡片点击两条路各进一次 |
| 2 | 真赢一次、真输一次 | headless `domStub` 装 360px，跑到 `onWin` / `onLose`，抄结算原文 |
| 3 | 第 1 / 76 / 188 关 | 三关各打到结算，记标题、HUD、星数 |
| 4 | 对战 / 无尽 / 双人到结算 | 每种 `mode` 各跑一局，抄结算原文 |
| 5 | 360px 热区 | 全量扒 CSS 的 `min-width` / `min-height` / `font-size`，逐条量到 ≥ 44px |
| 6 | `destroy` 泄漏 | 进 → 玩 → 退出 → 再进，连续 20 轮，数 window 监听 / rAF / timer / `root.children` 是否归零 |
| 7 | 商标 0 命中 | 去掉各款自带的黑名单守卫用例后，全量扫五个目录的可见串 |

### 3. 必须独立复验的第 2 轮三条严重（防回潮）

| 编号 | 游戏 | 第 2 轮症状 | 本轮复验点 |
| --- | --- | --- | --- |
| R2B-1 | `dark-chess` | 双人同屏朵朵的 `w/a/s/d` `f` `g` 无条件生效，星星回合能替对方落子 | 两人各管各的光标；星星回合朵朵按什么都落不了子；单人局别名键不能丢 |
| R2B-2 | `hop-pads` | 幽灵「起跳 16 次」对玩家「站满 16 座」，两把尺不一样；高手与地狱逐局同分 | 同一把「16」尺；`expert` 与 `hell` 必须分得开 |
| R2B-3 | `fight-king` | 默认出战的朵朵后段整片 0/8 | **测试员原文要的是「默认朵朵后段不能 0/8」**，修复员 `f3a351a` 改的是默认角色。本轮量清三件事：① 默认出战到底是谁；② 朵朵后段还是不是墙；③ 换人是否仍立刻能打。若只换了默认角色而朵朵仍 0/8，记一条给收官结论，**不自己改代码** |

### 4. 第 1 轮两条严重也要再确认无回潮

- **B-2** `tap-tiles` 同谱对战打平必须判平局，两边 `wins` 都不涨。
- **B-6** `hop-pads` 双人同屏按手指号分台，一个人抬手只能弹一台。

### 5. 一般遗留逐条给最终结论

`R2B-5`（`hop-pads` WASD / 方向键无语义）、`R2B-8`（`fight-king` 训练场进不了 `meta.modes`，平台枚举，记给主管）、
`R2B-9`（`fight-king` 无尽天花板）、`R2B-11`（`fight-king` 对手跨章撞车）。
另把第 2 轮报告里已判「闭合」的 R2B-4 / R2B-6 / R2B-7 / R2B-10 一并抽验，防止被后来的提交带回去。

### 6. 分级口径

- **阻断**：进不去、玩不了、结算不出、崩溃、构建或测试红。
- **严重**：能玩但关键规则 / 模式 / 键位 / 热区错，或 `meta` 与实现不符会误导首页；已修严重项回潮。
- **一般**：文案、边界体验、可维护性一类，不影响本窗口交付。

### 7. 红线与不做的事

- 不改 `src/games/**`、不改 `src/engine/**`、不碰窗口 1 的平台文件、不碰包 A / 包 C 的 10 款。
- 临时走查脚本只放 `/tmp`，**不往 `src/games/**` 留任何测试**，跑完即删。
- 不改第 1 / 2 轮任何报告原文。
- 报告两处：独占 `docs/qa/1.2-window2-round3-tester-packB.md`；
  向 `docs/qa/1.2-window2-round3-tester.md` **只追加** `## Pack B`（已有 Pack A 就不动那一节）。
- `npm test` 与 `npm run build` 必须全绿；商标黑名单 0 命中。
- 收尾要明确回答：**就本包五款，能否随本窗口一起发布。**

---

<!-- 走查结论在下一笔提交里补齐 -->
