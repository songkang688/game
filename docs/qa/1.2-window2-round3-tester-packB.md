# 1.2 窗口 2 · 第 3 轮验收 · 测试员 包 B（本窗口收官）

- 窗口：2
- 角色：QA 第 3 轮 · 测试员 · 包 B
- 模型 slug：`claude-opus-5-thinking-high-fast`
- 范围（只验这 5 款，别人的游戏一个字节都不碰）：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 基线：`origin/game-1.2-window2` @ `d4decfc`
- 输入（本轮开工前逐份读完）：
  - `docs/qa/1.2-window2-round2-tester-packB.md`（阻断 0 · 严重 3 · 一般 8）
  - `docs/qa/1.2-window2-round2-learner-packB.md`（落地 P2B-1 ~ P2B-6，记录 4 条）
  - `docs/qa/1.2-window2-round2-fixer-packB.md`（R2B-1 / R2B-2 / R2B-3 声称清零，`f3a351a` 把格斗塔默认出战改成星星）
- 本轮定位：**收官轮**。只写报告与临时走查脚本，`src/games/**` 一个字节都不改，走查脚本跑完即删。

---

## 一、走查计划（动笔前先立此计划）

### 1. 换样本：本轮抽第 1 / 第 76 / 第 188 关

第 1 轮抽的是「第 1 / 100 / 188 关」，第 2 轮换成 84 / 88 / 92 / 96 那一档。
本轮**这些号一个都不许再用**，统一抽 **第 1 关 / 第 76 关 / 第 188 关**：

| 游戏 | 第 1 轮中间关 | 第 2 轮中间关 | 本轮中间关 |
| --- | --- | --- | --- |
| `dark-chess` | 100 | 84 | **76** |
| `hue-hand` | 100 | 88 | **76** |
| `hop-pads` | 100 | 92 | **76** |
| `tap-tiles` | 100 | 96 | **76** |
| `fight-king` | 100（塔） | 92（塔） | **76（塔）** |

`fight-king` 的战役就是格斗塔，关号口径与另外四款一致（`meta.levels = 188`），
所以不需要另换号，76 本身已经与第 2 轮的 92 不同。第 76 关落在第 5 章前后，
与前两轮抽过的第 5/6 章尾巴（84 / 88 / 92 / 96）和第 7 章（100）都不是同一段。

三关都要真打到结算，不许只读关表。

### 2. 每款必做的 7 条

| 序号 | 铁则 | 取证方式 |
| --- | --- | --- |
| 1 | 首页 / 深链进得去 | 真走 `src/main.ts` 的路由：`#/g/<id>` 深链 + 首页卡片点击两条路各进一次 |
| 2 | 真赢一次、真输一次 | headless `domStub` 装 360px，跑到 `onWin` / `onLose`，抄结算原文 |
| 3 | 第 1 / 76 / 188 关 | 三关各打到结算，记标题、HUD、星数 |
| 4 | 对战 / 无尽 / 双人到结算 | 每种 `mode` 各跑一局，抄结算原文 |
| 5 | 360px 热区 | 全量扒 CSS 的 `min-width` / `min-height` / `font-size`，逐条量到 ≥ 44px |
| 6 | `destroy` 泄漏 | 进 → 玩 → 退出 → 再进，连续 20 轮，数 window 监听 / rAF / timer / `root.children` 是否归零 |
| 7 | 商标 0 命中 | 去掉各款自带的黑名单守卫用例后，全量扫五个目录的可见串 |

### 3. 必须独立复验的第 2 轮三条严重（防回潮）

| 编号 | 游戏 | 第 2 轮症状 | 本轮复验点 |
| --- | --- | --- | --- |
| R2B-1 | `dark-chess` | 双人同屏朵朵的 `w/a/s/d` `f` `g` 无条件生效，星星回合能替对方落子 | 两人各管各的光标；星星回合朵朵按什么都落不了子；单人局别名键不能丢 |
| R2B-2 | `hop-pads` | 幽灵「起跳 16 次」对玩家「站满 16 座」，两把尺不一样；高手与地狱逐局同分 | 同一把「16」尺；`expert` 与 `hell` 必须分得开 |
| R2B-3 | `fight-king` | 默认出战的朵朵后段整片 0/8 | **测试员原文要的是「默认朵朵后段不能 0/8」**，修复员 `f3a351a` 改的是默认角色。本轮量清三件事：① 默认出战到底是谁；② 朵朵后段还是不是墙；③ 换人是否仍立刻能打。若只换了默认角色而朵朵仍 0/8，记一条给收官结论，**不自己改代码** |

### 4. 第 1 轮两条严重也要再确认无回潮

- **B-2** `tap-tiles` 同谱对战打平必须判平局，两边 `wins` 都不涨。
- **B-6** `hop-pads` 双人同屏按手指号分台，一个人抬手只能弹一台。

### 5. 一般遗留逐条给最终结论

`R2B-5`（`hop-pads` WASD / 方向键无语义）、`R2B-8`（`fight-king` 训练场进不了 `meta.modes`，平台枚举，记给主管）、
`R2B-9`（`fight-king` 无尽天花板）、`R2B-11`（`fight-king` 对手跨章撞车）。
另把第 2 轮报告里已判「闭合」的 R2B-4 / R2B-6 / R2B-7 / R2B-10 一并抽验，防止被后来的提交带回去。

### 6. 分级口径

- **阻断**：进不去、玩不了、结算不出、崩溃、构建或测试红。
- **严重**：能玩但关键规则 / 模式 / 键位 / 热区错，或 `meta` 与实现不符会误导首页；已修严重项回潮。
- **一般**：文案、边界体验、可维护性一类，不影响本窗口交付。

### 7. 红线与不做的事

- 不改 `src/games/**`、不改 `src/engine/**`、不碰窗口 1 的平台文件、不碰包 A / 包 C 的 10 款。
- 临时走查脚本只放 `/tmp`，**不往 `src/games/**` 留任何测试**，跑完即删。
- 不改第 1 / 2 轮任何报告原文。
- 报告两处：独占 `docs/qa/1.2-window2-round3-tester-packB.md`；
  向 `docs/qa/1.2-window2-round3-tester.md` **只追加** `## Pack B`（已有 Pack A 就不动那一节）。
- `npm test` 与 `npm run build` 必须全绿；商标黑名单 0 命中。
- 收尾要明确回答：**就本包五款，能否随本窗口一起发布。**

---

## 二、怎么取的证

本轮不看这五款自带的 `*.test.ts` 断言，另起一份独立走查脚本（`.qa3b/`，跑完即删），
用各款自带的 `domStub` 装 **360px** 屏，真挂 `mount(api)`、真点按钮、真敲键盘 / 真派指针事件，
读的都是屏幕上那几个节点（HUD 串、说明行、结算浮层的 innerHTML）。

四款「屏幕能读出数字」的游戏另外挂了一份**镜像**：
同一串动作在纯逻辑层（`run.ts` / `rules.ts`）上重放一遍，**逐拍 / 逐手 / 逐跳**比对屏幕与镜像。
镜像对不上就是屏幕自己算了一套 —— 本轮五款全部「逐拍一致」，一处分歧都没有。

| 走查文件 | 例数 | 覆盖 |
| --- | --- | --- |
| `00-shell.qa.ts` | 3 | 首页 `collectGames` 收集 + `#/game/<id>` 深链 + 五款 360px 全量量表 + 商标黑名单 |
| `10-dark-chess.qa.ts` | 8 | 第 1/76/188 关 + 真输 + 三模式 + R2B-1 + destroy + 和棋倒数 |
| `20-fight-king.qa.ts` | 9 | 第 1/76/188 关 + 真输 + R2B-3 四问 + 三模式 + destroy + R2B-8/9/11 |
| `30-hop-pads.qa.ts` | 12 | 第 1/76/188 关 + 真输 + R2B-2 + B-6 三路 + 三模式 + destroy + R2B-5 |
| `40-hue-hand.qa.ts` | 16 | 第 1/76/188 关 + 真输 + 三模式 + 规则铁则 + AI 四档 + R2B-10 + R3B-1 + destroy |
| `50-tap-tiles.qa.ts` | 16 | 第 1/76/188 关 + 真输×2 + 188 关整表 + 三模式 + B-2 + R2B-4 + destroy + R2B-7 |
| 合计 | **71 例全绿** | |

---

## 三、走查结论

### 1. 第 1 / 76 / 188 关 —— 五款十五关全部真打到结算

| 游戏 | 第 1 关 | 第 76 关 | 第 188 关 |
| --- | --- | --- | --- |
| `dark-chess` | 第 1 章 · 新手 · 上限 220 手 → **过关 ★3** · 43 手 | 第 4 章 · 新手 → **过关 ★3** · 37 手 | 第 8 章 · 大师 → **过关 ★3** · 31 手（试了 3 种出手风格） |
| `hue-hand` | 「🎨 对上颜色」2 人 · 限 16 手 → **过关 ★3** · 6 手 | 「🌈 换个颜色」2 人 · 限 17 手 → **过关 ★3** · 10 手 | 「🏆 接龙杯」积分赛 · 目标 69 分 · 4 人对大师 → **过关 ★3** · 2 局 27 手 |
| `hop-pads` | 「⭕ 直线台」目标 5 座 → **过关 ★3** · 5 座 30 分 · 圆心 5 次 | 「↔️ 移动台」目标 6 座 → **过关 ★3** · 6 座 42 分 | 「🏆 跳跳杯」目标 16 座 → **过关 ★3** · 16 座 108 分 · 圆心 14 次 |
| `tap-tiles` | 「🎵 单轨热身」14 音符 · 速度 1.00 → **过关 ★3** · 14 完美 1650 分 | 「➰ 长按条」22 音符 · 速度 1.86 · 5 条长按 → **过关 ★3** · 22 完美 3000 分 | 「🏆 音符杯」44 音符 · 速度 3.15 · 12 条长按 · 三押 → **过关 ★3** · 44 完美 8400 分 |
| `fight-king` | 塔第 1 关 · 对手 星星（轻松档）→ **过关 ★3** · 回合 1:0 · 1050 帧 | 塔第 76 关 · 对手 星星（普通档）→ **过关 ★2** · 回合 1:0 · 1332 帧 | 塔第 188 关 · 对手 啾啾（高手档）→ **过关 ★3** · 回合 2:1 · 6997 帧 |

星数不是照抄屏幕的：每一关都另按关表算一遍应得星数，两边必须一致才算过。

`tap-tiles` 的第 188 关顺便验到了平台的收官庆祝：前 187 关都记满三星再打通第 188 关，
`api.onWin(3, "188 关全部弹完,四条轨都成了你的琴键!")` 真的响了。

**`tap-tiles` 还把 188 关整表跑了一遍**：`validateChart` 三条约束（并发上限 / 最小间隔 / 长按不重叠）
**0 错**，参考机器人 **188 关全部打得完**（第 1 关 1650 分 → 第 188 关 8400 分，速度 1.00 → 3.15 单调递增）。
`hue-hand` 抽了 24 关，**全可解**。

### 2. 真输一次 —— 六条失败路径，全部只鼓励不批评

| 游戏 | 怎么输的 | 屏幕原文 |
| --- | --- | --- |
| `dark-chess` | 第 188 关（大师档）摆烂 46 手 | 「就差一点点！」·「这一盘对方先收官了，下一盘先把兵留住。」 |
| `hue-hand` | 第 1 关一张不出只摸牌，19 步用光 16 手 | 「就差一点点！」·「这一局牌不太顺,换个顺序试试,先把最多的那个颜色打掉。」 |
| `hop-pads` | 第 76 关 0 毫秒蓄力松手 | 「就差一点点！」·「云朵接住你啦,再来一次。这次站住了 0 座,再站住 6 座就过关啦。」 |
| `tap-tiles` | 第 1 关站着不动，💗 掉到 0 | 「就差一点点！」·「有几个音符溜走了,慢一点、稳一点,再试一遍准能过。」 |
| `tap-tiles` | 第 76 关点一下空白轨（第 3 章起点空即收工） | 「就差一点点！」·「点到空白格啦,下一次先等音符压到判定线再落手。」 |
| `fight-king` | 第 188 关站着不动，回合 0:2 | 「就差一点点！」·「这一层的对手挺有一套，换个节奏再来！」 |

无血、无死亡描写、没有一句批评人的话。

### 3. 对战 / 无尽 / 双人同屏 —— 十五局全部跑到结算

| 游戏 | 对战 | 无尽 | 双人同屏 |
| --- | --- | --- | --- |
| `dark-chess` | 普通档 10 手 → `onLose`「这一盘对方先收官了…」 | 连胜 2 场收尾 →「这一轮连胜 2 场，历史最好 2 场。」 | 21 手 → `onWin`「朵朵赢下这一盘！」 |
| `hue-hand` | 4 人桌大师档 16 步 →「🤖 这局被 点点 拿下」·「这一局收了 134 分…」 | 认真打 48 步赢一局、摆烂 149 步输一局 →「连胜停在 2 局,最好成绩 2 连胜,一共收了 61 分。」 | 遮挡幕真出现 13 次（「🙈 轮到 星星 啦」/「🙈 轮到 朵朵 啦」）→「🏆 星星先出完啦!」 |
| `hop-pads` | 第 1 局真赢（我 230 : 新手幽灵 36）、第 2 局真输（0 : 36），顶栏「⚔️ 第 2 局 · 你 1 : 0 新手」 | 一路踩圆心到 37 座 650 分再故意掉 →「☁️ 云朵接住你啦」 | 朵朵 210 分 / 星星 158 分 →「🏁 这一局结束」·「总比分 朵朵 1 : 0 星星。」 |
| `tap-tiles` | 新手档 4200 : 2950 →「🏆 你赢下这一局!」，下一局顶栏「你 1 : 0 新手」 | 第 1 段 16 完美 1950 分过关 → 第 2 段故意漏 1 个（0 容错）→「有个音符溜走啦」·「撑到了第 2 段,一共 2450 分」 | 42 音符全用别名键（A / D / ← / L）打完 →「🎉 两个人一起弹完啦!」·「合力 7800 分,最高 42 连,漏了 0 个。」 |
| `fight-king` | 普通档先赢 2 回合 · 2725 帧 →「赢啦！」· 星星 +1 | 连赢 2 场收尾 →「连赢 2 场，手感在回来了！　🏅 最长连胜 2 场」· 星星 +3 | 1 号位真按 WASD+F/G · 3518 帧 →「赢啦！」 |

`hop-pads` 与 `tap-tiles` 的对战 / 无尽 / 双人三局也都挂了镜像，**逐跳 / 逐拍一致**。

### 4. 首页 / 深链

`collectGames()` 一共收到 **64 款**，本包五款一款不落，`load()` → `mount()` 全部成功：

| id | 首页标题 | 分类 | 关数 | `meta.modes` | 深链 |
| --- | --- | --- | --- | --- | --- |
| `dark-chess` | 翻翻暗棋 | party | 188 | campaign/versus/endless/twoPlayer | `#/game/dark-chess` ✅ |
| `hue-hand` | 花色接龙 | party | 188 | 同上 | ✅ |
| `hop-pads` | 跳跳台 | casual | 188 | 同上 | ✅ |
| `tap-tiles` | 音符下落 | casual | 188 | 同上 | ✅ |
| `fight-king` | 朵星格斗王 | party | 188 | 同上 | ✅ |

### 5. 360px 热区 / 字号

**真按钮全部 ≥ 44px**，字号全部 ≥ 12px。全量扒 CSS 之后剩下五处 `min-height < 44px`，
逐处看过去**都不是热区**，是文字行或装饰行：

| 位置 | 值 | 是什么 | 判定 |
| --- | --- | --- | --- |
| `dark-chess/view.ts:43` `.dc-note` | 20px | 棋盘下面那行提示文字 | 不可点，不算热区 |
| `hue-hand/index.ts:136/229` `.hh-backs` | 20 / 16px | `<span>`，画对手手上几张牌背 | 不可点 |
| `hue-hand/index.ts:149` `.hh-say` | 19px | 旁白行 | 不可点 |
| `hop-pads/index.ts:63` `.hp-say` | 22px | 旁白行 | 不可点 |
| `tap-tiles/index.ts:168` `.tt-say` | 20px | 判定说明行（`role=status`） | 不可点 |

`tap-tiles` 另做了一次窄屏实测：360px 下画布 **336×417**，四列各 **84px**（下限 80px），
判定线落在 y=334（画布内，点得着），触屏点第 1 列真的判出「完美!1 连」。
`fight-king` 的四颗触屏钮（轻击 / 重击 / 必杀 / 防御）在 360px 下都在，CSS 最小字号 12px。

### 6. `destroy` 泄漏

五款各跑 **20 轮「进 → 玩 → 退 → 再进」**，每轮退出后数一次；末尾再 `destroy()` 收一次总账：

| 游戏 | 20 轮监听序列 | `destroy()` 之后 |
| --- | --- | --- |
| `dark-chess` | `[0]×20` | 监听 0 / 子节点 0 |
| `hue-hand` | `[0]×20`（基线 0） | 监听 0 / 子节点 0 / 未清定时器 0 |
| `hop-pads` | `[1]×20`（基线就是 1，没长） | 监听 0 / 子节点 0 / 画布 0 / rAF 队列 0 |
| `tap-tiles` | 增量 `[0]×20` | 监听 0 / 子节点 0 / 画布 0 / rAF 队列 0 / 建过 1 个 AudioContext，关掉 1 个 |
| `fight-king` | `[0]×20` | 监听 0 / 子节点 0 |

`tap-tiles` 另把三种模式各开一遍再 `destroy()`：监听 0、节点 0、帧队列 0。

### 7. 商标黑名单

五个目录去掉各款自带的守卫用例后，**54 个文件逐词扫 24 条商标词，真命中 0**。
唯一一处报警是 `fight-king/index.ts:1214` 的镜头注释
`// 慢慢追上目标值，跳跃和击退时镜头才不会一跳一跳的` —— 「一跳一跳」含子串「跳一跳」。
**代码注释、不是可见文案、基线自带**，第 2 轮学习优化员与修复员都记过这处误报。**不算命中。**

---

## 四、第 2 轮三条严重的独立复验（防回潮）

### R2B-1 · `dark-chess` 双人同屏 —— **未回潮** ✅

不看 `duo.test.ts`，自己摆盘再复验：

- **两人各有一个光标**：朵朵的光标停在 1、星星的光标在 30 →（按自己的方向键）22。两条互不干涉。
- **星星回合，朵朵按 `w/a/s/d` + `f` + `g` 一整套下来，手数仍旧是 1** —— 一手都替不了。
- **单人局老键位没丢**：方向键与 `D` 拨的是同一个光标，`L` 也能落子（手数 0→1）。
- 顺带：取消按钮跟着回合改字，20 次 Esc 来回切之后写的是「取消选择 (G)」，轮到朵朵。
- P2B-5 和棋倒数还在：`QUIET_WARN_AT=8`，`quiet=19` 时顶栏写「再 1 手不吃不翻就算和」。

### R2B-2 · `hop-pads` 幽灵与玩家同一把「16」尺 —— **未回潮** ✅

满分玩家（一路踩圆心、站满 `MATCH_HOPS` 就冻结）对四档幽灵，前八局：

| 局 | 满分玩家 | 新手 | 普通 | 高手 | 大师 |
| --- | --- | --- | --- | --- | --- |
| 1 | 230 / 16 座 | 36 / 16 | 76 / 16 | 134 / 16 | 230 / 16 |
| 2 | 230 / 16 | 36 / 16 | 92 / 16 | 152 / 16 | 230 / 16 |
| 3 | 290 / 19 | 62 / 19 | 100 / 19 | 194 / 19 | 290 / 19 |
| 4 | 160 / 16 | 42 / 16 | 76 / 16 | 84 / 16 | 160 / 16 |
| 5 | 96 / 16 | 44 / 16 | 56 / 16 | 84 / 16 | 96 / 16 |
| 6 | 270 / 18 | 10 / 5（掉了） | 76 / 18 | 110 / 18 | 182 / 18 |
| 7 | 108 / 16 | 28 / 8（掉了） | 52 / 16 | 74 / 16 | 108 / 16 |
| 8 | 148 / 17 | 2 / 1（掉了） | 44 / 17 | 120 / 17 | 148 / 17 |

- **座数口径对齐**：八局里没有任何一档幽灵比满分玩家多站一座。
- **满分玩家一分不吃亏**：没有任何一档反超他。
- **四档逐局严格递增**，一处倒挂都没有。
- **高手与大师分得开**：另跑 20 局，**同分 0 局**（第 1 局 134 / 230、第 4 局 84 / 160…）。
- 参数原样：`TIER_NOISE = {0.25, 0.12, 0.05, 0.015}`、`TIER_SLIP_EVERY = {3, 5, 9, 0}`（大师一次都不偏）。

### R2B-3 · `fight-king` 格斗塔默认出战 —— **症状面未回潮，根因只是绕开** ⚠️

第 2 轮测试员的原话是「**默认朵朵后段不能 0/8**」，修复员 `f3a351a` 改的是「默认出战换成星星」。
本轮按要求把三件事分开量清楚，用的是和 `curve.test.ts` 同一把尺（玩家 lv4 的手、对手照配表取档位与增益、固定 seed、每关 8 局）：

**① 默认出战到底是谁 —— 是 ⭐ 星星，三处对齐**

`TOWER_HERO_ID = "xingxing"`（星星）；`CHARACTERS[0]` 仍是 `duoduo`（朵朵，排第 1 位没被挪走）；
真挂 360px 进塔，选人格里高亮的是 1 个 =「⭐星星」；第 1 关 HUD 1 号位写的也是「⭐ 星星」。
八位小伙伴还是 8 位，一位没少。

**② 朵朵后段还是不是墙 —— 还是墙**

| 关 | 🌸 朵朵 | ⭐ 星星（现默认） | 🍡 糯糯 |
| --- | --- | --- | --- |
| 第 125 关 | **0/8** | 5/8 | 5/8 |
| 第 133 关 | 1/8 | 4/8 | 5/8 |
| 第 149 关 | **0/8** | 6/8 | 7/8 |
| 第 160 关 | **0/8** | 4/8 | 5/8 |
| 第 165 关 | **0/8** | 2/8 | 6/8 |
| 第 176 关 | **0/8** | 1/8 | 2/8 |
| 第 180 关 | **0/8** | 5/8 | 4/8 |
| 第 188 关 | 1/8 | 4/8 | 5/8 |

**抽的八关里朵朵有 6 关整片 0/8。** 新默认星星这八关一处 0/8 都没有（1~6/8），
所以「默认出战推不动后段」这条**确实被解决了**；但**朵朵本人还是那堵墙，一个数都没动过**。

**③ 换人是否仍立刻能打 —— 能**

真在塔里点朵朵：高亮跟到「🌸朵朵」，第 160 关 HUD 1 号位跟着写「🌸 朵朵」。
同一关换糯糯上场，seed 7 一把就赢下来（回合 2:0 →「第 160 关过关！」）。

**④ 连败提示在**

同一关连输 3 场之后，选人排下面与结算里都写出
「连着几场没赢下来啦，上面那排换个小伙伴试试，换完还从这一关继续。」——只指路、不说人弱。

**结论（收官口径）**：R2B-3 **没有回潮**（第 2 轮记的症状「默认出战的朵朵后段基本打不过」已不成立），
但**只是把默认角色挪开，朵朵后段仍是 0/8**。这条按第 1 轮 B-10 / B-11 的分级留在**一般（平衡项）**，
登记为 **R3B-2**，交给平衡专项，本轮**不自己改代码**（改角色数值 / 增益曲线会让 `hero.test.ts` 的胜率表当场作废）。

---

## 五、第 1 轮两条严重的复验（防回潮）

### B-2 · `tap-tiles` 同谱对战打平 —— **未回潮** ✅

真进模式、真挑档、真弹完 `matchChart(1)` 的 28 个音符（28 完美 / 0 漏）：

| 场景 | 我 | 对手 | 结算标题 | 结算副标题 | 顶栏比分 |
| --- | --- | --- | --- | --- | --- |
| 打平（大师档） | 4200 | 4200 | **「🤝 打成平手!」** | 「你 4200 分 · 对手 4200 分。…一分不差地追平了…」 | 「你 0 : 0 大师 · **平 1**」 |
| 真赢（新手档） | 4200 | 2950 | 「🏆 你赢下这一局!」 | 「你 4200 分 · 对手 2950 分。」 | 「你 1 : 0 新手」 |
| 真输（一下不点） | 0 | 4175 | 「🤖 这局对手分高一点」 | 「你 0 分 · 对手 4175 分。」 | 「你 1 : 1 普通」 |

- **打平不再判输**，两边 `wins` 都不涨，单独记在「平 N」那一格里，星星照给 1 颗。
- **数字与文案不再打架**：打平那一局屏幕上两边写的都是 4200，标题也是平手。
- 赢仍旧是严格大于、输仍旧是严格小于。

### B-6 · `hop-pads` 双人分台 —— **未回潮** ✅

三条输入通路各验一遍（读两块 `.hp-canvas` 的 `data-phase`）：

| 通路 | 实测 |
| --- | --- |
| 指针 | 两人都按住 = `charging,charging`；**星星抬手 = `charging,flying`（只弹一台）**；陌生指号 `pointercancel` = `charging,flying`（碰不着别人）；朵朵也抬手 = `flying,flying` |
| 触摸 | 两人都按住 = `charging,charging`；星星 `touchend` = `charging,flying` |
| 键盘 | 按 `F` = `charging,ready`；再按 `L` = `charging,charging`；朵朵 `G` 收力 = `ready,charging`；星星 `K` 收力 = `ready,ready` |

顺带：P2B-4 的满力信号还在（按满之后 `full()=true`、`data-full=1`；`G` 收力之后回 `false`）。

---

## 六、一般遗留逐条最终结论

| 编号 | 游戏 | 第 2 轮说法 | **本轮最终结论** |
| --- | --- | --- | --- |
| R2B-4 | `tap-tiles` | 已闭合（P2B-2 四档分层） | **确认闭合。** 对战前八局四档**逐局严格递增、无一组同分**（第 1 局 2950 / 3800 / 3975 / 4200，第 8 局 4550 / 7050 / 7425 / 7800）；满分玩家最差也只是和大师打平。分档靠三层正交参数：噪声 `{80,40,15,5}ms`、手滑率 `{9%,3.5%,1.2%,0}`、故意打偏 `{3,5,9,0}` |
| R2B-5 | `hop-pads` | 仍在（WASD / 方向键无语义） | **仍在，最终判为口径题，建议主管定，不作为发布阻碍。** 第 1 关逐键实测：`w a s d ↑↓←→ g k l` **全是 `ready→ready`**，只有 `f` 与空格进 `charging`；`Esc` 置暂停位。这款是**单键蓄力**玩法，四个方向本来就没有语义，屏幕上的键位提示也只写了蓄力键 —— 属「全局统一键位口径」与「单键玩法」之间的取舍 |
| R2B-6 | `dark-chess` / `hop-pads` | 已闭合（档名改成新手/普通/高手/大师） | **确认闭合。** 本包四款屏幕上写的都是 `新手 / 普通 / 高手 / 大师`（`hop-pads` 顶栏「⚔️ 第 2 局 · 你 1 : 0 新手」、`tap-tiles`「🤖 大师 4200 分」、`hue-hand`「对手 大师/大师/大师」），没有「菜鸟 / 地狱」回潮 |
| R2B-7 | `tap-tiles` | 已闭合（第 1 轮 B-9） | **确认闭合。** 单人 `d→0 f→1 j→2 k→3`，屏幕写「键盘 D F J K 对四条轨 · 也能直接点 · Esc 暂停」，逐字吻合；双人主键 `a s k l` 外加别名 `d→1`、`←→ →2/3`，本轮**整整一局 42 个音符全用别名键打完、0 漏**。`Esc` 盖板「⏸ 先歇一会儿」，暂停钮同步成「▶️ 继续」，再按一次收起 |
| R2B-8 | `fight-king` | 仍在（训练场进不了 `meta.modes`） | **仍在，最终记给主管。** 实测 `meta.modes = ["campaign","versus","endless","twoPlayer"]`，平台 `GAME_MODES = ["campaign","versus","endless","coop","twoPlayer"]`，**枚举里就没有 `training`**。这是 `src/engine/types.ts` 的平台侧枚举，按纪律本包不碰。影响面只是首页玩法芯片筛不到训练场，训练场入口本身在菜单里、进得去 |
| R2B-9 | `fight-king` | 仍在（无尽天花板落在第 12 场上下） | **仍在，最终判为一般（平衡），不作为发布阻碍。** 增益到第 21 场封顶（元气×1.6 · 威力×1.45 · 速度×1.25），星奖第 8 场就到顶 3 星。默认出战星星、lv4 的手逐场 8 局：第 1 场 8/8、第 6 场 5/8、**第 13 场 1/8**、第 26 场 0/8、第 51 场 1/8。**封顶本身是对的**（不会无限膨胀，也不会越玩越不可能），只是「连胜」这个名头在十来场就见底 |
| R2B-10 | `hue-hand` | 部分闭合（重开换牌是设计口径） | **确认为设计口径，闭合。** 本轮复量：同一关连开 12 副牌，参考打法第 1 关过 6 副、第 76 关过 5 副、第 88 关过 5 副，**卡死 0 副**；而**生成时验过的那一副（`round=0`）三关全过**。也就是「这一关一定过得去」这条承诺守住了，往后重开是手气 —— 换牌玩法本来如此 |
| R2B-11 | `fight-king` | 仍在（`foeIdOf` 跨章撞车） | **仍在，最终判为一般（文案 / 观感），不作为发布阻碍。** 本轮抽的三关：第 1 / 76 关都是星星，第 188 关是啾啾。把八章首关摆开看更清楚：星星 / 绿绿豆 / 云云 / 朵朵 / **闪闪 / 闪闪 / 闪闪 / 闪闪** —— `(idx*3 + ci*5 + 1) % 8` 在后四章连着落在同一位身上。同章之内不重复这条是守住的，跨章会撞。属对手轮转表的口味问题，改它会让 `hero.test.ts` / `curve.test.ts` 的胜率表全部作废 |

第 1 轮遗留里 **B-2 / B-6 已在 §五 复验未回潮**，`B-3`→R2B-6 闭合，`B-7`→R2B-5 仍在，
`B-9`→R2B-7 闭合，`B-10` / `B-11` 的根因并进 **R3B-2**（见下），`B-12`→R2B-8 记给主管。

---

## 七、本轮问题清单（阻断 0 · 严重 0 · 一般 3）

> 第 2 轮的三条严重 + 第 1 轮的两条严重，本轮**一条都没回潮**，所以严重格是 0。
> 下面三条是本轮新记或需要收口的一般项。

| 编号 | 游戏 | 现象 | 证据 | 留给谁 |
| --- | --- | --- | --- | --- |
| **R3B-1** | `hue-hand` | **边角健壮性**：抽牌堆与弃牌堆同时见底（`deck=[]` 且 `pile.length<=1`）时，`refill()` 的守卫直接返回不洗牌，`pullCard()` 返回 `null`，`drawFromDeck()` 只把回合让给下一个人 —— 局面不再前进，也没有「牌用完了算平局」的收口。实测摆这么一个残局连摸 200 次：真摸到 **0 张**，手牌纹丝不动，`finished` 一直是 `false`，`moves` 从 0 涨到 200 | 直接构造 `deck=[]`、台面 1 张、两人各 1 张接不上的牌 | **记录。要凑出这个局面得让 108 张里的 107 张全在手上（两人各约 53 张），正常对局到不了；战役有手数上限兜底。不是发布阻碍**，建议以后给 `refill` 补一句「两边都空就判和」 |
| **R3B-2** | `fight-king` | **朵朵后段仍是墙**：R2B-3 的症状（默认出战推不动后段）已消失，但**只是把默认角色从朵朵换成星星**，朵朵本人的数值一个没动 —— 本轮抽的后段八关里她有 **6 关整片 0/8**（第 125 / 149 / 160 / 165 / 176 / 180 关）。朵朵是对外两位主角之一、选人排里仍排第 1 位，孩子点她进后段就推不动 | §四 R2B-3 ② 那张表 | **平衡专项。** 缓解手段已到位（默认换人 + 连败 3 场提示换人 + 换人立刻能打，实测第 160 关换糯糯一把就赢），所以**不作为发布阻碍**；沿用第 1 轮 B-10 / B-11 的一般分级 |
| **R3B-3** | `hue-hand` | **相邻档位分层不明显**：四档两两对打各 120 局，「普通」**没打过**「新手」（62 : 58）。跨档是清楚的（新手 0 : 120 高手、新手 0 : 120 大师），相邻两档（普通 vs 高手 68:52、高手 vs 大师 67:53）也只是小幅领先 | 六组 120 局互打全表 | **记录。** 这款自带的 `ai.test.ts` 本来也只断言「最强压最弱」，口径一致；孩子感知得到的是新手与大师的差别，不作为发布阻碍 |

**另记两条不算问题的观察**（避免下一轮再翻一遍）：

1. 五款 CSS 里那五处 `min-height < 44px` 全是**文字行 / 装饰行**（`.dc-note` `.hh-backs` `.hh-say` `.hp-say` `.tt-say`），**不是热区**，360px 达标。
2. 商标扫描唯一那处报警（`fight-king/index.ts:1214` 注释里的「一跳一跳」含子串「跳一跳」）是**误报**，代码注释、基线自带、不是可见文案。

---

## 八、验收数据

| 项 | 数 |
| --- | --- |
| 基线 | `origin/game-1.2-window2`（起手 `d4decfc`，收尾 rebase 到 `becadd1`） |
| `npx vitest run`（全量） | **258 个测试文件 / 6608 条用例全绿** |
| 本包 5 款单独跑 | **41 个文件 / 754 条用例全绿** |
| 本轮临时走查脚本 | **6 个文件 / 71 例全绿**（跑完即删，不入库） |
| `npm run build` | `tsc --noEmit` + `vite build` 通过，PWA **157 条预缓存**照常生成 |
| `src/**` 改动 | **0 个字节**（本轮只写 `docs/qa/`） |
| 商标黑名单 | 54 文件逐词扫，**真命中 0** |

### 复现用的关号 / seed

- `dark-chess`：关 0 / 75 / 187（=第 1 / 76 / 188 关）；三模式走 `mountVersus` / `mountEndless` / `mountTwoPlayer`。
- `hue-hand`：关 0 / 75 / 187；AI 互打 `duel(lo, hi, 120)`；重开换牌 `levelDeck(meta, round)` `round=0..11`。
- `hop-pads`：关 0 / 75 / 187；对战 `matchSeed(round)` / `matchDifficulty(round)` 第 1~20 局。
- `tap-tiles`：关 0 / 75 / 187；对战 `matchChart(1..8)`，假人 `aiRun(chart, tier, chart.seed + 5)`；无尽 `endlessWave(0..1)`；双人 `matchChart(41)`。
- `fight-king`：塔关 0 / 75 / 187；后段八关 124 / 132 / 148 / 159 / 164 / 175 / 179 / 187，玩家 `createBrain(4, g*100+r)`，每关 8 局。

---

## 九、收官结论 —— 本包五款能不能随本窗口一起发布

**能。五款全部建议随窗口 2 一起发布。**

理由摆在这儿：

1. **阻断 0、严重 0。** 第 2 轮三条严重（R2B-1 / R2B-2 / R2B-3）与第 1 轮两条严重（B-2 / B-6）
   本轮**独立复验，一条都没回潮**——不是照抄修复员的用例，是另起一套镜像重新量的。
2. **十五关真打到结算，星数与关表逐关对得上**；六条失败路径全部只鼓励不批评，无血无死亡。
3. **五款 × 三模式 = 十五局全部跑到结算**，屏幕与镜像**逐拍一致**，没有任何一处「屏幕自己算一套」。
4. **360px 达标**（真按钮全部 ≥44px，字号 ≥12px，`tap-tiles` 四列各 84px、判定线在屏内）；
   **`destroy` 五款全部归零**（20 轮进出不涨监听、rAF / timer / AudioContext 都收干净）；
   **商标真命中 0**。
5. **`npm test` 258 文件 / 6608 例全绿，`npm run build` 全绿。**

剩下的三条一般项（R3B-1 边角健壮性、R3B-2 朵朵后段平衡、R3B-3 hue-hand 相邻档位）
**都有缓解路径、都不挡孩子把游戏玩完**：R3B-1 正常对局到不了，R3B-2 有默认换人 + 连败提示 + 换人立刻能打三层兜底，
R3B-3 只影响相邻两档的手感差。R2B-8 是平台 `src/engine` 枚举，按纪律**记给主管**，不在本包范围内。

**唯一想请主管拍板的一件事**：`fight-king` 的 R3B-2 —— 朵朵作为对外两位主角之一，
在格斗塔后段仍是 0/8。这不影响本次发布，但建议排一个**平衡专项**统一处理
（同时收掉 R2B-9 无尽天花板与 R2B-11 对手跨章撞车，这三条都在同一张角色数值 / 增益曲线 / 对手轮转表上，
分开改会让 `hero.test.ts` / `curve.test.ts` 的胜率表反复作废）。
