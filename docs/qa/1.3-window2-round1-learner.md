# 1.3 窗口 2 · 第 1 轮视觉验收 · 学习优化员报告

> 角色：窗口 2 第 1 轮学习优化员（B 档）。模型：`claude-fable-5-thinking-xhigh`。
> 分支：`game-1.3-window2`。基线：`origin/game-1.3`。
> 范围：只评本窗 9 款——`merge-2048` `mine-garden` `sudoku-petal` `dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden` `dark-chess`。
> 权限：只写本报告 + 本窗 9 款的纯文案字符串（guide / blurb / 提示语）+ 为文案改动补的快照测试。绘制代码一行不碰；A 档 `...-tester.md` 与 C 档 `...-fixer.md` 一字不碰。

## 学习计划（动手前登记）

1. **向外看**：按题材（2048 合成、扫雷、数独、豆豆迷宫、合成大果、台球、军棋、国象、暗棋翻子）梳理成熟小游戏平台头部作品的**画面密度**（背景层数 / 装饰物件数 / 动效通道数，记成数字基准）与**角色剪影**（16px 下靠哪一笔认出）。只记结论文字，不下载、不截图入库、不描摹任何具体造型。
2. **向内看**：逐款读本库 `art.ts` / `index.ts` / `view.ts` 的绘制结构（只读不改），数出本库的背景层数 / 装饰数 / 动效通道数，与题材基准做差。
3. **产出**：四字段逐款评估表（密度差距 / 剪影达标 / 观感短板 TOP1 / 建议动作）→ 给 C 档的优先级 TOP10（绘制规格级）→ 商标风险提醒清单。
4. **唯一代码改动**：9 款范围内 guide.ts 攻略文案 / meta.ts blurb / 提示语纯字符串，把 1.3 新视觉亮点讲进引导；每处登记；补快照测试。
5. 收尾：fetch → rebase（保留他人文件）→ `npm test` / `npm run build` 全绿 → 普通 push。

（以下各节待本轮完成后填写。）
