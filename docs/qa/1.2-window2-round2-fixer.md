# 窗口 2 · 1.2 三轮验收 第 2 轮 · 修复员 · 汇总

> 每个 Pack 只追加自己那一节，不动别人的段落。
> 完整明细各自在 `docs/qa/1.2-window2-round2-fixer-pack*.md` 里。

## Pack B

- 范围：`dark-chess` `hue-hand` `hop-pads` `tap-tiles` `fight-king`
- 详版：[`docs/qa/1.2-window2-round2-fixer-packB.md`](./1.2-window2-round2-fixer-packB.md)
- 输入：`docs/qa/1.2-window2-round2-tester-packB.md`（阻断 0 · 严重 3 · 一般 8）、`docs/qa/1.2-window2-round2-learner-packB.md`（落地 6 条）
- 结论：**阻断 0 条（本来就是 0）· 严重 3 条全部清零 · 一般 8 条登记（其中 3 条已由学习优化员闭合）。**

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| R2B-1 | 严重 | `dark-chess` | 双人同屏一人一个光标：`cursor` 拆成 `cursors: Record<Side, number>`（朵朵左上角起、星星右下角起），`onKey` 拆成两张键表 —— 朵朵 `WASD + F / G` 只走 `duo` 座位、星星 方向键 `+ L / K` 只走 `star`，`clickCell` 再加一道「不是你的回合就返回」的闸。单人局里 `starSeat` 折回 `duo`，方向键与 `L / K` 仍是别名。顺手把「取消选择」按钮接到真的 `cancel()` 上 |
| R2B-2 | 严重 | `hop-pads` | 幽灵对战两边用同一把尺：`playGhost` 的循环从「起跳 N 次」改成 `while (run.hops < N)`（站满 N 座），弹簧台白送的座数两边都算。高手与大师的分层另加一层和噪声正交的**确定性故意打偏**（`TIER_SLIP_EVERY = 3 / 5 / 9 / 0`，`slipPower` 二分到「完美圈边→台沿」45% 处，掉分不掉人）；`TIER_NOISE` 那四个规格数一个没动 |
| R2B-3 | 严重 | `fight-king` | 格斗塔默认出战从 `CHARACTERS[0]`（朵朵）改成实测推得动后段的星星（新增导出常量 `TOWER_HERO_ID`）；同一关连输 3 场在选人格下面提一句「换个小伙伴试试」；朵朵的介绍不再写「新手先选她准没错」。**关卡表 / 角色数值 / 对手轮转 / 增益曲线一个数没动** |

先红后绿都留了证据：R2B-1 的红是「朵朵的 D 把星星的光标拨走了」「朵朵的 F 替星星把这一手下掉了」，
R2B-2 的红是「幽灵比玩家多站了 2 座」「大师没比高手高（270 : 270）」，
R2B-3 的红是「第 125 关默认角色一局都赢不下来（0/8）」。

两组量得出来的对比：

- `hop-pads` 对战前八局，改前**每一局**高手与大师同分、满分玩家倒输 40 ~ 100 分；改后**八局四档全部严格递增**，
  满分玩家在四档面前都不吃亏，对大师仍旧要一路踩满圆心才追得平（七局平、一局赢，不是必赢）。
- `fight-king` 后段十关用 `curve.test.ts` 那把尺（lv4 的手、每关 8 局），原默认角色八处 0/8；
  换成新默认之后**一处 0/8 都没有**，而第 188 关仍旧不是随手赢。

**新增回归 3 个文件 / 27 条**：`dark-chess/duo.test.ts` 8 条、`hop-pads/versus.test.ts` 9 条、`fight-king/hero.test.ts` 10 条。
既有用例一条没删、没放宽；`hue-hand` 与 `tap-tiles` 本轮 diff 为空。

**第 1 轮已清零的两条严重未回潮**：B-2（`tap-tiles` 打平判平局）所在文件本轮一个字节没动、4 条回归全绿；
B-6（`hop-pads` 按指号分台）的 `createStage` 输入段 diff 为空、9 条回归全绿。
学习优化员本轮落地的 6 条（P2B-1 ~ P2B-6）**一条都没回滚**。

**遗留 8 条一般级**：R2B-4 / R2B-6 / R2B-7 已由学习优化员闭合；R2B-10 部分闭合；
R2B-5（`hop-pads` 方向键无语义）是口径题、**R2B-8**（`fight-king` 训练场进不了 `meta.modes`，`src/engine/types.ts` 的 `GAME_MODES` 少一个 `training`）
**记给主管**；R2B-9 / R2B-11 属 `fight-king` 的平衡与对手轮转，改了会让本轮量 R2B-3 的那张胜率表作废，留后续。

`npm test` rebase 到分支尖后 **256 文件 / 6552 用例全绿**（本包净增 +3 文件 / +27 用例，其余是包 A 同期推的；本包 5 款单独跑 41 文件 / 754 例）；
`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
商标黑名单 0 命中；没引外部依赖、没改 `package.json` / lock、没碰 `src/engine/**` 与窗口 1 平台文件、没碰包 A / 包 C 的 10 款。

## Pack A

- 范围：`dot-maze` `fruit-stack` `pool-stars` `junqi-camp` `chess-garden`
- 详版：[`docs/qa/1.2-window2-round2-fixer-packA.md`](./1.2-window2-round2-fixer-packA.md)
- 输入：`docs/qa/1.2-window2-round2-tester-packA.md`（阻断 0 · 严重 0 · 一般 17）、`docs/qa/1.2-window2-round2-learner-packA.md`（列 12 条 / 落地 9 条）
- 结论：**17 条一般项逐条复验 —— 已被学习优化员清掉 12 条，仍在 5 条，本轮 5 条全修完，一条不留。阻断 0、严重 0 本来就是 0，第 1 轮三条严重项没有回潮。**

先复验再动手。复验是读当前 HEAD 的代码、指到具体那一行，不看学习优化员的自述：
`PA-DM-1` `PA-DM-3` `PA-FS-2` `PA-PS-2` `PA-PS-3` `PA-JQ-3` `PA-CG-1` `PA-CG-2` `PA-CG-3`
`R2-PA-1` `R2-PA-2` `R2-PA-5` 这 12 条确认已经消失，对应 commit 一个没回滚。

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| PA-DM-2 | 一般 | `dot-maze` | 卡片文案改口径：`meta.blurb` 原写「吃光小星星」，可界面上「⭐ 小星命」是**命数**、地图上一律叫「豆 / 能量豆」。改成「吃光每一颗豆子」，`maze.ts` 两处「小星星豆」注释一并对齐 |
| PA-FS-3 | 一般 | `fruit-stack` | `merge.ts:36` 与 `PLAN.md` 的「合一次几乎不回血」换成「合一次腾回来的地方很有限」。不上屏，但「血」是红线字，顺手补了一道全目录（含注释、含 `PLAN.md`）的源码级筛子 |
| PA-CG-4 | 一般 | `chess-garden` | 上屏「死」**全清 5 处**（测试员只登记了 1 处）：`meta.blurb` 的「请进死角」→「请进包围圈」；闯关目标句与残局连胜提示的「将死」→ 全款一直在用的「将杀」；攻略的「提前堵死」「逼得太死」→「提前堵住」「逼得太紧」 |
| R2-PA-3 | 一般 | `chess-garden` | 360px 棋盘格：走「允许溢出」这条路，窄屏给 `.cg-board` 补 `margin-inline:-10px` 让棋盘挣脱自己那层壳铺到整宽，一格 **38.9px → 41.4px**。残差**立为记录在案的例外**（见下） |
| R2-PA-4 | 一般 | `chess-garden` | 残局连胜后段：题面池 **35 → 41**（补 6 道题库现成余料，逐个用 `findForcedMate` 证明白方有 3 手内强制杀）＋ 抽出 `endlessChip(round, best)`，封顶后挂「· 已到最高档」、第二轮起挂「· 题面第 N 轮」。`endlessTier` / `endlessThinkMs` **一个数没动** |

**R2-PA-3 的例外**（题面允许的第二条路，算式写在详版第四节）：
360px 上这一款真正拿得到的是 `360 − 2×14.4（窗口 1 的 .screen 内边距）= 331.2px`，
而 8 列 × 44px = 352px，**短 20.8px，物理排不下**；这 20.8px 全在窗口 1 的平台样式里，本包不碰。
测试员按「360−20」算得 ≈42.5px 是漏了 `.screen` 那一层，实测只有 38.9px，比报告里还窄。
没有为凑 44 去压格子（`.cg-sq` 仍 40px + `aspect-ratio:1`，8×40 = 320 ≤ 331.2），所以**不重叠、不被裁**；
够不到 44 的只有棋盘格一类，五类按钮全 ≥ 44，键盘那条通路（`WASD`/方向键 `+ F`/`L` `+ G`/`K`）也是全的。
**这条例外带过期提醒**：算式里的两个内边距都是从样式表实时读出来算的，平台哪天收窄，断言会报红、例外自动作废。

**第 1 轮三条严重项复验（都没回潮）**：`PA-FS-1` 窄屏 44px（8 例全绿，含「窄屏那段里每个热区选择器都不许压到 44 以下」的兜底扫描）；
`PA-PS-1` 暂停吞输入（9 例全绿：滚球中途 `Esc`、蓄力中 `Esc`、键盘 / 击球钮 / 微调钮 / 台面拖动全挡住，恢复后照旧管用）；
`PA-JQ-1` / `PA-JQ-2` 双人各管各的光标（6 例全绿）。学习优化员的 `L2A-7` / `L2A-8` 也没绕过这三道闸。

**新增回归 27 例**（本包 5 款 **757 → 784**，既有断言只翻面、一条没删没放宽）：
`dot-maze/copy.test.ts` +1、`fruit-stack/round1-packa.test.ts` +3、
`chess-garden/levels.test.ts` +9、`chess-garden/round1-packa.test.ts` +14。
`PA-CG-4` 那道筛子是验过会红的：把「将杀」改回「将死」，源码字面量扫描立刻报错。

**接续复核（同岗第 2 名修复员，`6b9e960`，详版第七节）**：对着当前 HEAD 把五条落地项逐条重新指认，
五个 commit 一个没回滚、一行没改写；第 1 轮三条严重项再确认一次没回潮。
复核中发现 `R2-PA-4` 留了一处真空 —— 题面池靠 `FAM.xx.slice(N)` 那十一个偏移量和 `PLAN` **手工对齐**，
可原来的断言只看「41 个不重样」和「都有强制杀」，偏移量本身没人看着：
哪天有人调了 `PLAN` 某一段的 `count`，连胜就会悄悄开始发**孩子在闯关里已经做过的题**，而测试照样全绿。
补 5 例护栏（池子与 188 关不撞题、题面摆得出来、扩池只许往后追加、新收 6 道锁死标称步数、局号边界兜底），
其中前三条的红证是真跑出来的：把 `slice(12)` 写成 `slice(11)`、或把新收的 6 道挪到池子最前面，都立刻报红。
护栏一个难度参数都没碰。

**留给第 3 轮 1 条半**：`R2-PA-4` 的后半（对手过第 10 局继续加档、题面池做到上百个）——
属难度 / 内容重标定，加档要连 AI 胜率门槛一起重跑，本轮说好不动难度；
`R2-PA-3` 的 2.6px 残差已立例外，真要补上得动窗口 1 的 `.screen`，跨包。

`npm test` 在本包最后一个 commit `6b9e960` 上 **256 文件 / 6566 例全绿**，rebase 到分支尖 `97e4506` 之后 **258 文件 / 6604 例全绿**
（沿革：`255 / 6551` 包 A 自己那串 commit → `256 / 6561` rebase 到 `fa20033`，多出的 1 文件 / 10 例是包 B 同期推的
→ `256 / 6566` 接续复核的 5 例护栏 → `258 / 6604` rebase 到 `97e4506`，多出的 2 文件 / 38 例是包 C 同期推的。
**本包对全仓的净增始终是 +27 例、+0 文件**）；
`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
红线独立重扫过：本包 5 款**产品代码**（排除测试文件）商标 0 命中、上屏无血无死亡描写
（这一轮还反过来给 `fruit-stack` / `chess-garden` 各加了一道自动筛子）、角色只有朵朵 / 星星、
双键齐全（朵朵 `WASD+F+G` / 星星 `方向键+L+K` / `Esc` 暂停）、5 份 `meta.ts` 的 `import` 计数全是 0、
360px 热区只剩棋盘格一条记录在案的例外、没引 three.js / CDN / 任何外部依赖；
没碰 `src/engine/**` 与窗口 1 平台文件、没碰包 B / 包 C 的 10 款、没碰 `src/games/copy.test.ts` 这类全仓公共测试；
`src/styles.css` 只动了 `.cg-board` 那一条属于本包的窄屏规则。

## Pack C

- 范围（点名五项）：`gomoku` `match-stars` `rainbow-run` `ocean-munch` `xiangqi`
- 详版：[`docs/qa/1.2-window2-round2-fixer-packC.md`](./1.2-window2-round2-fixer-packC.md)
- 输入：`docs/qa/1.2-window2-round2-tester-packC.md`（阻断 0 · 严重 0 · 一般 8）、`docs/qa/1.2-window2-round2-learner-packC.md`（落地 7 条）
- 结论：**8 条一般项逐条复验后 8 条全都还在（学习优化员那 7 条是提示 / 文案 / 无障碍 / 求和门槛，与热区、红线词没有交集），本轮清掉 6 条，留 2 条给第 3 轮。阻断 0、严重 0 本来就是 0，第 1 轮 `gomoku` 那条相邻档阻断没有回潮。**

| 编号 | 级别 | 游戏 | 修了什么 |
| --- | --- | --- | --- |
| R2C-G1 | 一般 | `gomoku` | `.gmk-btns button`（悔棋 / 提示 / 确认落子 / 重摆 / 换玩法）36.8–39.6px → `min-height:44px`。`view.ts` 立了 `MIN_HIT_PX = 44` 常量，CSS 用模板串插进去 |
| R2C-G2 | 一般 | `gomoku` | `.gmk-seg button` 40.8–43.6px、`.gmk-mode` 36.0–39.0px 一并补到 44px；顺带把测试员标「⚠️ 临界」的 `.gmk-over-btn`（41.2–44.4px，换台设备就可能不够）也兜住 |
| R2C-X1 | 一般 | `xiangqi` | `.xq-btns button` 38.8–41.6px、`.xq-mode` 42.0–45.0px、`.xq-over-btn` 41.2–44.4px 补到 44px。**关键是 `@media (max-width:380px)` 那一档**——360px 上真正生效的是它，里面也显式写了 `min-height`，并有单独用例把那一段切出来量。顺带补了没被点名的 `.xq-rules-close`（36–39px） |
| R2C-R1 | 一般 | `rainbow-run` | `btnEndless` 字面量 h 42 → 44 直接撑；`btnCollection` 38×34、两处 `btnBack` 62×30 / 62×28 **画的照旧、热区兜到 44px 见方**（撑大了会压到章节标题与 y=40 起的 HUD 第二行） |
| R2C-O1 | 一般 | `ocean-munch` | 同一把尺：两处 `btnDex`（112×34 / 110×30）、四处 `btnBack`（76×36 / 74×30 / 62×30 / 80×34）热区全部兜到 44px 见方 |
| R2C-R2 | 一般 | `rainbow-run` | 上屏文案换口径：「给大王掉血」→「给大王卸护甲……卸一层 / 卸两层」；第 9 章第 12 关《广告墙走廊》→《霓虹招牌走廊》（障碍种类、任务目标、长度速度一个数没动）；写用例时顺手扫出的「不用死磕按键」→「不用非得掐准按键」 |

**热区这一类为什么不是一刀切**：五款里 `gomoku` / `xiangqi` 是 DOM + CSS，全局有
`* { box-sizing:border-box }`，`min-height:44px` 量出来就是 44px；
`rainbow-run` / `ocean-munch` 是纯画布，热区就是 `inRect()` 用的那个矩形，
而测试员点名的 10 处里只有 `btnEndless` 能直接把画出来的按钮撑到 44px，
其余 9 处都贴着标题或 HUD（局内的「◀ 回家」正下方 y=40 就是赛道进度条），撑大了会压字。
所以那 9 处走 `touchArea()`：**画的还是原来那颗，能点的范围往外扩到 44px 见方**，
扩到画布外的一截推回 0 —— 不推的话看着 44px、真能点的只有 42px。

**留给第 3 轮 2 条**：`R2C-G3`（`smart` 对 `normal` 和棋 13/36）与 `R2C-X2`（`easy` / `normal`
残局卷同分 36/36）—— 两条都**不是强弱倒挂**（gomoku 那对强档 21:2 没输过一局；
xiangqi 那对层数 1→2、预算 40→90ms、延时 380→460ms 三条硬指标严格递增），
要拉开区分度就得动 `ai.ts` 的权重或搜索层数 / 时间预算，本轮红线明令不碰，**只记录**。

**点名五项独立复验**（不引测试员表格，用 blob 哈希与 `git diff` 直接对）：
`gomoku` 相邻档回归全绿且 `ai.ts` / `tiers.test.ts` diff 为空；
`match-stars` 本包整个目录 diff 为空（无瞬变那条性质原样）；
`rainbow-run` 全仓 `three` 命中 0、`makeCamera` 仍旧只有 `index.ts:403`（初始化）与
`index.ts:415`（`cam.w !== w || cam.h !== h`）两个调用点、`view3d.ts` / `controls.ts` / `sim.ts` 与 1.1 逐字节相同；
`ocean-munch` `save.recordEndlessBest(meta.id, scoreDepth)` 仍在 `index.ts:1005` 且 `logic.ts` diff 为空；
`xiangqi` 只改了既有目录里的 `view.ts` / `view.test.ts` 两个文件。

**新增回归 +38 例 / +2 文件**（五款 1071 → 1109，既有断言一条没删没放宽，`it.skip` / `it.todo` 0 处）：
`gomoku/view.test.ts` +5、`xiangqi/view.test.ts` +4、`rainbow-run/touch.test.ts` 12、
`rainbow-run/logic.test.ts` +5、`ocean-munch/touch.test.ts` 12。
两份 `touch.test.ts` 除了量纯函数，还做**源码级巡检**：正则抓出 `index.ts` 里所有
`btnXxx = { … }` 的字面量热区逐条量 ≥ 44，并先放一条「抓得到东西」的哨兵，
防止正则哪天失效之后静悄悄全绿。

`npm test` rebase 到分支尖 `6b9e960` 之后 **258 文件 / 6604 用例全绿**（`6566 + 38`、`256 + 2`，账对得上）；
`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）；
本轮**反过来减少**了红线词（上屏文案的「血」「广告」「死」全部清零，有用例守着）、角色只有朵朵 / 星星、商标 0 命中、没引 three.js / 任何外部依赖；
`src/engine/**`、窗口 1 平台文件、`src/styles.css`、`package.json` / lock **diff 全部为空**（本包一行样式都不用往 `styles.css` 里加），包 A / 包 B 的 10 款一个字节没碰。

### Pack C · 接续复核（同岗第 2 名修复员）

对着分支尖把上面 6 条**逐条重新指认**（不看自述，重新量 CSS 与 `Rect`，
临时脚本取证后整目录删除、未入库）：**6 条全部真的落地，一条没打折**。
热区改前 → 改后，17 行逐个量过（详版第六节）：`.gmk-btns button` 36.8 → **44.0**、
`.gmk-mode` 36.0 → **44.0**、`.xq-btns button` 38.8 → **44.0**（窄屏那一档 35.6 → **44.0**）、
`btnCollection` 38×34 → **44×44 @ (311, 3)**、`btnBack`（跑动中）62×28 → **62×44 @ (6, 0)**、
`btnDex`（图鉴）110×30 → **110×44 @ (242, 1)** …… 十四处全部 ≥ 44px，
且扩出来的 44px **整块都落在画布里**（贴边的一律推回 0）。
主流程按钮一个没被弄坏：`.gmk-start` 47.6–51.2px、`.xq-seg button` 44.8–47.6px、
`.xq-start` 50.8–54.6px 都没被 `min-height` 拉动；`touchArea()` 对两边已 ≥44 的矩形**原样返回**，
所以复活 220×44、家长跳过 274×44、结算 132×44 / 132×46、玩法卡 320×44、选关格子约 46.7 宽 —— 一个像素没挪。

复核中补上的一处真空：**`R2C-R2` 只清到了上屏那一层**。
上屏的「血」「广告」确实归零了（有用例守着），但把筛子从「上屏文案」下沉到「源码字面量」再跑，
这一款还剩 7 处 —— `guide.ts:137`「跑到终点时**伤害**没打够」（**上屏**，与刚改好的 138 行「卸护甲」口径打架）、
`guide.ts:11`「能挡一次**伤害**的最实用」（上屏），以及 `index.ts` / `logic.ts` / `sim.ts` 里
「大王血条」「大王掉血」「大王打不打得死」那一批注释。
这些不上屏、不构成缺陷，但会让下一轮的词表扫描继续报红、每轮都得重新判一次「这是注释，不算」——
第 1 轮包 A 的 `PA-FS-3` 正是按这个理由把 `fruit-stack` 注释里的「回血」一并换掉并补了源码级筛子。
本轮照同一先例办：两句上屏改成「护甲还没卸完」「能替你挡一次撞」，五个文件的注释统一对齐到「卸护甲」口径，
**产品逻辑一个字节没动**（`bossHitsOf` / `bossDefeated` / `PERFECT_RUN_BOSS_DAMAGE` 原样，188 关参数零漂移）。
补 4 例源码级用例（与上一手那 5 例上屏级用例互不覆盖）：全目录 `*.ts`（排除 `*.test.ts`）扫
「血 / 尸 / 广告 / 内购 / 抽卡 / 充值 / 氪金」命中 0，外加一条「不联网、不引 three.js」的筛子。
**「死」故意不进这张表** —— `endless.ts` 里「原道被堵死 / 封死」是路况说法，
「无伤」更是十几个关名在用的褒义词（`noHit` 挑战），一刀切会误伤。
红证真跑过：把 `sim.ts` 那句改回「顺便给大王掉血」，筛子立刻报 `sim.ts 里出现了「血」`。

点名五项的硬约束独立重扫一遍，全部仍成立：`tiers.test.ts` **34 例全绿**且 `ai.ts` diff 为空；
`match-stars` 整个目录本轮 diff 为空、`Phase` 仍是八段（`fall` / `land` 没被并掉）；
`view3d.ts` / `controls.ts` 相对 `origin/game-1.1` **0 行**、全仓 `three` 命中 0、
`makeCamera` 仍旧只有 `index.ts:403` / `index.ts:415` 两个调用点；
`recordEndlessBest` 仍在 `index.ts:1005`、`logic.ts` 相对 1.1 仍是 +4/−0、`ENDLESS_TIERS` 本轮 diff 为空；
全仓 65 个目录里象棋只有 `xiangqi` 一套且 1.1 那 5 个文件全在原地；
学习优化员包 C 那 6 条一条没回滚；五款产品代码商标黑名单命中 0。
**`R2C-G3` / `R2C-X2` 复核后维持不修**：两条都是尺子的分辨率不足、不是强弱倒挂，
改动收益远小于打翻相邻档回归口径的风险，理由连同数据一起交给第 3 轮。

`npm test` 在分支尖 **258 文件 / 6608 例全绿**（沿革：`fa20033` 256/6552 →
包 A 的 `4e0e0da` + `6b9e960` 到 256/6566 → 包 C 六个 fix commit 到 258/6604 → 本节这一条到 258/6608）；
`npm run build` 通过（`tsc --noEmit` + `vite build`，PWA 预缓存 157 条）。
五款自带用例 **1071 → 1113（+42）**：`gomoku` 273 / `match-stars` 147 / `rainbow-run` 256 /
`ocean-munch` 174 / `xiangqi` 263。删掉的 `it` 0 条、`it.skip` / `it.todo` 0 处；
`src/engine/**`、窗口 1 平台文件、`src/styles.css`、`package.json` / lock 一个字节没动；包 A / 包 B 的 10 款没碰。
