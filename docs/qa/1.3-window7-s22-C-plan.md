# 1.3 第 22 步 · C 档 —— `poop-hero`「便便超人」视觉升级计划

分支 `game-1.3-window7`，基线 `origin/game-1.3`。只动 `src/games/poop-hero/**` 视觉与新增
`src/art/kit/cape.ts`；判定（`PLAYER_W/H`、`CROUCH_H`、`sweepT`/`dashT` 窗口）、豆豆怪行为、
章节数据、存档 key、`meta.ts` 一律不碰。

## 现状审美评测（已开代码核实,`src/games/poop-hero/index.ts`）

1. `drawHero`（661 行起）：披风两层纯色（683–696）、身体一个 `roundRect`（707）、头一个圆
   （720–723），**通体没有一处渐变**——「超人太简陋」属实；
2. 扫帚是两个 `roundRect` 拼的直棍（748–757），无残影、无握持手、无帚毛；
3. 冲刺气流直接 `emoji("💨")`（766 行附近），英雄时刻用系统字符打发；
4. 无奔跑姿态：站 / 跑 / 冲画面一模一样，只有 x 在动；
5. `drawMonster`（769 行起）糖果色底子可爱（保住），但清扫完成变花是 emoji `FLOWERS[]`
   （101 行定义、779/938/959 行使用）；
6. `render` 背景自查：八章共用同一套「圆角房 + 云」，无章节主题、无第二层视差、无深度层次
   （2.5D 补肉点）；地面两条平涂色带、无扫过痕迹；
7. `renderHud` 自查：全部裸 chip 一串，无卡片分组、无连击反馈；车尾气（1126）、尘土风
   （1160）、踩碎纸团粒子（359）也都是 💨 emoji，需一并清掉。

## 改造方案（只动皮肤）

- 新增 `src/games/poop-hero/visual.ts`：四·补一配色板 token（`phCapeOut/phCapeIn/phSuit/
  phBelt/phBroom/phClean/phShadow`）、四·补三动效时序常量、图层序清单、三态姿势 / 腿摆 /
  呼吸 / 细节降级 / 章节主题映射 / 变花帧等纯函数，以及 FX 粒子管理（残影 / 尾流 / 星花，
  `destroy` 一把归零）；豆豆怪糖果色常量原样搬入（值不变）。
- 新增 `src/art/kit/cape.ts` + 单测：披风三段形态（垂落 / 后飘 / 拉直）速度阈值映射、
  180ms ease-out 形态过渡、控制点几何；kit 已有文件只 import（`sparkle.ts` 的星花池）。
- `drawHero` 重写为九道工序（落影 → 披风外层渐变 + 亮边 → 内衬 → 身体三停渐变 + 菱形高光 +
  描边 → 腰带双色圆扣 → 头 + 刘海 + 下巴阴影 → 眼罩反光斜杠 → 手套靴子 + 腿摆 → 状态附加：
  呼吸 / 前倾 10° / 前倾 18° + 残影两帧 + 自绘星星尾流 3 颗替换 💨）。
- 扫帚升级：木柄渐变 + 金色捆环 + 8 根须状帚毛扇形，挥动弯曲 + 弧形残影一道（只在
  `sweepT > 0` 窗口）+ 接触豆豆怪星花 4 颗。
- 豆豆怪三停渐变体积 + 被扫中转圈眩晕星；变花改自绘五瓣花（花心渐变 + 3 帧展开，reduced 一帧）。
- 章节主题场景按 `chapterIndex % 3` 轮换：街道（路灯 + 橱窗）/ 公园（树 + 长椅）/ 星空屋顶
  （烟囱 + 月亮），双层视差补 2.5D 深度；地面加 `phClean` 发亮干净带（纯装饰，从已清扫状态
  只读推导）。
- HUD 卡片化（进度 / 连击 / 章节），连击徽章 500ms 发光脉冲；reduced 下呼吸 / 残影 / 尾流 /
  脉冲全停，静态渐变与一帧扫除反馈保留。
- 测试只增不减：新增 ≥ 12 个视觉用例（token / 三态 / 无 💨 无 FLOWERS / 披风阈值 / 判定盒
  只读 / 残影窗口 / 腿摆 4 帧 / 降级阈值 / 章节主题 / reduced / destroy 清零 / 时序常量）。
