# r34 Playbook · 测试员 C → A/B

**基线**：`origin/game-1.3` @ **`17356717`**。零改 `src/**`（本文档作者）。三视口 390×844 / 915×412 / 1024×768。热区 ≥44。C-8 禁改 `SKY_H`。

**本轮新号**：**N-162**、**N-163**、**N-164**。下一空号 **N-165**。

**撞车**：号段以先合入 `game-1.3` 的为准。r22–r33 学习 PR 未合主干时，本轮仍从 N-162 起编；若主干已占用 N-162+，顺延并在笔记勘误。

---

## 红线（抄自 r22–r33）

1. 零改学习员职责外的 `src/**`（C 只交两份 md）。
2. 已合号只回归：N-99/97/100/109、N-108 无尽画廊、N-12/10/3/55/81、C-8、N-90、N-105、N-75…N-91、N-60/61/62、N-87/88。
3. N-110…116 永久跳过。
4. 并号勿新开：见 `trio-r34-learn-notes.md` §0。
5. 在途勿重做：PR #94 / #96 / #99 / **#104**（平板横屏/16px/l99 可滚）/ #105（N-159…161）。
6. 不要用 r21 草稿改写 N-108。

---

## 本轮工单

### N-162 · B · 排行榜 `<summary>` 无高度

| 项 | 内容 |
| --- | --- |
| 文件 | `src/games/orb-arena/index.ts` ≈L97 `.oa-board summary`；`src/games/snake-royale/index.ts` ≈L111 `.sr-board summary` |
| 现状 | 仅 `cursor:pointer; font-size:16px`，无 padding / min-height |
| 不做 | 不改 `.oa-back`（N-135）；不改 snake 返回（N-147） |
| 验收 | 三视口点开排行榜折叠；热区高 ≥44；折叠内容仍可读 |

### N-163 · B · `.cg-log-sum`

| 项 | 内容 |
| --- | --- |
| 文件 | `src/styles.css` ≈L2331 `.cg-log-sum`（L3260 wrap 提级到 14px）；节点 `chess-garden/view.ts` ≈L183 |
| 现状 | `font-size:13px`，无 min-height（wrap 字号已 14） |
| 不做 | 不改棋盘 `.cg-sq`；不重开 `.xq-seg` |
| 验收 | 打开记谱折叠，summary 高 ≥44；字号债若并 N-125 族则本号只补高度 |

### N-164 · A · summary 静态巡检

| 项 | 内容 |
| --- | --- |
| 范围 | `src/games/**/*.{css,ts}` 与 `src/styles.css` 中 `summary` / 明确点到折叠标题的 `*-sum`（本库排行榜规则写在 `index.ts` 内联 CSS） |
| 规则 | 可点折叠标题必须 `min-height`≥44 或 padding 估高 ≥44 |
| 豁免 | 装饰、禁用、被更大热区包裹且自身不可点 |
| 不做 | 不替代 N-161 写死宽高扫描；不扫装饰 span |

---

## 建议顺序

1. A 先合 **N-164**（或与 B 并行，但巡检应能抓住 N-162/163 现状为红）。
2. B 修 **N-162**（两游戏可同一 PR）。
3. B 修 **N-163**。
4. 回归：N-99 数独可滚、N-97 农场钉底、N-100 进场锚定、orb-arena 返回（N-135）、仓鼠迷宫（N-156）勿回归破损。

---

## 本拍未覆盖

无头 Chrome 本轮未跑。A/B 必须以真机或无头三视口补测 N-162/163 的点击矩形，不能只看 CSS 行。
