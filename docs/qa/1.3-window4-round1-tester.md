# 1.3 窗口 4 · 第 1 轮视觉验收 · 测试员报告

> 范围：本窗 9 款（非全库 39 款）：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
> 规格模板：`docs/plan-1.3-step27-A-tester.md`（六大专项收窄为窗口 4 的 9 款）。
> 权限：只改 `*.test.ts` 与本报告；不改绘制代码；发现的问题交本窗 fixer。

## 一、全量跑数据

| 项 | 结果 |
| --- | --- |
| `npm ci` | 绿 |
| `npm test`（改动前基线复核） | **707 文件 / 15198 例全绿**（174.4s），与统筹宣称一致 |
| `npm test`（加本轮 30 例后收尾复跑） | **708 文件 / 15228 例全绿**（见文末） |
| `npm run build` | 绿（tsc --noEmit + vite 599ms；PWA precache 188 项 5226.42 KiB；dist 共 5604 KB） |
| `npx vite preview` 冒烟 | 绿（本轮全部浏览器实测都跑在 preview :4173 构建产物上） |

9 款构建产物体积：duo-rush 68KB / duo-arena 56KB / duo-vs-star 76KB / sling-birds 104KB / candy-swing 104KB / gold-hook 48KB / garden-guard 60KB / sprout-defense 60KB / monster-crisis 64KB（均为未 gzip 的 chunk 尺寸）。

### 盘点：9 款 plan ↔ 提交对账

`git log origin/game-1.3-window4 --not origin/game-1.2` 逐款核对，**9/9 都有 feat + test 成对提交**，与 `docs/plan-1.3-step11/12/13-*` 一一对应，无漏做、无做了没推：

| 款 | 视觉升级提交 | 契约测试提交 |
| --- | --- | --- |
| duo-rush | `131c797` `8a6a5e2` `57e41ab` | art 契约 29 例（合在 131c797） |
| duo-arena | `87bd95b` `b7e8aeb` | `83a319a`（18 例） |
| duo-vs-star | `8af5832` `f69a526` | `02dd0b7`（19 例） |
| sling-birds | `9dd8e6e` `f233ddf` | `4a2165f`（23 例） |
| candy-swing | `c511bbc` | `5a3215a`（19 例） |
| gold-hook | `6f3d5a7` `0eb5aab` `077a7e5` | `6d8f8ca`（24 例） |
| garden-guard | `a4ac9eb` | `7f965c4`（25 例） |
| sprout-defense | `24cd863` | `e2b56b`（19 例） |
| monster-crisis | `b1d2fd8` `044415d` | `d5a82cb`（26 例） |

注：`src/art/kit/` 目前尚未建立（全库皆然，非本窗缺口）；duo-rush / duo-arena 的 art.ts 头注均已登记「待上移共享 kit」的函数清单（coinFrames / drawSparkle / drawCrown / drawHeart / drawKitCoin / drawFacetStar），签名相互对齐，留给全局步骤合并。

## 二、扫描方法概述

- 静态：ripgrep 扫 9 款全部非测试 `.ts`（emoji 范围 U+1F300–1FAFF / 2600–27BF / 2B00–2BFF、商标词表中文 includes + 英文全词）；art.ts 逐函数读码核对渐变停靠点 / 高光 / 描边 / 落影。
- 动态：`npm run build` + `vite preview :4173` + 无头 Chrome（puppeteer-core 驱动系统 Chrome，**未写入仓库任何依赖文件**）。360×640 与 320×568 双档、DPR2、触屏仿真；逐款深链 `#/game/<id>` 进入 → 点开始按钮/画布进玩法画面 → 度量 `scrollWidth-innerWidth`、逐元素越界、触区 <40px、字号 <14px → 截图目检。
- 16px 灰度：经 vite dev 服务把各款 art.ts 的真实绘制函数 import 进无头 Chrome，把双人角色各自画到 20px 画布（角色 16px），逐像素算**剪影 XOR 占比**（占并集像素的百分比）与**重叠区平均灰度差**（0–255）。

## 三、六大专项

### 专项① 火柴人 / 草稿圆 / emoji 直出

方法：全 9 款 grep `fillText` 行内 emoji（排除注释行）+ 变量拼接链人工追踪 + 玩法画面截图目检主角与核心道具。

| 款 | canvas fillText emoji（行） | 主角/核心道具画法 | HUD 装饰 emoji 登记 |
| --- | --- | --- | --- |
| duo-rush | 0 | 双主角 2 头身矢量（drawRunnerSprite）+ 8 帧旋转金币 + 4 种材质障碍 ✅ | HUD 一行内 🌸⭐🪙❤️🤍✋☁️🫧🎊🧲🤝🌿🌈（canvas 变量拼接，≈13 种，仅 HUD 条） |
| duo-arena | 0 | 花/星双吉祥物 + 查表脸 + 金币/礼盒/迷糊泡全绘制 ✅ | canvas：人机档位徽章（🐣🙂😎🔥 一次一枚）+ 🤝 让分章；DOM 菜单若干 |
| duo-vs-star | 0 | 12 角色脸谱 + 头饰全绘制；出界金星、云朵重生均矢量 ✅ | DOM 触控键 ✋💥🤝、栏位 ☁️🫧🔨；canvas 0 |
| sling-birds | 0 | 五鸟体型互异 + 三种豆豆 + 材质碎片全绘制 ✅ | DOM 章节标签/星显示（★☆ 为字体字符非 emoji） |
| candy-swing | 0（art.ts:213 为注释） | 螺旋糖果/星星/怪物全绘制，💜 已清零 ✅ | DOM 选关页少量 |
| gold-hook | 0 | 11 种矿石矢量家族 + 双矿工 + 手绘问号 ✅ | DOM 商店/模式卡 emoji |
| garden-guard | **6**（index.ts:1474/1701/1756/1829/2107/2229） | 塔/怪/子弹/星全绘制 ✅；但 canvas 标题🌼、提示💡、造价卖价🌸 直出 | canvas 另有 hud12 变量拼接 🌸💗🤍 + 章节 st.emoji×13 + 菜单 c.emoji×3 → **W4R1-01** |
| sprout-defense | 0 | 坚果缺口/啃咬帧/kit 图标全绘制，emoji 清零 ✅ | canvas 0；DOM 攻略文案少量 |
| monster-crisis | 0 | 双画家英雄 + 五怪材质化 + 绘制元气糖 ✅ | DOM 챕chip 🧴🖌️、触控 🎨 |

无「纯圆 + 两点眼」草稿画法残留；1.2 反面名单款均不在本窗。**严重 0**；garden-guard 的 canvas emoji 记 W4R1-01（一般）。

### 专项② 金币 / 星星 / 收集物体积

方法：读码核对绘制函数的渐变停靠点数 / 高光 / 描边 / 落影，配玩法截图双证；已沉淀为静态断言（见第六节）。

| 款 | 核心收集物 | 渐变 | 高光 | 描边/落影 | 判定 |
| --- | --- | --- | --- | --- | --- |
| duo-rush | 金币 8 帧（drawCoinFrame） | 线性 2 停 | 白高光椭圆（翻面换边） | 内环描边 + 厚度棱 | ✅ |
| duo-arena | drawKitCoin | 径向 **3 停** | 左上高光弧 | 深金内环 + 侧厚 | ✅ |
| duo-vs-star | drawGoldStar | 线性 2 停 | ✗（顶停近白替代） | 0.5 透明描边 | ⚠️ W4R1-07（建议） |
| sling-birds | 星级（DOM ★）/主角小鸟 | 径向 2 停（鸟体） | 白高光椭圆 | 1.8px 描边 + 尾羽暗色 | ✅ |
| candy-swing | drawGoldStar | 径向 2 停 | 中心小高光星（0.85 白） | 2px 深金描边 | ✅ |
| gold-hook | drawOre 金块家族 | **✗ 平涂 fill** | skin.lit 高光椭圆 + 2s 扫光 | 1.6px 描边 + 落影 | ⚠️ W4R1-05（一般） |
| garden-guard | drawGoldStar | 线性 2 停 | 白高光点 0.85 | r*0.14 描边 | ✅ |
| sprout-defense | drawClearStar | 线性 2 停 | 白描边高光弧 | 描边 | ✅ |
| monster-crisis | drawCrumb 元气糖 + bodyGrad | 径向 2 停 | 高光点 | 描边 | ✅ |

「平涂圆内贴数字/星字符」为 0，**严重 0**。

### 专项③ 双人角色可区分（16px 灰度量化）

方法：真实绘制函数画到 16px，剪影 XOR 占比（并集%）+ 重叠区平均灰度差（0–255）。通过门槛：剪影/头饰/附件三通道至少两通道在 16px 灰度下可辨。

| 款 | 双人对 | 剪影 XOR | 重叠灰度差 | 16px 判定 | 备注 |
| --- | --- | --- | --- | --- | --- |
| duo-arena | 花 vs 折纸星 | **42.0%** | **84.8** | ✅ | 形状+灰度双通道都远超线 |
| gold-hook | 双矿工（drawCrew 0/1） | **30.6%** | 26.1 | ✅ | 单人玩法，双吉祥物同屏 |
| duo-vs-star | duoduo vs xingxing | 6.7% | 19.1 | ⚠️ | 头饰通道 2.2px 边界；团队赛另有线型环（实线/虚线/点线/点划线）兜底 → W4R1-04 |
| duo-rush | 手绘双主角 | 4.3% | 7.8 | ❌（16px） | 实际渲染 ~49px 时花苞/星呆毛/裙摆/披风可辨；默认还有照片头像 + SEAT_RING 座位色环 + 双半屏主题色 → W4R1-03 |
| monster-crisis | 双画家英雄 | **0%** | 11.6 | ❌ | 剪影完全重合（圆身+同形贝雷帽），唯一形状通道帽徽 ~3.6px → **W4R1-02（严重）** |

「只靠颜色区分」严格命中 1 款（monster-crisis 16px 下帽徽消失后仅剩颜色）。其余非双人款（sling-birds / candy-swing / garden-guard / sprout-defense）不适用。

### 专项④ 2.5D 纵深（点名款 duo-rush）

- **duo-rush 伪 3D 路面（点名）✅**：`view25d.ts` 针孔透视 `CAM_DEPTH=34`，三车道向地平线（0.32 高）收拢；雾从 95m 起到 190m 全遮（`fogAlpha`）；横向网格线 12m 间隔随里程后掠（`gridLineZs`）；三层视差：云 0.03 / 近景剪影 0.26 / 中景（`parallaxOffset`）；跳跃抛物线 + 下滑压扁 + 换道 9° 侧倾。360px 实测截图：两半屏车道收敛清晰、金币近大远小、护栏透视正确。
- 其余 8 款按「纵深表达不退化」核：gold-hook 矿洞三层视差（远洞穴/中矿脉/近岩壁，明暗 0.16+0.05i 分层，**刻意不做透视**——钩角瞄准需要，1.2 数学没动）✅；sling-birds 中景剪影 + 草丛带 + 地面阴影 ✅；candy-swing MID_PARALLAX=0.15 中景层 ✅；duo-vs-star 平台落影 + 云层 ✅；duo-arena 果冻垫 + 场地厚度 ✅；三款塔防为俯视/侧视口径，落影与体积渐变齐全 ✅。退化 0。

### 专项⑤ 360px / 320px 布局实测

方法：无头 Chrome 360×640 与 320×568（DPR2 触屏仿真），每款「入口页 + 玩法画面」两态度量 + 截图（18 组截图目检）。

| 款 | 360 溢出 px | 320 溢出 px | 触区 <40px | 字号 <14px（px 值） |
| --- | --- | --- | --- | --- |
| duo-rush | 0 | 0 | 0 | 无 |
| duo-arena | 0 | 0 | 0 | 模式卡副文案 11.5px |
| duo-vs-star | 0 | 0 | 0 | 说明段 12.5px ×2 |
| sling-birds | 0 | 0 | 0 | 星级符号 ☆ 9px |
| candy-swing | 0 | 0 | 0 | 副文案 12px ×3、星 10px |
| gold-hook | 0 | 0 | 0 | 卡片副文案 12–13px ×2 |
| garden-guard | 0 | 0 | 0 | 无 |
| sprout-defense | 0 | 0 | 0 | 无 |
| monster-crisis | 0 | 0 | 0 | 星符号 ★ 10px |

**横向溢出 0、遮挡 0、触区违规 0 —— 阻断 0**。字号 <14px 共 8 处，全部是次要副文案与星级符号（1.2 存量样式，无一因 1.3 视觉升级引入），登记 W4R1-06（一般）。garden-guard 的 hud12 还内置了「字号 ≥14 否则 fits=false」的自守护并有契约测试，360px 实测 HUD 一行放下。

### 专项⑥ 商标扫描

方法：33 个中文词 + 18 个英文词（全词匹配）扫 9 款全部非测试源码 + 逐款截图目检造型。

- 文案命中：**0**。初扫的 12–20 处“命中”全部是误报：`chimeLevelsAt` 撞 "elsa" 子串、`frozenUntil`/`FROZEN_LEVEL_COUNT` 撞 "frozen"、以及各款契约测试文件里自带的黑名单词表。
- 造型目检：sling-birds 五鸟为马卡龙配色 + 腮红圆眼，无「粗连眉红鸟」特征，靶子是豆豆非绿猪；monster-crisis 双英雄为贝雷帽小画家（非红帽工装）；duo-vs-star 12 角色均为本产品原创谱系；gold-hook 矿工为原创娃娃造型。相似造型命中 **0**。
- **阻断 0**。已沉淀为 9 款 × 51 词的常驻断言。

## 四、横向抽查（本窗 9 款范围内）

- **destroy 泄漏**：9/9 款「进 → 看 → 退」×5 后回首页，1 秒内残留 rAF tick 全部为 **0**。
- **性能**：CPU 4× 节流下玩法画面数 3 秒 rAF：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / monster-crisis 全部 **60fps**（无一掉到 30 以下）。
- **prefers-reduced-motion**：系统开关模拟下 duo-rush / garden-guard 正常进玩法画面；9 款 art 层全部带 reduced/calm/soft 参数分支（源码核对：duo-rush sparkleCount→0、金币扫光停成静态高光、duo-arena 公转星静止、monster-crisis 挥击弧痕关闭等），各款既有契约测试已覆盖弱动效分支。
- **光照一致性**：抽 9 款渐变方向全部左上亮 → 右下深（duo-rush drawCoinFrame、duo-arena drawKitCoin 径向originanchor 左上、monster-crisis bodyGrad(-0.32,-0.38)、sling-birds bodyGrad(-0.35,-0.4)、duo-vs-star drawCharBody(-0.32,-0.38)）；描边 1.5–2px 口径一致（duo-vs-star 1.5 / duo-arena 2 / candy-swing 2 / sling-birds 1.8）✅。

## 五、问题清单

#### W4R1-01　garden-guard：canvas 里残留 emoji 字体直出　**【一般】**
- **位置**：`src/games/garden-guard/index.ts:1474,1701,1756,1829,2107,2229`（🌼 标题 ×2、💡 提示 ×2、🌸 造价/卖价 ×2）；`src/games/garden-guard/hud12.ts:59-70`（HUD 左段 `🌸 ${petals}`、右段 💗/🤍 爱心）；`index.ts:1510/1547/1797`（章节 st.emoji ×13、菜单卡 c.emoji ×3）。
- **现状**：九款里唯一在 canvas 上以系统 emoji 字体渲染货币/爱心/图标的款；跨设备字体形状不受控（gold-hook art.ts 注释里已写明这条理由并自绘了全部图标）。截图上 360px 下显示正常，但与其余 8 款「canvas emoji 清零」口径不齐。
- **期望**：至少把**每帧都在画的 HUD 货币 🌸 与爱心 💗🤍**换成绘制资产（花瓣可复用 drawFace 同级的小花画法、爱心可抄 candy-swing drawHeart 签名）；标题/提示的 🌼💡 属菜单层装饰，可留但要登记。
- **量化**：fillText 行内 emoji 6 行（已钉在 `window4-visual-scan.test.ts`，修复后应改 0）；HUD 变量拼接另有 3 类。
- **建议责任文件**：`garden-guard/index.ts`、`garden-guard/hud12.ts`、`garden-guard/art.ts`。

#### W4R1-02　monster-crisis：双英雄 16px 灰度不可分　**【严重】**
- **位置**：`src/games/monster-crisis/art.ts` `HERO_SKINS` / `drawHero`（143–332）。
- **现状**：16px 灰度量化 —— 剪影 XOR **0%**（两人轮廓逐像素完全重合：同尺寸圆身 + 同形贝雷帽 + 同形围裙），重叠区平均灰度差仅 **11.6/255**。唯一形状通道是帽徽（HERO_R=14 时 3.6px，缩到 16px 后 ≈2px，灰度下不可辨，且双徽都是白色）。即色弱玩家在双人合作/对战里只能靠站位记人。
- **期望**：剪影/头饰/附件三通道至少两通道在 16px 灰度可辨。最小改法：P2 贝雷帽换形（如尖顶画家帽或帽揪揪改星形并放大到 r*0.5）+ 两人围裙下摆形状差异化（P1 圆摆 / P2 锯齿摆），不动 HERO_R 判定。
- **建议责任文件**：`monster-crisis/art.ts`（drawHero 绘制层，判定字段不动）。

#### W4R1-03　duo-rush：手绘双主角 16px 剪影弱可分　**【一般】**
- **位置**：`src/games/duo-rush/art.ts` `drawRunnerSprite`（373–511）。
- **现状**：16px 灰度 XOR 4.3%、灰度差 7.8/255——花苞/星呆毛（headR*0.32≈1.1px）与裙摆/披风在 16px 像素网格下几乎消失。**缓解因素**：本款实际渲染尺寸 ~49px（360px 宽、RUNNER_Z=2 时 laneWidth*0.6），三通道全部可辨；默认开启照片头像模式（drawAvatarBody + SEAT_RING 座位色环）；两人各占半屏且天空/路面主题色成对（粉/蓝）。
- **期望**：呆毛比例放大一档（headR*0.32→0.5）或 P2 身体压暗一停，把 16px 灰度差拉到 ≥20。
- **建议责任文件**：`duo-rush/art.ts`。

#### W4R1-04　duo-vs-star：duoduo vs xingxing 16px 处于及格线边界　**【一般】**
- **位置**：`src/games/duo-vs-star/art.ts` `drawCharTrait`（301–457）。
- **现状**：16px 灰度 XOR 6.7%、灰度差 19.1/255（#ff9ec4 vs #ffd75e）。头饰通道（星呆毛 r*0.34≈2.2px）边界可辨；团队赛有线型环（实/虚/点/点划）第四通道兜底；12 角色人人有头顶剪影特征。
- **期望**：星呆毛 r*0.34→0.5、花饰半径同步放大，16px XOR 拉到 ≥10%。
- **建议责任文件**：`duo-vs-star/art.ts`。

#### W4R1-05　gold-hook：金块体表无渐变（平涂 + 高光 + 描边 + 落影）　**【一般】**
- **位置**：`src/games/gold-hook/art.ts` `drawOre`（513–732）与 `ORE_SKIN`（458）。
- **现状**：金块家族 fill 是单色平涂（#FFD264 等），体积靠 skin.lit 高光椭圆 + 1.6px 描边 + 落影 + 每 2 秒斜向扫光四重线索撑着；按模板「必须 ≥2 停渐变」不满足，但绝非「平涂圆贴字」，故记一般不记严重。
- **期望**：币面加 2 停线性渐变（lit→fill），与 duo-arena drawKitCoin 的全产品金币标准对齐；扫光与描边保留。
- **量化**：`window4-visual-scan.test.ts` 已钉「现状无渐变 + 三重体积线索不许再丢」，修复后取反渐变断言。
- **建议责任文件**：`gold-hook/art.ts`。

#### W4R1-06　5 款 8 处次要文案字号 9–13px　**【一般】**
- **位置与量化**：duo-arena 模式卡 small 11.5px；duo-vs-star 说明段 12.5px ×2；sling-birds `.slb-stars` 9px；candy-swing 副文案 12px ×3 + 星 10px；gold-hook 卡片副文案 12/13px；monster-crisis `.l99-star` 10px。
- **现状**：全部为 1.2 存量 DOM 样式（选关页副文案与星级符号），**无一因 1.3 视觉升级引入**，360/320px 双档均不溢出不遮挡——不构成「升级把手机玩家挤出去」的阻断口径。
- **期望**：星级符号属图形可豁免；纯文字副文案建议逐步提到 ≥14px 或并入主文案。
- **建议责任文件**：各款 index.ts 的 CSS 段（fixer 酌情，非本轮必修）。

#### W4R1-07　duo-vs-star drawGoldStar 无独立高光点　**【建议】**
- **位置**：`src/games/duo-vs-star/art.ts:96-112`。
- **现状**：2 停渐变 + 描边有，独立白高光无（顶停 #fff2b8 近白充当受光面）；candy-swing / duo-arena / garden-guard 的同名函数都有第三层高光。跨款一致性建议，不扣分。
- **建议责任文件**：`duo-vs-star/art.ts`。

### 分级统计

| 级别 | 数量 | 编号 |
| --- | --- | --- |
| 阻断 | **0** | — |
| 严重 | **1** | W4R1-02 |
| 一般 | **5** | W4R1-01 / 03 / 04 / 05 / 06 |
| 建议 | **1** | W4R1-07 |

## 六、本轮新增机器化用例

`src/games/__tests__/window4-visual-scan.test.ts`，**30 例**，全绿：

- 专项⑥：9 款 × 51 词商标黑名单 0 命中（9 例）；
- 专项①：8 款 canvas fillText emoji = 0 + garden-guard 现状 6 处钉住防恶化（9 例，W4R1-01 修复后把 6 改 0）；
- 专项②：7 款收集物 ≥2 停渐变 + 描边契约 + monster-crisis 元气糖 + gold-hook 现状钉住（9 例，W4R1-05 修复后取反渐变断言）；
- 专项③：duo-rush / duo-arena / monster-crisis 形状通道源码契约（3 例）。

## 七、结果矩阵（9 款 × 六专项）

| 款 | ①emoji/火柴人 | ②收集物体积 | ③双人可分(16px) | ④2.5D 纵深 | ⑤360px | ⑥商标 |
| --- | --- | --- | --- | --- | --- | --- |
| duo-rush | ✅ | ✅ | ❌* | ✅（点名款） | ✅ | ✅ |
| duo-arena | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| duo-vs-star | ✅ | ✅ | ❌* | ✅ | ✅ | ✅ |
| sling-birds | ✅ | ✅ | —（单人） | ✅ | ✅ | ✅ |
| candy-swing | ✅ | ✅ | —（单人） | ✅ | ✅ | ✅ |
| gold-hook | ✅ | ❌* | ✅ | ✅ | ✅ | ✅ |
| garden-guard | ❌* | ✅ | —（单人） | ✅ | ✅ | ✅ |
| sprout-defense | ✅ | ✅ | —（单人） | ✅ | ✅ | ✅ |
| monster-crisis | ✅ | ✅ | ❌ | ✅ | ✅ | ✅ |

`❌*` = 一般级（见问题清单编号）；`❌` = 严重级 W4R1-02；`—` = 该专项不适用。**阻断级全零**（商标 0 命中、360px 新溢出 0）。

## 八、收尾复跑

- fetch → rebase `origin/game-1.3-window4`（保留他人文件，报告只写本文件）；
- `npm test`：**708 文件 / 15228 例全绿**（本轮 +1 文件 +30 例，只增不减）；
- `npm run build`：绿。
- 合并态复验与合并态 SHA 由本窗 fixer 执行，本报告配合提供问题清单与钉住断言。
