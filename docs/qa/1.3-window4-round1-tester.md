# 1.3 窗口 4 · 第 1 轮视觉验收 · 测试员报告

> 状态：**扫描计划已提交，执行中**。
> 范围：本窗 9 款（非全库 39 款）：duo-rush / duo-arena / duo-vs-star / sling-birds / candy-swing / gold-hook / garden-guard / sprout-defense / monster-crisis。
> 规格模板：`docs/plan-1.3-step27-A-tester.md`（范围收窄为窗口 4 的 9 款）。
> 权限：只改 `*.test.ts`（优先本窗 9 款）与本报告；不改绘制代码；别人的报告一字不碰。

## 扫描计划

1. **全量跑**：`npm ci` → `npm test`（对照统筹基线 707 文件 / 15198 例全绿）→ `npm run build`（记录体积）→ `npx vite preview` 冒烟。
2. **盘点**：9 款的 plan 文档（step11-A/B/C、step12-A/B/C、step13-A/B/C）与实际提交对照；art/绘制文件清单。
3. **六大专项**（9 款 × 六专项矩阵，逐条量化证据）：
   - ① 火柴人 / 草稿圆 / emoji 直出扫描：grep `fillText(` emoji 直出、纯圆两点眼画法；主角与核心道具裸 emoji 记「严重」，装饰性 HUD emoji 登记数量。
   - ② 金币 / 星星 / 收集物体积：≥2 停渐变 + 高光 + 描边/落影之一；平涂圆记「严重」。重点：gold-hook 金块、duo-vs-star 星星、candy-swing 糖果、sling-birds 得分星。
   - ③ 双人角色可区分：duo-rush / duo-arena / duo-vs-star 双人款，16px 灰度下剪影 / 头饰 / 附件三通道至少两通道可分；只靠颜色的记「严重」。
   - ④ 2.5D 纵深：duo-rush 伪 3D 路面（透视收敛 / 视差 / 投影）为点名款；其余款查纵深表达是否缺失或退化。
   - ⑤ 360px 布局：360×640 与 320px 极限实测；溢出 / 遮挡 / 字号 <14px / 触区 <40px 逐款记像素数；新溢出记「阻断」。
   - ⑥ 商标扫描：grep 素材与文案（4399 / 任天堂 / 迪士尼 / 马里奥 / 皮卡丘 / Angry Birds 相似度等）；任何一处记「阻断」。
4. **横向抽查**（9 款内）：光照约定一致性、`prefers-reduced-motion`、destroy 泄漏（进→看→退→再进 ×5）、粒子性能。
5. **沉淀**：机器化扫描断言写入 `*.test.ts`（优先本窗 9 款目录）。
6. **报告**：本文件补全六专项各节 + 问题清单（位置 / 现状 / 期望 / 严重度 / 建议责任文件）+ 9×6 结果矩阵；收尾 fetch → rebase → `npm test` / `npm run build` 全绿 → push `origin game-1.3-window4`。

## 待补：全量跑数据

## 待补：六大专项结果

## 待补：结果矩阵（9 款 × 六专项）
