# 1.3 窗口 7 · 第 1 轮视觉验收 · 监督修复员报告（C 档）

> 分支：`game-1.3-window7`（基线 `origin/game-1.3`）。
> 角色：窗口 7 第 1 轮监督修复员（C 档）。只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 本窗归属文件），不碰判定、数值、关卡数据、存档 key、玩法测试断言。
> 输入：A 档测试员报告 `docs/qa/1.3-window7-round1-tester.md`（已读，未改）；B 档报告 `docs/qa/1.3-window7-round1-learner.md`（开工时尚未产出，先按 A 档严重项 + 自查底线动手，B 档出稿后合并跟进）。
> 范围：仅本窗九款 —— `fruit-catch` · `fruit-slice` · `snake-snack` · `lianliankan` · `puzzle-tiles` · `memory-cards` · `landlord-cards` · `fishing-star` · `poop-hero`。

## 〇、修复计划（开工首 commit）

修复顺序铁律：阻断（0 条，A 档判定清零）→ 严重 6 条 → 一般 5 条 → 建议 2 条。每修一处一个 commit（`fix(visual-r1): <款> <问题>`），配套用例只增不减。

| # | 级别 | 款 | 问题（来源 = A 档报告条目号） | 计划修法 |
| --- | --- | --- | --- | --- |
| P1 | 严重 | poop-hero | 香香星 = 裸 ✨（A-4，专项①②双重不达标） | 改用 kit `sparkle.traceStar` + 径向渐变 + 描边 + 高光自绘 |
| P2 | 严重 | poop-hero | 地面垃圾 `item.emoji` 裸 emoji（A-3，核心道具） | 按垃圾类型自绘小物件（≥2 停渐变 + 描边 + 落影），emoji 仅当类型钥匙 |
| P3 | 严重 | poop-hero | 分类桶功能图标裸 emoji（A-5） | 三枚自绘小图标（可回收 / 厨余 / 其他），与桶身同规格 |
| P4 | 严重 | poop-hero | 双人 16px 灰度不可分（A-9） | 拉开披风/衣服灰阶差 ≥25 + 给 2P 加剪影级附件（星星发卡） |
| P5 | 严重 | landlord-cards | AI 头像 🐰🐼 裸 emoji（A-2） | 自绘 SVG 头像（小兔/熊猫，与朵朵星星同规格描边渐变） |
| P6 | 严重 | puzzle-tiles | 牌面内容裸 emoji `pzv-face`（A-1，工作量最大） | 牌面换自绘 SVG 图案切片，emoji 降级为主题钥匙；多处渲染路径统一走新函数 |
| P7 | 一般 | fruit-slice | 果王头顶标识 emoji（A-6） | 换自绘小皇冠徽记 |
| P8 | 一般 | fishing-star | 水下测深/瞄准小字 10–13px（A-10） | 统一 ≥14px |
| P9 | 一般 | poop-hero | 分类桶短签 8–9px（A-11） | 随 P3 图形化标识一并处理，文字提到 ≥14px 或删除 |
| P10 | 一般 | fruit-catch | 双人名牌文字 13px（A-12） | 提到 14px |
| P11 | 建议 | poop-hero | 场内装饰 emoji 6 处（A-7） | 随 P1–P3 顺手清理可清的 |
| P12 | 建议 | lianliankan | `MASK_FACE = "❓"` 残留常量（A-8） | 清理死常量（仅视觉层常量，不动判定） |

自查底线（第四节）：商标 grep、裸 emoji `fillText(` 残留、360px 新溢出、reduced-motion、kit 纪律 `git log` 核查——穿插在修复中执行，结果记入下文。

> 交接说明:原 C 档工人执行 P1–P4 与 P11 后 ERROR 中断(未付账单,非代码问题);补派工人从 `origin/game-1.3-window7` 接续,未重做已落地的 poop-hero 提交,续做 P5–P12 与 B 档建议 10,并执行自查底线与合并态复验。

## 一、修复清单（逐条落地）

每条一个 commit(`fix(visual-r1): <款> <问题>`),配套用例全部收在各款 `qaW7r1Fix.test.ts`(8 个新文件 + kit `pattern.test.ts`),只增不减。

| # | 级别 | 来源 | 款 / 位置 | 修法 | 配套用例 | commit |
| --- | --- | --- | --- | --- | --- | --- |
| P1 | 严重 | A-4 | poop-hero `index.ts:1500` | 香香星 ✨ 改 kit `traceStar` 三停渐变自绘星(`drawScentStar`) | `poop-hero/qaW7r1Fix.test.ts` | `eeae31c` |
| P2 | 严重 | A-3 | poop-hero `index.ts:1542/1688` | 地面垃圾 / 头顶携带件改 `trashArt.ts` 18 款自绘条目(≥2 停渐变 + 描边 + 落影) | 同上(18 款逐一实测) | `7fa3398` |
| P3+P9 | 严重+一般 | A-5 / A-11 | poop-hero `index.ts:1559/1093` | 分类桶三枚自绘图标(`drawBinIcon`)+ 8–9px 桶签图形化删除 + 功能小字 ≥14px | 同上 | `11c2259` |
| P4 | 严重 | A-9 | poop-hero `visual.ts` HERO_VIS + `index.ts drawHero` | 星星披风 `#7FA9F0→#6690E0` 拉开 Rec.601 灰阶差 Δ29 + 星星剪影级发卡(不走 showDetail 门槛) | 同上(灰阶差 ≥25 断言) | `d12db80` |
| P11 | 建议 | A-7 | poop-hero `index.ts` 六处 | 场内装饰 emoji(💫🫧🤔🧽🧼🔒)+ 粒子文本全自绘,`emoji()` 工具函数连根删除堵回退路 | 同上 | `2f3c182` |
| P5 | 严重 | A-2 | landlord-cards `index.ts:110/663` + `visual.ts` | AI 头像 🐰🐼 改 `botFaceSvg` 自绘 SVG:团团长耳 / 圆圆圆耳 + 眼周深色块,2 停渐变左上亮 + 1.5px 描边 + 左上高光,耳形几何差保 16px 灰度可分 | `landlord-cards/qaW7r1Fix.test.ts` | `56179c1` |
| P6 | 严重 | A-1 | puzzle-tiles `index.ts` 五渲染路径 + `visual.ts` + kit 新增 `pattern.ts` | 牌面裸 emoji 改**场景画切片**:每主题一张参数化 SVG 场景(底色 2 停渐变 + 光斑圆盘/远近地形带 1.5px 描边 + 每格贴纸 + 边饰),切片 `<use>` 引整画、齿边皮肤 clipPath 裁剪(凸齿上也有画);预览小样 / 底图虚影同源;托盘干扰块走同族 alt 换色场景;emoji 降级为关卡数据主题钥匙(THEME_TILES 一字未动);记忆关 `pz-hidden` 切片一并藏 | `art/kit/pattern.test.ts`(8 条)+ `puzzle-tiles/qaW7r1Fix.test.ts`(7 条) | `995ab45` |
| P7 | 一般 | A-6 | fruit-slice `index.ts:2104` + `visual.ts` | 果王头顶 emoji 身份牌改自绘小皇冠(宽 0.7r × 高 0.42r 三尖齿 + 珠 3 粒,`#FFE9A8→#E8A62E` 渐变 + 1.5px 描边 `#C7861F` + 束带 + 左上高光);珠色分果王(金/粉/蓝)保住身份通道 | `fruit-slice/qaW7r1Fix.test.ts` | `6b8e9eb` |
| P8 | 一般 | A-10 | fishing-star `index.ts:1020/1039/1354/1358` | 鱼群带标签 11px / 深度刻度 10px / 瞄准提示 12px / 风向标 13px 统一 ≥14px,标签小牌随字加宽加高;全量扫描断言画布再无 <14px | `fishing-star/qaW7r1Fix.test.ts` | `a56e08e` |
| P10 | 一般 | A-12 | fruit-catch `index.ts:739` | 双人半屏名牌 `bold 13px → bold 14px`(名牌无底板,无需加高) | `fruit-catch/qaW7r1Fix.test.ts` | `78a321f` |
| P12 | 建议 | A-8 | lianliankan `board.ts:366` | `MASK_FACE = "❓"` 死常量删除;`levels188.test.ts` 的池排斥断言降为字面量(语义原样);判定层 pickMasked / maskKey 一字未动 | `lianliankan/qaW7r1Fix.test.ts` | `5c95546` |
| B-10 | B 档建议 | B 档 TOP10 第 10 条 | snake-snack `visual13.ts` + kit `caterpillar.ts` | 双虫头部灰度差仅 Δ12:粉虫加剪影级蝴蝶结(kit `CatLook.bow` 可选字段,深梅 `#9B4E86` 对头色灰阶差 Δ57,不吃 showAntenna 12px 门槛),形状 + 亮度双通道认虫 | `snake-snack/qaW7r1Fix.test.ts`(小头径下粉虫比绿虫多 3 次填充的量化断言) | `f8b944a` |

**修复统计:阻断 0(A 档判定清零,维持)/ 严重 6 条全修 / 一般 5 条修 4(第 5 条见遗留)/ 建议 2 条全修 + B 档新发现 1 条顺手清。**

### 一之补、自查底线结果(第四节抽查)

- **商标扫描**:九款 + kit 非测试源码 30+ 品牌词全量 grep,唯一命中是 poop-hero `visual.ts:56` 的**防伪声明注释**(声明与超人/蝙蝠侠/奥特曼配色零相似,A 档已核准),非踩线;新画的头像/皇冠/场景画/蝴蝶结全部几何原创 ✅。
- **裸 emoji 主角/核心道具**:`fillText(emoji)` 与 `item.emoji`/头像 emoji 模板全量扫描,主角与核心道具零残留;剩余命中全部是 HUD/菜单/图鉴的**文案级装饰 emoji**(A 档口径「装饰性可留」)✅。
- **360px**:样式表字号九款仍 0 处 <14px;本轮修复全部是 canvas 内绘制或 `pointer-events:none` 皮肤层,不改盒模型;fishing-star 标签小牌加宽后 ≈184px(<320)不溢出;既有 fit/触区测试链全绿 → **新溢出 0** ✅。
- **reduced-motion**:本轮新增全部是静态绘制(头像/皇冠/切片/蝴蝶结),零新动效通道;九款「装饰停、功能提示留」的既有用例全绿 ✅。
- **lianliankan `logic.ts`**:本轮未触碰(P12 只动 `board.ts` 死常量与 `art.ts` 渲染路径断言)✅。

## 二、遗留清单

| # | 事项 | 理由 / 去向 |
| --- | --- | --- |
| 1 | plan 过程文档缺档 ×4(20-A / 20-C / 21-A / 21-C,A 档「一般」项) | 过程文档属各归属格,C 档不代写他人 plan;实现与测试都在。记过程规范问题,交 round2 点名 |
| 2 | puzzle-tiles blurb 文案跟进(B 档第五节第 10 条注:「牌面 emoji 待 C 档换画后再更新文案」) | 牌面已换成场景画切片;文案属 B 档职责且 B 已 IDLE,交下一轮 B 档更新 |
| 3 | HUD / 菜单 / 图鉴的文案级装饰 emoji(九款合计数百行) | 验收口径「装饰性可留」,非本轮范围;若后续轮提出全面图形化再单独排格 |
| 4 | destroy 泄漏浏览器真机「进出 ×5」实测 | node 环境无浏览器;A 档已静态审计 + 池级 clear() 用例,真机实测交后续轮 |

## 三、合并态复验数据

- 复验基准(代码合并态)SHA:**`f8b944a`**(fetch → rebase 后与 `origin/game-1.3-window7` 一致;三份报告 tester / learner / fixer 与全部修复在位;本报告回填提交为 docs-only,不影响复验结论)。
- `npm ci` ✅ 无报错。
- `npm test` ✅ **735 文件 / 15384 用例全绿**,179.7s。用例链条单调递增:A 档 716/15275 → B 档合并态 727/15350 → 本轮 **+8 文件 / +34 用例**,只增不减 ✅。
- `npm run build` ✅(tsc --noEmit + vite build,676ms);PWA 预缓存 **190 项 / 5249.04 KiB**(B 档基线 5225.25 KiB,+23.8KiB 主要来自 pattern 场景画与自绘头像)。
- `npx vite preview` 冒烟 ✅:`/`、`/sw.js`、`/manifest.webmanifest` 全部 200。
- 本窗九款 dist 单 chunk 体积(未压缩):fruit-catch 51.1KB · fruit-slice 78.0KB · snake-snack 41.1KB · lianliankan 48.8KB · puzzle-tiles 55.6KB(+5.3KB,场景画)· memory-cards 49.8KB · landlord-cards 73.1KB · fishing-star 73.9KB · poop-hero 87.6KB。

## 四、kit 纪律检查结论

`git log origin/game-1.3..HEAD -- src/art/kit/` 全查:

- 8 个既有 kit 模块(fruit / juice / sparkle / caterpillar / icons / jigsaw / cardArt / fishArt / cape)各自只有归属格的一条首建提交,**零越权改写**(与 A 档盘点一致);
- 本轮 C 档 kit 改动仅两处,均在本窗归属内:① **新增** `pattern.ts` + `pattern.test.ts`(为 21-B puzzle-tiles 首建,归 C 档,「谁先用到谁新增」);② `caterpillar.ts`(20-C snake-snack 归属,本窗文件)追加可选 `CatLook.bow` 字段与蝴蝶结工序,既有接口与默认输出不变(20-C 既有用例全绿);
- 跨格引用仍全部 import 只读 ✅。

## 五、窗口 7 第 1 轮视觉验收结论(C 档收尾)

- **阻断级:0 残留**(商标 0 命中、360 新溢出 0,维持 A 档清零判定);
- **严重级:0 残留**——A 档 6 条严重(puzzle-tiles 牌面 / landlord 头像 / poop-hero 垃圾·香香星·桶标 / poop-hero 双人)全部修复并有用例钉死;
- 一般 4/5 修复、建议 2/2 修复、B 档建议 1 条顺手清;遗留 4 项全部登记(上表第二节),无一是阻断或严重;
- `npm test`(735/15384)/ `npm run build` / `vite preview` 冒烟全绿,用例只增不减;
- 合并态复验基准 SHA `f8b944a`,普通 push `origin game-1.3-window7`,全程未 force、未动 `main` / `game-1.2`、未开 PR。
