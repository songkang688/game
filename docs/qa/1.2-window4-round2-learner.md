# 窗口 4 · 第 2 轮 · 学习优化员

## 档C

> 范围：档 C 五款（`alien-seek` / `box-hamster` / `bubble-aim` / `snake-snack` / `memory-cards`）。
> 状态：**已完成**。落地 5 条，新增 53 条用例，`npm test` / `npm run build` 全绿。

### 一、本轮主线

第 2 轮测试员交上来的 C2-01 是「`alien-seek` 无尽第 20 轮起题目规模冻住」。
顺着这条线把五款的无尽模式一起量了一遍，发现这不是一款的毛病，是**一类**毛病：

| 游戏 | 无尽的爬坡靠什么 | 改之前第几轮到顶 | 到顶之后还剩什么在变 |
| --- | --- | --- | --- |
| `alien-seek` | 藏身点数 / 目标数 / 限时 | **第 20 轮** | 只有场景种子和推理轮的线索条数 |
| `box-hamster` | 尺寸 / 箱子数 / 墙密度 / 冰 / 漩涡 | **第 15 仓** | 只有仓名和随机布局 |
| `snake-snack` | 速度 / 墙量 / 目标口数 | **第 16 座**（休闲档） | 经典档还能靠累计口数继续加速，休闲档什么都不剩 |
| `memory-cards` | 组数 / 挪窝 / 转盘 | 第 34 轮 | —（第 1 轮 L1-03 / L1-04 已经补过） |
| `bubble-aim` | 压行数 | **不封顶** | 结构不同，见下 |

所以本轮的主线定成：**把「无尽后段冻住」这一类问题一次收干净，并把五款的判定口径统一到
`endlessDifficulty` 这一个函数上**——这个写法是第 1 轮在 `memory-cards` 上先立起来的，
这轮把另外三款补齐，第五款（`bubble-aim`）说清为什么不进这张表。

统一之后，五款的曲线可以用同一句话验：
**难度分只增不减 + 到顶之前不会连着 10 轮走平 + 到顶之后老老实实是同一个分数。**
最后一条很要紧：它逼着我们把「天花板在哪、为什么在那儿」写成常数和注释，
而不是让曲线悄悄走平了没人发现——C2-01 当初就是这么漏过去的。

### 二、改进清单（12 条）

| 编号 | 游戏 | 一句话 | 结论 |
| --- | --- | --- | --- |
| L2-01 | `alien-seek` | 无尽第 20 轮起规模冻住（= C2-01） | **落地** |
| L2-02 | `box-hamster` | 无尽第 15 仓起规模冻住 | **落地** |
| L2-03 | `snake-snack` | 休闲无尽第 16 座起完全冻住 | **落地** |
| L2-04 | `bubble-aim` | 新手主题子弹最紧却没人说一声（= C2-03） | **落地（改说法，不改关卡）** |
| L2-05 | 五款 | 无尽曲线口径不统一，各写各的 | **落地** |
| L2-06 | `alien-seek` | 无尽罚时跟着 `chapter` 循环，第 9 轮比第 8 轮还轻 | **落地（并入 L2-01）** |
| L2-07 | `box-hamster` | 配方点了漩涡，实际造出来有近两成没有 | 记账，不动 |
| L2-08 | `bubble-aim` | 第 1 主题直接加子弹 | **驳回**（1.0 前 99 关有哈希锁） |
| L2-09 | `snake-snack` | 点心落点改成种子随机 | **驳回**（会让重开一模一样） |
| L2-10 | `memory-cards` | 发牌种子改成种子随机 | **驳回**（重开必须重洗，否则背位置） |
| L2-11 | `bubble-aim` | 无尽清屏补货抛异常卡死（C2-02） | 交本轮监督修复员 |
| L2-12 | 五款 | 无尽的「到顶轮次」应当写成导出常数 | **落地（并入 L2-01/02/03）** |

### 三、落地的 5 条

#### L2-01　`alien-seek` 无尽曲线延到第 36 轮（清 C2-01）

改动集中在 `src/games/alien-seek/logic.ts`，`levels.ts` / `index.ts` 各接一处。

1. **目标数分两段涨到 8 个。** 原来是 `min(5, 2 + ⌊r/4⌋)`，第 12 轮就到 5 个封顶。
   改成前 12 轮**一个数都不动**（老玩家的开局手感照旧），第 13 轮起换慢档
   `min(8, 5 + ⌊(r-12)/8⌋)`，每 8 轮多一个，第 36 轮到 8 个。
   8 是有依据的：场上一共 16 个藏身点（`layoutSpots` 的硬上限），要找的占一半已经很满。
2. **藏身点数和限时保持原样，并把「为什么不能再往下压」写进注释。**
   - 16 个点是 `layoutSpots` 的摆放上限，再多就得缩半径，360px 上会点不准；
   - 14 秒是 `sim.ts` 算出来的公平地板：按「键盘一格一格挪光标」这种最慢的玩法，
     8 个目标要走 8 秒上下，压到 9 秒就会出现怎么点都来不及的轮次。
     这一条本轮**实测过**：把地板降到 9 秒后 `sim.test.ts` 当场变红，所以维持 14。
3. **罚时改成跟限时走（L2-06）。** 无尽轮的 `chapter` 是 `(r-1) % 8` 循环出来的，
   原来直接套 `missPenalty(chapter)`，于是罚时在 2→5→2 之间来回跳，
   **第 9 轮反而比第 8 轮轻**——同一条曲线上出现回头路。
   新增 `endlessMissPenalty(round)`，按 `clamp(round(限时/8), 2, 4)` 算，大约咬掉八分之一。
   > 这里**没有**做成「越往后罚得越狠」，是想清楚之后的取舍：限时最后压到 14 秒，
   > 再罚 6 秒等于一次点错就没了四成时间，对小朋友太重。
   > **罚时管公平，难度交给目标数那条曲线**，所以它也不计入 `endlessDifficulty`。
4. **接线方式**：`BaseLevel` 加一个可选的 `penalty`，`buildEndlessRound` 填上，
   战役关不填（照旧 `missPenalty(chapter)`）。`index.ts` 的运行器取
   `lv.penalty ?? missPenalty(lv.chapter)`，两条路都只有一处判断。

**效果**：`endlessDifficulty` 从第 1 轮的 25 一路涨到第 36 轮，第 20→28→36 三段规模两两不同；
第 36 轮之后才是同一个分数，而且场景种子照旧每轮换。

#### L2-02　`box-hamster` 无尽仓延到第 31 仓

`buildEndless` 的 `ramp` 是 `min(1, r/14)`，尺寸、箱子数、墙密度、冰面、漩涡
五样在第 15 仓同时爬满，第 15 仓和第 60 仓除了名字完全一样。

改法和 L2-01 同一套路：**第一段一个数都不动**，第 15 仓起换慢档，
每 4 仓多一段冰或多一对漩涡，抽成 `endlessIceRuns` / `endlessPortalPairs` 两个纯函数。

- 上限取 **4 段冰 / 2 对漩涡**——不是拍脑袋，是战役第 4 章和第 5 章
  （`lerpInt(2, 4, t)` / `lerpInt(1, 2, t)`）已经跑通过的数，属于这套生成器手上还没用完的余量。
- **墙密度按兵不动**。0.26 以上没验证过；而且 `bestOfAttempts` 攒不够分会退回兜底关，
  墙太密反而更简单，加了等于白加。

新增 `endlessDifficulty(round)` 和 `ENDLESS_PEAK_ROUND = 30`。
第 15 / 23 / 27 / 31 仓的难度分严格往上走；第 15 / 23 / 27 / 31 / 37 / 45 / 61 仓
照参考解走完都能归位（没造出死局）。

#### L2-03　`snake-snack` 休闲无尽延到第 36 座

经典档还能靠 `endlessTickMs` 按**累计口数**一直加速到 150ms 地板，
所以第 16 座之后它还在变难；**休闲档速度是恒定的**，`endlessGarden` 的 `k` 又在第 16 座封顶，
于是休闲档从第 16 座起纯粹是换风景。

改法只动一样：目标口数抽成 `endlessTarget(garden)`，前 16 座一口都不动（到 13 口），
第 17 座起每 4 座多一口，18 口封顶（第 36 座）。
只动目标口数是有意的——多加墙、多加移动障碍会有造出走不通的园子的风险，
而目标口数是纯粹的「这一趟要吃多久」，改它不碰任何布局。

配套断言：延段后每一座**仍然全园连通**、空格数比「身子最长时 + 10」还多，
所以吃满目标口数不会把自己憋死在角落。

#### L2-04　`bubble-aim`：子弹紧不紧，现在会说出来（应对 C2-03）

先把 C2-03 的量化在测试里复现了一遍，结论成立：
第 1 主题（糖果果园）平均预算 **0.71 发/颗**，全场第二紧，只有终极的星尘试炼（0.54）更紧；
第 2 主题（石头城堡）反倒是全场最松的 **1.25**——石泡每颗白送 2 发。
曲线是「先紧 → 松一大截 → 再慢慢紧」，新手一进门打的就是仅次于终极章的紧度。

**为什么不直接给第 1 主题加子弹（L2-08 驳回）**：
`logic.test.ts` 上有一把 FNV 哈希锁——

```ts
expect(fnv(JSON.stringify(LEVELS.slice(0, LEGACY_LEVELS)))).toBe("1c62ba86");
```

1.0 的前 99 关被这把锁焊住，`shots` 改一个数就当场变红。
要改就得先把这把锁拆了，那是拿「老关卡一字不动」这条保证去换一个「一般」级的观感问题，
不划算。测试员那条「风险可控，要不要动请修复员定」的建议，据此**驳回**。

**改不了紧度，那就把紧度说出来**：

- `levels.ts` 新增 `budgetBand(def)`（限弹 / 偏紧 / 适中 / 宽松）与 `budgetNote(def)`。
  「限弹」这一档就是原来的 `isTightShots`，**没有造第二套口径**；
  新加的是 0.62～0.78 这一档「偏紧」，新手主题 17 关整章落在里面。
- `index.ts` 的开局提示卡多挂一行（子弹宽裕的关一个字都不多说，`budgetNote` 返回空串）。
- `THEMES[0].blurb` 从「纯颜色热身，瞄得准就爆得多！」
  改成「纯颜色热身；子弹不算宽裕，瞄准了再打！」。

**特意没做的事**：没有把这一档塞进 `levelMechanisms`。
那个数组被 `logic.test.ts` 用长度逐关断言（第 99 关必须正好 5 个机关、
纯色关必须正好 0 个），塞进去会牵动一大片和本档无关的断言。
所以新提示走**独立一行**，机关图标行原样不动。

#### L2-05　五款无尽曲线收敛到同一口径

`memory-cards/qaC2learn.test.ts` 里建了一张表，把四款的 `endlessDifficulty`
（`alien-seek` / `box-hamster` / `snake-snack` / `memory-cards`）放在一起验同一组性质。
`box-hamster` 是 0 基、另外三款是 1 基，表里带上 `first` 字段，越界一律折回第一轮。

`bubble-aim` **不进这张表**并且写清了原因：它的无尽是「每 5 发压一行，压到顶就收工」，
难度随压行数无上限地涨，压根没有「到顶」这回事，硬套会得出一个没有意义的常数。
它单独由「压行数一直涨、`endlessShouldPush` 逐发对得上」那条断言盯着。

### 四、记账但没动的 3 条

| 编号 | 说明 | 为什么这轮不动 |
| --- | --- | --- |
| L2-07 | `box-hamster` 的配方点了漩涡，实测第 11 / 21 / 36 / 37 / 41 / 46 仓造出来其实没有漩涡（约 15%） | `bestOfAttempts` 是「攒不够分就交出分最高的那张」，属于生成器打分策略，改它会牵动全部 188 关的布局。本轮先用一条断言把现状钉住（点了漩涡的仓里至少八成真有漩涡），漏得更多就是回归 |
| L2-09 | `snake-snack` 点心落点用 `Math.random` | **驳回**。纯函数 `pickSnack(pool, rand)` 已经把随机源注入化了，单测想要确定性随时能给；`index.ts` 里那个 `Math.random` 是有意的——点心落点固定下来，同一座园子重开就是同一条路线，跑图会变成背答案 |
| L2-10 | `memory-cards` 发牌种子里掺 `Math.random` | **驳回**，而且理由更硬：翻牌记忆游戏**重开必须重洗**。种子固定下来，上一局失败时看到的牌位下一局照样有效，等于把游戏玩法本身作废。`buildDeck(cfg, seed)` 已经是纯函数，测试要确定性直接传种子即可 |

第 1 轮挂账的 L1-08（`alien-seek` 无尽平台期）由本轮 L2-01 结清；
L1-09 / L1-10 即上表的 L2-09 / L2-10，本轮给出**驳回**结论，不再挂账。

### 五、本段新增用例

| 文件 | 新增用例 | 盯的是 |
| --- | --- | --- |
| `src/games/alien-seek/qaC2learn.test.ts` | 13 | L2-01 / L2-06 |
| `src/games/box-hamster/qaC2learn.test.ts` | 12 | L2-02 / L2-07 |
| `src/games/bubble-aim/qaC2learn.test.ts` | 10 | L2-04 / L2-08 |
| `src/games/snake-snack/qaC2learn.test.ts` | 10 | L2-03 |
| `src/games/memory-cards/qaC2learn.test.ts` | 8 | L2-05 |
| **合计** | **53** | |

### 六、改了口径的既有断言（用例数一条没减）

改行为就得改跟着行为写死的断言。下面 4 处只改了数字/边界，**没有删任何一条用例**：

| 文件 | 原来 | 现在 | 为什么 |
| --- | --- | --- | --- |
| `alien-seek/logic.test.ts` | `endlessTargetCount(999) <= 5` | `<= 8` | 目标数天花板从 5 提到 8 |
| `alien-seek/qaC1learn.test.ts` | `<= 5` | `<= ENDLESS_MAX_TARGETS` | 同上，顺手改成引常数 |
| `alien-seek/qaC2.test.ts` | 「第 20 轮之后规模一模一样」 | 「规模一路涨到第 36 轮」 | C2-01 已修，断言从「钉现状」翻成「钉结果」 |
| `snake-snack/levels188.test.ts` | `cfg.target <= 14` | `<= ENDLESS_MAX_TARGET` | 目标口数天花板从 13 提到 18 |

### 七、硬约束自查

- 商标黑名单：0 命中（`alien-seek/qaC1leak.test.ts` 的总闸扫描本轮照跑，五款全绿）。
- 角色只有朵朵 / 星星 + 原创配角；新写的两句提醒（`budgetNote`）无血无死亡、只鼓励。
  写注释时有一句用了「焊死了」，被自己那条红线断言当场拦下，已改成「焊住」。
- 没引 three.js / CDN / Socket；没碰 `src/styles.css`；没碰窗口 1 的平台文件。
- 存档 key 一个都没动：本轮改的全是关卡生成与提示文案，
  `recordEndlessBest` / `getGameProgress` 的键名和单位（`snake-snack` 仍是「累计口数」）原样。
- 音效仍然只走 `api.play(...)`。

### 八、交接

- **C2-02（阻断）** 原样交本轮监督修复员：`bubble-aim` 无尽清屏补货时
  `flip` 为偶数必抛异常，异常打断 rAF 重排帧那一句，整局卡死。
- **L2-07** 挂到第 3 轮，由第 3 轮学习优化员决定动不动生成器打分。

## 档A

> 范围：`brave-path` / `brick-break` / `balloon-pop` / `fruit-catch` / `lianliankan`。
> 状态：**第 2 轮学习优化员已完成**。`npm test` 226 文件 / 5859 用例全绿，`npm run build` 通过。
> 落地 **5 条**（每款一条），新增 **48 条**用例。防守性的四条（W4A-07 / 11 / 12 / 15）交本轮监督修复员。

### 改进列单（10 条，本轮落地 5 条）

| 编号 | 游戏 | 一句话 | 来源 | 本轮 |
| --- | --- | --- | --- | --- |
| A-L08 | 连连看 | 无尽换盘的提示语「变了几样就说几样」，不再只报一条 | W4A-08 | **已落地** |
| A-L09 | 戳戳小气球 | 上升速度按气球自己的出场波次算，天上不再整片往上跳 | W4A-13 | **已落地** |
| A-L10 | 接住小水果 | 给关卡加「手速门槛」这一维，新四章从四成爬到八成 | W4A-09 | **已落地** |
| A-L11 | 勇者小路 | 技能栏留一条下限：学会一招之后身上至少留一招 | W4A-16 | **已落地** |
| A-L12 | 碰碰砖块 | 「别接」的胶囊改成空心圈，不指望孩子分得出那点粉色 | R2 走查旁支 | **已落地** |
| A-L13 | 碰碰砖块 | 砖塔清一行要换来喘息而不是加速 | W4A-07 | 交监督修复员 |
| A-L14 | 戳戳小气球 | 气球节出场表走完要续排，别让天空空掉 | W4A-12 | 交监督修复员 |
| A-L15 | 接住小水果 | 水果雨出场表走完要续排，别自己收工 | W4A-11 | 交监督修复员 |
| A-L16 | 勇者小路 | 第八章那五关补歇脚点 | W4A-15 | 交监督修复员 |
| A-L17 | 勇者小路 | 无尽守关往前挪，让新玩家也见得到一次 | W4A-10 | 留待第 3 轮评估 |

### 落地细节

#### A-L08 · 连连看：变了几样就说几样

`endlessStepWord` 原来是一串 `if` 提前返回，谁先命中就只说谁。可无尽的第 7 / 10 / 13 盘是**一次拧三四个旋钮**的大台阶——图案又多一种、收拢方向又换、线只准拐一次、表还收紧了。孩子照着上一盘的打法下手，要撞好几次墙才发现规矩变的不止一条。

拆出 `endlessStepChanges(round): string[]` 把这一盘拧动的旋钮全列出来，`endlessStepWord` 变了几样就说几样。顺手补了一条原来根本没报过的：限时从 120 秒一档一档收到 45 秒的过程，之前是悄悄变紧的。

- 改：`src/games/lianliankan/logic.ts`
- 测：`src/games/lianliankan/learn.r2.test.ts`（8 条）

#### A-L09 · 戳戳小气球：速度按自己的波次算

`tick` 里算上升速度用的是 `festRiseSpeed(planAt - 1)`——planAt 是「已经出到第几个」，每出一个新球就加一。而 `floatAt` 是拿这个速度乘气球的**全部年龄**算位置的。速度一变，老气球的 y 立刻整体挪一截：飘了 5 秒的球每次跳 5.5 像素，密的时候一秒出两三个，看上去就是「气球在抽搐」，而且实际逃逸得比设计的更早。

给 `Balloon` 加一个 `wave` 字段，出场时记下自己是第几个，速度按它自己的波次算。

- 改：`src/games/balloon-pop/index.ts`
- 测：`src/games/balloon-pop/learn.r2.test.ts`（7 条）

#### A-L10 · 接住小水果：手速门槛终于参与了难度

生成器排链时，下一颗的落点是在「篮子跑得到的那一段」里随机挑的，而这一段一直按篮子极速顶格算。于是不管第几关，「照着链走最少要跑多快」都稳稳压在 210~230 像素/秒——第 1 关和第 188 关要的手速一模一样，变难的只有「要接几颗」和「间隔多密」。

给 `CatchLevel` 加一个 `reach`（这一关用掉多少「够得着的范围」，0..1），`layoutPlan` 按它收窄链宽；`reachOf(ci, t, size)` 把它铺成「章内一路加宽、换章松一口气、一章比一章起点高」的缓坡。量到手速上：

| 章 | 章头 | 章尾 |
| --- | --- | --- |
| 双篮果谷（100–122） | 44% 篮速 | 83% |
| 沉甸果坡（123–144） | 50% | 83% |
| 传送果道（145–166） | 56% | 83% |
| 连击星光坡（167–188） | 61% | 82% |

**前 99 关一个参数都没加**——那 99 关是 1.0 冻结的，`levels188.test.ts` 有生成指纹守着。坡只铺在 1.1 之后新加的四条果道上，这一点在 `reachOf` 的注释里写死了。

- 改：`src/games/fruit-catch/logic.ts`、`src/games/fruit-catch/levels.ts`
- 测：`src/games/fruit-catch/learn.r2.test.ts`（9 条）

#### A-L11 · 勇者小路：身上至少留一招

`toggleLoadout` 原来不设下限，四个技能能一个一个卸干净。卸干净之后星星那边照样带三个随等级涨阶的技能，朵朵只剩平砍——20 级的擂台胜率从 20/20 掉到 4/20，而孩子从界面上看不出是自己把招式卸光了，只会觉得「这游戏突然打不赢了」。

加 `MIN_LOADOUT = 1` 和 `canUnequip(save)`，最后一招卸不下来；界面点最后那一招时换成「身上至少留一招呀，空着手上擂台可打不过星星。」而不是照搬「上阵位置只有 4 个」。换招的路径一点没堵：先下一个再上另一个照旧。

- 改：`src/games/brave-path/logic.ts`、`src/games/brave-path/index.ts`
- 测：`src/games/brave-path/learn.r2.test.ts`（7 条）

#### A-L12 · 碰碰砖块：「别接」的胶囊靠形状说话

掉下来的胶囊里混着一个小板子（🌀，接到板子变窄 5 秒）。它和好道具原来只差一点点粉——`#FFFFFF` 对 `#FFE1E9`，两个都是实心圆、都印一个表情。色弱的孩子分不出这点色差；四岁还不识字的孩子更是只能靠颜色猜。

抽出纯函数 `capsuleLook(kind)`：好道具是实心圆，「别接」的那个是带描边的空心圈。形状本身就把话说清楚了，颜色只是锦上添花。

- 改：`src/games/brick-break/logic.ts`、`src/games/brick-break/index.ts`
- 测：`src/games/brick-break/learn.r2.test.ts`（6 条）

### 用例增量

| 游戏 | 新增文件 | 新增用例 |
| --- | --- | --- |
| 接住小水果 | `learn.r2.test.ts` | 9 |
| 连连看 | `learn.r2.test.ts` | 8 |
| 戳戳小气球 | `learn.r2.test.ts` | 7 |
| 勇者小路 | `learn.r2.test.ts` | 7 |
| 碰碰砖块 | `learn.r2.test.ts` | 6 |
| 五款的 `walkthrough.r2.test.ts` | 特征化用例随改动更新 | +11（净增） |
| **合计** | | **48** |

W4A-08 / 09 / 16 的特征化用例已经按新行为改写，标题从「特征化」改成「已修」，注释里保留了「原来是什么样、为什么要改」——这样后来的人读用例就能读到当时的判断。

### 硬约束复核

商标黑名单 0 命中；角色只有朵朵 / 星星 + 原创配角；新增文案「身上至少留一招呀，空着手上擂台可打不过星星。」不含任何批评字眼；没碰 `src/styles.css`；没碰窗口 1 平台文件；存档结构只**加**了可选字段（`CatchLevel.reach` 是关卡表字段，不进 localStorage；`HeroSave` 一个字段都没动），存档 key 原样。

## 档B

范围：`adventure-king` / `mole-pop` / `bubble-pop` / `fruit-slice` / `puzzle-tiles` 五款。
本轮**列 12 条、落地 5 条**，新增 **35 条**用例，`npm test && npm run build` 全绿。

### 一、本轮主线

第 2 轮测试员交上来的 B2-01 ~ B2-04 看着是四款各自的小毛病，量到一起才发现是**同一类**：
**五款的无尽玩法都会在某一轮之后彻底卡死在一个平台上，之后无论玩多久，参数一个都不再变。**

| 游戏 · 无尽玩法 | 爬坡靠什么 | 改之前第几轮到顶 | 到顶之后还剩什么在变 |
| --- | --- | --- | --- |
| `mole-pop` · 地鼠夜市 | 目标分 / 停留 / 间隔 / 台面 / 兔子 / 盾牌 | **第 25 摊** | 只有 `night` 每 5 摊换一次灯 |
| `fruit-slice` · 水果暴风 | 抛数 / 间隔 / 炸弹率 | **第 22 波** | 只有 `extras` 掷骰子 |
| `puzzle-tiles` · 无尽画廊 | 片数 / 提示 / 限时 | **第 19 幅** | 周期 6 的循环，第 30 幅 = 第 120 幅 |
| `bubble-pop` · 无尽泡泡海 | 上推间隔 / 颜色数 | **第 18 推** | 只有随机色 |
| `adventure-king` · 无尽古堡 | （本来就不谈难度曲线） | — | 花样不够：13.1% 的概率下一间还是刚才那间 |

所以本轮主线定成：**把「无尽后段冻住」这一类一次收干净，并且五款用同一套口径来收**。
口径是三句话：

1. **到顶之前一个数都不许动。** 加压只从 `over = 轮次 − 封顶轮次 > 0` 起算，
   老玩家的前二十几轮手感必须逐项一致——这一条每款都写了断言。
2. **到顶之后接着拧，但拧的是「还剩什么可拧」，不是把所有旋钮再拧一遍。**
   泡泡海的间隔压到 1.8 秒就不能再快了，那就加「大潮」；
   暴风的炸弹率再往上加会变成运气游戏，那就只加「一次要照顾几颗」。
3. **每个旋钮都得有下限/上限，而且写成导出常数。** 目的是「还在往上走」，不是把人劝退；
   把天花板写成 `SEA_PUSH_FLOOR_MS`、`ENDLESS_CONCURRENT_MAX` 这样的常数，
   下一轮才查得出「曲线是不是又悄悄走平了」——B2-01~B2-04 当初就是这么漏过去的。

### 二、改进清单（12 条，本轮落地 5 条）

| 编号 | 游戏 | 一句话 | 结论 |
| --- | --- | --- | --- |
| B2-L1 | `adventure-king` | 无尽古堡 13.1% 的概率连着抽到同一张房间模板（= B2-01） | **落地** |
| B2-L2 | `puzzle-tiles` | 无尽画廊第 26 幅起彻底不涨、厅名第 21 幅起卡死（= B2-02） | **落地** |
| B2-L3 | `mole-pop` | 夜市第 25 摊起参数全冻、场地名第 21 摊起卡死（= B2-03） | **落地** |
| B2-L4 | `bubble-pop` | 泡泡海第 18 推起不再变紧（= B2-04） | **落地** |
| B2-L5 | `fruit-slice` | 水果暴风第 22 波起节奏定死 | **落地** |
| B2-L6 | 五款 | 「无尽到顶轮次」应当写成导出常数，供横扫用例统一查 | **落地（并入 B2-L1~L5）** |
| B2-L7 | `fruit-slice` | 三个快闪回合每秒要切的分低于同章正课（= B2-05） | **驳回**（前 99 回合有 FNV 哈希锁，见下） |
| B2-L8 | `bubble-pop` | 泡泡海涨上来的新行永远是纯色泡，石头 / 冰冻 / 彩虹 / 闪电一个都不上来 | 记账，留第 3 轮 |
| B2-L9 | `adventure-king` | 无尽遗迹连下 60 层，机器人有 2 层打不通 | 记账，不动 |
| B2-L10 | `puzzle-tiles` | 提示下限 3 次对 6×6 的板子偏松 | 记账，不动 |
| B2-L11 | `mole-pop` | 摊位名周期 15 摊、场地名周期 25 摊，两套命名不同步 | 记账，不动 |
| B2-L12 | 五款 | 无尽玩到深处没有任何「里程碑」反馈 | 记账，留第 3 轮 |

### 三、落地的 5 条

#### B2-L1　`adventure-king`：无尽古堡不再连着重样（清 B2-01）

原来 `buildCastleRoom(seed, room)` 每一间**各挑各的**，而 `index.ts` 每换一间就现摇一个新种子；
`templatePoolFor` 前几间只放 3 张模板，于是有 1/3 的概率下一间和上一间一模一样。

改了两处，缺一不可：

1. `explore.ts` 新增 `templateChain(seed, room)`：从第 1 间**链式**挑到第 room 间，
   每一步都把上一间那张从池子里排除（池子只剩一张时不过滤，兜底）。
2. `index.ts` **一趟古堡认一个 `runSeed`**，房号从 1 往上数。
   > 这一处是关键：只改 `explore.ts` 是没用的——链是按种子算的，
   > 而原来每间都换种子，链的「上一间」根本不是玩家真正走过的那一间。
   > 第一版就栽在这儿，用例照跑 1471 次相邻重复。

**效果**：400 趟 × 30 间共 11600 次换房，相邻重复从 **1514 次（13.1%）降到 0**；
最长连开同一张图从 5 间降到 1 间。同一个 `runSeed` 仍旧完全可复现，纪录照样能比；
「同一趟里该重的还是会重」（池子就 12 张）也留了一条用例，免得下次有人改成轮流发牌。

#### B2-L2　`puzzle-tiles`：无尽画廊到顶之后接着加压（清 B2-02）

`levels.ts` 改了两处：

1. **厅名从 clamp 改成 cycle**：`endlessHallName` 原来 `Math.min(len-1, ...)`，
   第 21 幅之后招牌永远是「焰火厅」；改成取模，五个厅逛完从头再逛（一圈 20 幅）。
2. **新增 `galleryPressure(round, baseHints)`**，`GALLERY_CAP_ROUND = 19` 之后接着拧三样：
   - 提示次数每 8 幅少一次，下限 `GALLERY_HINT_FLOOR = 3`；
   - 每 4 幅来一次「不看图挑战」（`hidePreview`），是穿插不是从此都不给看；
   - 步数上限每幅收紧 1%，最多收 `GALLERY_TRIM_MAX = 25%`。

**6×6 的板子没有再变大**，这是有意的：6×6 是 360px 上还点得准的上限
（第 2 轮测试员那条「画板有封顶」用例盯着）。板子不能再大，能加的就只有这三样。
三种玩法（推格子 / 旋转 / 缺块）**都吃到了加压**——第一版只有推格子那一路接了限时，
另外两路等于没改，是自己回看时发现的。

**效果**：第 21 / 30 / 42 幅（同为推格子）三张配置两两不同；到第 44 幅前后各项先后触底，
之后稳住（第 60 幅 = 第 120 幅，这是「有下限」而不是「又冻住了」）。

#### B2-L3　`mole-pop`：夜市第 25 摊之后第二批旋钮接着走（清 B2-03）

`levels.ts` 里 `endlessWave` 的 `k = Math.min(n-1, 24)` 一到第 25 摊就不动了。
改成保留这条基础曲线，再按 `over = n − ENDLESS_CAP_WAVE` 接一段：

| 旋钮 | 第 25 摊 | 接着怎么走 | 到底停在 |
| --- | --- | --- | --- |
| 目标分 | 18 | 每 4 摊 +1 | +10（第 65 摊） |
| 出洞间隔 | 340ms | 每摊 −2ms | 240ms（第 75 摊） |
| 停留下限 | 480ms | 每摊 −2ms | 340ms（第 95 摊） |
| 台面预算 | 3 只 | 第 40 摊 4 只、第 60 摊 5 只 | `ENDLESS_CONCURRENT_MAX = 5`（9 个洞留一半给反应时间） |
| 兔子 / 盾牌 | 0.14 / 0.18 | 每摊 +0.002 | 0.22 / 0.27 |

顺手把 `endlessFieldName` 也从 clamp 改成 cycle（一圈 25 摊）。

**效果**：第 25 摊与第 100 摊六项逐项不同；所有旋钮在第 113 摊前先后触底，
之后稳住（第 120 摊 = 第 400 摊）。第 1 / 25 / 40 / 60 / 90 / 150 / 300 摊
的满分打法都够得着目标分——**越守越难，但永远守得住**。

#### B2-L4　`bubble-pop`：泡泡海接一段缓坡，后段还会来「大潮」（清 B2-04）

`collapse.ts` 改了两处：

1. `seaPushMs` 原来第 18 推封在 2600ms。改成第一段坡（每推快 220ms）走到 2600ms 之后，
   换一段更缓的坡（每推快 20ms）走到 `SEA_PUSH_FLOOR_MS = 1800` 再稳住。两段在第 18 推平滑接上。
2. 新增 `seaTideRows(pushes)`：第 20 推起会来「大潮」，一次涨两行；
   越到后面来得越勤，但最勤也是 `SEA_BIG_TIDE_MIN_EVERY = 3` 推一次。
   `index.ts` 的 `tide()` 按它循环推行，**每一行都各自看一眼有没有顶穿**。

**为什么不让石头 / 冰冻 / 彩虹 / 闪电从海里涨上来（B2-L8 记账）**：
那是更对味的加压，但 `onCell` / `hasMovesOn` / `showPreview` 三处都只认普通色泡，
要一起改才不会出现「点了没反应」。这不是一条能顺手带的改动，留第 3 轮评估。

#### B2-L5　`fruit-slice`：水果暴风第 22 波之后抛数续爬

`blade.ts` 的 `stormPace`：`count` 第 15 波、`interval` 与 `bombChance` 第 22 波先后到顶。
封顶之后 `count` 每 12 波再 +1，爬到 `STORM_COUNT_MAX = 9` 为止；
`stormWave` 的三种新目标（双倍果 / 花朵 / 连体果）概率各自 +0.006/波，最多 `STORM_EXTRA_BUMP_MAX = 0.2`。

**间隔和炸弹率一个都没再加**，这是想清楚之后的取舍：
0.55 秒已经是「看得见就来得及」的地板，炸弹率再往上加会把暴风变成运气游戏。
所以后段的压力从「手快」转到「一次要照顾几颗、看清楚再下刀」。

### 四、驳回的 1 条

#### B2-L7　三个快闪回合不够紧（B2-05）——**驳回**

第 7 / 8 / 9 果园的「打烊前快切」「喷发前快切」「圣火快切礼」每秒要切的分
（1.80 / 1.86 / 1.80）低于同章最难的正课（1.83 / 1.96 / 2.00），挂着快闪的名字却不快。

本轮**真的改过一版**：把目标分提到 62 / 58 / 64，同时把每波抛数从 3~4 提到 4~6
（只提目标分会撞上老问题——这三关的注释写着当年就是因为供给不够才把目标从 68 / 74 / 80 调下来的）。
改完当场撞上 `rounds188.test.ts` 的哈希锁：

```ts
expect(fnv1a(legacy)).toBe("b58944be");   // 前 99 回合逐字未改
```

这三关分别是第 **70 / 81 / 92** 回合，全落在 1.1 冻起来的前 99 回合里。
动它们等于把老玩家在这三关上的星星记录一起改掉——拿「老关卡一字不动」这条保证
去换一个**轻微**级的观感问题，不划算，**驳回**，已回滚。
`qa-b-r2-learn.test.ts` 里留了一条用例把「为什么不能动」钉住（三关都在 `LEGACY_ROUNDS` 之内）。

### 五、记账但没动的 5 条

| 编号 | 说明 | 为什么这轮不动 |
| --- | --- | --- |
| B2-L8 | 泡泡海的新行永远是纯色泡 | 要同时改 `onCell` / `hasMovesOn` / `showPreview` 三处才不会出现「点了没反应」，牵动面比一条无尽加压大得多。留第 3 轮 |
| B2-L9 | 无尽遗迹 60 层里机器人有 2 层打不通 | 复核过：是机器人自己的走位极限（贴墙起跳的时机），不是地形不可解。手动给那两层喂一条最优路径能过。属于测试机器人的精度问题，不是游戏问题 |
| B2-L10 | 提示下限 3 次对 6×6 偏松 | 想降到 2，但 `levels188.test.ts` 有一条 `hints >= 3` 的既有约定，那是全 188 关共用的下限。为一个无尽后段的手感去松动全局约定不值当 |
| B2-L11 | 摊位名周期 15 摊、场地名周期 25 摊 | 两套名字挂在不同的 UI 位置（摊位名在 chip 上、场地名在夜市外），本来就不要求同步。统一周期是纯口味问题，不改 |
| B2-L12 | 无尽玩到深处没有「里程碑」反馈 | 五款都只有「最好纪录」一个数。想加「第 50 摊 / 第 100 幅」这类节点鼓励，但那会碰到平台层的 `recordEndlessBest` 与提示条，超出本档 5 个目录。留第 3 轮写进结论 |

### 六、本段新增用例

| 文件 | 新增 | 盯的是 |
| --- | --- | --- |
| `src/games/adventure-king/qa-b-r2-learn.test.ts` | 8 | B2-L1 + **五款横扫** |
| `src/games/puzzle-tiles/qa-b-r2-learn.test.ts` | 7 | B2-L2 |
| `src/games/mole-pop/qa-b-r2-learn.test.ts` | 6 | B2-L3 |
| `src/games/mole-pop/levels188.test.ts` | 1 | B2-L3 的下限 |
| `src/games/bubble-pop/qa-b-r2-learn.test.ts` | 5 | B2-L4 |
| `src/games/bubble-pop/collapse.test.ts` | 1 | B2-L4 的大潮 |
| `src/games/fruit-slice/qa-b-r2-learn.test.ts` | 6 | B2-L5 + B2-L7 的驳回理由 |
| `src/games/fruit-slice/blade.test.ts` | 1 | B2-L5 的新目标浓度 |
| **合计** | **35** | |

其中「五款横扫」那两条是本轮的总闸：把四款讲难度的无尽放进同一张表，
逐款验「第 N 轮和第 N+36 轮不能一模一样」；第五款（古堡）单独验「闯到 120 间都不连着重样」。
下一轮谁把哪条曲线又改平了，这两条会当场变红。

### 七、改了口径的既有断言（用例数一条没减）

改行为就得改跟着行为写死的断言。下面 4 处只改了数字/边界，**没有删任何一条用例**，
其中两处还顺手把范围从 60 轮扩到 400 轮：

| 文件 | 原来 | 现在 | 为什么 |
| --- | --- | --- | --- |
| `mole-pop/levels188.test.ts` | 「每一波都能打」查到第 60 摊，`target <= 18` / `gapMs >= 330` / `maxConcurrent <= 3` | 查到第 400 摊，上下限改引常数 | 夜市天花板整体上移 |
| `mole-pop/levels188.test.ts` | `expect(endlessWave(25)).toEqual(endlessWave(60))` | 改成「第 60 摊比第 25 摊难」，另加一条查触底后稳住 | 这条断言原本正是**把平台期钉成了「预期行为」** |
| `bubble-pop/collapse.test.ts` | `seaPushMs(999) === 2600` | `=== SEA_PUSH_FLOOR_MS`，并补查第 18 推的接坡点 | 间隔地板从 2600ms 降到 1800ms |
| `fruit-slice/blade.test.ts`、`fruit-slice/qa-b-r1.test.ts` | `stormPace(999).count === 7` / `<= 7` | `=== STORM_COUNT_MAX`（9），并补查 400 波单调 | 抛数天花板从 7 提到 9 |

### 八、硬约束自查

- 商标黑名单：0 命中（第 1 轮那条「一次扫五款」的总闸本轮照跑，五款全绿）。
- 角色只有朵朵 / 星星与原创配角；本轮新写的注释与文案无血无死亡、失败只鼓励。
- 没引 three.js / CDN / Socket；没碰 `src/styles.css`；没碰窗口 1 平台文件；没碰别人的游戏目录。
- 存档 key 一个都没动：本轮改的全是关卡与无尽曲线的生成函数，
  `recordEndlessBest` / `getGameProgress` 的键名和单位原样。
- 音效仍然只走 `api.play(...)`：本轮没有新增任何发声点。

### 九、交接

- **B2-05** 已在本轮给出**驳回**结论（前 99 回合有哈希锁），不再挂账，留给第 3 轮写进最终结论。
- **B2-L8 / B2-L12** 挂到第 3 轮。
- 本轮**没有产生新的阻断 / 严重问题**，交本轮监督修复员复核。
