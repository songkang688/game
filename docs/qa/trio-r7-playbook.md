# 三人组第 7 轮 · 测试修复员 A/B 任务清单（playbook）

> **r9 对账标注（对照 `6a9f42d0`，见 trio-r9-learn-notes 第二节）**：下方 r8 标注的「全部 🔧 原样有效」已过期——测试员 A 第 2 轮（`d451c32d`）合入后，本清单的 **S-1 ✅ / S-2 ✅ / S-3 ✅ / S-4(l99 侧) ✅ / L-1 ✅（quiz99 紧凑档 + find-diff 真横屏双图并排，r9 浏览器复证）/ C-1 四款含 ak-bar ✅（=N-28 销账，守门测试已入库）** 全部销账，别再修。仍开：L-2 / L-3 / N-25 / N-26 / N-27 / N-29+N-23 / N-30 / N-31 / N-32（**N-30 古堡与 N-16/17/18、N-1 等已在 B 在途分支 `casual-duo-fit-r5-b-4683` 上，动手前先对账，撞车取先合版**）。行号漂移：level99.ts 的 469/529 现为 517/579（已修）。新账见 `trio-r9-playbook.md`（N-39 蓝本地图聚焦 + N-37 加重档）。
>
> **r8 对账标注（对照 `77c89fc8`，见 trio-r8-learn-notes 第二节）**：r7 基线 `43a79eb9` 以来 `src/**` 零提交（git 可证），**本清单全部 🔧 原样有效，无一落地**；关键源码位（level99.ts:469/:529、parentAuth、clockSVG、boardArt 头注、六款模式菜单款目录）逐一复核未动。r8 新增 N-33…N-38（壳层结算弹窗矮横屏必点钮线下为最重）见 `trio-r8-playbook.md`——修 S-4 时按 r8 版扩容（qz-jump-input 同族），其余照本清单继续执行。
>
> 依据：`trio-r7-learn-notes.md`（基线 `game-1.3 = 43a79eb9`，全部条目带实测数字与截图核验）。
> **只列仍未落地的 🔧**。r6 playbook 里唯一被收掉的是 L-4（测试员 A `640b3242` 以修代裁，见 learn-notes 第二节对账表）；r4/r5/r6 原编号（S-x/L-x/C-x/N-1…N-29）继续有效，本清单新发现从 N-30 续编。
> **注意撞车**：测试修复员 A/B 正在并行执行 r4–r6 项。动手前先 `git fetch` 对最新 `game-1.3`，凡已合入的按「后合者拉最新、撞车取先合版」纪律处理，别按本表重复修。
> 分工建议：**A = 壳层 + 闯关学习**，**B = 休闲对战动手**。
> 修补配方 A/B/C 见 r4 learn-notes 第六节，D/E/F 见 r5 第四节，G/H 与 C-1 守门口径见 r6 第四节；本轮无新配方，N-30/31 走 G、N-32 走 E。

## 通用纪律（与 r4/r5/r6 一字不差的红线）

- **不改存档 key**：`yiduo-yixing.l99.<id>` / `yiduo-yixing.l99skip.<id>` / 平台钱包 key、`meta.id` 一个字符不动。
- **不动题库/判定/关卡生成**：`levels.ts` 数据、seed 链路、win/lose 判定零触碰。
- **测试只增不减**：进场先 `npm test` 记水位（`43a79eb9` = 1090 文件 / 19248 用例全绿）；每条修复配 1 份小测试；交卷水位 ≥ 进场。
- **kit 冻结**：`src/art/kit/` 已有文件只 import 不改（stickers.ts 扩容例外）。
- **宽屏零回归**：钳高改动在 412×915 与 1280×800 复测「修前修后一致」。
- 收尾：fetch → rebase → `npm test` / `npm run build` 全绿 → 普通 push，禁 force。

## 测试步骤备忘

- 本地起服：`npm run build && npx vite preview --port 4173`；无头验证 puppeteer-core + `/usr/local/bin/google-chrome`。
- 视口五档：360×640 / 390×844 / 412×915 / **915×412（重灾主档）** / 1024×768；宽屏对照 1280×800。
- **管理员直达**：家长面板 → 管理员权限 → 密码 `kangkang`（可选时长或永久；开着时选关全解锁 + 关内「🎫 直达」）。直达与解锁不写星级存档。
- 量裁切口径同 r4（`.game-stage` clientHeight）；「crop=0 但控件够不着」先扫自滚容器（r5 口径）；带模式菜单的款逐模式点进玩法态再量（r6 模式 H）；**本轮追加：有显隐开关的控件排（如 fight-king「📱 触屏按键」）把开关打开再量**，否则假绿。
- 模式菜单款全名单（玩法态均已量过一遍，修复回归照此矩阵）：fight-king 5 模式 / duo-vs-star 6 模式 / dot-maze 4 模式 / adventure-king 3 模式 + 速通选章 / brave-path 4 模式。

---

## 壳层（给 A）

### S-1（续）首页首屏看不到游戏卡——三档一次修净 🔧

- 本轮核实 `styles.css` 0 diff（`home.ts` 只加了搜索输入 44px 热区）。实测数继续有效：360×640 首屏 0 卡（首卡 top=722）、390×844 只露 127px 半张（top=717）、915×412 横屏 0 卡（top=557）；1024×768 无伤。
- **改哪**：`src/styles.css`（必要时 `src/ui/home.ts` 结构顺序）。竖屏 `@media (max-height:740px)` 收 hero+筛选条；横屏补 `@media (max-height:500px)`。方向对齐「hero 是论点」：首屏主角是「最近玩过/继续玩」。
- **验收**：三档首屏（不滚）完整露出首行卡上半张（卡 top ≤ 视口高 − 100）；筛选条 ≥44px；1280×800 零回归。

### S-2（续）l99 星级字符星 → 12×12 内联 SVG 🔧 + S-4 `.l99-jump-input` 38→44 顺手项

- 本轮核实 `level99.ts:469` `starRowHTML` 仍 ★ 字符、`:529` 仍 `min-height:38px`。配方 = 窗口 1 r1 learner P7（双态 fill `#ffb937`/`#e3ddef`，`.l99-node-stars`/`.l99-beststars`/`.l99-ov-stars` 三处一次改净）。

### S-3（续）parentAuth 弹窗跨路由残留 🔧

- 本轮核实 `src/ui/parentAuth.ts` 仍无 hashchange 处理。r4 原文有效（hashchange 即 `finish(false)`；密码不落存储的约定一字不动）。家长门弹窗本体 915×412 干净（底 396 < 412），别顺手动布局。

---

## 闯关学习（给 A）

### L-1（续）学习 4 款横屏答题控件折叠线下 🔧（find-diff 档口径更新）

- shape-kingdom/color-fun/music-stars 三款 0 diff，r4 原文有效（915×412 下 3 钮 / 7 控 / 4 控折叠线下），走配方 B。
- **find-diff 档更新**：`640b3242` 的 regrow 修复后 915×412 关内 stage 裁切只剩 6，但**「下图」6 格在折叠线下、`.fdf-viewport` 自滚 203px**——原图与工具行看得见、要点的那张图够不着。修法从「钳格子」改为「横屏矮屏下双图并排或 viewport 钳高」，`regrowCellPx` 只放大不缩小的约定别破坏（390×844 已实测干净，勿回归）。

### L-2（续）clock-house 前 99 关题面钟两代同堂 🔧

- 本轮核实 `levels.ts:78` `clockSVG` 原样。r4 原文有效（换 arrowHandD/hubSVG/11px 刻度，`data-h`/`data-q` 与角度公式零触碰）。

### L-3（续）find-diff 盘面贴纸补第 4–10 章 🔧

- 本轮核实 `boardArt.ts` 头注原样。净活 = stickers.ts 补图 + boardArt.ts 映射加行，门控逻辑不改。

> L-4 已由 A 的 `regrowCellPx` 以修代裁结案 ✅，从本清单除名。

---

## 休闲对战动手（给 B）

> 先与最新 `game-1.3` 对账：A/B 并行批次里已修的项按已合入版为准。

### N-25（续）fight-king 格斗塔关内两档折叠线下 🔧（连续两轮最重，走配方 G）

- 本轮核实 fight-king 0 diff，r6 原文有效：915×412 进塔第 1 关裁 498 / canvas 出屏 335 / 轻击·重击·必杀·防御·⏸ 全部折叠线下；390×844 也裁 123；人机/双人对战态干净（裁 21）。
- **本轮补测护栏**：无尽连胜 915×412 裁 19 干净、战斗内暂停面板干净——修塔壳（`showTower` 出战八宫格折叠）时别扩大化碰这两态。
- 改哪/验收照 r6 playbook 原文；可与 N-31 同 PR（共用矮屏判断）。

### N-31 fight-king 训练场触屏键排 + 假人切换钮折叠线下 🔧（新，走配方 G）

- **现象**：915×412 点开「📱 触屏按键」后裁 801，轻/重/必杀/防御两排 8 键 + 站立/蹲防/随机反击 3 钮全部折叠线下——触屏孩子打不了也换不了假人；不开触屏键裁 656（键盘可玩）。412×915 / 390×844 / 1024×768 裁 315/375/469 但折叠线下 0（可用，勿动）。
- **机理**：训练场 = 画布 + 假人行 + 帧数教学面板（自滚，设计内）+ 触屏键排卷进同一条纵向流；滚动边界没有切在「读的（帧数表）」与「按的（键排/假人行）」之间。
- **改哪**：`src/games/fight-king/index.ts` 训练场壳（`fk-train-*` 一带）。矮横屏把触屏键排 sticky 置底或移画布两侧，假人切换行保持首屏内；教学面板单独限高自滚。`FIGHT_MIN_H`、帧数数据、判定零触碰。
- **验收**：915×412 开触屏键后 8 键 + 假人 3 钮不滚可点；教学面板仍可滚可读；其余三档与键盘操作零回归；补训练场壳断言。

### N-30 adventure-king 无尽古堡 13 控件折叠线下 🔧（本轮新伤最重，走配方 G）

- **现象**：915×412 进无尽古堡：裁 394，⬆⬅➡⬇ D-pad + 🔄 复位本间 / 🗺️ 小地图 / 🏁 结束这趟 / 🏛️ 打开陈列共 13 控件**全部折叠线下**——房间棋盘看得见、人走不了（截图实锤）。390×844 轻伤（裁 74，仅 🏛️ 陈列线下）；412×915（裁 16）与 1024×768（裁 62）干净。
- **机理**：古堡是 1.2 俯视探索层（`advk-` 前缀），与走廊引擎（N-16）不同家：房间 DOM 网格 + 底部控件纵向堆叠，无横屏矮屏钳制；`.ak-bar` 模式条残留（N-28）在此态还叠加吃一排高度。
- **改哪**：`src/games/adventure-king/index.ts` 古堡壳（`advk-` 一带）。横屏矮屏 D-pad 挪房间网格右侧、工具钮（复位/小地图/结束/陈列）收一行置顶或 sticky；房间格尺寸按可视余量钳。**先把 N-28 的 `.ak-bar[hidden]{display:none}` 补上再量净高**（两账一起清，见 C-1）。房间生成与钥匙/秘密判定零触碰。
- **验收**：915×412 D-pad 与四个工具钮不滚可点、房间主体 ≥60% 可见；390×844 陈列钮回屏；412×915 / 1024×768 零回归；补古堡壳断言。
- **附注**：无尽遗迹 / 计时速通与闯关同走廊引擎（本轮实测 304/193/6 控 ≈ N-16 的 332/204/6 控），**归入 N-16 一次修三态**，别单独立项也别漏验收。

### N-26（续）duo-vs-star 闯关关内 915×412 七键折叠线下 🔧（连同 r4 C-9 一次清账）

- 本轮核实 duo-vs-star 0 diff，r6 原文有效（裁 310 / canvas 出屏 111 / ◀▲▼▶✋💥🤝 七键整排线下）。
- **本轮补测护栏**：双人对战/人机混战/团队赛/合作特训/无尽车轮战五模式全部轻伤级（裁 62–204、键排全在屏、画布顶裁 13–62px）——修闯关时五模式做回归即可，别当重伤陪修；顺手把画布顶部小裁收掉更好。
- 改哪/验收照 r6 playbook 原文（配方 G 双栏或钳 `.dvs-canvas`；`.dvs-back` 补 `min-height:40px` + 取反断言）。

### N-27（续）dot-maze 四模式方向键折叠线下 🔧（走配方 G，一次修四态）

- 本轮核实键排 0 diff（`8a4d576f` 只动进门菜单白底，且 915×412 菜单裁 0 无回归）。**扩围**：闯关裁 167（5 键线下）/ 双人追逃 143（9 控件）/ **无尽 121（◀▼▶ 3 键）/ 抢豆 143（两套键 12 控件）**——四模式全中，共用同一键排布局。
- 改哪照 r6 原文（横屏双栏：单人 D-pad 挪画布右侧，双人一人一侧）；**验收矩阵 = 4 模式 × 915×412 + 竖屏零回归**，补横屏排布断言。

### C-1（收尾扩容）modebar `[hidden]` 残余 4 款 + 修正口径守门测试 🔧（先做，半小时级）

- 本轮核实四款仍未动：box-hamster `.bh-modebar`（40px）/ prince-princess `.pcp-modebar`（82px，进模式还要补 `bar.hidden=true`；**注意 `0e9100fc` 只是给 `.pcp-btn` 加热区，别误当已收**）/ sudoku-petal `.sp-modebar`（154px）/ adventure-king `.ak-bar`（N-28，412×915 残留 88px、1024×768 残留 40px，古堡态还在叠加——见 N-30）。
- 守门测试按 r6 修正口径写（CSS `display:flex` 的 modebar 族类名 **且** 同文件对该元素设过 `.hidden = true` 才纳管，防 bba-modes/cds-modes/bvp-bar/dvs-bar 四个误报）。13 款 r4 测试欠账一并补或由守门测试统一覆盖。
- **验收**：四款进模式后 `getComputedStyle(bar).display === "none"`、高度 0，退出还原；守门测试全库绿（未修前四款当场红，正好当验收）。

### N-29（续）bubble-aim 关内横屏发射台出屏 🔧 + N-23（补充版）三款地图 focusCurrent 🔧

- 本轮核实 bubble-aim / candy-swing / sling-birds 全部 0 diff，r6 原文有效：N-29 = 915×412 关内 canvas 显示高 480 > 视口 412、出屏 206、发射台整个线下（配方 B 之 1 钳 `.ba-canvas`）；N-23 补充 = 三款地图渲染后当前关 `scrollIntoView({block:"center"})`（蓝本 `level99.ts:871`）+ bubble-aim clamp 下限 420 > 视口 412 的死角改按可视余量取小。
- 同款同文件，建议一个 PR 一起动；验收照 r6 原文。

### N-32 brave-path 无尽地牢战斗三钮折叠线下 🔧（新，走配方 E）

- **现象**：915×412 进无尽地牢第 1 层战斗：裁 268，👊 攻击 / 🛡️ 防御 / 🍓 莓果三个**每回合必点钮**折叠线下——滚着打等于回合时长翻倍。同款闯关模式（l99）干净（裁 38），菜单层裁 324 已在 r4 C-8 账上。
- **改哪**：`src/games/brave-path/index.ts` 无尽层战斗壳。配方 E：操作行 `position:sticky; bottom:0`（不透明底 + 上缘阴影），或战况区/敌人卡钳高。战斗数值、莓果计数、层数生成零触碰。
- **验收**：915×412 战斗三钮不滚可点、敌我状态可见；竖屏/平板零回归；补战斗壳断言。备战小屋长列表（裁 1860）属设计内别动。

### r5 未动项重申（原编号有效，本清单不重抄）

- **N-1 fruit-catch**（裁 741/出屏 617 + 平板 415/281，r5 单款最重、三轮未动）、**N-2/N-3/N-4 回合必点组**（flight-chess/star-estate/hero-cards，配方 E，可与 N-32 打包）、**N-5…N-9、N-20 盘面完整可见组**（配方 B 之 3）、**N-10 棋类三款横屏**、**N-11…N-15 + N-19 stagePlayRoom 家当组**（配方 F）、**N-16 实时触控键组之 adventure-king**（本轮确认走廊引擎三态同伤一次修全）、**N-17/N-18**（prince-princess/box-hamster，与 C-1 同款可并 PR）、**N-21/N-22 已修款横屏残余**（block-drop/combo-clash）、**S-1 之 N-24 首页横屏**（并入 S-1）。全部照 r5 playbook 原文执行。
- **r4 未动项**：C-2 brick-break / C-3 snake-snack / C-4 snake-royale / C-5 mole-pop / C-6 alien-seek / C-7 match-stars / C-8 名单（本轮 915×412 复证与原文一致：iff 248/18 控、snow-fight 191/12 控、shoot-range 314/6 控、puzzle-tiles 564、monster-crisis 137、balloon-pop 236、duo-rush 234、duo-arena 199）/ C-9（并入 N-26）。照 r4 playbook 原文执行。

---

## 完成定义（两人共用）

1. 全部 🔧 关账或书面降级（降级写数学/物理理由，照 BL-W6-03 格式）。
2. `npm test` / `npm run build` 全绿，用例水位只增不减（进场 = 1090 文件 / 19248 用例）；每条修复有配套测试或取反断言。
3. 本轮新伤（N-30 古堡、N-31 训练场、N-32 无尽战斗）在 915×412 复测截图或数字留档，逐条对账 N 编号；模式菜单款修复按「模式 × 视口」矩阵验收（fight-king 5 / duo-vs-star 6 / dot-maze 4 / adventure-king 3+选章 / brave-path 4）。
4. 报告按既有命名续档，对账本 playbook 与 r4/r5/r6 playbook 四方编号；与并行批次撞车的按「先合版」纪律记录明细。
