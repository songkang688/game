# 1.3 窗口 4 · 第 2 轮视觉验收 · 学习优化员报告

> 分支 `game-1.3-window4`。规格模板 `docs/plan-1.3-step28-B-learner.md`，范围收窄为本窗 9 款：
> `duo-rush` `duo-arena` `duo-vs-star` `sling-birds` `candy-swing` `gold-hook`
> `garden-guard` `sprout-defense` `monster-crisis`。
> 只验视觉；本人可改范围＝本报告＋这 9 款的纯文案（guide / blurb / 提示语）＋文案快照测试。
> 绘制代码一行不碰；A 档（tester）与 C 档（fixer）的报告一字不碰。

## 学习计划（动手前提交）

1. **回看采纳率**：对照 `docs/qa/1.3-window4-round1-fixer.md` 与合并态源码，逐条核对第 1 轮
   TOP10 的采纳状态与落地效果；已知 TOP2 / TOP3 被 C 档独立修掉（交叉验证已写进第 1 轮报告），
   其余逐条读码验证；未采纳的修订后重新排序。
2. **补盲区（本窗版）**：本窗没有棋牌 / 教育 / 养成，改为把第 1 轮密度差 1 层的 3 款
   （duo-vs-star 中景带 / gold-hook 地表天空 / monster-crisis 天空动效）以及
   duo-arena 顶视「少而精」留白的基准写透，给出可直接执行的绘制规格。
3. **横向一致性**：9 款放在一起看像不像一家人，挑最违和的最多 5 款，写清违和在哪，
   给 C 档统一化建议（绘制规格级）。
4. **文案优化**：把 C 档第 1 轮修复亮点（garden-guard 手绘花瓣币 / 徽章、duo-arena 技能图标
   与星阶、sling-birds 绘制角标）讲进对应引导；核对第 1 轮文案未被 rebase 冲掉；
   每处改动登记在第六节。
5. **商标风险**：继续维护「题材 → 易踩线对象 → 规避要点」清单，点名第 1 轮修复后仍有风险的点。
6. **收尾**：`npm test` / `npm run build` 全绿 → fetch → rebase → 普通 push `origin game-1.3-window4`。

学习红线（重申）：只学画面密度与角色剪影，只记文字结论；不下载、不截图入库、不描摹造型；
商标与官方角色连「像」都不许像。

（以下各节待执行后填写。）
