# 三人组父监督任务 · 10 轮（r9–r18）合入 `game-1.3`

> 用户指令：两条测试修复子进程全方位测+不好用立刻修；一条学习子进程读别人项目与 skills、只记录；空闲立刻派下一轮；学习清单下一轮由 A/B 执行；全部推到 `game-1.3`。
> 本文件由父监督维护。已有 trio r1–r8 与测试员 A 第 1–2 轮合入；本战役从 **r9** 起再走 **10 轮**。

## 角色

| 子进程 | 角色 | 每轮产出 |
| --- | --- | --- |
| A | 测试修复员 · 壳层 + 闯关学习 | 真测 + 立刻修 + `docs/qa/trio-rN-tester-A.md` |
| B | 测试修复员 · 休闲 / 对战 / 动手 | 真测 + 立刻修 + `docs/qa/trio-rN-tester-B.md` |
| C | 学习优化员 | **零代码**；`trio-rN-learn-notes.md` + 给下一轮的 `trio-rN-playbook.md` |

## 红线（每轮一字不差）

- 不改存档 key / `meta.id`；不动题库、seed、win/lose 判定。
- 测试只增不减；kit 已有文件只 import 不改（`stickers.ts` 扩容例外）。
- 宽屏 1280×800 零回归；禁 force push。
- 脚本与截图放 `/tmp`，不进库。
- 撞车纪律：后合者拉最新 `game-1.3`，取先合版。
- 文件分家：A 不动 B 的游戏目录，B 不动壳层/`level99.ts`/`quiz99.ts`/`styles.css`（N-33 结算弹窗属 A）。C 只写 `docs/qa/trio-rN-*`。

## 进场时点（战役第 1 轮 = trio r9）

- 基线：`origin/game-1.3`（监督开场已 fetch）。
- **r8 笔记基线 `77c89fc8` 已过时**：其后测试员 A 已合入 S-1 / S-2 / S-4 / S-3 / L-1 / C-1（含 `.ak-bar`）/ 竞技场留白 / garden-guard 节点图。C 第 1 件事是对账销账，禁止把已修项再派给 A。
- r8 新伤仍有效：N-33 结算弹窗、N-34/35 拼音深关、N-36 描红、N-37 root 挤压、N-38 永久分钟文案。
- r5–r7 重灾仍大多未动：N-25 格斗塔、N-1 fruit-catch、N-30 古堡、N-26/27、N-31/32。

## 轮次看板

| 战役轮 | trio | A | B | C | 合入 `game-1.3` |
| --- | --- | --- | --- | --- | --- |
| 1 | r9 | 进行中 | 进行中 | 进行中 | 待 PR |
| 2–10 | r10–r18 | 排队：执行上一轮 C 的 playbook | 同左 | 排队：对账本轮 A/B + 新抽验 | |

## 第 1 轮（r9）派工摘要

- **A 先做**：N-33（配方 I）→ N-38 一行 → N-37 → N-36 → 收藏册 44px → 有余力再 L-2/L-3。勿重做 S-1/S-2/S-3/S-4/L-1/C-1。
- **B 先做**：C-1 若 HEAD 已绿则跳过 → N-25+N-31 同款 → N-1 → N-30 → N-26+N-27。勿扩大化碰 fk 无尽连胜 / dvs 非闯关五模式。
- **C**：对账 A 已合入项；抽未测维度（失败态、双人第二手柄、横屏+root、其它深关 `isXxxLevel`）；读 `.cursor/skills/1.3-visual/*`；写 r9 笔记 + r10 playbook。编号从 N-39 续。
