# 窗口 8 · 1.3 视觉升级 · 第 3 轮终验监督修复员报告

- 角色：窗口 8 三人验收组 · C 监督修复员（第 3 轮 / 共 3 轮 · **终验清零收官**）
- 工作分支：`game-1.3-window8`（基线 `origin/game-1.3`）
- 范围锁死本窗 12 款：
  `red-blue-race` `red-blue-tap` `red-blue-tug` `clock-house` `math-farm` `pinyin-train` `word-garden` `shape-kingdom` `find-diff` `color-fun` `music-stars` `kitty-care`
- 权限边界：只动绘制层（绘制函数 / CSS / 视觉常量 / `src/art/kit/` 新增或本窗独占文件——
  `stickers.ts` / `runnerDuoTrim.ts` 是本窗 C 档自建可继续扩）；
  判定、数值、关卡数据、存档 key、玩法测试断言、`parentAuth.ts` / `parentGate.ts` 一律不碰；
  其余冻结 kit 只 import 不改；别人的报告只读不改；教育三连不改题目对错；
  find-diff / clock-house 题库被 SHA 钉死不改题目数据；禁止 three.js。
- 实际使用模型 slug：`claude-fable-5-thinking-xhigh`。

## 清零计划（动手前提交）

输入：本轮 A 档 `1.3-window8-round3-tester.md`（终验结论：**带条件放行**，746 文件 / 15563 例，
SHA `a7bb10af`）、第 2 轮遗留 `1.3-window8-round2-fixer.md`（第四节 50 张贴纸设计稿）。
开工时本轮 B 档 `1.3-window8-round3-learner.md` 与 `1.3-window8-style-baseline.md` 尚未落地——
按覆盖指令先依 A 档开修并遵守既有两轮固化的 PASTEL / 左上光照 / 描边工序，B 档出了再跟。

按 A 档终验交付的优先序清零：

1. **（严重 · 放行条件①）W8R1-04 余量**：find-diff 第 4–10 章 50 张贴纸按第 2 轮
   fixer 第四节设计稿实施——工序沿 stickers 图集统一约定（双色分面 + 2px 深描边 +
   左上高光 + 软投影，48×48 viewBox）；FLIPPABLE ▲16 种非对称契约逐张落实并加用例；
   双胞胎对「像但可辨」区分位逐对落实；全部过去色剪影 / 商标自查（🧸🚀 避开知名造型、
   🧿 通用装饰珠不做宗教细节、💣 类伤害暗示本池不涉及）。题库 `levels.ts` 一字不动，
   走既有 `boardArt.ts` 整关门控自动开闸；配齐一章亮一章、一章一个 commit。
   补齐后 188 关全量回归（含 rush 多轮 / mirror / 三图 / 无尽前若干轮）确认无混排无漏网；
   round3 scan 图集 ≥143 ratchet 与前 3 章不熄灯 ratchet 必须继续绿。
2. **（建议 · 放行条件②）shape-kingdom 问答关城堡氛围帽**：按 B 档第 2 轮修订清单
   第 7 条，`castleSvg(0)` 静态复用、一处 innerHTML、不进答题区包围盒、不改判定。
3. **备档（可并轮可后置，能修则修）**：music-stars 星轨远端锚点星（B 档修订清单第 8 条，
   data-URI 字符串追加 2px 小星）；pinyin-train 打孔圆点下同类色圆点副记号
   （B 档修订清单第 6 条推荐的形状通道方案，与第 2 轮 8px 加宽叠加）。修不动写入 backlog。
4. **W8R1-09 壳层装饰星 / 顶栏**：不改壳（覆盖指令明令），写入
   `docs/qa/1.3-window8-backlog.md` 移交全局。
5. 终验新发现 0 条，无当轮清零动作；不发明玩法改动。

每修一处一个 commit（`fix(visual-r3): <款> <问题>`），每处配套测试钉住修后状态，
用例只增不减。收尾走第五节流程：fetch → rebase → `npm test` / `npm run build` /
`npx vite preview` 冒烟 → 记录终态 SHA / 用例曲线 / 体积对比 → 普通 push。

---

## 一、清零清单

说明：B 档本轮报告与风格基线（`ca25b75e`）在本档修复中途落地，已回读对照——三处新修
全部符合基线条目（氛围帽 B-38 装饰不进答题区包围盒、B-39 城堡剪影去色自查自动继承；
锚点星与副记号分别沿 `starTheme` / `TRAIN_COLORS` 既有 token，B-03 达标）；B 档终评
「无新增点名交 C 档」，故本轮清零面 = A 档终验交付的 4 项 + B 档商标终审移交 1 项（入 backlog）。

### 1.1 放行条件①（严重 · A 终验表唯一 ❌ 格）：find-diff 第 4–10 章 50 张贴纸

- **来源**：A 终验表专项①唯一不达标格 = 第 2 轮遗留 W8R1-04 余量（严重 · 推进中），
  A 档终验裁决「合理降级 → 转带条件放行条件①，交本轮 C 档按稿实施 + 188 关全量回归」。
- **位置**：`src/art/kit/stickers.ts`（本窗 C 档自建图集，143 → **193 张**）；
  门控消费端 `src/games/find-diff/boardArt.ts` **零改动**（既有 `sceneStickersReady()`
  整关门控自动开闸）；题库 `levels.ts` 一字未动（SHA-256 快照全绿）。
- **修法**：按第 2 轮 fixer 第四节设计稿逐章实施，工序沿图集统一约定（双色分面不走
  `<defs>` 防重复 id + 2px 深描边 `stickerOutline` + 左上高光 `hi()` + 底部软投影 `gs()`，
  48×48 viewBox）；配齐一章亮一章、一章一个 commit：

| 章 | 张数 | 图案与要点 | commit |
| --- | --- | --- | --- |
| 4 甜品小屋 | 7 | 🍰🎂🧁🍩🍫🍭🍮；双胞胎 🍰↔🎂（切片草莓 vs 双层蜡烛）/ 🍭↔🍬（圆盘棒 vs 扭纸）/ 🍩↔🍪（中孔糖霜 vs 巧克力豆） | `34127167` |
| 5 夜空营地 | 4 | 🪐🌈🌠🔭（🌙⭐🌟✨☁️ 复用既有图集）；🌠↔✨ = 单星长拖尾 vs 三星簇 | `158380cd` |
| 6 玩具城堡 | 7 | 🧸🚂✈️🚁🏀🎈🥁；🚂↔🚗 = 烟囱冒烟、✈️↔🚁 = 固定翼 vs 顶置旋翼、🏀↔⚽ = 橙底弧线缝 vs 五边形；🧸 通用简笔过去色剪影自查 | `3eb49419` |
| 7 三图侦探社 | 9 | 🔍🧩🗝️📜🕯️🎩🧭📒📕；FLIPPABLE ▲ 六张非对称剪影（柄 / 凸耳 / 齿花 / 卷芯 / 线圈 / 书页口偏一侧）；📒↔📕 = 金黄线圈角标 vs 正红页口丝带 | `088ca03f` |
| 8 旋转灯塔 | 9 | 🌀🚦🛟⚓🪁🎐🌪️🛶🧿；FLIPPABLE ▲ 五张（旋涡尾 / 漏斗尖 / 风筝尾穗 / 纸签 / 翘船头偏一侧）；🌀↔🌪️ = 圆盘螺旋 vs 上宽下尖漏斗；🧿 = 通用蓝白同心圆装饰珠、不做宗教细节 | `5bb442b1` |
| 9 镜像水面 | 5 | 🪞🫧🌊🪷🪸；▲ 🌊 浪头左卷浪尾右摊；🫧↔💧 = 三连泡 vs 单滴、🪷↔🪸 = 整朵花荷叶座 vs 分叉珊瑚枝 | `5e0c9f63` |
| 10 连环挑战场 | 9 | ⏱️🏅🎯🎲🚀💎🔔🏁⌛；▲ 四张（表冠偏侧 / 绶带甩左 / 尾焰斜飞 / 旗面甩右）；⏱️↔⌛ = 圆表盘表冠 vs 细沙腰木框；🚀 圆头圆窗通用简笔 | `f175b3e0` |

- **配套用例**（`boardArt.test.ts` 改造 + `stickers.test.ts` 探针换发，净增 5 例）：
  - **188 关全量回归**：十章全亮灯下，`LEVELS` 188 关逐关逐轮（含 rush 多轮 / mirror /
    三图）+ 无尽模式前 30 轮，`sceneStickersReady` 全真、每格产出贴纸 SVG——
    **无混排、无漏网**；`LEVELS.length === 188` 钉死题库未被碰；
  - **FLIPPABLE 非对称契约**（机器可验）：sharp 把每张 ▲ 贴纸栅格化取 alpha 剪影、
    与水平镜像逐像素比对，**非对称度 ≥10% 全表达标**；对称对照组（⭐🔵）**<5%** 防用例失真；
  - **双胞胎区分位**：`TWIN_CUES` 覆盖 `levels.ts:LOOKALIKE` 全部 13 对，每对钉
    「区分位 SVG 片段只出现在自己那张、双胞胎那张没有」；
  - **门控机制不因全亮灯而失守**：关闸路径改用未收录图案（🤷）合成场景继续钉住
    `sceneStickersReady === false`（原「第 4 章还没亮」用例的空转替换）；
  - 回归 commit `6b635700`；round3 scan ratchet（图集 ≥143、前 3 章不熄灯）继续绿，
    终态注册数 **193 张**。

### 1.2 放行条件②（建议 · A 终验裁决交本轮）：shape-kingdom 问答关城堡氛围帽

- **来源**：第 2 轮遗留（B 档修订清单第 7 条）→ A 档终验裁决「交本轮 C 档实施」= 放行条件②。
- **位置**：`src/games/shape-kingdom/review.ts`（`runQuizWithReview` 装配段 + `REVIEW_CSS`）；
  `kingdom.ts` / 判定 / 公共答题器 `quiz99.ts` 零改动。
- **修法**：按 B 档规格原文——问答关顶部 `castleSvg(0)` 剪影紫静态复用、**一处 innerHTML**；
  挂在提示条面板顶部、答题器宿主 `.shk-quizhost` 之外（**不进答题区包围盒**，基线 B-38）；
  `aria-hidden` + `pointer-events:none` 纯装饰；lit=0 六段全剪影、无描金无升旗无动画类，
  天生静态 reduced 无关。矮屏两档退让：≤720（沿作图关 `SHORT_SCREEN_PX` 同门槛）收一档
  140px 不藏帽——**360×640 验收基准屏必须看得见**；真挤的 ≤480 横屏才整顶藏掉。
  浏览器 360×640 实测：问答关首屏剪影紫城堡在位（33px 高、居中、不接指针）。
- **配套用例**：新增 `quizCastle.test.ts` **5 例**（castleSvg(0) 字符串级原样复用 / 纯装饰
  边界 aria-hidden+pointer-events / 不进答题区包围盒且面板序第一 / 矮屏两档且禁止 720 档
  display:none / destroy 不留孤儿节点）。
- **commit**：`5b27d607` 实施 + `58ae46a1` 矮屏档位修正（720 档由藏改收，否则验收屏白做）。

### 1.3 备档两条（B 档修订清单第 8 / 6 条）：能修则修 → 均已修

- **music-stars 星轨远端锚点星**（来源：第 1 轮第 11 条备档 → B 档修订清单第 8 条）：
  `starTheme.ts` `skyStageSvg()` data-URI 字符串追加——三颗四角小星（`ANCHOR_STARS`）
  钉在第 1/3/5 条星轨线右端（x≥90），纯 fill 静态、无 SMIL、reduced 无关；谱线
  「宽 2 光晕 + 宽 0.6 亮芯」两笔工序既有计数不动。配套用例 `starTheme.test.ts` +1
  （颗数 2–3 / 远端 / 纵坐标锚在 `STAFF_LINE_YS` 上 / 无动画）。commit `290133f6`。
- **pinyin-train 打孔圆点下同类色圆点副记号**（来源：第 1 轮 TOP10 二选一的另一案 →
  B 档修订清单第 6 条推荐的形状通道方案）：`scene.ts` `ticketBg()` 一处模板字符串追加
  radial-gradient——孔下 9px 处 r=2px 同类色实心圆，与第 2 轮 8px 边条加宽叠加成
  「色 + 形」双通道；孔底到圆点顶留 3.5px 不粘连、`CHIP_MIN_PX=48` 最矮票内画得下，
  热区与字号下限零改动。浏览器实测 360×640 三类车票副记号可见。配套用例
  `ticketBand.test.ts` +2（六处票面 + quiz 皮肤同步 / 层序与纪律）。commit `8e829e93`。

### 1.4 不动项（按裁决执行）

- **W8R1-08** find-diff 26px 盘面格触区：A 档终验裁决**结案**，本档遵照未改热区；
- **W8R1-09** 壳层装饰星 / 顶栏：覆盖指令明令不改壳，登记 `1.3-window8-backlog.md` BL-01 移交全局；
- **B 档商标终审移交**（`train.ts:10` 注释级品牌词）：`train.ts` 属冻结 kit 超本档权限，
  登记 backlog BL-02（两案处置建议原文收录）；
- A 档终验新发现 **0 条**，无当轮清零动作；玩法逻辑一行未动。

---

## 二、降级申请记录与裁决结果

- **本轮新降级申请：0 条。**
- 第 2 轮对 W8R1-04 余量的书面降级（现象 / 修不动原因 / 影响面 / 后续计划四要素）已由
  A 档终验裁决「**合理降级 → 转带条件放行条件①**」；本轮按稿全量落地并过 188 关回归后，
  该降级状态终结、条件①关闭（见第五节）。

---

## 三、backlog 条目统计

`docs/qa/1.3-window8-backlog.md` 成文：**共 2 条，均建议级**——BL-01 壳层装饰星 / 顶栏
统一件（W8R1-09，移交全局统一窗）、BL-02 kit `train.ts` 注释级品牌词（B 档商标终审移交，
kit 冻结超本窗权限）。阻断 / 严重级入册 0；两条备档增强项因已当轮修复不入册。

---

## 四、kit 终态整理与纪律自查

- 本档三轮新增文件全数有引用（`stickers.ts` / `runnerDuoTrim.ts` / `dotArt.ts` /
  `boardArt.ts` / `faceLift.ts` 及配套测试），**无死文件需删**；别人的 kit 文件与公共导出
  零触碰（本轮唯一动到的非独占绘制文件 = `review.ts` / `starTheme.ts` / `scene.ts` 三处
  本款独占模块，均属绘制层）。
- 历史 skip 用例 0（A 档 round3 scan 第 4 例钉着「防线文件在位且不许 skip」）；
  用例只增不减（见第五节曲线）。
- 判定 / 数值 / 关卡数据 / 存档 key / 玩法测试断言 / parentAuth / parentGate 零触碰；
  find-diff / clock-house 题库 SHA 快照全绿；教育三连题目对错零改动；未用 three.js。

---

## 五、终态合并数据与放行条件关闭状态

- **合并**：三人终验报告 + `1.3-window8-style-baseline.md` + 全部修复确认同枝在位
  （B 档 `f28f11bf` 后本档 13 个 commit 经 rebase 落其上，他人文件零触碰）。
- **`npm test`**：**747 文件 / 15576 例全过**。三轮曲线（只增不减达标）：
  R1 合并态 736/15507 → R2 合并态 745/15559 → R3 A 档水位 746/15563 →
  **本档终态 747/15576**（+1 文件 / +13 例，全部为本档修复配套）。
- **`npm run build`**：绿（tsc --noEmit + vite build）；PWA precache
  **192 项 / 5452.22 KiB**。体积曲线：进窗基线（`origin/game-1.3`）191 项 / 5307.75 KiB →
  R1 5385.52 → R2 5407.10 → 终态 **5452.22 KiB**——对 R2 **+0.83%**、对进窗基线
  **+2.72%**，远低于 15% 说明线；增量 = 50 张贴纸 SVG 字符串（stickers chunk 131.15 kB，
  gzip 30.56 kB）。
- **`npx vite preview` 冒烟**：`/`、`/sw.js`、`/manifest.webmanifest` 全部 **200**；
  随机 5 款（find-diff / shape-kingdom / music-stars / pinyin-train / red-blue-tap）
  360×640 headless Chrome 各 **30 秒随机点按**：pageerror 0、横向溢出 0、无空白无
  视觉崩坏（尾帧截图目检；shape-kingdom 问答关氛围帽 / pinyin-train 副记号在截图中实锤）。
- **放行条件关闭状态**（A 档终验已给「带条件放行」，两条条件本档落地）：
  - **条件① 已关闭**：50 张贴纸按稿实施完毕（图集 143→193），188 关全量回归 + 无尽 30 轮
    无混排无漏网，FLIPPABLE 契约与双胞胎区分位机器可验，ratchet 继续绿；
  - **条件② 已关闭**：氛围帽按 B 档规格落地，360×640 浏览器实测在位，判定零改动。
  - 备档两条（不作放行前提）亦已落地，超额完成。

---

## 六、窗口 8 · 1.3 视觉升级收官声明

窗口 8（12 款）1.3 视觉升级三轮验收到此收官。终态为 `game-1.3-window8` 分支
HEAD（终态 SHA 见下），`npm test` 747 文件 / 15576 例全绿、`npm run build` 全绿、
preview 冒烟全绿；A 档终验结论「**带条件放行**」的两条条件已全部落地并关闭，
另两条备档增强项一并完成；阻断 / 严重级问题终态 **0**；遗留 backlog **2 条**
（均建议级、均因超窗权限移交，见 `docs/qa/1.3-window8-backlog.md`）。
三轮曲线：六大专项达标率 91.7% → 97.2% → 98.6%（唯一 ❌ 格的缺口内容已按稿补齐、
有用例钉死）；用例 15507 → 15559 → 15576 只增不减；体积对进窗基线 +2.72%。
交付物即本分支终态：不合并、不动 `game-1.2` 与 `main`、未 force、未开 PR。

- 终态（合并态复验）SHA：**`58ae46a1`**——最后一个修复 commit，第五节的
  test / build / preview 数据均基于它；本报告与 backlog 的 docs commit 落其上后
  普通 push `origin game-1.3-window8`，分支 HEAD 即交付终态。
- 实际使用模型 slug：`claude-fable-5-thinking-xhigh`。
