# 1.2 升级第 37 步 / 共 57 步 —— 双人冲刺 · 擂台 · 弹飞混战
> 每代理 1 款。派发整段提示词，末尾标明你是 A/B/C。新游戏暂定 id 以主管 catalog 为准。

---

## A —— `duo-rush` 朵星双人冲刺

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你就是被派发的执行者。禁止再用 Task 派生云端子代理。只推 game-1.2，禁止 force，禁止改 main。】

仓库 https://github.com/songkang688/game ，产品「一朵一星」，1.2 升级第 37 步 / 共 57 步，角色 **A**，独占 `duo-rush`「朵星双人冲刺」。

## 分支纪律
- `git fetch origin game-1.2`，工作分支建在 `origin/game-1.2` 上。
- 开工先提交一条 git 记录（角色 + 本款计划），再改代码。
- 收尾：fetch → rebase `origin/game-1.2` → `npm test && npm run build` 全绿 → 普通 push。
- 模型 slug 必须是提示词里的 `claude-opus-5-thinking-high-fast`；完成后在回复里写实际 slug。

## 1. 现状审查（先读源码 / 第一版，再动手）

通读 `src/games/duo-rush/`（`meta.ts` `index.ts` `logic.ts`/`levels.ts`/`engine.ts` 及测试、`guide.ts`）。对照 1.1 派发与计划文档，以**仓库真实代码**为准。

1. 障碍必须对称（已有则加强单测）。
2. 窄屏上下分屏角色被压扁。
3. AI 三档若会穿障碍要修。

## 2. 通行规则对照（检索后的要点，必须进实现）

- 公平：同一 seed 两边障碍镜像或完全相同。
- 土狼/缓冲与 rainbow-run 同哲学但参数可不同。

结构参考：可参考同类开源的纯逻辑拆分，不引入运行时依赖、不带商标素材。

## 3. 玩法升级

| 模式 | 决策 |
| --- | --- |
| 闯关 | 可加短赛道杯 188 或保持纯竞速；若加关必须 openLevel。 |
| 对战 | 核心，键位朵朵 WASD 星星方向键。 |
| 无尽 | 1.1 要求无尽竞速+幽灵，缺则补。 |

- 幽灵跑
- 人机三档反应延迟

**2.5D / 3D 决策：** 若 1.1 第 6 步已 2.5D 分屏，在其基础上打磨，不要推倒。未合入则补伪 3D 三车道。不做真 3D。

## 4. 视觉 / 建模 / 动效（含手机）

- 分屏分隔线 ≥ 4px
- 360px 自动上下分屏

- 360px 不溢出：字号 ≥ 14px，正文对比度 ≥ 4.5:1，触控热区 ≥ 44px。`prefers-reduced-motion` 下降动画时长。
- 面向约小学六年级，粉彩萌系，失败只鼓励。禁止商标 / 官方角色名（注释也不许）。

## 5. 跳关 / 解锁（游戏侧）

无固定 188 关表：把 `level` 映射到人机档或残局序号（在回复里写对照表）。仍要提供 skip/unlock 钩子给家长演示。不要自己做密码。

## 6. 文件所有权

只许碰 `src/games/duo-rush/` 与该游戏测试。公共框架（`level99.ts`、`src/ui/*`、`src/engine/*`、`src/styles.css`、`index.html`、`home.ts`）**只读**。
不要改 rainbow-run。

## 7. 测试与验收 / 不要做什么 / 邻接冲突

新增测试只增不减。`npm test` 与 `npm run build` 必须绿。

- 障碍对称
- 两套键位互不抢占
- destroy 双 rAF

不要做什么：
- 不要改 rainbow-run 摄像机
- 不要合并两款跑酷

与邻接游戏的冲突点：
- 方向键与彩虹跑跑；本游戏 mount 独占
- CSS `dr-`

完成后回复：改了哪些文件、模式对照、测试结果、**实际使用的模型 slug**。
~~~~

---

## B —— `duo-arena` 朵星擂台

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你就是被派发的执行者。禁止再用 Task 派生云端子代理。只推 game-1.2，禁止 force，禁止改 main。】

仓库 https://github.com/songkang688/game ，产品「一朵一星」，1.2 升级第 37 步 / 共 57 步，角色 **B**，独占 `duo-arena`「朵星擂台」。

## 分支纪律
- `git fetch origin game-1.2`，工作分支建在 `origin/game-1.2` 上。
- 开工先提交一条 git 记录（角色 + 本款计划），再改代码。
- 收尾：fetch → rebase `origin/game-1.2` → `npm test && npm run build` 全绿 → 普通 push。
- 模型 slug 必须是提示词里的 `claude-opus-5-thinking-high-fast`；完成后在回复里写实际 slug。

## 1. 现状审查（先读源码 / 第一版，再动手）

通读 `src/games/duo-arena/`（`meta.ts` `index.ts` `logic.ts`/`levels.ts`/`engine.ts` 及测试、`guide.ts`）。对照 1.1 派发与计划文档，以**仓库真实代码**为准。

1. 可能仍无关卡表。
2. 礼物/炸弹判定若瞬结算，补抛物线。
3. 窄屏半场过扁点不到。

## 2. 通行规则对照（检索后的要点，必须进实现）

- 分屏公平、命中半径、三局两胜。

结构参考：可参考同类开源的纯逻辑拆分，不引入运行时依赖、不带商标素材。

## 3. 玩法升级

| 模式 | 决策 |
| --- | --- |
| 闯关 | 可加 188 小挑战（道具种子）。 |
| 对战 | 核心三回合点点。 |
| 无尽 | 连胜先到 7。 |

- 人机档：乱点 / 会躲炸弹 / 会抢金币
- 无尽连胜

**2.5D / 3D 决策：** 2D 上下分屏。不要 3D。

## 4. 视觉 / 建模 / 动效（含手机）

- 360px 每半场最小高度
- 炸弹下落可见阴影

- 360px 不溢出：字号 ≥ 14px，正文对比度 ≥ 4.5:1，触控热区 ≥ 44px。`prefers-reduced-motion` 下降动画时长。
- 面向约小学六年级，粉彩萌系，失败只鼓励。禁止商标 / 官方角色名（注释也不许）。

## 5. 跳关 / 解锁（游戏侧）

无固定 188 关表：把 `level` 映射到人机档或残局序号（在回复里写对照表）。仍要提供 skip/unlock 钩子给家长演示。不要自己做密码。

## 6. 文件所有权

只许碰 `src/games/duo-arena/` 与该游戏测试。公共框架（`level99.ts`、`src/ui/*`、`src/engine/*`、`src/styles.css`、`index.html`、`home.ts`）**只读**。
邻接游戏目录一个字都不要动。

## 7. 测试与验收 / 不要做什么 / 邻接冲突

新增测试只增不减。`npm test` 与 `npm run build` 必须绿。

- 命中半径
- 键位不抢
- 连胜存档前缀

不要做什么：
- 不要做成 fight-king

与邻接游戏的冲突点：
- 点按与 duo-vs-star 击退不是同一套，不要复用全局物理

完成后回复：改了哪些文件、模式对照、测试结果、**实际使用的模型 slug**。
~~~~

---

## C —— `duo-vs-star` 朵朵大战星星

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你就是被派发的执行者。禁止再用 Task 派生云端子代理。只推 game-1.2，禁止 force，禁止改 main。】

仓库 https://github.com/songkang688/game ，产品「一朵一星」，1.2 升级第 37 步 / 共 57 步，角色 **C**，独占 `duo-vs-star`「朵朵大战星星」。

## 分支纪律
- `git fetch origin game-1.2`，工作分支建在 `origin/game-1.2` 上。
- 开工先提交一条 git 记录（角色 + 本款计划），再改代码。
- 收尾：fetch → rebase `origin/game-1.2` → `npm test && npm run build` 全绿 → 普通 push。
- 模型 slug 必须是提示词里的 `claude-opus-5-thinking-high-fast`；完成后在回复里写实际 slug。

## 1. 现状审查（先读源码 / 第一版，再动手）

通读 `src/games/duo-vs-star/`（`meta.ts` `index.ts` `logic.ts`/`levels.ts`/`engine.ts` 及测试、`guide.ts`）。对照 1.1 派发与计划文档，以**仓库真实代码**为准。

1. 击退纯函数应已有；检查无限击飞。
2. 窄屏 4 人挤成一团。
3. 道具名复查商标。

## 2. 通行规则对照（检索后的要点，必须进实现）

- 击退值累积、方向、边界爆出。无血。

结构参考：可参考同类开源的纯逻辑拆分，不引入运行时依赖、不带商标素材。

## 3. 玩法升级

| 模式 | 决策 |
| --- | --- |
| 闯关 | 188 挑战塔保留。 |
| 对战 | 弹飞混战核心。 |
| 无尽 | 车轮战。 |

- 补落地无敌 12 帧
- AI 不会全跳崖

**2.5D / 3D 决策：** 2D 平台。可加一点景深层。不要 3D 乱斗。

## 4. 视觉 / 建模 / 动效（含手机）

- 击飞抛物线 200ms+
- 360px 缩小角色碰撞盒显示

- 360px 不溢出：字号 ≥ 14px，正文对比度 ≥ 4.5:1，触控热区 ≥ 44px。`prefers-reduced-motion` 下降动画时长。
- 面向约小学六年级，粉彩萌系，失败只鼓励。禁止商标 / 官方角色名（注释也不许）。

## 5. 跳关 / 解锁（游戏侧）

平台密码（kangkang / 管理员 18438037080 / 1 小时自动关）由 A 档实现。游戏侧：
- 实现 `openCampaignLevel(n: number)`（1 基），越界 clamp。
- mount 时若 `api.initialLevel` 或 hash `?level=N` 存在，直接进第 N 关，不要卡在选关封面。
- 跳过按钮调 `getLevelExtras().requestSkip(id, n-1)`，成功则本关星 0、解锁下一关。
- 不要自己做密码框。

## 6. 文件所有权

只许碰 `src/games/duo-vs-star/` 与该游戏测试。公共框架（`level99.ts`、`src/ui/*`、`src/engine/*`、`src/styles.css`、`index.html`、`home.ts`）**只读**。
邻接游戏目录一个字都不要动。

## 7. 测试与验收 / 不要做什么 / 邻接冲突

新增测试只增不减。`npm test` 与 `npm run build` 必须绿。

- 击退公式
- 无限击飞防护
- openLevel

不要做什么：
- 禁止商业乱斗角色名

与邻接游戏的冲突点：
- 与 fight-king 键位相同，destroy 必卸

完成后回复：改了哪些文件、模式对照、测试结果、**实际使用的模型 slug**。
~~~~
