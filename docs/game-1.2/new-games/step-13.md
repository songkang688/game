# 一朵一星 1.2 · 第 13 步 / 共 7 步 —— 星星合成 + 扫雷花园 + 数独花田

> 须等第 12 步三人推完且测试构建全绿再派。三款都是益智榜常客，规则必须按经典实现，不要自创到面目全非。

| 档 | 独占 | id | title |
| --- | --- | --- | --- |
| A | `src/games/merge-2048/` | `merge-2048` | 星星合成 |
| B | `src/games/mine-garden/` | `mine-garden` | 扫雷花园 |
| C | `src/games/sudoku-petal/` | `sudoku-petal` | 数独花田 |

---

## A 档提示词

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 13 步 · A 档**：新建 `merge-2048`「星星合成」。

## 分支纪律
基于 origin/game-1.2，计划 commit，只推 game-1.2，rebase 后测绿普通 push，禁止 force。

## 统一约定
meta 纯数据 + 懒加载；188 走 level99；destroy；内置音效；无外部依赖；禁止商标；不要改 home.ts。

操作：WASD 或滑屏四向；双人分盘（朵朵 WASD，星星方向键）。Esc 暂停。360px 格子间距足够，数字 ≥ 16px。

## 完整规则（经典 2048）
- 默认 4×4。每回合把所有块沿方向滑到不能再滑，**相同数字相撞合并一次**（新合成块本回合不再二次合并）。
- 合并得分 = 新块数字。出现 2048 先弹出「达成」可继续。
- 每回合若有移动，在随机空格生成 **2（90%）或 4（10%）**。无空格且四向都不能动 → 失败。
- 变体（闯关后段）：3×3、5×5、不可移动障碍花、限步、必须合成指定数。

禁止：消完瞬变。每块要有 **滑行 tween**（80–140ms），合并有短促放大。reduced-motion：瞬移但保留顺序。

### meta
```
id: "merge-2048"
title: "星星合成"
emoji: "🔢"
category: "casual"
color: "#FFF3C8"
blurb: "同一数字撞在一起就会变成更大的星星。合成到 2048，还可以继续往上叠。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 滑动 | `slideRow(row) → {row, moved, score, merges}` |
| 四向 | `move(board, dir)` |
| 生成 | `spawn(board, rng)` |
| 终局 | `canMove` `hasTile(n)` |
| 变体 | `applyHazards` |

### 模式
闯关 188：目标数字从 32 升到 4096，穿插障碍。无尽：经典马拉松记最高分。对战：同 seed 竞速谁先到目标。双人：分盘。

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 小数入门 | 24 | 4×4 目标 32/64 |
| 2 | 一二八 | 24 | 目标 128 |
| 3 | 五一二花园 | 24 | 目标 512 |
| 4 | 二千零四八 | 24 | 目标 2048 |
| 5 | 障碍花 | 22 | 不可滑入的格 |
| 6 | 三乘三 / 五乘五 | 22 | 变棋盘 |
| 7 | 限步挑战 | 24 | 步数上限 |
| 8 | 合成杯 | 24 | 4096 / 对战竞速 |

24×4 + 22×2 + 24×2 = 188。生成器用 seed，每关从固定开局出发可达成（测试用搜索或作者验证的开局）。

### 前端
DOM 或 Canvas 圆角砖，粉彩配色。分数 HUD。合并播 `coin`/`pop`。

### AI
对战假人：菜鸟随机合法方向；普通贪心得分；高手空位+单调性启发。

### 可参考
https://github.com/gabrielecirulli/2048 （滑动与合并顺序，不抄皮肤）

### 独占
只许 `src/games/merge-2048/**`。禁止 B/C、home.ts。

### 测试 ≥ 20
同行二次合并禁、2/4 生成概率可用伪随机测频率、不能动才结束、障碍格、188 章和、滑动幂等（空滑不 spawn）。

### 不要做什么
- 不要做成三消。
- 不要无动画。

### 验收 checkbox
- [ ] 滑动合并规则正确 + 动画
- [ ] 188 + 无尽 + 对战竞速
- [ ] 360px 数字清晰
- [ ] `npm test` `npm run build` 绿

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 A、用例数、SHA、实际模型 slug。
~~~~

---

## B 档提示词

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 13 步 · B 档**：新建 `mine-garden`「扫雷花园」。

## 分支纪律
基于 origin/game-1.2，计划 commit，只推 game-1.2，禁止 force。

## 统一约定
meta 纯数据 + 懒加载；188 走 level99；destroy；内置音效；无外部依赖；禁止商标；不要改 home.ts。题材可爱：雷是「带刺的种子」，踩中是种子爆成花瓣，**不是战争地雷画面**。文案用「刺种 / 扫种」，对内函数可用 `mine`。

操作：左键/`F` 翻开，右键/`G` 插旗。触屏：点翻开，长按插旗。双人竞速两张同 seed 图。

## 完整规则
- 网格，每格：空 / 1–8 / 刺种。数字 = 邻 8 格刺种数。
- **首次点击必安全**：第一次翻开的格子及其邻格不含刺种（把刺种重排）。单测。
- 翻到 0：洪水填充打开连通空区。
- 翻到刺种：失败。可开「保护第一次失误」（闯关前期）。
- 标旗数 = 刺种总数，可多标但不能超太多（允许超标，但赢的条件是 **所有非刺种格打开**，不要求旗全对）。
- **双键和弦**（邻格旗数=数字时点数字打开邻未开格）：必须实现，错误旗会引爆。
- 计时：从第一次翻开开始。三星看时限。
- 难度：初级 9×9 10 雷、中级 16×16 40、高级 16×30 99，闯关可定制。

胜负：打开全部安全格即胜；踩刺种即负（动画后结算）。

### meta
```
id: "mine-garden"
title: "扫雷花园"
emoji: "🌼"
category: "casual"
color: "#E5F8D8"
blurb: "看数字躲开刺种。第一次一定是安全的，插上小旗，把花园全部翻开。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。360px：格子 ≥ 28px，高级图可滚动。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 放种 | `placeMines(w,h,n,safeIndex,seed)` |
| 数字 | `hintMap` |
| 洪水 | `floodOpen` |
| 和弦 | `chord` |
| 胜负 | `won` `lost` |

### 模式
闯关 188：尺寸与雷密度上升，后期有「限时」「限旗」「开迷雾只看 3×3」。无尽：连续清盘密度+1。对战：同图竞速。

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 小苗床 | 24 | 5×5 ~ 9×9，雷很少 |
| 2 | 初级园 | 24 | 9×9×10 |
| 3 | 和弦课 | 24 | 必须用和弦才能三星 |
| 4 | 中级林 | 24 | 16×16×40 |
| 5 | 迷雾园 | 22 | 只亮光标周围 |
| 6 | 限时花期 | 22 | 倒计时 |
| 7 | 高级田 | 24 | 16×30×99 缩小版逐步加 |
| 8 | 园丁杯 | 24 | 综合 |

24×4 + 22×2 + 24×2 = 188。每关生成后 **必须有解**（不是雷把空区锁死到纯猜？扫雷有猜的成分——要求：用「第一下安全 + 不强制无猜」；第 3 章起尽量无猜：生成后用约束求解，失败就重 seed。无猜生成至少在测试里对 50 个 seed 断言可逻辑解）。

### 前端
DOM 网格。翻开有翻转动画。踩中：该格开花，其它刺种揭开但 **不要连环爆炸写实**。旗是小三角旗。

### AI
竞速假人按简单规则点确定格；地狱会和弦。

### 可参考
任意开源 minesweeper 的 flood fill / 无猜生成论文实现。不引入依赖。

### 独占
只许 `src/games/mine-garden/**`。

### 测试 ≥ 20
首点安全、洪水、和弦错旗失败、胜条件不依赖旗、无猜生成抽样、188 章和。

### 不要做什么
- 不要战争画面。
- 不要首点即死。

### 验收 checkbox
- [ ] 经典规则 + 首点安全 + 和弦
- [ ] 188 + 无尽 + 竞速
- [ ] 360px 可点
- [ ] `npm test` `npm run build` 绿

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 B、无猜策略、用例数、SHA、实际模型 slug。
~~~~

---

## C 档提示词

~~~~text
请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 13 步 · C 档**：新建 `sudoku-petal`「数独花田」。

## 分支纪律
基于 origin/game-1.2，计划 commit，只推 game-1.2，禁止 force。

## 统一约定
meta 纯数据 + 懒加载；188 走 level99（每关一题）；destroy；内置音效；无外部依赖；禁止商标；不要改 home.ts、不要改 `math-farm`（那是四则运算）。

点选格 + 数字钮 1–9；`G` 笔记（铅笔标记）；`F` 填入。双人：同题竞速。360px：数字钮 ≥ 44px。

## 完整规则
- 9×9，九宫、行、列均 1–9 不重复。
- **每题唯一解**。生成：挖洞 + 唯一解检测（回溯计数器碰到 2 就停）。
- 难度：按技巧分级而不是只按已知格数量——至少区分：唯余 / 隐式唯一 / 对对消（naked pair）/ 指向对。后段可出现。
- 笔记：每格 1–9 小标记。
- 冲突：填入后同行列宫高亮，**不要立刻判负**；闯关可开「错 3 次失败」。
- 变体关：不规则宫（jigsaw）、对角线（六数独的对角规则，名称自拟「对角花」）、4×4 入门、6×6。

胜负：盘面填满且合法即胜。对战比用时。无尽：连续解题，错 3 题结束。

### meta
```
id: "sudoku-petal"
title: "数独花田"
emoji: "9️⃣"
category: "edu"
color: "#E8DDFF"
blurb: "每一行、每一列、每一朵九宫花都要种满 1 到 9。提示只讲方法，不把答案写在攻略里。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。`ageHint: 9`。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 合法 | `isValidPlacement` |
| 解计数 | `countSolutions(limit=2)` |
| 生成 | `generate(seed, diff)` |
| 技巧检测 | `hintTechnique`（给玩家的提示等级，不直接填） |
| 评分 | `starsByTimeAndErrors` |

### 模式
闯关 188 题。对战同题。无尽马拉松。双人分盘或同题。

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 四宫萌芽 | 24 | 4×4 |
| 2 | 六宫苗 | 24 | 6×6 |
| 3 | 唯余九宫 | 24 | 9×9 入门 |
| 4 | 铅笔笔记 | 24 | 必须用笔记三星 |
| 5 | 成对花 | 22 | naked pair 关 |
| 6 | 对角花 | 22 | 对角线约束 |
| 7 | 异形宫 | 24 | jigsaw |
| 8 | 花田杯 | 24 | 高难唯一解 |

24×4 + 22×2 + 24×2 = 188。

**攻略 `guide.ts` 只讲方法（先看宫再看行），严禁写某关答案。** `copy.test.ts` 风格自检：guide 里不能出现完整 81 数字串。

### 前端
DOM 表格。选中宫高亮。填入有小缩放。完成时九宫依次开花，不要瞬变满屏。

### AI
竞速：按生成时记录的「最小技巧路径」逐步填，档位控制每步延迟。

### 可参考
开源 sudoku generator（唯一解回溯）。不引入 npm 求解库。

### 独占
只许 `src/games/sudoku-petal/**`。

### 测试 ≥ 25
188 题 `countSolutions===1`、冲突检测、4×4/6×6、对角规则、挖洞不破坏唯一、笔记不计入完成。

### 不要做什么
- 不要随机填到多解。
- 不要在 guide 里写答案。

### 验收 checkbox
- [ ] 唯一解生成
- [ ] 188 + 对战 + 无尽
- [ ] 360px 数字钮可点
- [ ] 完成有逐宫动画
- [ ] `npm test` `npm run build` 绿

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 C、生成器策略、188 唯一解如何断言、SHA、实际模型 slug。
~~~~
