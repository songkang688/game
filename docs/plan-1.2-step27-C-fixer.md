# 1.2 第 27 步 · C 档 —— 验收三人组 **第 1 轮** · 监督修复员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 27 步（共 29 步）· C 档 —— 验收三人组第 1 轮（共 3 轮）的监督修复员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。**你是这一轮的收口人。**

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「C 档 · 第 1 轮监督修复员 · 修复计划」的 commit**。
- 收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.2-round1-fixer.md`。

## 二、开工前必须认的基线

- 1.2 的盘子：**新游戏 21 款 + 升级 55 款 = 76 款**（第 15 步 C 档一档带 `bumper-cars` 与 `bowling-lane` 两款，因为这两款是 1.1 做完但没合进 1.2 的遗留）。
- 起点水位：**142 个测试文件 / 3918 个用例全绿**。**用例只增不减。**
- 存档 key 前缀一律 `yiduo-yixing.`，公共 key：`save.v1`、`l99.<id>`、`l99skip.<id>`、`collection.v1`、`fav.v1`、`recent.v1`。**只增不改。**

## 三、修复职责

1. 等 A 档的报告出来（或与 A 保持同步），**按严重度从高到低修**：阻断 → 严重 → 一般。**本轮必须把全部阻断与严重问题清零。**
2. 每修一个问题：**先写一个能复现该问题的测试（先红后绿），再修，再跑全量。** 没有回归测试的修复不算修完。
3. **A 报告里「盘子清点」那一节的结论优先级最高**：如果 76 款对不上（缺款、`meta` 与实际不符、首页列不出来），这是本轮第一优先级的阻断项，先把盘子补齐再谈别的。
4. 修改跨越别人目录时（比如某款游戏的 bug 在共用框架里），改共用文件前先跑全量确认基线，改完再跑全量，并在报告里点名可能受影响的游戏。

## 四、监督职责（这是你区别于普通修复的地方）

- **复审 A 有没有漏测的口子**，尤其是：崩溃入口、竞态（快速连点 / 快速切游戏 / 快速切页签 / 暂停恢复 20 次 / 后台切前台）、存档损坏路径、`destroy` 泄漏。漏了就自己补测并记录。
- **复审 B 的 diff**：有没有破坏既有手感与断言、有没有偷偷引入外部运行时依赖、性能数据能不能复现。**B 的量化数据你要独立复测一遍，对不上就在报告里直接指出。**
- **点名五项的独立复验**（不能只信 A 的结论，自己再验一遍）：
  - `gomoku` 解局关有解 + AI 六档强度单调；
  - `match-stars` 消除后**看得见下落与补位**（不是瞬变）；
  - `rainbow-run` 的 2.5D 确实接住了 1.1 第 6 步的成果；
  - `ocean-munch` 无尽可持续、成绩落盘；
  - `xiangqi` 多档 AI 单调 + 残局有解。
  这五项任意一项不达标，**本轮不许判过**。
- **三方合并态复验**：把 A / B / C 三人的分支都 rebase 到一起，在合并态上再跑一次 `npm test` + `npm run build` + `vite preview` 冒烟，把结果与**合并态 SHA** 写进报告。

## 五、报告要求

产出 `docs/qa/1.2-round1-fixer.md`：

- 修了哪些问题（对应 A 报告的编号）、怎么修的、新增了哪些回归测试；
- 补测发现的、A 没写到的问题；
- 对 B 的 diff 与量化数据的复核结论；
- 点名五项的独立复验证据；
- 三方合并态验证结果与 SHA、最终用例总数；
- **本轮遗留清单**：一般 / 建议级问题逐条登记，交给第 2 轮（第 28 步）。

## 六、本轮验收标准（三人共同，必须全部满足才算这一轮过）

- `npm test` 全绿且用例只增不减（基线 3918）；`npm run build` 全绿；`vite preview` 冒烟全绿。
- 阻断与严重问题 **0 残留**（一般 / 建议级可留到下一轮，但必须在报告里登记）。
- 76 款盘子对得上，每一款都能从首页进入。
- 点名五项全部达标。
- 三方合并态复验全绿，SHA 写进报告。
- 不改存档 key；不引入外部运行时依赖；**无任何商业商标或官方角色名**；文案面向约小学六年级、不过于低幼、保持中文粉彩萌系。

## 七、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告、无内购、无抽卡、无联网上报；攻略不泄题。完成后回复：你是 C 档第 1 轮监督修复员；清零了多少阻断 / 严重问题；新增了哪些回归测试；对 A 的补测与对 B 的复核结论；点名五项复验结果；三方合并态 SHA 与用例总数；遗留清单条数；报告路径；提交 SHA；**实际使用的模型 slug**。
