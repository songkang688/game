# 1.3 第 29 步 · C 档 —— 视觉验收三人组 **第 3 轮（终验）** · 监督修复员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉、只修视觉**，不重开玩法验收，一行玩法逻辑都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 29 步 · C 档 —— 视觉验收三人组第 3 轮（共 3 轮，终验）的监督修复员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**（读可以，改不行）。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「C 档 · 第 3 轮终验修复员 · 清零计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round3-fixer.md`。

## 二、本轮定位：清零收官，交付终态

这是 1.3 视觉升级的最后一轮，你的产出就是**可放行的终态**。输入五份材料：本轮 A 档 `docs/qa/1.3-round3-tester.md`（终验总表 + 新问题）、本轮 B 档 `docs/qa/1.3-round3-learner.md`（终账 + 仍违和款）、第 2 轮你自己的遗留清单 `docs/qa/1.3-round2-fixer.md`、B 档固化的 `docs/qa/1.3-style-baseline.md`、各款 1.3 提示词规格。

修复顺序铁律：**A 档终验总表里的不达标格最优先**（尤其六大专项：火柴人草稿圆 / 金币体积 / 双人可区分 / 2.5D 纵深 / 360px / 商标）→ 第 2 轮遗留的阻断 / 严重 → B 档点名的仍违和款 → 一般 → 建议。每修一处一个 commit（`fix(visual-r3): <款> <问题>`）。

## 三、终验轮的清零标准

- **阻断级 / 严重级问题必须清零**：终验后六大专项不允许留任何一格不达标；确实修不动的，写书面降级申请（现象 / 为什么修不动 / 影响面 / 后续计划）交 A 档裁决，A 档不接受就继续修；
- **一般 / 建议级**：能修则修，不能修的登记进 `docs/qa/1.3-backlog.md`（新建，作为 1.4 的输入），每条带「来源轮次 / 位置 / 期望 / 估算改动面」；
- 修复仍限绘制层且必须遵守 `1.3-style-baseline.md` 固化的规范，不许在终验轮引入新风格。

## 四、修复的红线（三轮一致，最后重申）

- **只动绘制层**：绘制函数、CSS、视觉常量、`src/art/kit/` 新增文件。判定、数值、关卡数据、存档 key、玩法测试断言一律不碰；
- 每处修复配套测试钉住修后状态；**用例只增不减**；被跳过（skip）的历史用例一律恢复运行；
- kit 终态整理：只删**自己在三轮里新增且已无人引用**的死文件，别人的文件与公共导出一个都不许动。

## 五、终态合并与放行流程（由你执行）

1. 三人都 push 后：`git fetch origin game-1.2-kk` → rebase → 确认三份终验报告、`1.3-style-baseline.md`、全部修复都在；
2. `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟（首页 / `sw.js` / `manifest.webmanifest` 200，随机 5 款各 30 秒无视觉崩坏）；
3. 全绿后记录**终态 SHA、用例总数（对比三轮曲线只增不减）、构建体积（对比 1.2 基线与前两轮，总涨幅超 15% 要逐项说明）**；
4. 请 A 档基于终态复核终验结论；A 档给「放行」或「带条件放行」后，你在报告里写下**1.3 视觉升级收官声明**（终态 SHA + 结论 + 遗留 backlog 条数）。普通 push 后收工，**不合并、不动 `game-1.2` 与 `main`，交付物就是 `game-1.2-kk` 分支终态**。

## 六、报告要求

产出 `docs/qa/1.3-round3-fixer.md`：

- 清零清单：每条「来源（A 终验表 / 第 2 轮遗留 / B 点名）/ 位置 / 修法 / 配套用例 / commit SHA」；
- 降级申请记录（如有）与 A 档裁决结果；
- `docs/qa/1.3-backlog.md` 的条目统计；
- 终态合并数据（SHA / 用例曲线 / 体积对比）；
- **1.3 视觉升级收官声明**。

## 七、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；`vite preview` 冒烟全绿；
- 六大专项 39 款 × 6 项全达标（或有 A 档接受的书面降级）；阻断 / 严重级终态为 0；
- backlog 成文、收官声明成文、终态 SHA 写进三份报告。

## 八、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 29 步 C 档第 3 轮终验修复员；清零统计（按来源与严重度）；降级申请与裁决结果；backlog 条数；终态 SHA 与 `npm test` / `npm run build` / `vite preview` 结果；收官声明摘要；报告路径；**实际使用的模型 slug**。
