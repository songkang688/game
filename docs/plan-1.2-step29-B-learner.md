# 1.2 第 29 步 · B 档 —— 验收三人组 **第 3 轮** · 学习优化员（收口 + 文档）

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 29 步（共 29 步）· B 档 —— 验收三人组第 3 轮的学习优化员。这是 1.2 的最后一步：这一轮结束时，1.2 必须处于「随时可发布」的状态。**
本步三人：A 测试员、B 学习优化员、C 监督修复员。**别人的报告文件你一个字都不许碰。**

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「B 档 · 第 3 轮学习优化员 · 收口计划」的 commit**。
- 收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.2-round3-learner.md`。
- **开工先读第 1、2 轮全部六份报告。**

## 二、开工前必须认的基线

- 1.2 的盘子：**新游戏 21 款 + 升级 55 款 = 76 款**。起点水位 **142 个测试文件 / 3918 个用例**，**只增不减**。
- **不引入任何外部运行时依赖。**

## 三、收口：只做低风险高收益

- 最后一轮吸收优点，**只做低风险高收益的改进，不许在最后一轮做大重构**。至少落地 **4 条**。
- 优先挑那些「A 报告里反复出现、但严重度不到阻断」的体验毛刺（比如提示文案不一致、空状态难看、加载闪烁、焦点丢失）。
- **复核前两轮全部落地项**：逐条确认没有回退、没有副作用、都有测试守着。有裸奔的（改了但没测试守）本轮补上测试。

## 四、文档：这是 1.2 的门面（本轮的重头戏）

必须与**实际代码一致，不许写虚**——A 档会拿着 README 逐条核对。

1. **`README.md`** 更新到 1.2：
   - 合集款数（76）、分类表与各类计数；
   - 188 关体系说明；
   - 对战 / 无尽 / 双人 / 合作各有哪些款（**列表要与 `meta.modes` 全库遍历的结果完全一致**，建议写个脚本或测试来生成与校验）；
   - 键位表（双人键盘游戏的两套键位）；
   - 家长功能（家长门两级、跳关、进度导出导入、清空）；
   - 离线与隐私声明（无联网上报、无广告、无内购）。
2. **家长面板文案**同步到 1.2 的实际能力。
3. **新增 `docs/upgrade-prompts/13-game-1.2-release-notes.md`** 发布说明：1.2 相比 1.1 的全部变化，按「新增游戏（21 款，逐款一句话）/ 升级（55 款，按批次归纳）/ 平台能力 / 修复」分节。
4. **新增 `docs/upgrade-prompts/14-game-1.3-baseline.md`** 下一版基线：收尾时的游戏总数、188 关游戏数、各模式款数、测试文件数与用例总数、主 chunk 体积、存档 key 清单、共用框架清单、以及三轮里明确「留给 1.3」的条目。**下一版要靠这份文件开工，写实。**

## 五、README 一致性要做成测试

光靠人写容易写虚。加一个测试：遍历全库 `meta.ts`，断言 README 里的**款数**与**各模式款名列表**与代码一致。以后谁改了 `meta` 忘了改 README，测试会红。这是本轮最有价值的一条落地。

## 六、与 A / C 的冲突规避

- 你改的每个文件登记成表。与 C 的修复撞车时以更彻底的实现为准，并在三方合并态上复验。
- 不要碰 `docs/qa/1.2-round3-tester.md` 与 `docs/qa/1.2-round3-fixer.md`。

## 七、报告要求

产出 `docs/qa/1.2-round3-learner.md`：

- 本轮落地的 4+ 条（来源思路 / 落地方式 / 收益 / 风险）；
- **前两轮落地项复核表**：逐条标 仍生效 / 有副作用 / 已回退，以及是否有测试守着；
- 文档更新清单与「README 与代码一致性」测试的说明；
- 交给 1.3 的条目清单（与 C 档的最终结论对齐）。

## 八、本轮验收标准（1.2 收官线，三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；preview 冒烟与**离线冒烟**全绿。
- 三轮累计的阻断 / 严重 / 一般问题 **全部清零**。
- **README 与实际代码一致，并有测试守着。**
- 1.2 全部 76 款每一款都至少被实玩走查过一次（A 档负责）。
- 三方合并态复验全绿，SHA 写进报告。
- 不改存档 key；无外部运行时依赖；**无任何商业商标或官方角色名**；文案面向约小学六年级、不过于低幼、保持中文粉彩萌系。

## 九、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告、无内购、无抽卡、无联网上报；攻略不泄题。完成后回复：你是 B 档第 3 轮学习优化员；落地了哪几条；前两轮落地项复核结论；README / 家长面板 / 发布说明 / 1.3 基线四份文档的更新要点；README 一致性测试怎么写的；报告路径；`npm test` / `npm run build` 结果与用例总数；提交 SHA；**实际使用的模型 slug**。
