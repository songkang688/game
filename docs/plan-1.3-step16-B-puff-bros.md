# 1.3 第 16 步 · B 档 —— `puff-bros`「噗噗兄弟」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 16 步 · B 档**：给 `puff-bros`「噗噗兄弟」做只动皮肤不动骨头的美术精修。主管点名：**双人角色必须都精美且可区分**——本款是重点整改对象。
本步三人：A `ice-fire-forest`、B `puff-bros`、C `prince-princess`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「B 档 · puff-bros 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/puff-bros/**` 里的绘制函数、CSS、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 吹气 / 泡泡 / 平台物理与判定：`PUFF_WINDUP` 前摇、`landingSquash` / `pushSquish` 的形变量函数（形变三件套是 1.2 调好的手感，**保住并只在其上叠加**）、判定盒 `PLAYER_W × PLAYER_H`；
- 存档 key、`meta.ts` 的 `modes/levels/category`；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，全部程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/puff-bros/index.ts`，绘制入口是 `render` / `drawBro` / `drawPuffRing` / `drawBubble` / `drawCrate` / `drawSpring` / `drawGoo` / `drawBrittle` / `drawUpdraft` / `drawWarp` / `drawClimbLine` / `drawGadget` / `drawTumbleRing`。已核实的问题：

1. `drawBro`（约 897 行起）的两兄弟 = 椭圆身体 + 肚皮椭圆 + 两点眼 + 腮红：**剪影完全相同**，唯一区别是 `BROS[pi]` 的调色——「兄弟」的长幼、性格全靠颜色脑补，缩小后根本分不清；
2. 身体是两层纯色平涂（`c.body` + `c.belly`），没有渐变、没有绒毛质感、没有 rim light；
3. 嘴部三态（吹泡泡 / 攒气 / 常态，954–969 行）逻辑很好但画得极简：吹泡就是一个白圆，攒气就是一个椭圆；
4. 形变三件套（落地压扁 / 被吹扁 / 掉出打转，909–917 行）是本款的好底子，**必须原样保住**；
5. `drawBubble` 泡泡、`drawCrate` 箱子、`drawSpring` 弹簧、`drawGoo` 黏液、`drawUpdraft` 上升气流、`drawWarp` 传送门逐个自查：预期平涂几何为主，把每个的结论记进开工 commit。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **哥哥「噗噗」重绘** | 剪影：头顶一撮翘起的呆毛（三段贝塞尔）+ 背带裤（两条肩带 + 前袋方块）+ 圆耳朵一对；材质：身体三停径向渐变（顶光 +22%）+ 肚皮浅色域 + 1.5px 描边；表情比弟弟稳重（眼睛稍小、眉毛平）。 |
| **弟弟「泡泡」重绘** | 剪影：头戴圆边小帽（帽檐 + 顶球）+ 围兜（半圆 + 波浪边）+ 尾巴小揪揪；材质同规格但主色更浅、腮红更大、眼睛更圆——「幼」感从形状来不是只从颜色来。 |
| **可区分性验证** | 16px 灰度自测：呆毛 vs 帽子、背带裤 vs 围兜、耳朵 vs 揪揪三通道可分辨，结论写进回复。 |
| **嘴部与吹气** | 吹泡泡的白圆升级为「腮帮鼓起 + 泡泡从嘴边长大」两层（沿用 `blowCd` 时序）；`drawPuffRing` 攒气环加彩虹渐变描边与内圈星尘 3 颗（提示功能不变，reduced 只描边）。 |
| **泡泡与场景** | 泡泡加薄膜质感：径向渐变 + 顶部月牙高光 + 底部 1px 彩虹缘（细到不喧宾）；箱子画木纹 + 角铁；弹簧画螺旋圈数随压缩变密（读既有压缩量）；黏液加流挂圆珠与光泽；上升气流画成半透明羽毛旋涡；传送门双色旋涡互为反色。 |
| **关卡氛围与布局** | 背景按关卡序号轮换三套淡色天空（晨 / 昼 / 暮），加两层软云视差；HUD 兄弟头像徽章 + 泡泡计数卡片化；胜利时兄弟击掌动画（两帧，reduced 静止合影）。 |

- 渐变 / 描边 / 落影统一走 `src/art/kit/`；光源统一左上 45°。
- `prefers-reduced-motion`：既有约定是形变三件套全关——新加的呆毛摆动、云视差、星尘也全部并入该开关。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`（`ballGradient` / `softShadow`）、`outline.ts`（统一描边）、`sparkle.ts`（星屑 / 彩纸）。
- 你需要而 kit 没有的能力（比如 `bubbleSkin.ts` 泡泡薄膜）：**新增**文件并配套单测，不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看泡泡龙 / 双人平台页面，**只学两件事：画面密度（平台装饰层次）与双角色剪影（兄弟一眼分清）**。
- 禁止抄任何商标与官方角色：**不准像泡泡龙（Bubble Bobble）的双龙**，也不准像马里奥兄弟的红绿配置照搬；回到本库粉彩原创。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：兄弟帽饰 / 呆毛在最小渲染尺寸可辨；
- 泡泡彩虹缘在小屏不糊成杂色（低于 6px 半径时只留高光）；
- HUD 一行放得下，字号 ≥ 14px；触屏按键热区不动。

## 八、独占文件与冲突

只许改 `src/games/puff-bros/**` 与新增 `src/art/kit/` 文件。不要碰 `ice-fire-forest` / `prince-princess`。CSS 前缀沿用本款既有前缀；`destroy` 清干净新加的粒子与计时。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 兄弟两套剪影路径函数各自可调用且路径点集不相等（可区分性断言）；
- 呆毛 / 帽子 / 背带裤 / 围兜绘制分支存在性断言；
- 形变三件套的形变量函数输出与升级前一致（0 / 0.5 / 1 三点断言，钉住不许动）；
- 泡泡彩虹缘在半径 < 6px 时自动省略；
- reduced 下星尘 / 视差 / 摆动为 0；`destroy` 归零；
- 既有物理与判定测试断言一个不许改。

## 十、分级红线与回复

摔出平台是「打转飘回」不是受伤；失败只鼓励；无商标、不像泡泡龙。完成后回复：你是 1.3 第 16 步 B 档、`puff-bros`；第三节每个函数的现状结论；兄弟可区分三通道自测结果；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
