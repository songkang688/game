# 1.3 第 27 步 · C 档 —— 视觉验收三人组 **第 1 轮** · 监督修复员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉、只修视觉**，不重开玩法验收，一行玩法逻辑都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 27 步 · C 档 —— 视觉验收三人组第 1 轮（共 3 轮）的监督修复员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**（读可以，改不行）。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上（若不存在则 fetch `origin/game-1.2` 后自建）。
- **动手前先提交一条「C 档 · 第 1 轮监督修复员 · 修复计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round1-fixer.md`。

## 二、你的角色定位

你是三人组里**动手修**的那个人：A 档报告里的视觉问题、B 档建议清单里的高优先级项，由你落地。你也是**合并态的守门员**：三人各自 push 之后，由你做最终的合并态复验并把 SHA 写进报告。

## 三、修复输入与优先级

1. 读 `docs/qa/1.3-round1-tester.md`（A 档）与 `docs/qa/1.3-round1-learner.md`（B 档）。若你开工时报告还没出，先做第四节的「自查修复」，之后再合并跟进。
2. 修复顺序铁律：**阻断（商标、360 新溢出）→ 严重（六大专项不达标：裸 emoji 主角、平涂圆、双人不可分、2.5D 缺失、金币无体积）→ 一般 → 建议**。
3. 每修一处一个 commit（`fix(visual-r1): <款> <问题>`），别攒大杂烩。

## 四、自查修复（不等报告也要做的底线检查）

对照 1.3 各步提示词的规格，抽查以下高风险点并直接修：

- **商标扫描**：grep 全库文案与代码注释里的品牌词（4399 / 马里奥 / 皮卡丘 / 托马斯 / Kitty 等），逐款目检易踩线题材（坦克 / 火车 / 猫 / 炸弹人 / 泡泡龙）的造型相似度；
- **裸 emoji 主角残留**：grep `fillText("🐹"` / `fillText(car.emoji` / `P_EMOJI` / `fish.emoji` 等 1.2 时代的直出点，确认 1.3 各步都换掉了；漏网的按该步提示词的规格补画；
- **360px 溢出**：360 与 320 宽跑一遍首页 + 抽 15 款，新溢出立刻修；
- **reduced-motion**：抽 10 款验证装饰动画停、功能提示留；
- **kit 纪律**：`src/art/kit/` 有没有人改了别人的文件（`git log` 查）；有冲突改动要恢复并在报告记名。

## 五、修复的红线

- **只动绘制层**：绘制函数、CSS、视觉常量、`src/art/kit/` 新增文件。判定、数值、关卡数据、存档 key、玩法测试断言一律不碰。
- 修复必须遵守各款 1.3 提示词的既定规格（光源左上 45°、描边 1.5–2px、粉彩色板、reduced 约定），不许自创第二套风格。
- 每处修复配套测试：新增或加强视觉用例钉住修后状态（如「不再含 emoji 直出」的静态断言）；**用例只增不减**。
- 修不动或代价过大的，降级处理写清理由，登记到报告的「遗留清单」交下一轮。

## 六、合并态复验（三人共同的收尾，由你执行）

1. 三人都 push 后：`git fetch origin game-1.2-kk` → rebase 本地 → 确认三份报告与全部修复都在；
2. `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟（首页 / `sw.js` / `manifest.webmanifest` 200）；
3. 全绿后把**合并态 SHA、用例总数、构建体积**写进你的报告，并在报告末尾给「第 1 轮视觉验收结论」：阻断清零与否、严重残留几条、遗留清单。

## 七、报告要求

产出 `docs/qa/1.3-round1-fixer.md`：

- 修复清单：每条「来源（A 报告条目 / B 建议 / 自查）/ 位置 / 修法 / 配套用例 / commit SHA」；
- 无法修复的遗留清单与理由；
- 合并态复验数据（第六节）；
- kit 纪律检查结论。

## 八、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；`vite preview` 冒烟全绿；
- **阻断级 0 残留**（商标、360 新溢出当轮清零）；严重级尽量清零，残留的必须在遗留清单登记；
- 合并态 SHA 写进报告。

## 九、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 27 步 C 档第 1 轮监督修复员；修复统计（阻断 / 严重 / 一般各修几条）；遗留清单摘要；合并态 SHA 与 `npm test` / `npm run build` 结果；报告路径；**实际使用的模型 slug**。
