# 1.3 第 28 步 · B 档 —— 视觉验收三人组 **第 2 轮** · 学习优化员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉**，不重开玩法验收，也不改任何玩法代码。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 28 步 · B 档 —— 视觉验收三人组第 2 轮（共 3 轮）的学习优化员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「B 档 · 第 2 轮学习优化员 · 计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round2-learner.md`。

## 二、本轮定位：验证学习成果 + 补盲区

第 1 轮你建立了密度基准与剪影经验，本轮做三件事：

1. **回看采纳率**：对照 `docs/qa/1.3-round1-fixer.md`，你上一轮 TOP10 建议有几条被采纳、落地效果如何（逐条截图级评价）；没被采纳的，是建议不可行还是优先级不够？修订后重新排序。
2. **补盲区题材**：第 1 轮覆盖不足的题材（棋牌桌面 / 教育四款 / 照护养成）补齐基准学习——这几类在 4399 等平台的头部作品密度普遍**低**而留白讲究，学的是「怎么少而精」，把这条结论写透。
3. **横向一致性评审**：39 款升级后放在首页一起看，风格像不像「一家人」？挑出最违和的 5 款，写清违和在哪（色板偏离 / 描边粗细 / 光照方向 / 动效节奏），给 C 档统一化建议。

## 三、学习红线（与第 1 轮相同，重申）

- 只学**画面密度**与**角色剪影**两件事，只记文字结论；
- 不下载、不截图入库、不描摹任何具体造型；
- 商标与官方角色连「像」都不许像；本轮继续维护「题材 → 易踩线对象 → 规避要点」清单，把第 1 轮修复后仍有风险的点名。

## 四、文案与引导优化（你唯一可以动代码的范围）

- 延续第 1 轮的范围：`guide.ts` 攻略、`blurb`、纯文案字符串；
- 本轮重点：把 C 档第 1 轮修复的视觉亮点补进对应款的引导（如泡泡薄膜、球道透视、超人披风）；
- 检查第 1 轮你改过的文案有没有被 rebase 冲掉，丢了的补回；
- 每处改动登记进报告。

## 五、报告要求

产出 `docs/qa/1.3-round2-learner.md`：

- 第一节：TOP10 建议采纳率对账表（采纳 / 部分采纳 / 未采纳 + 落地效果评价）；
- 第二节：盲区题材的密度基准补充（棋牌 / 教育 / 养成，附「少而精」结论）；
- 第三节：横向一致性评审——最违和 5 款及统一化建议（绘制规格级）；
- 第四节：修订后的建议清单（给本轮与下一轮 C 档，按严重度 × 成本重排）；
- 第五节：商标风险清单更新；
- 第六节：本轮文案改动清单。

## 六、你的权限边界

你能改的文件限于：`docs/qa/1.3-round2-learner.md`、各游戏的**纯文案**字符串、为文案改动补的快照测试。绘制代码一行不许碰；A 档与 C 档的报告一字不许碰。

## 七、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；
- 采纳率对账覆盖第 1 轮全部 TOP10；一致性评审给出明确的 5 款点名与规格级建议；
- 三方合并态复验全绿，合并态 SHA 写进报告（由 C 档执行，你要配合）。

## 八、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 28 步 B 档第 2 轮学习优化员；TOP10 采纳率；盲区基准补充摘要；最违和 5 款点名；修订后建议清单标题；文案改动清单；报告路径；`npm test` / `npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
