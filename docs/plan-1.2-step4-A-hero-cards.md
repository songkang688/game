# 1.2 第 4 步 · A 档 —— `hero-cards`「英杰令」

> 短计划：独占新建 `src/games/hero-cards/`。本步另两档是 `weiqi-garden`、`flight-chess`。
> 身份 + 手牌 + 装备的桌面卡牌杀。**禁止**官方武将名与那款身份卡牌的中文俗称。不要改 `landlord-cards`。象棋 `xiangqi` 不在本步、也不新建。

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`，不要改 main、不要 force。】

仓库 https://github.com/songkang688/game ，**1.2 第 4 步 · A 档**：新建 `hero-cards`「英杰令」。假设 1.1 的 55 款已全部做完。

## 分支纪律
基于 `origin/game-1.2`，计划 commit，rebase 后测绿普通 push。禁止 force。不要用 gh 写 PR。

## 新游戏统一约定（对接更新后的 1.1）
- `meta.ts` 纯数据 + 懒加载 mount。**不要改 `src/ui/home.ts`。** 闯关走 `level99.ts` 188 关。存档 `yiduo-yixing.l99.hero-cards`。
- 键位：点选牌；`F` 确定 / `G` 取消；WASD 移动光标。手机点选，热区 ≥ 44px。
- `destroy` 干净。内置音效。无外部依赖。
- **收藏只读**：`luckMul` 可微调判定牌「好结果」权重（不要破坏技能单测）。禁止改 collection 源文件。暂停可 `openCollection("hero-cards")`。
- 分级：体力叫「元气」。受伤是掉花瓣。元气 0 = 「退场休息」，禁止死亡描写。
- 360px：手牌横滑，距离环缩成顶栏 5 个头像。验证 360 / 375 / 1280。不要改 supervisor / step1 / step9+。

## 规则：身份场简化到可单机

人数：**5 人**（玩家 + 4 AI）。身份：
- 1 主公（开局亮明，元气 +1）
- 1 忠臣
- 2 反贼
- 1 内奸

胜利：
- 主公/忠臣：反贼与内奸全部退场。
- 反贼：主公退场（此时若内奸还在也算反贼胜——简化：主公退场且场上还有反贼 → 反贼胜；主公退场且只剩内奸 → 内奸胜）。
- 内奸：成为最后存活者，且主公在忠反都退场之后才退场。若主公过早退场而忠/反仍在，内奸失败。

**边界**：主公误伤忠臣退场 → 主公弃光手牌和装备。任何人打退反贼 → 摸 3 张。这些必须单测。

### 牌堆（原创名，结构对齐经典身份牌）
基本牌：击（杀）、挡（闪）、愈（桃）。约 30/15/8。
锦囊：顺手、过河拆桥类（「抽一张远邻的牌」）、决斗、万箭/南蛮类群体、乐不思蜀类延时、无懈、借刀。全部用花园/星空主题改名（例：「花瓣击」「星盾」「蜜桃愈」「春风借力」）。
装备：武器（加攻击距离 1–4）、防具、+1 坐骑（别人到你的距离 +1）、-1 坐骑（你到别人 -1）。每类最多一件。

牌堆抽空：弃牌堆洗回。判定：翻牌堆顶。

### 距离
座位环，退场者从环上摘掉。距离 = 环上最短步数。
- 对别人用「击」：需要 攻击范围 ≥ 距离。
- 顺手类：通常要求距离 1（坐骑会改）。
- 武器只改「击」的范围，不改锦囊距离。单测：距离 2 无武器不能击；武器 2 可以击仍不能顺手。

### 回合
判定 → 摸 2 → 出牌（击默认每回合 1 次，酒/技能可改）→ 弃牌至手牌 ≤ 当前元气。

### 原创英杰（≥ 12，禁止真名）
每名 1–2 个技能，强度接近「标准版」而不是阴间包。例：
- 花主（主公候选）：出牌阶段可把任意手牌给别人，每给 1 张摸 0 或回 1 元气（有次数）
- 星督：受到击可翻判定，红色当挡
- 豆将：击红色时不可挡
- 云牧：别人对你造成伤害，你可抽他一张
……自己设计，名字全部原创。技能必须是纯函数 `onEvent(state, event) → effects`。

### meta
```
id: "hero-cards"
title: "英杰令"
emoji: "🎴"
category: "party"
color: "#FFD9C8"
blurb: "亮出花主，藏起身份。算距离、出技能，让对桌的伙伴露出马脚。"
modes: ["campaign", "versus", "endless"]
levels: 188
```
端：双端。双人同屏不是本款本命（座位只有一个玩家位），可不填 `twoPlayer`；若做「朵朵主公星星忠臣」合作，再加 `coop`。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 距离 | `distance(from, to, seats, horses)` |
| 攻击范围 | `inSlashRange` |
| 合法目标 | `legalTargets(card, state)` |
| 牌堆 | `draw` `discard` `recycle` |
| 身份胜负 | `winnerOf(alive, roles)` |
| 奖惩 | `onEliminated(killer, victim)` |
| 技能 | `trigger(event)` |
| AI 身份 | `roleHeuristic` |

### 模式
| 模式 | 做 | 为什么 |
| --- | --- | --- |
| 对战身份 | 是 | 本命，1 人 4 AI |
| 闯关 188 | 是 | 残局：给定身份与手牌，N 回合内达成阵营胜利 |
| 无尽 | 是 | 连胜身份场，败断，`recordEndlessBest` |
| 国战 | **不做完整国战** | 工作量过大；可用「双势力合作」小关代替（同势力不能击） |

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 击挡愈 | 24 | 只有基本牌 |
| 2 | 距离学堂 | 24 | 坐骑与武器 |
| 3 | 锦囊风雨 | 24 | 非延时锦囊 |
| 4 | 延时与判定 | 24 | 乐/闪电类 |
| 5 | 技能初绽 | 22 | 2 技能英杰 |
| 6 | 身份推理 | 22 | 隐藏身份，根据出牌猜 |
| 7 | 内奸残局 | 24 | 必须按内奸路线赢 |
| 8 | 英杰杯 | 24 | 全牌堆 + 地狱 AI |

24×4 + 22×2 + 24×2 = 188。每关有解（预置行动或求解器）。

### 前端建模与动画
- DOM 桌面：五人座位，装备区，判定区，牌堆数。
- 出牌飞行动画；受伤掉花瓣，不要红闪过头（reduced-motion 只改数字）。
- 胜负弹出身份揭晓。

### AI 档位
菜鸟乱打；普通按身份打主公或护主公；高手会留挡、会无懈关键牌；地狱会装忠、会计算距离与牌数、内奸会两边卖。

### 可参考 GitHub（结构 only，禁止运行时依赖）
- https://github.com/Mogara/QSanguosha
- https://github.com/FreeKill-org/FreeKill
只学「事件-技能-牌」分层，不抄角色名、不抄卡图、不嵌引擎。

### 独占
只许 `src/games/hero-cards/**`，可选 `scripts/smoke-step4-a.mjs`。禁止本步 B/C、`landlord-cards`、`home.ts`、collection 源文件、supervisor / step1 / step9+。

### 测试 ≥ 35（硬性 ≥ 15）
距离+马、武器只影响击、退场重算距离、主公杀忠弃牌、杀反摸三、内奸胜负边界、牌堆循环、188 可解。

### 不要做什么
- 不要完整国战 8 人。
- 不要真实兵器血腥。
- 不要官方武将名。

### 验收 checkbox
- [ ] 5 人身份 + 距离 + 技能事件
- [ ] 188 残局可解；AI 四档；360px 可出牌
- [ ] `npm test` `npm run build` 绿；destroy 干净；收藏只读

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 A、英杰列表、胜负边界用例、SHA、实际模型 slug（派发指定 `claude-opus-5-thinking-high-fast`）。
