# 1.2 第 4 步 · C 档 —— `flight-chess`「飞行棋乐园」

> 短计划：独占新建 `src/games/flight-chess/`。本步另两档是 `hero-cards`、`weiqi-garden`。
> 四色飞行棋：掷骰、起飞、叠子、跳格飞线、终点折返。象棋 `xiangqi` 不在本步。

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 4 步 · C 档**：新建 `flight-chess`「飞行棋乐园」。假设 1.1 的 55 款已全部做完。

## 分支纪律
基于 origin/game-1.2，计划 commit，只推 game-1.2，禁止 force。

## 新游戏统一约定（对接更新后的 1.1）
- `meta.ts` 纯数据 + 懒加载。**不要改 `src/ui/home.ts`。** 闯关走 `level99.ts` 188 关。存档 `yiduo-yixing.l99.flight-chess`。
- `F` 掷骰，`G` 切换要走的飞机，WASD 选棋。双人：朵朵一色、星星一色，另两色 AI。手机：掷骰大钮 + 点飞机（热区 ≥ 44px）。
- `destroy` 干净。内置音效。无外部依赖。禁止商标。
- **收藏只读**：`luckMul` 不要改骰子公平性（骰子必须可种子）。暂停可 `openCollection("flight-chess")`。
- 360px：棋盘正方形完整入屏。验证 360 / 375 / 1280。不要改 supervisor / step1 / step9+。

## 完整规则（传统飞行棋，写死默认；闯关可改开关）
- 2–4 人，每色 **4 架**飞机。先 4 架全部到达终点者胜。
- **起飞**：掷到 **6** 才能从基地到起飞格，并 **再掷一次**。改进规则（闯关后期）：5 或 6 可起飞，掷 5 不再掷。
- **连掷**：路上掷 6 再掷，直到不是 6。连续三个 6：取消本次三点并跳过（或进惩罚，选一种写进常量并测）。
- **撞子**：落点有 **1 架**敌机 → 敌机回基地。落点是敌方 **叠子（≥2）**：不能飞越；若正好停在叠子上 → **集体回基地**（自己也回）。跳子落点同样执行撞/集体撞。
- **叠子**：己方两架同格叠在一起。敌机不能从叠子上飞过（骰子步数超过则碰到叠子后 **折返剩余步**）。跳子时叠子 **不挡跳**（传统常见例外，必须单测）。
- **安全区**：终点通道（本色 6 格）只有自己能进；通道内可叠；敌机飞不过去。基地内的飞机不受撞。
- **快捷 / 跳 / 飞**：
  - 停在本色格 → 再跳到下一个本色格（+4 色格）。
  - 若该格有虚线航线：沿虚线飞到对面本色格，再按规则决定是否连跳（传统：跳到航线格再飞，飞完一般不再跳；从跳进入航线格则飞完停止）。**必须按同一套写成纯函数 `resolveLanding`，用表格测 8 个典型落点。**
- **终点**：必须 **正好**到达。超出则在终点通道 **折返**。到终点的飞机退役，不再控制。
- 骰子动画要转几圈再停，禁止瞬出点数（reduced-motion 可少转）。

胜负：第一名到达结束（默认）；闯关可要求「同时拿第 1」。无尽：连胜记 `recordEndlessBest`。

### meta
```
id: "flight-chess"
title: "飞行棋乐园"
emoji: "✈️"
category: "party"
color: "#D6F0FF"
blurb: "四人掷骰绕圈飞。叠在一起最安全，跳格飞线最开心，先到齐的人获胜。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 骰子 | `roll(seed)` |
| 起飞 | `canTakeOff(dice, rules)` |
| 路径 | `pathOf(color)` 含环线+通道+航线 |
| 落点结算 | `resolveLanding`（跳、飞、撞、叠、折返） |
| 叠子阻挡 | `blockedByStack` |
| 终点折返 | `bounceInHome` |
| 胜负 | `allHome(color)` |

### 模式
| 模式 | 做 |
| --- | --- |
| 对战 4 人 | 是（缺人 AI 补） |
| 双人 | 是（两色，两 AI） |
| 闯关 188 | 是（残局：指定骰序列，N 回合内先到） |
| 无尽 | 是（连胜） |

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 起飞跑道 | 24 | 只有 6 起飞 |
| 2 | 跳格子 | 24 | 本色跳 |
| 3 | 航线飞 | 24 | 虚线飞 |
| 4 | 撞机演练 | 24 | 必须撞回指定敌机 |
| 5 | 叠机堡垒 | 22 | 叠子阻挡 |
| 6 | 通道折返 | 22 | 正好到终点 |
| 7 | 改进规则 | 24 | 5/6 起飞、连 6 惩罚 |
| 8 | 四人决赛 | 24 | 地狱 AI |

24×4 + 22×2 + 24×2 = 188。骰序列固定，每关可解。

### 前端建模与动画
- Canvas 或 SVG 棋盘，四色粉彩。飞机棋子是朵朵/星星/小花/小鸟造型。
- 移动：**一格一格跳**，跳格和飞线走曲线，禁止瞬移到终点。
- 撞机：被撞的飞回基地有小轨迹。

### AI 档位
菜鸟有 6 就起飞否则走最前；普通会撞人；高手会叠子挡路、会算正好到终点；地狱会挡你的航线、会拆你的叠。

### 可参考 GitHub（结构 only，禁止运行时依赖）
开源 ludo / aeroplane-chess 棋盘图结构。不要用版权棋盘图，不要 npm 包。

### 独占
只许 `src/games/flight-chess/**`，可选 `scripts/smoke-step4-c.mjs`。禁止本步 A/B、`home.ts`、collection 源文件、supervisor / step1 / step9+。

### 测试 ≥ 25（硬性 ≥ 15）
6 起飞再掷、三连 6、撞单机、叠子挡与集体回、跳+飞组合表、终点折返、188 可解。

### 不要做什么
- 不要随机到无解关（骰子必须可种子）。
- 不要 8 人棋盘。

### 验收 checkbox
- [ ] 四人 + 安全区 + 叠子 + 快捷飞跳 + 终点折返
- [ ] 棋子移动有动画；188 可解；360px 棋盘完整
- [ ] `npm test` `npm run build` 绿；destroy 干净；收藏只读

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 C、落点结算表、用例数、SHA、实际模型 slug（派发指定 `claude-opus-5-thinking-high-fast`）。
