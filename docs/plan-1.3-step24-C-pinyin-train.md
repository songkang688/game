# 1.3 第 24 步 · C 档 —— `pinyin-train`「拼音小火车」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 24 步 · C 档**：给 `pinyin-train`「拼音小火车」做只动皮肤不动骨头的美术精修。
本步三人：A `clock-house`、B `math-farm`、C `pinyin-train`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「C 档 · pinyin-train 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/pinyin-train/**` 里的渲染层、CSS/SVG、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 拼音题库与拼读规则、判定、TTS / speech 朗读接线（教育正确性红线）、声调符号的正字法（`ā á ǎ à` 显示必须准确）；
- 存档 key、`meta.ts` 的 `modes/levels/category`；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，DOM/SVG 程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/pinyin-train/index.ts` 逐段自查。已核实与预期的问题：

1. **本款没有任何 draw / render / paint 函数**（全库扫描核实）——整个游戏是纯 DOM 文本按钮排版，「小火车」只存在于标题里，画面上没有火车、没有轨道、没有站台；
2. 音节选项预期是文本按钮裸排（自查确认）；
3. 拼对一个音节的反馈预期只有文本 / 音效变化，没有「车厢挂上去」的过程感；
4. 声母 / 韵母 / 声调的视觉分组（色彩助记）有没有做自查；
5. 进度呈现方式自查，把全部结论记进开工 commit。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **火车舞台（核心）** | 画一列 SVG 小火车：车头（圆头锅炉 + 烟囱 + 排障器 + 大圆灯）+ 车厢若干节（圆角车身 + 双轮 + 挂钩）；底部铁轨双线 + 枕木，向右延伸并透视收窄到远山隧道（2.5D 灭点）；背景远山 + 云两朵 + 站台一角。 |
| **音节车厢化** | 每答对一个音节 = 一节车厢从站台滑入挂上列车（220ms + 挂钩「咔」一顿，reduced 瞬挂）；车厢侧面写该音节（大字 + 声调标色）；车头烟囱冒圈圈白烟（每挂一节喷一朵，reduced 停）。 |
| **声韵调三色助记** | 声母一律暖橙牌、韵母青绿牌、整体认读紫牌、声调符号红色加粗——颜色即语法的助记通道（**正字法与题库不动**，只上色）；选项牌做成车票样式（锯齿边 + 圆孔）。 |
| **答题反馈** | 拼对：车灯亮一下 + 鸣笛音效对应的视觉汽笛圈两圈；拼错：车厢轻晃不脱钩 + 站牌提示「再听一遍」（配 TTS 重读按钮，接线不动）。 |
| **发车仪式** | 一轮拼完全部音节：火车鸣笛发车驶向隧道（600ms 驶出 + 车尾小旗，reduced 淡出）+ 站台挥手小人 + 彩纸；下一轮新列车进站。 |
| **布局与 HUD** | 进度 = 已挂车厢数 / 目标节数（列车本身就是进度条）；朗读按钮做成站台广播喇叭；题目区（听音选拼 / 看图拼音）卡片化置顶。 |

- 色板走 `src/art/kit/palette.ts`；火车部件可**新增** `src/art/kit/train.ts`（车头 / 车厢 / 轨道参数化）并配套单测。
- `prefers-reduced-motion`：滑入、白烟、发车、汽笛圈全停，保留静态列车与三色助记。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`、`outline.ts`、`sparkle.ts`。
- 你需要而 kit 没有的能力（`train.ts` 等）：**新增**文件并配套单测，不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看拼音 / 识字火车类教育页面，**只学两件事：画面密度（站台氛围层）与火车剪影（车头一眼认出）**。
- 禁止抄任何商标与官方角色：**不准像托马斯小火车**（不给车头画人脸五官照搬其造型），回到本库粉彩原创（可以给车头画简单笑眼但造型必须独立）。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：列车横向滚动展示（已挂车厢多时可左右滑看），当前待拼音节车厢始终居中可见；
- 车票选项牌高度 ≥ 44px、拼音字号 ≥ 18px（教育款读音优先）、声调符号清晰不裁切；
- 朗读按钮热区 ≥ 44px。

## 八、独占文件与冲突

只许改 `src/games/pinyin-train/**` 与新增 `src/art/kit/` 文件。不要碰 `clock-house` / `math-farm`。CSS 前缀沿用本款既有前缀；`destroy` 清干净白烟 / 发车计时。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 车厢数 = 已答对音节数（映射断言，数据不变）；
- 车厢侧面文字 = 题库音节原文（含声调符号逐字符断言，正字法钉死）；
- 声 / 韵 / 整体认读三色分类与语法类别一致（抽 6 例断言）；
- 拼对 / 拼错走不同视觉分支且不脱钩；
- TTS 按钮接线换肤后原样（回归断言）；reduced 下动画全停；
- `destroy` 归零；既有题库与判定测试断言一个不许改。

## 十、分级红线与回复

拼错只是「再听一遍」不批评；失败只鼓励；无商标、不像托马斯。完成后回复：你是 1.3 第 24 步 C 档、`pinyin-train`；第三节的现状结论；火车舞台与音节车厢化的实现说明；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
