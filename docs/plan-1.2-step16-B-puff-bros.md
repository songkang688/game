# 1.2 第 16 步 · B 档 —— `puff-bros`「噗噗兄弟」平台跳跃手感

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 16 步（共 29 步）· B 档**：升级已有游戏 `puff-bros`「噗噗兄弟」。
本步是双人平台三连：A `ice-fire-forest`、B `puff-bros`、C `prince-princess`。别人的目录你一个字都不许碰。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「B 档 + 本款升级计划」的 commit**。
- 只在 `game-1.2` 线上干活；收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**

## 二、开工前必须认的基线

- `origin/game-1.1` 收尾后是 **55 款**；本款若落后就先对齐 1.1，续写不推翻。1.1 有冒烟脚本 `scripts/smoke-puff-bros.mjs`，**改前改后各跑一遍**。
- 本款在 1.1 的事实：`category: "party"`、`modes: ["campaign","versus","endless","coop","twoPlayer"]`（五模式）、`levels: 188`；目录里有独立 `arena.ts`（对战场）、`logic.ts` `index.ts` `meta.ts` `guide.ts` + 测试。
- 复用：188 框架、收藏册只读、家长门、`save.recordEndlessBest`。**不引入依赖。**
- 分级红线：题材保持干净可爱，「噗噗」是气泡 / 空气，不是排泄物玩笑；**禁止恶心化表达**。

## 三、现状审查（回复里逐条回答）

1. 跳跃手感常量（重力 / 初速 / 二段跳 / 土狼时间 / 缓冲）在哪？有没有单测？
2. 「噗」的推力机制怎么算的（推自己？推别人？推箱子？），有没有冷却？
3. `arena.ts` 的对战胜负条件是什么？五种模式是否都能玩到结算？
4. 188 关的机关种类有几种？后段是不是只是变长变窄？
5. 掉落 / 出界后怎么处理？
6. 双人 + 触屏是否互不抢占？

## 四、1.2 玩法升级

| 项 | 规格 |
| --- | --- |
| **手感常量化** | 重力、跳跃、二段跳、土狼时间 ~90ms、跳跃缓冲 ~120ms、空中横向控制系数，全部常量 + 单测；帧率无关（30/60fps 位移差 < 2%）。 |
| **推力系统** | 「噗」有三种用途：自我加速（空中一次）、推开对手、推动物件。每种有明确冷却与前摇提示；写纯函数 + 用例。 |
| **关卡机关补齐** | 至少 5 种：气流管、可推箱、脆弱地板（踩两次碎）、弹簧云、传送泡。 |
| **对战场平衡** | `arena.ts` 的出生点与道具刷新镜像对称（写断言）；出界两段式（先打转再出局）。 |
| **无尽「上升气流」** | 不断上升的关卡（掉出屏底结束），高度记 `save.recordEndlessBest("puff-bros", height)`。 |

## 五、模式矩阵

五模式全保留（玩不了就修好或删声明）；闯关 **前 99 关数据不改**。

## 六、2.5D / 3D 决策

**保持 2D 侧视**。允许背景视差。

## 七、视觉 · 建模 · 手感

- 「噗」有可爱的气流环 + `api.play("pop")`；角色被推有形变但不受伤。
- 落地有轻微压扁回弹（8%），`prefers-reduced-motion` 下取消形变保留位移。
- 脆弱地板要有裂纹预警。

## 八、手机 360px

- 左右半屏各一套控件（方向 + 跳 + 噗），热区 ≥ 44px 不重叠。
- 高度 / 比分 / 计时一行显示，字号 ≥ 14px。

## 九、平台接线

- `openCampaignLevel(n)` / `initialLevel` / `?level=` 直开第 N 关；Skip 走 `requestSkip`。
- 无尽成绩 `save.recordEndlessBest`；`meta.platform` 按实测填；`blurb` 与事实对齐。

## 十、独占文件与冲突

只许改 `src/games/puff-bros/**` 与 `scripts/smoke-puff-bros.mjs`。不要碰 `ice-fire-forest` / `prince-princess`。CSS 类名 `pfb-` 前缀；两套键位 `destroy` 时全卸。

## 十一、测试（新增 ≥ 20 个用例）

手感常量、帧率无关、推力三用途与冷却、5 种机关、对战出生点对称、出界两段式、无尽高度、五模式各跑一次结算、`destroy` 归零。冒烟脚本改前改后各一遍。

## 十二、分级红线与回复

题材干净可爱、无恶心化表达、无血无伤无死亡；失败只鼓励；无商标。完成后回复：你是 B 档、`puff-bros`；手感常量；推力系统；对战平衡断言；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
