# 1.3 第 28 步 · C 档 —— 视觉验收三人组 **第 2 轮** · 监督修复员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉、只修视觉**，不重开玩法验收，一行玩法逻辑都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 28 步 · C 档 —— 视觉验收三人组第 2 轮（共 3 轮）的监督修复员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**（读可以，改不行）。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「C 档 · 第 2 轮监督修复员 · 修复计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round2-fixer.md`。

## 二、修复输入与优先级

1. 输入四份材料：本轮 A 档 `docs/qa/1.3-round2-tester.md`（对账表 + 深度走查 + 新问题）、本轮 B 档 `docs/qa/1.3-round2-learner.md`（修订建议 + 一致性评审）、第 1 轮你自己的遗留清单 `docs/qa/1.3-round1-fixer.md`、以及各款 1.3 提示词的规格。
2. 修复顺序铁律：**第 1 轮「修复不彻底 / 修出回归」的问题最优先** → 本轮新发现的阻断（商标、360 新溢出）→ 严重 → B 档一致性评审点名的 5 款统一化 → 一般 → 建议。
3. 每修一处一个 commit（`fix(visual-r2): <款> <问题>`）。

## 三、本轮修复重点（对照第 1 轮的常见死角）

- **修复不彻底的典型**：换了皮但没换干净（局内换了、结算界面还是老样子）；渐变加了但描边 / 落影没跟上规格；reduced 分支忘了给新动画接线——逐款补全；
- **一致性统一化**：按 B 档评审，把最违和 5 款拉回统一规格（色板 token、描边宽度、光照方向、动效时长档位），改动仍限绘制层；
- **性能回压**：A 档深度走查里帧率 < 30fps 的款，粒子数量 / 图层数做减法（保留视觉等级，砍冗余绘制调用）；
- **kit 收敛**：第 1、2 轮各款重复造的轮子（多份雷同的渐变 / 星花实现）**只在自己新增的文件范围内**收敛进 kit 的新文件，import 替换只动自己修的款，别人的 kit 文件仍然不碰。

## 四、修复的红线（与第 1 轮相同，重申）

- **只动绘制层**：绘制函数、CSS、视觉常量、`src/art/kit/` 新增文件。判定、数值、关卡数据、存档 key、玩法测试断言一律不碰；
- 遵守各款 1.3 提示词的既定规格与 reduced 约定，不自创第二套风格；
- 每处修复配套测试钉住修后状态；**用例只增不减**；
- 修不动的降级登记进遗留清单，交第 3 轮终验裁决。

## 五、合并态复验（由你执行）

1. 三人都 push 后：`git fetch origin game-1.2-kk` → rebase → 确认三份报告与全部修复都在；
2. `npm ci` → `npm test` → `npm run build` → `npx vite preview` 冒烟（首页 / `sw.js` / `manifest.webmanifest` 200）；
3. 全绿后把**合并态 SHA、用例总数（对比第 1 轮合并态只增不减）、构建体积（对比第 1 轮，涨幅超 10% 要说明原因）**写进报告，并给「第 2 轮视觉验收结论」。

## 六、报告要求

产出 `docs/qa/1.3-round2-fixer.md`：

- 修复清单：每条「来源 / 位置 / 修法 / 配套用例 / commit SHA」；
- 第 1 轮遗留清单的处理结果（清掉几条、还剩几条）；
- 一致性统一化的改动说明（5 款各自改了什么）；
- 性能回压前后帧率对比；
- 合并态复验数据与第 2 轮结论。

## 七、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；`vite preview` 冒烟全绿；
- 第 1 轮「不彻底 / 回归」问题 100% 处理；本轮阻断级 0 残留；严重级残留 ≤ 3 条且全部登记；
- 合并态 SHA 写进报告。

## 八、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 28 步 C 档第 2 轮监督修复员；修复统计（按来源与严重度）；第 1 轮遗留处理结果；一致性统一化摘要；性能对比数字；合并态 SHA 与 `npm test` / `npm run build` 结果；报告路径；**实际使用的模型 slug**。
