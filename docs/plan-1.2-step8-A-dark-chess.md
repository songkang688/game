# 1.2 第 8 步 · A 档 —— `dark-chess`「翻翻暗棋」

> 短计划：独占新建 `src/games/dark-chess/`。本步另两档是 `hop-pads`、`tap-tiles`。本步是 B 档新游戏接入的最后一步。
> **补象棋名额**：`xiangqi` 已在仓库，本阶段不新建明棋象棋。暗棋/揭棋是同棋子的另一种大众排行榜棋牌。**禁止修改 `src/games/xiangqi/**`。**

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是执行者，禁止再派生云端子代理。只推 `game-1.2`。】

仓库 https://github.com/songkang688/game ，**1.2 第 8 步 · A 档**：新建 `dark-chess`「翻翻暗棋」。假设 1.1 的 55 款已全部做完。

## 分支纪律
基于 origin/game-1.2，计划 commit，只推 game-1.2，禁止 force。不要改 `xiangqi`。

## 新游戏统一约定（对接更新后的 1.1）
- `meta.ts` 纯数据 + 懒加载。**不要改 `src/ui/home.ts`。** 闯关走 `level99.ts` 188 关。存档 `yiduo-yixing.l99.dark-chess`。
- 点选翻子或走子。朵朵 WASD 光标 + F 翻/走 / G 取消；星星方向键 + L/K。360px 8×4 格子够大。
- `destroy` 干净。内置音效。无外部依赖。禁止商标。
- **收藏只读**：不要用 luck 改开局洗子公平性（必须可种子）。暂停可 `openCollection("dark-chess")`。
- 验证 360 / 375 / 1280。不要改 supervisor / step1 / step9+。

## 为什么做这款
`xiangqi`「朵朵星星象棋」已在仓库，本阶段不新建明棋象棋。暗棋是同棋子的另一种大众玩法，用来 **补满 21 个新 id 的棋牌名额**。

## 完整规则（中国象棋暗棋，4×8）
- 棋盘 4 行 8 列，32 子全部盖放随机。
- 红黑各：将1 士2 象2 车2 马2 炮2 兵5（与象棋相同兵种，**文案用「将/士/象/车/马/炮/兵」**，不要商标）。
- 第一手只能翻子；翻出的颜色成为你的阵营，对方为另一色。
- 之后每手：翻一张盖子，**或**走己方已翻开的子。
- 走法：将士象车马炮兵在暗棋里通常 **只走一格正交**（炮吃子除外）。
- **炮**：隔一个（无论盖/翻）吃，走一格不吃。必须单测。
- 吃子大小（常见台湾/大陆暗棋）：
  将 > 士 > 象 > 车 > 马 > 炮 > 兵。
  **兵吃将**，将不能吃兵（本款：**兵可吃将，将不可吃兵**）。
  同级可互吃。
  炮吃不受大小限制（隔子吃任意），但走法必须隔一。
- 盖子被翻开才参战。不能吃未翻开的子（翻是单独动作）。
- 胜：吃光对方将，或对方无子可动且无盖可翻。将死后也可比子力（本款 **将死即胜**）。
- 和：双方连续 20 手未吃未翻。

边界：第一手翻到将也合法。不能走对方的子。炮翻开后才能隔吃。

无尽：连胜 `recordEndlessBest`。

### meta
```
id: "dark-chess"
title: "翻翻暗棋"
emoji: "🀄️"
category: "party"
color: "#F6DFC5"
blurb: "棋子盖着排成四行。先翻到的颜色就是你的，兵可以请将休息，炮要隔一个吃。"
modes: ["campaign", "versus", "endless", "twoPlayer"]
levels: 188
```
端：双端。与 xiangqi 同色系可以，但玩法完全不同。

### 系统表
| 系统 | 函数 |
| --- | --- |
| 开局洗子 | `dealCovered(seed)` |
| 定边 | `firstFlipColor` |
| 大小 | `canCapture(a,b)` 含兵将 |
| 炮 | `cannonCaptures` |
| 一步正交 | `kingLikeStep` |
| 胜负 | `status` |

### 关卡切分（188，8 章）
| # | 章节 | 关数 | 新机制 |
| --- | --- | --- | --- |
| 1 | 翻翻看 | 24 | 只翻，教定边 |
| 2 | 走一步 | 24 | |
| 3 | 大小吃 | 24 | 兵将特例 |
| 4 | 隔山炮 | 24 | |
| 5 | 记牌 | 22 | 盖子信息 |
| 6 | 残局明 | 22 | 全翻残局 |
| 7 | 无翻可走 | 24 | 逼无棋 |
| 8 | 暗棋杯 | 24 | 地狱 AI |

24×4 + 22×2 + 24×2 = 188。

### 前端建模与动画
DOM 棋。盖子是粉彩背面（花/星）。翻开翻转动画。吃子淡出。禁止与 xiangqi 共用逻辑文件（可只读其兵种中文名）。

### AI 档位
菜鸟随机翻；普通吃得了就吃；高手记已翻、留炮；地狱估值子力+将安全。

### 可参考 GitHub（结构 only，禁止运行时依赖）
揭棋/暗棋规则百科。不要改 xiangqi 源码。

### 独占
只许 `src/games/dark-chess/**`，可选 `scripts/smoke-step8-a.mjs`。**禁止修改 `src/games/xiangqi/**`。** 禁止本步 B/C、`home.ts`、collection 源文件、supervisor / step1 / step9+。

### 测试 ≥ 20（硬性 ≥ 15）
定边、兵吃将、将不能吃兵、炮隔一、同级互吃、无盖无棋、188。

### 不要做什么
- 不要做 9×10 明棋象棋。
- 不要把暗棋规则写进 xiangqi。

### 验收 checkbox
- [ ] 4×8 暗棋规则含炮与兵将
- [ ] 未改 xiangqi
- [ ] 188 + 对战 + 无尽；360px 可翻
- [ ] `npm test` `npm run build` 绿；destroy 干净；收藏只读

### 测试命令
```
npm test
npm run build
npx vite preview
```

完成后回复：你是 A、与 xiangqi 的差异说明、用例数、SHA、实际模型 slug（派发指定 `claude-opus-5-thinking-high-fast`）。
