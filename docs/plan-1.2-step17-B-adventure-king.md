# 1.2 第 17 步 · B 档 —— `adventure-king`「冒险小王」探索与地图

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 17 步（共 29 步）· B 档**：升级已有游戏 `adventure-king`「冒险小王」。
本步是冒险探索三连：A `brave-path`、B `adventure-king`、C `alien-seek`。别人的目录你一个字都不许碰。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「B 档 + 本款升级计划」的 commit**。
- 只在 `game-1.2` 线上干活；收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**

## 二、开工前必须认的基线

- `origin/game-1.1` 收尾后是 **55 款**；本款若落后就先对齐 1.1，续写不推翻（1.1 第 9 步 C 做的，分支 `cursor/step9-c-adventure-alien-3899` 有历史）。
- 本款在 1.1 的事实：`category: "action"`、`modes: ["campaign","endless"]`、`levels: 188`；目录 `logic.ts` `levels.ts` `sim.ts` `index.ts` `meta.ts` `guide.ts` + 测试。已有 `sim.ts`，**继续用它做可通关校验**。
- 复用：188 框架、收藏册只读、家长门、`save.recordEndlessBest`。**不引入依赖。**
- **与 `brave-path`（本步 A）的区分**：那款是「战斗 + 成长的地牢式小路」；**本款是「探索 + 收集 + 解谜的地图冒险」**。规格不许互抄，也不许共用文件。

## 三、现状审查（回复里逐条回答）

1. 一关的目标是什么（找到出口？收齐宝物？救出伙伴？）三星怎么算？
2. 地图有多大、要不要滚动？孩子会不会迷路？有没有小地图 / 已探索标记？
3. 有几种互动物件（箱子 / 机关 / 钥匙 / 传送）？
4. 无尽是什么形态？
5. 有没有「走进死路必须重来」的设计（应当避免）？
6. `sim.ts` 覆盖到哪些关？

## 四、1.2 玩法升级

| 项 | 规格 |
| --- | --- |
| **探索反馈** | 已探索区域淡色留痕 + 小地图（可折叠）；发现秘密房有独立音效与「+1 秘密」计数。 |
| **解谜物件补齐** | 至少 6 种：钥匙门、推箱压板、颜色开关、跷跷板、隐藏墙、传送门配对。每种一个纯函数 + 用例。 |
| **收集与图鉴** | 每章一套收集品（贴纸图鉴），全收集给额外星星；图鉴序列化要有往返测试。 |
| **不卡死原则** | 任何关卡在任何状态下都必须可解或可重置：把箱子推死角要给「一键复位本房间」（不扣星，写用例）。 |
| **无尽「无尽古堡」** | 房间模板随机拼接（模板库 ≥ 12），每次生成必须校验连通性与钥匙可达；成绩 `save.recordEndlessBest("adventure-king", rooms)`。 |

## 五、模式矩阵

`campaign` 188 保留（**前 99 关数据不改**）、`endless` 深化；对战不做（探索类同屏对战意义不大，写明理由）。

## 六、2.5D / 3D 决策

**保持 2D 俯视**。允许墙体轻微厚度描边。

## 七、视觉 · 建模 · 手感

- 互动物件都要有「可互动」提示（轻微浮动或描边），孩子不用试错。
- 拾取有小弹跳与 `api.play("coin")`；秘密房揭开有轻光扫过。
- `prefers-reduced-motion`：关抖动与光扫。

## 八、手机 360px

- 点触移动为主，摇杆可选；小地图折叠在角落，展开时半透明不挡路。
- 目标条一行显示，字号 ≥ 14px；背包面板可滚动。

## 九、平台接线

- `openCampaignLevel(n)` / `initialLevel` / `?level=` 直开第 N 关；Skip 走 `requestSkip`。
- 无尽成绩 `save.recordEndlessBest`；`meta.platform` 按实测填；`blurb` 与事实对齐。

## 十、独占文件与冲突

只许改 `src/games/adventure-king/**`。不要碰 `brave-path` / `alien-seek`。CSS 类名 `advk-` 前缀；`destroy` 清 rAF 与监听。

## 十一、测试（新增 ≥ 18 个用例）

6 种物件纯函数、房间复位不扣星、图鉴序列化往返、无尽拼接连通性 + 钥匙可达（随机 1000 次）、`sim.ts` 抽样 188 关可解（含 100 / 145 / 188）、小地图探索标记、`destroy` 归零。

## 十二、分级红线与回复

无血无伤无死亡；失败只鼓励；无商标。完成后回复：你是 B 档、`adventure-king`；解谜物件清单；不卡死怎么保证；无尽生成校验；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
