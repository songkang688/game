# 1.2 第 29 步 · A 档 —— 验收三人组 **第 3 轮** · 测试员（全覆盖终检）

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 29 步（共 29 步）· A 档 —— 验收三人组第 3 轮的测试员。这是 1.2 的最后一步：这一轮结束时，1.2 必须处于「随时可发布」的状态。**
本步三人：A 测试员、B 学习优化员、C 监督修复员。**别人的报告文件你一个字都不许碰。**

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「A 档 · 第 3 轮测试员 · 终检计划」的 commit**。
- 收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**（是否合回 `main` 由用户自己决定。）
- 报告写到 `docs/qa/1.2-round3-tester.md`。
- **开工先读第 1、2 轮全部六份报告**，所有遗留问题本轮必须有最终结论。

## 二、开工前必须认的基线

- 1.2 的盘子：**新游戏 21 款 + 升级 55 款 = 76 款**。起点水位 **142 个测试文件 / 3918 个用例**，**只增不减**。
- 平台共用件：`level99.ts`、`quiz99.ts`、`speech.ts`、`collection.ts`、`parentAuth.ts`、`save.ts`、`level188Contract.ts`。
- 存档 key 前缀 `yiduo-yixing.`；公共 key：`save.v1`、`l99.<id>`、`l99skip.<id>`、`collection.v1`、`fav.v1`、`recent.v1`。

## 三、全量跑

`npm ci` → `npm test` → `npm run build` → `vite preview` 冒烟；**另外验证 PWA 离线可玩与 Service Worker 更新**（装上旧版 → 发新版 → 确认能更新且不卡在旧版、不白屏）。

## 四、覆盖率补全：**76 款一款不漏**

前两轮各走查 26 款，本轮把**没实玩过的所有游戏全部玩一遍**，目标是 **1.2 全部 76 款每一款都至少被走查过一次**。对每款记录：

能否从首页进入 / 能否玩到真实胜负 / 有无对战·无尽·双人且能否结算 / 188 关最后一关能否通 / 退出是否干净。

在报告末尾给一张**完整的 76 行表格**，并注明每款是在第几轮被走查的。**表格行数不等于 76，本轮不算过。**

## 五、回归终检清单（逐条打勾并附证据）

- **存档升级链路**：1.0 老存档 → 1.2、1.1 存档 → 1.2，进度完整（钱包 + 每关星级 + 最好成绩 + 收藏册 + 收藏 / 最近玩过）；
- 存档损坏自愈（每个公共 key 写垃圾 JSON 都不白屏）；隐私模式（`localStorage` 不可用）可玩；
- 家长门 `parentAuth`（basic + high）、跳关记录、清空进度、导出 / 导入；
- **root 管理员门（1.2 新增）**：密码 `kangkang` 能开；开启后可直达第 N 关（1–188）与进未解锁的关；**1 小时自动过期**（用可注入假时钟验）；能手动关闭；连错 3 次锁 120 秒；「联系管理员 18438037080」文案在；**密码不落盘**（`localStorage` / `sessionStorage` / cookie 全翻一遍，只应有 `yiduo-yixing.root.v1`）；root 开着时跳关不再要求做算术题，root 关着时回落到 `requestParentAuth("high")`；
- **手游 / 端游筛选（1.2 新增）**：「全部 / 手游 / 端游」三芯片与分类页签、玩法芯片、搜索自由组合；**76 款的 `platform` 都填准了**（没填的按 `both`），筛出来的款数与实际对得上；
- 攻略侧栏在**全部 76 款**上可用，且**学习类不泄题**；
- 首页筛选 / 搜索 / 收藏 / 最近玩过 / 分类计数与 76 款对得上；
- **模式声明一致性**：全库遍历 `meta.modes`，声明了 `versus` / `endless` / `twoPlayer` / `coop` 的**每一款每一种模式都要真能玩到结算**；声明与实现不符的按「严重」级记；
- **`meta` 与实现一致性**：`levels`、`blurb`、`platform` 逐款核对（1.0 遗留的「99 关」文案是否都改干净了）；
- 四断点 320 / 375×667 / 768 / 1280×800 无溢出；键盘全可达、焦点可见；`prefers-reduced-motion` 全库生效；
- 商标 / 官方角色名 / 低幼词巡检测试通过；
- 主 chunk 体积与首屏耗时在阈值内。

## 六、点名五项的最终复验

第 1 轮验过一次，收官轮再验一次，确认没有在后续修复中被改回去：

- `gomoku`：解局关有解 + 菜鸟到地狱各档强度单调；
- `match-stars`：消除后**看得见下落与补位的动画过程**，不是瞬变；
- `rainbow-run`：2.5D 接住了 1.1 第 6 步的成果；
- `ocean-munch`：无尽可持续、成绩落盘；
- `xiangqi`：多档 AI 单调 + 残局有解。

## 七、报告要求

产出 `docs/qa/1.2-round3-tester.md`：

- 开头是前两轮遗留问题的最终复测结果（逐条：已修 / 无法复现 / 仍存在）；
- 中间是终检清单逐条证据 + 点名五项复验；
- 末尾是 **76 行走查总表**；
- **文件最后必须给出明确的发布结论**：`可发布` / `有条件可发布（列出条件）` / `不可发布（列出阻断项）`。这句话是 1.2 的放行签字，别含糊。

## 八、你的权限边界

**只写报告与补 `*.test.ts`，不改玩法代码。**

## 九、本轮验收标准（1.2 收官线，三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；preview 冒烟与**离线冒烟**全绿。
- 三轮累计的阻断 / 严重 / 一般问题 **全部清零**。
- **1.2 全部 76 款每一款都至少被实玩走查过一次，并有记录。**
- 所有 `meta.modes` 声明的模式都真能玩到结算。
- 三方合并态复验全绿，SHA 写进报告。
- 不改存档 key；无外部运行时依赖；**无任何商业商标或官方角色名**；文案面向约小学六年级、不过于低幼、保持中文粉彩萌系。

## 十、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告、无内购、无抽卡、无联网上报；攻略不泄题。完成后回复：你是 A 档第 3 轮测试员；76 款是否全覆盖（缺哪几款）；前两轮遗留复测结论；终检清单结果；点名五项复验结论；**发布结论那一句原文**；报告路径；`npm test` / `npm run build` 结果与用例总数；提交 SHA；**实际使用的模型 slug**。
