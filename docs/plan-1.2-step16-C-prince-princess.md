# 1.2 第 16 步 · C 档 —— `prince-princess`「王子公主大冒险」关卡阅读性

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：`claude-opus-5-thinking-high-fast`。
请在独立功能分支上进行修改，叫 game-1.2 持续优化。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2`，不要改 `main`，不要 force。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.2 第 16 步（共 29 步）· C 档**：升级已有游戏 `prince-princess`「王子公主大冒险」。
本步是双人平台三连：A `ice-fire-forest`、B `puff-bros`、C `prince-princess`。别人的目录你一个字都不许碰。

## 一、分支纪律

- `git fetch origin game-1.2`，工作分支建立在 `origin/game-1.2` 之上。
- **动代码前先提交一条「C 档 + 本款升级计划」的 commit**。
- 只在 `game-1.2` 线上干活；收尾 fetch → rebase → `npm test` 与 `npm run build` 全绿 → 普通 push。**禁止 force、不改 `main`、不用 `gh` 开 PR。**

## 二、开工前必须认的基线

- `origin/game-1.1` 收尾后是 **55 款**；本款若落后就先对齐 1.1，续写不推翻。
- 本款在 1.1 的事实：`category: "action"`、`modes: ["campaign","coop","twoPlayer","endless"]`、`levels: 188`；目录 `logic.ts` `levels.ts` `index.ts` `meta.ts` `guide.ts` + 测试。
- 复用：188 框架、收藏册只读、家长门、`save.recordEndlessBest`。**不引入依赖。**
- 角色：王子 / 公主用本作原创小人（朵朵、星星等）的换装造型，**不许出现任何童话 IP 的官方角色形象与名字**。

## 三、现状审查（回复里逐条回答）

1. 两位角色能力有区别吗（跳得高 / 能滑翔 / 能推重物）？还是只是换皮？
2. 关卡阅读性：孩子第一次看这一屏，能不能看懂「往哪走、哪里危险、哪里是奖励」？举 3 个反例。
3. 有没有引导性视觉语言（危险统一红棕、可踩统一亮边、奖励统一发光）？
4. `endless` 现在是什么？随机拼段还是循环关卡？
5. 检查点密度如何？失败重来的挫败感大不大？
6. 双人时摄像机与掉队处理？

## 四、1.2 玩法升级

| 项 | 规格 |
| --- | --- |
| **角色差异化** | 两位角色各给 1 个专属能力（例如公主滑翔 2 秒 / 王子推重物），并让部分关卡需要交替使用；单人模式可切换角色。 |
| **视觉语言统一** | 建一张「关卡元素规范表」写进 `guide.ts`：危险 / 可踩 / 可推 / 奖励 / 出口各自的形状 + 颜色 + 描边规则，全 188 关统一。**这是本步的核心交付。** |
| **教学关** | 每章第 1 关是无风险教学关（只教本章新机制），并在 3 秒内给出图形提示（不用长句）。 |
| **检查点** | 每关至少 2 个；死亡回检查点、保留已收集道具。 |
| **无尽「城堡塔」** | 程序化拼段（模板 + 必过窗口校验，随机 2000 段全部可过），高度 / 层数记 `save.recordEndlessBest("prince-princess", n)`。 |

## 五、模式矩阵

四模式全保留（玩不了就修好或删声明）；闯关 **前 99 关数据不改**，只改视觉语言与提示（不改碰撞体）。

## 六、2.5D / 3D 决策

**保持 2D 侧视**，允许 2–3 层背景视差营造城堡纵深。

## 七、视觉 · 建模 · 手感

- 元素规范表落到实处：同一含义在所有章节长得一样。
- 受伤 = 「戴上小护盾闪一下」，掉落 = 「被小云朵托回检查点」，**无死亡描写**。
- 收集品有轻微浮动与吸附半径（磁力可由收藏册加成，只读）。
- `prefers-reduced-motion`：关抖动与闪烁。

## 八、手机 360px

- 左右半屏控件不重叠、热区 ≥ 44px；单人时切换角色按钮固定位置。
- 关卡内提示文字 ≤ 12 字一行，字号 ≥ 14px，不遮角色。

## 九、平台接线

- `openCampaignLevel(n)` / `initialLevel` / `?level=` 直开第 N 关；Skip 走 `requestSkip`。
- 无尽成绩 `save.recordEndlessBest`；`meta.platform` 按实测填；`blurb` 与事实对齐。

## 十、独占文件与冲突

只许改 `src/games/prince-princess/**`。不要碰 `ice-fire-forest` / `puff-bros`。CSS 类名 `pcp-` 前缀；两套键位 `destroy` 时全卸。

## 十一、测试（新增 ≥ 18 个用例）

角色能力纯函数、元素规范表覆盖全部关卡元素（用测试遍历关卡数据断言每种元素都有规范条目）、检查点恢复、教学关无风险（模拟站着不动不会失败）、无尽必过窗口 2000 段、188 抽样可解、`destroy` 归零。

## 十二、分级红线与回复

无血无伤无死亡；失败只鼓励；**不许出现任何童话 IP 的官方角色名与形象**；无商标。完成后回复：你是 C 档、`prince-princess`；元素规范表内容；角色差异化设计；无尽生成方案；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
