# 1.3 第 29 步 · B 档 —— 视觉验收三人组 **第 3 轮（终验）** · 学习优化员

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本轮**只验视觉**，不重开玩法验收，也不改任何玩法代码。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 29 步 · B 档 —— 视觉验收三人组第 3 轮（共 3 轮，终验）的学习优化员**。
本步三人：A 测试员、B 学习优化员、C 监督修复员。三人各写各的报告，**别人的报告文件你一个字都不许碰**。

## 一、分支纪律

- `git fetch origin game-1.2-kk`，工作分支建立在 `origin/game-1.2-kk` 之上。
- **动手前先提交一条「B 档 · 第 3 轮终验学习优化员 · 计划」的 commit**。
- 收尾 fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-*.md`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。**禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**
- 报告写到 `docs/qa/1.3-round3-learner.md`。

## 二、本轮定位：收官沉淀 + 终审把关

前两轮你建立了密度 / 剪影基准并做了横向一致性评审，终验轮做四件事：

1. **采纳率终账**：把两轮全部建议（第 1 轮 TOP10 + 第 2 轮修订清单）做最终对账——采纳 / 部分采纳 / 未采纳各几条，未采纳的逐条写「同意放弃」或「升级为终验条件」（后者交 A 档并入结论）。
2. **风格基线固化**：把三轮验收沉淀出的视觉规范整理成 `docs/qa/1.3-style-baseline.md`——粉彩色板 token 表、描边宽度档位、左上 45° 光照约定、动效三段式与 reduced 退化规则、金币三件套规格、双人剪影差异化手法、2.5D 纵深的三种做法（地面透视 / 多层视差 / 落影缩放）。这是给 1.4 及日常维护用的传家宝，每条规范都要注明出自哪一款的哪个函数（文件 + 函数名），能溯源。
3. **商标终审**：把两轮维护的「题材 → 易踩线对象 → 规避要点」清单做最后一次全库对照，给出「商标风险清零」或逐条残留说明，这一条直接进 A 档终验结论。
4. **首页整体感终评**：39 款升级后的入口卡片放一起看最后一遍，确认第 2 轮点名的最违和 5 款统一化后确实归队；仍违和的点名交 C 档当轮处理。

## 三、学习红线（三轮一致，最后重申）

- 只学**画面密度**与**角色剪影**两件事，只记文字结论；
- 不下载、不截图入库、不描摹任何具体造型；
- 商标与官方角色连「像」都不许像。

## 四、文案与引导终查（你唯一可以动代码的范围）

- 范围仍是 `guide.ts` 攻略、`blurb`、纯文案字符串；
- 终查三件事：两轮文案改动有没有被 rebase 冲掉（丢了补回）；文案里提到的视觉亮点与终态画面是否一致（说有披风就得真有披风）；错别字与语气分级（只鼓励、无恐吓词汇）全库过一遍；
- 每处改动登记进报告。

## 五、报告要求

产出 `docs/qa/1.3-round3-learner.md`：

- 第一节：两轮建议采纳率终账表；
- 第二节：`docs/qa/1.3-style-baseline.md` 的编写说明与要点摘录（正文单独成文件）；
- 第三节：商标终审结论（清零 / 残留逐条）；
- 第四节：首页整体感终评与仍违和款点名（如有）；
- 第五节：文案终查改动清单。

## 六、你的权限边界

你能改的文件限于：`docs/qa/1.3-round3-learner.md`、`docs/qa/1.3-style-baseline.md`、各游戏的**纯文案**字符串、为文案改动补的快照测试。绘制代码一行不许碰；A 档与 C 档的报告一字不许碰。

## 七、本轮验收标准（三人共同）

- `npm test` 全绿且用例只增不减；`npm run build` 全绿；
- 两轮建议 100% 对账完毕；`1.3-style-baseline.md` 成文且每条规范可溯源；
- 商标终审结论明确（清零或逐条残留）；
- 三方合并态复验全绿，合并态 SHA 写进报告（由 C 档执行，你要配合）。

## 八、分级红线与回复

全库分级红线：无血、无伤害、无死亡描写；失败只鼓励；无广告无内购；无任何商标与官方角色。完成后回复：你是 1.3 第 29 步 B 档第 3 轮终验学习优化员；两轮建议采纳率终账；风格基线文档路径与条目数；商标终审结论；首页整体感终评摘要；文案终查改动数；报告路径；`npm test` / `npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
