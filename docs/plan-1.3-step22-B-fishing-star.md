# 1.3 第 22 步 · B 档 —— `fishing-star`「钓鱼小达人」视觉升级

请通过 Task 工具派生一或多个云端子代理执行本任务。
指定模型 slug：claude-fable-5-thinking-xhigh。
请在独立功能分支上进行修改，叫 game-1.2-kk 视觉升级。后续都用这个。不要直接修改主分支。
完成后说明修改了哪些文件、测试结果，以及实际使用的模型 slug

【读到这段话的你，就是被派发的那个云端子代理。上面那句「请通过 Task 派生」是写给派发方看的。你就是执行者，禁止再派生任何云端子代理，必须自己动手把活干完。只推 `game-1.2-kk`，不要改 `main`，不要 force。本步**只改视觉表现**，一行玩法逻辑、一个判定数值、一条关卡数据都不许动。】

仓库 `https://github.com/songkang688/game`，产品「一朵一星」。
这是 **1.3 视觉升级（共 29 步）第 22 步 · B 档**：给 `fishing-star`「钓鱼小达人」做只动皮肤不动骨头的美术精修。
本步三人：A `landlord-cards`、B `fishing-star`、C `poop-hero`。别人的目录你一个字都不许碰。

## 一、任务定位与分支纪律

- `git fetch origin game-1.2-kk`；远端已有就基于它建工作区，没有就 `git fetch origin game-1.2` 后从 `origin/game-1.2` 建出 `game-1.2-kk`。
- **动代码前先提交一条「B 档 · fishing-star 视觉升级计划」的 commit**。
- 收尾：fetch → rebase（保留别人的文件，尤其 `docs/plan-1.3-supervisor.md`、`index.md`、`step1-*` 到 `step13-*`、`.cursor/skills/**`）→ `npm test` 与 `npm run build` 全绿 → 普通 push `origin game-1.2-kk`。
- **禁止 force、不改 `main`、不推 `game-1.2`、不用 `gh` 开 PR。**

## 二、视觉红线：什么能动、什么不能动

**能动的**：
- `src/games/fishing-star/**` 里的绘制函数、CSS、颜色 / 尺寸 / 动画时长常量、新增纯视觉模块；
- `src/art/kit/` 里**新增**你自己的文件（见第五节）；
- 本款的视觉测试（只增不减）。

**不能动的**：
- 鱼的游动数据（`swimmers` 的 `x/speed/depth` 演算，827–829 行）、上钩判定、深度换算 `yOfDepth`、力度 / 时机玩法逻辑；
- 存档 key、`meta.ts` 的 `modes/levels/category`；鱼种表 `s.fish` 的数据字段；
- 别人的游戏目录与 1.3 文档、`.cursor/skills/**`；
- 严禁引入运行时依赖与位图大文件，全部程序化绘制。

## 三、现状审美评测（先开代码再动笔）

打开 `src/games/fishing-star/index.ts`，绘制入口是 `render` / `drawWater` / `drawShore` / `drawSwimmers` / `drawLine` / `drawAim` / `drawBand` / `drawRuler`。已核实的问题：

1. **鱼是 emoji**：`drawSwimmers` 就是 `g.fillText(s.fish.emoji, px, py)` 加 0.55 透明度（822–834 行）——「钓鱼小达人」的鱼群是一排半透明字符，游动只有正弦上下漂，没有转向、没有摆尾、没有深浅水的体感；
2. `drawWater` 水面 / `drawShore` 岸边自查：预期是色带渐变，有没有波光、涟漪、深度光衰要评；
3. `drawLine` 钓线预期是直线（自查确认）：真钓线该有垂坠弧与入水折射断点；浮标有没有、上钩时有没有「点头」提示自查；
4. `drawAim` 瞄准与 `drawBand` / `drawRuler` 力度条刻度是功能件（保住），但观感预期是 debug 风，自查；
5. 钓上来的瞬间（收获展示）画法自查，把结论记进开工 commit。

## 四、极高质量改进方案（绘制规格）

| 项 | 规格 |
| --- | --- |
| **鱼群自绘（核心）** | emoji 替换为参数化矢量鱼：纺锤身 + 尾鳍 + 背鳍 + 圆眼，6–8 个鱼种各自差异（体型胖瘦 / 尾形叉圆 / 花纹条点 / 稀有鱼加金鳞高光）；游动加摆尾两帧（相位随 `speed`）与朝向翻转（沿速度方向）；深水鱼加饱和度衰减与轮廓虚化（读 `depth` 只做映射）。**游动坐标演算一行不动。** |
| **水体质感** | `drawWater` 升级三层：水面波光网（两条正弦高光带缓慢平移）+ 中层光柱两道（斜向白 6%）+ 深层暗蓝渐变；水面与岸交界加浪花白边；随机小气泡上浮（≤ 6 颗，reduced 静止）。 |
| **钓线与浮标** | 钓线改两段：空中段贝塞尔垂坠 + 入水点折射错位 2px + 水下段颜色变淡；入水点涟漪圆环扩散（上钩窗口时涟漪加密——读既有判定窗口只做映射）；浮标自绘（红白双色 + 上钩时点头下沉 3px）。 |
| **收获仪式** | 钓起：鱼跃出水面沿弧线到岸（240ms）+ 水花皇冠 5 瓣 + 星屑；展示卡（鱼名 + 大小）做成木牌样式；稀有鱼加金光描边一闪。 |
| **岸上场景** | `drawShore` 升级：钓鱼小人（Q 版背影：草帽 + 钓竿，拉杆时后仰 8°）、岸边草丛与小桶（钓到的鱼进桶，桶口露鱼尾）；远景山影 + 云两朵视差。 |
| **布局与 HUD** | 力度条 `drawBand` 改成钓竿弯曲量的图形化（映射不变）；`drawRuler` 深度刻度做成木尺质感；分数 / 目标卡片化。 |

- 渐变 / 描边 / 星屑统一走 `src/art/kit/`；光源统一左上 45°。
- `prefers-reduced-motion`：波光、气泡、摆尾、跃出弧线全停（钓起改瞬移展示），保留静态层次。

## 五、共享美术套件 src/art/kit/

- 先看 `src/art/kit/` 是否已有别人落的文件；**已有的只 import 不修改**。
- 套件约定接口（谁先用到谁新增，一个文件只归一个人）：`palette.ts`（粉彩 token 与 `shade`）、`volume.ts`、`outline.ts`、`sparkle.ts`。
- 你需要而 kit 没有的能力（比如 `fishArt.ts` 参数化鱼）：**新增**文件并配套单测，不改别人的文件。

## 六、参考与禁抄

- 可以打开 4399 等小游戏站看钓鱼页面，**只学两件事：画面密度（水体层次与岸上道具）与鱼种剪影（一眼分清普通 / 稀有）**。
- 禁止抄任何商标与官方素材：不准像捕鱼达人 / 动森钓鱼的具体造型；鱼全部原创、被钓起是开心表情（进桶是「搬家做客」语义）。
- 竞品截图不进仓库；报告里引用竞品只写文字结论。

## 七、手机 360px 布局

- 360px 宽实测：**上钩判定窗口与深度换算一个像素都不动**；
- 鱼最小渲染尺寸（`clamp(W/22,13,22)` 沿用）下鱼种仍可辨（低于 15px 省略花纹只留体型差）；
- 力度条与刻度不叠、字号 ≥ 14px；触屏拉杆热区不动。

## 八、独占文件与冲突

只许改 `src/games/fishing-star/**` 与新增 `src/art/kit/` 文件。不要碰 `landlord-cards` / `poop-hero`。CSS 前缀沿用本款既有前缀；`destroy` 清干净涟漪 / 气泡粒子。

## 九、测试（只增不减，新增 ≥ 10 个视觉用例）

- 鱼种矢量两两不同（抽 3 对断言）且不再 `fillText(s.fish.emoji)`（字符串断言）；
- 摆尾相位与 `speed` 映射一测、朝向随速度符号翻转一测；
- 深度 → 饱和度 / 虚化映射一测（读 `depth` 不改）；
- 钓线两段分界点 = 入水点（断言）；
- 涟漪密度映射只读判定窗口（逻辑回归断言）；reduced 下粒子为 0；
- `destroy` 归零；既有判定与演算测试断言一个不许改。

## 十、分级红线与回复

鱼全程开心、无钩伤表达；失败只鼓励；无商标。完成后回复：你是 1.3 第 22 步 B 档、`fishing-star`；第三节的现状结论；鱼群自绘与水体三层的实现说明；新增用例数与 `npm test`、`npm run build` 结果；提交 SHA；**实际使用的模型 slug**。
